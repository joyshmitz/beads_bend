# Port state — BEADS_RUST → Bend 2

<!-- Read FIRST on every resume; rewritten at the END of every session.
     Facts only, each with the command that produced it. No "should",
     no "later". -->

## Where we are

| field | value |
|---|---|
| phase | 3 reference port, at its exit gate, with Phase 4 find-fix rounds running. Done: −1 fit screen; 0 truth pack (2728 goldens, captured with the pinned br 0.6.0; every case added in session 10 floored STABLE); 1 spec (909 clauses, spec-lint 0 findings, every case cited); 2 architecture (every clause homed). The port: 31 modules under `port/core/` and the IO shell `port/main.bend` (custom effects `Sys.exit`, `Sys.cwd`, `Sys.remove`, `Sys.stdin`, `Sys.argv`, `Clock.wall`). **2726 of 2728 cases pass on c-1t, c-8t and js**, one build each of the working tree after `b81b58d` (committed with this table); the two failures on every lane are `prefix_config_both` (DISC-010) and `delete_hard_cascade_json` (DISC-011), whose goldens hold one random draw of the original each and moved at this session's re-captures (the original itself drew differently on the next run). **No OQ is open** (OQ-137 resolved). The parity board is `DEBT` (29 present, 0 partial, 14 excluded). The interpreter lane has not run on this code |
| tier | T3 |
| bend | `bend 2.0.20` (release binary, sha256 `fab9e564c578a0a1…`; run through `scripts/bend-cli.sh`); drift vs the skills' 2.0.16: DRIFT, itemized in PLAN §2b and §8 |
| original | `br 0.6.0`, tag `v0.6.0`, commit `b1cfebe`, release binary sha256 `21b967c1ae68df1a…`, run as `scripts/ws-run.sh --oracle br --no-db ::`. **Since 2026-09-25 10:16 `~/.local/bin/br` is `br 0.7.0` (sha256 `d7dad8ea55840343…`), installed by something outside this port**; session 10 ran every capture, floor and comparison against a byte-identical copy of the pinned 0.6.0 placed first on `PATH` (bead `bb-p8k`, owner) |
| last updated | 2026-09-26 by Claude (Opus 5.5, 1M context), session 10 |
| remote | `origin` = https://github.com/Dicklesworthstone/beads_bend (public); sessions end with `main` pushed. Open work is in the repository's own `.beads/` workspace (prefix `bb`, `br ready --json`); `docs/BACKLOG.md` section H is the bridge plan the beads came from |

Every command below assumes: `cd /data/projects/beads_bend; export BEND_NO_TELEMETRY=1 BEND_CLI=$PWD/scripts/bend-cli.sh LANE_WRAP=$PWD/scripts/ws-run.sh BUN_JSC_forceRAMSize=10737418240`

**Memory on the 30 GB host, shared with other sessions.** `BUN_JSC_forceRAMSize=10737418240` is what makes the gates fit. Measured this session: `PROOF.bend` 11.0–12.8 GB, 316–384 s; the JS build 13.8–16.5 GB, 128–244 s; the C build in two steps, emission `bend main.bend -o <out>/bn.c` 19.0–23.6 GB, 268–302 s, then `clang -std=c11 -O3 <out>/bn.c -lpthread -lm -o <out>/bn` 5.7–6.0 GB, 331–358 s. Announce an emission to the other sessions on the host before starting it. A JS-lane run of the port on a 25 MB store reached 21 GB and was stopped (bead `bb-rvt`).

## Last gate outputs (paste, do not paraphrase)

| gate | command | result | date |
|---|---|---|---|
| c-1t | `./scripts/conform.sh goldens/cases.tsv goldens --lane c-1t --timeout 120 -- scripts/ws-run.sh <out>/bn --threads 1 --gpu off -- ::` (binary: the two-step build of this commit's `port/`, `bend main.bend -o bn.c` then `clang -std=c11 -O3`). Every lane row pastes conform's last line without its `cases` array; on each lane the two failed cases are `prefix_config_both` (DISC-010) and `delete_hard_cascade_json` (DISC-011) | `{"lane": "c-1t", "passed": 2726, "failed": 2, "inconclusive": 0, "stderr_compared": true, "verdict": "FAIL", "oracle_identity_checked": true}` | 2026-09-26 |
| c-8t | the same binary, `--lane c-8t --timeout 120 … --threads 8` | `{"lane": "c-8t", "passed": 2726, "failed": 2, "inconclusive": 0, "stderr_compared": true, "verdict": "FAIL", "oracle_identity_checked": true}` | 2026-09-26 |
| js | `./scripts/conform.sh goldens/cases.tsv goldens --lane js --timeout 120 -- scripts/ws-run.sh python3 $PWD/scripts/js-lane.py <out>/bn.js -- ::` (bundle of the same tree) | `{"lane": "js", "passed": 2726, "failed": 2, "inconclusive": 0, "stderr_compared": true, "verdict": "FAIL", "oracle_identity_checked": true}` | 2026-09-26 |
| interpreter | `scripts/interp-sample.sh` | PARTIAL: not on this code; the last sample ran on `02393f5` (session 8), 43 cases: `{"lane": "interpreter", "passed": 42, "failed": 0, "inconclusive": 1, "stderr_compared": true, "verdict": "INCONCLUSIVE", "oracle_identity_checked": true}` plus `scn_lifecycle` alone PASS; the whole corpus (~2484 × 5 min) never (bead `bb-oxc`) | 2026-09-24 |
| lanes | `LANE_WRAP=$PWD/scripts/ws-run.sh ./scripts/lanes.sh goldens/cases.tsv goldens $PWD/port/main.bend --threads 8 --timeout 120 --interpreter-timeout 600` | BLOCKER: not run. `lanes.sh` builds in one step (22 GB held while clang runs) and runs the interpreter over the corpus; the three fast lanes were run lane by lane instead | 2026-09-26 |
| proofs | `(cd port && $BEND_CLI PROOF.bend)` on this commit's `port/` | `All terms check.` (0 @unsafe; 76 laws, 3 of them quantified over all inputs: `model_dedup_collapses` S2.36, `query_tally_same_key` S4.148, `resolve_stored_head` S4.41, on reflexivity lemmas for `Bool`/`Word`/`U32`/`String` comparison; bend 2.0.20; 11.5 GB peak, 573 s) | 2026-09-26 |
| law coverage | `./scripts/law-coverage.sh` | `{"laws": 76, "proofs": 76, "unproved": "", "ghost_proofs": "", "ghost_cited": "", "uncited": "blocked_relation_table_hierarchy blocked_set_member blocked_set_nonmember blocked_table_lookup blocked_table_lookup_missing fast_is_spec store_load_fast_dangling store_load_fast_duplicate work_blocked_rows_hierarchy work_blocked_rows_table work_figures_table work_lead_subsecond work_ready_table", "duplicate_laws": [], "duplicate_proofs": [], "unsafe": 0, "unsafe_annotations": 0, "verdict": "OK"}` | 2026-09-26 |
| spec | `python3 -B scripts/spec-lint.py docs/EXISTING_BEADS_RUST_STRUCTURE.md goldens/cases.tsv` | PASS: `spec-lint: 909 clauses, 2728 cases, 2728 cases cited, 0 finding(s)` | 2026-09-26 |
| architecture | `python3 -B scripts/arch-lint.py` (the one finding: `LAWS.bend still carries the template's S0.1 / core_fast law`, owner question below) | `{"clauses": 774, "homed": 774, "numeric_rows": 25, "s6_rows": 36, "fast_twins": 2, "missing": [], "findings": 1, "verdict": "FAIL"}` | 2026-09-26 |
| parity | `./scripts/parity-board.sh docs/FEATURE_PARITY.md` | `{"rows": 43, "present": 29, "partial": 0, "missing": 0, "excluded": 14, "na": 0, "no_evidence": 0, "verdict": "DEBT"}` | 2026-09-26 |
| floor | `./scripts/floor.sh <new>.tsv goldens --repeat 3 -- scripts/ws-run.sh --oracle br --no-db ::` with the pinned 0.6.0 first on `PATH`, over the first 487 of the 974 cases added in sessions 9–10 (round 16's 195 re-floored: their first floor ran against 0.7.0) | `{"repeat":3,"stable":487,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` | 2026-09-26 |
| floor (second half) | the same, over the other 487 | `{"repeat":3,"stable":487,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` | 2026-09-26 |
| floor (OQ-137) | the same, over the 76 cases of the `OQ-137` block of `goldens/cases.tsv` | `{"repeat":3,"stable":76,"unstable":[],"inconclusive":[],"oracle_identity_checked":true,"verdict":"STABLE"}` | 2026-09-26 |
| cases | `./scripts/cases-lint.sh goldens/cases.tsv` (the 99 notes: `@env=BEADS_DIR=`/`BEADS_JSONL=` values and `@file` operands that name sandbox paths) | `{"cases": 2728, "errors": 0, "notes": 99, "classes_missing": "", "verdict": "OK"}` | 2026-09-26 |
| pin | `./scripts/pin-check.sh docs/PIN.toml`, run with the pinned 0.6.0 first on `PATH` (with the installed `~/.local/bin/br`, 0.7.0, the version check does not hold: bead `bb-p8k`; since this session `scripts/ws_inner.py` refuses, exit 125, any oracle whose sha256 is not the pinned one) | `{"schema":"p2b.pin-check.v1","sha":"b81b58d","bend":"bend 2.0.20","host":"Linux-x86_64","checks":[{"check":"original_present","status":"GREEN","note":"legacy/BEADS_RUST_v0.6.0"},{"check":"original_commit","status":"YELLOW","note":"no VCS identity; preserve original source hashes with the capture"},{"check":"original_gitignored","status":"GREEN","note":"git check-ignore: legacy/BEADS_RUST_v0.6.0"},{"check":"original_version","status":"GREEN","note":"br 0.6.0"},{"check":"manifest","status":"GREEN","note":"captured: 2026-09-26T17:11:45.736391+00:00"},{"check":"manifest_hashes","status":"GREEN","note":"8184 hashes and capture inputs verified"},{"check":"manifest_command","status":"GREEN","note":"[\"scripts/ws-run.sh\", \"--oracle\", \"br\", \"--no-db\", \"::\"]"},{"check":"manifest_version","status":"YELLOW","note":"MANIFEST version lacks the pinned version string 'br 0.6.0' (the original does not answer --version, or the pin moved)"},{"check":"case_count","status":"GREEN","note":"2728 cases = 2728 goldens = MANIFEST 2728"},{"check":"bend_version","status":"GREEN","note":"bend 2.0.20"},{"check":"lane_interpreter","status":"GREEN","note":"/data/projects/beads_bend/scripts/bend-cli.sh"},{"check":"lane_c-1t","status":"GREEN","note":"Ubuntu clang version 21.1.8 (6ubuntu1)"},{"check":"lane_c-Nt","status":"GREEN","note":"Ubuntu clang version 21.1.8 (6ubuntu1)"},{"check":"lane_js","status":"GREEN","note":"bun 1.4.2"},{"check":"approver","status":"GREEN","note":"Jeffrey Emanuel (repository owner)"}],"verdict":"YELLOW"}` | 2026-09-26 |
| incumbent | `./scripts/incumbent-bench.sh --pin …` | NOT_RUN: Phase 5 has not begun (the parity gate comes first) | |

**The real-store sweep (a probe, not a gate).** `scripts/real-store-sweep.py run <dir> <out>/bn -j 3` runs `count`, `list --json`, `ready --json`, `blocked --json`, `stats --json` and `show <first id> --json` on the original and on the port over snapshots of the host's `.beads/issues.jsonl` stores, each copied in through `scripts/ws-run.sh … :: @store=<path>` (`/dp/beads_rust` excluded); runs 1 and 2 ran the same code from the session's scratch directory before it was committed as this script. Run 1, binary of `d3f9239`: `{"stores": 138, "runs": 828, "differences": 34, "stores_with_difference": 13}`. Run 2, binary of `fd0a945`: `{"stores": 138, "runs": 828, "differences": 16, "stores_with_difference": 5}`; all 16 are DISC-012 (both sides refuse, exit 6, `SYNC_CONFLICT`; only the named record differs), in the 5 stores with several refused records. Run 3, binary of this commit: `{"stores": 138, "runs": 828, "differences": 16, "stores_with_difference": 5}`; every difference is DISC-012 (both sides refuse, exit 6, `SYNC_CONFLICT`, only the named record differs), in the same 5 stores as run 2; the stored-metadata rule refused no real store (none holds a non-JSON `metadata`).

Reading the table. The three fast lanes are the whole corpus on one build each. The sweep is a probe, not goldens: the stores are other projects' trackers and are not committed. Its run 1 found three behaviors no case had captured (round 11 below); each is now a set of captured cases and a repair. What run 2 still reports is DISC-012: with several refused records the original names one at random (for the same store it named `bd-38oh` in run 1 and `bd-8po2` in run 2).

What the port does NOT do, and answers with its own `INTERNAL_ERROR` failure (exit 1) instead of guessing: `--agent-context @path`; an `--agent-context` float beyond 15 significant digits or outside ryu's plain notation, a malformed JSON literal, a repeated key; a stored dependency `metadata` that is JSON the port cannot reprint exactly (none among the 190,307 set `metadata` values of the 138 real stores); `stats --activity` (needs git, PLAN §3); `init` (OQ-005, excluded: SQLite and mkdir); the help text of the levels no case prints.

## What this session added

Session 10 (2026-09-25/26): find-fix rounds 16–22 repaired and captured (round 22's store-path findings excepted: OQ-137).

- **Rounds 16–22**: 1325 new cases in rounds 16–22 (rounds by fresh non-author agents over areas no round had probed, with only the spec and the harness; round 17 by the author), 50 behaviors found; every finding repaired and captured except round 22's `BEADS_JSONL` and `--db`, which join OQ-137 (bead `bb-2q6`). Round 22's repairs: `BD_ACTOR` on the actor ladder and `BEADS_ACTOR` on the comment-author ladder; the `help` subcommand's refusals, `help help` and the `status` alias's help; the counting flag's `...`; a held value's option in the next usage line. Seven round-22 cases that override `USER` were dropped: the sandbox sets `USER` and refuses to override it (exit 125). Chief repairs: the write-time validation of every changed record (the load's validator plus `updated_at` before `created_at`: `update`, `close`, `reopen`, `defer`, `undefer`, `create`, `q`); `--format json`/`csv` for `list` and `search` (CSV with its column list, quoting and formula guard); `--format toon` answers the port's not-ported refusal instead of Plain; `create --deps` typing; label edits (`--set-labels` precedence, cancelling adds and removes, empty labels, `at least one label required`); `--parent` no-ops; priorities read as integers (`-p 01`); the estimate ceiling; `--dry-run --silent`; repeated `--append-notes`; `events_removed` (measured rule; the cause of DISC-011); `show` on a conflict store; `search -p` single values; `external:` titles in `dep list`/`dep tree` and their sibling order; `BEADS_DIR` normalization, name check and `where`'s redirect note; the claim check against the loaded status; `update`'s check order (`--session` before `--agent-context`); `close --suggest-next` order.
- **The oracle replaced**: `~/.local/bin/br` became 0.7.0 on 2026-09-25 10:16. Captures 9–13 and every floor and round ran against a byte-identical copy of the pinned 0.6.0; round-16/17 findings seen only on 0.7.0 were dropped (bead `bb-p8k`).
- **Two C-backend walls met and passed**: the emitter's 255-live-values limit (`an arity over 255`) in `update`'s and `create`'s batch segments (found by running the pinned compiler source with the failing segment's name printed; fixed by not duplicating whole records through reusable parameters); and a C-only divergence in `events_removed` (eager `Bool.pick` over `Bool`; rewritten with match helpers; bead `bb-yx7`).
- **Registers**: OQ-137 (the `BD_DB` overrides of S2.1, open, bead `bb-2q6`); DISC-006 (a proposed, unapproved amendment for `version --short`) and DISC-011 (the measured cause) amended; the spec amended in 40 clauses with every new case cited.
- **Corpus** 1327 → 2652 cases; fixtures 221 → 232.
- **OQ-137 resolved** (bead `bb-2q6`): `BEADS_JSONL` (the store path, its five refusals in the original's order, `where`'s `jsonl:` line), the `--db`/`BD_DB` overrides that stay inside the workspace (`where`'s `database:` line), and the no-workspace refusal under an override (S2.9) are ported; the shell reads and writes the store at the path the core names. A database override whose directory is another directory is a new PLAN §3 excluded row (the port answers its own refusal). Found on the way and repaired: an empty value for a path option (`--db ""`, `create --file ""`, …) is a usage refusal (S1.20); U+212A KELVIN SIGN lower-cases to `k` for id lookup (S4.36). 76 new cases, floored STABLE; corpus 2652 → 2728.
- **The oracle guard** (bead `bb-4xx`): `scripts/ws_inner.py` hashes the `--oracle` program and refuses (exit 125) anything but the pinned br 0.6.0; its re-capture moved no golden but the two random draws (DISC-010, DISC-011).
- **The board re-judged** (bead `bb-0g1`): every in-scope row `present`; verdict `DEBT` (29 present, 14 excluded).

## Open items

| id | what | blocks | owner |
|---|---|---|---|
| DISC-009, DISC-010, DISC-011, DISC-012 | all four OPEN, each with its measured impact (DISC-011 with its measured cause: the original's purge order). DISC-010's and DISC-011's cases fail on every lane whenever their captured draw differs from the port's, as after this session's re-captures | convergence (an OPEN DISC blocks it); two lane failures | **the repository owner: accept, reject, or choose a canonicalizing wrapper** (bead `bb-mjf`) |
| `bb-p8k` | `~/.local/bin/br` is 0.7.0, not the pinned 0.6.0 | every capture that finds `br` on `PATH` | **the repository owner** |
| DISC-006 amendment | `version --short`: the original prints its bare version even with `--json`; a case needs the canonicalizer to reduce a bare version line (proposed in the entry, not approved) | one case | **the repository owner** (bead `bb-mjf`) |
| scaffold law | `LAWS.bend`'s template law `fast_is_spec` (S0.1, `core/scaffold.bend`) binds nothing of this port; arch-lint FAIL and port-lint PL-17 both name it. LAWS.bend is human-owned | arch-lint, port-lint | **the repository owner** (bead `bb-ym9`) |
| rounds | T3 needs the last two rounds clean; rounds 16–22 each found behaviors | convergence | author, non-author |
| `bb-yx7` | the C-only `Bool.pick` divergence, not reduced to a probe | a Bend bug report | author |
| `bb-rvt` | the JS lane on the real 25 MB and 46 MB stores: not re-run since its repairs | the JS half of the large-store claim | author |
| interpreter lane | not run on this code; the whole corpus never | T3 "every lane" | author (bead `bb-oxc`) |

## Find-fix rounds (Phase 4/5)

| round | lens | new genuine findings | fixed | clean | date |
|---|---|---|---|---|---|
| 1 | the spec clauses the corpus does not reach, over the five commands session 3 ported (author round) | 1: S4.341 — a second `delete` of an already-tombstoned id rewrote four fields and the store; the original writes nothing | 1 (`core/remove.bend`) | no | 2026-09-20 |
| 2 | the S9 error table against the port's failures (author round) | 2: S9.22 — the `SELF_DEPENDENCY` context key is `id`, not `issue_id`; S9.11 — the shell's `String.trim_end` ate a trailing SPACE | 2 (`core/edges.bend`; `port/main.bend`) | no | 2026-09-20 |
| 3 | the JSON envelopes and payload rows of the six commands session 3 ported (author round) | 0: six captured envelopes matched the port's spec-derived text byte for byte | 0 | yes | 2026-09-20 |
| 4 | Quiet mode over the mutations (S5.106, S5.107, S9.26, OQ-015) (author round) | 2: `close --quiet` still printed the per-item `Warning: Skipped …` lines; `reopen --quiet` printed nothing where the original prints `✓ Reopened` | 2 (`core/run.bend`) | no | 2026-09-20 |
| 5 | the actor ladders (S4.200, S4.201) (author round) | 0: `actor_flag_dep_add`, `actor_flag_delete` and `actor_flag_label_add` matched on their first run | 0 | yes | 2026-09-20 |
| 6 | the dependency graph beyond depth 0 (author round) | 4: S5.179 precedence; S4.184's sort keys; the original's mixed-pair cycle gap (OQ-129); the DISC register's `Resolution` field shape | 4 | no | 2026-09-21 |
| 7 | `dep list --direction up` and `--direction both` (author round) | 0: ten captured cases matched on their first run | 0 | yes | 2026-09-22 |
| 8 | re-test the last repairs: the forms closed in sessions 5–6 crossed with flags their cases did not combine (author round) | 0: thirteen captured cases (`r8_*`, `scn_r8_*`), floored STABLE, matched on js on their first run | 0 | yes | 2026-09-23 |
| 9 | `--quiet` and the JSON error envelopes over the same closures (author round) | 0: thirteen captured cases (`r9_*`, `scn_r9_*`), floored STABLE, matched on js on their first run | 0 | yes | 2026-09-23 |
| 10 | the query commands over the larger fixtures no query case had used, run by a fresh agent that did not write the port, with only the spec and the harness (non-author) | 0: thirty-five captured cases (`r10_*`), floored STABLE, matched on js on their first run | 0 | yes | 2026-09-23 |
| 11 | real stores: the original and the port on snapshots of 138 `.beads/issues.jsonl` stores found on this host, six read commands each (author round) | 3: the import refuses dependency edges the corpus never had (S2.57, S2.58, OQ-131); `show` of a missing target (S5.151, OQ-132); lead times keep fractions of a second (S4.154, OQ-133) | 3 (`core/store.bend`, `core/show.bend`, `core/work.bend`, `core/model.bend`) | no | 2026-09-24 |
| 12 | the load's import checks, the BLOCKED relation's corners, lead-time edges and `show`'s dependency lines (S2.57–S2.58, S4.151–S4.178, S5.151), run by a fresh agent that did not write the port, with only the spec and the harness (non-author) | 5: in 8 of 35 captured cases (`r12_*`, floored STABLE): a `parent-child` edge to an `external:` target is a third S2.58 refusal; an ephemeral record is dropped before the import checks, so an edge to one dangles (2 cases); an `external:` end's title drops the prefix for `<project>:<id>`; an `external:` end sorts by its edge's `created_at` (OQ-134) | 5 (`core/store.bend`, `core/show.bend`); 35 of 35 on js after the repair | no | 2026-09-24 |
| 13 | long texts on the JS lane (bead `bb-rvt`) and what building their fixture exposed: the import's check of a stored dependency's `metadata`, serde_json's placement and quoting of a refused JSON text (S2.24, S2.27, S2.58, S4.350, S5.115, S5.193) (author round) | 8: in 19 of 27 captured cases: `show` and `search` faulted the JS lane on an 80 KB body (per-character recursion in Base `String.split`/`String.join`, an eager `Bool.pick` in `indent_of`, the folds); a comment body kept only its trailing line feeds stripped, not its trailing blanks; the import refuses a stored `metadata` that is not one JSON value (S2.58, every command but `show`); the refused text is Rust `{:?}`-quoted; the position counts lines; a bad `\u` escape is placed at its fourth byte; a stray character is judged where it stands and stops the lexing; a keyword prefix at the end is `EOF while parsing a value` (OQ-136). Not repaired: a store line's missing field before `trailing characters` (bead `bb-04n`) | 7 (`core/show.bend`, `core/query.bend`, `core/views.bend`, `core/decode.bend`, `core/json.bend`, `core/failure.bend`, `core/store.bend`, `core/edges.bend`, `core/model.bend`); 27 of 27 on js | no | 2026-09-24 |
| 14 | the open questions, closed by running the original (`dep cycles` on hand-written cycle stores, `.beads/redirect`, the similarity threshold, non-UTF-8 arguments, the store line's error order) (author round) | 6: `dep cycles` refused every store with a cycle; its `--include-closed` JSON said `"scope":"all"`; the port ignored `.beads/redirect`; an exact 0.7 similarity was not similar; a non-UTF-8 argument was taken as text (C) or faulted the runtime (JS); a store line named the missing field or trailing characters before a member's own error | 6 (`core/views.bend`, `core/run.bend`, `main.bend`, `core/cli.bend`, `core/json.bend`, `core/decode.bend`, `effs/sys_argv.{c,js}`) | no | 2026-09-25 |
| 15 | multi-id mutations, labels, comments, list/search/count filter combinations and create/q options, by a fresh agent that did not write the port, with only the spec and the harness (non-author) | 5: an id twice in one atomic batch was acted on twice (the original refuses); `update --claim` took over an issue someone holds and claimed a closed one; `undefer` of a closed issue without a gate said `cannot undefer`; `label list` without an issue printed counts; `comments add --author ""` stored an empty author | 5 (`core/status.bend`, `core/update.bend`, `core/run.bend`, `core/views.bend`); 173 of 173 on js after the repair | no | 2026-09-25 |
| 16 | `dep add/list/tree/remove` on the larger fixtures, `show` flag combinations, `stats` and `epic status`, `--quiet`/`--json` over the round-15 repairs, by a fresh agent that did not write the port, with only the spec and the harness (non-author) | 4: in 17 of 195 cases (`r16_*` and repair cases): the claim of a deferred issue; `parent-child` edges in the cycle check; the self-dependency before the tombstone refusal; `show` of empty texts and an empty owner (verified against the pinned 0.6.0 after the oracle was replaced) | 4 (`core/update.bend`, `core/edges.bend`, `core/run.bend`, `core/show.bend`, `core/views.bend`) | no | 2026-09-25 |
| 17 | the write paths against the record validator, `--format`, `--deps` typing, label and parent edits, `close`/`reopen` reasons (author round) | 12: F1–F6 and F8–F13: a clock before `created_at` and empty titles refused on write; `--parent` no-ops; `--set-labels` precedence; cancelling label edits; empty reasons; `--format json`/`csv`; `--deps` unknown prefixes and empty elements; `--status tombstone`; acceptance item 0; the shrink guard's JSON context | 12 (`core/status.bend`, `core/update.bend`, `core/run.bend`, `core/render.bend`, `core/create.bend`, `core/acceptance.bend`) | no | 2026-09-25 |
| 18 | `ready`/`blocked` filters, `count`/`stats` flags, `dep tree`/`epic`, date inputs, global flags, CSV/JSON crossed with filters, by a fresh agent (non-author) | 2: `count --status <unknown>` is not refused; `version --short` wins over `--json` (and `--format toon` printed Plain) | 2 (`core/query.bend`, `core/run.bend`); `version --short` waits on a DISC-006 amendment | no | 2026-09-25 |
| 19 | `create`/`q` options in depth, field-by-field `update`, `delete` variants, `show` over every fixture's odd records, `list` sort keys and limits, error forms, by a fresh agent (non-author) | 10: `-p 01`; the estimate ceiling; `create --external-ref ""`; `create --status tombstone` added `closed_at`; the dry-run's edges; `--dry-run --silent`; repeated `--append-notes`; `events_removed`; `delete --reason ""`; `show` on a conflict store | 10 (`core/query.bend`, `core/status.bend`, `core/store.bend`, `core/create.bend`, `core/run.bend`, `core/update.bend`, `core/remove.bend`) | no | 2026-09-25 |
| 20 | `search` options, `dep add/remove`, labels and comments, `stats`/`epic` on the large fixtures, unusual stores, batch mutations with `-q`/`--json`/`--actor`, by a fresh agent (non-author) | 5: `search -p 0,1` is refused; `external:` titles in `dep list`; `label add -l ""` and `-l ","`; `label rename` to an empty name (and `dep tree`'s sibling order for `external:` ends, found repairing it) | 5 (`core/query.bend`, `core/views.bend`, `core/labels.bend`, `core/update.bend`, `core/run.bend`) | no | 2026-09-25 |
| 21 | workspace discovery and `BEADS_DIR` shapes, refusals under `--json`, multi-flag `update` on closed/deferred/tombstoned records, `close --suggest-next`, dates at boundaries, sort ties, by a fresh agent (non-author) | 10: four `BEADS_DIR` shapes (trailing `/`, relative, not `.beads`, a file) and the redirect wording under `where`; the `BD_DB` override (OQ-137); the claim with `--status`; the tombstone refusal's field; `--set-labels "" --add-label`; `--session` before `--agent-context`; the unblocked order | 9 (`core/run.bend`, `main.bend`, `core/views.bend`, `core/update.bend`); `BD_DB` is OQ-137 | no | 2026-09-26 |
| 22 | help forms, argument-parser edges, environment actors, `BEADS_JSONL`, `--db`, error envelopes of each exit code, by a fresh agent (non-author) | 7: `BD_ACTOR` and `BEADS_ACTOR` actors; `BEADS_JSONL`; `--db` under `--no-db`; the `help` subcommand's refusals and `help help`/`help status`; two usage lines | 5 (`core/run.bend`, `core/cli.bend`, `core/surface.bend`); `BEADS_JSONL` and `--db` joined OQ-137 | no | 2026-09-26 |

Convergence (`./scripts/converge.sh docs/PORT_STATE.md`): `{"tier": "T3", "rounds": 22, "clean": 6, "clean_tail": 0, "last_two_clean": false, "non_author_round": true, "open_oq": [], "open_disc": ["DISC-009", "DISC-010", "DISC-011", "DISC-012"], "verdict": "NOT_CONVERGED", "missing": ["last two rounds not both clean", "open DISC: DISC-009, DISC-010, DISC-011, DISC-012"]}` — rounds 16–22 each found behaviors, so the clean tail restarts (round 23 is next); four DISCs are OPEN, which only the repository owner can accept or reject.

## Next action (one line, executable)

`ROUND_BUNDLE=<bundle of this commit> python3 round_cmp.py <generated>.tsv <out>.jsonl` — round 23 (bead `bb-m7v`): a generated differential run of at least 3000 argv by a non-author agent, bucketed by first difference; each unexplained bucket's representative captured with the pinned 0.6.0 first on `PATH`, cited, and repaired.
