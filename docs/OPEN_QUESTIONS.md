# Open questions — the Bend 2 port of BEADS_RUST

<!-- A spec gap is never resolved by reading the original's source during
     implementation; it is resolved by RUNNING the original on a new case,
     capturing it as a golden, and amending the spec clause. Each entry
     names the case that resolved it. -->

| id | question | spec clause | resolve by (case to add) | resolution | date |
|---|---|---|---|---|---|
| OQ-001 | The original sorts through SQL `ORDER BY created_at` over TEXT. With mixed fraction widths (`…05Z`, `…05.1Z`, `…05.000000001Z`, an offset zone), is the order textual or chronological, and what breaks ties? | S6 (order), S4 (ready/list sort) | `edge_precision_ready`, `edge_precision_sort_created`, `edge_precision_list_json` (fixture `precision`) | RESOLVED: chronological. `edge_precision_ready.out` lists `proj-aae` (12:00:04+02:00 = 10:00:04Z), `proj-aaa` (`…05Z`), `proj-aac` (`…05.000000001Z`), `proj-aab` (`…05.100Z`), `proj-aad` (`…05.1Z`, equal instant: id ascending breaks the tie), then `aaf`, `aag`, `aah`. `--sort created_at` is newest first with the same id-ascending tie-break (`aah aag aaf aab aad aac aaa aae`) | 2026-09-20 |
| OQ-002 | When a mutation rewrites `issues.jsonl`, what happens to records it did not touch: unknown fields, `""` optionals, duplicate or unsorted labels, non-canonical timestamps, absent defaults? | S2 (load), S5 (write-back) | `edge_precision_rewrite` | RESOLVED from `edge_precision_rewrite.out`: every record is rewritten. `unknown_field` is dropped. `"description":""` and `"assignee":""` are dropped, while `"compacted_at_commit":""` is kept. Labels come back sorted and deduplicated (`["alpha","zeta"]`). `…05.1Z` becomes `…05.100Z`; `12:00:04+02:00` becomes `10:00:04Z`. Records without them gain `"source_repo":"."`, `"compaction_level":0`, `"original_size":0`. Which other optionals behave like `description` versus like `compacted_at_commit` is OQ-007 | 2026-09-20 |
| OQ-003 | `br version` prints the original's build metadata (`0.6.0`, commit `b1cfebe…`, rustc). The port cannot truthfully print those bytes. What is the port's `version` contract? | S1 | (case to add: `version_plain`, `version_json`) | RESOLVED: both cases are captured (`br version 0.6.0 (release) (v0.6.0@b1cfebe)` and the seven-member JSON object), and the port's contract is DISC-006 (ACCEPTED): its own name `bn`, its own fields, compared through a shape canonicalizer once `version` is ported | 2026-09-20 |
| OQ-004 | What precision does Bend's `now` effect deliver on each lane (the original prints nanoseconds, AutoSi 0/3/6/9 digits)? | S7, S8 | read the host functions of the pinned Bend: `~/.bend/bend2/effs/now.c`, `now.js`, and `io_tick` in `toolchain/bend-v2.0.20-src/bend2/comp.ts:5383` | RESOLVED, and worse than a precision gap: Bend 2.0.20 has NO wall clock. `IO.now() -> IO(Nat)` is `io_tick() / 1000000` with `io_tick` = `clock_gettime(CLOCK_MONOTONIC)` on the C lane (milliseconds since boot) and `BigInt(Math.floor(performance.now()))` on the JS lane (milliseconds since process start); `rg 'CLOCK_REALTIME\|gettimeofday\|Date\.now'` over `comp.ts` and `effs/` finds only the audio effects. Consequence for Phase 2: the shell needs a custom effect pair (`.c` with `CLOCK_REALTIME`, `.js` with `Date`/`performance.timeOrigin`), returning seconds and nanoseconds separately because epoch nanoseconds (≈1.8e18) exceed `Nat`'s 2^48−1; its precision on the JS lane and whether custom effects load on the interpreter lane are the probe that follows (OQ-009) | 2026-09-20 |
| OQ-005 | What does `br --no-db init` do (the default `init` creates `beads.db`, which the port cannot)? Which files, which messages, and what when `.beads/` exists? | S1, S8 | (case to add: `init_fresh` on `@fx=none`, `init_existing`) — needs ws-run to dump every file under `.beads/`, not only the store | EXCLUDED (external-dependency, effect), measured 2026-09-24 with `@ls`: `br --no-db init` prints `Prefix set to: proj` and `Initialized beads workspace in ./.beads` (JSON: `{"initialized":true,"beads_dir":…,"files":{…"created"…}}`), exit 0, on a fresh directory (`@fx=none`), an empty `.beads/` and an existing store alike (the store is kept); it writes `beads.db` (323,584 bytes), its WAL, SHM, lock and migration sidecars, `config.yaml`, `metadata.json`, `.gitignore` and an empty `issues.jsonl`. The port has no SQLite and Bend 2.0.20 no mkdir, so `init` stays excluded (PLAN §3 row `init` creating `beads.db`; board row excluded); no case is captured for an excluded command | 2026-09-24 |
| OQ-006 | Id length grows with the issue count (birthday bound; 3 characters up to a threshold near 163 issues, then 4, …). At which exact counts does the length step, in `--no-db` mode? | S4 (id generation), S7 | (case to add: `edge_idlen_163`, `edge_idlen_164` on generated fixtures `n163`, `n164`) | RESOLVED for the first two steps from `edge_idlen_163`, `edge_idlen_164`, `edge_idlen_983` and `edge_idlen_984` (fixtures `n163`, `n164`, `n983`, `n984`, built by the original): 3 up to 163 records, 4 from 164 to 983, 5 from 984, exactly as S4.9 computes; the bands from 5898 up stay computed (a fixture that large costs O(n^2) oracle steps to build) | 2026-09-22 |
| OQ-007 | For each optional string field, does an empty string survive a rewrite (as `compacted_at_commit` does) or vanish (as `description` and `assignee` do)? | S2, S5 | (case to add: `edge_empty_optionals_rewrite` on a fixture with every optional set to `""`) | RESOLVED by extractor S2+S5a running the original (clause S5.9): `compacted_at_commit` is the ONLY optional string that survives a rewrite as `""`; every other one is read back through an accessor that maps `""` to absent, and `external_ref` loses it earlier, to a trim. The all-optionals fixture case stays a (case to add) under S5.9 | 2026-09-20 |
| OQ-008 | The sidecars: when exactly is `.beads/last-touched` written and what are its bytes (trailing newline?), and do read-only commands ever create files? | S8 | `create_min`, `update_status`, `scn_last_touched` already capture the dump; (case to add: a read-only case under a ws-run mode that lists `.beads/`) | RESOLVED from `ls_read_list`, `ls_read_show`, `ls_nostore_list` (2026-09-24, harness mode `@ls`): a read-only run creates no file in `.beads/`; `last-touched` holds the id and LF (the dumps of `create_min`, `update_status`). The sidecars of a MUTATING run are OQ-084 | 2026-09-24 |
| OQ-009 | Does a custom effect (a `def` whose body imports `./x.c` and `./x.js`) load on all three engines of the pinned Bend (interpreter, C, JS), and what sub-millisecond precision does the JS side deliver? | S8 | `port/probes/clock/` (`main.bend`, `clock_wall.c`, `clock_wall.js`): `Clock.wall() -> IO(String)` answering `<epoch seconds>.<nine digits>` | RESOLVED 2026-09-20 under bend 2.0.20: it loads on all three. interpreter `1789884581.715000000`, C `1789884582.378291177`, JS `1789884582.638000000` (`date +%s.%N` said `1789884582.646053126`). C is nanosecond (`CLOCK_REALTIME`); interpreter and JS are millisecond (`Date.now()`), so an unpinned timestamp prints 9 fraction digits on the C lane and 3 on the JS lanes: a DISC (class `Platform`) when the clock lands. Two drift facts seen in the same run: (1) a program that reaches a foreign def gets the verdict `All terms check, but 2 defs rely on unsafe or foreign code:` followed by one `- <def>` line each (`- Clock.wall`, `- main`), on every engine, so the pure core and the effectful shell live in separate files and `PROOF.bend` imports only the core; (2) that note is now several lines, while `scripts/interp-lane.sh` was written for 2.0.16's one-line note: to be verified on the first real lane run in Phase 3 | 2026-09-20 |
| OQ-010 | What are the exact bytes and exit code of `br version` and `br version --json` for the port, given that the commit, rust version and target cannot be truthfully reproduced? (OQ-003, still open) | S9.33 | `version_plain`, `version_json` (listed in cases.tsv; capture them, then a DISC of class Platform states the port's fields) | RESOLVED by DISC-006 (ACCEPTED): `version_plain` and `version_json` hold the shape the canonicalizer keeps (`<name> version <version> <details>`, `{"version": <string>}`), and the port prints its own name and fields | 2026-09-23 |
| OQ-011 | What happens when `.beads/` exists but `issues.jsonl` does not? Is it an empty store (exit 0) or `NOT_INITIALIZED`/an I/O error? | S9.30, S9.31 | `edge_no_jsonl_list` (a fixture selector that creates `.beads/` with no `issues.jsonl`) | RESOLVED from `edge_nostore_list`, `edge_nostore_count_json`, `edge_nostore_where`, `edge_nostore_create` (2026-09-24, fixture selector `@fx=nostore`): a `.beads/` directory without `issues.jsonl` is an empty store — `list` prints nothing, `count --json` `{"count":0}`, exit 0 — and `create` writes the first record | 2026-09-24 |
| OQ-012 | Does an argument that is not valid UTF-8 abort inside the argument reader, and with what bytes and exit code (probe RS-20)? | S1.2 | `usage_non_utf8_arg` — argv `["\xff"]` | RESOLVED from the 9 `usage_non_utf8_*` cases (2026-09-24): no abort; an invalid VALUE is the parser's `invalid UTF-8 was detected in one or more arguments` refusal at its level, exit 2; an invalid NAME keeps its U+FFFD text (S1.2 amended). The port answered wrongly on both lanes before (C: U+FFFD taken as text; JS: the runtime faulted on the lone surrogate), repaired by the `Sys.argv` effect | 2026-09-24 |
| OQ-013 | Is the `CLOSE_INCOMPLETE` context really `{"closed","skipped","summary"}`, and in that key order? Only the human rendering was measured. | S9.14 | `error_close_partial_json` — `["@fx=basic","close","proj-mta","proj-mkh","--json"]` | RESOLVED from `close_transition_comment_batch` (a `--json` close with one id closed and one skipped): the context is `{"closed","skipped","reason"}` in that order, `reason` holding the summary; not `summary`. The port had `summary` and was fixed with this capture (S9.14 amended) | 2026-09-22 |
| OQ-014 | Does a clap parse failure with `BR_OUTPUT_FORMAT=json` set still print plain text on stderr, as it does with `--json`? | S1.30, S9.4 | `usage_limit_bad_env_json` (needs a ws-run mode that sets one environment variable) | RESOLVED from `usage_limit_bad_env_json` (2026-09-24, harness mode `@env`): a clap refusal stays plain text on stderr, exit 2, with `BR_OUTPUT_FORMAT=json` set | 2026-09-24 |
| OQ-015 | Does `--quiet` suppress the per-item `Warning: Skipped …` lines of S9.26, or only the command's stdout? | S1.8, S9.26 | `close_blocked_quiet` — `["@fx=basic","close","proj-170","--quiet"]` | RESOLVED by running the original (lint-fix pass, 2026-09-20): `--quiet` DOES suppress the per-item `Warning:` lines (`src/output/context.rs:1278`); clause S9.26 amended. The proposed case stays a (case to add) so the bytes are pinned by a golden | 2026-09-20 |
| OQ-016 | What are the exact bytes when more than five conflict markers are present (the preview is capped at five)? | S9.23 | `error_conflict_markers_many` (a fixture with six or more markers) | RESOLVED from `error_conflict_markers_many`: the report lists the FIRST FIVE markers only (six in the file), with no line about the rest (S9.23 amended) | 2026-09-23 |
| OQ-017 | Does `--actor ""` fall through to the configuration and `USER`, or is the empty string taken as the actor? | S1.64 | `actor_flag_empty` — `["create","X","--actor","","--json"]` | RESOLVED from `actor_flag_empty`: `--actor ""` falls through; the record says `"created_by":"tester"` (the sandbox `USER`) | 2026-09-23 |
| OQ-018 | OQ-007 is answered by S5.9: `compacted_at_commit` is the only optional string that survives a rewrite as `""`. Confirm against the original rather than against the accessor table | S5.9, S2.38 | `edge_empty_optionals_rewrite` (a fixture with every optional string set to `""`) | RESOLVED from `edge_empty_optionals_*` and `edge_empty_source_repo`: NOT as S5.9 said. `""` in `agent_context`, `bypass_reason`, `source_repo` or `source_repo_path` makes every command but `show` refuse the load with SYNC_CONFLICT, exit 6 (new clause S2.57); `""` in the other optional strings loads and vanishes on write-back as S5.9 says (`edge_precision_rewrite`) | 2026-09-23 |
| OQ-019 | Does a UTF-8 BOM refuse the load, and with which line number and column? | S2.25 | `error_jsonl_bom` (fixture `bom`) | RESOLVED from `error_jsonl_bom`, `error_jsonl_bom_json`: as S2.25 says, `Invalid JSON at line 1: expected value at line 1 column 1`, CONFIG_ERROR, exit 7; the column is the BOM's first byte | 2026-09-23 |
| OQ-020 | Does a 6-fraction-digit instant exist in the timestamp ladder, i.e. is a microsecond written with exactly six digits? | S5.12 | `edge_precision_micros` (a fixture line with `…05.000001Z`) | RESOLVED from `edge_precision_micros`, `edge_precision_micros_rewrite`: a fraction is written with 3, 6 or 9 digits, the fewest that hold it: `.000001Z` and `.123456Z` kept, `.000000001Z` kept, `.1Z` and `.100000000Z` both become `.100Z`, `.120Z` kept | 2026-09-23 |
| OQ-021 | Does a second line with the same `id` raise the duplicate message on the import path as well as on the `show` fast path, and with which line number? | S2.40 | `error_jsonl_duplicate_id` | RESOLVED from `error_jsonl_duplicate_id`, `error_jsonl_duplicate_id_show`, `error_load_empty_optionals_dup`: `Configuration error: Duplicate issue id 'proj-aaa' in /mnt/proj/.beads/issues.jsonl at line 3` (the second occurrence), exit 7, on `list`, `count` and `show` alike (S2.40 amended) | 2026-09-23 |
| OQ-022 | Is a custom `status` really written back lower-cased, does `inprogress` normalize to `in_progress`, and does `done` really fold to `closed` and gain a `closed_at`? | S2.31, S2.36 | `edge_status_spellings` (a fixture with `InProgress`, `inprogress`, `QaReview`, `done`) | RESOLVED from `edge_status_spellings`, `edge_status_spellings_rewrite`: `InProgress` and `inprogress` load as `in_progress`, `QaReview` as `qareview`, `done` as `closed` with `closed_at` set to `2026-01-01T10:00:00Z` (the fixture's `created_at` and `updated_at` are equal, so which one it copies is not separated), and the write-back keeps those spellings | 2026-09-23 |
| OQ-023 | Does `BEADS_DIR` bypass the walk, and does a `redirect` file relocate the workspace? Both need a ws-run mode that plants a sidecar or adds one environment variable | S2.4, S2.5 | `env_beads_dir`, `redirect_basic` | RESOLVED (2026-09-24): the `BEADS_DIR` half from `env_beads_dir_where`, `env_beads_dir_list` and `env_beads_dir_missing`; the `redirect` half from 15 cases (`redirect_*`, fixture sidecars `<name>.redirect`): S2.5 amended (normalized target, trimmed, blank ignored, missing target and oversize file refused, exit 7) | 2026-09-24 |
| OQ-024 | Does a configured `issue_prefix` that sanitizes to empty abort rather than fall back to `br`? | S2.14 | `error_bad_issue_prefix` (a `config.yaml` fixture sidecar) | RESOLVED from `error_create_empty_prefix`: an `issue_prefix` of `!!!` is refused: `Configuration error: issue_prefix "!!!" contains no usable ASCII letters, digits, or separators`, exit 7; no fall-back to `br` | 2026-09-23 |
| OQ-025 | Does a record missing `title` produce serde's `missing field` wording inside the `Invalid JSON at line <n>` frame, and does a record that parses but fails validation produce the `Validation failed for issue … at line …` frame? | S2.28, S2.30 | `error_jsonl_missing_title`, `error_jsonl_bad_id_format` | RESOLVED from `error_jsonl_missing_title`, `error_jsonl_bad_id_format` and the 24 `error_load_*` cases: a missing `title` is `Invalid JSON at line 2: missing field `title` at line 1 column 138`; a record that breaks a rule is `Configuration error: Validation failed for issue <id> at line <n>: <messages>`, CONFIG_ERROR, exit 7 (not 4), every message of the record joined by `, ` in the order of S2.43–S2.54 as measured (S2.30 amended) | 2026-09-23 |
| OQ-026 | Does an imported dependency with no `created_by` really come back as `"created_by":"import"`? | S5.14 | `edge_sparse_dependency_rewrite` (a fixture whose dependency carries only the first four members) | RESOLVED from `edge_sparse_dependency_rewrite`: yes, `"created_by":"import"`, with `"metadata":"{}"` and `"thread_id":""` | 2026-09-23 |
| OQ-027 | At which exact issue counts does the id hash length step from 3 to 4 to 5? (OQ-006, still open; S4.9 states the computed table) | S4.9 | `edge_idlen_163` — `["@fx=n163","create","Boundary","--json"]`; `edge_idlen_164` — `["@fx=n164","create","Boundary","--json"]` (generated fixtures `n163`, `n164`) | RESOLVED from `edge_idlen_163`, `edge_idlen_164`, `edge_idlen_983`, `edge_idlen_984` (OQ-006): 3 characters up to 163 records, 4 from 164 to 983, 5 from 984 | 2026-09-23 |
| OQ-028 | Does the count that chooses the hash length include tombstones in `--no-db`? | S4.10 | `edge_idlen_tombstones` — a fixture of 163 live records plus one tombstone, then `create` | RESOLVED from `edge_idlen_tombstones`: yes: 163 live records and one tombstone make 164, and the new id has four characters (`proj-lff7`) | 2026-09-23 |
| OQ-029 | Does a tombstoned id block a colliding id candidate? | S4.13 | `edge_id_collides_with_tombstone` — a fixture whose tombstone id equals the nonce-0 candidate for a known title/instant | RESOLVED from `edge_id_collides_with_tombstone`: yes: with a tombstone holding `proj-u5m` (the nonce-0 candidate of `create X`), `create X` takes the next candidate, `proj-fhn` | 2026-09-23 |
| OQ-030 | After deleting a child, is its number reused by the next child? | S4.25 | `edge_child_after_delete` — `@scn` deleting `proj-uly.1` then creating another child of `proj-uly` | RESOLVED from `scn_child_after_delete`: after `delete proj-uly.2` the next child is `proj-uly.3`; the tombstoned child keeps its number | 2026-09-23 |
| OQ-031 | What exactly does `--slug` produce, and what does it do when the slug normalizes to empty or exceeds the prefix budget? | S4.19–S4.23 | `create_slug` — `["create","Slugged","--slug","My Thing!","--json"]`; `create_slug_unicode` — `["create","S","--slug","  Survey — my  thing  ","--json"]`; `create_slug_empty` — `["create","S","--slug","!!!","--json"]` | RESOLVED from `create_slug`, `create_slug_empty`, `create_slug_long`: `My Thing!` gives `proj-my-thing-89a` (lowercased, runs of other characters to one `-`, trimmed, and a 3-character hash after it); a slug that normalizes to empty gives the plain id `proj-u5m`; a long slug is cut to 48 characters (`abcdefghij-abcdefghij-abcdefghij-abcdefghij-abcd`) before `-u5m` | 2026-09-23 |
| OQ-032 | What are the bytes for a wrong-prefix partial id and for an uppercase full id? | S4.36, S4.43 | `show_uppercase_id` — `["@fx=basic","show","PROJ-MTA"]`; `error_show_wrong_prefix` — `["@fx=basic","show","other-mta"]` | RESOLVED from `show_uppercase_id` and `error_show_wrong_prefix`: `PROJ-MTA` resolves to `proj-mta`; `xx-mta` is `Issue not found: xx-mta`, exit 3, with the `br list` hint | 2026-09-23 |
| OQ-033 | What is the message for a blank id argument? | S4.40 | `error_show_blank_id` — `["@fx=basic","show","  "]` | RESOLVED from `error_show_blank_id`: `Error: Invalid issue ID format: ` (the id printed empty), exit 3 | 2026-09-23 |
| OQ-034 | Which RFC 3339 spellings does `--until` accept, and what does `tomorrow` produce under the pinned clock? | S4.57, S4.60 | `defer_until_rfc3339` — `["@fx=basic","defer","proj-mta","--until","2027-06-01T12:00:00+02:00","--json"]`; `defer_until_tomorrow` — `["@fx=basic","defer","proj-mta","--until","tomorrow","--json"]` | RESOLVED from `defer_until_rfc3339`, `defer_until_tomorrow`: `2027-06-01T12:00:00+02:00` is kept as `2027-06-01T10:00:00+00:00`; `tomorrow` at the pinned instant 2026-01-02 03:04:05 is `2026-01-03T09:00:00+00:00` | 2026-09-23 |
| OQ-035 | What is the field name and message for a bad `create --due`? | S4.62 | `error_create_bad_due` — `["create","X","--due","not-a-date"]` | RESOLVED from `error_create_bad_due`: `Validation failed: date: invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)`, exit 4; the field is named `date`, not `due` | 2026-09-23 |
| OQ-036 | What are the messages for out-of-band estimates? | S4.69 | `error_create_negative_estimate` — `["create","X","-e","-1"]`; `error_create_huge_estimate` — `["create","X","-e","525961"]` | RESOLVED from `error_create_negative_estimate`, `error_create_huge_estimate`: `-1` is refused by the parser as `unexpected argument '-1' found`, exit 2; `99999999999` is refused as `invalid value '99999999999' for '--estimate <ESTIMATE>': 99999999999 is not in -2147483648..=2147483647`, exit 2 | 2026-09-23 |
| OQ-037 | What do priority ranges, comma lists and a backwards range print? | S4.67 | `list_priority_range` — `["@fx=basic","list","-p","0-1","--json"]`; `list_priority_list` — `["@fx=basic","list","-p","0,3","--json"]`; `error_list_priority_backwards` — `["@fx=basic","list","-p","3-1"]` | RESOLVED from `list_priority_range`, `list_priority_list`, `list_priority_backwards`: `0-1` selects priorities 0 and 1, `0,2` selects 0 and 2, and `3-1` is `Priority must be 0-4, got: 3-1` with the hint `Ranges run low to high: use 1-3.`, exit 4 | 2026-09-23 |
| OQ-038 | What does a non-numeric priority report? (the uppercasing oddity of S4.66) | S4.66, S10 | `error_create_priority_word` — `["create","X","-p","high"]` | RESOLVED from `error_create_priority_word`: `Priority must be 0-4, got: HIGH` (upper-cased) with the hint `Did you mean --priority 1?`, exit 4 | 2026-09-23 |
| OQ-039 | Does `--status all` include tombstones in `list`, and in what position do they sort? | S4.104 | `list_status_all` — `["@fx=basic","list","--status","all","--json"]` (needs a fixture with a tombstone: `basic_tomb`) | RESOLVED from `scn_list_status_all`: `--status all` does NOT list tombstones; the deleted `proj-b75` is absent from both the JSON and the plain form, and the rest sort as without the flag | 2026-09-23 |
| OQ-040 | Is a custom status carried by a stored issue accepted by `--status`, and is it matched lowercased? | S4.106 | `list_status_custom` — `["@fx=custom_status","list","--status","Triage","--json"]` | RESOLVED from `list_status_custom`, `list_status_custom_lower`: a stored `Triage` loads as `triage`, and `--status Triage` and `--status triage` both select it; plain mode shows it with the `?` glyph | 2026-09-23 |
| OQ-041 | Does `:parent-blocked` really reach every descendant, and does a grandchild of a blocked ancestor drop out of `ready`? | S4.159 | `blocked_parent_propagation` — a `hierarchy` fixture with `epic → child → grandchild` plus a `blocks` edge on the epic; run `blocked --json` and `ready` | RESOLVED from `scn_parent_blocked`: with the epic `proj-uly` blocked by `proj-5u2`, the grandchild `proj-170.1` is not ready and `blocked` lists it as `Blocked by 1 open dependencies: [proj-170]` (its parent, itself blocked) | 2026-09-23 |
| OQ-042 | What does a real cycle look like — the witness node order, the numbering, and the exit code? | S4.187, S4.188 | `dep_cycles_found` / `dep_cycles_found_json` on a `cycles` fixture with `a —blocks→ b —blocks→ c —blocks→ a` | RESOLVED from `dep_cycles_found`, `dep_cycles_found_json`, `dep_cycles_dfs_path` and `dep_cycles_dfs_backtrack_json` (2026-09-24): the witness starts at the component's lowest id and follows a depth-first walk over sorted neighbours back to it; numbered from 1 in text; exit 5 (S4.187 amended) | 2026-09-24 |
| OQ-043 | How does `dep tree` render a diamond (shared node) and a cycle: which occurrence carries `repeat`, which carries `truncated`? | S4.185 | `dep_tree_shared` and `dep_tree_cycle` on a `diamond` / `cycles` fixture | RESOLVED from `scn_dep_tree_shapes`: in a diamond the first occurrence of the shared node is expanded, and the second prints with the `(truncated) ` prefix; in JSON the second carries `"truncated":true,"repeat":true` | 2026-09-23 |
| OQ-044 | Does `--max-depth` cut where S4.185 says, and does the cut node carry `truncated` true? | S4.185 | `dep_tree_max_depth` — `["@fx=hierarchy","dep","tree","<root>","--max-depth","1","--json"]` | RESOLVED from `dep_tree_max_depth`: at `--max-depth 2` the nodes at depth 2 print with the `(truncated) ` prefix and go no deeper | 2026-09-23 |
| OQ-045 | Does an `external:` blocking edge really block its source with no external project configured, and what does `blocked` print for it? | S4.166 | `blocked_external` on a fixture carrying `proj-mta —blocks→ external:other-1` | RESOLVED from `scn_external_blocker`: yes; `blocked` lists `proj-mta` as `Blocked by 1 open dependencies: [external:other-1]`, and `show` prints the edge as `  -> external:other-1 (blocks) - ⏳ external:other-1` | 2026-09-23 |
| OQ-046 | What is the third empty-`ready` message, verbatim, on a store where every open issue is blocked? | S4.178 | `ready_none_actionable` on a fixture where the only open issue has an open `blocks` prerequisite | RESOLVED from `scn_ready_all_blocked`: `✨ No ready issues — remaining work is not currently actionable`; `ready --json` prints `[]` | 2026-09-23 |
| OQ-047 | Are templates really invisible to `list`/`ready`/`count`, and does `count --include-templates` bring them back? | S4.103 | `list_template_hidden` and `count_include_templates` on a fixture with `"is_template":true` | RESOLVED from `list_template_hidden`, `ready_template_hidden`, `count_templates`, `count_include_templates`: a template is absent from `list` and `ready` and from `count` (1), and `count --include-templates` counts it (2) | 2026-09-23 |
| OQ-048 | Do `--desc-contains`/`--notes-contains` fold non-ASCII case (Unicode lowercasing) while `--title-contains` does not? | S4.129, S4.109 | `list_desc_contains_unicode` — `["@fx=basic","list","--desc-contains","LINE ONE"]` and `["@fx=basic","list","--title-contains","ünï"]` | RESOLVED from `list_desc_contains_nonascii`, `list_title_contains_nonascii`, `search_nonascii`: `--desc-contains école` finds `ÉCOLE` (Unicode lower-casing); `--title-contains ärger` and `search ärger` do not find `Ärger` (ASCII-only) | 2026-09-23 |
| OQ-049 | Which sibling key decides first in `dep tree` — priority or status rank? | S4.184 | `dep_tree_siblings` on a fixture where one node has two dependencies differing in priority and in status | RESOLVED from `scn_dep_tree_shapes`: priority first (`P0`, `P1`, then the two `P2`), and among equal priorities the open node before the closed one | 2026-09-23 |
| OQ-050 | Does `epic status` list a *deferred* epic (include_deferred true) and hide a closed one? | S4.189 | `epic_status_deferred` on a fixture with a deferred epic and a closed epic | RESOLVED from `scn_epic_status_deferred`: a deferred epic is listed (`○ proj-wax Deferred epic`, `Progress: 0/0 children closed (0%)`), a closed epic is not | 2026-09-23 |
| OQ-051 | Does `create -f/--file` bulk import write `last-touched` with the last created id, and what are the exact plain and JSON output shapes and the per-issue defaults? | S4.237 | `create_from_file` (a markdown file staged next to the fixture; needs a ws-run mode that can place extra input files) | RESOLVED from `create_file_md`, `create_file_md_json`, `create_file_md_edges`, `create_file_md_rules`, `create_file_md_one` (2026-09-24, harness mode `@file`; floored STABLE): `last-touched` gets the last created id; plain output `✓ Created <n> issues from <path>:` and `  <id>: <title>` lines, a refused heading `✗ Failed to create <title>: <reason>` on stderr, exit 0; `--json` a compact array; the rules are S4.237's amendment. `### Parent` and `### Dependencies` are measured by probe but not captured (the port keeps its own refusal for them, bead bb-y4p) | 2026-09-24 |
| OQ-052 | What exactly does `delete --from-file` accept, and does the sorted-id rule change which id is reported first? | S4.340 | `delete_from_file` (same staging need) | RESOLVED from `delete_from_file`, `delete_from_file_json`, `error_delete_from_file_*` and `stdin_delete_from_file_dash`: one id per trimmed line, empty and `#` lines skipped, the file ids join the positionals before the ascending sort; `-` is a file named `-` (S4.340, S4.397) | 2026-09-23 |
| OQ-053 | Does `epic close-eligible` write `last-touched`, and does an eligible epic really receive `close_reason` `All children completed` rather than `done`? | S4.391 | `scn_epic_close_eligible` — a scenario on `@fx=basic`: `["close","proj-170"]`, `["epic","close-eligible","--json"]`, `["epic","status"]` | RESOLVED from `scn_epic_close_eligible`: the epic close writes the store and NOT `.beads/last-touched` (the scenario dumps last-touched after its two `close` steps only), and the epic carries `close_reason` `All children completed` (S4.391) | 2026-09-23 |
| OQ-054 | Does `label add <issue> <id-shaped-label>` really treat the last positional as a label, and what happens when a label is id-shaped and `-l` is absent? | S4.370 | `label_add_id_shaped` — `["@fx=basic","label","add","proj-mta","needs-review"]` | RESOLVED from `label_add_id_shaped`: `label add proj-mta needs-review` adds the label `needs-review` (`✓ Added label needs-review to proj-mta`) | 2026-09-23 |
| OQ-055 | Does `comments add -f <file>` keep a trailing newline in the stored `text`? | S4.381 | `comments_add_file` (needs a fixture-side file) | RESOLVED from `comments_add_file`: the text is kept untrimmed, trailing newline included (`"text":"proj-mta\n"`, S4.397) | 2026-09-23 |
| OQ-056 | Is `close`'s `Partially applied` exit code 3 stable for every mix of skip kinds, and does the store always hold the successful closures? | S4.294 | `close_partial_skip` — `["@fx=basic","close","proj-mkh","proj-mta"]` | RESOLVED from `close_partial_skip`: `✓ Closed proj-mta` on stdout, `Warning: Skipped proj-mkh: already closed` and `Error: Partially applied: 1 closed, 1 skipped — proj-mkh: already closed` on stderr, exit 3, and the store holds the closure | 2026-09-23 |
| OQ-057 | Does `update --status <same non-terminal value>` really rewrite the store on every status, or only on some? | S4.262 | `update_status_same` — `["@fx=basic","update","proj-mta","--status","open"]` | RESOLVED from `update_status_same`: `update --status open` on an open issue prints `Updated proj-mta: …` and rewrites the store with a new `updated_at` | 2026-09-23 |
| OQ-058 | What does `--agent-context` store byte-for-byte after JSON re-serialization (key order, number formatting), and what is the verbatim refusal for invalid inline JSON? | S4.232 | `create_agent_context`, `error_create_agent_context` | RESOLVED from `create_agent_context`, `create_agent_context_scalar`, `error_create_agent_context`, `error_create_agent_context_json`: compact serde_json with keys in input order and serde_json number spellings (`1.50` → `1.5`, `-0` → `-0.0`, `1e3` → `1000.0`); the refusal is `CONFIG_ERROR`, exit 7, `inline argument is not valid JSON: key must be a string at line 1 column 2` plus the `@path` hint (S4.232 amended) | 2026-09-23 |
| OQ-059 | What is the exact normalization of `--slug` at the 48-character cap and for a slug that normalizes to the empty string? | S4.233 | `create_slug`, `create_slug_empty` — `["create","X","--slug","!!!","--json"]` | RESOLVED with OQ-031 from `create_slug_long` and `create_slug_empty`: the cap is 48 characters of the normalized slug; an empty slug falls back to the plain hash id | 2026-09-23 |
| OQ-060 | Does `delete --hard` with no ids purge tombstones in a store that has some, and what is the resulting byte content? | S4.339 | `delete_hard_all` on a fixture containing a tombstone | RESOLVED from `scn_delete_purge`: with tombstones present `delete --hard` purges them all, prints `✓ Purged <n> tombstone(s)` and their ids ascending, and rewrites the store without them (S4.339) | 2026-09-23 |
| OQ-061 | Which comment id does a store with NO comments assign, and does the sequence survive a rewrite that renumbers nothing? | S4.382 | `comments_add_first` — `["@fx=empty"…]` is not possible (no issue); a scenario `create` then `comments add` | RESOLVED from `scn_comments_first`: the first comment in a store with none is `"id":1`, the next `"id":2` | 2026-09-23 |
| OQ-062 | Does `update --parent <id>` replace an existing parent edge or add a second one when the single-parent rule is bypassed by the update path? | S4.270 | `update_reparent` — `["@fx=basic","update","proj-170","--parent","proj-b75","--json"]` | RESOLVED from `update_reparent`: `--parent proj-b75` replaces the `parent-child` edge to `proj-uly`; the record keeps one parent edge | 2026-09-23 |
| OQ-063 | Is a long title truncated in plain mode when stdout is a pipe? (source says no; no case proves it) | S5.114 | `list_long_title` — `["@fx=longtitle","list"]` on a fixture with a 300-column title | RESOLVED from `scn_long_texts`: a 306-character title is printed whole by plain `list` on a pipe (a 332-character line) | 2026-09-23 |
| OQ-064 | At what width does `br show` wrap a free-text body when `COLUMNS` is unset? (source says 100) | S5.115 | `show_wrap_100` — `["@fx=longdesc","show","proj-aaa"]` | RESOLVED from `scn_long_texts`: with `COLUMNS` unset, `show` wraps a body at 100 columns (lines of 99 characters from `Word ` repeated) | 2026-09-23 |
| OQ-065 | What are the exact bytes of `dep tree` at depth ≥ 1, and of the `(truncated)` / `(shown above)` prefixes? | S5.179 | `dep_tree_children` — `["@scn=child_ids"]` extended with `["dep","tree","proj-uly","--direction","up"]` | RESOLVED from `scn_dep_tree_shapes`: each depth indents two more spaces before `├── `; nodes at `--max-depth` that have more below carry the `(truncated) ` prefix | 2026-09-23 |
| OQ-066 | What does a plain `close --suggest-next` print, and is the `Unblocked` block preceded by exactly one blank line? | S5.225 | `close_suggest_next_plain` — `["@fx=basic","close","proj-mta","--suggest-next"]` | RESOLVED from `close_suggest_next_plain`: the `✓ Closed` line, one blank line, `Unblocked 1 issue(s):`, then `  proj-170: Implement user auth` | 2026-09-23 |
| OQ-067 | Does a mixed batch (one closed, one skipped) print the `✓` line on stdout and still exit non-zero? | S5.224 | `close_mixed` — `["@fx=basic","close","proj-170","proj-mta"]` | RESOLVED from `close_mixed`: `proj-170` is blocked by `proj-mta`, and closing both in one batch closes both, exit 0: a blocker in the same batch does not refuse the close; the closed-plus-skipped mix is OQ-056 (exit 3) | 2026-09-23 |
| OQ-068 | Does `br reopen --quiet` really print, and does `br create --silent --quiet` print the id? | S5.108 | `reopen_quiet`, `create_silent_quiet` | RESOLVED from `reopen_quiet` and `create_silent_quiet`: `reopen --quiet` still prints `✓ Reopened …`; `create --silent --quiet` prints the id | 2026-09-23 |
| OQ-069 | What are the bytes of a quiet mutation (stdout, stderr, and the store dump)? | S5.107 | `update_quiet` — `["@fx=basic","update","proj-mta","-p","0","--quiet"]` | RESOLVED from `update_quiet`: nothing on stdout or stderr before the store dump; the store is written | 2026-09-23 |
| OQ-070 | Which stream carries the `list` truncation note and which of the two wordings applies to a plain `--limit`? | S5.124 | `list_limit_plain` — `["@fx=basic","list","--limit","2"]` | RESOLVED from `list_limit_plain`: the note is on stderr, `[note] Output truncated to 2 issues. Use --limit 0 for all results.` | 2026-09-23 |
| OQ-071 | Does the `search` truncation note really land on stdout, and what is the hidden-closed note's exact text? | S5.159, S5.160 | `search_limit`, `search_hidden_closed` | RESOLVED from `search_limit`, `search_hidden_closed`: the hidden-closed note is on stdout, `note: 1 closed match(es) hidden; rerun with --all to include them`, after the rows, and the truncation note follows it on stdout: `note: showing 1 search result(s) from offset 0; more matches exist. Use --limit 0 for all results (current limit: 1)` | 2026-09-23 |
| OQ-072 | What is the plain form of `defer --until` — RFC 3339 with `+00:00`, or a date? | S5.230 | `defer_until_plain` — `["@fx=basic","defer","proj-mta","--until","2027-06-01"]` | RESOLVED from `defer_until_plain`: `⏱ Deferred proj-mta: Set up database schema (until 2027-06-01T09:00:00+00:00)`: RFC 3339 with `+00:00`; a bare date is 09:00 | 2026-09-23 |
| OQ-073 | What are the plain forms of `undefer`, `label rename`, `update --title`, `update` no-op and `label list` on an unlabeled issue? | S5.232, S5.189, S5.216, S5.218, S5.186 | `undefer_plain`, `label_rename_plain`, `update_title_plain`, `update_noop_plain`, `label_list_empty` | RESOLVED from the five cases it names: `✓ Undeferred proj-7vm: Refactor config loader (now open)`, `✓ Renamed label 'db' to 'database' on 1 issue`, `Updated proj-mta: New title`, `No updates specified for proj-mta`, `No labels for proj-ptp.` | 2026-09-23 |
| OQ-074 | What does `dep cycles` print when a cycle exists (stdout rows vs the stderr warning)? | S5.182 | `dep_cycles_found` — `["@fx=cycle","dep","cycles"]` on a fixture containing a cycle | RESOLVED from `dep_cycles_found` and `dep_cycles_real` (2026-09-24): the numbered lines go to stdout and the `Warning: Found N dependency cycle(s):` line to stderr (S5.182 amended) | 2026-09-24 |
| OQ-075 | What does `ready` print when filters exclude everything, versus when everything is blocked (three different `✨` lines)? | S5.133 | `ready_filtered_empty`, `ready_all_blocked_empty` | RESOLVED from `ready_filtered_empty` and `scn_ready_all_blocked`: filters that exclude everything print `✨ No ready issues match the requested filters or configured ready status group`; a store whose work is all blocked prints OQ-046's line | 2026-09-23 |
| OQ-076 | What is the countdown suffix on a plain `show` of a deferred issue under the pinned clock? | S5.148 | `show_deferred_plain` — `["@fx=basic","show","proj-7vm"]` | RESOLVED from `show_deferred_plain`: `Deferred until: 2027-06-01 (gate opens in over 515 days)` | 2026-09-23 |
| OQ-077 | Does `blocked --limit` really print a header count larger than the row count? | S5.138 | `blocked_limit` — `["@fx=basic","blocked","--limit","1"]` | RESOLVED from `blocked_limit`: yes, the header says `🚫 Blocked issues (2):` over one row, and stderr says `[note] Showing 1 of 2 blocked issues. Use --limit 0 for all results.` | 2026-09-23 |
| OQ-078 | `stats --json` prints `average_lead_time_hours` as an f64 in shortest round-trip form. The corpus reaches only the whole value `2.0`. What are the bytes when the mean of the whole-hour lead times is not an integer (e.g. 7/3), and does the plain form print the same digits? | S7.20, NUMERIC_PLAN §3 | (case to add: `stats_lead_time_fraction_json`, `stats_lead_time_fraction` on a fixture `leadtime` built through the oracle: three issues closed after 1 h, 2 h and 4 h) | RESOLVED from `scn_lead_time_half`, `scn_lead_time_third`: `"average_lead_time_hours":1.5` and `1.3333333333333333` (binary64 shortest round trip); plain `Avg Lead Time: 1.5 hours` and `1.3 hours` (one decimal below a day) | 2026-09-23 |
| OQ-079 | Does the JSONL export order use raw byte order for ids containing `.` (child ids sort `proj-uly` before `proj-uly.1` before `proj-ulz`)? | S6.2 | `edge_child_id_export_order` — a fixture holding `proj-uly`, `proj-uly.1`, `proj-uly.10`, `proj-uly.2`, `proj-ulz`, then any mutation | RESOLVED from `scn_child_export_order`: raw byte order, `proj-uly`, `proj-uly.1`, `proj-uly.10`, `proj-uly.11`, `proj-uly.2` … `proj-uly.9` | 2026-09-23 |
| OQ-080 | Do `dep list --json` and `dep list` really disagree, and by which key? | S6.25, S10.35 | `edge_dep_list_json_vs_text` — a fixture with three same-priority dependencies whose `created_at` order differs from their id order, run both ways | RESOLVED from `scn_dep_list_order`: both forms list `proj-5u2.1`, `.2`, `.3`: id order at equal priority, not the order the edges were added (`.3` first) | 2026-09-23 |
| OQ-081 | What order does `dep cycles` give when two cycles share a member, and what does `--include-closed` change? | S6.29 | `edge_dep_cycles_two` and `edge_dep_cycles_include_closed` | RESOLVED from `edge_dep_cycles_two`, `edge_dep_cycles_two_json`, `dep_cycles_shared_json`, `edge_dep_cycles_include_closed` and `dep_cycles_closed_only_include` (2026-09-24): two cycles through one node are one component and one witness; witnesses ascend by their lowest id; `--include-closed` adds the archived ones and the two kind arrays (S4.187, S4.188, S6.29 amended) | 2026-09-24 |
| OQ-082 | Does `blocked` order by blocker count before or after `created_at` when priorities are equal? | S6.15 | `edge_blocked_count_tiebreak` — two P4 issues blocked by two and one open issues | RESOLVED from `scn_blocked_count_first` with `scn_blocked_tiebreak`: at equal priority the issue with more blockers lists first whether it was created earlier or later: the blocker count decides before `created_at` | 2026-09-23 |
| OQ-083 | Is the `--sort title` fold ASCII-only for every command, including `search` with an offset (which takes a different code path)? | S6.10, S6.17 | `edge_title_fold_ascii_only` — a fixture with `İstanbul match`, `jackal match`, `hotel match`, `ZULU match`, `apple match`, run as `list --sort title`, `search match --sort title` and `list --sort title --offset 1` | RESOLVED from `scn_title_fold`: ASCII-only fold in `list --sort title` and in `search --sort title` with and without `--offset`: `apple`, `hotel`, `jackal`, `ZULU`, then `İstanbul` | 2026-09-23 |
| OQ-084 | Exactly which files does a mutating run leave in `.beads/`, and does a read-only run ever create one? | S8.27, S8.28, S8.29 | a ws-run mode that lists `.beads/` recursively after the step, for both a read-only and a mutating argv (extends OQ-008) | RESOLVED (2026-09-24): a read-only run creates nothing (`ls_read_list`, `ls_read_show`, `ls_nostore_list`); a mutating run of the original leaves `.br_history/issues.<stamp>.jsonl` with its `.meta.json`, `.write.lock`, `.br-db-write-<hash>.lock` and `.br-jsonl-write-<hash>.lock` beside `issues.jsonl` and `last-touched` (probe `@ls @fx=basic create X`, 2026-09-24). The port writes only the two files: DISC-003 and DISC-004 (ACCEPTED), so no golden lists a mutating run's directory | 2026-09-24 |
| OQ-085 | What does `BEADS_DIR` do to `where`'s output, and why does the `prefix:` line disappear? | S8.2, S10.49 | `env_beads_dir_where` — needs a ws-run mode that sets one extra environment variable | RESOLVED from `env_beads_dir_where` (2026-09-24): with `BEADS_DIR=/mnt/proj/.beads`, run from a subdirectory, `where` prints the same four lines as from the root, `prefix:` included; the question's premise came from a run outside the sandbox | 2026-09-24 |
| OQ-086 | What does `BR_OUTPUT_FORMAT=json` change for a command that already has a `--json` flag, and for one that does not? | S8.7 | `env_output_format_json` on `count` and on `where` | RESOLVED from `env_output_format_count`, `env_output_format_where`, `env_output_format_error` (2026-09-24): `BR_OUTPUT_FORMAT=json` gives exactly the `--json` bytes (`where_json`, `error_show_missing_json`), on commands with and without their own `--json`; S1.6 and S1.61 stated it and the port now follows (`Run.json_mode`) | 2026-09-24 |
| OQ-087 | Does `BR_DISABLE_PARALLEL_JSONL_EXPORT` change the exported bytes on a large store? | S8.10 | `env_serial_export` on a generated 1000-issue fixture | RESOLVED from `env_serial_export` (2026-09-24): `BR_DISABLE_PARALLEL_JSONL_EXPORT=1` changes nothing: the same `update` on fixture `large` without the variable printed the same bytes (sha256 `7542d7ca…` both) | 2026-09-24 |
| OQ-088 | Which per-field rule decides whether an empty optional survives a rewrite? | S10.1 | `edge_empty_optionals_rewrite` (already OQ-007) | RESOLVED with OQ-018: the rule is per member, S2.57 (four members refuse the import) and S5.9 (the rest drop `""`) | 2026-09-23 |
| OQ-089 | Does `close`'s `closed_at` differ from `updated_at` under a clock the harness can move between the two readings? | S10.45, S8.31 | none is capturable through the pinned harness; recorded as a clock-seam note for DISC-001 | RESOLVED (2026-09-24) by a second extraction pass over the tag snapshot: `close` reads `closed_at` (`close.rs:1135`) and `updated_at` (`sqlite.rs:7813`) separately, so under a moving clock they differ; not capturable under the pinned clock; the port's single reading is DISC-009 (amended) | 2026-09-24 |
| OQ-090 | Is the average lead time truncated per issue before averaging, and in which direction? | S6.34, S10.41 | `edge_stats_lead_time_truncation` — three closed issues with lead times of 1 s, 79 h 13 m 11 s and 35 h 22 m 33 s | RESOLVED from `scn_lead_times`: each lead time is truncated to whole hours before averaging: 1 s, 79 h 13 m 11 s and 35 h 22 m 33 s give `"average_lead_time_hours":38.0` ((0 + 79 + 35) / 3) and `Avg Lead Time: 1.6 days` | 2026-09-23 |
| OQ-091 | Does a plain-mode closed pipe kill the process with status `141` while a `--json` run exits `0`? | S8.37 | `edge_sigpipe_plain` — needs a harness mode that closes stdout after the first line | EXCLUDED (platform), not measured (2026-09-24): what a write to a closed stdout pipe does is the host process's signal disposition, not the program's logic, and it differs between the port's own engines (the C runtime takes SIGPIPE's default action, bun ignores SIGPIPE and reports EPIPE), so no single answer could hold on every lane; the harness captures the whole output before writing and has no closed-pipe mode. A byte-exact claim covers a fully read stdout only | 2026-09-24 |
| OQ-092 | In `--json` mode the original writes the error envelope to stdout, NOTHING to stderr, and exits non-zero (S9.1–S9.5). Bend's only exit path, `IO.die(A, code, msg)`, always writes `msg` to stderr. What does `IO.die(Unit, 3, "")` leave on stderr, and can a custom effect exit silently? | S8.34–S8.36, S9.1–S9.7 | probe `port/probes/exit/` (`main.bend`, `sys_exit.c`, `sys_exit.js`) on all three engines | RESOLVED 2026-09-20 under bend 2.0.20: `IO.die(Unit, 3, "")` → stdout `{"error":1}\n`, stderr `\n` (one stray empty line), exit 3, identically on interpreter, C and JS. A custom `Sys.exit(code)` (C: `fflush` both streams, `exit`; JS: `process.exit`) → the same stdout, EMPTY stderr, exit 3, identically on all three. The shell therefore writes stdout with `IO.write`, stderr with `IO.print_err`/a write, and leaves through `Sys.exit`; `IO.die` is not used | 2026-09-20 |
| OQ-093 | Clause contradiction: S4.313 lists `issue not found` as a `reopen` SKIP reason with exit 0, while S9.9, S4.212 and S5.251 make it an error with exit 3. | S4.313, S9.9, S4.212, S5.251 | `error_reopen_missing` (golden: exit 3 — the reviewer predicted 3 and matched) | RESOLVED from `error_reopen_missing`: exit 3, `Issue not found`: an error before the batch, not a skip (S4.313 amended) | 2026-09-23 |
| OQ-094 | Clause contradiction: S2.56 gives a VALIDATION wording for a missing `dep add` target; S4.353 says target resolution fails first with exit 3. S2.56 does not mark its wording as unreachable on that path. | S2.56, S4.353 | `error_dep_add_missing` (golden matched the S4.353 reading) | RESOLVED from `error_dep_add_missing`: exit 3, target resolution fails first; S2.56's wording is not reached on that path (S2.56 amended) | 2026-09-23 |
| OQ-095 | `show --json`: the timestamp spelling (`Z` versus `+00:00`) is not stated in S5.30, and that the file's `source_repo`, `source_repo_path`, `original_size` are echoed is only implied. | S5.30 | `show_json` (the reviewer guessed `Z` and matched) | RESOLVED from `show_json`: timestamps are spelled with `Z`, and `source_repo`, `source_repo_path`, `compaction_level`, `original_size` are echoed as stored | 2026-09-23 |
| OQ-096 | Clause contradiction: S4.195 and S5.30 disagree on whether `show --json` labels are sorted (S6.38 and S10 say they are NOT normalized). | S4.195, S5.30, S6.38 | `edge_precision_show_json` | RESOLVED from `edge_precision_show_json`: `show --json` prints the labels as the file holds them, unsorted and with duplicates (`["zeta","alpha","alpha"]`); S4.195 amended | 2026-09-23 |
| OQ-097 | S5.194 (`comments list` on an issue without comments) and S5.179 (`dep tree`) do not say whether a header line precedes the output. | S5.194, S5.179 | `comments_list_empty`, `dep_tree` (both matched on a guess) | RESOLVED from `comments_list_empty` and `dep_tree`: no header line (`No comments for proj-mta.`; the tree starts with its root line); S5.194 and S5.179 amended | 2026-09-23 |
| OQ-098 | Clause contradiction: S1.12 and S5.25 disagree on a repeated `--json`. | S1.12, S5.25 | `usage_json_twice` (golden: exit 2, `error: the argument '--json' cannot be used multiple times`) | RESOLVED from `usage_json_twice`: the parser refuses a repeated `--json`, `error: the argument '--json' cannot be used multiple times`, exit 2 (S1.12 holds; S5.25 amended) | 2026-09-23 |
| OQ-099 | Which RFC 3339 spellings does the loader accept beyond the captured ones: a space instead of `T`, more than nine fraction digits, a leap second `:60`, year `0000`? `port/core/time.bend` accepts `T`/`t`, `Z`/`z`, one to nine fraction digits, seconds up to 59, years from 0001, and refuses the rest as a verdict. | S4.57, S2.24–S2.42 | (case to add: `edge_time_spellings_list_json` on a fixture `timespell` with one record per spelling; a refused record shows how the loader reports it) | RESOLVED from `edge_time_spellings`, `edge_time_spellings_ok`: a space in place of `T`, a lower-case `t` and `z`, and an offset (`+02:00`, converted to UTC) are all accepted and written as `…T10:00:00Z` | 2026-09-23 |
| OQ-100 | Clause contradiction: S4.52 says a mutating command reads the wall clock ONCE per attempt and uses that one instant for every timestamp it writes; S8.31 says one invocation observes several instants (a fresh reading for `updated_at`, another for each edge or comment). Under the pinned clock both predict the same bytes. | S4.52, S8.31 | not capturable under a frozen clock: needs an advancing fake clock (`FAKETIME=@…`), which makes ids nondeterministic; decide by reading the pinned source in a second extraction pass, and state the port's single reading as a DISC if S8.31 holds | RESOLVED (2026-09-24) by a second extraction pass over the tag snapshot: S8.31 holds — one invocation takes several readings (per id in `update`, edge and issue apart in `dep add`, comment in SQL at second resolution); the port's single reading per run is DISC-009 (amended with every reading's `file:line`), as this entry foresaw | 2026-09-24 |
| OQ-101 | What does the original print when `.beads/issues.jsonl` is not valid UTF-8 (a lone 0xFF byte; an overlong form; an encoded surrogate)? The port refuses with its OWN words (`Configuration error: .beads/issues.jsonl is not valid UTF-8`, exit 7) and its decoder does not yet refuse overlong forms, surrogates or scalars past U+10FFFF. | S2.24, S9 | (case to add: `error_store_not_utf8` on a fixture `notutf8` holding the byte 0xFF; the fixture tooling writes text today, so it needs a binary fixture path) | RESOLVED from `error_store_not_utf8`, `error_store_not_utf8_json` (a lone 0xFF): `I/O error: stream did not contain valid UTF-8`, IO_ERROR, exit 8, `context` null. The port's decoder accepting overlong forms and surrogates is a port gap, not an open question: the words are Rust's for any invalid UTF-8 | 2026-09-23 |
| OQ-102 | serde_json's messages for STRUCTURAL errors and wrong value shapes. The corpus pins one (`EOF while parsing a string at line 1 column 40`). The port emits serde's wording for `expected value`, `expected `:``, `expected `,` or `}``, `key must be a string`, `trailing comma`, `trailing characters`, `EOF while parsing an object`/`a value`, `invalid escape`, `control character (\u0000-\u001F) found while parsing a string`, `lone leading surrogate in hex escape`, but the columns and the exact texts are unverified; for a wrong shape (`"priority":"high"`, a missing `id`) it emits its own short words (`invalid type: expected an integer`, `missing field `id``) where serde says more (`invalid type: string "high", expected i32 at line 1 column N`). | S2.27, S2.28 | (cases to add: one fixture per message, e.g. `error_json_trailing_comma`, `error_json_wrong_type_priority`, `error_json_missing_id`, `error_json_lone_surrogate`; each `["@fx=<name>","list"]`) | RESOLVED from the 13 `error_jsonl_<structure>` and 20 `error_jsonl_type_*` cases: the structural messages and their columns (`trailing comma`, `expected `:``, `expected `,` or `}``, `key must be a string`, `expected ident`, `invalid escape`, `control character ( -) found while parsing a string`, `trailing characters`, `unexpected end of hex escape`, `EOF while parsing an object`), and `invalid type: <found>, expected <one of: a string, i32, i64, a boolean, a sequence, an RFC 3339 formatted date and time string>` at the value's last byte (the byte before a bracket), `invalid value: integer … expected i32` beyond i32; a timestamp string that does not parse is chrono's words (`error_jsonl_ts_*`): `premature end of input`, `input contains invalid characters`, `trailing input`, `input is out of range` | 2026-09-23 |
| OQ-103 | A non-UTF-8 or unreadable `.beads/issues.jsonl`, and a `.beads/` directory WITHOUT `issues.jsonl`: the port's workspace probe opens `./.beads/issues.jsonl` and treats a failure as "not initialized"; the original finds the `.beads` DIRECTORY (walking upward, honoring `BEADS_DIR` and `redirect`). | S2.1–S2.9, S8.12–S8.16 | (cases to add: `where_from_subdir` needs a ws-run directive that sets the working directory below the workspace; `list_beads_dir_without_store` needs a fixture selector that creates `.beads/` with no store) | RESOLVED from `where_from_subdir`, `list_from_subdir`, `create_from_subdir`, `edge_nostore_list` (2026-09-24, harness modes `@cwd`, `@fx=nostore`): the walk finds `.beads/` from below (a record created from `sub/` carries `source_repo_path` `/mnt/proj`), and a `.beads/` directory without a store is an empty store, not NOT_INITIALIZED | 2026-09-24 |
| OQ-104 | Which values does `--format` list on `show`, `stats`, `ready`, `blocked` and `dep list`, and is `dep tree --direction` an enumerated option? The port assumes `text, json, toon` and an enumerated direction. | S1.78 | (cases to add: `usage_enum_format_show` `["show","--format","bogus"]`, and the same on `stats`, `ready`, `blocked`, `dep list`; `usage_enum_tree_direction` `["dep","tree","x","--direction","sideways"]`) | RESOLVED from `usage_enum_format_show`, `_stats`, `_ready`, `_blocked`, `_dep_list` (each `[possible values: text, json, toon]`) and `usage_enum_tree_direction` (`[possible values: down, up, both]`): both assumptions hold; S1.78 amended | 2026-09-20 |
| OQ-105 | Does a refused enumerated value that is similar to a listed one earn a tip line, and with which words? The port prints `  tip: a similar value exists: 'status'` after the possible values. | S1.79 | (case to add: `usage_enum_similar_value` `["count","--by","statu"]`) | RESOLVED from `usage_enum_similar_value`: the tip is `  tip: a similar value exists: 'status'`, after the list and a blank line; S1.79 amended; law `cli_similar_value` | 2026-09-20 |
| OQ-106 | The similarity threshold is judged in 64-bit floating point by the original and in exact fractions by the port. Is there a pair (typed word, candidate) among the port's names whose exact similarity is 0.7 or within one rounding step of it? | S1.80 | enumerate the fractions m/la + m/lb + (m−t)/m = 2.1 for lb ≤ 19 and capture one case per hit | RESOLVED from `usage_enum_tie_similar` and `usage_enum_tie_similar_label` (2026-09-24): yes — an exact 0.7 rounds above in binary64, so the original offers the tip where the port's exact comparison did not (a port bug, repaired: `Cli.tie_above`); of the 57 exact ties reachable with names of at most 19 code points, three round down (S1.80 amended) | 2026-09-24 |
| OQ-107 | When two long names are equally similar to an unknown option, which one is suggested? The port suggests the last in declaration order (own options, aliases after their option, globals, `help`, `version`). | S1.81 | (case to add: a typed name equidistant from two options, e.g. `["list","--lon"]` against `--long`/`--json`) | RESOLVED from `usage_similar_lon`: `--lon` suggests `--long` (`tip: a similar argument exists: '--long'`, `Usage: br list --long`), exit 2 | 2026-09-23 |
| OQ-108 | What does a group print when it is given options but no subcommand (`dep --json`)? The port prints the S1.16 shape for that level (`error: 'br dep' requires a subcommand but one was not provided`, the subcommand list with aliases, the `[OPTIONS]` usage line). | S1.83 | (case to add: `usage_dep_flag_only` `["dep","--json"]`) | RESOLVED from `usage_dep_flag_only`: the S1.16 shape with `'br dep'`, the list `add, import, remove, rm, list, tree, cycles, help` and the `[OPTIONS]` usage line; S1.83 amended | 2026-09-20 |
| OQ-109 | In an exclusion, is X (the argument named first) the first in ARGV order or the one that declares the exclusion? All four captured cases give both readings the same answer. | S1.74 | (case to add: `usage_conflict_wrap_reversed` `["@fx=basic","show","proj-170","--no-wrap","--wrap"]`) | RESOLVED from `usage_conflict_wrap_reversed`: argv order (`'--no-wrap' cannot be used with '--wrap'`); S1.74 amended | 2026-09-20 |
| OQ-110 | `help <command>` (the `help` subcommand followed by words): the port prints the top-level help whatever follows. | S1.89 | (case to add: `usage_help_subcommand_list` `["help","list"]`) | RESOLVED from `usage_help_subcommand_list`: `help list` prints `list`'s long help (3698 bytes); the port follows level words after `help`; S1.89 amended; law `cli_help_forms` | 2026-09-20 |
| OQ-111 | `ready --assignee` without a value against `ready --assignee ""`: the port records both as the empty value. Does the original tell them apart? | S1.49 | (cases to add: `ready_assignee_bare`, `ready_assignee_empty` on `@fx=basic`) | RESOLVED from `ready_assignee_bare`, `ready_assignee_empty`: both print the filtered-empty line, exit 0; the original does not tell them apart | 2026-09-23 |
| OQ-112 | Which of `update`'s and `q`'s options accept a value that begins with `-`? The port gives `-d/--description` that property on `create`, `q` and `update`, `--acceptance-criteria` and `--prerequisites` on `create` only, `-r/--reason` on `close` and `reopen`, `-m/--message` on `comments add`. | S1.36, S1.38, S1.39, S1.87 | (cases to add: `update_description_dash` `["@fx=basic","update","proj-mta","-d","-x"]`, `q_description_dash`) | RESOLVED for `-d`: `usage_update_description_dash` and `usage_q_description_dash` show `-d -x` taken as a value on `update` and `q`. The other options of `update` stay as assumed (no leading `-`) until a case says otherwise | 2026-09-20 |
| OQ-113 | A once-only option with a value given twice at one level (`list --limit 1 --limit 2`): the port refuses it as S1.12 does a flag, spelled `'--limit <LIMIT>'`. | S1.12 | (case to add: `usage_limit_twice`) | RESOLVED from `usage_limit_twice`: refused exactly as assumed; S1.91 | 2026-09-20 |
| OQ-114 | The JSON envelope of the validation failures `list` raises (`status: unknown status …`, `sort: invalid sort field …`, `by: only one count grouping selector …`): which `hint`, `retryable` and `context` members? The port writes `hint` null, `retryable` true and `context` `{"field","reason"}` (none for `by`), after the shape `dep_add_custom_type` shows. | S4.105, S4.118, S4.147, S5.21 | (cases to add: `list_status_bad_json`, `list_sort_bad_json`, `count_by_two`, `count_by_two_json`) | RESOLVED from `list_status_bad_json`, `list_sort_bad_json`, `count_by_two_json`: `hint` null, `retryable` true, `context` `{"field","reason"}`, including `by` for the count grouping | 2026-09-23 |
| OQ-115 | When does plain `list` write its truncation note, and which of the two wordings? The port writes it when rows remain beyond the page (`total > offset + limit`), with the total exactly when one of the seven post-query filters was given. No plain `list` case has a limit. | S4.121, S5.124 | (cases to add: `list_limit_plain` `["@fx=basic","list","--limit","2"]`, `list_limit_plain_id` with `--id`, `list_limit_exact` where the limit equals the total) | RESOLVED with OQ-070 from `list_limit_plain`: rows remain beyond the page, and the note has no total when no post-query filter is given | 2026-09-23 |
| OQ-116 | Priority tokens beyond the captured one: what is quoted for `P9` and for `9-1` (low above high), and does a range with a bad end quote the end or the token? The port quotes the whole token in upper case and does not yet read ranges. | S4.107 | (cases to add: `list_priority_range` `-p 0-1`, `list_priority_list` `-p 0,2`, `list_priority_p9`, `list_priority_range_reversed`) | RESOLVED from `list_priority_p9`, `list_priority_backwards`, `list_priority_range`: `P9` quotes `9` with the long hint (`Priority must be 0-4 or P0-P4 …`); `3-1` quotes the whole token with the range hint; ranges are read | 2026-09-23 |
| OQ-117 | `--desc-contains` and `--notes-contains` fold case with full Unicode lower-casing in the original; the port folds the 26 ASCII letters only. | S4.129 | (case to add: a fixture whose description holds `ÉCOLE`, searched with `école`) | RESOLVED with OQ-048 (`list_desc_contains_nonascii`): the original folds with Unicode lower-casing. The port folds ASCII, Latin-1, Latin Extended-A and Additional, Greek and Cyrillic; other scripts are a port gap stated in `core/query.bend`, not an open question | 2026-09-23 |
| OQ-118 | Id lookup lower-cases with full Unicode rules in the original (S4.36); the port folds the 26 ASCII letters. | S4.36 | (case to add: `show` of an id typed with a non-ASCII capital, on a fixture that stores its lower-case form) | RESOLVED from `error_show_nonascii_id`: `Issue not found: PROJ-MTÄ`, the id quoted as typed. No stored id can hold a non-ASCII letter (the id shape of S2.44 refuses it at load), so the fold of a non-ASCII capital never decides a match | 2026-09-23 |
| OQ-119 | `show` with several ids of which a later one fails: does the original print the earlier issues before the refusal? The port prints nothing of them (S5.155's wording). | S4.48, S5.155 | (case to add: `show_two_second_missing` `["@fx=basic","show","proj-mta","proj-zzz"]`) | RESOLVED from `show_two_second_missing`: nothing of the first issue is printed; `Issue not found: proj-zzz`, exit 3 | 2026-09-23 |
| OQ-120 | The plain `show` of bodies longer than 100 columns (S5.115), of a deferred issue (the `Deferred until:` countdown of S5.148 under the pinned instant), of an estimate, an external ref and a due date: no plain case exercises any of them. The port does not wrap yet. | S5.115, S5.146–S5.149 | (cases to add on a new fixture `rich`: `show_wrapped`, `show_deferred_plain`, `show_estimate`, `show_due_ref`) | RESOLVED from `scn_long_texts` and `show_deferred_plain`: `Ref: gh-1`, `Due: 2026-03-01`, `Estimate: 1h 30m` in S5.146 order; the body wraps at 100 columns (OQ-064); the countdown is OQ-076 | 2026-09-23 |
| OQ-121 | The JSON envelope of `show` with no id and no last-touched issue (`VALIDATION_FAILED`): the port writes `hint` null, `retryable` true, `context` `{"field":"ids","reason":…}`. | S1.55, S5.21 | (case to add: `usage_show_alone_json`) | RESOLVED from `usage_show_alone_json`: `hint` null, `retryable` true, `context` `{"field":"ids","reason":"no issue IDs provided and no last-touched issue"}`, exit 4 | 2026-09-23 |
| OQ-122 | `label add`/`label remove` with more than two positionals and no `-l`: S4.370's split walks left while the preceding positional "does not parse as an id-shaped string", which no case exercises and which the clause does not define byte for byte. The port refuses such a command line with its own failure (exit 1) instead of guessing the predicate. | S4.370 | (case to add: `label_add_many` — `["@fx=basic","label","add","proj-mta","proj-5u2","alpha","beta"]`; `label_add_label_looks_like_id` — a label that is id-shaped) | RESOLVED from `label_add_many`, `label_add_label_looks_like_id`, `label_add_middle_words`, `label_add_partial_first` and `scn_label_shapes`: the walk and the id-shape predicate are in S4.370 as measured | 2026-09-22 |
| OQ-123 | A `label add`/`label remove` positional that resolves to no issue: S4.370 quotes `'<input>' is not an issue ID and does not match any issue` with a `Labels go after all issue IDs:` hint, which is NOT the `ISSUE_NOT_FOUND` of S4.212, and no case captures its code, exit or JSON envelope. The port answers its own failure (exit 1). | S4.370, S4.212 | (case to add: `error_label_add_missing` — `["@fx=basic","label","add","proj-zzz","urgent"]`) | RESOLVED from `error_label_add_missing`, `error_label_add_missing_json`, `error_label_add_not_an_issue`, `error_label_add_word_before_id`, `error_label_add_flag_word` and `error_label_remove_word_json`: an id-shaped word keeps `ISSUE_NOT_FOUND` (exit 3); any other gets the quoted message as `VALIDATION_FAILED` on `issues`, exit 4 (S4.370) | 2026-09-22 |
| OQ-124 | `dep add` with a tombstoned endpoint, and a `parent-child` edge whose target is `external:…` (S4.357: `parent-child dependencies must link local issues: <endpoint>`, exit 4): neither message's code, hint or JSON envelope is captured. The port answers its own failure (exit 1) for both. | S4.353, S4.357 | (case to add: `error_dep_add_tombstone` — a scenario that deletes an id and then depends on it; `error_dep_add_external_parent` — `["@fx=basic","dep","add","proj-mta","external:x","-t","parent-child"]`) | RESOLVED from `scn_dep_tombstone`, `error_dep_add_external_parent` and `error_dep_add_external_parent_json`: all three are `VALIDATION_FAILED`, exit 4 (S4.353, S4.357) | 2026-09-22 |
| OQ-125 | The 64-label ceiling of S4.372 (`Validation failed: labels: exceeds 64 labels`): is it counted per issue after the add, and does it refuse the whole command as the charset rule does? The port does not check it. | S4.372 | (case to add: `error_label_add_65` on a fixture whose issue already carries 64 labels) | RESOLVED from `scn_label_ceiling` and `scn_label_ceiling_update`: 64 distinct labels per issue; `label add` stops label by label and keeps what it already wrote, `update` and `create` refuse as a whole (S4.372) | 2026-09-22 |
| OQ-126 | `dep add --type` with a near miss (S4.352 adds `Did you mean --type <nearest>?` to the hint for a normalization, unique-prefix or edit-distance-≤2 match): the captured case `dep_add_custom_type` has no near match, so which of the three tests fires first, and whether the distance is the same Jaro the clap tips use (S1.77), is unmeasured. The port prints the plain hint. | S4.352, S1.77 | (case to add: `error_dep_add_type_near` — `["@fx=basic","dep","add","proj-ptp","proj-mta","-t","block","--json"]`) | RESOLVED from the nine `dep_add_type_*` cases: `BLOCKS` is accepted; `Parent_Child` (normalization), `paren`, `dup`, `waits` (unique prefix), `relate` (a prefix of two types, one edit from `related`), `block` and `blcks` (edit distance) each hint `Did you mean --type <type>?`; `xyz` hints `Use --type with one of: <the eleven>.` | 2026-09-23 |
| OQ-127 | `delete --json` when the run is a dependents preview or a `--dry-run`, and `--dry-run` together with `--force`/`--cascade` (S4.338's `Would also cascade delete <n> dependent(s):` / `Would orphan <n> dependent(s):` blocks): no case captures the JSON shape of a preview, or the lines of those blocks. The port answers its own failure (exit 1) for those four combinations. | S4.333, S4.338 | (case to add: `delete_with_dependents_json`, `delete_dry_run_json`, `delete_dry_run_cascade`, `delete_dry_run_force`) | RESOLVED from `delete_preview_json`, `delete_preview_json_many`, `delete_dry_run_json`, `delete_dry_run_quiet`, `delete_dry_run_force`, `delete_dry_run_force_json`, `delete_dry_run_cascade`, `delete_dry_run_cascade_json` and `delete_dry_run_cascade_none`: the JSON of a preview and of every dry run is one compact `{"preview":true,…}` line (S4.333); the two blocks list ids without titles and are absent when empty (S4.338) | 2026-09-22 |
| OQ-128 | `delete --hard` (a purge, S4.339) and `delete --from-file` (S4.340) are not ported; the port answers its own failure (exit 1). Both have verbatim messages in the spec but no captured case. | S4.339, S4.340 | (case to add: `delete_hard`, `delete_hard_all`, `delete_from_file`) | RESOLVED: `--hard` from `delete_hard`, `delete_hard_force_json`, `delete_hard_cascade_json`, `delete_hard_preview`, `delete_hard_quiet`, `delete_hard_touched`, `delete_hard_all`, `delete_hard_all_dry_run`, `scn_delete_hard_tombstone` and `scn_delete_purge` (S4.339 and S4.336 amended); `--from-file` from `delete_from_file`, `delete_from_file_json`, `error_delete_from_file_missing`, `error_delete_from_file_missing_json`, `error_delete_from_file_dir`, `error_delete_from_file_empty` and `error_delete_from_file_lines` (S4.340 amended; the read itself is S4.397, `IO_ERROR` exit 8) | 2026-09-22 |
| OQ-129 | The witness search of `dep cycles` (S4.187) is unexercised: no captured case makes the command REPORT a cycle, so the `cycles` array shape, the node order inside a cycle, the four counters and the plain wording are all unmeasured. The reason is the original itself — `dep add` refuses a blocking cycle on every route tried (both endpoints open, the source force-closed first, and the target closed), while `related` cycles are accepted and then ignored by the cycle graph (S4.186 counts only `blocks`-family edges and reversed `parent-child`). A store holding a blocking cycle is still reachable in the field, since a git merge of two `issues.jsonl` files is not checked. The port reports "no cycles" and has never had to print one. | S4.186, S4.187, S5.48, S5.181 | (case to add: `dep_cycles_real` on a hand-written `cycles` fixture, the only fixture that could not be the original's own output, plus `dep_cycles_real_json`) | RESOLVED (2026-09-24) by hand-written cycle fixtures, the same class as `dup_id` and `dep_dangling` (a store a git merge reaches and the CLI never writes; bead bb-ptx): 30 cases, `dep cycles` ported in `core/views.bend` | 2026-09-24 |
| OQ-130 | `dep tree --format mermaid` (S1.51 names `mermaid` as the other documented value of `--format`) has no captured case; the port ignores `--format` on `dep tree` and always renders the text shape of S5.179. | S1.51, S5.179 | (case to add: `dep_tree_mermaid` — `["@fx=hierarchy","dep","tree","proj-6ja","--format","mermaid"]`) | RESOLVED from `dep_tree_mermaid`, `dep_tree_mermaid_up`, `dep_tree_mermaid_leaf`, `dep_tree_mermaid_upper`, `dep_tree_mermaid_unicode`, `dep_tree_mermaid_json`, `dep_tree_mermaid_quiet`, `dep_tree_format_other` and `scn_mermaid_titles`: the mermaid shape is S5.253 (pre-order nodes `n0…`, edges from the parent's place, `"` → `'`), the selection is S4.394 (`mermaid` in any case; any other value is text; `--json` wins). The port implements both (`Views.tree_mermaid`, `Views.mermaid_of`); all nine pass on js; closed law `run_dep_tree_mermaid` | 2026-09-22 |
| OQ-131 | Does the import refuse a record whose dependencies do not survive it (a target that is not in the store, an edge on itself, an edge whose `issue_id` names another record), and with which bytes, exit code and precedence? Found by the real-store sweep (bb-acg, 2026-09-24): 5 of 138 real stores refuse with `differing fields: dependencies` where the port answered. | S2.57, S2.58 | `error_load_dep_dangling`, `error_load_dep_dangling_json`, `error_load_dep_dangling_related`, `error_load_dep_dangling_empty`, `edge_load_dep_external`, `edge_load_dep_dangling_show`, `error_load_dep_self`, `error_load_dep_self_json`, `edge_load_dep_self_show`, `error_load_dep_issue_id`, `error_load_dep_issue_id_json`, `error_load_dep_other_then_self`, `error_load_dep_dangling_then_self` | RESOLVED from those 13 cases (captured 2026-09-24; the capture's `--repin` reason names this entry `OQ-139` by mistake): a dependency whose target is neither stored nor `external:` adds `dependencies` to S2.57's differing members (any type; a tombstone is stored; a target later in the file is stored); before that check, every record in file order, and within it every dependency in order, is refused for an `issue_id` other than the record's (VALIDATION_FAILED, exit 4) and then for a target equal to the record's id (SELF_DEPENDENCY, exit 5): S2.58. `show` does none of the three. With several S2.57 conflicts the original names one at random (DISC-012) | 2026-09-24 |
| OQ-132 | How does `show` render a dependency whose target is not in the store (the one command that reads such a store, S2.57), in plain and JSON forms, and where does it sort? | S5.151, S5.31 | `show_dep_missing`, `show_dep_missing_json`, `edge_load_dep_dangling_show` | RESOLVED from those cases (2026-09-24): `[missing issue: <id>]` as the title, status `tombstone`, priority 2, sorted by the edge's own `created_at` (probed against a stored record created later than the edge, and against runs at two different pinned instants) | 2026-09-24 |
| OQ-133 | Does `average_lead_time_hours` truncate the exact duration (fractions of a second included) or the difference of whole seconds? The real-store sweep (2026-09-24) found 8 of 138 stores whose mean differed by one hour in the sum. | S4.154 | `stats_lead_subsecond` (fixture `lead_subsecond`: 3599.78 s, 3 h exactly, 2 h 0.5 s) | RESOLVED from `stats_lead_subsecond` (2026-09-24): the exact duration, fractions included: 3599.78 s is 0 hours; the mean is 1.6666666666666667 | 2026-09-24 |
| OQ-134 | Round 12 (non-author, 2026-09-24): what do the import checks do with an ephemeral record and with a `parent-child` edge to an `external:` target, and how does `show` title and sort an `external:` end? | S2.57, S2.58, S5.151, S5.31 | the 35 `r12_*` cases | RESOLVED from them (floored STABLE): an ephemeral record is dropped before the checks and is not a stored target; a `parent-child` edge to `external:` is a third S2.58 rule (VALIDATION_FAILED, exit 4, after the `issue_id` and self rules on the same edge); an `external:` end sorts by the edge's `created_at` and drops the prefix from its title when the rest is `<project>:<id>` | 2026-09-24 |
| OQ-135 | How does the markdown import of `create -f` resolve `### Parent` and `### Dependencies`, and what do unresolved references and cycles print? | S4.237 | `create_file_md_parent`, `create_file_md_parent_json`, `create_file_md_parent_store`, `create_file_md_deps`, `create_file_md_deps_json`, `create_file_md_cycle`, `create_file_md_cycle3`, `create_file_md_mixed` | RESOLVED from those 8 cases (2026-09-24): S4.237's amendment (earlier titles and ids make dotted children, a later title defers the edge; dependency words by title or id; unresolved words and cycles warn and exit 5) | 2026-09-24 |
| OQ-136 | Does the import check a stored dependency's `metadata`, and how exactly does serde_json place and quote a refused JSON text (lines, multi-byte characters, `\u` escapes, stray characters, control characters)? Found 2026-09-24 while building the bb-rvt `long_texts` fixture: a store whose edge metadata was not JSON loaded silently in the port and was refused by the original on every command but `show` | S2.24, S2.27, S2.58, S4.350 | `error_load_dep_meta_count`, `error_load_dep_meta_list_json`, `edge_load_dep_meta_show`, `error_load_dep_meta_update`, `error_load_dep_meta_before_self`, `error_load_dep_meta_before_ext`, `error_load_dep_meta_lines`, `error_load_dep_meta_lines_json`, `error_load_dep_meta_stray`, `error_load_u_escape_bad`, `error_dep_add_metadata_lines`, `error_dep_add_metadata_escape`, `error_dep_add_metadata_quoting`, `error_dep_add_metadata_prefix` | RESOLVED from those 14 cases (2026-09-24): S2.58 gains the metadata rule between self-dependency and the `external:` rule (field `dependencies[<k>].metadata`, k in stored order); S4.350's text is Rust `{:?}` quoting (the `\u{…}` set measured over every scalar value by `dep add --metadata`) and `line L column C` positions; S2.27's `\u` escape is placed at the fourth byte, a stray character stops the lexing and is judged where it stands, a keyword prefix at the end is `EOF while parsing a value`. Not yet reproduced (bead): on a store line, serde reports a struct's missing field before `trailing characters` (`{"id":"proj-a1"} X`) | 2026-09-24 |
| OQ-137 | Round 21 (non-author, 2026-09-25): how do the `BD_DB`/`BD_DATABASE`/`BEADS_DB` overrides of S2.1 behave under `--no-db`? The port does not read them (S2.1's step 2 is not implemented). Measured on the original (not yet captured): `BD_DB=/mnt/proj/.beads/beads.db count` answers the workspace (6); `BD_DB=/mnt/nowhere/.beads/x.db` and, without a `.beads` directory, `BD_DB=/mnt/proj/.beads/x.db` are CONFIG_ERROR exit 7 `Configuration error: database override '<value>' not found or not a .beads directory: <its .beads directory>`; `BD_DB=/mnt/proj/x.db` (no `.beads` component) prints `0` for a store of 6, which S2.1 does not predict. | S1.20, S1.60, S2.1, S2.9, S4.36, S5.56 | the 76 cases of the `OQ-137` block of `goldens/cases.tsv` (`count_beads_jsonl_*`, `error_beads_jsonl_*`, `where_beads_jsonl_*`, `count_db_*`, `count_bd_*`, `where_db_*`, `where_bd_*`, `error_no_workspace_*db*`, `error_db_override_missing_beads_json`, `usage_*_empty`, `show_kelvin_sign_*`, `update_kelvin_sign`) | RESOLVED from those cases (2026-09-26, captured with the pinned br 0.6.0): `BEADS_JSONL` is a store path with five refusals in a fixed order (S1.60); `--db` outranks `BD_DB`, `BD_DATABASE` and `BEADS_DB` only name the no-workspace refusal, whose text, code and context S2.9 now state; an override whose directory is the workspace directory runs normally (S2.1). An override whose directory is ANOTHER directory makes the original read `<that directory>/issues.jsonl` with command-dependent results (`--db /mnt/proj/x.db count` prints 0 for a store of 6, `r21_ws_bddb_env_missing`'s shape): EXCLUDED, PLAN §3 row "database override outside the workspace"; the port answers its own not-ported refusal there. Found on the way: the argument parser refuses an EMPTY path value (`--db ""`, `create --file ""`, S1.20) | 2026-09-25; resolved 2026-09-26 |
