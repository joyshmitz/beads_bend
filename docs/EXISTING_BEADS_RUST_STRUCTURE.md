# EXISTING BEADS_RUST STRUCTURE — THE SPEC

<!-- Phase 1 document. THE contract of the port. Rule: after reading this
     file, an implementer must NOT need the original's source. Behavior is
     described as WHAT (inputs → outputs, verbatim strings, exact numbers),
     never HOW (no "uses a dict", no code). Every clause is numbered
     S<section>.<n> and cites its provenance `file:line` in the original;
     port defs, laws and golden cases refer to clauses by number.
     Data (formats, strings, constants) is quoted verbatim. Algorithms are
     described behaviorally with an | input | output | examples table.
     A gap discovered later is an OQ entry, resolved by RUNNING the
     original on a new case (captured as a golden), then amended here. -->

Provenance: the original's source pinned at tag `v0.6.0` (commit `b1cfebe05437463e91a353cf2bedafac27266f5b`, tree `45cc06f56529d614d2602a327f469a8478fea17e`), read from `legacy/BEADS_RUST_v0.6.0/`; behavior from the captured goldens (`goldens/MANIFEST.txt`) and from runs of the pinned `br 0.6.0` binary in the `scripts/ws-run.sh` sandbox. Extracted 2026-09-20 by seven section-owning extractors; assembled by `scripts/merge-spec-parts.py` from `docs/spec-parts/` (the part files are kept).
Coverage: see §12.

## S1. Command-line surface

<!-- from docs/spec-parts/S1_S9.md -->

| S1.n | clause | provenance | cases |
|---|---|---|---|
| S1.1 | The binary is `br`. The invocation form is `br [GLOBAL OPTIONS] <COMMAND> [COMMAND OPTIONS] [POSITIONALS]`, and the port's contract adds the global `--no-db` to every invocation: `br --no-db <command> …`. Exactly one subcommand is required; there is no default command. | `src/cli/mod.rs:691` (`#[command(name = "br", …)]`), `src/cli/mod.rs:694` | usage_none, list_plain |
| S1.2 | Arguments are read from the process argv as UTF-8 strings; an argument that is not valid UTF-8 terminates the process inside the argument reader before any command runs (Rust `std::env::args`, probe RS-20). No golden exercises a non-UTF-8 argument; the corpus is entirely UTF-8. Amended (2026-09-24, OQ-012): measured, and the first sentence is wrong: the process does NOT stop in the argument reader. Each argument is decoded lossily (an invalid sequence reads as one U+FFFD, as WHATWG decodes) and the argument parser runs: an invalid argument taken as a VALUE (an option's value, attached with `=` or the next word, or a positional) is refused as `error: invalid UTF-8 was detected in one or more arguments`, a blank line, the level's bare usage line (`Usage: br list [OPTIONS]`, `Usage: br dep add [OPTIONS] <ISSUE> <DEPENDS_ON>`, `Usage: br [OPTIONS] <COMMAND>` for a global's value before the command), a blank line, `For more information, try '--help'.`, exit 2; taken as a NAME it keeps its U+FFFD text in the ordinary refusals (`unrecognized subcommand '�'`, `unexpected argument '--fo�' found`); `--help` before it still answers the help. The port reads argv through the custom effect `Sys.argv` (validity flag per argument). | `src/main.rs:35` (`Cli::parse()`) | usage_non_utf8_arg, usage_non_utf8_value, usage_non_utf8_attached, usage_non_utf8_global, usage_non_utf8_positional, usage_non_utf8_flag, usage_non_utf8_sub, usage_non_utf8_help, usage_non_utf8_truncated, r22_g_version_json_top, r22_g_nocolor_count, r22_g_many_globals, r22_g_actor_eq, r22_g_actor_twice, r22_g_quiet_twice, r22_g_v_cluster_q, r22_g_unknown_sub_json, r22_e_actor_beats_bd_actor_comment |
| S1.3 | The twelve global options are accepted at any position — before the subcommand, after it, or after a positional — because every one is declared `global = true`: `--db <DB>` (path), `--actor <ACTOR>` (string), `--json` (flag), `--no-daemon` (flag), `--no-auto-flush` (flag), `--no-auto-import` (flag), `--allow-stale` (flag), `--lock-timeout <LOCK_TIMEOUT>` (unsigned 64-bit), `--no-db` (flag), `-v`/`--verbose` (repeat-counted flag), `-q`/`--quiet` (flag), `--no-color` (flag). Measured: `br --no-db --json list` and `br --no-db list --json` produce identical stdout. | `src/cli/mod.rs:698`–`src/cli/mod.rs:744` | list_json, list_quiet, actor_flag_create |
| S1.4 | Under `--no-db` the globals `--db`, `--no-daemon`, `--no-auto-flush`, `--no-auto-import`, `--allow-stale` and `--lock-timeout` parse and are accepted but reach no observable behavior: every database path they select is skipped. `--no-db` itself selects the JSONL-only store. | `src/main.rs:81` (`storage_enabled = ctx.is_initialized() && !ctx.no_db()`), `src/main.rs:2238` | list_plain, where_plain |
| S1.5 | `-v` is a counting flag: `-v` sets verbosity 1, `-vv` sets 2, and repeating it is not an error (unlike `--json`, S1.12). Verbosity only reaches the `tracing` subscriber on stderr, which the sandbox pins to `RUST_LOG=error`, so no golden's bytes change with it. | `src/cli/mod.rs:735`, `src/main.rs:41` | (case to add: `verbose_twice` — `["-vv", "list"]` on `@fx=basic`) |
| S1.6 | Output mode is selected by this ordered test, first match wins: (1) `--json`, or a command-local `--robot` on one of `close`, `reopen`, `ready`, `scheduler`, `blocked`, `stats`/`status`, `defer`, `undefer`, `orphans`, `changelog`, `sync`, `vcs-status`, `dep import`, `coordination status`, `gate`, `capacity`, `doctor --robot-triage` → **JSON**; (2) `--quiet` → **Quiet**; (3) `BR_OUTPUT_FORMAT`, else `TOON_DEFAULT_FORMAT`, parsed case-insensitively after trimming as `json`→JSON, `toon`→TOON, `text`/`plain`/`csv`→no decision here; (4) `--no-color`, or a non-empty `NO_COLOR`, or `TERM` equal to `dumb` ignoring case, or a stdout that is not a terminal → **Plain**; (5) otherwise **Rich**. Measured: `--json` beats `--quiet` (`br --no-db --quiet --json count` on `@fx=basic` prints `{"count":6}` and LF). | `src/output/context.rs:858`–`src/output/context.rs:875`, `src/output/context.rs:764`, `src/cli/mod.rs:1649`, `src/cli/mod.rs:1715` | list_json, list_quiet, count_json, blocked_json |
| S1.7 | Rich (TTY) and TOON output are outside the port's surface (PLAN §3 exclusions "Rich (TTY) output", "TOON output"). Every captured golden is produced with stdout piped and `NO_COLOR=1`, so step (4) of S1.6 always fires and the only modes the port reproduces are Plain, JSON and Quiet. | PLAN §3 exclusion rows; `src/output/context.rs:767` | every case; named examples: list_plain (Plain), list_json (JSON), list_quiet (Quiet) |
| S1.8 | Quiet mode suppresses the command's normal stdout entirely while leaving the exit code unchanged: `br --no-db list --quiet` on `@fx=basic` writes zero bytes to stdout and stderr and exits `0`, although the same command in Plain mode lists seven issues. | `src/output/context.rs:905`, `src/cli/commands/where.rs:41` | list_quiet |
| S1.9 | Quiet mode does not suppress errors. Measured: `br --no-db --quiet show proj-zzz` on `@fx=basic` writes `Error: Issue not found: proj-zzz` LF `Hint: Run 'br list' to see available issues.` LF to stderr and exits `3`. | `src/main.rs:1875` (`handle_error` ignores the mode except for JSON) | error_show_missing, (case to add: `quiet_error_show_missing`) |
| S1.10 | Both option-value spellings are accepted: `--flag value` (separate argv words) and `--flag=value` (one word). Measured: `br --no-db list --limit=1 --json` on `@fx=basic` produces exactly the bytes of `list_limit_one`. | clap derive defaults, `src/cli/mod.rs:1885` | list_limit_one, (case to add: `list_limit_equals` — `["list", "--limit=1", "--json"]`) |
| S1.11 | Option names are never abbreviated. Measured: `br --no-db list --li 1` fails with `error: unexpected argument '--li' found` LF LF `  tip: a similar argument exists: '--limit'` LF LF `Usage: br list --limit <LIMIT>` LF LF `For more information, try '--help'.` LF on stderr, exit `2`. The usage line names the *suggested* option, not the one that was typed. | clap's behavior, observed by running the original through the sandbox (re-measured 2026-09-20: the seven stderr lines quoted, exit `2`, stdout empty); no golden holds these bytes yet. Source side: `src/cli/mod.rs:692` (the `#[command(name = "br", …)]` attribute sets no long-option inference, so clap's default of exact names applies), `src/cli/mod.rs:1885`–`src/cli/mod.rs:1886` (`--limit`, the option clap suggests) | (case to add: `usage_abbrev_flag` — `["list", "--li", "1"]`) |
| S1.12 | A flag whose action is "set true" is refused when it appears twice. Golden `usage_json_twice` (`["list","--json","--json"]`): stderr is `error: the argument '--json' cannot be used multiple times` LF LF `Usage: br list [OPTIONS]` LF LF `For more information, try '--help'.` LF, stdout empty, exit `2`. | `src/cli/mod.rs:707` | usage_json_twice |
| S1.13 | A repeatable option accumulates instead of refusing. Measured on `@fx=basic`: `list -p 1 -p 2 --json` returns the union of priorities 1 and 2 (four issues), and `list --status open --status closed --json` returns six issues. | `src/cli/mod.rs:1830`, `src/cli/mod.rs:1858` | list_priority_one, list_status_open, list_label_two_and, list_label_any |
| S1.14 | A literal `--` ends option parsing; the remaining words become positionals. Measured: `br --no-db show -- proj-170` on `@fx=basic` is byte-identical to `show_plain` (amended 2026-09-20: the first text named `proj-mta`, which is not the issue `show_plain` shows). The tip in S1.19 advertises this form. | clap's behavior, observed: `goldens/usage_unexpected_flag.err:3` (the tip `use '-- --priority'` that advertises the form); `src/cli/mod.rs:1960`–`src/cli/mod.rs:1961` (`ShowArgs::ids`, the positional list that receives the words after `--`). Re-measured through the sandbox 2026-09-20: `show -- proj-mta` is byte-identical to `show proj-mta`, and `show -- proj-170` is byte-identical to `goldens/show_plain.out` — the argv of show_plain (`goldens/cases.tsv:85`) names `proj-170`, not `proj-mta`, so the clause's "same panel as `show_plain`" holds only with `proj-170` | show_plain, (case to add: `show_double_dash` — `["@fx=basic", "show", "--", "proj-170"]`, expected bytes = show_plain) |
| S1.15 | An unknown top-level option is a parse failure, exit `2`, stderr only, stdout empty. Golden `usage_unknown_flag` (argv `["--no-such-flag"]`, invoked as `br --no-db --no-such-flag`): `error: unexpected argument '--no-such-flag' found` LF LF `  tip: a similar argument exists: '--no-auto-flush'` LF LF `Usage: br --no-db --no-auto-flush <COMMAND>` LF LF `For more information, try '--help'.` LF. The usage line lists the option already consumed (`--no-db`) followed by the suggested option, which is an oddity, not a typo (hand-over to S10). | clap's behavior, observed: `goldens/usage_unknown_flag.err:1` (error line), `goldens/usage_unknown_flag.err:3` (tip), `goldens/usage_unknown_flag.err:5` (usage line), `goldens/usage_unknown_flag.err:7` (help pointer); `goldens/usage_unknown_flag.exit:1` is `2`; `goldens/usage_unknown_flag.out` is zero bytes. Source side: `src/main.rs:35` (`Cli::parse()`), `src/cli/mod.rs:715`–`src/cli/mod.rs:716` (`--no-auto-flush`, the suggested global), `src/cli/mod.rs:732` (`--no-db`) | usage_unknown_flag |
| S1.16 | No subcommand at all is a parse failure, exit `2`, stderr only. Golden `usage_none` (argv `[]`, invoked as `br --no-db`): `error: 'br' requires a subcommand but one was not provided` LF `  [subcommands: agents, audit, blocked, capabilities, capacity, changelog, close, comments, comment, completions, completion, config, coordination, coord, count, create, defer, delete, dep, doctor, epic, gate, graph, history, info, init, label, lint, list, orphans, q, query, ready, reopen, robot-docs, robot_docs, scheduler, schedule, schema, search, show, stale, stats, status, sync, undefer, update, vcs-status, upgrade, version, where, help]` LF LF `Usage: br [OPTIONS] <COMMAND>` LF LF `For more information, try '--help'.` LF. The subcommand list is fixed: it names 52 entries separated by `, `, in that order, including aliases (`comment`, `completion`, `coord`, `robot_docs`, `schedule`, `status`) and out-of-scope commands, and is reproduced verbatim although most of those commands are excluded from the port. | `src/cli/mod.rs:748`–`src/cli/mod.rs:1006` | usage_none |
| S1.17 | An unknown subcommand is a parse failure, exit `2`, stderr only. Golden `usage_unknown_cmd` (argv `["frobnicate"]`): `error: unrecognized subcommand 'frobnicate'` LF LF `Usage: br [OPTIONS] <COMMAND>` LF LF `For more information, try '--help'.` LF. No suggestion line is printed for a name with no near match. | clap's behavior, observed: `goldens/usage_unknown_cmd.err:1` (error line), `goldens/usage_unknown_cmd.err:3` (usage line), `goldens/usage_unknown_cmd.err:5` (help pointer) — five lines in all, none of them a tip; `goldens/usage_unknown_cmd.exit:1` is `2`; `goldens/usage_unknown_cmd.out` is zero bytes. Source side: `src/main.rs:35` (`Cli::parse()`), `src/cli/mod.rs:748` (`enum Commands`, the closed set of names) | usage_unknown_cmd, r22_g_unknown_sub_similar, r22_g_label_unknown_sub, r22_g_epic_unknown_sub, r22_e_fmt_json_usage_err |
| S1.18 | An empty-string first argument is treated as an unknown subcommand, not as an absent one. Golden `usage_empty_arg` (argv `[""]`): `error: unrecognized subcommand ''` LF LF `Usage: br [OPTIONS] <COMMAND>` LF LF `For more information, try '--help'.` LF, exit `2`. | clap's behavior, observed: `goldens/usage_empty_arg.err:1` (error line with the empty pair of quotes), `goldens/usage_empty_arg.err:3` (usage line), `goldens/usage_empty_arg.err:5` (help pointer); `goldens/usage_empty_arg.exit:1` is `2`; `goldens/usage_empty_arg.out` is zero bytes. Source side: `src/main.rs:35` (`Cli::parse()`), `src/cli/mod.rs:748` (`enum Commands`) | usage_empty_arg |
| S1.19 | An option that is valid globally but not for the chosen command is refused with a quoting tip. Golden `usage_unexpected_flag` (argv `["show","--priority","1"]`): `error: unexpected argument '--priority' found` LF LF `  tip: to pass '--priority' as a value, use '-- --priority'` LF LF `Usage: br show [OPTIONS] [IDS]...` LF LF `For more information, try '--help'.` LF, exit `2`. | `src/cli/mod.rs:1958` (`ShowArgs` has no priority option) | usage_unexpected_flag |
| S1.20 | An option that takes a value but is given none at the end of argv is refused without a usage block. Golden `usage_limit_noval` (argv `["list","--limit"]`): `error: a value is required for '--limit <LIMIT>' but none was supplied` LF LF `For more information, try '--help'.` LF, exit `2`. Amended (2026-09-26, OQ-137): a path-valued option (`--db`, `create --file`, `update --description-file`, `delete --from-file`, `comments add --file`) given an EMPTY value, as a separate word or after `=`, is refused with the same message, exit 2 (`--db ""`, `--db=`). | `src/cli/mod.rs:1886` | usage_limit_noval, usage_db_empty, usage_create_file_empty, usage_update_description_file_empty, usage_delete_from_file_empty, usage_comments_file_empty, usage_db_equals_empty |
| S1.21 | A value that does not parse into the option's type is refused by the parser, before any workspace is touched, with the Rust parse error appended after a colon and a space. Golden `usage_limit_bad` (argv `["list","--limit","x"]`): `error: invalid value 'x' for '--limit <LIMIT>': invalid digit found in string` LF LF `For more information, try '--help'.` LF, exit `2`. `--limit` is an unsigned pointer-width integer, so `+1` is accepted (measured: same bytes as `list_limit_one`), `-1` is not a value at all but an unknown short option (`error: unexpected argument '-1' found`), and `0` is accepted and means unlimited (S1.31). | `src/cli/mod.rs:1886`, probe RS-10 | usage_limit_bad, list_limit_one |
| S1.22 | A value outside a bounded integer type is refused with a range message. Measured: `br --no-db list --priority-min 300` gives `error: invalid value '300' for '--priority-min <PRIORITY_MIN>': 300 is not in 0..=255` LF LF `For more information, try '--help'.` LF, exit `2`, because `--priority-min` is an 8-bit unsigned value. The in-range refusal `9` is *not* a parse failure but a validation failure (S9.12). | `src/cli/mod.rs:1862` | (case to add: `usage_priority_min_range` — `["list","--priority-min","300"]`), list_priority_min_max |
| S1.23 | A value for an enumerated option is refused with the possible values listed on an indented continuation line. Measured: `br --no-db count --by bogus` gives `error: invalid value 'bogus' for '--by <BY>'` LF `  [possible values: status, priority, type, assignee, label]` LF LF `For more information, try '--help'.` LF, exit `2`; `dep list proj-170 --direction sideways` gives the same shape with `[possible values: down, up, both]`; `list --format bogus` gives `[possible values: text, json, csv, toon]`. `dep tree --format` is a free string and accepts anything (measured: `--format bogus` renders the text tree and exits `0`). | `src/cli/mod.rs:2532` (`CountBy`), `src/cli/mod.rs:2276` (`DepDirection`), `src/cli/mod.rs:1631` (`OutputFormat`), `src/cli/mod.rs:2306` (`DepTreeArgs::format` is `String`) | count_by_status, dep_list, dep_tree, (cases to add: `usage_count_by_bad`, `usage_direction_bad`, `usage_format_bad`) |
| S1.24 | A missing required positional is a parse failure listing each missing placeholder on its own two-space-indented line. Golden `usage_search_alone` (argv `["search"]`): `error: the following required arguments were not provided:` LF `  <QUERY>` LF LF `Usage: br search <QUERY>` LF LF `For more information, try '--help'.` LF, exit `2`. Golden `usage_dep_add_one_arg` (argv `["dep","add","proj-mta"]`): the same shape with `  <DEPENDS_ON>` and `Usage: br dep add <ISSUE> <DEPENDS_ON>`. Measured with both missing (`dep add`): the two placeholder lines appear in declaration order, `  <ISSUE>` then `  <DEPENDS_ON>`. | `src/cli/mod.rs:1950` (`SearchArgs::query`), `src/cli/mod.rs:2216`–`src/cli/mod.rs:2222` (`DepAddArgs`) | usage_search_alone, usage_dep_add_one_arg |
| S1.25 | A command group given with no subcommand prints its **long help** to stderr and exits `2` — it is not the "requires a subcommand" error of S1.16. Golden `usage_dep_alone` (argv `["dep"]`) is, verbatim: `Manage dependencies` LF LF `Usage: br dep [OPTIONS] <COMMAND>` LF LF `Commands:` LF `  add     Add a dependency: <issue> depends on <depends-on>` LF `  import  Bulk import dependency edges from JSONL` LF `  remove  Remove a dependency [alias: rm]` LF `  list    List dependencies of an issue` LF `  tree    Show dependency tree rooted at issue` LF `  cycles  Detect and report dependency cycles` LF `  help    Print this message or the help of the given subcommand(s)` LF LF followed by the shared `Options:` block of S1.28. Subcommand names are padded with spaces to the width of the longest name plus two. | `src/cli/mod.rs:810`, `src/cli/mod.rs:1983`–`src/cli/mod.rs:1997` | usage_dep_alone |
| S1.26 | Golden `usage_label_alone` (argv `["label"]`), verbatim: `Manage labels` LF LF `Usage: br label [OPTIONS] <COMMAND>` LF LF `Commands:` LF `  add       Add label(s) to issue(s)` LF `  remove    Remove label(s) from issue(s)` LF `  list      List labels for an issue or all unique labels` LF `  list-all  List all unique labels with counts` LF `  rename    Rename a label across all issues` LF `  help      Print this message or the help of the given subcommand(s)` LF LF then the `Options:` block of S1.28; exit `2`, stdout empty. | `src/cli/mod.rs:855`, `src/cli/mod.rs:2323`–`src/cli/mod.rs:2334` | usage_label_alone |
| S1.27 | Golden `usage_epic_alone` (argv `["epic"]`), verbatim: `Epic management commands` LF LF `Usage: br epic [OPTIONS] <COMMAND>` LF LF `Commands:` LF `  status          Show status of all epics (progress, eligibility)` LF `  close-eligible  Close epics that are eligible (all children closed)` LF `  help            Print this message or the help of the given subcommand(s)` LF LF then the `Options:` block of S1.28; exit `2`, stdout empty. | `src/cli/mod.rs:819`, `src/cli/mod.rs:2001`–`src/cli/mod.rs:2007` | usage_epic_alone |
| S1.28 | The `Options:` block that closes each of S1.25–S1.27 is byte-identical in all three and lists exactly the global options of S1.3, in declaration order, each help text aligned in a column starting at 1-based column 38 of the line: `Options:` LF `      --db <DB>                      Database path (auto-discover .beads/*.db if not set; env: BEADS_DB)` LF `      --actor <ACTOR>                Actor name for audit trail` LF `      --json                         Output as JSON` LF `      --no-daemon                    Force direct mode (no daemon) - effectively no-op in br v1` LF `      --no-auto-flush                Skip auto JSONL export` LF `      --no-auto-import               Skip auto import check` LF `      --allow-stale                  Allow stale DB (bypass freshness check warning)` LF `      --lock-timeout <LOCK_TIMEOUT>  ` + "`SQLite`" + ` busy/write-lock timeout in ms` LF `      --no-db                        JSONL-only mode (no DB connection)` LF `  -v, --verbose...                   Increase logging verbosity (-v, -vv)` LF `  -q, --quiet                        Quiet mode (no output except errors)` LF `      --no-color                     Disable colored output` LF `  -h, --help                         Print help` LF. The backticks around `SQLite` are literal bytes in the output: the doc comment was written for the lint, and the help renderer does not strip them. | `src/cli/mod.rs:698`–`src/cli/mod.rs:744` | usage_dep_alone, usage_label_alone, usage_epic_alone |
| S1.29 | `-h`/`--help` on any level and `-V`/`--version` print help/version text to **stdout** and exit `0` (measured: `br --no-db --version` prints `br 0.6.0` LF; `br --no-db --help` starts `Agent-first issue tracker (SQLite + JSONL)` LF LF `Usage: br [OPTIONS] <COMMAND>`; `br --no-db list --help` starts `List issues` LF LF `Usage: br list [OPTIONS]`). PLAN §3 excludes "clap help and usage texts beyond the captured error shapes", so only the three long-help texts of S1.25–S1.27 and the error shapes of S1.15–S1.24 are reproduced. | `src/cli/mod.rs:692` (`author, version, about`) | (case to add: `help_root`, `version_flag` — `["--version"]`) |
| S1.30 | A clap parse failure is always a plain-text stderr message with exit `2` and an empty stdout, no matter which output mode the other arguments select. Measured: `br --no-db list --json --limit x` writes zero bytes to stdout and the bytes of S1.21 to stderr. This is the one error class that never produces the JSON envelope of S9.2. | `src/main.rs:35` (parse precedes `should_render_errors_as_json`) Captured (2026-09-24, OQ-014): with `BR_OUTPUT_FORMAT=json` too. | usage_json_twice, usage_limit_bad, usage_limit_bad_env_json |
| **`list`** | | | |
| S1.31 | `br list` takes no positionals. Its options are: `-s`/`--status <STATUS>` (repeatable, no comma splitting), `-t`/`--type <TYPE>` (repeatable), `--assignee <ASSIGNEE>`, `--unassigned`, `--id <ID>` (repeatable), `-l`/`--label <LABEL>` (repeatable, AND), `--label-any <LABEL_ANY>` (repeatable, OR), `-p`/`--priority <PRIORITY>` (repeatable), `--priority-min <PRIORITY_MIN>` (8-bit), `--priority-max <PRIORITY_MAX>` (8-bit), `--title-contains <TITLE_CONTAINS>`, `--desc-contains <DESC_CONTAINS>`, `--notes-contains <NOTES_CONTAINS>`, `-a`/`--all`, `--limit <LIMIT>` (pointer-width unsigned; default unlimited, and `0` also means unlimited), `--offset <OFFSET>` (default `0`), `--sort <SORT>`, `-r`/`--reverse`, `--deferred`, `--overdue`, `--long`, `--pretty`, `--tree`, `--wrap`, `--format <text|json|csv|toon>`, `--stats`, `--fields <FIELDS>`. Measured: `--limit 0` returns all seven issues of `@fx=basic` with `"limit":0` and `"has_more":false` in the envelope. | `src/cli/mod.rs:1827`–`src/cli/mod.rs:1943`, `src/cli/mod.rs:30` (`DEFAULT_LIST_LIMIT = 0`) | list_plain, list_json, list_json_again, list_all, list_all_json, list_status_open, list_status_closed, list_status_in_progress (the `-s` short form), list_type_bug, list_priority_one, list_priority_min_max, list_assignee, list_unassigned, list_label, list_label_two_and, list_label_any, list_title_contains, list_limit_one, list_limit_offset, list_sort_title, list_reverse, list_deferred, list_long, list_fields, list_id_filter |
| S1.32 | `--sort` accepts `priority`, `created_at`, `updated_at`, `title` and the aliases `created` and `updated`; the value is a free string validated after parsing, so an unknown key is a validation failure and not a parse failure (S9.14). | `src/cli/mod.rs:1894`, `src/cli/mod.rs:160` (`SORT_KEY_CANDIDATES`) | list_sort_title, list_sort_created, list_sort_updated, list_sort_bad |
| S1.33 | `list`'s label options do **not** split on commas: `-l backend,db` is one label named `backend,db` and matches nothing (measured: exit `0`, empty stdout on `@fx=basic`). Two labels require two flags. This differs from `create -l` and `label add -l`, which do split (S1.36, S1.51). | `src/cli/mod.rs:1849` (no `value_delimiter`) | list_label_two_and, (case to add: `list_label_comma` — `["list","-l","backend,db"]`) |
| **`search`** | | | |
| S1.34 | `br search <QUERY>` takes one required positional and then every option of `list` (the `list` option set is flattened into it). The default result cap is 50 rather than unlimited. | `src/cli/mod.rs:1948`–`src/cli/mod.rs:1954`, `src/cli/mod.rs:39` (`DEFAULT_SEARCH_LIMIT = 50`) | search_plain, search_json, search_status, search_none, search_unicode, search_case_insensitive, search_description, usage_search_alone |
| **`show`** | | | |
| S1.35 | `br show [IDS]...` takes zero or more issue ids and the options `--format <text|json|toon>`, `--wrap`, `--no-wrap` (which conflicts with `--wrap`) and `--stats`. Several ids render several panels in argv order. With no id it falls back to `.beads/last-touched` (S1.55). | `src/cli/mod.rs:1958`–`src/cli/mod.rs:1980` | show_plain, show_json, show_two, show_partial_hash, show_closed, show_epic, show_comments, show_comments_json, show_deferred_json, show_unicode, show_unicode_json, usage_show_alone |
| **`create`** | | | |
| S1.36 | `br create [TITLE]` takes an optional positional title plus: `--title <TITLE>` (conflicts with the positional), `-t`/`--type <TYPE>`, `--slug <SLUG>`, `-p`/`--priority <PRIORITY>`, `-d`/`--description <DESCRIPTION>` (visible alias `--body`, accepts values starting with `-`), `--description-file <PATH>` (conflicts with `--description`), `-a`/`--assignee <ASSIGNEE>`, `--owner <OWNER>`, `-l`/`--labels <LABELS>` (repeatable **and** comma-split), `--parent <PARENT>`, `--deps <DEPS>` (repeatable and comma-split), `-e`/`--estimate <ESTIMATE>` (signed 32-bit), `--due <DUE>`, `--defer <DEFER>`, `--external-ref <EXTERNAL_REF>`, `--ephemeral`, `-s`/`--status <STATUS>`, `--acceptance-criteria` (visible alias `--acceptance`, accepts leading `-`), `--prerequisites` (accepts leading `-`), `--agent-context <AGENT_CONTEXT>`, `--dry-run`, `--silent`, `-f`/`--file <FILE>`, `--agent-name <NAME>`, `--harness <HARNESS>`, `--model <MODEL>`. | `src/cli/mod.rs:1039`–`src/cli/mod.rs:1170` | create_min, create_min_json, create_title_flag, create_full_json, create_silent, create_dry_run, create_dry_run_json, create_with_parent, create_with_deps, create_status_in_progress, create_priority_p_form, create_custom_type, create_unicode, create_ephemeral, edge_create_max_title |
| S1.37 | `-p` accepts the bare digits `0`–`4` and the `P`-prefixed forms `P0`–`P4`; both reach the same stored priority. Any other value is a validation failure (S9.12). `-t` and `-s` are free strings: unknown values are stored verbatim, not refused (measured: `create X -t "bad type!"` writes `"issue_type":"bad type!"` and exits `0`; `create X -s bogus` writes `"status":"bogus"` and exits `0`). Amended (2026-09-25, round 19): after the optional `P` the value is read as a signed integer: a `+` and leading zeros are accepted (`01`, `+1`, `P01` are priority 1); a refusal quotes the number read (`05` → `got: 5`, `p-1` → `got: -1`) and a value that is not a number as given (`+`, `+-1`). | `src/cli/mod.rs:1061`, `src/cli/mod.rs:1048`, `src/cli/mod.rs:1122` | create_priority_p_form, create_custom_type, error_create_bad_priority, r19_create_p_zero_pad, create_priority_zero_pad, create_priority_plus, create_priority_p_zero_pad, error_create_priority_zero_pad_high, error_create_priority_p_negative, error_create_priority_plus_alone, update_priority_zero_pad, list_priority_zero_pad |
| **`q`** | | | |
| S1.38 | `br q [TITLE]...` collects **all** trailing positionals as the title words; its options are `-p`/`--priority`, `-t`/`--type`, `-l`/`--labels` (repeatable, no comma splitting), `-d`/`--description` (alias `--body`), `--parent` and `-e`/`--estimate`. With no positional the title is empty and validation fails (measured: `Error: Validation failed: title: cannot be empty`, exit `4`). | `src/cli/mod.rs:1174`–`src/cli/mod.rs:1203` | q_basic, q_priority, (case to add: `usage_q_alone` — `["q"]`) |
| **`update`** | | | |
| S1.39 | `br update [IDS]...` takes zero or more ids and: `--title`, `-d`/`--description` (alias `--body`), `--description-file <PATH>` (conflicts with `--description`), `--design`, `--acceptance-criteria` (alias `--acceptance`), `--prerequisites`, `--check-acceptance <ITEMS>` (repeatable, conflicts with `--acceptance-criteria`), `--uncheck-acceptance <ITEMS>` (same conflict), `--add-acceptance <TEXT>` (repeatable, same conflict), `--notes`, `--append-notes <TEXT>` (repeatable, conflicts with `--notes`), `--transition-comment <COMMENT>`, `-s`/`--status`, `-p`/`--priority`, `-t`/`--type`, `--assignee`, `--owner`, `--claim`, `--force`, `--due`, `--defer`, `--estimate`, `--add-label` (repeatable), `--remove-label` (repeatable), `--set-labels` (repeatable, visible alias `--labels`), `--parent`, `--external-ref`, `--source-repo`, `--source-repo-path`, `--agent-context`, `--session`, `--agent-name`, `--harness`, `--model`. Several ids are updated in one run. | `src/cli/mod.rs:1207`–`src/cli/mod.rs:1405` | update_status, update_status_json, update_priority, update_title, update_assignee, update_claim, update_notes, update_add_label, update_remove_label, update_set_labels, update_two_ids, update_noop_same_value, update_status_closed, update_last_touched |
| S1.40 | `--set-labels` splits on commas although its parser does not: the values are joined with `,` and then re-split, each piece trimmed, empty pieces dropped. `--add-label` and `--remove-label` do not split (measured: `--add-label "x,y"` is one label and fails label validation, S9.13). Example: `--set-labels x,y` yields the stored labels `["x","y"]`. | `src/cli/mod.rs:1357`, `src/cli/commands/update.rs:708`–`src/cli/commands/update.rs:716` | update_set_labels, update_add_label |
| S1.41 | `--status closed` (and any other terminal status) is refused by `update`; the dedicated `close`/`delete` commands are the only path. Measured message in S9.15. `--status` with a non-terminal unknown value is accepted and stored verbatim (measured: `update proj-mta --status bogus` prints `  status: open → bogus` and exits `0`). | `src/cli/mod.rs:1302`–`src/cli/mod.rs:1306` | (case to add: `error_update_status_closed`, `update_custom_status`) |
| **`close`** | | | |
| S1.42 | `br close [IDS]...` takes zero or more ids and: `-r`/`--reason <REASON>` (accepts a leading `-`), `--transition-comment <COMMENT>`, `-f`/`--force`, `--suggest-next`, `--session <SESSION>`, `--robot`, `--agent-name`, `--harness`, `--model`, `--bypass-policy`, `--bypass-reason <REASON>`. `--robot` selects JSON output exactly as `--json` does (S1.6). | `src/cli/mod.rs:2834`–`src/cli/mod.rs:2892` | close_basic, close_reason_json, close_suggest_next, close_two, close_blocked_force, error_close_blocked |
| **`reopen` / `defer` / `undefer`** | | | |
| S1.43 | `br reopen [IDS]...` takes `-r`/`--reason` (accepts a leading `-`), `--robot`, `--agent-name`, `--harness`, `--model`. | `src/cli/mod.rs:2895`–`src/cli/mod.rs:2925` | reopen_closed, reopen_closed_json, reopen_open, error_reopen_missing |
| S1.44 | `br defer [IDS]...` takes `--until <UNTIL>`, `--robot`, `--transition-comment`, `--agent-name`, `--harness`, `--model`. `--until` accepts a date (`2027-06-01`), a relative offset (`+1h`, `-7d`), and the words `tomorrow` and `next-week`; anything else is a validation failure (S9.16). Omitting `--until` is accepted and defers with the command's own default. | `src/cli/mod.rs:2624`–`src/cli/mod.rs:2656` | defer_until_date, defer_no_until, defer_relative, error_defer_bad_until |
| S1.45 | `br undefer [IDS]...` takes `--robot`, `--transition-comment`, `--agent-name`, `--harness`, `--model`. | `src/cli/mod.rs:2658`–`src/cli/mod.rs:2687` | undefer_basic, undefer_not_deferred |
| **`delete`** | | | |
| S1.46 | `br delete [IDS]...` takes `--reason <REASON>` (default value `delete`, so the option is never absent), `--from-file <FROM_FILE>`, `--cascade`, `--force` (conflicts with `--cascade`), `--hard`, `--dry-run`. | `src/cli/mod.rs:1410`–`src/cli/mod.rs:1437` | delete_basic, delete_json, delete_dry_run, delete_with_dependents, delete_with_dependents_force, delete_cascade |
| **`count` / `stats`** | | | |
| S1.47 | `br count` takes `--by <status\|priority\|type\|assignee\|label>` and the five boolean aliases `--by-status`, `--by-priority`, `--by-type`, `--by-assignee`, `--by-label`, plus the filters `--status` (repeatable **and** comma-split), `--type` (repeatable and comma-split), `--priority` (repeatable and comma-split), `--assignee`, `--unassigned`, `--include-closed`, `--include-templates`, `--title-contains`. | `src/cli/mod.rs:2529`–`src/cli/mod.rs:2592` | count_plain, count_json, count_by_status, count_by_priority_json, count_by_type, count_by_assignee, count_by_label, count_include_closed |
| S1.48 | `br stats` (and its alias `br status`) takes `--by-type`, `--by-priority`, `--by-assignee`, `--by-label`, `--activity`, `--no-activity`, `--activity-hours <ACTIVITY_HOURS>` (unsigned 32-bit, default `24`), `--format`, `--stats`, `--robot`. The commit-activity part of `stats` runs `git` and is excluded by PLAN §3. | `src/cli/mod.rs:3212`–`src/cli/mod.rs:3251`, `src/cli/mod.rs:914` | stats_plain, stats_json, empty_stats |
| **`ready` / `blocked`** | | | |
| S1.49 | `br ready` takes `--limit <LIMIT>` (default `0`, meaning unlimited), `--assignee [ASSIGNEE]` (zero or one value; with no value it means the current actor, and it conflicts with `--unassigned`), `--unassigned`, `-l`/`--label` (repeatable), `--label-any` (repeatable), `-t`/`--type` (repeatable), `-p`/`--priority` (repeatable), `--sort <hybrid\|priority\|oldest>` (default `hybrid`), `--include-deferred`, `--parent <PARENT>`, `-r`/`--recursive`, `--epic <EPIC>` (conflicts with `--parent`), `--wrap`, `--format`, `--stats`, `--robot`. Measured (2026-09-23, OQ-111): a bare `--assignee` and `--assignee ""` print the same filtered-empty line (`ready_assignee_bare`, `ready_assignee_empty`). | `src/cli/mod.rs:2689`–`src/cli/mod.rs:2760` | ready_plain, ready_json, ready_limit, ready_sort_priority, ready_sort_oldest, ready_include_deferred, ready_type, ready_unassigned, ready_label, edge_precision_ready, ready_parent_short_r, usage_ready_parent_epic, ready_assignee_bare, ready_assignee_empty |
| S1.50 | `br blocked` takes `--limit <LIMIT>` (default `50`, `0` meaning unlimited), `--detailed`, `--wrap`, `-t`/`--type` (repeatable), `-p`/`--priority` (repeatable), `-l`/`--label` (repeatable), `--format`, `--stats`, `--robot`. The default `50` is visible in the empty-store envelope (`"limit":50`), unlike `list`'s `0`. | `src/cli/mod.rs:2794`–`src/cli/mod.rs:2828` | blocked_plain, blocked_json, empty_blocked_json |
| **`dep`** | | | |
| S1.51 | `br dep` has the subcommands `add`, `import`, `remove` (visible alias `rm`), `list`, `tree`, `cycles`. `dep add <ISSUE> <DEPENDS_ON>` takes `-t`/`--type <TYPE>` (default value `blocks`, free string, so custom types are accepted) and `--metadata <METADATA>`. `dep remove <ISSUE> <DEPENDS_ON>` takes `-t`/`--type` (optional). `dep list <ISSUE>` takes `--direction <down\|up\|both>` (default `down`), `-t`/`--type`, `--format`, `--stats`. `dep tree <ISSUE>` takes `-d`/`--direction` (default `down`), `--max-depth <MAX_DEPTH>` (default `10`), `--format <FORMAT>` (default `text`, free string, `mermaid` being the other documented value). `dep cycles` takes `--blocking-only` and `--include-closed`. `dep import` is outside the port's surface. | `src/cli/mod.rs:1983`–`src/cli/mod.rs:2318` | dep_add_blocks, dep_add_json, dep_add_related, dep_add_custom_type, dep_add_duplicate, dep_add_external, dep_remove_basic, dep_remove_absent, dep_list, dep_list_json, dep_list_none, dep_tree, dep_tree_json, dep_tree_leaf, dep_cycles, dep_cycles_json, empty_dep_cycles |
| **`label`** | | | |
| S1.52 | `br label` has the subcommands `add`, `remove`, `list`, `list-all`, `rename`. `label add <ISSUES>...` and `label remove <ISSUES>...` take a positional list in which the ids come first and bare label names may follow, plus `-l`/`--label <LABEL>` which is repeatable **and** comma-split. Measured on `@fx=basic`: `label add proj-ptp -l a,b` adds two labels and prints `✓ Added label a to proj-ptp` LF `✓ Added label b to proj-ptp` LF, while the positional form `label add proj-ptp a,b` treats `a,b` as one label and fails label validation (S9.13). `label list [ISSUE]` takes an optional id. `label list-all` takes no arguments. `label rename <OLD_NAME> <NEW_NAME>` takes two required positionals. | `src/cli/mod.rs:2323`–`src/cli/mod.rs:2375` | label_add, label_add_json, label_add_existing, label_remove, label_remove_absent, label_rename, label_list, label_list_json, label_list_all, label_list_all_json, error_label_add_invalid |
| **`comments`** | | | |
| S1.53 | `br comments` has an **optional** subcommand: with `add` or `list` it acts on that subcommand, and with a bare issue id it lists that issue's comments (the shorthand `br comments <ID>`). Its own options are the positional `[ID]` and `--wrap`. `comments add <ID> [TEXT]...` takes `-f`/`--file <FILE>`, `--author <AUTHOR>`, `-m`/`--message <MESSAGE>` (visible alias `--content`, accepts a leading `-`); the trailing positionals are the comment text when `-m` is absent. `comments list <ID>` takes `--wrap`. | `src/cli/mod.rs:2377`–`src/cli/mod.rs:2432` | comments_add, comments_add_json, comments_add_author, comments_add_second, comments_list, comments_list_json, comments_list_empty, actor_flag_comment |
| **`epic` / `where` / `version`** | | | |
| S1.54 | `br epic status` takes `--eligible-only`; `br epic close-eligible` takes `--dry-run` and `--transition-comment <COMMENT>`. `br where` takes no arguments beyond the globals. `br version` takes `-c`/`--check` and `-s`/`--short`. | `src/cli/mod.rs:2010`–`src/cli/mod.rs:2027`, `src/cli/mod.rs:1006`, `src/cli/mod.rs:3296`–`src/cli/mod.rs:3303` | epic_status, epic_status_json, where_plain, where_json, version_plain, version_json, r18_stats_alias_by, r18_epic_ce_dry, r18_epic_ce |
| S1.55 | `show`, `update`, `close` and `reopen` accept an empty id list and then read the single id in `.beads/last-touched`; `delete`, `defer` and `undefer` do not have that fallback. The absent-id refusals differ per command and are S9.9–S9.11. Measured (2026-09-23, OQ-121): with no id and no last-touched issue, `show --json` is `VALIDATION_FAILED` with `context` `{"field":"ids","reason":"no issue IDs provided and no last-touched issue"}`, exit 4 (`usage_show_alone_json`). | The fallback read, one site per command: `src/cli/commands/show.rs:239`, `src/cli/commands/update.rs:381`, `src/cli/commands/close.rs:644`, `src/cli/commands/reopen.rs:75` (each calls the reader `src/util/mod.rs:112` when the id list is empty; the file name is `src/util/mod.rs:29`). No fallback: `src/cli/commands/delete.rs:108`, `src/cli/commands/defer.rs:101` (`defer`), `src/cli/commands/defer.rs:404` (`undefer`). Fixture `goldens/fixtures/basic_touched.last-touched:1` holds `proj-mta` | update_last_touched, usage_show_alone, usage_close_alone, usage_update_alone, usage_delete_alone, usage_show_alone_json |
| S1.56 | A positional issue id may be a full id (`proj-mta`), the bare hash suffix (`mta`), or any unique prefix of the suffix; a prefix matching several issues is `AMBIGUOUS_ID` (S9.5) and one matching none is `ISSUE_NOT_FOUND` (S9.4). An empty-string id is neither: it is `INVALID_ID` (S9.6). A dependency target may also be an external reference of the form `external:<name>`, which is stored without resolution. | `src/util/id.rs` (`parse_id`), `src/cli/commands/where.rs:10` | show_partial_hash, show_partial_ambiguous, error_show_missing, dep_add_external |
| **environment** | | | |
| S1.57 | `BEADS_DIR`, when set and not blank after trimming, replaces the upward walk: its value must itself be an existing `.beads` directory. Measured: `BEADS_DIR=/mnt/proj/.beads br --no-db count` on `@fx=basic` prints `6` LF and exits `0`; `BEADS_DIR=/mnt/nowhere` gives `Error: Configuration error: BEADS_DIR not found or not a .beads directory: /mnt/nowhere` LF on stderr, exit `7`. | `src/config/mod.rs:262`, `src/config/mod.rs:285`, `src/config/mod.rs:691` Captured (2026-09-24, OQ-023, harness mode `@env`): `env_beads_dir_where`, `env_beads_dir_list` (run from a subdirectory), `env_beads_dir_missing`. | (case to add: `env_beads_dir_ok`, `error_env_beads_dir_missing`), env_beads_dir_where, env_beads_dir_list, env_beads_dir_missing |
| S1.58 | With no `BEADS_DIR`, the workspace is found by walking up from the current directory and taking the first directory that contains a `.beads` subdirectory; the walk then follows up to ten `.beads` redirects. Reaching the filesystem root with no hit is `NOT_INITIALIZED` (S9.3). The sandbox always runs in `/mnt/proj`, so the resolved directory is `/mnt/proj/.beads` and the derived prefix is `proj`. | `src/config/mod.rs:299`–`src/config/mod.rs:320` | where_plain, error_no_workspace_list |
| S1.59 | The database-path environment variables are read in the fixed order `BD_DB`, `BD_DATABASE`, `BEADS_DB`; the first one set to a non-empty value wins, and `--db` overrides all three. Under `--no-db` none of them changes an observable byte. | `src/config/mod.rs:679`, `src/config/mod.rs:681`–`src/config/mod.rs:688` | where_plain |
| S1.60 | `BEADS_JSONL`, when set to a non-blank value, overrides the JSONL path with the highest priority; pointing it outside `.beads/` additionally requires `--allow-external-jsonl`. No captured case sets it. Amended (2026-09-26, OQ-137): measured. A value that trims to empty is ignored; any other is used as given (never trimmed: a leading space makes it relative) and made absolute against the working directory. Outside the workspace directory (compared component by component with empty and `.` components dropped and `..` kept) every command but `where` is refused `Configuration error: <external-path sha256=HEX> is outside .beads; pass --allow-external-jsonl to authorize it` (CONFIG_ERROR, exit 7, no hint, null context), HEX being the SHA-256 of the value as given; `--allow-external-jsonl` is not a flag of 0.6.0 (a usage refusal). Inside it, in this order: a `..` component is `Path '<value>' contains traversal sequences`; the directory itself is `Path '<value>' must be a regular file`; an extension (after the last `.` of the last component, `none` without one, case-sensitive) outside `db`, `db-wal`, `db-wal-cert`, `db-wal-cert-head`, `db-shm`, `db-journal`, `db-fsqlite-ns-gate`, `db-fsqlite-ns-use`, `jsonl`, `jsonl.tmp` is `Path '<value>' has disallowed extension '<ext>' (allowed: ["db", …, "jsonl.tmp"], plus pid-scoped '*.jsonl.<pid>.tmp')`; a missing directory is `Could not securely traverse JSONL source <external-path sha256=HEX>: No such file or directory (os error 2)` for an absolute value and `Failed to canonicalize path '<value>': No such file or directory (os error 2)` for a relative one; each is `Configuration error: ` + that text, CONFIG_ERROR exit 7. Otherwise the file IS the store: read (a missing file is an empty store) and written instead of `.beads/issues.jsonl`, which is left alone; `last-touched` stays in the workspace directory. `where` refuses none of these and prints `  jsonl: <value as given>` (the default path when the value resolves to it), with no `prefix` line when that store is empty. | `src/config/mod.rs:5898`–`src/config/mod.rs:5899`, `src/cli/mod.rs:3104` | count_beads_jsonl_default, count_beads_jsonl_other, count_beads_jsonl_relative, error_beads_jsonl_traversal, error_beads_jsonl_outside, error_beads_jsonl_outside_json, error_beads_jsonl_outside_relative, usage_allow_external_jsonl, count_beads_jsonl_blank, where_beads_jsonl_other, where_beads_jsonl_default, create_beads_jsonl_other, error_beads_jsonl_no_workspace, where_beads_jsonl_outside, show_beads_jsonl_not_found, list_beads_jsonl_missing, error_beads_jsonl_leading_space, where_beads_jsonl_relative, where_beads_jsonl_relative_json, where_beads_jsonl_dotdot, error_beads_jsonl_outside_dotdot, count_beads_jsonl_dot, where_beads_jsonl_dot, count_beads_jsonl_double_slash, error_beads_jsonl_directory, error_beads_jsonl_missing_dir, show_beads_jsonl_record, update_beads_jsonl_not_found, error_beads_jsonl_outside_list_json, error_beads_jsonl_outside_quiet, error_beads_jsonl_extension, error_beads_jsonl_malformed, count_beads_jsonl_beads_dir, count_beads_jsonl_and_db, error_beads_jsonl_no_extension, count_beads_jsonl_db_extension, count_beads_jsonl_tmp_extension, error_beads_jsonl_upper_extension, error_beads_jsonl_relative_missing_dir, error_beads_jsonl_no_workspace_where, where_beads_jsonl_dotdot_default |
| S1.61 | `BR_OUTPUT_FORMAT` is consulted first and `TOON_DEFAULT_FORMAT` second; each value is trimmed and lowercased and recognized as `text`, `plain`, `json`, `csv` or `toon`. An unrecognized value is ignored entirely and the next rule of S1.6 applies. Measured on `@fx=basic`: `BR_OUTPUT_FORMAT=json br --no-db count` prints `{"count":6}` LF; `BR_OUTPUT_FORMAT=toon` prints `count: 6` LF; `BR_OUTPUT_FORMAT=bogus` prints `6` LF; `TOON_DEFAULT_FORMAT=json` prints `{"count":6}` LF. Amended (2026-09-25, round 17): `--format json` is JSON mode on every command that takes `--format`, and outranks `--quiet`; `--format text` does not override an environment `json`. `--format csv` on `list` and `search` (or no `--format` and an environment `csv`) prints a header line and one line per row: default columns `id,title,status,priority,issue_type,assignee,created_at,updated_at`; `--fields` is split at commas, trimmed, empties skipped, and keeps only `id title description notes status priority issue_type assignee owner created_at updated_at closed_at due_at defer_until external_ref` in the given order, repeats kept (`--fields ""` is the default); instants with `+00:00`, absent values empty. A value starting with `=`, `+`, `-`, `@`, a tab, CR or LF gets a `'` prefix and is quoted; otherwise one holding `,`, `"`, LF or CR is quoted; `"` is doubled. The `list` truncation note (S5.124) and the `search` more-matches note (S4.141) go to stderr; no hidden-closed note. | `src/cli/mod.rs:1649`–`src/cli/mod.rs:1670` Captured (2026-09-24, OQ-086): the same bytes as `--json`, on `count`, `where` and an error envelope. | count_plain, count_json, (case to add: `env_output_format_json`, `env_output_format_bad`), env_output_format_count, env_output_format_where, env_output_format_error, r17_list_format_json, r17_list_format_text, r17_list_format_csv, r17_list_format_csv_all, r17_list_format_csv_parents, r17_list_format_json_long, r17_list_csv_unicode, r17_list_format_json_limit, r17_list_format_csv_limit, r17_list_format_env_json, r17_list_format_csv_empty, r17_list_format_json_empty, r17_list_format_csv_json, r17_list_format_text_json, r17_list_format_csv_labels, list_csv_quoting, list_csv_quoting_fields, list_csv_fields_unknown, list_csv_fields_none_known, list_csv_fields_commas, list_csv_fields_empty, list_csv_fields_repeat, list_csv_env, list_format_json_quiet, list_format_text_env_json, search_csv, search_csv_more, search_csv_none, search_format_json, show_format_json, ready_format_json |
| S1.62 | `NO_COLOR` forces Plain mode when it is set to a non-empty value; `TERM` equal to `dumb` ignoring case does the same; a non-terminal stdout does the same. All three are tested with the same helper, so the answer cannot differ between commands. The sandbox sets `NO_COLOR=1` and pipes stdout, so both conditions hold in every golden. | `src/output/context.rs:764`–`src/output/context.rs:768` | every case; named examples: list_plain, show_plain, ready_plain, stats_plain (all Plain, none carrying an escape byte) |
| S1.63 | Error coloring is decided separately from output mode, against **stderr**: color is used only when `--no-color` is absent, `NO_COLOR` is empty/unset, `TERM` is not exactly `dumb`, and stderr is a terminal. In the sandbox stderr is a pipe, so every `Error:`/`Hint:` line is uncolored. When color is on, the prefixes are `\x1b[31mError:\x1b[0m ` and `\x1b[33mHint:\x1b[0m ` — out of the port's Plain/JSON surface. | `src/main.rs:1855`–`src/main.rs:1872`, `src/error/structured.rs:609`–`src/error/structured.rs:627` | every error case; named examples: error_show_missing, error_no_workspace_list, error_close_missing, error_update_missing (each `.err` is the bare `Error:` line then the bare `Hint:` line, no escape byte) |
| S1.64 | The acting actor for `create`, `q`, `update`, `close`, `reopen`, `defer`, `undefer`, `delete`, `dep` and `label` is resolved in this order: `--actor` (when non-blank), then the `actor` key of the merged configuration (project `.beads/config.yaml`, user config, and any `BD_ACTOR` environment variable, which enters configuration through the `BD_`-prefix rule), then `USER` trimmed, then the literal `unknown`. Measured: `actor_flag_create` with `--actor robot-7` stores `"created_by":"robot-7"`; without it the sandbox's `USER=tester` gives `"created_by":"tester"`. Measured (2026-09-23, OQ-017): `--actor ""` is blank and falls through to `USER` (`actor_flag_empty`). Amended (2026-09-26, round 22): measured, `BD_ACTOR` (trimmed, when not blank) comes after `--actor` and before `USER` for every command on this ladder (`BD_ACTOR=envbot create X` stores `"created_by":"envbot"`; the edge `dep add` writes and `update --claim`'s assignee likewise); `BEADS_ACTOR` is not on this ladder. | `src/config/mod.rs:7063`–`src/config/mod.rs:7072`, `src/config/mod.rs:6016`–`src/config/mod.rs:6026`, `src/cli/commands/create.rs:142` | actor_flag_create, create_min, q_basic, actor_flag_empty, r22_e_bd_actor_create, r22_e_bd_actor_dep_add, actor_env_bd_actor_create, actor_env_bd_actor_blank, actor_env_flag_over_bd_actor, actor_env_bd_actor_dep_add, actor_env_bd_actor_claim, actor_env_bd_actor_delete, create_beads_actor_not_used |
| S1.65 | `comments add` resolves its author on a **different** ladder that never consults the configuration file: `--author` (when non-blank), then `--actor` (when non-blank), then the first non-blank of `BD_ACTOR`, `BEADS_ACTOR`, `USER`, `LOGNAME`, `USERNAME` in that order, then the literal `unknown`. Measured: `BD_ACTOR=envbot … comments add proj-mta hi --json` stores `"author":"envbot"`; with `USER` set to the empty string and nothing else set, `"author":"unknown"`. Amended (2026-09-25, round 15): `comments add --author ""` stores the actor (`tester`), not an empty author. | `src/cli/commands/comments.rs:449`–`src/cli/commands/comments.rs:475` | comments_add, comments_add_author, actor_flag_comment, r15_comments_add_author_empty, r16_comments_author_empty_json, r16_comments_author_empty, r18_comments_add_multi_words, r18_comments_add_content, r18_comments_add_unknown, r18_comments_add_empty_msg, r20_m_add_file_issues, r20_m_add_file_cfg, r20_m_add_file_and_text, r20_m_add_unicode_long, r20_m_add_ctrl, r20_m_add_closed_plain, r21_jr_comment_missing, r21_u_comment_crlf, r21_u_comment_emoji, r22_e_fmt_json_comments_add, r22_e_bd_actor_comment, r22_e_beads_actor_comment, r22_e_bd_blank_beads_actor, r22_m_precision_comment, comments_author_bd_actor, comments_author_beads_actor, comments_author_beads_actor_after_blank |
| S1.66 | The attribution options `--agent-name`, `--harness` and `--model` of `create`, `update`, `close`, `reopen`, `defer` and `undefer` fall back to the environment variables `BR_AGENT_NAME`, `BR_HARNESS` and `BR_MODEL`. They are recorded on the audit event only and never gate anything; no captured golden sets them, and none of their values reaches `issues.jsonl`. | `src/cli/mod.rs:1161`–`src/cli/mod.rs:1170` | (case to add: `env_attribution_create`) |
| S1.67 | An id written into `.beads/last-touched` by a mutating command, and the `BEADS_FLUSH_DEBOUNCE`, `BEADS_IDENTITY`, `BEADS_REMOTE_SYNC_INTERVAL` and `BEADS_AUTO_START_DAEMON` variables, complete the environment surface; the last four feed configuration keys that no in-scope command reads under `--no-db`. | `src/config/mod.rs:6028`–`src/config/mod.rs:6041` | update_last_touched, scn_last_touched |

### S1.68 The argument parser, measured (amendment 2026-09-20)

Every clause below was written from captured output, not from the source: the provenance column names
the golden line that shows it. Where a clause states something no golden shows, it says so and names the
case to add.

| S1.n | clause | provenance | cases |
|---|---|---|---|
| S1.68 | An unknown short option is refused as `error: unexpected argument '-z' found`. The message names the offending letter alone even when it sits inside a cluster (`-az` reports `-z`, although `-a` is a valid option of `list`). There is never a similarity tip, and the usage line is always the `[OPTIONS]` form of S1.70, whatever else is present. The `--` advice of S1.19 is printed exactly when the level takes positionals. A negative number is an unknown short option: `list --limit -1` reports `-1`. | `goldens/usage_unknown_short.err:1`, `goldens/usage_unknown_short_in_cluster.err:1`, `goldens/usage_value_negative.err:1` | usage_unknown_short, usage_unknown_short_in_cluster, usage_value_negative |
| S1.69 | Short options combine into one word. A letter whose option takes no value lets the cluster continue. A letter whose option takes a value ends the cluster: the rest of the word is its value (`-p1` is `--priority 1`; one leading `=` is dropped), and when nothing follows the letter the next word is its value (`-rs open` is `--reverse --status open`). A counted flag repeats inside one cluster (`-vv`). | `goldens/usage_short_combined.out:1`, `goldens/usage_short_attached_value.out:1`, `goldens/usage_verbose_counted.out:1` | usage_short_combined, usage_short_attached_value, usage_verbose_counted |
| S1.70 | The usage line of a refusal is `Usage: br`, then the words that lead to the level (`list`, `dep add`), then EITHER ` [OPTIONS]` OR the options present at this level, in first-seen order, each spelled as in S1.72; then every positional of the level (S1.71); then ` <COMMAND>` when a subcommand must follow. `[OPTIONS]` stands exactly when the refusal names no present argument. Refusals that name the present arguments: an unknown long option (the present ones, then the suggested one of S1.81); an unexpected value (S1.75: the present ones and the flag itself); an exclusion (S1.74: the present ones except the second of the pair); missing positionals (S1.24: the present ones, then the missing ones). Refusals that never name any: a repeated option (S1.12), an unknown short option (S1.68), an unexpected positional (S1.73), an unrecognized subcommand (S1.17) and a missing subcommand (S1.16). "Present" counts this level only: a global given before the command is not present at the command's level, and at the top level the contract's own `--no-db` is present (`Usage: br --no-db <COMMAND>`). A value whose option was refused or is still waiting is not present (`list --limit --zzqq` prints `Usage: br list [OPTIONS]`). The `[OPTIONS]` form of every in-scope command is captured by the 31 `usage_badflag_*` cases. | `goldens/usage_unknown_flag_top_plain.err:3`, `goldens/usage_unknown_flag_after_used.err:3`, `goldens/usage_value_pending_then_unknown.err:3`, `goldens/usage_badflag_dep_add.err:5` | usage_unknown_flag_top_plain, usage_unknown_flag_after_used, usage_value_pending_then_unknown, usage_badflag_create, usage_badflag_q, usage_badflag_update, usage_badflag_close, usage_badflag_reopen, usage_badflag_defer, usage_badflag_undefer, usage_badflag_delete, usage_badflag_show, usage_badflag_list, usage_badflag_search, usage_badflag_count, usage_badflag_stats, usage_badflag_ready, usage_badflag_blocked, usage_badflag_dep_add, usage_badflag_dep_remove, usage_badflag_dep_list, usage_badflag_dep_tree, usage_badflag_dep_cycles, usage_badflag_label_add, usage_badflag_label_remove, usage_badflag_label_list, usage_badflag_label_list_all, usage_badflag_label_rename, usage_badflag_comments_add, usage_badflag_comments_list, usage_badflag_epic_status, usage_badflag_epic_close_eligible, usage_badflag_where, usage_badflag_version |
| S1.71 | A positional is printed on the usage line as `<NAME>` when it is required or present and as `[NAME]` otherwise, followed by `...` when it collects every remaining word: `br show [OPTIONS] [IDS]...` with no id, `br show --json <IDS>...` with one; `br comments add <ID> [TEXT]...`; `br search --json <QUERY>`; `br label rename <OLD_NAME> <NEW_NAME>`. The positionals per command: `create [TITLE]`; `q [TITLE]...`; `update`, `close`, `reopen`, `defer`, `undefer`, `delete`, `show` `[IDS]...`; `search <QUERY>`; `dep add` and `dep remove` `<ISSUE> <DEPENDS_ON>`; `dep list` and `dep tree` `<ISSUE>`; `label add` and `label remove` `[ISSUES]...`; `label list [ISSUE]`; `label rename <OLD_NAME> <NEW_NAME>`; `comments add <ID> [TEXT]...`; `comments list <ID>`; every other in-scope command takes none. | `goldens/usage_unknown_flag_used_positional.err:5`, `goldens/usage_missing_comments_add.err:4`, `goldens/usage_missing_with_used_flag.err:4`, `goldens/usage_missing_rename.err:4`, `goldens/usage_missing_two.err:5` | usage_unknown_flag_used_positional, usage_missing_comments_add, usage_missing_with_used_flag, usage_missing_rename, usage_missing_two |
| S1.72 | An option is spelled in messages by its long name: `--json` when it takes no value, `--limit <LIMIT>` when it takes one, `--assignee [<ASSIGNEE>]` when its value may be left out (`ready`). The placeholder is the long name in upper case with `-` written `_` (`--priority-min <PRIORITY_MIN>`, `--lock-timeout <LOCK_TIMEOUT>`), with these exceptions: `create --title <TITLE_FLAG>` (this amends S1.36, which printed `<TITLE>`), `--description-file <PATH>`, `--check-acceptance <ITEMS>`, `--uncheck-acceptance <ITEMS>`, `--add-acceptance <TEXT>`, `--append-notes <TEXT>`, `--transition-comment <COMMENT>`, `--agent-name <NAME>`, `--bypass-reason <REASON>`. A positional is spelled `[TITLE]` in a message when it is optional. Amended (2026-09-26, round 22): a counting flag (no value, repeatable) is spelled with `...`: `Usage: br list --verbose...`. | `goldens/usage_conflict_assignee_unassigned.err:1`, `goldens/usage_conflict_title.err:1`, `goldens/usage_u8_range.err:1` | usage_conflict_assignee_unassigned, usage_conflict_title, usage_u8_range, r22_g_list_version, usage_list_counting_flag |
| S1.73 | A word the level has no positional for is refused as `error: unexpected argument 'extra' found`, with no tip and the `[OPTIONS]` usage line: `list extra` (no positionals at all) and `create a b` (one optional positional, already filled). | `goldens/usage_unexpected_positional.err:1`, `goldens/usage_second_positional.err:3` | usage_unexpected_positional, usage_second_positional |
| S1.74 | Two arguments that exclude each other are refused after the whole argv was read and before missing positionals are judged: `error: the argument 'X' cannot be used with 'Y'`, where X is the first present argument, in argv order, that has a present partner, and Y is that partner; the usage line names the present arguments except Y (S1.70). The pairs: `show --wrap` / `--no-wrap`; `delete --force` / `--cascade`; `create [TITLE]` / `--title`; `create` and `update` `--description` / `--description-file`; `update --acceptance-criteria` / each of `--check-acceptance`, `--uncheck-acceptance`, `--add-acceptance`; `update --notes` / `--append-notes`; `ready --assignee` / `--unassigned`; `ready --epic` / `--parent`. The order is argv order, not declaration order: `show proj-170 --no-wrap --wrap` names `--no-wrap` first (OQ-109, resolved). | `goldens/usage_conflict_wrap_reversed.err:1`, `goldens/usage_conflict_wrap.err:1`, `goldens/usage_conflict_force_cascade.err:1`, `goldens/usage_conflict_title.err:3`, `goldens/usage_conflict_assignee_unassigned.err:3` | usage_conflict_wrap, usage_conflict_wrap_reversed, usage_conflict_force_cascade, usage_conflict_title, usage_conflict_assignee_unassigned |
| S1.75 | A value attached to an option that takes none is refused: `list --json=true` gives `error: unexpected value 'true' for '--json' found; no more were expected` LF LF `Usage: br list --json` LF LF `For more information, try '--help'.` LF, exit `2`. | `goldens/usage_flag_with_value.err:1` | usage_flag_with_value |
| S1.76 | An attached empty value is a value: `list --limit=` is refused as `error: invalid value '' for '--limit <LIMIT>': cannot parse integer from empty string`, not as a missing value (S1.20). | `goldens/usage_value_eq_empty.err:1` | usage_value_eq_empty |
| S1.77 | The integers the argument parser itself checks. Unsigned 64-bit (`--limit`, `--offset`, `--max-depth`, `--lock-timeout`): one optional `+`, then at least one ASCII digit and nothing else, else `invalid digit found in string` (a `-` is not a digit here); the empty string is S1.76; above 18446744073709551615 the reason is `number too large to fit in target type`; `--limit +1` is `--limit 1`. Ranged (`--priority-min` and `--priority-max` 0..=255; `--activity-hours` 0..=4294967295; `--estimate` -2147483648..=2147483647): the value is first read as a signed 64-bit integer (one optional `+` or `-`, digits; `invalid digit found in string`, `cannot parse integer from empty string`, `number too large to fit in target type`, `number too small to fit in target type`), then held to the range with the reason `N is not in LO..=HI`, N printed without `+` and without leading zeros. `1.5` is not an integer: `create T -e 1.5` is refused with `invalid digit found in string`; `create T -e 2147483648` with `2147483648 is not in -2147483648..=2147483647`. A negative value cannot be given as the next word at all, because it reads as a short option (S1.68): `create T -e -9223372036854775809` reports `unexpected argument '-9' found` with the `--` advice. | `goldens/usage_u8_range.err:1`, `goldens/usage_lock_timeout_bad.err:1`, `goldens/usage_estimate_bad.err:1`, `goldens/usage_limit_overflow.err:1`, `goldens/usage_limit_plus.out:1`, `goldens/usage_estimate_range.err:1`, `goldens/usage_estimate_i64_overflow.err:1` | usage_u8_range, usage_lock_timeout_bad, usage_estimate_bad, usage_limit_bad, usage_limit_overflow, usage_limit_plus, usage_estimate_range, usage_estimate_i64_overflow |
| S1.78 | The options whose value is one of a fixed list, and the list as printed: `count --by` `status, priority, type, assignee, label`; `dep list --direction` `down, up, both`; `list --format` (and `search`, which shares `list`'s options) `text, json, csv, toon`; `ready --sort` `hybrid, priority, oldest`; `--format` on `show`, `stats`, `ready`, `blocked` and `dep list` `text, json, toon` (no `csv`); `dep tree --direction` `down, up, both` (OQ-104, resolved). | `goldens/usage_enum_by.err:2`, `goldens/usage_enum_direction.err:2`, `goldens/usage_enum_format.err:2`, `goldens/usage_enum_ready_sort.err:2`, `goldens/usage_enum_format_show.err:2`, `goldens/usage_enum_tree_direction.err:2` | usage_enum_by, usage_enum_direction, usage_enum_format, usage_enum_ready_sort, usage_enum_format_show, usage_enum_format_stats, usage_enum_format_ready, usage_enum_format_blocked, usage_enum_format_dep_list, usage_enum_tree_direction, usage_enum_direction_dep_list |
| S1.79 | A refused enumerated value that is similar (S1.80) to a listed one earns a tip after the list: `count --by statu` prints `error: invalid value 'statu' for '--by <BY>'` LF `  [possible values: status, priority, type, assignee, label]` LF LF `  tip: a similar value exists: 'status'` LF LF `For more information, try '--help'.` LF (OQ-105, resolved). | `goldens/usage_enum_similar_value.err:4` | usage_enum_similar_value |
| S1.80 | Similarity between a typed word and a candidate is the Jaro similarity over code points. With lengths la and lb, the window radius is max(la, lb) / 2 − 1 (integer division, never below 0). Each code point of the typed word, left to right, is matched to the first not yet matched equal code point of the candidate whose index differs from its own by at most the radius. With m matches, and t half (integer division) the number of positions at which the two matched sequences, each read in its own order, differ, the similarity is (m/la + m/lb + (m − t)/m) / 3, and 0 when m is 0. A candidate is similar when its similarity is above 0.7. Measured: `lst` is similar to `lint` (2 matches: 0.7222) and to `list` (3 matches: 0.9167) and to no other top-level word. The original computes in 64-bit floating point; the port compares exact fractions, which can differ only for a similarity within one rounding step of 0.7 (OQ-106). Amended (2026-09-24, OQ-106): an exact similarity of 0.7 IS similar in the original, because `(m/la + m/lb + (m−t)/m) / 3.0` in binary64 rounds to 0.7000000000000001 (`count --by staqq` offers `status`: la 5, lb 6, m 3, t 0; `count --by labxyz` offers `label`). Enumerating every feasible tuple (m ≤ lb ≤ 19, the longest name being `acceptance-criteria`; la ≤ 10m, since m/la must reach 0.1; t ≤ m/2) finds 57 exact ties, of which three round DOWN to 0.6999999999999998 and are not similar: (la, lb, m, t) = (25, 10, 10, 3), (60, 12, 11, 0), (30, 15, 11, 0) (arithmetic, not captured: those need typed words of 25 to 60 code points). | `goldens/usage_similar_subcommand.err:3` | usage_similar_subcommand, usage_enum_tie_similar, usage_enum_tie_similar_label |
| S1.81 | An unknown long option is compared (S1.80) with every long name the level answers to, in declaration order: its own options with their aliases, the twelve globals, `help`, and at the top level `version`. The most similar one is suggested on a tip line, `  tip: a similar argument exists: '--json'`, and named on the usage line after the present arguments (S1.70); when several are equally similar the last in declaration order is suggested (not captured: OQ-107). A suggestion replaces the `--` advice of S1.19. The name compared is the text between `--` and the first `=`. Measured (2026-09-23, OQ-107): `list --lon` suggests `--long` (`usage_similar_lon`). | `goldens/usage_similar_flag_in_cmd.err:3`, `goldens/usage_unknown_flag.err:3`, `goldens/usage_unknown_flag_used_positional.err:3` | usage_similar_flag_in_cmd, usage_unknown_flag, usage_unknown_flag_used_positional, usage_similar_lon |
| S1.82 | A word that is no subcommand of a level that requires one is compared (S1.80) with every subcommand word of the level, aliases and `help` included, in the order of S1.16. Every similar word is listed in ascending similarity, equal ones in declaration order: one word prints `  tip: a similar subcommand exists: 'where'`, several print `  tip: some similar subcommands exist: 'lint', 'list'`; none prints no tip. The same holds inside a group (`dep ad` suggests `add`; `dep frob` suggests nothing, with `Usage: br dep [OPTIONS] <COMMAND>`). | `goldens/usage_similar_subcommand_one.err:3`, `goldens/usage_similar_subcommand.err:3`, `goldens/usage_similar_sub_in_group.err:3`, `goldens/usage_dep_unknown_sub.err:3` | usage_similar_subcommand_one, usage_similar_subcommand, usage_similar_sub_in_group, usage_dep_unknown_sub |
| S1.83 | A group (`dep`, `label`, `epic`) given options but no subcommand is not the bare group of S1.25: `dep --json` prints `error: 'br dep' requires a subcommand but one was not provided` LF `  [subcommands: add, import, remove, rm, list, tree, cycles, help]` LF LF `Usage: br dep [OPTIONS] <COMMAND>` LF LF `For more information, try '--help'.` LF, exit `2`. The list names the alias `rm` after its command, and the usage line is the `[OPTIONS]` form although `--json` is present (OQ-108, resolved). | `goldens/usage_dep_flag_only.err:1` | usage_dep_flag_only |
| S1.84 | Refusals are reported in argv order, with one exception. A value given as the NEXT word (`--limit x`) is judged only when the following argument is read, or at the end of argv; a value attached with `=` or inside a short cluster is judged at once. When the following argument is itself unknown (an unknown long or short option, or a word the level has no place for), that argument's refusal wins and the value's refusal is never reported: `list --limit --zzqq` reports `--zzqq`, and `list --limit -1` reports `-1`. When the following argument is known, the waiting refusal is reported first: `list --limit --json` reports `a value is required for '--limit <LIMIT>' but none was supplied`. Amended (2026-09-26, round 22): the option whose next-word value is held counts as named for a later refusal's usage line (`list --limit x --zzz` prints `Usage: br list --limit <LIMIT>`). | `goldens/usage_value_pending_then_flag.err:1`, `goldens/usage_value_pending_then_unknown.err:1`, `goldens/usage_value_negative.err:1` | usage_value_pending_then_flag, usage_value_pending_then_unknown, usage_value_negative, usage_limit_noval, r22_g_list_unknown_after_bad, usage_list_held_value_then_unknown |
| S1.85 | The once-only rule of S1.12 counts per level: `--json count --json` is accepted and prints `{"count":6}` on `@fx=basic`. An option given before the command, after it, or after a positional reaches the command alike, and `--limit=1` equals `--limit 1`. `show -- proj-170` equals `show proj-170`. | `goldens/usage_global_twice_across_levels.out:1`, `goldens/usage_global_before_cmd.out:1`, `goldens/usage_flag_after_positional.out:1`, `goldens/usage_value_eq_form.out:1`, `goldens/usage_double_dash_positional.out:1` | usage_global_twice_across_levels, usage_global_before_cmd, usage_flag_after_positional, usage_value_eq_form, usage_double_dash_positional |
| S1.86 | Commands the argument parser lets through with nothing to act on are refused by the command, exit `4`, stderr only: `comments` alone prints `Error: Validation failed: id: missing issue id`; `q` alone prints `Error: Validation failed: title: cannot be empty`. A `-` alone is a positional, not an option: `show -` on `@fx=basic` prints `Error: Issue not found: -` LF `Hint: Run 'br list' to see available issues.` LF, exit `3`. At `comments`, which takes both a positional and subcommands, a word that is not exactly `add`, `list` or `help` is an issue id, however similar it is: `comments lis` on `@fx=basic` prints `Error: Issue not found: lis` and its hint, exit `3`. | `goldens/usage_comments_alone.err:1`, `goldens/usage_q_alone.err:1`, `goldens/usage_dash_alone_positional.err:1`, `goldens/usage_comments_similar_sub.err:1` | usage_comments_alone, usage_q_alone, usage_dash_alone_positional, usage_comments_similar_sub |
| S1.87 | An option declared to accept a leading `-` takes the next word as its value whatever it looks like: `create T -d --not-a-flag` stores `"description":"--not-a-flag"`, and `close proj-5u2 -r -x` stores `"close_reason":"-x"` and prints `✓ Closed proj-5u2: Write API docs (-x)`. `-d` has the same property on `q` and on `update` (`update proj-mta -d -x` reaches the command, which then refuses the overwrite, exit `4`; OQ-112, resolved for `-d`). Every other option reads a next word that begins with `-` as an option (S1.84). Correction (2026-09-22, measured by running each option with `-x`): the options that take a leading `-` are also `update --design`, `--acceptance-criteria` (alias `--acceptance`), `--prerequisites`, `--notes`, `--append-notes`, `--check-acceptance`, `--uncheck-acceptance`, `--add-acceptance` and `--transition-comment`; `create --acceptance-criteria` and `--prerequisites`; `--transition-comment` of `close`, `defer`, `undefer` and `epic close-eligible`; `comments add -m`. Refused as an option word: `--title`, `--assignee`, `--owner`, `--external-ref`, `--estimate`, `--due`, `--defer`, `--source-repo`, `--parent`, `--status`, `--type`, `--priority`, `create --labels`, `comments add --author` (`usage_hyphen_value_title`, `usage_hyphen_value_create_assignee`). | `goldens/usage_hyphen_value_description.out:3`, `goldens/usage_hyphen_value_reason.out:1`, `goldens/usage_update_description_dash.err:1`, `goldens/usage_q_description_dash.out:1` | usage_hyphen_value_description, usage_hyphen_value_reason, usage_update_description_dash, usage_q_description_dash, usage_hyphen_value_design, usage_hyphen_value_notes, usage_hyphen_value_acceptance, usage_hyphen_value_add_acceptance, usage_hyphen_value_transition, usage_hyphen_value_title, usage_hyphen_value_create_assignee |
| S1.88 | Aliases are exact second names: `create --body` is `--description`; the command `status` is `stats`; `dep rm` is `dep remove`. | `goldens/usage_alias_body.out:3`, `goldens/usage_alias_status_cmd.out:1`, `goldens/usage_alias_dep_rm.out:1` | usage_alias_body, usage_alias_status_cmd, usage_alias_dep_rm |
| S1.89 | `--help` at the top level and the `help` subcommand print the same 3604 bytes on stdout, exit `0`. `list --help` prints the long form (3698 bytes) and `list -h` the short form (3172 bytes). The texts the port carries are reproduced verbatim in the appendix `docs/spec-parts/HELP_TEXTS.md`, which is part of this clause; the help text of every other level is outside the port's surface (PLAN §3). A help option stops the parse where it stands: `list --help --zzqq` prints the help and exits `0`, while `list --zzqq --help` is the refusal of `--zzqq`. `help list` prints `list`'s long help (OQ-110, resolved). Amended 2026-09-22: the appendix carries the `--help` and `-h` texts of every command level (72 texts; each block names its golden), and `port/core/help.bend` routes all of them. Amended (2026-09-26, round 22): after `help` (at any level) a word that is not a subcommand of the level, an option included, is refused as `error: unrecognized subcommand '<word>'` with the level's usage line and no tip, exit 2 (`help frob`, `help --json`, `help list extra`, `dep help frob`); `help help` prints `Print this message or the help of the given subcommand(s)` and `Usage: br help [COMMAND]...` (`br dep help [COMMAND]...` below `dep`); `status` is an alias command whose help is `stats`' with `Alias for stats` as its first line and `Usage: br status [OPTIONS]`. | `goldens/usage_help_flag_top.out:1`, `goldens/usage_help_subcommand.out:1`, `goldens/usage_help_list.out:1`, `goldens/usage_help_short_list.out:1`, `goldens/usage_help_subcommand_list.out:1`, `goldens/usage_help_before_bad.out:1`, `goldens/usage_help_after_bad.err:1` | usage_help_flag_top, usage_help_subcommand, usage_help_list, usage_help_short_list, usage_help_subcommand_list, usage_help_before_bad, usage_help_after_bad, usage_help_create, usage_help_short_create, usage_help_q, usage_help_short_q, usage_help_update, usage_help_short_update, usage_help_close, usage_help_short_close, usage_help_reopen, usage_help_short_reopen, usage_help_defer, usage_help_short_defer, usage_help_undefer, usage_help_short_undefer, usage_help_delete, usage_help_short_delete, usage_help_show, usage_help_short_show, usage_help_search, usage_help_short_search, usage_help_count, usage_help_short_count, usage_help_stats, usage_help_short_stats, usage_help_ready, usage_help_short_ready, usage_help_blocked, usage_help_short_blocked, usage_help_where, usage_help_short_where, usage_help_version, usage_help_short_version, usage_help_dep_add, usage_help_short_dep_add, usage_help_dep_remove, usage_help_short_dep_remove, usage_help_dep_list, usage_help_short_dep_list, usage_help_dep_tree, usage_help_short_dep_tree, usage_help_dep_cycles, usage_help_short_dep_cycles, usage_help_label_add, usage_help_short_label_add, usage_help_label_remove, usage_help_short_label_remove, usage_help_label_list, usage_help_short_label_list, usage_help_label_list_all, usage_help_short_label_list_all, usage_help_label_rename, usage_help_short_label_rename, usage_help_comments, usage_help_short_comments, usage_help_comments_add, usage_help_short_comments_add, usage_help_comments_list, usage_help_short_comments_list, usage_help_epic_status, usage_help_short_epic_status, usage_help_epic_close_eligible, usage_help_short_epic_close_eligible, usage_help_short_dep, usage_help_short_label, usage_help_short_epic, usage_help_short_top, r22_h_help_dep, r22_h_help_dep_add, r22_h_help_label_rename, r22_h_help_comments_add, r22_h_help_epic_ce, r22_h_dep_help, r22_h_dep_help_add, r22_h_label_help_listall, r22_h_epic_help_status, r22_h_comments_help, r22_h_help_frob, r22_h_help_list_extra, r22_h_help_help, r22_h_help_json, r22_h_dep_help_frob, r22_h_help_status_alias, r22_h_help_dep_rm, usage_help_unknown_word, usage_help_option_word, usage_help_list_extra, usage_dep_help_unknown, usage_help_dep_unknown, usage_help_list_help_flag, help_help, help_dep_help, help_status_alias, help_status_flag, stats_status_alias |
| S1.90 | A priority the argument parser accepts as a free string is judged by the command: `list -p nine` on `@fx=basic` exits `4` with `Error: Priority must be 0-4, got: NINE` LF `Hint: Priority must be 0-4 or P0-P4 (0=critical, 4=backlog); list filters also accept ranges and comma lists such as 0-1 or 0,2.` LF on stderr; the message quotes the value in upper case. | `goldens/usage_priority_bad_value.err:1` | usage_priority_bad_value |
| S1.91 | The once-only rule of S1.12 holds for an option with a value, spelled with its placeholder: `list --limit 1 --limit 2` prints `error: the argument '--limit <LIMIT>' cannot be used multiple times` and the `[OPTIONS]` usage line (OQ-113, resolved). At the top level the usage line is `Usage: br [OPTIONS] <COMMAND>` (`--json --json list`). `--no-db` given again after the command is accepted (`count --no-db`). `count` has no short options: `count -p 1` is refused as `unexpected argument '-p'`. | `goldens/usage_limit_twice.err:1`, `goldens/usage_top_json_twice.err:3`, `goldens/usage_no_db_explicit_sub.out:1`, `goldens/usage_count_short_p.err:1` | usage_limit_twice, usage_top_json_twice, usage_no_db_explicit_sub, usage_count_short_p |
| S1.92 | `--version` (and `-V`) at the top level prints `br 0.6.0` LF on stdout and exits `0`. The port prints its own name and version there (DISC-006). An equally similar pair of long names is not captured: `list --lon` suggests `--long`, which is simply the most similar (OQ-107 stays open). | `goldens/usage_version_flag.out:1`, `goldens/usage_similar_flag_tie.err:3` | usage_version_flag, usage_similar_flag_tie |



## S2. Inputs

<!-- from docs/spec-parts/S2_S5a.md -->

### S2.A Workspace discovery under `--no-db`

| S2.n | clause | provenance | cases |
|---|---|---|---|
| S2.1 | Every in-scope command resolves the workspace directory before anything else, and `--no-db` does not change the resolution — it only changes what is opened afterwards. The order is: (1) `--db <path>` when that path has a `.beads` or `_beads` component, that component being the workspace; (2) the first non-empty of the environment variables `BD_DB`, `BD_DATABASE`, `BEADS_DB`, in that order, treated the same way; (3) the environment variable `BEADS_DIR` when it does not trim to empty; (4) the upward walk of S2.2. Steps 1–3 short-circuit the walk entirely. No `BR_`-prefixed variable takes part in discovery Amended (2026-09-26, OQ-137): measured. With a workspace found, `BD_DATABASE` and `BEADS_DB` change nothing; `--db` (resolved against the working directory) outranks `BD_DB` (resolved against the workspace directory, so `BD_DB=x.db` is `<dir>/x.db`). When the override's directory is the workspace directory the command runs normally and `where` prints `  database: <dir>/<the override's file name>` (plus `  (via redirect from <dir>)` for a relative `--db` that names `.beads` by a component). When it is any other directory the original reads `<that directory>/issues.jsonl`, with results that differ by command; the port answers its own not-ported refusal there (PLAN §3, excluded row "database override outside the workspace"). | `src/config/mod.rs:502`, `:572`, `:679`, `:690`, `:284` | where_plain, where_json, count_db_override_workspace, count_bd_db_workspace, count_bd_db_relative, where_db_override_workspace, count_db_override_over_bd_db, count_bd_database_ignored, count_beads_db_ignored, where_bd_db_relative, where_bd_db_json, where_db_override_json, count_db_override_dot, where_db_override_dot |
| S2.2 | The walk starts at the process working directory. At each level it tests `<dir>/.beads` first and `<dir>/_beads` second and accepts the first that is a directory; it then moves to the parent and repeats until the parent cannot be popped, that is, all the way to the filesystem root. There is no git-root stop, no `$HOME` stop and no required marker file: a bare `.beads/` directory with nothing in it is accepted. A working directory that cannot be read is an I/O failure, not a "not initialized" failure Measured outside the sandbox (2026-09-22; the sandbox always runs at the workspace root): from `myrepo/src/deep` the original finds `myrepo/.beads` and a `create` writes there, with `source_repo` `myrepo` and `source_repo_path` the directory that holds `.beads`; a bare `_beads` workspace is found the same way and `where` reports `<root>/_beads`; with no workspace up to the root the answer is S2.8. | `src/config/mod.rs:300`, `:309`, `:57` Captured (2026-09-24, OQ-103, harness mode `@cwd`): from `sub/` and `sub/dir/` the walk finds `/mnt/proj/.beads`, and a record created there carries `source_repo_path` `/mnt/proj`. | where_plain, create_min, list_plain, where_from_subdir, list_from_subdir, create_from_subdir |
| S2.3 | The only two recognized directory names are `.beads` and `_beads`, compared byte for byte against the final path component | `src/config/mod.rs:57` | where_plain |
| S2.4 | `BEADS_DIR` names the workspace directory directly. It is accepted on the single condition that the path is a directory; it is not required to be named `.beads` or `_beads` Measured outside the sandbox (2026-09-22): `BEADS_DIR=<root>/.beads` from an unrelated directory creates in that workspace with `source_repo_path` `<root>`, and `where` reports the `BEADS_DIR` path. Amended (2026-09-25, round 21): the value is used as given (never trimmed: `" /mnt/proj/.beads "` is not found, and the message quotes it as given), made absolute against the working directory and read component by component (a trailing `/`, a doubled `/` and a `.` component are dropped; `..` is left to the file system). It must be an existing directory (a regular file is `BEADS_DIR not found or not a .beads directory`), named `.beads` or `_beads`: any other directory is `Redirect target must be a .beads or _beads directory: <dir>`, wrapped as `BEADS_DIR is invalid: Configuration error: …` except under `where`. A relative value makes `where` print `  (via redirect from <dir>)` after the first line (`redirected_from` after `path` under `--json`). | `src/config/mod.rs:690`, `:712` | (case to add: `env_beads_dir` — needs a ws-run mode that adds one environment variable to the cleared set), r21_ws_beadsdir_trailing_slash, r21_ws_beadsdir_relative, r21_ws_beadsdir_projroot, r21_ws_beadsdir_file, r21_ws_beadsdir_none_fx, r21_ws_beadsdir_create, where_beads_dir_double_slash, where_beads_dir_dot_component, where_beads_dir_trailing_dot, where_beads_dir_dot_relative, where_beads_dir_relative_json, count_beads_dir_relative, error_beads_dir_dotdot_missing, error_where_beads_dir_projroot, error_beads_dir_projroot_json, error_where_beads_dir_file, error_beads_dir_padded, error_beads_dir_missing_parent, error_redirect_rel_beads_dir_count |
| S2.5 | A `redirect` file inside the resolved directory relocates the workspace. It must be a regular file of at most 4096 bytes; the whole content is UTF-8 and is trimmed as one string, never split into lines. Content that is empty after trimming is ignored and the original directory is kept. An absolute target is used as given; a relative target resolves against the `.beads` directory itself, so `.` stays put. Each hop is canonicalized and followed again, at most ten hops Measured (2026-09-24, OQ-023, fixture sidecars `<name>.redirect`): a target that exists is used normalized (`../.beads` from `/mnt/proj/.beads` is `/mnt/proj/.beads` in `where`); surrounding blanks are trimmed; a blank file is ignored; a redirect to its own directory answers normally. A target that does not exist refuses every command that reads the workspace (not `version`, not `--help`): `Configuration error: Redirect target not found: <target as joined, not normalized>` (`/mnt/proj/.beads/../proj/.beads`), CONFIG_ERROR, exit 7, `context` null; under `BEADS_DIR` the message becomes `BEADS_DIR is invalid: Configuration error: Redirect target not found: …` with `context` `{"wrapper_context": "BEADS_DIR is invalid"}`. A file over 4096 bytes is refused as `Configuration error: Redirect file exceeds maximum size of 4096 bytes: <dir>/redirect`, exit 7, not wrapped under `BEADS_DIR`. Chains of several directories are not reachable in the one-directory sandbox; the port follows at most ten hops as stated. Amended (2026-09-25, round 21): under `where` a redirect failure is never wrapped with `BEADS_DIR is invalid:`. | `src/config/routing.rs:189`, `:204`, `:222`, `:256`, `:28` | redirect_self_where, redirect_blank_count, redirect_missing_count, redirect_missing_json, redirect_missing_create, redirect_missing_version, redirect_missing_beads_dir, redirect_missing_beads_dir_json, redirect_relative_where, redirect_parent_rel_where_json, redirect_parent_rel_list, redirect_padded_where, redirect_oversize_where, redirect_oversize_json, redirect_oversize_beads_dir, r21_ws_redirect_beadsdir_rel |
| S2.6 | The redirect chain refuses, each as a configuration error on stderr in Plain mode and as the envelope of S5.21 in JSON mode: an over-long file with `Redirect file exceeds maximum size of 4096 bytes: <path>`; non-UTF-8 content with `Redirect file must be valid UTF-8: <path>: <io error>`; a repeat of an already-visited directory with `Redirect loop detected: <current> -> <next>`; more than ten hops with `Redirect chain exceeds max depth (10): <start>`; a target that does not exist with `Redirect target not found: <path>`; a target named neither `.beads` nor `_beads` with `Redirect target must be a .beads or _beads directory: <path>` | `src/config/routing.rs:222`, `:241`, `:256`, `:290`, `:300` | (case to add: `error_redirect_loop`, `error_redirect_missing_target`) |
| S2.7 | When the workspace is found and the enclosing tree's `.git` is a *file* (a linked git worktree marker), and that directory has neither a `redirect` file nor the database file named by `metadata.json`'s `database` key, the workspace resolves instead to the primary checkout's `.beads`. The primary tree is derived by reading `gitdir:` from the `.git` file and then the `commondir` file beside it, with a fallback that strips a trailing `worktrees/<name>`; a common directory not named `.git` yields no relocation. This is the only way `metadata.json` participates in discovery | `src/config/mod.rs:385`, `:412`, `:435` | (case to add: `worktree_linked` — needs a ws-run fixture that plants a `.git` file) |
| S2.8 | When discovery finds nothing the failure is `NOT_INITIALIZED`, exit code **2**. Plain mode writes to stderr, each line with a trailing newline, exactly `Error: Beads not initialized: run 'br init' first` then `Hint: Run: br init`. JSON mode writes the envelope of S5.21 to stdout with `"code": "NOT_INITIALIZED"`, `"message": "Beads not initialized: run 'br init' first"`, `"hint": "Run: br init"`, `"retryable": false`, `"context": null`. Read commands and `create` behave identically, because both sit in the auto-import command set whose startup failure is returned unchanged | `src/error/mod.rs:242`, `:444`, `src/error/structured.rs:512`, `:1347`, `src/main.rs:71`, `:1617` | error_no_workspace_list, error_no_workspace_list_json, error_no_workspace_create |
| S2.9 | When discovery fails while an explicit `--db` or `BD_DB`-family override was given, the message gains a prefix and becomes `Cannot resolve the project .beads directory for database override '<path>'; run from the target workspace or set BEADS_DIR: Beads not initialized: run 'br init' first`, with the same code and exit code as S2.8 Amended (2026-09-26, OQ-137): measured. The override named is `--db`, else the first non-empty of `BD_DB`, `BD_DATABASE`, `BEADS_DB`, as given (`BD_DB=" "` is named; an empty one is not); the code is NOT_INITIALIZED, the hint `Run: br init`, and the JSON context `{"wrapper_context": "Cannot resolve the project .beads directory for database override '<path>'; run from the target workspace or set BEADS_DIR"}`. An override with a `.beads` component whose directory does not exist is instead `Configuration error: database override '<path>' not found or not a .beads directory: <that .beads directory>` (CONFIG_ERROR, exit 7). | `src/config/mod.rs:614`, `:662`, `src/error/mod.rs:263` | error_no_workspace_db_override, error_no_workspace_bd_db_relative, error_db_override_missing_beads_json, where_no_workspace_db_override, error_no_workspace_bd_db_empty, error_no_workspace_bd_database, error_no_workspace_bd_db_space, error_no_workspace_db_override_json, error_no_workspace_bd_db_beads_missing, error_no_workspace_bd_database_first, error_no_workspace_bd_db_first, error_no_workspace_db_flag_first |

### S2.B The issue-id prefix

| S2.n | clause | provenance | cases |
|---|---|---|---|
| S2.10 | Under `--no-db` the prefix that new ids use is resolved once at startup, in this order: (1) the merged configuration value under `issue_prefix`, `issue-prefix` or `prefix` — key lookup lower-cases and maps `_` to `-`, so `Issue_Prefix` matches — normalized by S2.13 and fatal when it normalizes to empty; (2) when a store file exists, the prefix of the first usable record in it (S2.11); (3) the sanitized, abbreviated name of the directory that contains `.beads` (S2.12); (4) the literal `br` Measured (2026-09-22): a nested key does not count (`sync:` then `  prefix: zz` leaves the directory rule in force, `prefix_config_nested`); with both `issue_prefix` and `prefix` configured the original chose between them at random, 5 of 8 runs `prefix` and 3 of 8 `issue_prefix` (`prefix_config_both` is UNSTABLE on the floor; DISC-010). | `src/config/mod.rs:5677`, `:5703`, `:5396` | where_plain, where_json, create_min, q_basic, prefix_config_create, prefix_config_create_json, prefix_config_nested, prefix_config_both, prefix_store_create, prefix_store_q |
| S2.11 | Scanning the store for a prefix walks the lines in file order and skips a blank line, a line that is not JSON, a record whose `status` is `tombstone`, a record without a string `id`, and an `id` that does not split. The prefix is everything before the **last** `-` in the id, so `document-intelligence-0sa` yields `document-intelligence`. The first line that survives wins | `src/config/mod.rs:5826`, `src/util/id.rs:356` | edge_precision_list_json, list_json, prefix_store_create, prefix_store_q, prefix_store_show_partial |
| S2.12 | Deriving a prefix from a directory name applies S2.13 first, then: a result of six characters or fewer is used unchanged; otherwise the name is split on every non-alphanumeric ASCII character and, when two or more non-empty segments remain and their initials form more than one character, those initials are the prefix (`beads_bend` → `bb`); otherwise the first three ASCII-alphanumeric characters are the prefix (`beadsrustproject` → `bea`); if even that is empty, the normalized string is used. Under the pinned environment the workspace is `/mnt/proj`, whose name `proj` is four characters, so the prefix is `proj` Measured outside the sandbox (a create in ten scratch workspaces, 2026-09-22; the sandbox workspace is always `proj`): `beads_bend` → `bb`, `beadsrustproject` → `bea`, `My Project!` → `myp`, `a` → `a`, `123abc` → `123abc`, `je_private_skills_repo` → `jpsr`, `Ünï-code` → `n-code`, `sixsix` → `sixsix`, `seven77` → `sev`, `x.y.z-w` → `xyzw`; `source_repo` is the directory name unchanged in every case. | `src/util/id.rs:692`, `:596` | where_plain, where_json, create_min_json, prefix_config_nested |
| S2.13 | Prefix sanitization trims the input, then lower-cases each character with the ASCII rule and keeps only `a`–`z`, `0`–`9`, `_`, `-`, `.`, `:` and `#`; every other character, including all non-ASCII, is dropped rather than replaced. At most 64 surviving characters are kept, then trailing `_ - . : #` are stripped. Leading digits and leading separators survive Measured: `issue-prefix: "Web App!"` mints `webapp-…` (`prefix_config_quoted`). | `src/util/id.rs:570`, `:9` | prefix_config_quoted |
| S2.14 | A configured `issue_prefix` that sanitizes to the empty string is fatal — it does not fall back to `br`: `issue_prefix "<raw value>" contains no usable ASCII letters, digits, or separators`, with the raw value rendered in Rust debug form, that is double-quoted with escapes Measured: the refusal is `CONFIG_ERROR`, exit 7, `"hint": null`, `"context": null`, prefixed `Configuration error: ` (`error_prefix_config_bad`, `error_prefix_config_bad_json`), and every command refuses it, reads included (`error_prefix_config_bad_list`), except `where` (S2.15). Measured (2026-09-23, OQ-024): `issue_prefix: '!!!'` refuses: `Configuration error: issue_prefix "!!!" contains no usable ASCII letters, digits, or separators`, exit 7. | `src/util/id.rs:615` | error_prefix_config_bad, error_prefix_config_bad_json, error_prefix_config_bad_list, error_create_empty_prefix |
| S2.15 | `br where`'s reported `prefix` uses a different rule from S2.10 and reports the raw configured value with no sanitization: configured value first; else a full scan of the store that reports a prefix only when every record agrees; else the private database's `config` table; else the prefix of the first stored row. Over the corpus the rules agree on `proj` Measured: `where` reports the configured value raw (`prefix: Web App!`, `prefix_config_quoted_where`; `!!!`, `prefix_config_bad_where`), and an unconfigured empty store reports no prefix at all: plain `where` has no `prefix:` line and `--json` no `prefix` member (`where_empty`, `where_empty_json`). | `src/cli/commands/where.rs:84`, `:184`, `:143` | where_plain, where_json, prefix_config_where, prefix_config_quoted_where, prefix_config_bad_where, prefix_store_where, where_empty, where_empty_json |

### S2.C `metadata.json`, `config.yaml` and the resolved paths

| S2.n | clause | provenance | cases |
|---|---|---|---|
| S2.16 | `<beads_dir>/metadata.json` is optional; absent means all defaults and no message. The keys that change in-scope behavior are `database` (default `beads.db`; a blank value falls back to the default; an absolute value is used as is, otherwise it is joined to the cache directory) and `jsonl_export` (default `issues.jsonl`, honored only when it differs from the default and is not an excluded name). `backend` and `deletions_retention_days` are parsed and do not affect the in-scope path | `src/config/mod.rs:115`, `:62`, `:5873`, `:5905` | where_plain, where_json |
| S2.17 | A `metadata.json` that is not valid JSON aborts the command with `JSON error: <serde message>`; the file name is not part of the message | `src/config/mod.rs:115`, `src/error/mod.rs:255` | (case to add: `error_bad_metadata_json`) |
| S2.18 | `config.yaml` is read from four places and merged lowest to highest: `~/.beads/config.yaml`, then `~/.config/beads/config.yaml` (falling back to `~/.config/bd/config.yaml` when the first is absent), then `<beads_dir>/config.yaml`, then the environment layer; the command line is merged on top of all four. A missing `HOME` makes the two user layers empty without error. Each document is flattened to dotted keys and every key has `-` canonicalized to `_` | `src/config/mod.rs:6538`, `:6196`, `:6205`, `:6223`, `:7337`, `:7444` | where_plain, prefix_config_create, prefix_config_quoted, prefix_config_nested |
| S2.19 | A `config.yaml` that is not valid YAML aborts the command with `YAML error: <serde_yml message>`; the file name is not part of the message | `src/config/mod.rs:6004`, `src/error/mod.rs:259` | (case to add: `error_bad_config_yaml`) |
| S2.20 | The `config.yaml` keys that change in-scope behavior are `issue_prefix` (aliases `issue-prefix`, `prefix`; S2.10); `min_hash_length` 3, `max_hash_length` 8 and `max_collision_prob` 0.25 for id minting; `default_priority` 2 and `default_type` `task`; `display.color`; `sync.auto_flush` with its legacy inverted spelling `no-auto-flush`; `sync.auto_import`; `sync.history_enabled`; **`no_db`** with aliases `no-db` and `no.db`, so a project file can put a workspace into JSONL-only mode with no flag at all; `db` / `database`; `actor`; `claim_exclusive`; `lock_timeout` in milliseconds, default 30000; and `external_projects.<name>`. There is no `quiet` key and no output-format key — the output mode comes from flags and from `BR_OUTPUT_FORMAT`, `TOON_DEFAULT_FORMAT` and `NO_COLOR` only | `src/config/mod.rs:7111`, `:5545`, `:5560`, `:6866`, `:6890`, `:6930`, `:6072`, `:6147` | list_quiet, list_json |
| S2.21 | The store path is resolved, in order, from the environment variable `BEADS_JSONL`; an explicit non-default `metadata.json` `jsonl_export`; a sibling of an explicit database override; an existing `<beads_dir>/issues.jsonl` and then the legacy `<beads_dir>/beads.jsonl`; and finally the default `<beads_dir>/issues.jsonl`. Under the pinned environment the answer is always `/mnt/proj/.beads/issues.jsonl` | `src/config/mod.rs:5893`, `:5899`, `:192` | where_json, where_plain |
| S2.22 | The `last-touched` sidecar is never read at startup. It is read only by `show`, `close`, `update` and `reopen`, and only when the command was given no issue id, and by `delete` only to clear a pointer at a deleted issue. Its path is `<beads_dir>/last-touched` unless `BEADS_CACHE_DIR` is set and non-empty, in which case it is that directory's `last-touched`. A read takes the first line and trims it; a file larger than 4096 bytes, a non-UTF-8 file and an absent file all read as the empty string, with no message | `src/util/mod.rs:57`, `:112`, `:29`, `:47` Measured (2026-09-24, OQ-008, OQ-084, harness mode `@ls`): a read-only run creates no file in `.beads/`. | update_last_touched, scn_last_touched, ls_read_list, ls_read_show, ls_nostore_list |
| S2.23 | A command that needs the sidecar and reads the empty string fails with the validation error on field `ids` whose message is `no issue IDs provided and no last-touched issue`, exit code 4 | `src/cli/commands/show.rs:239`, `close.rs:644`, `update.rs:381`, `reopen.rs:75` | (case to add: `error_update_no_last_touched` — `@fx=basic`, `update --status in_progress` with no id and no sidecar) |

### S2.D The `issues.jsonl` grammar as the loader accepts it

| S2.n | clause | provenance | cases |
|---|---|---|---|
| S2.24 | The store is strict UTF-8 text, one JSON object per line. A line is everything up to and including `\n`; its content is trimmed of leading and trailing whitespace before parsing, so a `\r\n` terminator and stray spaces or tabs are tolerated. A line that is empty after trimming is skipped silently and **still advances the line counter**. A missing final newline on the last record is accepted. A file of zero bytes, and a file of nothing but blank lines, both load as an empty store with no message Measured (2026-09-23, OQ-101): a store that is not UTF-8 is `I/O error: stream did not contain valid UTF-8`, IO_ERROR, exit 8. | `src/sync/mod.rs:13823`, `:13828`, `:13833` | empty_list, empty_list_json, empty_ready, empty_ready_json, empty_blocked, empty_blocked_json, empty_stats_json, empty_count, empty_search, empty_dep_cycles, error_store_not_utf8, error_store_not_utf8_json |
| S2.25 | A leading UTF-8 byte-order mark is **not** stripped on the load path: U+FEFF is not whitespace, so it survives the trim of S2.24 and reaches the JSON parser. The result is the invalid-JSON refusal of S2.27 reading `Configuration error: Invalid JSON at line 1: expected value at line 1 column 1`. Only `br doctor`, which is out of scope, knows how to strip a BOM Measured (2026-09-23, OQ-019): as stated; the column is the BOM's first byte. | `src/sync/mod.rs:13833`, `src/cli/commands/doctor.rs:4964` | (case to add: `error_jsonl_bom` on a fixture whose first three bytes are `EF BB BF`), error_jsonl_bom, error_jsonl_bom_json |
| S2.26 | A git merge-conflict marker anywhere in the file refuses the whole load **before any line is parsed as JSON** and before any database work, exit code **7**, code `CONFIG_ERROR`. A marker is a line whose trimmed text starts with the seven characters `<<<<<<<` (type `Start`), starts with `=======` (type `Separator`), or starts with `>>>>>>>` (type `End`); no trailing space is required and the diff3 base marker `\|\|\|\|\|\|\|` is not detected. At most the first five markers are listed, in file order, one per line, each as `<store path>:<1-based line> <type>` followed by ` (<remainder of the marker line, trimmed>)` for `Start` and `End` — an empty remainder prints as ` ()` — and by nothing for `Separator`. The whole message is, for the `conflict` fixture, verbatim and newline-joined: `Configuration error: Merge conflict markers detected in /mnt/proj/.beads/issues.jsonl.` / `/mnt/proj/.beads/issues.jsonl:2 Start (HEAD)` / `/mnt/proj/.beads/issues.jsonl:4 Separator` / `/mnt/proj/.beads/issues.jsonl:6 End (branch)` / `Resolve conflicts before importing.`. In Plain mode the whole block goes to stderr behind a single `Error: ` prefix; in JSON mode it is one `"message"` string with `\n` escapes. Nothing is written and no partial result is printed Amended (2026-09-25, round 19): `show` does not scan for markers; it reports the first line that is not JSON (`Configuration error: Invalid JSON at line 2: expected value at line 1 column 1` on the `conflict` store), exit 7. | `src/sync/mod.rs:10291`, `:10334`, `:10357`, `:10381`, `:14630` | error_conflict_markers, error_conflict_markers_json, r19_conflict_show_json, error_conflict_show_plain, error_conflict_show_missing, error_conflict_dep_list |
| S2.27 | A line that is not valid JSON refuses the whole load, exit code **7**, code `CONFIG_ERROR`, message `Configuration error: Invalid JSON at line <n>: <serde_json message>`, where `<n>` is the 1-based line number in the file (blank lines included in the count) and the serde message reports its own line and column relative to that single trimmed line. For the `malformed` fixture, whose second line is the 40-character fragment `{"id":"proj-aab","title":"truncated line`, the message is exactly `Configuration error: Invalid JSON at line 2: EOF while parsing a string at line 1 column 40`. The load aborts: nothing is skipped, nothing is written, and the first, valid record is not reported Measured (2026-09-23, OQ-102): `trailing comma`, `expected `:``, `expected `,` or `}``, `key must be a string`, `expected ident` (at the first mismatching byte of a keyword), `invalid escape`, `control character (\u0000-\u001F) found while parsing a string`, `trailing characters`, `unexpected end of hex escape`, `EOF while parsing an object`, each at the column serde_json reports; a line that is an array fills the members in order, so `[1]` is `invalid type: integer `1`, expected a string`. Amended (2026-09-24, OQ-136): an invalid `\uXXXX` escape is reported at the column of the FOURTH byte after the `u` (serde_json takes the four bytes before it judges them: `\u00zz`, `\uzzzz` and `\u0z",` all report the same column), or as `EOF while parsing a string` at the end of the line when fewer than four bytes remain; a character that can begin no JSON token (an upper-case or non-ASCII letter, a raw control character) is judged where it stands, at its FIRST byte: `expected value` in value position, `key must be a string` in key position, and nothing after it is reported first; a keyword prefix that runs to the end of the text (`tru`) is `EOF while parsing a value` at the end. Amended (2026-09-24, bb-04n): on one line serde_json reports, in this order, a member's own error (as it reads the object: `{"priority":"x"}` is `invalid type: string "x", expected i32 at line 1 column 15`, not a missing `id`), then a missing member AT THE `}` (`{"id":"proj-a1"} X` is `missing field `title` at line 1 column 16`), and only then `trailing characters` after the `}`. | `src/sync/mod.rs:13801` | error_malformed_jsonl, error_malformed_jsonl_json, error_jsonl_trailing_comma, error_jsonl_colon, error_jsonl_comma, error_jsonl_key, error_jsonl_ident, error_jsonl_escape, error_jsonl_ctrl, error_jsonl_trailing, error_jsonl_array, error_jsonl_int_id, error_jsonl_prio_word, error_jsonl_surrogate, error_jsonl_eof_object, error_load_u_escape_bad, error_load_dep_meta_stray, error_jsonl_missing_then_trailing, error_jsonl_missing_at_close, error_jsonl_type_then_trailing, error_jsonl_type_then_missing |
| S2.28 | The members with no default are `id`, `title`, `created_at` and `updated_at`; every other member may be absent. An absent `status` reads as `open`, an absent `priority` as `2`, an absent `issue_type` as `task`, absent `labels` / `dependencies` / `comments` as empty, absent `ephemeral` / `pinned` / `is_template` as false, and every other absent member as "not set". A record missing one of the four required members is the same refusal as S2.27, carrying serde's own wording inside the frame: `Configuration error: Invalid JSON at line <n>: missing field \`title\` at line 1 column <c>` Measured (2026-09-23, OQ-025): `missing field `title` at line 1 column 138` for a record of 138 bytes (the column is the end of the record). Measured (2026-09-23, OQ-102): a value of the wrong type is `invalid type: <integer `n` or floating point `x` or string "s" or boolean `b` or null or sequence or map>, expected <a string or i32 or i64 (a comment id) or a boolean or a sequence or an RFC 3339 formatted date and time string>` at the value's last byte (a container: the byte before its bracket); an integer beyond i32 is `invalid value: integer `n`, expected i32`. | `src/model/mod.rs:462`, `:495`, `:645`, `src/sync/mod.rs:13801` | edge_precision_list_json, (case to add: `error_jsonl_missing_title`), error_jsonl_missing_title, error_jsonl_type_created_int, error_jsonl_type_created_word, error_jsonl_type_labels_str, error_jsonl_type_labels_int, error_jsonl_type_ephemeral_str, error_jsonl_type_deps_int, error_jsonl_type_comments_obj, error_jsonl_type_priority_float, error_jsonl_type_priority_null, error_jsonl_type_desc_int, error_jsonl_type_desc_bool, error_jsonl_type_estimate_str, error_jsonl_type_id_null, error_jsonl_type_title_arr, error_jsonl_type_title_obj, error_jsonl_type_priority_big, error_jsonl_type_priority_neg_float, error_jsonl_type_dep_bad_member, error_jsonl_type_comment_bad, error_jsonl_type_status_int |
| S2.29 | Unknown object members are accepted silently and are dropped: neither the issue, the dependency nor the comment record declares `deny_unknown_fields`, and the write-back of S5 re-serializes only the known members. The `precision` fixture's `"unknown_field":"is it kept?"` survives a read — `show` echoes the raw line (S5.30) — and disappears the first time any command rewrites the store | `src/model/mod.rs:461`, `:894`, `:930` | edge_precision_show_json, edge_precision_rewrite |
| S2.30 | A record that parses but fails the validation rules of S2.41–S2.54 refuses the load with `Validation failed for issue <id> at line <line>: <messages joined by a comma and a space>`, exit code 4 Corrected (2026-09-23, OQ-025): the refusal is `Configuration error: Validation failed for issue <id> at line <n>: …`, CONFIG_ERROR, exit **7** (not 4), `context` null. Every message of the record is joined by `, ` in this measured order: id; title (empty, over 500, NUL); the NUL rule of description, design, acceptance_criteria, prerequisites, notes; status then issue_type (over 50, NUL); assignee, owner, created_by (over 200, NUL); external_ref (over 200 bytes, white space, NUL); source_system; labels (over 64, then each label by index in BYTE ORDER of the sorted labels); priority; estimated_minutes; closed_at before created_at. `show` does not validate (`error_load_bad_id_show`). The port applies it on the import path only. | `src/sync/mod.rs:13806`, `src/validation/mod.rs:77` | (case to add: `error_jsonl_bad_id_format` on a fixture whose `id` has no hyphen), error_jsonl_bad_id_format, error_load_title_blank, error_load_title_long, error_load_priority, error_load_estimate_neg, error_load_estimate_big, error_load_closed_before, error_load_assignee_long, error_load_extref_space, error_load_extref_long, error_load_nul_title, error_load_status_long, error_load_labels_many, error_load_label_bad, error_load_two_rules, error_load_two_rules_json, error_load_all_a, error_load_all_b |
| S2.31 | `status` is read as a string and matched case-insensitively against `open`, `in_progress`, `inprogress`, `blocked`, `deferred`, `draft`, `closed`, `tombstone`, `pinned`; `inprogress` is an alias that is written back as `in_progress`. Any other spelling becomes a custom status, **lower-cased**, so `QaReview` loads as `qareview`. A second, later fold then maps the custom values `done`, `complete`, `completed`, `finished` and `resolved` (trimmed, ASCII-lower-cased) to `closed`, which is why such a record also picks up a `closed_at` under S2.36 Measured (2026-09-23, OQ-022): `InProgress`, `inprogress` → `in_progress`; `QaReview` → `qareview`; `done` → `closed` with a `closed_at`; the write-back keeps them. | `src/model/mod.rs:76`, `:87`, `src/sync/mod.rs:13726` | list_status_in_progress, (case to add: `edge_status_spellings` on a fixture carrying `InProgress`, `inprogress`, `QaReview` and `done`), edge_status_spellings, edge_status_spellings_rewrite |
| S2.32 | `issue_type` is read the same way and matched case-insensitively against `task`, `bug`, `feature`, `epic`, `chore`, `docs`, `question`; any other spelling becomes a custom type, lower-cased (`Odd_Type` → `odd_type`) | `src/model/mod.rs:209`, `:220` | list_type_bug, create_custom_type |
| S2.33 | A dependency's `type` is read the same way and matched case-insensitively against `blocks`, `parent-child`, `conditional-blocks`, `waits-for`, `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes`, `caused-by`; any other spelling becomes a custom type, lower-cased (`Odd-Dep` → `odd-dep`). A custom type is then repaired once: every `_` is replaced by `-` and the result re-read, and the repair is adopted only when it lands on one of the eleven known types, so `parent_child` becomes `parent-child` while `odd_dep` stays `odd_dep`. Only the first four types are blocking. Note the asymmetry the goldens show — a stored custom dependency type loads without complaint while `dep add -t <custom>` refuses (S9) | `src/model/mod.rs:288`, `:337`, `src/sync/mod.rs:13662` | dep_list_json, dep_add_custom_type |
| S2.34 | `priority` in the store is a bare JSON integer read with no range check at load time; the 0–4 range is enforced by validation (S2.43) and by the argument parser. The command-line forms accept surrounding whitespace and an optional `P` or `p` prefix — the token is upper-cased before the prefix is stripped — so `P3`, `p3` and `3` are one value | `src/model/mod.rs:148`, `:174` | list_priority_one, create_priority_p_form, error_create_bad_priority |
| S2.35 | Every timestamp member (`created_at`, `updated_at`, `closed_at`, `due_at`, `defer_until`, `deleted_at`, `compacted_at`, a dependency's `created_at`, a comment's `created_at`) is an RFC 3339 string with a mandatory zone. Fraction digits of any width are accepted (`…05Z`, `…05.1Z`, `…05.100Z`, `…05.000000001Z`) and a numeric offset is accepted and converted to UTC on load, so `2026-01-01T12:00:04+02:00` is the instant `2026-01-01T10:00:04Z`. The value is kept as an instant, so what is written back is the canonical form of S5.12, not the source text Measured (2026-09-23, OQ-102): a timestamp string that does not parse is reported at its closing quote in chrono's words: `premature end of input` (it stops early), `input contains invalid characters`, `trailing input`, `input is out of range` (a well-formed spelling with month 13, February 30, hour 25). | `src/model/mod.rs:519`, `:526`, `src/storage/sqlite.rs:18607` | edge_precision_list_json, edge_precision_rewrite, edge_precision_ready, edge_precision_sort_created, error_jsonl_ts_short, error_jsonl_ts_month, error_jsonl_ts_trail, error_jsonl_ts_feb30, error_jsonl_ts_nozone, error_jsonl_ts_hour, error_jsonl_ts_empty, error_jsonl_ts_slash |
| S2.36 | Loading applies these repairs, in this order, to every record, before validation and before the record is usable: `labels` is sorted ascending by byte order and de-duplicated; the dependency-type repair of S2.33; duplicate dependencies sharing `(issue_id, depends_on_id, type)` collapse to the one with the greatest `created_at`, first occurrence winning a tie, with the survivors left in file order; byte-identical duplicate comments collapse to their first occurrence; the closed-status fold of S2.31; an `id` containing `-wisp-` sets `ephemeral` to true; a `closed` or `tombstone` record with no `closed_at` gains `closed_at` equal to its `updated_at`, and any other record's `closed_at` is discarded; `external_ref` that is whitespace-only becomes "not set" and otherwise is trimmed — it is the only string member that is trimmed; and an `updated_at` earlier than `created_at` is raised to `created_at`. The last two repairs mean the corresponding validation rules (S2.44, S2.46) can never fire from a loaded record | `src/sync/mod.rs:13652`, `:13656`, `:13676`, `:13708`, `:13726`, `:13737`, `:13742`, `:13752`, `:13764` | edge_precision_rewrite, show_closed, create_ephemeral |
| S2.37 | A dependency's `metadata` that is absent, or is a string that is empty or only whitespace, loads as "not set"; any other string is kept verbatim. The `basic` fixture's `"metadata":"{}"` is a non-blank string and survives unchanged | `src/model/mod.rs:46`, `:919` | dep_list_json, update_status_json |
| S2.38 | An optional string member present as `""` loads as an empty string, not as "not set" — `show`, which reads the file directly, echoes `"description":""` and `"assignee":""` back. What survives a *write-back* is a different question, answered by S5.9 | `src/model/mod.rs:474`, `:507` | edge_precision_show_json, edge_precision_rewrite |
| S2.39 | `labels` is read as the array in the file for the direct-read path of `show`, duplicates and file order intact (`["zeta","alpha","alpha"]`), while the loaded model sorts and de-duplicates under S2.36. The two paths therefore disagree on the same record | `src/model/mod.rs:645`, `src/cli/commands/show.rs:893`, `src/sync/mod.rs:13656` | edge_precision_show_json, edge_precision_list_json |
| S2.40 | Two lines carrying the same `id` refuse the load with `Duplicate issue id '<id>' in <absolute store path> at line <n>`, naming the **second** occurrence's 1-based line number, before any write. The `show` fast path raises the identical text Measured (2026-09-23, OQ-021): the text is prefixed `Configuration error: `, code CONFIG_ERROR, exit 7; `list`, `count` and `show` refuse alike. | `src/sync/mod.rs:13863`, `src/cli/commands/show.rs:823` | (case to add: `error_jsonl_duplicate_id` on a fixture with two `proj-aaa` lines), error_jsonl_duplicate_id, error_jsonl_duplicate_id_show, error_load_empty_optionals_dup |
| S2.41 | A record whose `id` prefix does not match the workspace prefix would normally refuse with `Prefix mismatch at line <n>: expected '<prefix>', found issue '<id>'`, tombstones exempt — but the `--no-db` open sets `skip_prefix_validation`, which makes the check unconditionally false, so under this port's contract a foreign-prefix id round-trips untouched and is never renamed | `src/sync/mod.rs:13854`, `src/config/mod.rs:5419`, `:5761` | edge_precision_list_json, list_json |
| S2.42 | A record with `"status":"tombstone"` loads as a row, counts in `stats` as a tombstone, is skipped by the prefix scan of S2.11 and is written back (S5.16). When a tombstone is already present locally under the same id, an incoming record for that id is skipped with the reason `Tombstone protection: <id>`. A record with `"ephemeral":true`, and any record whose `id` contains `-wisp-` (which S2.36 turns into an ephemeral), is parsed and counted and takes part in the duplicate check, then is dropped before any storage work — it is never queryable and never written | `src/sync/mod.rs:13617`, `:14113`, `:14288`, `src/storage/sqlite.rs:15005` | empty_stats_json, stats_json, create_ephemeral, delete_cascade |

### S2.E Validation limits and their verbatim messages

Each message below is a validation failure: code `VALIDATION_FAILED`, exit code **4**, Plain mode on stderr as `Error: Validation failed: <field>: <message>` with a trailing newline, JSON mode as the envelope of S5.21. Several failures on one record are joined by `, `; the goldens exercise one at a time.

| S2.n | clause | provenance | cases |
|---|---|---|---|
| S2.43 | `title`: a title that is empty or only whitespace produces `title: cannot be empty`; a title of more than 500 **characters** (Unicode scalar values, not bytes) produces `title: exceeds 500 characters`. Exactly 500 characters is accepted | `src/validation/mod.rs:21`, `:158`, `:162` | error_create_empty_title, error_create_blank_title, error_create_long_title, edge_create_max_title |
| S2.44 | `id`: an empty id produces `id: cannot be empty`; an over-long id produces `id: exceeds <max> characters`; an id that is not `prefix-hash` shaped produces `id: invalid format (expected prefix-hash)` Measured (2026-09-23): the maximum is 105 characters, and an over-long id also reports the format (`id: exceeds 105 characters, id: invalid format …`); refused shapes: `proj-`, `-aab`, `PROJ-AAB`, `proj-a_b`, `proj-a b`, `proj-aab.x`; accepted: hyphenated lower-case words (`proj-my-thing-89a`) and numeric dotted children (`proj-uly.1`). Bounds (2026-09-23, `edge_load_id_*`): at least two hyphen-separated lower-case alphanumeric words before the first `.` (any number of hyphens: `a-b-c-d-e-f-g-h-i-j-k`, `123-abc`), the LAST word at most 40 characters (40 loads, 41 is refused; a 50-character middle word is fine), everything before the last hyphen at most 64 characters (64 loads, 65 is refused), and digits after each `.` (not counted in the 40). Corrected (2026-09-23, found by running the port on real stores): the rule splits at the LAST hyphen; the part before it allows lower-case letters, digits, `_`, `.` and `-` (`my_proj-13w`, `_proj-13w`, `a.b-13w`, `p_1-13w`, `franken_agent_detection-13w` load; `MyProj-13w` and `my_proj-a_b` are refused). | `src/validation/mod.rs:78`, `:82`, `:87` | (case to add: `error_jsonl_bad_id_format`), error_load_id_empty, error_load_id_long, error_load_id_trailing, error_load_id_leading, error_load_id_upper, error_load_id_underscore, error_load_id_space, error_load_id_child_word, error_load_id_upper_show, error_load_bad_id_show, edge_load_id_hash8, edge_load_id_hash9, edge_load_id_hash12, edge_load_id_hash16, edge_load_id_hash17, edge_load_id_hash32, edge_load_id_hash33, edge_load_id_hash64, edge_load_id_hash65, edge_load_id_hash100, edge_load_id_slug_segments, edge_load_id_prefix_long, edge_load_id_many_hyphens, edge_load_id_digits_prefix, edge_load_id_hash40, edge_load_id_hash48, edge_load_id_hash49, edge_load_id_hash50, edge_load_id_hash51, edge_load_id_hash52, edge_load_id_hash56, edge_load_id_hash60, edge_load_id_hash63, edge_load_id_mid_long, edge_load_id_last_long, edge_load_id_slug57, edge_load_id_slug_child, edge_load_id_hash41, edge_load_id_hash42, edge_load_id_hash43, edge_load_id_hash44, edge_load_id_hash45, edge_load_id_hash46, edge_load_id_hash47, edge_load_id_head64, edge_load_id_head65, edge_load_id_head80, edge_load_id_head100, edge_load_id_hyphens11, edge_load_id_prefix_underscore, edge_load_id_prefix_real, edge_load_id_prefix_upper, edge_load_id_prefix_dot, edge_load_id_prefix_underscore_lead, edge_load_id_hash_underscore_prefix, edge_load_id_prefix_underscore_child, edge_load_id_prefix_digits_underscore |
| S2.45 | `priority`: a value outside 0–4 produces `priority: must be 0-4`. The argument parser reports the narrower `Priority must be 0-4, got: <value>` with code `INVALID_PRIORITY` before validation is reached | `src/validation/mod.rs:98`, `src/model/mod.rs:183` | error_create_bad_priority, error_create_bad_priority_json, error_update_bad_priority |
| S2.46 | `updated_at` earlier than `created_at` produces `updated_at: cannot be before created_at`; S2.36 raises the value first, so only a command-supplied timestamp can reach this rule Amended (2026-09-25, round 17): a command clock before the record's `created_at` reaches this rule: `update` of a stored column (`-p`, `--title`, `--status`, `--claim`, `--notes`, `--assignee`, `--due ""`, …), `reopen`, `defer` and `undefer` refuse the whole batch with `Validation failed: updated_at: cannot be before created_at` (VALIDATION_FAILED, exit 4, context `field`/`reason`, retryable), nothing written; an `update` whose only edits are labels or the parent, `label add`, `comments add`, `dep add`, `delete` and `create` are not validated. `update --title ""` (or blank) is `Validation failed: title: cannot be empty`. | `src/validation/mod.rs:103` | (case to add: `error_jsonl_updated_before_created`), r17_upd_title_empty, r17_upd_title_blank, r17_early_upd_prio, r17_early_upd_status, r17_early_upd_notes, r17_early_upd_label, r17_early_upd_assignee, r17_early_upd_parent, r17_early_upd_claim, r17_early_reopen, r17_early_defer, r17_early_undefer, r17_early_label_add, r17_early_comment, r17_early_dep_add, r17_early_delete, r17_early_create, r17_late_title_child, error_update_early_multi, error_reopen_early_json, error_defer_early, error_update_title_blank |
| S2.47 | `estimated_minutes`: a negative value produces `estimated_minutes: cannot be negative`; a value above 525960 produces `estimated_minutes: exceeds maximum (525960 minutes / ~1 year)` Amended (2026-09-25, round 19): the check runs on `create`, `q` and `update --estimate` (`create X -e 2147483647` exits 4 with `Validation failed: estimated_minutes: exceeds maximum (525960 minutes / ~1 year)`; `--estimate=-5` is `cannot be negative`); in a combined refusal it comes after `title` and `updated_at`. | `src/validation/mod.rs:113`, `:118` | create_full_json, (case to add: `error_create_estimate_too_big`), r19_create_est_max, create_estimate_max, error_create_estimate_over, error_create_estimate_negative_json, error_q_estimate_over, error_update_estimate_over_json, error_update_estimate_negative, error_update_three_checks |
| S2.48 | `closed_at` invariants: a `closed` issue with no `closed_at` produces `closed_at: closed issues must set closed_at`; a `closed_at` on an issue that is neither `closed` nor `tombstone` produces `closed_at: only closed or tombstone issues may set closed_at`; a `closed_at` earlier than `created_at` produces `closed_at: cannot be before created_at`. S2.36 repairs the first two before they can fire on a loaded record Amended (2026-09-25, round 17): `close` with a clock before `created_at` fails both rules at once: `Validation errors: [ValidationError { field: "updated_at", message: "cannot be before created_at" }, ValidationError { field: "closed_at", message: "cannot be before created_at" }]`, exit 4, with the JSON context `errors`: an array of `{field, message}` objects. | `src/validation/mod.rs:127`, `:134`, `:141` | update_status_closed, show_closed, close_reason_json, r17_early_close, r17_early_close_json, r17_late_close_blocker_suggest, r17_late_graph_tip_suggest, error_close_early_plain |
| S2.49 | `assignee`, `owner`, `created_by` and `source_system`: more than 200 characters produces `<field>: exceeds 200 characters` | `src/validation/mod.rs:22`, `:185` | actor_flag_create, (case to add: `error_create_long_assignee`) |
| S2.50 | `external_ref`: more than 200 **bytes** produces `external_ref: exceeds 200 characters`; any Unicode whitespace character in the value produces `external_ref: cannot contain whitespace` | `src/validation/mod.rs:210` | create_full_json, (case to add: `error_create_external_ref_space`) |
| S2.51 | A NUL byte in `title`, `description`, `design`, `acceptance_criteria`, `prerequisites`, `notes`, `status`, `issue_type`, `assignee`, `owner`, `created_by`, `external_ref`, `source_system` or a comment body produces `<field>: cannot contain NUL bytes`. The long-text members have no length limit at all — only the NUL rule applies to them | `src/validation/mod.rs:230`, `:167` | create_unicode, (case to add: `error_create_nul_description`) |
| S2.52 | A custom `status` longer than 50 characters produces `status: custom status exceeds 50 characters`; a custom `issue_type` longer than 50 characters produces `issue_type: custom issue type exceeds 50 characters` | `src/validation/mod.rs:23`, `:253`, `:266` | create_custom_type, (case to add: `error_create_long_custom_type`) |
| S2.53 | `labels`: more than 64 labels on one issue produces `labels: exceeds 64 labels`; an individual label that fails S2.54 produces `labels: label at index <i>: <label message>` with a 0-based index | `src/validation/mod.rs:24`, `:275`, `:283` | (case to add: `error_create_65_labels`) |
| S2.54 | A single label must be non-empty (`label: cannot be empty`), at most 50 bytes (`label: exceeds 50 characters`), and built only from ASCII alphanumerics, `-`, `_` and `:`; anything else produces `label: invalid characters (only alphanumeric, hyphen, underscore, colon allowed)`. The `create` and `label add` paths report that message directly, without the `labels: label at index …` wrapper | `src/validation/mod.rs:425`, `:429`, `:433` | error_create_bad_label, error_label_add_invalid, label_add, label_rename |
| S2.55 | A comment must have a positive `id` (`id: must be positive`), a non-blank `issue_id` (`issue_id: cannot be empty`), a non-blank body (`content: cannot be empty`), a non-blank `author` (`author: cannot be empty`) and an `author` of at most 200 bytes (`author: exceeds 200 characters`). Comment bodies have no length limit. On import a comment whose `id` is not positive is given a fresh one, and a comment whose `id` collides with an existing row's is re-minted rather than refused; two comments on the *same* issue carrying one id are refused with `duplicate import comment id <n>`, and a comment whose `issue_id` disagrees with its record's id is refused with `comment issue_id '<a>' does not match import issue '<b>'` | `src/validation/mod.rs:462`, `:466`, `:470`, `:478`, `:482`, `src/storage/sqlite.rs:20794`, `:20942` | error_comments_add_empty, comments_add, comments_add_author, comments_list_json |
| S2.56 | A dependency must not point at itself (`depends_on_id: issue cannot depend on itself`), both ends must exist (`issue_id: issue not found`, `depends_on_id: dependency target not found`), a blocking type must not close a cycle (`depends_on_id: would create dependency cycle`), and the edge must not already exist (`depends_on_id: dependency already exists`). The `dep add` command reports its own wording for the cycle and duplicate cases (S9) Resolved (2026-09-23, OQ-094): on `dep add` the missing target fails target resolution first (exit 3, `error_dep_add_missing`); this wording is not reached on that path. | `src/validation/mod.rs:373`, `:381`, `:386`, `:396`, `:404` | error_dep_add_self, error_dep_add_missing, error_dep_add_cycle, error_dep_add_cycle_json, dep_add_duplicate, dep_add_external |
| S2.57 | **The import check.** Every command but `show` imports the store, and the import refuses a record that holds the empty string `""` in any of `agent_context`, `bypass_reason`, `source_repo`, `source_repo_path`: `Sync conflict: Import semantic verification failed: issue <id> does not match its normalized JSONL payload (differing fields: <members, alphabetical, joined by `, `>); rolling back the import`, code `SYNC_CONFLICT`, exit 6, `hint` null, `retryable` false, `context` `{"message": <the message without its `Sync conflict: ` prefix>}`. It runs after every line has loaded and validated; the first such record in file order is named. `""` in any other optional string loads and drops on write-back (S5.9). `show` reads the file directly and prints the `""` members back. Amended (2026-09-24, OQ-131): `dependencies` is a differing member too, when any of the record's dependencies names a target that is neither `external:…` nor the id of a stored record (any dependency type; a tombstone is stored; a target further down the file is stored), and it sorts among the others (`differing fields: dependencies, source_repo`). `show` skips this check as well. "The first such record in file order" holds only when one record fails: with several, the original names one at random (DISC-012), and the port names the first in file order. Amended (2026-09-24, round 12, OQ-134): an ephemeral record (S2.42, including a `-wisp-` id) is dropped before the import's checks: neither S2.57 nor S2.58 applies to it, and it is not a stored target (`r12_dep_to_ephemeral_count`, `r12_dep_from_ephemeral_count`); a tombstone and a closed record are stored targets; the dangling rule holds for a custom dependency type and for a tombstone's own edges | `goldens/edge_empty_optionals_list.err:1`, `goldens/edge_empty_optionals_list_json.out:1`, `goldens/edge_empty_source_repo.err:1` (measured 2026-09-23, OQ-018, OQ-088); `goldens/error_load_dep_dangling.err:1`, `goldens/error_load_dep_dangling_empty.err:1` (measured 2026-09-24, OQ-131) | edge_empty_optionals_list, edge_empty_optionals_list_json, edge_empty_optionals_rewrite, edge_empty_optionals_rewrite_json, edge_empty_optionals_show, edge_empty_source_repo, error_load_dep_dangling, error_load_dep_dangling_json, error_load_dep_dangling_related, error_load_dep_dangling_empty, edge_load_dep_external, edge_load_dep_dangling_show, r12_load_dangle_both_json, r12_load_dangle_tombstone, r12_load_dangle_custom, r12_dep_tomb_target_ready_json, r12_dep_to_ephemeral_count, r12_dep_from_ephemeral_count, r12_show_mix_list_json |
| S2.58 | **The import's dependency edges.** Before S2.57's check, the import walks the records in file order and, within a record, its dependencies in order; the first dependency that breaks a rule refuses the whole load. (1) A dependency whose `issue_id` is not the record's `id`: `Validation failed: dependency.issue_id: dependency issue_id '<issue_id>' does not match import issue '<id>'`, code `VALIDATION_FAILED`, exit 4, `hint` null, `retryable` true, `context` `{"field": "dependency.issue_id", "reason": <the text after the second `: `>}`. (2) Otherwise, a dependency whose target is the record's own `id`: `Issue cannot depend on itself: <id>`, `Hint: An issue cannot depend on itself`, code `SELF_DEPENDENCY`, exit 5, `retryable` false, `context` `{"id": <id>}`. (3) Otherwise, a `parent-child` dependency (after the type repair of S2.33) whose target starts with `external:`: `Validation failed: depends_on_id: parent-child dependencies must link local issues: <target>`, code `VALIDATION_FAILED`, exit 4, `retryable` true, `context` `{"field": "depends_on_id", "reason": <the text after the second `: `>}` (the words of S4.357; added 2026-09-24, round 12, OQ-134). Rule (1) is checked before rule (2) on the same dependency (`dependency issue_id 'proj-b1'` on an edge `proj-b1 → proj-b1` of record `proj-a1`). A record that breaks either rule is named even when an earlier record fails S2.57 (`error_load_dep_dangling_then_self`). `show` checks neither Amended (2026-09-24, OQ-136): between rules (2) and (3), a dependency whose `metadata` is set (S2.37: absent or blank is not set) and is not one JSON value refuses the load: `Validation failed: dependencies[<k>].metadata: dependency metadata must be valid JSON for issue <id> -> <target> (type=<type>); found <text>: <serde message> at line <l> column <c>`, code `VALIDATION_FAILED`, exit 4, `hint` null, `retryable` true, `context` `{"field": "dependencies[<k>].metadata", "reason": <the text after the second `: `>}`. `<k>` is the edge's place in the record's array AS STORED, from 0 (not the sorted order of S5.14); `<type>` is the repaired type (S2.33: `BLOCKS` → `blocks`); `<text>` and the position are those of S4.350. Any JSON value passes (`[1]`, `7`, `null`, ` {} `). The metadata rule precedes a later edge's self-dependency and the `external:` rule on the same edge; an ephemeral record is not checked (S2.42); `show` skips it with the rest of the import. Every other command that reads the store (queries and mutations alike) is refused. | `goldens/error_load_dep_self.err:1`, `goldens/error_load_dep_issue_id.err:1`, `goldens/error_load_dep_other_then_self.err:1`, `goldens/error_load_dep_dangling_then_self.err:1` (measured 2026-09-24, OQ-131) | error_load_dep_self, error_load_dep_self_json, edge_load_dep_self_show, error_load_dep_issue_id, error_load_dep_issue_id_json, error_load_dep_other_then_self, error_load_dep_dangling_then_self, r12_load_dangle_self, r12_load_self_other_json, r12_load_dangle_then_other, r12_load_self_related, r12_ext_pc_load_json, r12_ext_pc_then_self, error_load_dep_meta_count, error_load_dep_meta_list_json, edge_load_dep_meta_show, error_load_dep_meta_update, error_load_dep_meta_before_self, error_load_dep_meta_before_ext, error_load_dep_meta_lines, error_load_dep_meta_lines_json, error_load_dep_meta_stray |


## S3. Data model

<!-- from docs/spec-parts/ARCHITECT_S3_S11_S12.md -->

Types and their invariants as the outputs reveal them. Numeric widths feed
`docs/NUMERIC_PLAN.md`; the exact bytes of each record are S5's.

| S3.n | clause | provenance | cases |
|---|---|---|---|
| S3.1 | An **issue** is a record of 44 serialized members, always in this order: `id`, `title`, `description`, `design`, `acceptance_criteria`, `prerequisites`, `notes`, `status`, `priority`, `issue_type`, `assignee`, `owner`, `estimated_minutes`, `created_at`, `created_by`, `updated_at`, `closed_at`, `close_reason`, `closed_by_session`, `bypassed_policy`, `bypass_reason`, `policy_gates_fired`, `due_at`, `defer_until`, `external_ref`, `source_system`, `source_repo`, `source_repo_path`, `agent_context`, `deleted_at`, `deleted_by`, `delete_reason`, `original_type`, `compaction_level`, `compacted_at`, `compacted_at_commit`, `original_size`, `sender`, `ephemeral`, `pinned`, `is_template`, `labels`, `dependencies`, `comments`. A 45th member, `content_hash`, exists on the record and is never serialized (its arithmetic: S4.51) | `src/model/mod.rs:462-650` (`content_hash` at `:468`) | `create_full_json`, `show_json`, `list_json` |
| S3.2 | Seven members are present in every serialized issue: `id`, `title`, `status`, `priority`, `issue_type`, `created_at`, `updated_at`. `compaction_level` is also always present, as an integer, with an absent value written as `0`. Every other scalar member is omitted when it has no value. After a store rewrite `source_repo` and `original_size` are present on every record as well (the rule and its bytes: S5, OQ-002) | `src/model/mod.rs:464-526`, `:623-624`, `:26` | `create_min_json`, `edge_precision_rewrite` |
| S3.3 | The three flags `ephemeral`, `pinned`, `is_template` are booleans that are omitted when false and absent means false everywhere | `src/model/mod.rs:20`, `:636-642` | `create_ephemeral`, `list_json` |
| S3.4 | `labels` is a collection of strings, `dependencies` of dependency records, `comments` of comment records; each is omitted when empty. Their order inside a record is S5's and S6's | `src/model/mod.rs:646-650` | `show_json`, `show_comments_json`, `list_label` |
| S3.5 | A **dependency** is a record of 7 members in this order: `issue_id`, `depends_on_id`, `type`, `created_at`, `created_by`, `metadata`, `thread_id`; the last three are optional. `depends_on_id` may name something that is not a stored issue (an `external:` reference or a missing id) | `src/model/mod.rs:896` | `dep_add_json`, `show_json`, `dep_add_external` |
| S3.6 | A **comment** is a record of 5 members in this order, all always present: `id` (a signed 64-bit integer; how it is assigned without a database is S4c's), `issue_id`, `author`, `text`, `created_at`. `text` may hold interior newlines and tabs | `src/model/mod.rs:933` | `comments_list_json`, `comments_add_json`, `show_comments_json` |
| S3.7 | `status` is an **open set** of lower-case strings. The named members are `open`, `in_progress`, `blocked`, `deferred`, `draft`, `closed`, `tombstone`, `pinned`; any other string is a custom status and is carried as written. Terminal statuses are `closed` and `tombstone` | `src/model/mod.rs:60` | `create_status_in_progress`, `update_status_closed`, `list_all_json` |
| S3.8 | `issue_type` is an **open set**: `task`, `bug`, `feature`, `epic`, `chore`, `docs`, `question`, and any other string as a custom type | `src/model/mod.rs:196` | `create_custom_type`, `list_type_bug` |
| S3.9 | A dependency `type` is one of 11 kebab-case strings: `blocks`, `parent-child`, `conditional-blocks`, `waits-for`, `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes`, `caused-by`. `dep add` refuses any other string on stdout/stderr per S9 with `VALIDATION_FAILED`, exit 4, naming these 11 in this order (golden `dep_add_custom_type`; the refusal's bytes are S4's mutation clauses and S9's). The model names the first four as blocking; how each takes part in the BLOCKED relation is S4's "The BLOCKED relation". What the loader does with a type outside the 11 found in `issues.jsonl` is not exercised by the corpus (case to add: `edge_unknown_dep_type_list` — `["@fx=unknown_dep_type","list","--json"]` on a fixture whose one dependency has `"type":"inspired-by"`) | `src/model/mod.rs:272`, `:337` | `dep_add_custom_type`, `dep_add_related`, `blocked_json` |
| S3.10 | `priority` is an integer from 0 (critical) to 4 (backlog), default 2, serialized as a bare number and rendered `P{n}` in text | `src/model/mod.rs:152` | `create_priority_p_form`, `error_create_bad_priority`, `list_plain` |
| S3.11 | Every timestamp member (`created_at`, `updated_at`, `closed_at`, `due_at`, `defer_until`, `deleted_at`, `compacted_at`, and the `created_at` of dependencies and comments) is an instant in UTC with nanosecond resolution; parsing, ordering and printing are S4's time clauses | `src/model/mod.rs:519-626` | `edge_precision_list_json`, `edge_precision_ready` |
| S3.12 | Events (the audit trail of a database workspace) are not members of an issue record and are not in `issues.jsonl`: no in-scope output carries them | `src/model/mod.rs:962` | `show_json` |
| S3.13 | An **error** is a five-member record `code`, `message`, `hint`, `retryable`, `context` inside `{"error": …}`; its bytes and every instance are S9's | `src/error/structured.rs:593` (S9.1) | `error_show_missing_json`, `error_show_ambiguous_json` |
| S3.14 | An issue `id` is `<prefix>-<hash>` with optional `.<n>` child segments; its generation, validation, length limits and partial resolution are S4's identifier clauses. Ids are unique within a store, and the store's line order is by id | `src/util/id.rs:216`, `src/util/id.rs:306` (S4.1 and following) | `create_min`, `scn_child_ids`, `scn_dup_title` |
| S3.15 | A **store** is the sequence of issue records of `.beads/issues.jsonl`, one per line, plus the sidecar `.beads/last-touched` holding one issue id. Nothing else under `.beads/` is read or written by the in-scope surface of the port (the original's other sidecars: S8, DISC-002, DISC-004) | `src/sync/mod.rs:10779` (S5.A), `src/util/mod.rs:29` (`last-touched`) | `create_min`, `update_last_touched`, `scn_last_touched` |


## S4. Algorithms (behavioral)

<!-- from docs/spec-parts/S4a_S7.md -->

### Issue-id generation

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.1 | The id seed is a byte string built by concatenating exactly five length-prefixed parts, in this order: title, description, creator, timestamp, nonce. Each part is written as the part's **byte** length in decimal, then the single byte `:` (U+003A), then the part's raw UTF-8 bytes. There is no separator between parts and no terminator. Absent description and absent creator are each the empty string, which still contributes the two bytes `0:`. The length counts UTF-8 **bytes**, not characters — shown by golden `create_unicode`, whose 10-character title `日本語のタイトル 🚀` contributes the prefix `29:` | title `First issue`, no description, creator `tester`, nanos `1767323045000000000`, nonce `0` → seed bytes `11:First issue0:6:tester19:17673230450000000001:0` → `proj-fyw` **✓recomputed**; title `日本語のタイトル 🚀`, description `quote " slash \ bell <U+0007> del <U+007F> nbsp <U+00A0>` (36 bytes, no trailing space), creator `tester` → seed begins `29:` and the description part begins `36:` → `proj-7x0` **✓recomputed** | `src/util/id.rs:276`, `src/util/id.rs:295` | create_min, create_min_json, create_unicode, create_full_json, scn_dup_title |
| S4.2 | The timestamp part is the issue's `created_at` rendered as a **decimal count of nanoseconds since 1970-01-01T00:00:00Z**, signed, with no padding, no sign for non-negative values, and no unit suffix. Under the pinned instant 2026-01-02 03:04:05 UTC this is the 19-byte string `1767323045000000000`. When the instant lies outside the representable nanosecond range the count is `0` (see S7.14) Measured with a fractional pinned instant (2026-09-22): the count carries the fraction's nanoseconds (`create_fraction_json` at `…05.25`). | instant `2026-01-02T03:04:05Z` → `1767323045000000000`; instant `2026-02-03T09:00:00Z` → `1770109200000000000` (both **✓recomputed** against their goldens) | `src/util/id.rs:283` | create_min, scn_dup_title, create_fraction_json, create_fraction_eighth, q_fraction |
| S4.3 | The nonce part is the collision counter rendered as an unsigned decimal string, starting at `0`. Nonce `0` contributes `1:0`, nonce `10` contributes `2:10`, nonce `2000` contributes `4:2000` | nonce `0` → `1:0`; nonce `2` → `1:2` | `src/util/id.rs:284` | scn_dup_title |
| S4.4 | The seed bytes are hashed with **SHA-256**. The first 8 bytes of the 32-byte digest are folded into one unsigned 64-bit value **most-significant byte first** (big-endian): the accumulator starts at 0 and, per byte in digest order 0..7, is shifted left 8 bits and the byte is combined in by bitwise OR. Digest bytes 8..31 are discarded | digest `f1 c4 …` → value `0xf1c4…` (first byte is the high byte) | `src/util/id.rs:306` | create_min |
| S4.5 | The 64-bit value is rendered in **base 36** with the alphabet `0123456789abcdefghijklmnopqrstuvwxyz` (index 0 is `0`, index 35 is `z`; lowercase only). The value 0 renders as the single character `0`. Otherwise digits are produced least-significant first by repeated remainder and truncating integer division by 36, then reversed, so the rendering carries no leading `0`. A 64-bit value needs at most 13 base-36 characters (`3w5e11264sgsf` is the maximum) | value `0` → `0`; value `1295` → `zz`; value `46656` → `1000` | `src/util/id.rs:331` | create_min |
| S4.6 | If the base-36 rendering is shorter than the requested `length`, it is left-padded with the character `0` to exactly `length`. Then the **last `length` characters** are taken (the least-significant base-36 digits), which is the whole padded string when padding happened. The hash portion is therefore always exactly `length` characters from the set `[0-9a-z]` | value rendering `3aq2fyw`, length 3 → `fyw`; rendering `7x0`, length 3 → `7x0`; a rendering shorter than 3 (value < 46656) is padded, e.g. `5` with length 3 → `005` | `src/util/id.rs:317` | create_min, create_priority_p_form |
| S4.7 | The generated id is the prefix, then one `-` (U+002D), then the hash portion: `<prefix>-<hash>`. In the pinned sandbox the workspace is `/mnt/proj`, so the prefix is `proj` and ids read `proj-<hash>` | prefix `proj`, hash `fyw` → `proj-fyw` | `src/util/id.rs:132` | create_min, q_basic, where_plain |
| S4.8 | The hash **length is adaptive** in the issue count. Lengths are tried in ascending order from `min_hash_length` 3 through `max_hash_length` 8. For a candidate length the space is 36 raised to that length, evaluated as a 64-bit binary float, and the birthday-bound collision estimate is one minus the exponential of the negated quantity (count × count) ÷ (2 × space), also in 64-bit binary float. The first length whose estimate is **strictly less than** `max_collision_prob` 0.25 is chosen. When no length in 3..=8 satisfies it, the result is 8. Equivalently, over exact integers the predicate is `count × count < 0.575364144903562 × 36^length`, where the constant is the negated natural logarithm of 0.75 | count 0 → 3; count 8 → 3; count 163 → 3; count 164 → 4 | `src/util/id.rs:103` | create_min (count 0), create_full_json (count 8) |
| S4.9 | **The adaptive-length table.** Evaluating S4.8 over every 64-bit-float boundary gives exactly these issue-count bands. The corpus never leaves the first band, so bands 4 and up are computed, not captured — OQ-006 remains open on them and the proposed boundary cases are listed under OQ proposals. [inference] for every row except the first Measured (2026-09-22, OQ-006): the first two steps are captured on fixtures the original built by creating issues one at a time (`idlen_163`, `idlen_164`, `idlen_983`, `idlen_984` scenarios): a create over 163 records mints a 3-character hash (`proj-t8o`), over 164 a 4-character one (`proj-xt8o`), over 983 still 4, over 984 a 5-character one (`proj-7xt8o`); the stores themselves hold 164 three-character ids, then four-character ones. The bands from 5898 up remain computed, not captured. | count `0`–`163` → length **3**; `164`–`983` → **4**; `984`–`5898` → **5**; `5899`–`35389` → **6**; `35390`–`212339` → **7**; `212340` and above → **8** (length is never 9 or more) | `src/util/id.rs:103` | create_min, create_full_json; (cases to add: `edge_idlen_163`, `edge_idlen_164` — `["@fx=n163","create","Boundary"]` / `["@fx=n164","create","Boundary"]`), edge_idlen_163, edge_idlen_164, edge_idlen_983, edge_idlen_984 |
| S4.10 | The count the predicate sees in `--no-db` is the number of **records in the store**, unfiltered: every record loaded from `.beads/issues.jsonl` counts, including `closed`, `deferred`, `draft` and **`tombstone`** records, because `--no-db` rebuilds an in-memory issue table from the whole file and the count is a plain row count over that table with no status predicate. Ephemeral issues are never written to the file, so they never count on a later invocation. The count is read **once**, before the id loop, and does not change across collision retries within one invocation. Unobservable in the corpus because every fixture is far below 164; [inference] Measured (2026-09-23, OQ-028): tombstones count: 163 live records and one tombstone mint a four-character id (`proj-lff7`). | fixture `basic` (8 records) → count 8 → length 3; fixture `empty` (0 records) → count 0 → length 3; after `delete_basic` the tombstoned `proj-5u2` is still a record and still counts | `src/storage/sqlite.rs:12263`, `src/cli/commands/create.rs:452` | create_min, create_full_json, delete_basic, edge_idlen_tombstones |
| S4.11 | **Collision ladder.** At the chosen length, nonces `0,1,2,…,9` are tried in order; the first candidate the store does not already hold is the answer. If all ten collide and the length is below 8, the length increases by one and the ten nonces are tried again at the new length. The nonce restarts at 0 at every length | three `create` calls with identical title, creator and instant in one workspace: nonce 0 → `proj-86w` (free), nonce 0 → `proj-86w` (taken) then nonce 1 → `proj-e1f`, nonce 0 and 1 taken then nonce 2 → `proj-fa9`. All three **✓recomputed** from the same seed differing only in the nonce part | `src/util/id.rs:230` | scn_dup_title |
| S4.12 | **Fallback rung.** When length 8 has also exhausted nonces 0..9, the length is abandoned and a fixed 12-character hash is used with nonces `0,1,2,…,1999` in order (the same seed rule, the same digest rule, `length` 12). The first free candidate wins. If all 2000 collide, one final candidate is built with nonce `2000` at length 12; it is returned when free, and when it too is taken the run fails with `Issue ID collision: <id>` on stderr, code `ID_COLLISION`, exit **3**. Unreachable in the corpus | nonce 1999 candidate taken and nonce 2000 candidate taken → `Error: Issue ID collision: proj-<12 chars>` | `src/util/id.rs:241`, `src/util/id.rs:12`, `src/error/structured.rs:245` | (case to add: `error_id_collision` — needs a fixture holding every candidate) |
| S4.13 | The existence test used by the ladder is "a record with exactly this id is present in the store", with no status filter, so a **tombstone occupies its id** and a colliding candidate is skipped. [inference]: no captured case forces a collision against a tombstone Measured (2026-09-23, OQ-029): a tombstone holding the candidate blocks it; the next candidate is taken (`proj-fhn`). | store holds tombstone `proj-5u2` → a candidate `proj-5u2` is treated as taken | `src/storage/sqlite.rs:12093` | delete_basic; (case to add: `edge_id_collides_with_tombstone`), edge_id_collides_with_tombstone |
| S4.14 | On top of the ladder, `create` retries the whole id generation up to **10 further times** when the store itself reports a collision at insert time, sleeping 10, 20, … milliseconds between attempts; each retry re-reads the clock, so the seed's timestamp part changes. In `--no-db` the store is a fresh private in-memory table, so this path is never taken and the sleep never happens | — | `src/cli/commands/create.rs:576` | create_min |
| S4.15 | `create` feeds the id seed the **raw** title exactly as given on the command line (no trim, no case change) and the raw `-d`/`--description`/`--description-file` value, and the creator is the resolved actor (`tester` in the pinned environment, or the `--actor` value). `--title` and the positional title are the same input to the seed | `create "First issue"` → title part `11:First issue`; `create "By actor" --actor robot-7` → creator part `7:robot-7` → `proj-k6b` **✓recomputed** | `src/cli/commands/create.rs:406`, `src/cli/commands/create.rs:458` | create_min, create_title_flag, actor_flag_create, create_silent |
| S4.16 | `q` (quick capture) feeds a **different** seed: the title is the positional words joined with a single space and then trimmed, the description part is **empty** and the creator part is **empty** even when an actor is configured. Everything else (SHA-256, byte order, base36, adaptive length, ladder) is identical | `q "Quick one"` → seed `9:Quick one0:0:19:17673230450000000001:0` → `proj-asw` **✓recomputed**; `q "Quick urgent" -p 0` → `proj-tkn` **✓recomputed** | `src/cli/commands/q.rs:47`, `src/cli/commands/q.rs:120` | q_basic, q_priority |
| S4.17 | `--dry-run` still generates the id through the full ladder and prints it; nothing is written | `create "Not written" --dry-run` → `Dry run: would create issue proj-8h9` → `proj-8h9` **✓recomputed** | `src/cli/commands/create.rs:540` | create_dry_run, create_dry_run_json |
| S4.18 | `--ephemeral` does not change id generation; the id is generated and reported, and only the write-back differs | `create "Scratch" --ephemeral` → `proj-kr0` **✓recomputed** | `src/cli/commands/create.rs:458` | create_ephemeral |

### Optional slug form (`--slug`)

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.19 | With a `--slug` value that is non-empty after trimming, the id becomes `<prefix>-<slug>-<hash>`: prefix, `-`, the normalized slug, `-`, the hash portion. The seed, digest and base36 rules are unchanged; only the assembled string differs | prefix `proj`, slug `My Thing!`, hash `fyw` → `proj-my-thing-fyw` | `src/util/id.rs:182` | (case to add: `create_slug` — `["create","Slugged","--slug","My Thing!","--json"]`) |
| S4.20 | **Slug normalization.** Characters are scanned in order. An ASCII letter is lowercased and kept; an ASCII digit is kept; every other character contributes a single `-`, and consecutive such characters contribute only one `-`. A non-alphanumeric run at the very start contributes nothing (a `-` is only appended once output is non-empty), so the result never starts with `-`. Trailing `-` characters are then removed. If the result exceeds **48** characters it is truncated to 48 and trailing `-` characters are removed again. Non-ASCII characters count as "other" and collapse to `-` | `My Thing!` → `my-thing`; `  Survey — my  thing  ` → `survey-my-thing`; `!!!` → empty | `src/util/id.rs:640`, `src/util/id.rs:628` | (case to add: `create_slug_unicode`) |
| S4.21 | **Prefix budget.** After S4.20 the slug is further truncated so that its length does not exceed `64 − prefix length − 1`, with trailing `-` characters removed again. When the prefix is 63 characters or longer the budget is zero or underflows and the slug becomes empty | prefix `proj` (4) → budget 59; prefix of 64 characters → budget underflows → slug empty | `src/util/id.rs:668` | (case to add: `create_slug_long_prefix`) |
| S4.22 | When the slug normalizes to the empty string, generation falls back to the plain hash-only path of S4.7–S4.12 with the same seed | `--slug "!!!"` → id has no slug segment | `src/util/id.rs:157` | (case to add: `create_slug_empty`) |
| S4.23 | **Slug collision ladder.** At the adaptive length, nonces 0..9 are tried; if all collide and the length is below 8 the length increases by one and the ten nonces repeat. When length 8 is exhausted the slug is **dropped entirely** and generation restarts on the hash-only path (S4.11–S4.12) rather than widening the slug segment | all slug candidates through length 8 taken → id without a slug segment | `src/util/id.rs:169` | (case to add: `create_slug_saturated`) |

### Child ids

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.24 | With `--parent`, the id is **not** hashed. It is the resolved parent id, then `.` (U+002E), then a decimal child number: `<parent>.<n>`. The number has no padding and no sign | parent `proj-uly`, number 1 → `proj-uly.1`; parent `proj-uly.1`, number 1 → `proj-uly.1.1` | `src/util/id.rs:352`, `src/cli/commands/create.rs:604` | scn_child_ids, create_with_parent |
| S4.25 | **How `n` is chosen in `--no-db`.** No counter survives between invocations (the counter table is not part of the JSONL). Each invocation rebuilds the counter table from the loaded records: every record whose id parses as non-root and whose **direct parent id is also present** contributes its last path segment to that parent's maximum. `n` is that maximum plus 1, and 1 when the parent has no child yet. A tombstoned child still contributes its number, so numbers are not reused after a delete. [inference] on the tombstone half — no captured case deletes a child Measured (2026-09-23, OQ-030): a tombstoned child still counts: after `delete proj-uly.2` the next child is `proj-uly.3` (`scn_child_after_delete`). | parent `proj-uly` with no children → `proj-uly.1`; then `proj-uly.2`; parent `proj-uly.1` with no children → `proj-uly.1.1` (the grandchild raises `proj-uly.1`'s maximum, not `proj-uly`'s, so the next child of `proj-uly` stays `proj-uly.3`) | `src/storage/sqlite.rs:14398`, `src/storage/sqlite.rs:10907`, `src/sync/mod.rs:14380` | scn_child_ids, scn_child_after_delete |
| S4.26 | If the computed child id already exists, the number is incremented until a free one is found, at most **100** increments past the computed number; beyond that the run fails with `Validation failed: parent: could not find available child ID` on stderr, exit **4**. Unreachable in the corpus | — | `src/cli/commands/create.rs:612` | (case to add: `error_child_id_exhausted`) |
| S4.27 | Child nesting has **no explicit depth limit**. Depth is bounded only by the whole-id length cap of S4.35 (105 bytes) and by each segment fitting an unsigned 32-bit integer. `q --parent` uses the identical child-number rule as S4.25–S4.26 | `proj-uly.1.1` is accepted at depth 2 | `src/util/id.rs:384`, `src/cli/commands/q.rs:87` | scn_child_ids |

### Id parsing, validation and prefix normalization

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.28 | An id is split at its **last** `-`: everything before it is the prefix, everything after it is the remainder. A missing `-`, an empty prefix, or an empty remainder makes the id invalid. This lets a prefix itself contain `-` | `proj-mta` → prefix `proj`, remainder `mta`; `my-team-abc` → prefix `my-team`, remainder `abc`; `abc` → invalid; `-abc` → invalid; `abc-` → invalid | `src/util/id.rs:356`, `src/util/id.rs:360` | show_plain, error_show_missing |
| S4.29 | The prefix must be non-empty, at most **64** characters, and made only of ASCII lowercase letters, ASCII digits, `_`, `-`, `.`, `:` and `#`. Any other character (including an ASCII uppercase letter) makes the id invalid | `proj` → valid; `PROJ-mta` → invalid; `a.b:c#d_e-f` → valid prefix | `src/util/id.rs:485`, `src/util/id.rs:9` | show_plain |
| S4.30 | The remainder is split on `.`; the first segment is the hash. The hash must be non-empty, at most **40** characters, and made only of ASCII lowercase letters and ASCII digits | `mta` → hash `mta`; `MTA` → invalid; a 41-character hash → invalid | `src/util/id.rs:502`, `src/util/id.rs:10` | show_plain |
| S4.31 | Every segment after the hash must be non-empty, made only of ASCII digits, and must parse as an unsigned 32-bit integer (0 through 4294967295). A segment that is empty, signed, non-digit or out of range makes the id invalid. Leading zeros are accepted by the parse and are preserved in the input string | `proj-uly.1.2` → child path 1, 2; `proj-uly.` → invalid; `proj-uly.-1` → invalid; `proj-uly.4294967296` → invalid | `src/util/id.rs:517` | scn_child_ids |
| S4.32 | An id is a child id when the part after the prefix separator contains a `.`; when the id has no prefix separator at all, the test falls back to "the whole string contains a `.`". Depth is the count of `.` in that same part, so a root id has depth 0 | `proj-uly.1` → child, depth 1; `proj-uly.1.1` → depth 2; `proj-mta` → depth 0 | `src/util/id.rs:372`, `src/util/id.rs:384` | scn_child_ids |
| S4.33 | The parent of a parsed id is the id with its last child segment removed: with one segment it is `<prefix>-<hash>`, with more it is `<prefix>-<hash>` followed by the remaining segments each preceded by `.`. A root id has no parent. An id is a child of another when it starts with that other id and the next character is `.` | `proj-uly.1.1` → parent `proj-uly.1`; `proj-uly.1` → parent `proj-uly`; `proj-uly` → no parent | `src/util/id.rs:428`, `src/util/id.rs:457` | scn_child_ids |
| S4.34 | Prefix validation against a configured prefix succeeds when the parsed prefix equals the expected prefix or appears in the allowed list; otherwise the run fails with `Prefix mismatch: expected '<expected>', found '<found>'`, code `PREFIX_MISMATCH`, exit **4** | expected `proj`, id `other-abc` → `Prefix mismatch: expected 'proj', found 'other'` | `src/util/id.rs:547`, `src/error/mod.rs:116` | (case to add: `error_prefix_mismatch`) |
| S4.35 | Record-level id validation additionally rejects an id that is empty after trimming, an id longer than **105** bytes (64 + 1 + 40), and an id that fails S4.28–S4.31, with the reasons `id: cannot be empty`, `id: exceeds 105 characters` and `id: invalid format (expected prefix-hash)` | a 106-byte id → `id: exceeds 105 characters` | `src/validation/mod.rs:84`, `src/util/id.rs:11` | (case to add: `error_jsonl_id_too_long`) |
| S4.36 | Id normalization for lookup is a **full Unicode lowercase** of the input (not ASCII-only), applied after trimming. A non-ASCII uppercase character therefore folds by Unicode rules and may change byte length Measured (2026-09-23, OQ-118): `show PROJ-MTÄ` is `Issue not found: PROJ-MTÄ`, quoting the id as typed. | `PROJ-MTA` → `proj-mta`; `İ` folds to two code points Amended (2026-09-26): measured, U+212A KELVIN SIGN, whose Unicode lower case is ASCII `k`, finds the id spelled with `k` (`show proj-mKh`, `show mKh`, `show K` and `update proj-mKh -p 1` reach `proj-mkh`); every other code point whose lower case differs lower-cases to a non-ASCII text that no stored id contains. | `src/util/id.rs:566` | show_uppercase_id, error_show_nonascii_id, show_kelvin_sign_full, show_kelvin_sign_bare, update_kelvin_sign, show_kelvin_sign_alone |
| S4.37 | **Prefix normalization** of an inferred prefix: trim, then per character lowercase it by ASCII rules and keep it only when it is an ASCII lowercase letter, ASCII digit, `_`, `-`, `.`, `:` or `#`; take at most the first 64 kept characters; then strip every trailing `_`, `-`, `.`, `:` and `#`. When nothing remains the inferred prefix is `br` | `  My_Project!  ` → `my_project`; `///` → `br`; `proj` → `proj` | `src/util/id.rs:570`, `src/util/id.rs:596` | where_plain |
| S4.38 | An **explicitly configured** prefix runs the same normalization but, when nothing remains, fails with `issue_prefix "<raw value>" contains no usable ASCII letters, digits, or separators` (the raw value rendered with Rust debug quoting), rather than defaulting to `br` | configured `///` → that message | `src/util/id.rs:615` | (case to add: `error_config_prefix_unusable`) |
| S4.39 | **Prefix abbreviation** (used when the prefix is derived from the directory name): normalize per S4.37; a result of 6 characters or fewer is used unchanged. Otherwise the result is split on every non-alphanumeric character, empty pieces dropped; when two or more pieces remain, the first character of each is concatenated and used when that concatenation is longer than one character. Failing that, the first three alphanumeric characters are used, and when even that is empty the normalized value is used | `proj` (4) → `proj`; `my_long_project` → `mlp`; `verylongword` → `ver` | `src/util/id.rs:692` | where_plain |

### Partial-id resolution

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.40 | The user-supplied id is first trimmed of leading and trailing whitespace. An input that is empty after trimming fails with `Invalid issue ID format: ` (the id part is the empty string), code `INVALID_ID`, exit **3** | `"  "` → that error | `src/util/id.rs:853` | (case to add: `error_show_blank_id`) |
| S4.41 | **Step 1, exact match (highest precedence).** The trimmed input is lowercased per S4.36 and looked up as a complete id. A hit ends resolution immediately, before any prefix repair or substring search | `proj-mta` → `proj-mta`; `PROJ-MTA` → `proj-mta` | `src/util/id.rs:862` | show_plain, show_two |
| S4.42 | **Step 2, missing prefix.** Only when the lowercased input contains **no** `-` at all, the configured default prefix and `-` are prepended and that is looked up as a complete id. A hit ends resolution | `mta` with default prefix `proj` → `proj-mta` | `src/util/id.rs:871` | show_partial_hash |
| S4.43 | **Step 3, substring match on the hash portion.** The lowercased input is split per S4.28. When it splits, the part before the last `-` is remembered as a required prefix and the part after it is the search pattern; when it does not split, the whole input is the pattern and no prefix is required. An empty pattern skips this step. Every stored id is tested: the candidate's own remainder is taken, its base hash is the text before the candidate's first `.`, and the candidate qualifies when its base hash **contains** the pattern's base part as a substring. When the pattern itself contains a `.`, the candidate must additionally have a child path that equals the pattern's part after the first `.` **exactly**. Candidates are then filtered to those starting with the required prefix followed by `-`, when a prefix was remembered | `m` → `proj-7vm`, `proj-mkh`, `proj-mta` (every id whose base hash contains `m`); `mta` after step 2 misses would match `proj-mta`; `other-mta` → candidates filtered to those starting `other-` → none | `src/util/id.rs:883`, `src/util/id.rs:1060` | show_partial_ambiguous, error_show_ambiguous_json, error_update_ambiguous |
| S4.44 | The candidate list is produced in **ascending id order** by the store (a plain byte-order sort of the id text), and the harness additionally sorts the rendered list, so the captured order is ascending in both readings (see DISC-005 and the S10 handover) | store ids sorted → `proj-7vm`, `proj-mkh`, `proj-mta` (`7` = 0x37 sorts before `m` = 0x6D) | `src/storage/sqlite.rs:12302`, `scripts/ws_inner.py:105` | show_partial_ambiguous, error_show_ambiguous_json |
| S4.45 | Exactly one candidate resolves to that id. Zero candidates falls through to step 4 | `mta` → `proj-mta` | `src/util/id.rs:897` | show_partial_hash |
| S4.46 | Two or more candidates fail with `Ambiguous ID '<input>': matches ["<id>", "<id>", …]` — the **untrimmed-case original input** in single quotes, then the candidate list rendered with Rust debug formatting: `[`, each id in double quotes, separated by `, `, then `]`. The hint is `Provide more characters of the ID`. Code `AMBIGUOUS_ID`, exit **3**, retryable true. Plain mode writes `Error: <message>` then `Hint: <hint>` to **stderr** and nothing to stdout; JSON mode writes the pretty-printed error envelope to **stdout** with context fields `partial_id` and `matches` | `show m` → stderr `Error: Ambiguous ID 'm': matches ["proj-7vm", "proj-mkh", "proj-mta"]` and `Hint: Provide more characters of the ID`, exit 3 | `src/error/mod.rs:70`, `src/error/structured.rs:671` | show_partial_ambiguous, error_show_ambiguous_json, error_update_ambiguous |
| S4.47 | **Step 4, no match.** The run fails with `Issue not found: <input>` using the trimmed original input (not the lowercased or prefixed form), hint `Run 'br list' to see available issues.`, code `ISSUE_NOT_FOUND`, exit **3**, retryable false, JSON context field `searched_id` | `show proj-zzz` → stderr `Error: Issue not found: proj-zzz`, exit 3 | `src/util/id.rs:919`, `src/error/structured.rs:668` | error_show_missing, error_show_missing_json, error_update_missing, error_close_missing, error_reopen_missing, error_delete_missing, error_comments_add_missing, error_create_missing_parent |
| S4.48 | When a command takes several ids, they are resolved **left to right** and the first failure ends the run; ids to its right are never resolved and nothing is mutated Measured (2026-09-23, OQ-119): `show proj-mta proj-zzz` prints nothing of `proj-mta` (`show_two_second_missing`). | `show proj-mta proj-5u2` → both; `update m -p 1` → ambiguity error at the first id | `src/util/id.rs:1017` | show_two, close_two, update_two_ids, error_update_ambiguous, show_two_second_missing |
| S4.49 | A dependency argument beginning with `external:` bypasses resolution entirely and is stored verbatim. Otherwise, when resolution fails, `create --deps` and `dep add` retry the argument as an **exact title** match against the store: no title match re-raises the resolution error, one match uses that id, and several matches fail with `Validation failed: deps: dependency title '<input>' matches multiple issues: <id>, <id>` (comma-space joined), exit **4** | `dep add proj-mta external:other-1` → edge to `external:other-1` | `src/cli/commands/create.rs:661` | dep_add_external, create_with_deps |

### Content hash

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.50 | The content hash is SHA-256 over a fixed sequence of length-prefixed fields, each written as the field's byte length in an **unsigned 64-bit little-endian** 8-byte prefix followed by the field's raw UTF-8 bytes. The order is: title; description; design; acceptance criteria; notes; status text; priority rendered as a decimal integer; issue-type text; assignee; owner; created-by; external ref; source system; the literal `pinned` when pinned and empty otherwise; the literal `template` when a template and empty otherwise. Then seventeen reserved placeholder fields in this order: empty, empty, empty, empty, `0`, empty, empty, empty, empty, empty, empty, empty, empty, empty, empty, empty, empty (the fifth is the literal `0`; the second is the empty-valued `crystallizes` flag). Finally, when prerequisites are present and non-empty, two more fields: the literal `prerequisites` then the prerequisites text. An absent optional is the empty string. The digest is rendered as 64 lowercase hexadecimal characters | title `Test Issue`, description `A test description`, status `open`, priority `2`, type `task`, everything else absent → a 64-character lowercase hex string | `src/util/hash.rs:101`, `src/util/hash.rs:198` | (case to add: none is possible — the value reaches no output and no decision; see S4.51) |
| S4.51 | **The content hash is not observable in `--no-db`.** The field is skipped by the record's JSON representation, so it appears in no `--json` payload and in no line of `.beads/issues.jsonl`; and no in-scope command's decision reads it (the only readers are the SQLite export-hash tracking and the sync witness, both excluded from scope by PLAN §3). S4.50 is recorded for completeness and the port reproduces no byte from it. Confirmed against every golden: the string `content_hash` occurs in none of them | every captured stdout and every captured `.beads/issues.jsonl` dump → no `content_hash` key | `src/model/mod.rs:467`, `src/util/hash.rs:77` | create_min, list_json, edge_precision_rewrite (all show the key absent) |

### Time: producing, re-serializing and parsing timestamps

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.52 | A mutating command reads the wall clock **once per attempt** and uses that single instant for every timestamp it writes in that attempt: `created_at`, `updated_at`, a dependency's `created_at`, a comment's `created_at`, `deleted_at`, and `closed_at` when the new status is closed. In the pinned sandbox that instant is fixed, so all of them are byte-identical within one invocation | `create "First issue"` at the pinned instant → `created_at` and `updated_at` both `2026-01-02T03:04:05Z`; `create "Child A" --parent proj-uly` → the record and its `parent-child` edge share `2026-02-04T09:00:00Z` | `src/cli/commands/create.rs:456`, `src/cli/commands/create.rs:474` | create_min, scn_child_ids, comments_add |
| S4.53 | `closed_at` is set to that instant exactly when the status becomes `closed`, and `deleted_at` exactly when the status becomes `tombstone`. A record that is neither closed nor a tombstone carrying `closed_at` is a validation failure (`closed_at: only closed or tombstone issues may set closed_at`), and a closed record without it fails with `closed_at: closed issues must set closed_at` | `delete proj-5u2` → `deleted_at` and `updated_at` both `2026-01-02T03:04:05Z` | `src/cli/commands/create.rs:490`, `src/validation/mod.rs:130` | close_basic, delete_basic, update_status_closed |
| S4.54 | **Re-serialization.** Every timestamp written to `.beads/issues.jsonl` and to a `--json` record is RFC 3339 in **UTC with the literal `Z`**, never a numeric offset, and with a fraction whose digit count is 0, 3, 6 or 9 — the smallest of those that represents the value exactly. A whole second prints no fraction and no decimal point | `2026-01-01T10:00:05Z` → `2026-01-01T10:00:05Z`; `…05.100Z` → `…05.100Z`; `…05.1Z` → `…05.100Z` (widened to 3); `…05.000000001Z` → `…05.000000001Z` (9 digits) | `src/model/mod.rs:519` | edge_precision_list_json, edge_precision_show_json, edge_precision_rewrite, create_fraction_json, create_fraction_eighth, update_fraction, close_fraction_json |
| S4.55 | A stored timestamp with a **numeric offset** is converted to the same instant in UTC and re-serialized per S4.54, so the offset never survives a rewrite | `2026-01-01T12:00:04+02:00` → `2026-01-01T10:00:04Z` | `src/util/time.rs:146` | edge_precision_rewrite, edge_precision_ready |
| S4.56 | **Flexible timestamp parsing** (used by `--due`, `--defer`, `defer --until`) trims the input and tries the forms in this fixed order, taking the first that succeeds: (1) RFC 3339; (2) the bare date `YYYY-MM-DD`; (3) a signed relative duration; (4) a keyword. A value matching none of them is a validation failure | — | `src/util/time.rs:35` | defer_until_date, defer_relative, error_defer_bad_until, create_full_json |
| S4.57 | **Form 1, RFC 3339.** Any RFC 3339 spelling is accepted, including `Z` and a numeric offset, with any fraction length, and is converted to UTC. In addition, an input whose last nine bytes are a sign, two digits, `:`, two digits, `:`, then exactly `00` is retried with those trailing three bytes removed — so a zero **offset-seconds** spelling parses. Non-zero offset seconds are rejected Measured (2026-09-23, OQ-099): on load a space for `T`, a lower-case `t` and `z`, and an offset are accepted and written back as `…T10:00:00Z`. | `2025-01-15T12:00:00Z` → that instant; `2025-01-15T12:00:00+00:00` → the same instant; `2025-01-15T12:00:00+00:00:00` → the same instant; `2025-01-15T12:00:00+00:00:01` → rejected; `1969-12-31T23:59:59.123456789Z` → a pre-epoch instant, nanoseconds preserved | `src/util/time.rs:145`, `src/util/time.rs:156` | (case to add: `defer_until_rfc3339`), edge_time_spellings, edge_time_spellings_ok |
| S4.58 | **Form 2, bare date.** `YYYY-MM-DD` with exactly that layout becomes **09:00:00 local time** on that date, converted to UTC. The pinned sandbox sets `TZ=UTC`, so the captured result is 09:00:00Z | `--until 2027-06-01` → `2027-06-01T09:00:00+00:00` in the command's JSON payload and `2027-06-01T09:00:00Z` in the store; `--due 2026-03-01` → `due_at` `2026-03-01T09:00:00Z` | `src/util/time.rs:44` | defer_until_date, create_full_json |
| S4.59 | **Form 3, relative duration.** The value starts with `+` or `-`; its **last character** is the unit and everything before that last character — the sign included — is parsed as a signed 64-bit integer. The units are `m` minutes, `h` hours, `d` days (86400 seconds), `w` weeks (7 days). The duration is added to the current instant. A last character that is none of those four fails with `<field>: invalid unit (use m, h, d, w)`. An amount that overflows a signed 64-bit integer, a unit-times-amount product that overflows, or an addition that leaves the representable range fails with `<field>: relative duration is out of supported range`. A leading `+`/`-` whose remaining amount does not parse as an integer is **not** an error here: it falls through to form 4 | `+1h` at the pinned instant → `2026-01-02T04:04:05+00:00`; `-7d` → seven days earlier; `+30m`; `+1w`; `+9999999999999999999d` → out-of-range message | `src/util/time.rs:183` | defer_relative |
| S4.60 | **Form 4, keywords.** The trimmed input is lowercased and matched exactly against `today` → 17:00:00 local today; `yesterday` → 09:00:00 local yesterday; `tomorrow` → 09:00:00 local tomorrow; `next-week` and `nextweek` → 09:00:00 local seven days from today. Each is converted to UTC | `tomorrow` at the pinned instant with `TZ=UTC` → `2026-01-03T09:00:00Z` | `src/util/time.rs:69` | (case to add: `defer_until_tomorrow`) |
| S4.61 | **Local-time resolution.** A local wall time that is ambiguous (a fall-back repeated hour) resolves to the **earlier** UTC instant. A local wall time that does not exist (a spring-forward gap) is retried with one hour added; when that also does not exist the value fails with `<field>: invalid local time around DST transition`. With `TZ=UTC` neither path is reachable in the corpus | — | `src/util/time.rs:294` | (case to add: `defer_until_dst_gap` — needs a non-UTC `TZ`) |
| S4.62 | A value matching no form fails with the verbatim reason `invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)`. The field name is `defer_until` for `defer --until` and `date` for `create --due` and `create --defer`. Plain mode writes `Error: Validation failed: <field>: <reason>` to **stderr**, nothing to stdout, exit **4** | `defer proj-mta --until not-a-date` → stderr `Error: Validation failed: defer_until: invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)`, exit 4 | `src/util/time.rs:93`, `src/cli/commands/defer.rs:254`, `src/cli/commands/create.rs:1497` | error_defer_bad_until; (case to add: `error_create_bad_due`) |
| S4.63 | An empty or whitespace-only `--due`/`--defer` value on `create` is treated as **absent**, not as an error | `create X --due ""` → no `due_at` | `src/cli/commands/create.rs:1496` | (case to add: `create_empty_due`) |
| S4.64 | **`defer`'s own JSON payload prints the instant differently from the store.** The `defer_until` value inside the `{"deferred":[…]}` document is RFC 3339 with the numeric offset spelling `+00:00`, not `Z`, while the same instant written into `.beads/issues.jsonl` uses `Z` per S4.54. Both use the 0/3/6/9 fraction rule | `defer proj-mta --until 2027-06-01 --json` → stdout `…"defer_until":"2027-06-01T09:00:00+00:00"}]}` and the store line `…"defer_until":"2027-06-01T09:00:00Z",…` | `src/cli/commands/defer.rs:352` | defer_until_date, defer_relative |
| S4.79 | `defer` **without** `--until` parses no timestamp at all: the deferral is indefinite and the record carries no `defer_until`. `undefer` likewise parses nothing and clears any `defer_until` it finds. Re-deferring a record that is already deferred to the identical instant (including the identical absence) is a no-op | `defer proj-mta` → stdout `⏱ Deferred proj-mta: Set up database schema (indefinitely)`, no `defer_until` in the record; `undefer proj-7vm --json` → `{"undeferred":[{…,"previous_status":"deferred","status":"open"}]}` and the record loses `defer_until`; `undefer proj-mta` → stdout `⊘ Skipped proj-mta: not deferred (status: open)`, exit 0, nothing changes | `src/cli/commands/defer.rs:251`, `src/cli/commands/defer.rs:584` | defer_no_until, undefer_basic, undefer_not_deferred |

### Value parsing

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.65 | **Priority parsing.** The value is trimmed, then uppercased in full, then a single leading `P` is removed when present; the remainder is parsed as a signed 32-bit integer. A value in 0..=4 is the priority. Because the uppercase happens first, the lowercase forms `p2` and `P2` and the bare `2` are all accepted. A leading `+` is accepted by the integer parse. Interior or surrounding spaces other than the outer trim are rejected | `2` → 2; `P2` → 2; `p2` → 2; ` 3 ` → 3; `+2` → 2 | `src/model/mod.rs:177` | create_priority_p_form, list_priority_one, update_priority, update_noop_same_value, q_priority |
| S4.66 | A priority that parses but falls outside 0..=4 fails with `Priority must be 0-4, got: <n>` naming the parsed integer. A value that does not parse fails with the same message naming the value **after** trim, uppercase and `P`-stripping — so a lowercase input is reported uppercased. The hint is `Priority must be 0-4 or P0-P4 (0=critical, 4=backlog); list filters also accept ranges and comma lists such as 0-1 or 0,2.` (with the trailing full stop). Code `INVALID_PRIORITY`, exit **4**, retryable true; JSON context carries `priority` and `hint` | `-p 9` → `Priority must be 0-4, got: 9`; `-p 7` → `…got: 7`; `-p x` → `…got: X` (uppercased) | `src/error/mod.rs:107`, `src/error/structured.rs:539` | error_create_bad_priority, error_create_bad_priority_json, error_update_bad_priority; (case to add: `error_create_priority_word`) |
| S4.67 | **Priority filters** (`list`, `ready`, `blocked`, `count` `-p`/`--priority`, repeatable) parse a different grammar. Each occurrence is split on `,`; each token is trimmed; empty tokens are skipped. A token that splits at its **first** `-` into two parts that are both non-empty after trimming is an **inclusive range**: both sides parse per S4.65 and every priority from low through high is selected; a range running backwards fails with `Priority must be 0-4, got: <token>` naming the whole token. Any other token is a single priority per S4.65. The selected values are de-duplicated and returned in **ascending** order | `-p 1` → [1]; `-p 0-1` → [0,1]; `-p P0-P1` → [0,1]; `-p 0,2` → [0,2]; `-p 2 -p 0` → [0,2]; `-p 3-1` → error naming `3-1`; `-p -1` (first `-` splits into an empty left part) → falls through to the single-value path → error naming `-1` | `src/validation/mod.rs:38` | list_priority_one; (cases to add: `list_priority_range`, `list_priority_list`, `error_list_priority_backwards`) |
| S4.68 | `--priority-min` and `--priority-max` take a plain unsigned 8-bit integer through the argument parser, not the S4.65 grammar: `P1` is rejected by the parser with exit **2**, and a value above 255 is rejected by the parser | `--priority-min 1 --priority-max 3` → the inclusive band 1..=3 | `src/cli/mod.rs:1861` | list_priority_min_max |
| S4.69 | **`--estimate`/`-e`** takes a signed 32-bit integer through the argument parser. A negative value fails validation with `estimated_minutes: cannot be negative`; a value above **525960** fails with `estimated_minutes: exceeds maximum (525960 minutes / ~1 year)`. Both are exit **4**. A value that does not fit a signed 32-bit integer is rejected by the argument parser with exit **2** | `-e 90` → `"estimated_minutes":90`; `-e -1` → the negative message; `-e 525961` → the maximum message | `src/cli/mod.rs:1103`, `src/validation/mod.rs:112` | create_full_json; (cases to add: `error_create_negative_estimate`, `error_create_huge_estimate`) |
| S4.70 | **Label splitting.** `-l`/`--labels` is repeatable and each occurrence is additionally split on `,` by the argument parser. Each resulting label is then trimmed; an empty result is dropped; a label already collected is dropped (first occurrence wins, comparison is exact byte equality). `q` performs the same split-trim-drop itself | `-l one,two` → `["one","two"]`; `-l "  trimmed  "` → `["trimmed"]`; `-l a -l a` → `["a"]`; `-l ",,"` → none | `src/cli/mod.rs:1090`, `src/cli/commands/create.rs:772`, `src/cli/commands/q.rs:14` | create_full_json, list_label_two_and, update_set_labels |
| S4.71 | **Label validation.** A label must be non-empty, at most **50 bytes**, and made only of ASCII alphanumerics, `-`, `_` and `:`. Otherwise the run fails with `Validation failed: label: <reason>` on stderr, exit **4**, where the reason is `cannot be empty`, `exceeds 50 characters` or `invalid characters (only alphanumeric, hyphen, underscore, colon allowed)`. A record may carry at most **64** labels (`labels: exceeds 64 labels`) | `-l "bad label!"` → `Error: Validation failed: label: invalid characters (only alphanumeric, hyphen, underscore, colon allowed)`, exit 4; `backend`, `a-b_c:d`, `x1` → valid | `src/validation/mod.rs:428`, `src/validation/mod.rs:24` | error_create_bad_label, error_label_add_invalid |
| S4.72 | **Status spellings.** A status value is lowercased and matched against `open`, `in_progress` (the spelling `inprogress` is also accepted and folds to `in_progress`), `blocked`, `deferred`, `draft`, `closed`, `tombstone`, `pinned`. Anything else becomes a **custom** status carrying the lowercased text. On `create -s` and `update --status` a custom value is accepted; on the `list --status` filter an undeclared custom value is refused with `Validation failed: status: unknown status '<value>'. Built-in statuses: open, in_progress, blocked, deferred, draft, closed, tombstone, pinned. Custom statuses must be declared in .beads/policy.yaml (workflow.statuses) or exist on at least one issue. Use --status all to include every status`, exit **4**. A custom status is capped at 50 characters | `-s in_progress` → `in_progress`; `-s InProgress` → `in_progress`; `--status bogus` on `list` → the refusal above | `src/model/mod.rs:87`, `src/model/mod.rs:139` | create_status_in_progress, list_status_in_progress, list_status_bad |
| S4.73 | **Issue-type spellings.** A type value is lowercased and matched against `task`, `bug`, `feature`, `epic`, `chore`, `docs`, `question`. Anything else becomes a **custom** type carrying the lowercased text and is accepted on create; a custom type is capped at 50 characters | `-t feature` → `feature`; `-t spike` → `spike`; `-t SPIKE` → `spike` | `src/model/mod.rs:220`, `src/model/mod.rs:261` | create_custom_type, create_full_json, list_type_bug |
| S4.74 | **Dependency-type spellings.** A dependency type is lowercased and matched against `blocks`, `parent-child`, `conditional-blocks`, `waits-for`, `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes`, `caused-by`. Anything else becomes a custom type carrying the lowercased text | `-t related` → `related`; `-t inspired-by` → `inspired-by` | `src/model/mod.rs:288` | dep_add_related, dep_add_custom_type, dep_add_blocks |
| S4.75 | **Title limits.** A title that is empty or whitespace-only fails with `Validation failed: title: cannot be empty`, exit **4**. A title longer than **500 characters** — counted in Unicode scalar values, not bytes, unlike the id seed of S4.1 — fails with `Validation failed: title: exceeds 500 characters`, exit **4**. Exactly 500 is accepted. A title containing a NUL byte fails with `title: contains NUL byte` | `""` → cannot be empty; `"   "` → cannot be empty; 500 `t` characters → accepted (`edge_create_max_title` → `proj-bc6`, **✓recomputed**); 501 `t` characters → exceeds 500 characters | `src/validation/mod.rs:162`, `src/validation/mod.rs:21` | error_create_empty_title, error_create_blank_title, error_create_long_title, edge_create_max_title |
| S4.76 | **Comment text.** A comment whose text is empty fails with `Validation failed: text: comment text cannot be empty`, exit **4** | `comments add proj-mta ""` → that message | `src/validation/mod.rs:460` | error_comments_add_empty |
| S4.77 | **`--limit` and `--offset`** are unsigned pointer-width integers (64-bit on the pinned target). `--limit 0` means **unlimited**, and 0 is also the value printed in the JSON envelope when the user passed no limit. `has_more` is false whenever the effective limit is 0; otherwise it is true exactly when the total is strictly greater than offset plus limit, that addition being saturating | no `--limit` → `"total":7,"limit":0,"offset":0,"has_more":false`; `--limit 1` → `"total":7,"limit":1,"offset":0,"has_more":true`; `--limit 2 --offset 1` → `"total":7,"limit":2,"offset":1,"has_more":true` | `src/cli/mod.rs:1886`, `src/cli/commands/list.rs:221` | list_json, list_limit_one, list_limit_offset, empty_list_json, scn_dup_title |
| S4.78 | A `--limit` value that is not a non-negative decimal integer is refused by the **argument parser** before any workspace is touched: stderr carries `error: invalid value 'x' for '--limit <LIMIT>': invalid digit found in string`, a blank line, then `For more information, try '--help'.`, exit **2**. A `--limit` with no value carries `error: a value is required for '--limit <LIMIT>' but none was supplied` in the same shape | `list --limit x` → exit 2 as above; `list --limit` → exit 2 as above | `src/cli/mod.rs:1886` | usage_limit_bad, usage_limit_noval |


<!-- from docs/spec-parts/S4b.md -->

### Shared query vocabulary

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.100 | **Default visibility.** Every issue-listing relation (`list`, `search`, `count`, `epic status`) selects from the issue set minus a status exclusion. With `include_closed` false and `include_deferred` true the excluded statuses are exactly `closed` and `tombstone`. With `include_closed` false and `include_deferred` false the excluded statuses are `closed`, `tombstone` and `deferred`. With `include_closed` true and no explicit `--status`, the only excluded status is `tombstone`. With `include_closed` true and an explicit `--status`, nothing is excluded by this rule (a `--status tombstone` request therefore shows tombstones). Status matching is on the stored lowercase status string; custom statuses are never excluded by this rule. | fixture `basic`, 8 records, one `closed` (`proj-mkh`), one `deferred` (`proj-7vm`) → `list` yields 7 rows; `list --all` yields 8; `count` yields 6 | `src/storage/sqlite.rs:8413`, `src/storage/sqlite.rs:9141` | list_plain, list_all, list_all_json, count_plain, count_include_closed, list_graph, r15_list_label_repeat, r15_list_label_repeat_none, r15_list_label_any_two, r15_list_label_and_any, r15_list_prio_range, r15_list_prio_inverted, r15_list_assignee_unassigned, r15_list_unassigned_all, r15_list_sort_title_reverse, r15_list_sort_created_reverse_json, r15_list_sort_priority_reverse_all, r15_list_limit0_large, r15_list_long_all, r15_list_long_label, r15_list_large_label_limit, r15_list_parents_sort_id_rev, r15_list_hier_status, r15_list_precision_sort_title, r15_list_precision_label, r15_list_type_multi, r15_list_status_multi_all, r15_list_prio_word, r15_search_label, r15_search_status_json, r15_search_prio, r15_search_assignee, r15_search_all_closed |
| S4.101 | **`include_closed` derivation for `list`/`search`.** `include_closed` is true when `--all`/`-a` is given, or when any `--status` value is the meta-value `all` (case-insensitive after trimming), or when any parsed `--status` value is terminal (`closed` or `tombstone`). Otherwise false. | `list --status closed --json` → the closed record is returned although `--all` was not given, `total` 1 | `src/cli/commands/list.rs:530`, `src/cli/commands/search.rs:597` | list_status_closed, search_status |
| S4.102 | **`include_deferred` derivation for `list`/`search`.** `include_deferred` is true when `--deferred` is given, or `--all` is given, or no `--status` was given at all, or a parsed `--status` value is `deferred`. It is false only when an explicit `--status` list was given that does not contain `deferred`. Deferred work is therefore visible by default and vanishes the moment any other status is named. | `list` → `proj-7vm` (deferred) present; `list --status open --json` → `total` 5, `proj-7vm` absent | `src/cli/commands/list.rs:537` | list_plain, list_status_open, list_deferred |
| S4.103 | **Templates are always excluded** from `list`, `search`, `count`, `ready`, the blocked relation and the epic child counts, unless `--include-templates` was given (`count` only). A record with no template marker counts as not a template. Measured (2026-09-23, OQ-047): hidden from `list`, `ready` and `count`; `count --include-templates` counts it. | (no template in any fixture; every case exercises the "not a template" branch) | `src/storage/sqlite.rs:8426`, `src/storage/sqlite.rs:10275` | list_plain *(case to add: `list_template_hidden` — a fixture with `"is_template":true`)*, list_template_hidden, ready_template_hidden, count_templates, count_include_templates |
| S4.104 | **`--status all`.** When any `--status` value equals `all` (ASCII case-insensitive, surrounding whitespace trimmed) the status filter is dropped entirely and both `include_closed` and `include_deferred` become true. It is not a status name and is never matched against a record. Corrected (2026-09-23, OQ-039): `--status all` does NOT list tombstones; the example's "plus tombstones" is wrong (`scn_list_status_all`: the deleted `proj-b75` is absent from the JSON and the plain form). | `list --status all` → same rows as `list --all` plus tombstones | `src/cli/commands/mod.rs:287` | scn_list_status_all |
| S4.105 | **Unknown `--status` value (error).** A `--status` value that is not one of `open`, `in_progress`/`inprogress`, `blocked`, `deferred`, `draft`, `closed`, `tombstone`, `pinned`, `all` (all matched after lowercasing) is a *custom* status. A custom status is accepted only when it appears in `.beads/policy.yaml` `workflow.statuses` or is the status of at least one stored issue (both compared lowercased); otherwise the run fails before any query with exit code `4` and this single stderr line (with a trailing newline, no trailing space): `Error: Validation failed: status: unknown status 'bogus'. Built-in statuses: open, in_progress, blocked, deferred, draft, closed, tombstone, pinned. Custom statuses must be declared in .beads/policy.yaml (workflow.statuses) or exist on at least one issue. Use --status all to include every status` — the quoted name is the user's value verbatim, not lowercased. stdout is empty. Measured (2026-09-23, OQ-114): the JSON envelope has `hint` null, `retryable` true, `context` `{"field":"status","reason":…}` (`list_status_bad_json`). | `list --status bogus` → exit 4, the line above | `src/cli/commands/list.rs:449` | list_status_bad, list_status_bad_json |
| S4.106 | **Custom status accepted.** A `--status` value that is not built-in but *is* carried by a stored issue passes S4.105 and then matches records whose stored status equals the value lowercased. It is never terminal, so it does not set `include_closed`. Measured (2026-09-23, OQ-040): a stored custom `Triage` loads as `triage` and is selected by `--status Triage` and `--status triage`; plain mode shows the `?` glyph. | *(case to add: `list_status_custom` on a fixture whose record carries `"status":"triage"`)* | `src/cli/commands/list.rs:459`, `src/model/mod.rs:139` | *(case to add as above)*, list_status_custom, list_status_custom_lower |
| S4.107 | **`--priority`/`-p` forms (list, count, blocked, ready).** Each occurrence is split on `,`; each token is trimmed and an empty token is skipped. A token containing `-` with a non-empty part on each side is a closed range: both ends are parsed as priorities and every integer from low to high inclusive is selected; low greater than high is an error. Any other token is one priority. A priority token is trimmed, uppercased, an optional leading `P` is stripped, and the remainder must parse as an integer in `0..=4`. The selected set is deduplicated and yielded in ascending numeric order. The filter then matches records whose priority is in that set. Measured (2026-09-23, OQ-037, OQ-116): `3-1` is `Priority must be 0-4, got: 3-1` with the hint `Ranges run low to high: use 1-3.`; `P9` quotes `9` with the long hint (`list_priority_backwards`, `list_priority_p9`), exit 4. | `-p 1` → `{1}` → `proj-170`, `proj-mta`; `-p 0-1` → `{0,1}` → `proj-ptp`, `proj-170`, `proj-mta`; `-p P3,0` → `{0,3}` | `src/validation/mod.rs:38`, `src/model/mod.rs:174` | list_priority_one, *(case to add: `list_priority_range` — `["@fx=basic","list","-p","0-1"]`, observed: `proj-ptp`, `proj-170`, `proj-mta`)*, list_priority_backwards, list_priority_p9 |
| S4.108 | **`--priority-min` / `--priority-max` bounds (error).** Both are parsed by the argument layer as unsigned integers, then validated to lie in `0..=4`. Out of range fails with exit code `4` and stderr `Error: Priority must be 0-4, got: 5` followed by `Hint: Priority must be 0-4 or P0-P4 (0=critical, 4=backlog); list filters also accept ranges and comma lists such as 0-1 or 0,2.` (each line newline-terminated). No comparison between min and max is performed, so `--priority-min 4 --priority-max 0` is accepted and yields no rows. | `list --priority-min 5` → exit 4, the two lines above | `src/cli/commands/list.rs:867` | list_priority_min_max, *(case to add: `error_list_priority_min_bad` — `["@fx=basic","list","--priority-min","5"]`)* |
| S4.109 | **`--title-contains` matching (`LIKE`).** The needle is escaped by replacing `\` with `\\`, then `%` with `\%`, then `_` with `\_`, and wrapped as `%<escaped>%`; the comparison is SQL `LIKE … ESCAPE '\'`, which folds case for the 26 ASCII letters only. A non-ASCII letter therefore matches case-sensitively, and `%`/`_` in the needle are literal. Measured (2026-09-23, OQ-048): `--title-contains ärger` and `search ärger` do not find `Ärger`: ASCII-only. | `--title-contains auth` → `proj-170` ("Implement user **auth**"), `proj-uly` ("**Auth** epic"); `--title-contains Ünï` → `proj-b75`; `--title-contains ünï` → no rows; `--title-contains %` → no rows | `src/storage/sqlite.rs:8434`, `src/storage/sqlite.rs:18814` | list_title_contains, *(case to add: `list_title_contains_unicode_lower` — `["@fx=basic","list","--title-contains","ünï"]` → empty; `list_title_contains_percent` — `["@fx=basic","list","--title-contains","%"]` → empty)*, list_title_contains_nonascii, search_nonascii |
| S4.110 | **Timestamp comparison.** `created_at` / `updated_at` comparisons are chronological on the instant, not textual on the input bytes. Two inputs denoting the same instant compare equal however they were spelled. Mechanism: each timestamp is parsed on load and re-emitted in one canonical form (UTC, fixed `+00:00` offset, fractional digits 0, 3, 6 or 9 — the shortest that is exact), and lexicographic order of that canonical form coincides with chronological order; the Rust-side default sort compares parsed instants directly. [inference: that the two paths coincide — both were observed to give the same order on fixture `precision`.] | fixture `precision`: `…05Z`, `…05.000000001Z`, `…05.100Z`, `…05.1Z`, `12:00:04+02:00` order ascending as `aae` (10:00:04Z) < `aaa` (…05) < `aac` (…05.000000001) < `aab` = `aad` (…05.100) < `aaf` (…06) | `src/storage/sqlite.rs:1658`, `src/storage/sqlite.rs:18248`; resolves OQ-001 | edge_precision_ready, edge_precision_sort_created, edge_precision_list_json |
| S4.111 | **Id tie-break.** Every ordering in this section ends with the issue id ascending, compared as a byte sequence (no case folding, no numeric awareness). Ids are unique, so every ordering below is total and the sorts' stability never reaches an output. | `proj-aab` before `proj-aad` at the identical instant `…05.100Z`; `proj-170` before `proj-5u2` at the identical `updated_at` `11:00:00Z` (`'1'` 0x31 < `'5'` 0x35) | `src/storage/sqlite.rs:18253`, `src/storage/sqlite.rs:8467` | edge_precision_ready, list_sort_updated |
| S4.112 | **Title ordering (`COLLATE NOCASE`).** `--sort title` orders by title with the ASCII-only case-insensitive collation: bytes `A`–`Z` (0x41–0x5A) compare equal to `a`–`z` (0x61–0x7A); every other byte compares by its own value, so any byte of a multi-byte UTF-8 character (≥ 0xC2) sorts after every ASCII character. | `precision` fixture ascending: `apple lower title`, `empty strings kept`, `millis`, `nanos`, `offset zone`, `one tenth short form`, `UPPER case title`, `whole second`; `basic` ascending ends with `Ünïcödé title …` after `Write API docs` | `src/storage/sqlite.rs:8460` | edge_precision_sort_title, list_sort_title |
| S4.113 | **The pinned instant.** Every "now" in this section (`ready`'s `defer_until` comparison, `stats`' ready count, `list --overdue`) is one wall-clock read per invocation. Under the capture harness it is the case's `@time=` value, default `2026-01-02 03:04:05` UTC. `defer_until` and `due_at` comparisons are against that instant, both sides as instants. | `proj-7vm` has `defer_until` `2027-06-01T09:00:00Z` > now → excluded from `ready` | `src/storage/sqlite.rs:10262`, `src/cli/commands/stats.rs:513` | ready_plain, ready_include_deferred, scn_lifecycle, q_fraction, update_fraction, close_fraction_json |

### `list`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.114 | **The `list` relation.** `list` selects every issue that survives S4.100/S4.103 and every filter in S4.121–S4.132 simultaneously (all filters conjoined), orders it by S4.115, then drops `--offset` rows and keeps at most `--limit` rows. Nothing is deduplicated; each issue appears at most once. | `@fx=basic list` → 7 rows in the order `proj-ptp`, `proj-170`, `proj-mta`, `proj-b75`, `proj-uly`, `proj-5u2`, `proj-7vm` | `src/cli/commands/list.rs:62`, `src/storage/sqlite.rs:8334` | empty_list, list_plain, list_json, list_json_again, r17_list_long_parents, r17_list_long_graph, r17_list_long_due, r17_list_long_wrap, r17_list_wrap, r17_list_long_reverse, r17_list_long_tree, r17_list_title_contains_fold, r18_list_overdue_due, r18_list_overdue_json, r18_list_overdue_all, r18_list_deferred_due, r18_list_deferred_basic_json, r18_list_bad_limit_json, r18_list_csv_filter, r18_list_csv_reverse_offset, r18_list_csv_all_fields, r18_list_csv_fields_bad, r18_list_json_fields_all, r18_list_csv_quiet, r19_list_sort_updated_rev_json, r19_list_large_title_rev, r19_list_limit0_offset3, r19_list_offset_beyond, r19_list_offset_beyond_json, r19_list_offset_end_json, r19_list_desc_long, r19_list_notes_long, r19_list_notes_basic_json, r19_list_id_repeat, r19_list_id_missing, r19_list_prio_zero_zero, r19_list_prio_min4_all, r19_list_prio_min5, r19_list_large_long, r19_list_large_pretty, r19_list_graph_tree, r19_list_wrap_unicode, r19_list_large_status_ip, r19_usage_list_offset_neg, r19_usage_list_offset_word, r20_x_empty_optionals_list, r20_x_lead_subsecond_list, r20_x_comment_trailing_ws_list, r20_x_empty_texts_list, r20_x_v_labels_many_list, r20_x_dup_id_list, r21_ws_cwd_deep_list_json, r21_ws_redirect_self_list, r21_ws_redirect_oversize_json, r21_so_tree_prio, r21_so_tree_prio_rev_json, r21_so_tree_title, r21_so_tree_id_rev, r21_so_tree_created, r21_so_precision_updated_rev, r21_so_due_list_due, r21_so_diamond_prio, r21_so_status_sort, r21_so_large_prio_offset, r21_sh_cols20_list, r21_u_title_contains_ss, r21_u_list_assignee_upper, r22_g_db_eq, r22_g_short_p_eq, r22_g_short_s_attached, r22_g_short_ar, r22_g_short_cluster_value_end, r22_g_list_dashdash_extra, r22_g_limit_plus, r22_g_limit_neg, r22_g_offset_huge, r22_g_prio_min_neg, r22_g_prio_max_eq_empty, r22_g_list_two_errors, r22_e_toon_default_json_list, r22_e_term_dumb, r22_l_list_closed_limit, r22_l_list_assignee_json, r22_l_list_ids, r22_l_list_prio_range, r22_l_list_offset_past, r22_l_list_sort_updated, r22_l_list_deferred_limit, r22_j_priority_synonym |
| S4.115 | **Default order (`priority ASC, created_at DESC, id ASC`).** With no `--sort` and no `--reverse`, rows are ordered by priority ascending (0 first), then `created_at` descending (newest first, S4.110), then id ascending (S4.111). This order is produced by three code paths that agree: a Rust sort over parsed values when limit is 0/absent and offset is 0; a per-priority scan 0,1,2,3,4 each ordered `created_at DESC, id ASC` when a positive limit is given with offset 0 and no other filter; and SQL `ORDER BY priority ASC, created_at DESC, id ASC` otherwise. | `basic`: P0 `proj-ptp`; P1 `proj-170` (10:00:01) then `proj-mta` (10:00:00); P2 `proj-b75` (10:00:06) then `proj-uly` (10:00:04); P3 `proj-5u2`; P4 `proj-7vm`. With `--all`, P2 becomes `proj-mkh` (10:00:07), `proj-b75`, `proj-uly` | `src/storage/sqlite.rs:18248`, `src/storage/sqlite.rs:8519`, `src/storage/sqlite.rs:8484` | list_plain, list_all, list_limit_one, list_limit_offset |
| S4.116 | **`--sort` keys.** Accepted keys and their orders (ascending direction shown; `--reverse` flips as stated in S4.117): `priority` → priority ascending, `created_at` descending, id ascending; `created_at` and its alias `created` → `created_at` descending, id ascending; `updated_at` and its alias `updated` → `updated_at` descending, id ascending; `title` → title ascending under S4.112, id ascending. Note that `created_at`/`updated_at` are *descending* in the un-reversed form. | `--sort created_at` on `basic` → `proj-b75`(06), `proj-7vm`(05), `proj-uly`(04), `proj-5u2`(03), `proj-ptp`(02), `proj-170`(01), `proj-mta`(00); `--sort updated_at` → `proj-7vm`(14:00), `proj-ptp`(12:30), `proj-170`(11:00), `proj-5u2`(11:00), `proj-b75`, `proj-uly`, `proj-mta` | `src/storage/sqlite.rs:8444` | list_sort_created, list_sort_updated, list_sort_title, edge_precision_sort_created, edge_precision_sort_title |
| S4.117 | **`--reverse`/`-r`.** With no `--sort`, `--reverse` gives `priority DESC, created_at ASC, id ASC` — the id tie-break stays ascending. With `--sort priority` it gives `priority DESC, created_at ASC, id ASC`; with `--sort created_at`/`updated_at` it flips that key to ascending; with `--sort title` it flips the title key to descending. In every case the trailing id key remains ascending. | `list -r` on `basic` → `proj-7vm`, `proj-5u2`, `proj-uly`(10:00:04), `proj-b75`(10:00:06), `proj-mta`(10:00:00), `proj-170`(10:00:01), `proj-ptp` | `src/storage/sqlite.rs:8487` | list_reverse |
| S4.118 | **Bad `--sort` (error).** A `--sort` value other than `priority`, `created_at`, `updated_at`, `title`, `created`, `updated` fails before the query with exit code `4` and the single stderr line `Error: Validation failed: sort: invalid sort field 'nonsense'` (newline-terminated, the value quoted verbatim). stdout is empty. For `list` the check runs *after* the status check of S4.105 and after the query filters are built; for `search` it runs first. Measured (2026-09-23, OQ-114): the JSON envelope has `hint` null, `retryable` true, `context` `{"field":"sort","reason":…}` (`list_sort_bad_json`). | `list --sort nonsense` → exit 4, that line | `src/cli/commands/list.rs:853`, `src/cli/commands/search.rs:656` | list_sort_bad, list_sort_bad_json |
| S4.119 | **`--limit` and `--offset`.** `list`'s `--limit` default is **0**, and 0 means unlimited — the whole matching set. `--offset` default is 0. A positive limit keeps the first `limit` rows after skipping `offset` rows of the ordered relation. An offset at or past the end yields no rows (not an error). | `list --limit 1 --json` → 1 row (`proj-ptp`); `list --limit 2 --offset 1 --json` → `proj-170`, `proj-mta`; `list --offset 99 --json` → `"issues":[]` with `"total":7` | `src/cli/mod.rs:30`, `src/storage/sqlite.rs:8492` | list_limit_one, list_limit_offset, *(case to add: `list_offset_past_end` — `["@fx=basic","list","--offset","99","--json"]`)* |
| S4.120 | **The structured page envelope `{issues,total,limit,offset,has_more}`.** `limit` and `offset` echo the user's values (0 and 0 by default). `total` is the count of the matching set *before* limit and offset: it is a separate count query when structured output is requested and (`limit` ≠ 0 or `offset` ≠ 0) and no client-side filter is active; otherwise it is the length of the returned row vector. `has_more` is `false` when `limit` is 0, else `total > offset + limit` (saturating addition). | `list --json` → `"total":7,"limit":0,"offset":0,"has_more":false`; `list --limit 1 --json` → `"total":7,"limit":1,"has_more":true`; `list --limit 2 --offset 1 --json` → `"total":7,"limit":2,"offset":1,"has_more":true`; `list --offset 6 --limit 2 --json` → 1 row, `"total":7,"has_more":false`; empty store → `"total":0,"limit":0,"offset":0,"has_more":false` | `src/cli/commands/list.rs:101`, `src/cli/commands/list.rs:216` | empty_list_json, list_json, list_limit_one, list_limit_offset, list_all_json |
| S4.121 | **Truncation notes (text output only).** When text output was truncated by a positive `--limit` the run writes one stderr line and continues with exit 0. On the SQL path (no client-side filter) the line is `[note] Output truncated to 2 issues. Use --limit 0 for all results.`; on the client-filter path (S4.133) the exact pre-truncation total is known and the line is `[note] Showing 2 of 7 issues. Use --limit 0 for all results.`. Neither line is emitted for `--json`/`--toon`, nor under `--quiet`. | `list --limit 2` → two rows on stdout plus the first line on stderr | `src/cli/commands/list.rs:188` | *(case to add: `list_limit_note` — `["@fx=basic","list","--limit","2"]`)* |
| S4.122 | **`--status`/`-s` filter.** Repeatable; the record's status must equal one of the values, each lowercased (S4.105/S4.106). Several values are an OR. The status filter also feeds S4.101/S4.102, so naming `closed` makes closed rows visible and naming only non-deferred statuses hides deferred rows. | `list -s in_progress` → `proj-ptp`; `list -s open -s closed --json` → 6 rows, `"total":6`, ordered `proj-170`, `proj-mta`, `proj-mkh`, `proj-b75`, `proj-uly`, `proj-5u2` | `src/storage/sqlite.rs:8375` | list_status_open, list_status_closed, list_status_in_progress, *(case to add: `list_status_two` — `["@fx=basic","list","-s","open","-s","closed","--json"]`)* |
| S4.123 | **`--type`/`-t` filter.** Repeatable; the record's issue type must equal one of the parsed values. Built-in types are matched case-insensitively and an unrecognized name becomes a custom type matched by its lowercased spelling. Several values are an OR. | `list -t bug --json` → `proj-ptp` only, `"total":1` | `src/storage/sqlite.rs:8387` | list_type_bug |
| S4.124 | **`--assignee` and `--unassigned`.** `--assignee <v>` keeps records whose stored assignee equals `v` exactly (byte equality, case-sensitive); a record with no assignee never matches. `--unassigned` keeps records whose assignee is absent or the empty string. The two flags are mutually exclusive at the argument layer. | `list --assignee alice --json` → `proj-170` only; `list --unassigned` → `proj-mta`, `proj-b75`, `proj-uly`, `proj-5u2`, `proj-7vm` (the `alice` and `bob` rows dropped) | `src/storage/sqlite.rs:8407` | list_assignee, list_unassigned |
| S4.125 | **`--id` filter.** Repeatable. Applied in Rust after the SQL query, it keeps records whose id is *exactly* one of the given strings — no prefix, hash or partial-id resolution (unlike `show`). An id that matches nothing simply contributes nothing. | `list --id proj-mta --json` → 1 row, `"total":1` | `src/cli/commands/list.rs:640` | list_id_filter |
| S4.126 | **`--label`/`-l` (AND).** Repeatable. Duplicate values are collapsed first. One distinct label keeps records carrying that label. Two or more distinct labels keep only records carrying *all* of them. Label comparison is exact byte equality. | `-l backend --json` → `proj-170`, `proj-mta`; `-l backend -l auth` → `proj-170` only | `src/storage/sqlite.rs:1019` | list_label, list_label_two_and |
| S4.127 | **`--label-any` (OR).** Repeatable, not deduplicated. Keeps records carrying at least one of the values. | `--label-any db --label-any auth` → `proj-170` (auth), `proj-mta` (db) | `src/storage/sqlite.rs:1056` | list_label_any |
| S4.128 | **`--label` and `--label-any` together** are conjoined: a record must carry every `--label` value *and* at least one `--label-any` value. | *(case to add: `list_label_and_any` — `["@fx=basic","list","-l","backend","--label-any","db"]` → `proj-mta`)* | `src/storage/sqlite.rs:1019` | *(case to add as above)* |
| S4.129 | **`--desc-contains` and `--notes-contains`.** Applied in Rust after the SQL query. The needle is lowercased with Rust's full Unicode lowercasing and the haystack (the record's `description` / `notes`, or the empty string when absent) is lowercased the same way; the test is substring containment. Because the folding is Unicode here and ASCII-only in `--title-contains` (S4.109) and `search` (S4.137), these two options fold non-ASCII case while the others do not. Measured (2026-09-23): `--desc-contains "LINE ONE"` matches the description `Line one…` (`list_desc_contains_unicode`); the non-ASCII fold stays OQ-048. Measured (2026-09-23, OQ-048, OQ-117): `école` finds `ÉCOLE`. The port folds ASCII, Latin-1, Latin Extended-A and Additional, Greek and Cyrillic. | `--desc-contains tables` → `proj-mta` ("Create the initial **tables**."); `--notes-contains z` → no rows (no fixture record has notes) | `src/cli/commands/list.rs:652` | *(case to add: `list_desc_contains` — `["@fx=basic","list","--desc-contains","tables"]`; `list_notes_contains_none` — `["@fx=basic","list","--notes-contains","z"]`)*, list_desc_contains_unicode, list_desc_contains_nonascii |
| S4.130 | **`--priority-min` / `--priority-max`.** Applied in Rust after the SQL query, after the bounds check of S4.108: a record is kept when its priority is ≥ min (when given) and ≤ max (when given). | `--priority-min 1 --priority-max 3 --json` → `proj-170`, `proj-mta`, `proj-b75`, `proj-uly`, `proj-5u2`; `"total":5` | `src/cli/commands/list.rs:664` | list_priority_min_max |
| S4.131 | **`--deferred`.** It does *not* restrict the result to deferred issues; it forces `include_deferred` true (S4.102) and switches `list` onto the client-filter path (S4.133). With no other filter its output is byte-identical to plain `list`, because deferred issues are already visible by default. | `list --deferred` → the same 7 rows in the same order as `list` | `src/cli/commands/list.rs:583`, `src/cli/commands/list.rs:684` | list_deferred, list_plain |
| S4.132 | **`--overdue`.** Applied in Rust: a record is kept only when it has a `due_at`, that instant is strictly before the pinned now (S4.113), and its status is not terminal (`closed`/`tombstone`). `--overdue` additionally flips the deferred default: with `--overdue` and no `--status` and no `--deferred`/`--all`, deferred records are hidden. When nothing is overdue the command prints nothing and exits 0. Measured on fixture `due` at 2026-06-01 (2026-09-22): `--all` and `--deferred` bring the deferred past-due record back and the closed one never shows; `--status deferred` alone keeps only it; a due date given as a bare day is stored at `09:00:00Z` of that day (`list_overdue_json`). | `list --overdue` on `basic` (no record has `due_at`) → empty stdout, exit 0 | `src/cli/commands/list.rs:687` | *(case to add: `list_overdue_none` — `["@fx=basic","list","--overdue"]` → empty stdout, exit 0)*, list_overdue, list_overdue_json, list_overdue_all, list_overdue_status_deferred, list_overdue_deferred, list_overdue_priority, list_overdue_none |
| S4.133 | **How filters combine and in what order.** All filters are conjunctive. `--status`, `--type`, `--priority`, `--assignee`, `--unassigned`, `--label`, `--label-any`, `--title-contains` and the visibility rule run inside the query; `--id`, `--priority-min`, `--priority-max`, `--desc-contains`, `--notes-contains`, `--deferred` and `--overdue` run afterwards in Rust. When any of the latter seven is present the query runs *without* `LIMIT`/`OFFSET` and the Rust stage applies the offset (by dropping the leading rows) and then the limit; `total` then equals the post-filter, pre-offset row count. The result order is unchanged by the Rust stage. | `list --limit 2 --id proj-mta --id proj-170` → `proj-170`, `proj-mta` (query order kept), no truncation note | `src/cli/commands/list.rs:113`, `src/cli/commands/list.rs:160` | list_id_filter, list_priority_min_max |
| S4.134 | **`--fields` does not change selection.** `--fields` names CSV columns only. In text and JSON modes the option is parsed and then ignored: the row set, the row order and every emitted field are exactly those of the same invocation without it. | `list --json --fields id,title,status,priority` → byte-identical to `list --json` | `src/cli/mod.rs:1942`, `src/cli/commands/list.rs:206` | list_fields, list_json |
| S4.135 | **`--quiet`.** Every filter, sort and count still runs; all stdout and the stderr notes of S4.121 are suppressed and the exit code is unchanged. Errors (S4.105, S4.108, S4.118) are still reported. | `list --quiet` → empty stdout, empty stderr, exit 0 | `src/cli/commands/list.rs:195` | list_quiet |
| S4.136 | **`--long` selects the same rows.** `list --long` changes only the rendering (S5); the relation, filters and order are those of S4.114/S4.115. | `list --long` → the same seven ids in the same order as `list` | `src/cli/commands/list.rs:139` | list_long, list_plain |

### `search`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.137 | **The `search` relation — fields and matching rule.** The query string is trimmed; an all-whitespace or empty query is the error of S4.142. A record matches when the needle occurs as a **substring** (not tokenized, no word boundaries, no ranking) in any one of four places: its `title`, its `description`, its `id`, or the `text` of any of its comments. Comparison is on both sides lowercased by SQL `lower()` / Rust `to_ascii_lowercase()`, which fold the 26 ASCII letters and leave every other byte alone. An absent `description` never matches. | `search auth` → `proj-170`, `proj-uly`; `search AUTH` → the same two; `search tables` → `proj-mta` (description); `search PROJ-MTA` → `proj-mta` (id); `search staging` → `proj-ptp` (comment "Reproduced on staging."); `search zzzz` → none Amended (2026-09-24, bb-rvt): the rule holds for a field of any length (`search_long_texts`: an 80 KB description and 40 KB of notes). | `src/storage/sqlite.rs:1582`, `src/storage/sqlite.rs:9709` | search_plain, search_case_insensitive, search_description, search_none, empty_search, search_long_texts |
| S4.138 | **Non-ASCII case in `search`.** Because the folding of S4.137 is ASCII-only, a needle whose letters are non-ASCII matches case-sensitively. | `search Ünï` → `proj-b75`; `search ünï` → `Found 0 issue(s) matching 'ünï'` | `src/storage/sqlite.rs:9711` | search_unicode, *(case to add: `search_unicode_lower` — `["@fx=basic","search","ünï"]` → 0 results)* |
| S4.139 | **`search` order.** There is no relevance ranking: matched rows are ordered exactly as `list` is — default `priority ASC, created_at DESC, id ASC`, or the `--sort`/`--reverse` order of S4.116/S4.117. | `search auth` → `proj-170` (P1) then `proj-uly` (P2); `search auth --sort title` → `proj-uly` ("Auth epic") then `proj-170` ("Implement user auth") | `src/storage/sqlite.rs:9718` | search_plain, *(case to add: `search_sort_title` — `["@fx=basic","search","auth","--sort","title"]`)*, r17_search_type, r17_search_type_json, r17_search_status_multi, r17_search_status_all, r17_search_label_and, r17_search_label_any, r17_search_sort_title, r17_search_sort_title_rev, r17_search_sort_created, r17_search_sort_bad, r17_search_limit0, r17_search_case_unicode, r17_search_case_upper_unicode, r17_search_quote, r17_search_backslash, r17_search_id, r17_search_id_short, r17_search_multiword, r17_search_space, r17_search_desc, r17_search_priority, r17_search_hier_long, r17_search_deferred, r17_search_deferred_flag, r17_search_fold_arger, r17_search_fold_buro, r17_search_fold_title_nonascii, r18_search_csv_label, r18_search_json_all_sort, r19_none_search, r20_s_status_ip, r20_s_status_ip_json, r20_s_type_two, r20_s_type_upper, r20_s_labelany_json, r20_s_assignee_alice_json, r20_s_unassigned, r20_s_sort_priority_rev, r20_s_sort_updated_json, r20_s_sort_title_all, r20_s_offset_limit, r20_s_offset_past, r20_s_offset_past_json, r20_s_limit1_json, r20_s_limit_all_hidden, r20_s_all_json, r20_s_hidden_json, r20_s_comment_text, r20_s_comment_text_json, r20_s_desc_tab, r20_s_punct_amp, r20_s_punct_pct, r20_s_emdash, r20_s_id_partial, r20_s_id_upper, r20_s_padded, r20_s_newline_q, r20_s_priority_max, r20_s_deferred_flag, r20_s_title_contains, r20_s_desc_contains, r20_s_id_filter_two, r20_s_large_sort, r20_s_large_default, r20_s_large_count_limit0, r20_s_unicode_desc_body, r20_s_long_texts_comment, r20_s_custom_status, r20_s_status_spellings, r20_s_template, r20_s_tree_pc, r20_s_csv_quoting, r20_s_wrap_long, r20_s_quiet, r21_u_search_quotes, r22_e_fmt_json_search, r22_e_fmt_csv_search, r22_l_search_x0 |
| S4.140 | **`search` accepts every `list` filter** and conjoins them with the match of S4.137, with the same visibility derivation (S4.101/S4.102) and the same client-filter split (S4.133) — except that `search`'s `--priority` parses each value as a *single* priority with no ranges and no comma lists (S4.148 records the divergence). | `search work --status closed` → `proj-mkh`; `search auth --id proj-uly` → `proj-uly`; `search auth -p 1` → `proj-170` Amended (2026-09-25, round 20): a `search --priority` value that is not one priority (a comma list, a range, a word) is refused before the search runs: `Priority must be 0-4, got: <value>` (upper case, a number as read), INVALID_PRIORITY, exit 4, with the plain priority hint (a word suggests its number); the flag is repeatable and each value is trimmed. | `src/cli/commands/search.rs:559` | search_status, *(case to add: `search_id_filter` — `["@fx=basic","search","auth","--id","proj-uly"]`)*, r20_s_priority_range, search_priority_range_refused, search_priority_word_refused, search_priority_repeated, search_priority_spaced, error_search_priority_list_json |
| S4.141 | **`search` pagination.** `search`'s `--limit` default is **50** (not 0); 0 means unlimited. The query fetches `limit + 1` rows; `has_more` is true when more than `limit` came back, and the extra row is dropped. The JSON envelope is `{issues, hidden_closed_count, limit, offset, has_more}` — it has **no** `total` field. In text mode the header word changes with `has_more`: `Found N issue(s) matching 'q'` when false, `Showing N issue(s) matching 'q'` when true, and a trailing **stdout** line `note: showing 1 search result(s) from offset 0; more matches exist. Use --limit 0 for all results (current limit: 1)` is printed when true. | `search auth --json` → `"limit":50,"offset":0,"has_more":false`; `search auth --limit 1 --json` → 1 row, `"limit":1,"has_more":true`; `search auth --limit 1` → the `Showing …` header plus the note line | `src/cli/mod.rs:39`, `src/cli/commands/search.rs:101`, `src/cli/commands/search.rs:353` | search_json, *(case to add: `search_limit_one` — `["@fx=basic","search","auth","--limit","1","--json"]`)* |
| S4.142 | **`hidden_closed_count`.** When the corpus excludes closed issues (`include_closed` false) and `--overdue` was not given, a second count runs over exactly the closed matches the exclusion hid, with the same needle and the same other filters and no limit/offset. It is reported as `hidden_closed_count` in JSON and, when greater than 0, as a trailing stdout line in text mode: `note: 1 closed match(es) hidden; rerun with --all to include them` — printed even when the visible result set is empty. With `include_closed` true, or under `--overdue`, it is 0 without a query. Tombstones are never counted. | `search work` → `Found 0 issue(s) matching 'work'` then the note line; `search work --json` → `{"issues":[],"hidden_closed_count":1,…}`; `search auth --json` → `"hidden_closed_count":0` | `src/cli/commands/search.rs:389`, `src/cli/commands/search.rs:376` | search_json, *(case to add: `search_hidden_closed` — `["@fx=basic","search","work"]` and `…,"--json"]`)* |
| S4.143 | **Empty `search` query (error).** A query that is empty or only whitespace fails before any query with exit code `4` and stderr `Error: Validation failed: query: search query cannot be empty`. | *(case to add: `error_search_empty_query` — `["@fx=basic","search","   "]`)* | `src/cli/commands/search.rs:81` | usage_search_alone *(the no-argument form is the argument-layer error, a different message: S1)* |

### `count`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.144 | **`count`'s default corpus is narrower than `list`'s.** `count` starts from an empty filter set in which `include_deferred` is **false**, so by default it excludes `closed`, `tombstone` **and** `deferred`. `list` includes deferred by default (S4.102). `count` sets `include_deferred` true only when `--status all` is given or a `--status` value is `deferred`. | `@fx=basic count` → `6`; `@fx=basic list` → 7 rows | `src/cli/commands/count.rs:61`, `src/storage/sqlite.rs:9141` | count_plain, count_json, empty_count, r15_list_limit0_label_large, r15_count_label_any, r15_count_by_label_all, r15_count_prio_range_by_type, r15_count_unassigned_json, r15_count_large_by_assignee |
| S4.145 | **`count --include-closed`.** Sets `include_closed` true; with no explicit `--status` the only remaining exclusion is `tombstone`, so deferred rows come back too. | `count --include-closed` → `8` (all of `basic`) | `src/cli/commands/count.rs:88` | count_include_closed |
| S4.146 | **`count` filters.** `--status` (comma-splittable, repeatable), `--type`, `--priority` (full form of S4.107), `--assignee`, `--unassigned`, `--include-closed`, `--include-templates`, `--title-contains`. They are conjoined exactly as in `list`. `count` has no `--id`, `--label`, `--limit`, `--offset`, `--sort`. A `--status` value setting `is_terminal` also sets `include_closed`. | `count --status closed` → 1 Amended (2026-09-25, round 18): `count` does not refuse an unknown `--status` value (unlike `list`, S4.105): the value matches nothing (`count --status bogus` prints `0`; `open,bogus` counts the open issues; `--by status` prints `Total: 0`). | `src/cli/commands/count.rs:60` | count_plain *(case to add: `count_status_closed` — `["@fx=basic","count","--status","closed"]`)*, r18_count_by_status_flag, r18_count_by_label_json, r18_count_by_assignee_json, r18_count_by_type_status, r18_count_status_comma, r18_count_status_comma_by, r18_count_type_comma, r18_count_prio_comma, r18_count_assignee, r18_count_unassigned_by, r18_count_title_by, r18_count_by_and_alias, r18_count_by_label_closed, r18_count_status_bogus, r18_count_prio_bad, r18_count_tree_by_prio_json, r18_count_due_by_status, r18_env_beads_dir_blank, count_status_unknown, count_status_unknown_json, count_status_known_and_unknown, count_by_status_unknown, r20_x_malformed_count, r20_x_precision_count_by, r20_x_notutf8_count, r21_ws_redirect_padded_json, r22_g_lock_timeout_bad, r22_g_qv_cluster, r22_g_count_by_eq, r22_g_count_by_twice, r22_g_count_by_and_flag, r22_e_fmt_JSON_padded, r22_e_fmt_plain, r22_e_fmt_text_toon_json, r22_e_fmt_bogus_toon_json, r22_e_fmt_json_quiet, r22_e_fmt_csv_count, r22_m_custom_count_by_status, r22_l_count_by_label, r22_l_count_by_type_all, r22_j_conflict_count |
| S4.147 | **Grouping selectors.** `--by <field>` or one of the aliases `--by-status`, `--by-priority`, `--by-type`, `--by-assignee`, `--by-label`. Selecting more than one is an error: exit code `4`, stderr `Error: Validation failed: by: only one count grouping selector can be specified; got --by-status, --by-type` — the flags are listed in the fixed order `--by`, `--by-status`, `--by-priority`, `--by-type`, `--by-assignee`, `--by-label`, joined with `, `. Measured (2026-09-23, OQ-114): two grouping selectors are `Validation failed: by: only one count grouping selector can be specified; got --by-status, --by-type`, exit 4; the JSON context is `{"field":"by","reason":…}` (`count_by_two`, `count_by_two_json`). | `count --by-status --by-type` → exit 4, that line | `src/cli/commands/count.rs:140` | *(case to add: `error_count_two_groupings` — `["@fx=basic","count","--by-status","--by-type"]`)*, count_by_two, count_by_two_json |
| S4.148 | **Group keys and the missing-value labels.** For `status` the key is the stored status string; for `priority` it is `P` followed by the digit (`P0`…`P4`); for `type` it is the type string; for `assignee` it is the assignee, or the literal `(unassigned)` when absent; for `label` one entry per label the issue carries, or the literal `(no labels)` when the issue carries none. Each issue contributes 1 to exactly one group except under `--by-label`, where a multi-label issue contributes 1 to each of its labels — so the group counts then sum to more than `total`. | `count --by-assignee` → `(unassigned): 4`, `alice: 1`, `bob: 1`, total 6; `count --by-label` → `(no labels): 4`, `auth: 1`, `backend: 2`, `db: 1`, total 6 (sum 8) | `src/cli/commands/count.rs:266`, `src/storage/sqlite.rs:9341` | count_by_status, count_by_priority_json, count_by_type, count_by_assignee, count_by_label |
| S4.149 | **Group order.** Groups are emitted in ascending byte order of the key string — not by count, not by priority number. `(` is 0x28 and therefore sorts before every ASCII letter, so `(unassigned)` and `(no labels)` come first; `P0`…`P4` happen to sort numerically because they are equal-length. | `count --by-type` → `bug`, `docs`, `epic`, `feature`, `task`; `count --by-priority --json` → `[{"group":"P0",…},{"group":"P1",…},{"group":"P2",…},{"group":"P3",…}]` | `src/cli/commands/count.rs:271` | count_by_type, count_by_assignee, count_by_label, count_by_priority_json |
| S4.150 | **`total` under grouping** is the number of *issues* in the filtered corpus, computed independently of the group counts (so it is unaffected by the multi-label inflation of S4.148). Groups with count 0 are never emitted. | `count --by-label` → `Total: 6` with four groups summing to 8 | `src/cli/commands/count.rs:328` | count_by_label, count_by_status |

### `stats`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.151 | **The `stats` summary figures.** Computed over *every* stored issue (no visibility filter, no template filter): `total_issues` = number of issues whose status is not `tombstone`; `open_issues`, `in_progress_issues`, `closed_issues`, `deferred_issues`, `draft_issues`, `tombstone_issues` = number of issues with exactly that status; `pinned_issues` = number of issues carrying the pinned marker or the status `pinned`. An issue with a custom status or the status `pinned` contributes to none of the per-status counters except `pinned_issues`. | `basic` → `total_issues` 8, `open_issues` 5, `in_progress_issues` 1, `closed_issues` 1, `deferred_issues` 1, `draft_issues` 0, `tombstone_issues` 0, `pinned_issues` 0; empty store → all 0 | `src/cli/commands/stats.rs:419` | stats_plain, stats_json, empty_stats, empty_stats_json, r16_stats_par, r16_stats_hier, r16_stats_hier_json, r16_stats_large, r16_stats_large_json, r16_stats_large_by_all, r16_stats_due_json, r16_stats_prec, r16_stats_prec_json, r16_stats_epics, r16_stats_tomb, r16_stats_q, r18_stats_noact, r18_stats_noact_json, r18_stats_act_hours, r18_stats_by_assignee_json, r18_stats_by_prio_json, r18_stats_by_label_json, r18_stats_tree_by_type, r18_stats_hier, r18_stats_graph_json, r20_st_tree_json, r20_st_r12_propagate, r20_st_r12_diamond, r20_st_r12_diamond_json, r20_st_status_spellings_json, r20_st_custom_status_json, r20_st_template_json, r20_st_large_by_label_json, r20_x_malformed_stats, r21_st_basic_early_time, r21_st_basic_far_future, r21_st_activity_hours1, r21_st_activity_hours0, r21_st_activity_hours_big, r21_st_timespell, r21_st_micros_by, r21_st_due_at_gate, r21_st_lead_subsecond, r21_st_leap_activity, r22_e_fmt_json_stats |
| S4.152 | **`blocked_issues`.** The number of distinct ids in the union of (a) issues whose stored status is literally `blocked` and (b) the keys of the BLOCKED relation (S4.159–S4.165) restricted to issues whose status is not `closed`/`tombstone`. The two sets are deduplicated by id. | `basic` → 2 (`proj-170`, `proj-uly`); after `proj-mta`, `proj-170`, `proj-uly` are closed → 0 | `src/cli/commands/stats.rs:466` | stats_plain, stats_json, scn_lifecycle |
| S4.153 | **`ready_issues`.** The number of issues that are status `open`, not ephemeral, whose id does not contain `-wisp-`, not pinned, not a template, whose `defer_until` is absent or ≤ the pinned now, that are not in the BLOCKED relation and have no unsatisfied external blocker. This is the `ready` candidate rule of S4.171 with the default status group and without `--include-deferred`. | `basic` → 3 (`proj-mta`, `proj-5u2`, `proj-b75`); after the lifecycle closes → 2 | `src/cli/commands/stats.rs:452` | stats_plain, stats_json, ready_plain, scn_lifecycle |
| S4.154 | **`average_lead_time_hours`.** For each issue whose status is `closed` **and** that carries a `closed_at`, the lead time is `closed_at − created_at` **truncated toward zero to whole hours** (the fractional part is discarded before averaging). The figure is the arithmetic mean of those whole-hour values as a floating-point number; it is absent (JSON key omitted, "Extended" section suppressed) when no closed issue carries a `closed_at`. | `basic`: only `proj-mkh`, created `10:00:07`, closed `13:00:00` → 2 h 59 m 53 s → truncated to 2 → mean 2.0 → `"average_lead_time_hours":2.0`, text `Avg Lead Time:          2.0 hours`. After `scn_lifecycle`: leads 2, 743, 743, 744 → mean 558.0 h → 23.25 days → text `23.2 days` Measured (2026-09-24, OQ-133): the subtraction keeps the fractions: a close 3599.78 s after its creation (`02:09:22.449598Z` to `03:09:22.232700Z`) is 0 whole hours, not 1 (`stats_lead_subsecond`; found by the real-store sweep on 8 of 138 stores). Measured (2026-09-24, round 12): equal instants are 0 hours; offsets (`+02:00`, `-01:30`) and micro- and nanosecond fractions are applied before the subtraction; a `closed_at` before `created_at`, even by 0.1 s or only after the offset is applied, refuses the load (exit 7, the record validation of S2.30). | `src/cli/commands/stats.rs:428`, `src/cli/commands/stats.rs:473`, `goldens/stats_lead_subsecond.out` | stats_plain, stats_json, scn_lifecycle, empty_stats, stats_lead_subsecond, r12_lead_edges_stats_json, r12_lead_micro_nano_json, r12_lead_offset_before, r12_lead_frac_before_json, r12_lead_days_stats |
| S4.155 | **`epics_eligible_for_closure`.** Over every issue of type `epic` whose status is not `closed`/`tombstone` and that is not a template, count those whose parent-child child count (S4.186) is greater than 0 and equals its closed-child count. | `basic` → 0 (`proj-uly` has one open child); after `proj-170` is closed → 1 Measured (2026-09-24, round 12): a tombstoned child counts among the closed children. | `src/cli/commands/stats.rs:521` | stats_json, scn_lifecycle, r12_epics_stats, r12_epics_blocked_json |
| S4.156 | **Conditional summary lines.** `Deferred`, `Tombstones`, `Pinned` and `Epics ready to close` lines appear only when their figure is greater than 0. The `Extended:` block appears only when the average lead time exists or the tombstone count is greater than 0. (Exact line spelling and column alignment: S5.) | empty store → the summary block has no `Deferred` line and no `Extended:` block; `basic` → `Deferred:               1` and an `Extended:` block | `src/cli/commands/stats.rs:1235` | empty_stats, stats_plain |

### The BLOCKED relation

The relation maps an issue id to a list of *blocker refs*. It is the single
source of truth for `blocked`, for `ready`'s exclusion, for `stats`'
blocked/ready counts, and for the claim and close guards. It is rebuilt from
the dependency graph on every read (under `--no-db` the cached projection is
always stale at start-up, so the compute path below is the one the goldens
exercise).

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.157 | **Step 1 — direct blockers.** For every dependency edge whose type is one of `blocks`, `conditional-blocks`, `waits-for`, whose target id does **not** begin with `external:`, and whose target is either absent from the store or has a status other than `closed`/`tombstone` and is not a template, add to the *source* issue the blocker ref `<target-id>:<target-status>`, where the status is the target's stored status or the literal `unknown` when the target does not exist. Edges of any other type (`related`, `parent-child`, custom types) never produce a direct blocker. Duplicate (source, ref) pairs collapse. | `proj-170 —blocks→ proj-mta` with `proj-mta` open → `proj-170` gets `proj-mta:open`; `proj-5u2 —related→ proj-170` contributes nothing Measured (2026-09-24, round 12): of the `external:` targets, only `blocks`, `conditional-blocks` and `waits-for` edges block, whatever the shape of the reference. | `src/storage/sqlite.rs:11333` | blocked_plain, blocked_json, dep_list, blocked_graph, blocked_graph_json, r10_n984_blocked, r10_parents_blocked_json, r12_ext_types_blocked_json, r12_ext_types_ready, r12_ext_names_blocked, r12_diamond_blocked_json |
| S4.158 | **External targets are not direct blockers here.** An edge to an `external:…` target is excluded from step 1 and handled separately (S4.166). A `parent-child` edge whose either endpoint begins with `external:` is excluded from step 2 as well. | `dep add proj-mta external:other-1` leaves `proj-mta` out of the step-1 map | `src/storage/sqlite.rs:11338`, `src/storage/sqlite.rs:11429` | dep_add_external *(mutation case; the resulting edge exercises this rule)* |
| S4.159 | **Step 2 — parent-blocked propagation.** Taking the step-1 map's keys as the initial frontier, repeatedly: for each id in the frontier that is the *target* of one or more `parent-child` edges, add to each such edge's *source* (the child) the blocker ref `<parent-id>:parent-blocked`, and push that child onto the frontier if the ref was newly added. Each id is processed at most once. A child of a blocked parent therefore becomes blocked, and the effect reaches every descendant. `parent-child` alone never makes a child block its parent in this step. Measured (2026-09-23, OQ-041): the propagation reaches a grandchild: `proj-170.1` is not ready and `blocked` names its parent `proj-170` (`scn_parent_blocked`). | a child of `proj-170` (which is blocked by `proj-mta:open`) would receive `proj-170:parent-blocked` Measured (2026-09-24, round 12): the propagation runs through four levels, and a closed source that is still a key of the relation passes `:parent-blocked` to its child although `blocked` does not list it. | `src/storage/sqlite.rs:11628` | *(case to add: `blocked_parent_propagation` — a fixture with `epic → child → grandchild` plus a `blocks` edge on the epic)*, scn_parent_blocked, r12_propagate_blocked, r12_propagate_blocked_detailed, r12_propagate_ready_json, r12_propagate_stats_json |
| S4.160 | **Step 3 — epics blocked by open children.** For every `parent-child` edge whose *target* exists, is of issue type `epic`, and whose *source* (the child) exists, has a status other than `closed`/`tombstone` and is not a template, and where neither endpoint begins with `external:`, append to the epic the blocker ref `<child-id>:child-open`. The rule applies to epics only: a non-epic parent with open children is not blocked by them. | `proj-uly` is an epic and `proj-170` is its open child → `proj-uly` gets `proj-170:child-open`; after `proj-170` closes, `proj-uly` leaves the relation | `src/storage/sqlite.rs:11515` | blocked_plain, blocked_json, scn_lifecycle |
| S4.161 | **The order of the three steps is observable.** Propagation (step 2) runs *before* the epic rollup (step 3). An epic that becomes blocked only by its own open children therefore never propagates `:parent-blocked` down onto those same children, so the children of a blocked epic remain ready. Running step 3 first would remove them from `ready`. | after `create --parent proj-uly` twice and `--parent proj-uly.1` once, `proj-uly` is blocked (open children) yet `ready` still lists `proj-uly.1`, `proj-uly.2`, `proj-uly.1.1` | `src/storage/sqlite.rs:10977` | scn_child_ids |
| S4.162 | **Blocker-list normalization.** After the three steps each issue's blocker list is sorted ascending by byte order of the whole ref string and adjacent duplicates removed; an issue left with an empty list is dropped from the relation. The sort is over the annotated ref, so `<id>:child-open` sorts before `<id>:open` before `<id>:parent-blocked` before `<id>:unknown` for the same id. | `{proj-mta:open, proj-mta:open}` → `[proj-mta:open]` | `src/storage/sqlite.rs:10996` | blocked_json |
| S4.163 | **Blocker ref → displayed id.** Wherever a ref reaches an output it is reduced to an id by cutting at the **last** `:` (so an `external:other-1:blocked` ref yields `external:other-1`). `blocked_by` in JSON and the text `[…]` list carry those ids, in the normalized order of S4.162. | `proj-mta:open` → `proj-mta`; `blocked_by":["proj-mta"]` | `src/cli/commands/blocked.rs:462` | blocked_plain, blocked_json |
| S4.164 | **Start-blockers (claim / move to `in_progress`).** The blocker list minus every ref ending in `:child-open` and every ref ending in `:parent-blocked`. An epic with only open children is claimable; a child of a blocked parent is claimable; an issue with a real `blocks`/`conditional-blocks`/`waits-for` prerequisite is not. | `update proj-170 --claim` is refused (`proj-mta:open` survives the filter) | `src/storage/sqlite.rs:10754` | error_update_blocked_claim |
| S4.165 | **Close-blockers.** The blocker list minus every ref ending in `:parent-blocked` only — the `:child-open` rollup is kept, so an epic cannot close over open children while a finished child of a blocked epic can close. | `close proj-170` is refused; `close proj-uly` is refused while `proj-170` is open | `src/storage/sqlite.rs:10782` | error_close_blocked, error_close_blocked_json, close_epic_with_open_child |
| S4.166 | **External blockers (overlay).** After the local relation is built, every edge of type `blocks`/`conditional-blocks`/`waits-for` whose target begins with `external:` adds the ref `<target>:blocked` to its source unless the external project's status lookup reports the target satisfied; and every `parent-child` edge whose *source* begins with `external:` and whose target is a local epic adds a child-open style ref to that epic on the same condition. With no external project configured no target is ever satisfied, so such an edge always blocks. The overlay is merged into the existing entry, then the list is re-sorted and deduplicated and `blocked_by_count` recomputed; an issue not already in the relation is appended (and skipped if its status is terminal). Measured (2026-09-23, OQ-045): an `external:` blocks edge blocks its source with no external project configured: `Blocked by 1 open dependencies: [external:other-1]` (`scn_external_blocker`). | `proj-mta —blocks→ external:other-1` with no external project → `proj-mta` blocked by `external:other-1` | `src/storage/sqlite.rs:11949`, `src/cli/commands/blocked.rs:148` | *(case to add: `blocked_external` — a fixture carrying an `external:` blocking edge)*, scn_external_blocker |

### `blocked`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.167 | **Candidate set.** `blocked` reports every issue that is a key of the BLOCKED relation (S4.157–S4.166) **and** whose status is not `closed`/`tombstone`. A closed issue that still has open blockers is not listed. | `basic` → `proj-170`, `proj-uly` Measured (2026-09-24, round 12): a template epic with an open child is listed. | `src/storage/sqlite.rs:11152` | blocked_plain, blocked_json, r12_dep_to_template_blocked |
| S4.168 | **`blocked` filters.** Applied in Rust after the relation is built, all conjunctive: `--type`/`-t` (repeatable, the record's type must be one of the parsed values), `--priority`/`-p` (repeatable, the full form of S4.107), `--label`/`-l` (repeatable, the record must carry **all** the labels). There is no status, assignee, id or title filter. | `blocked -t epic` → `proj-uly` only, header count 1; `blocked -p 1 --json` → `proj-170` only, `"total":1` | `src/cli/commands/blocked.rs:330` | *(case to add: `blocked_type` — `["@fx=basic","blocked","-t","epic"]`; `blocked_priority_json` — `["@fx=basic","blocked","-p","1","--json"]`)* |
| S4.169 | **`blocked` order.** After filtering, rows are sorted by priority ascending, then **blocker count descending**, then `created_at` descending, then id ascending. The blocker-count key sits between priority and `created_at`; it is absent from every other ordering in this section. | `basic` → `proj-170` (P1) then `proj-uly` (P2) | `src/cli/commands/blocked.rs:318` | blocked_plain, blocked_json |
| S4.170 | **`blocked` pagination.** `blocked`'s `--limit` default is **50**; 0 means unlimited. `total` is the filtered count *before* truncation and is what both the JSON envelope and the text header `🚫 Blocked issues (N):` report. `has_more` is `limit > 0 && shown < total`; `offset` is always 0 (`blocked` has no `--offset`). When truncation happened and output is text and not quiet, one stderr line follows: `[note] Showing 1 of 2 blocked issues. Use --limit 0 for all results.`. When nothing is blocked the JSON envelope is `{"issues":[],"total":0,"limit":50,"offset":0,"has_more":false}` and text prints `✨ No blocked issues`. | `blocked --json` → `"total":2,"limit":50,"offset":0,"has_more":false`; `blocked --limit 1` → header `(2)`, one row, the note on stderr | `src/cli/mod.rs:2796`, `src/cli/commands/blocked.rs:271` | blocked_json, empty_blocked, empty_blocked_json, *(case to add: `blocked_limit_one` — `["@fx=basic","blocked","--limit","1"]`)*, r16_blocked_hier, r16_blocked_hier_detailed, r16_blocked_par, r16_blocked_par_detailed, r16_blocked_hier_label, r16_blocked_hier_limit1_json, r16_blocked_large, r16_blocked_cycle3_detailed, r16_blocked_q, r18_blocked_type, r18_blocked_prio, r18_blocked_label, r18_blocked_limit0, r18_blocked_hier, r18_blocked_hier_detailed, r18_blocked_hier_json, r18_blocked_parents_detailed, r18_blocked_diamond_detailed, r18_blocked_quiet, r18_blocked_wrap_limit, r20_bl_large_json, r22_e_fmt_json_blocked, r22_m_spell_blocked, r22_l_blocked_json_limit |

### `ready`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.171 | **Candidate rule.** An issue is a `ready` candidate when **all** of: its status is in the configured ready status group — the default group is exactly `[open]`, so `in_progress` is *not* ready; it is not a key of the BLOCKED relation (S4.157–S4.166); its `defer_until` is absent or ≤ the pinned now (S4.113); it is not pinned (an absent marker counts as not pinned); it is not ephemeral (absent counts as not ephemeral); its id does not contain the substring `-wisp-`; it is not a template. | `basic` → `proj-mta`, `proj-5u2`, `proj-b75`; `proj-ptp` excluded (in_progress), `proj-170` excluded (blocked by `proj-mta`), `proj-uly` excluded (open child), `proj-7vm` excluded (deferred, future `defer_until`), `proj-mkh` excluded (closed) | `src/cli/commands/ready.rs:131`, `src/storage/sqlite.rs:10247` | ready_plain, ready_json, empty_ready, empty_ready_json, ready_graph, ready_graph_json, r10_n984_ready_limit, r10_n984_ready_json_oldest, r10_tree_ready_priority, r16_ready_par_parent_json_limit, r16_ready_par_sort_oldest, r16_ready_par_unassigned, r16_ready_hier, r16_ready_hier_assignee, r16_ready_large_assignee, r16_ready_large_label, r16_ready_large_label2, r16_ready_large_unassigned, r16_ready_large_limit0, r16_ready_large_t_epic, r16_ready_large_hybrid, r16_ready_tree_parent, r18_ready_assignee_alice, r18_ready_assignee_bob_json, r18_ready_label_any, r18_ready_label_any_json, r18_ready_label_and, r18_ready_label_and_any, r18_ready_prio_multi, r18_ready_prio_json, r18_ready_prio_comma, r18_ready_prio_bad, r18_ready_prio_word, r18_ready_type_multi, r18_ready_limit_neg, r18_ready_limit_json2, r18_ready_sort_hybrid_json, r18_ready_sort_oldest_tree, r18_ready_incdef_plain, r18_ready_incdef_due, r18_ready_due, r18_ready_due_json, r18_ready_hier, r18_ready_hier_json, r18_ready_tree_parent, r18_ready_tree_parent_rec, r18_ready_tree_parent_rec_limit, r18_ready_tree_parent_leaf, r18_ready_epic_tree, r18_ready_parent_type, r18_ready_parent_closed, r18_ready_unassigned_json, r18_ready_graph_prio, r18_ready_format_json, r18_ready_robot_quiet, r18_ready_quiet, r18_env_beads_dir_ready, r19_none_ready_json, r20_rd_large, r20_rd_large_json, r20_rd_large_label, r21_ws_nostore_ready_json, r21_ready_at_gate_exact, r21_ready_just_before_gate, r21_so_tree_ready_prio_json, r21_so_tree_ready_hybrid, r21_so_precision_ready_oldest_json, r21_so_parents_ready_prio, r21_sh_cols30_ready_wrap, r22_g_ready_sort_similar, r22_g_ready_assignee_unassigned, r22_g_ready_parent_epic, r22_e_fmt_json_ready, r22_e_fmt_csv_ready, r22_e_bd_actor_ready_assignee, r22_m_custom_ready, r22_m_spell_ready, r22_l_ready_json_one, r22_l_ready_oldest, r22_l_ready_type_bug, r22_j_config_malformed |
| S4.172 | **`--include-deferred`.** Adds the literal status `deferred` to the status group (unless already present, compared ASCII case-insensitively) **and** drops the `defer_until ≤ now` condition entirely — so a deferred issue with a future `defer_until` becomes a candidate. | `ready --include-deferred --json` → four rows, `proj-7vm` inserted between `proj-5u2` and `proj-b75` by its `created_at` 10:00:05 | `src/storage/sqlite.rs:1612`, `src/storage/sqlite.rs:10261` | ready_include_deferred |
| S4.173 | **`ready` filters.** Conjunctive with S4.171: `--assignee` (with no value it resolves to the current actor; with a value it is exact equality), `--unassigned` (assignee absent or empty; mutually exclusive with `--assignee`), `-l`/`--label` (AND, S4.126), `--label-any` (OR, S4.127), `-t`/`--type` (repeatable, OR), `-p`/`--priority` (full form of S4.107), `--parent <id>` (direct parent-child children of that id), `-r`/`--recursive` (with `--parent`: all transitive parent-child descendants, breadth-first with a visited set so cycles terminate), `--epic <id>` (sugar for `--parent <id> --recursive`). A `--parent` whose membership set is empty yields no rows. Measured on fixture `parents` (2026-09-22): the parent itself is never a member, even when it is ready (`ready_parent_nested`); its id resolves like any partial id (`ready_parent_partial`), and one that resolves to nothing refuses with `ISSUE_NOT_FOUND`, exit 3 (`error_ready_parent_missing`); `-r` is `--recursive`, not a reverse flag (`ready_parent_short_r`); `--recursive` without `--parent` changes nothing (`ready_recursive_only`); the members keep the ready order of the sort policy. | `ready -t task` → `proj-mta`, `proj-b75`; `ready -l db --json` → `proj-mta`; `ready --unassigned` → all three (none of the ready rows has an assignee) | `src/cli/commands/ready.rs:136`, `src/storage/sqlite.rs:10078` | ready_type, ready_label, ready_unassigned, ready_parents, ready_parent, ready_parent_json, ready_parent_recursive, ready_parent_recursive_json, ready_parent_short_r, ready_parent_nested, ready_parent_partial, ready_parent_none, ready_parent_none_json, ready_epic, ready_epic_json, ready_recursive_only, error_ready_parent_missing, error_ready_parent_missing_json |
| S4.174 | **Sort policy `hybrid` (default).** Two buckets: bucket 0 is priority ≤ 1 (P0 and P1), bucket 1 is everything else. Order by bucket ascending, then `created_at` **ascending** (oldest first), then id ascending. Priority is *not* a key inside a bucket, so a P3 can precede a P2. | `basic` → 1. `proj-mta` (P1, bucket 0); 2. `proj-5u2` (P3, created 10:00:03); 3. `proj-b75` (P2, created 10:00:06) — the P3 before the P2. `precision` (all P2) → `aae`, `aaa`, `aac`, `aab`, `aad`, `aaf`, `aag`, `aah` | `src/storage/sqlite.rs:18186`, `src/storage/sqlite.rs:10334` | ready_plain, ready_json, edge_precision_ready |
| S4.175 | **Sort policy `priority`.** Priority ascending, then `created_at` ascending, then id ascending. | `ready --sort priority` → `proj-mta` (P1), `proj-b75` (P2), `proj-5u2` (P3) | `src/storage/sqlite.rs:10339` | ready_sort_priority |
| S4.176 | **Sort policy `oldest`.** `created_at` ascending, then id ascending; priority is ignored. | `ready --sort oldest` → `proj-mta` (10:00:00), `proj-5u2` (10:00:03), `proj-b75` (10:00:06) | `src/storage/sqlite.rs:10344` | ready_sort_oldest |
| S4.177 | **`ready` pagination.** `ready`'s `--limit` default is **0** — unlimited. A positive limit truncates the sorted candidate list after external-blocker filtering; there is no offset and no page envelope (JSON output is a bare array). When truncation happened and output is text and not quiet, one stderr line follows: `[note] Showing 1 of 3 ready issues. Use --limit 0 for all results.` — the second number is the pre-truncation candidate count. | `ready --limit 1` → header `(1 issue with no blockers)`, one row, the note on stderr; `ready --limit 0` → identical to `ready` | `src/cli/mod.rs:2691`, `src/cli/commands/ready.rs:181` | ready_limit, ready_plain |
| S4.178 | **Empty-`ready` message selection.** When no candidate survives, text mode prints exactly one of three lines, chosen in this order: `✨ All work complete — no issues to work on` when the store holds no issue with a status other than `closed`/`tombstone` that is not a template; otherwise `✨ No ready issues match the requested filters or configured ready status group` when any of `--assignee`, `--unassigned`, `--label`, `--label-any`, `--type`, `--priority`, `--parent` is set or the ready status group is not the default `[open]`; otherwise `✨ No ready issues — remaining work is not currently actionable`. JSON mode prints `[]` in every case. Measured (2026-09-23, OQ-046, OQ-075): every open issue blocked or deferred prints `✨ No ready issues — remaining work is not currently actionable` (`scn_ready_all_blocked`). | empty store → the first line; `@fx=basic ready -l nosuch` → the second line | `src/cli/commands/ready.rs:293` | empty_ready, empty_ready_json, ready_parent_none (the second line, under `--parent`), ready_filtered_empty, scn_ready_all_blocked |

### `dep list`, `dep tree`, `dep cycles`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.179 | **`dep list` direction and selection.** `--direction down` (the default) collects the issue's own dependency edges; `up` collects the edges pointing at it; `both` collects both, in that order (down first). Every edge type is included, external targets too. `--type`/`-t` keeps only edges whose type string equals the normalized filter value. Each row carries the edge's `issue_id`, `depends_on_id`, `type` plus the *other* endpoint's title, status and priority (absent endpoint → the placeholder metadata of the dangling-target path). | `dep list proj-170` → 2 rows; `dep list proj-mta --direction up --json` → 1 row `{"issue_id":"proj-170","depends_on_id":"proj-mta","type":"blocks",…}`; `dep list proj-mta --direction both` → the same single dependent row under the header `Dependencies and dependents of proj-mta (1):` | `src/cli/commands/dep.rs:922`, `src/storage/sqlite.rs:14170` | dep_list, dep_list_json, dep_list_up, dep_list_up_json, dep_list_up_type_json, dep_list_both, dep_list_both_json, dep_list_both_basic, r10_tree_dep_list_up, r16_dep_list_par_both, r16_dep_list_par_up_json, r16_dep_list_par_up_pc, r16_dep_list_par_down_blocks, r16_dep_list_par_q, r16_dep_list_hier_t11_both, r16_dep_list_hier_none, r16_dep_list_hier_none_json, r16_dep_list_show_mix, r16_dep_list_show_mix_both_json, r16_dep_list_show_mix_waits, r16_dep_list_cycle3_both, r16_dep_list_large, r16_dep_list_missing_json, r16_dep_list_tree_up, r18_dep_list_type, r18_dep_list_type_json, r18_dep_list_up_type, r18_dep_list_both, r18_dep_list_both_json, r19_none_dep_list, r20_d_list_up_pc, r20_d_list_both_related, r20_d_list_both_blocks_json, r20_d_list_show_mix, r20_d_list_show_mix_json, r20_d_list_propagate, r22_g_dep_dir_similar |
| S4.180 | **`dep list` order.** The query order is: the other endpoint's priority ascending with a missing endpoint treated as priority 2, then that endpoint's `created_at` descending, then the other endpoint's id ascending. **JSON output emits that order unchanged.** Text output re-sorts by priority ascending, then `issue_id` ascending, then `depends_on_id` ascending, then dependency type ascending — so a `--direction both` text listing interleaves dependencies and dependents by priority while JSON keeps them grouped. | `dep list proj-170` → `proj-mta` (blocks, P1) then `proj-uly` (parent-child, P2) in both modes | `src/storage/sqlite.rs:14179`, `src/cli/commands/dep.rs:1239` | dep_list, dep_list_json, dep_list_up, dep_list_up_json, dep_list_both, dep_list_both_json |
| S4.181 | **`dep list` with no edges.** Text mode prints `No dependencies for proj-ptp` for direction `down`, `No dependents for …` for `up`, `No dependencies or dependents for …` for `both`, and exits 0. JSON mode prints `[]`. | `dep list proj-ptp` → `No dependencies for proj-ptp` | `src/cli/commands/dep.rs:1010` | dep_list_none, dep_list_graph_parent, dep_list_graph_none, dep_list_up_none, dep_list_up_none_json, dep_list_both_none |
| S4.182 | **`dep tree` adjacency.** The whole dependency edge set is loaded once and turned into two maps: child→targets (`down`) and target→children (`up`); each adjacency list is sorted by id and deduplicated. `--direction both` concatenates the two lists, then sorts and deduplicates. Edge type is ignored — every dependency type participates in the tree. An `external:` node is never expanded. | `dep tree proj-uly` (down) → `proj-uly` alone: the parent-child edge runs child→parent, so the epic has no dependencies of its own. `dep tree proj-uly --direction up` → `proj-uly`, then `proj-170`, then `proj-5u2` | `src/cli/commands/dep.rs:1302` | dep_tree, dep_tree_json, dep_tree_leaf, scn_child_ids, dep_tree_up_basic (the `up` walk of `proj-uly` reaches `proj-170` then `proj-5u2`, as this clause predicted), dep_tree_up, dep_tree_up_json, dep_tree_both, dep_tree_both_json, r16_dep_tree_par_9vo, r16_dep_tree_par_9vo_json, r16_dep_tree_par_9vo_up, r16_dep_tree_par_3av_up_json, r16_dep_tree_par_111_down, r16_dep_tree_par_111_both_mermaid, r16_dep_tree_par_9vo_mermaid, r16_dep_tree_par_9vo_up_mermaid, r16_dep_tree_par_maxd1_up, r16_dep_tree_hier_ig8_up_d2, r16_dep_tree_hier_ig8_up_d2_json, r16_dep_tree_hier_both_d1, r16_dep_tree_hier_q, r16_dep_tree_cycle3_json, r16_dep_tree_cycle3_up, r16_dep_tree_cycle3_both, r16_dep_tree_cycle3_mermaid, r16_dep_tree_cycle2_d01, r16_dep_tree_cycle_pc, r16_dep_tree_tomb, r16_dep_tree_showmix, r16_dep_tree_showmix_json, r16_dep_tree_showmix_up, r16_dep_tree_showmix_mermaid, r16_dep_tree_large, r16_dep_tree_missing, r16_dep_tree_partial, r16_dep_tree_tree_up_mermaid, r16_dep_tree_diamond, r18_dep_tree_up, r18_dep_tree_both, r18_dep_tree_depth1, r18_dep_tree_depth0, r18_dep_tree_mermaid, r18_dep_tree_mermaid_up, r18_dep_tree_json, r20_d_tree_tree_up, r20_d_tree_tree_both, r20_d_tree_propagate, r20_d_tree_propagate_json, r20_d_tree_diamond_mermaid, r20_d_tree_diamond, r22_e_fmt_json_dep_tree |
| S4.183 | **`dep tree` traversal order.** Depth-first, pre-order: the root is emitted first at depth 0 with no parent; a node's children are sorted by S4.184 and then visited left to right, each child's whole subtree emitted before the next sibling. Node keys are assigned `n0`, `n1`, … in emission order and a child records its parent's key and id. | `dep tree proj-uly --direction up` emits `proj-uly` (depth 0), `proj-170` (depth 1), `proj-5u2` (depth 2) | `src/cli/commands/dep.rs:1435` | dep_tree_json, *(case to add as in S4.182)*, r10_tree_dep_tree_up, r10_tree_dep_tree_down, r10_parents_dep_tree_both, r10_tree_dep_tree_root_up, r10_tree_dep_tree_grand_down |
| S4.184 | **`dep tree` sibling order.** Siblings are ordered by the target's priority ascending (a node with no metadata ranks last), then by a status rank `open` 0, `in_progress` 1, `blocked` 2, `deferred` 3, `closed` 4, `deleted`/`tombstone` 5, anything else 6 (no metadata ranks last), then by title ascending byte order, then by id ascending byte order. Measured (2026-09-23, OQ-049): P0, P1, then the open P2 before the closed P2 (`scn_dep_tree_shapes`). | `dep_tree_repeat` separates the keys: `proj-0i4` and `proj-t11` share priority 2, so the STATUS RANK decides (`open` 0 before `in_progress` 1) and it decides against the title order; `dep_tree_diamond` and `dep_tree_up_basic` separate title from id (`proj-170` before `proj-mta` at equal priority and status, although `mta` is the larger id) | `src/cli/commands/dep.rs:1809` | dep_tree_repeat, dep_tree_repeat_json, dep_tree_diamond, dep_tree_up_basic, scn_dep_tree_shapes |
| S4.185 | **`dep tree` depth limit, shared nodes and cycles.** `--max-depth` defaults to **10**. A node is expanded only when its depth is below `max_depth`, it is not external, and its id has not been expanded anywhere else in this traversal. A node whose id *is* already expanded is still emitted under each parent but carries `repeat` true when it has neighbours, and its subtree is not repeated. A node that is not expanded but has neighbours carries `truncated` true; an expanded node at exactly `max_depth − 1` whose children would exceed the limit also carries `truncated` true. A node already on its own ancestor path is skipped entirely, which terminates cycles. The expansion slot is claimed only by an occurrence that really expands, so a shallower later occurrence is never demoted. In text output `truncated` renders the prefix `├── (truncated) `, `repeat` renders `├── (shown above) `, and an ordinary non-root node `├── `; the depth-0 node has no prefix; indentation is two spaces per depth level. JSON emits every node with `id`, `title`, `depth`, `parent_id` (`null` at the root), `priority`, `status`, `truncated`, `repeat`. Measured (2026-09-23, OQ-043, OQ-065): in a diamond the second occurrence prints `(truncated) ` and in JSON carries `"truncated":true,"repeat":true`; at `--max-depth 1` a node with more below prints `(truncated) ` (`scn_dep_tree_shapes`). | `dep tree proj-uly --json` → `[{"id":"proj-uly","title":"Auth epic","depth":0,"parent_id":null,"priority":2,"status":"open","truncated":false,"repeat":false}]` | `src/cli/commands/dep.rs:1293`, `src/cli/commands/dep.rs:1758` | dep_tree, dep_tree_json, dep_tree_leaf, dep_tree_repeat_json (`truncated` and `repeat` TRUE on the same node), dep_tree_max_depth_json (nothing ever expands `proj-in9`, so both occurrences are `truncated` with `repeat` FALSE — the expansion slot is claimed only by an occurrence that really expands), dep_tree_max_depth_0 (the ROOT itself carries `truncated`), dep_tree_diamond_json (a shared node with NO neighbours carries neither flag), dep_tree_back_edge (an ancestor is skipped entirely, with no node and no marker), scn_dep_tree_shapes |
| S4.186 | **`dep cycles` edge set.** The cycle graph is built from exactly two edge families, regardless of `--blocking-only` (the flag is a compatible alias and changes nothing): every edge of type `blocks`, `conditional-blocks` or `waits-for`, oriented source→target; and every `parent-child` edge oriented **reversed**, parent→child. `related` and custom types are never counted. Both endpoints of each edge become graph nodes even when they have no outgoing edges, and each adjacency list is sorted and deduplicated. | `basic` → edges `proj-170→proj-mta` and `proj-uly→proj-170`; no cycle Amended (2026-09-24, OQ-129): measured on hand-written cycle fixtures (a git merge reaches such a store; `dep add` never does): a `conditional-blocks`/`waits-for`/`blocks` loop is a cycle; a `related` or custom edge never closes one; a `parent-child` edge child→parent plus a `blocks` edge child→parent is the cycle `[child, parent, child]` because the `parent-child` edge runs parent→child; an `external:` or dangling target is a node that closes nothing. The import accepts a store holding a cycle, and every other command reads it as any other store: `ready` leaves the members out, `blocked` lists each behind its successor, `dep tree` stops at the repeat, `show` lists both directions, `stats` counts them blocked. | `src/storage/sqlite.rs:19182` | dep_cycles, dep_cycles_json, empty_dep_cycles, dep_cycles_graph, dep_cycles_graph_json, dep_cycles_blocking_family, dep_cycles_related_ignored, dep_cycles_parent_child_json, dep_cycles_dangling, ready_cycle, blocked_cycle_json, dep_tree_cycle, show_cycle, stats_cycle_json |
| S4.187 | **`dep cycles` witness and ordering.** Strongly connected components are computed; components are each sorted by id ascending and the component list is sorted ascending. A one-node component reports a cycle only when the node has a self-edge, reported as `[id, id]`. A larger component reports one witness: starting from the component's lowest id, the first neighbour (in sorted order) that is inside the component and is not the start itself is followed to a path back to the start, and the witness is `[start, …path…]` ending at the start. The witness list is then sorted ascending, comparing the id vectors element by element. Text mode numbers them from 1 and joins ids with ` -> `. | no cycle → text `✓ No dependency cycles detected.`, JSON `{"cycles":[],"count":0,"active_count":0,"archived_closed_count":0,"total_count":0,"blocking_only":false,"include_closed":false,"scope":"active"}` Amended (2026-09-24, OQ-042, OQ-129): the path back to the start is found DEPTH FIRST over sorted neighbours inside the component, entering each node once and backtracking from a dead end; the witness is that walk's stack, not a shortest path (a→b; b→c, b→d; c→e→a; d→a gives `a b c e a`, not `a b d a`; b→c→d with d→c and d→b dead-ends back to b, then b→e→a). A start whose first neighbour lies outside the component takes the first one inside it. Two loops through one node are one component and one witness. | `src/storage/sqlite.rs:19242`, `src/cli/commands/dep.rs:2124` | dep_cycles, dep_cycles_json, empty_dep_cycles, *(case to add: `dep_cycles_found` — a `cycles` fixture with `a→b→c→a` on `blocks` edges)*, dep_cycles_found, dep_cycles_found_json, dep_cycles_dfs_path, dep_cycles_dfs_backtrack_json, dep_cycles_outside_neighbour, dep_cycles_shared_json, dep_cycles_order_json, r16_dep_cycles_par, r16_dep_cycles_large, r18_dep_cycles_graph, r18_dep_cycles_graph_json, r18_dep_cycles_two_blocking, r18_dep_cycles_closed_json, r22_e_fmt_json_dep_cycles |
| S4.188 | **`dep cycles` active vs archived.** A component in which every node's status is `closed` or `tombstone` is *archived*; all others are *active*. `active_count` and `archived_closed_count` are the two witness counts and `total_count` their sum, all reported regardless of `--include-closed`. Without `--include-closed` the reported `cycles` list holds the active witnesses only, `count` is `active_count`, `scope` is `active`, and the keys `active_cycles`/`archived_closed_cycles` are omitted when empty; when archived cycles exist but are hidden, text mode says `No active dependency cycles detected. N archived closed-only cycle(s) hidden; rerun with --include-closed to inspect them.`. With `--include-closed`, `cycles` is the concatenation of both lists re-sorted ascending, `count` is its length, `scope` is `active_and_archived`, and both extra key arrays are emitted when non-empty. A non-zero `active_count` sets the process exit code to the cycle-detected code *after* the output is written, so the JSON stream stays a single clean object; archived-only cycles never change the exit code. | `dep cycles --json` on `basic` → the object in S4.187 with exit 0 Amended (2026-09-24, OQ-081): without `--include-closed` the keys `active_cycles` and `archived_closed_cycles` are NEVER emitted, even when not empty; a component with at least one member not `closed`/`tombstone` (a stored open record, or a node that is not a stored record) is active; `--include-closed` over archived-only cycles prints them with the `Warning:` count and still exits 0; `--quiet` prints nothing and keeps exit 5. | `src/cli/commands/dep.rs:1960` | dep_cycles_json, *(case to add as in S4.187)*, dep_cycles_closed_and_open, dep_cycles_closed_and_open_json, edge_dep_cycles_include_closed, dep_cycles_closed_only, dep_cycles_closed_only_include, dep_cycles_tombstone_json, dep_cycles_half_closed, dep_cycles_found_quiet |

### `epic status` and `epic close-eligible`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.189 | **The epic set and its order.** `epic status` lists every issue of type `epic` that survives the visibility rule with `include_closed` false and `include_deferred` true (so a deferred epic is listed, a closed or tombstoned one is not) and that is not a template, ordered by the default order of S4.115 (`priority ASC, created_at DESC, id ASC`). When the set is empty text mode prints `No open epics found` and JSON prints `[]`. Measured (2026-09-23, OQ-050): the deferred epic is listed and the closed one is not (`scn_epic_status_deferred`). | `basic` → one epic, `proj-uly`; `epic status --eligible-only` on `basic` → `No open epics found` (the only epic is not eligible) | `src/cli/commands/epic.rs:260` | epic_status, epic_status_json, scn_epic_status_deferred, r16_epic_par, r16_epic_par_json, r16_epic_large, r16_epic_r12, r16_epic_r12_json, r16_epic_r12_eligible, r16_epic_hier, r16_epic_q, r18_epic_status_eligible, r18_epic_status_eligible_json, r18_epic_status_epics, r18_epic_status_epics_json, r20_ep_tree_eligible, r20_ep_propagate, r20_ep_large_json, r22_e_fmt_json_epic_status, r22_l_epic_status_json |
| S4.190 | **Child counting.** For each epic, `total_children` is the number of `parent-child` edges whose target is that epic and whose source exists and is not a template; `closed_children` is how many of those sources have status `closed` or `tombstone`. Children are counted by dependency edge, so only *direct* children count: a grandchild whose `parent-child` edge points at an intermediate child is not counted for the epic. Dotted id shape plays no part in this count. | `basic` → `proj-uly`: 1 total, 0 closed. After `create --parent proj-uly` twice and `create --parent proj-uly.1` once → 3 total (`proj-170`, `proj-uly.1`, `proj-uly.2`; `proj-uly.1.1` counts for `proj-uly.1`, not for `proj-uly`), 0 closed | `src/storage/sqlite.rs:12318` | epic_status, epic_status_json, scn_child_ids, r10_tree_epic_status, r10_tree_epic_status_json |
| S4.191 | **Eligibility and progress.** `eligible_for_close` is true exactly when `total_children > 0` and `closed_children == total_children`. The displayed percentage is `min(closed, total) * 100 / total` computed in integers with truncation, and 0 when `total` is 0. | `proj-uly` with 0/1 → `eligible_for_close` false, `Progress: 0/1 children closed (0%)`; after its only child closes → true, `1/1 children closed (100%)` plus the line `   Eligible for closure` | `src/cli/commands/epic.rs:276`, `src/cli/commands/epic.rs:435` | epic_status, epic_status_json, scn_lifecycle |
| S4.192 | **`--eligible-only`** filters the list of S4.189 down to the eligible epics after the counts are computed; an empty result takes the empty-set path of S4.189. | `epic status --eligible-only` on `basic` → `No open epics found` | `src/cli/commands/epic.rs:75` | *(case to add: `epic_status_eligible_only` — `["@fx=basic","epic","status","--eligible-only"]`)* |
| S4.193 | **`epic close-eligible` selection (mutation — selection rule only).** It computes the same list as S4.189 and keeps exactly the eligible epics of S4.191. Those are the epics it would close, with the fixed close reason `All children completed`. Under `--dry-run` nothing is written; text mode prints `Would close N epic(s):` followed by one `  - <id>: <title>` line per epic in the order of S4.189, and JSON prints the same epic-status objects. With no eligible epic and no `--dry-run`, text prints `No epics eligible for closure` and JSON prints `{"closed":[],"count":0}`. | `epic close-eligible --dry-run` on `basic` → `Would close 0 epic(s):` and no further line | `src/cli/commands/epic.rs:121` | *(case to add: `epic_close_eligible_dry_run` — `["@fx=basic","epic","close-eligible","--dry-run"]`)* |

### `show` — what is gathered, and in what order

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.194 | **Target selection.** The positional ids are the targets, in the order given on the command line; duplicates are not collapsed. With no positional id the single target is the id stored in `.beads/last-touched`, and an empty/absent sidecar is the error `no issue IDs provided and no last-touched issue`. Each target is resolved (exact id, then dotted-child/partial/hash resolution — the resolution algorithm itself is outside this section) and the records are emitted in the requested input order, not in any store order. | `show proj-mta proj-5u2` → `proj-mta` block then `proj-5u2` block | `src/cli/commands/show.rs:236`, `src/cli/commands/show.rs:396` | show_two, show_plain, show_graph_root, show_graph_root_json |
| S4.195 | **Related data gathered per target, and the order each is gathered in.** For each resolved target, in this order: (1) the issue record itself; (2) a single presence probe answering five questions — has labels, has dependency edges, has dependent edges, has comments, has parent-child children — plus the *parent*, taken as the target of the target's own `parent-child` edges, the last one by insertion order when there is more than one; (3) labels, only when present, ordered ascending by byte order of the label; (4) dependencies, only when present, the `--direction down` relation and order of S4.179/S4.180; (5) dependents, only when present, the `--direction up` relation and order of S4.179/S4.180; (6) comments, only when present, ordered by `created_at` ascending then comment id ascending; (7) events — **not** gathered by `show` (`include_events` is false, so the event list is always empty); (8) a parent-child subtree rollup, only when the target has children. An absent group is an empty list, and S5 suppresses its heading. Resolved (2026-09-23, OQ-096): `show --json` prints labels as the file holds them, unsorted and with duplicates (`edge_precision_show_json`). | `show proj-170 --json` → `"labels":["auth","backend"]`, then `"dependencies":[proj-mta (blocks), proj-uly (parent-child)]`, then `"dependents":[proj-5u2 (related)]`, then `"parent":"proj-uly"`; `show proj-uly` → no Dependencies block, one Dependents block; `show proj-ptp` → one comment | `src/storage/sqlite.rs:15717`, `src/storage/sqlite.rs:15879` | show_plain, show_json, show_epic, show_comments, show_comments_json, show_closed, show_deferred_json, show_unicode, show_unicode_json |
| S4.196 | **The `parent` field.** It is the *target* of the issue's own `parent-child` edge, i.e. the issue this record is a child of — not its children. It is present only when such an edge exists. Children are reachable only through the dependents list and the rollup. | `proj-170` has a `parent-child` edge to `proj-uly` → `"parent":"proj-uly"`; `proj-uly` itself has no `parent` key and lists `proj-170` under dependents | `src/storage/sqlite.rs:15888` | show_json, show_epic |
| S4.197 | **The subtree rollup.** Gathered only when the target is the target of at least one `parent-child` edge. It walks the parent-child subtree level by level with a visited set seeded with the target itself, so a cycle terminates and the target never counts as its own descendant, and it tallies descendant statuses. The derived status is the furthest-along non-terminal descendant status by the project's declared status order, or by the built-in ladder `draft < deferred < open < blocked < in_progress` when none is declared; statuses outside the order rank lowest and a tie resolves to the lexicographically greatest name. When every descendant is terminal the rollup status is `closed`. | `proj-uly` with one open child → a rollup is computed; `proj-mta` (no children) → no rollup | `src/storage/sqlite.rs:15790` | show_epic, *(case to add: `show_rollup_json` — `["@fx=basic","show","proj-uly","--json"]`)* |
| S4.198 | **`show` gathers no blocked information.** The BLOCKED relation is not consulted by `show`; a blocked issue's record carries its dependency edges only, with each edge's current status. | `show proj-170` lists `proj-mta (blocks) … [open]` but never names it a blocker | `src/cli/commands/show.rs:539` | show_plain, show_json |


<!-- from docs/spec-parts/S4c.md -->

### S4.200–S4.219 Cross-cutting rules for every mutating command

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.200 | **Actor resolution.** The actor is the first non-empty of: the global `--actor <name>` option; the `actor` key of the merged config layer (`.beads/config.yaml`); the environment variable `USER` (trimmed); otherwise the literal `unknown`. The same resolved actor lands in `created_by` on `create`/`q`, in `created_by` of every dependency edge the command writes, in `deleted_by` on `delete`, and in `assignee` on `update --claim`. | `USER=tester`, no `--actor` → `"created_by":"tester"` · `--actor robot-7` → `"created_by":"robot-7"` | `src/config/mod.rs:7054` (`actor_from_layer`), `src/config/mod.rs:7063` (`resolve_actor`), `src/cli/mod.rs:702` (`--actor` is a global option) | create_min, actor_flag_create, update_claim, delete_basic, actor_flag_dep_add (the new edge's `created_by`), actor_flag_delete (`deleted_by`), actor_flag_label_add (measured 2026-09-20: `label add` records no actor anywhere, so `--actor` changes nothing in its output or its store line), r18_ready_assignee_actor, r18_actor_close_json, r18_actor_label_add, r20_d_add_actor_json, r20_l_rename_actor, r20_m_add_actor_author, r20_mu_close_actor_reason, r20_mu_reopen_actor, r20_mu_defer_actor, r20_mu_claim_actor, r20_mu_claim_taken, r20_mu_delete_actor_json, r21_jr_update_claim_other, r21_up_claim_deferred_held |
| S4.201 | **Comment author resolution is a different ladder.** For `comments add` the author is the first non-blank of: `--author <name>`; the config layer's `actor` key (which the global `--actor` option feeds, and which — unlike S4.200 — has no `$USER` fallback at this stage); the first non-blank of the environment variables `BD_ACTOR`, `BEADS_ACTOR`, `USER`, `LOGNAME`, `USERNAME`, in that order; otherwise the literal `unknown`. The net effect under the pinned environment is the same as S4.200, reached one rung lower. | `--author zoe` → `"author":"zoe"` · `--actor robot-7` → `"author":"robot-7"` · neither, `USER=tester` → `"author":"tester"` | `src/cli/commands/comments.rs:449` (`resolve_author`), `:467` (`resolve_author_from_env`), `:106`, `src/config/mod.rs:6154` | comments_add, comments_add_author, actor_flag_comment |
| S4.202 | **The write-back happens once per invocation, after every id has been processed** — never per id. The command mutates an in-memory store, marking each touched issue dirty, and at the end calls one export that rewrites `.beads/issues.jsonl` in full. When the dirty set is empty and no explicit flush marker is armed, the export does not run at all. | `update proj-mta proj-5u2 -p 4` → one store dump containing both changes | `src/config/mod.rs:4914` (`flush_no_db_if_dirty`; returns early when `dirty_issue_count == 0` and `needs_flush` is not `true`), `src/cli/commands/close.rs:1322`, `src/cli/commands/create.rs:164` | update_two_ids, close_two, delete_cascade |
| S4.203 | **A mutation that changes nothing leaves the store's bytes untouched.** No dirty issue ⇒ no export ⇒ no `--- .beads/issues.jsonl ---` dump. This holds for `update` whose every named value already equals the stored value, for `dep add` of an existing edge, for `label add` of an existing label, for `label remove` of an absent label, for `dep remove` of an absent edge, for `reopen` of a non-closed issue, for `undefer` of a non-deferred issue, and for every refusal. | `update proj-mta -p 1` on a P1 issue → stdout only, no store dump | `src/config/mod.rs:4914`, `src/storage/sqlite.rs:7808` (`set_clauses.is_empty()` returns before any write) | update_noop_same_value, dep_add_duplicate, label_add_existing, label_remove_absent, dep_remove_absent, reopen_open, undefer_not_deferred |
| S4.204 | **`updated_at` becomes the invocation's instant, and only when at least one stored column actually changes.** It is stamped inside the storage update, not supplied by the command, so all ids of one batch receive the same pinned instant in a pinned run. When nothing changes, `updated_at` keeps its old value. | fixture `updated_at":"2026-01-01T10:00:00Z"`, `update proj-mta --status in_progress` at `2026-01-02T03:04:05Z` → `"updated_at":"2026-01-02T03:04:05Z"` · `update proj-mta -p 1` (already P1) → `"updated_at":"2026-01-01T10:00:00Z"` | `src/storage/sqlite.rs:7808`–`7825` | update_status, update_noop_same_value, update_two_ids |
| S4.205 | **A mutation rewrites every record, not only the changed ones.** The export re-emits each issue from the in-memory model, so untouched records lose unknown fields and gain the export defaults. Records whose own fields did not change keep their `updated_at`. This normalization is owned by S2/S5; the mutation surface only guarantees that it happens on every write-back. | `update proj-aaf -p 1` on fixture `precision` → `proj-aaa`…`proj-aah` all re-emitted: `unknown_field` dropped, `"description":""` and `"assignee":""` dropped, labels `["zeta","alpha","alpha"]` → `["alpha","zeta"]`, `…05.1Z` → `…05.100Z`, `12:00:04+02:00` → `10:00:04Z`, absent `source_repo` → `"."`, absent `compaction_level`/`original_size` → `0` | `src/config/mod.rs:4914` (export of the whole store), OQ-002 | edge_precision_rewrite |
| S4.206 | **Store line order after any mutation is ascending byte order of `id`.** New records take their place by id, not at the end. | after `create Full issue` on fixture `basic`: `proj-170, proj-5u2, proj-7vm, proj-b75, proj-bsx, proj-mkh, proj-mta, proj-ptp, proj-uly` · after `create --parent proj-uly`: `…, proj-uly, proj-uly.1` · with three children: `proj-uly, proj-uly.1, proj-uly.1.1, proj-uly.2` | Goldens (the `--- .beads/issues.jsonl ---` dump): `goldens/create_full_json.out:7` (the new `proj-bsx` lands between `proj-b75` at `goldens/create_full_json.out:6` and `proj-mkh` at `goldens/create_full_json.out:8`), `goldens/create_with_parent.out:11` (`proj-uly.1` after `proj-uly`), `goldens/scn_child_ids.out:42`–`goldens/scn_child_ids.out:45` (`proj-uly`, `proj-uly.1`, `proj-uly.1.1`, `proj-uly.2`). Source, the `--no-db` flush `src/config/mod.rs:4914` → export: `src/sync/mod.rs:10619` (`ORDER BY id ASC` over the export ids), `src/sync/mod.rs:11296` with `src/storage/sqlite.rs:15006` (small stores are hydrated by one full scan, also `ORDER BY id ASC`), `src/sync/mod.rs:10634` (batched hydration re-sorts by `id`) (handover to S6) | create_full_json, create_with_parent, scn_child_ids |
| S4.207 | **`.beads/last-touched` is one line: the id followed by one `\n`, and nothing else.** The file is created with mode `0600`, truncated on each write, and written best-effort: a failure to write it never fails the command and never changes the exit code. | `create First issue` → file bytes `proj-fyw\n` | `src/util/mod.rs:86` (`set_last_touched_id`), `src/util/mod.rs:29` (`LAST_TOUCHED_FILE = "last-touched"`) | create_min, update_status, OQ-008 |
| S4.208 | **`last-touched` records the LAST SUCCESSFULLY ACTED-ON id, not the last id on the command line.** Ids that were skipped do not become the last-touched id; when every id was skipped, the file is not written at all. | `update proj-mta proj-5u2 -p 4` → `proj-5u2\n` · `undefer proj-7vm proj-mta` (mta skipped) → `proj-7vm\n` · `reopen proj-mta proj-mkh --reason Redo` (mta skipped) → `proj-mkh\n` · `reopen proj-mta` alone (skipped) → file not written | `src/cli/commands/close.rs:746`, `src/cli/commands/reopen.rs:139`, `src/cli/commands/update.rs:525` | update_two_ids, close_two, reopen_open, (case to add: `undefer_mixed` — `["@fx=basic","undefer","proj-7vm","proj-mta"]`) |
| S4.209 | **`last-touched` is written even when the mutation changed nothing**, for the commands that reach their per-id body: `update` (including the no-change and the no-flag forms), `dep add` of a duplicate edge, `dep remove` of an absent edge, `label add` of an existing label, `label remove` of an absent label. It is NOT written when the command aborts before that point (any refusal) and not by `reopen`/`undefer`/`defer`/`close` skips. | `dep add proj-170 proj-mta` (edge exists) → no store dump, last-touched `proj-170\n` | `src/cli/commands/dep.rs:594`, `src/cli/commands/dep.rs:778`, `src/cli/commands/update.rs:525` | update_noop_same_value, dep_add_duplicate, dep_remove_absent, label_add_existing, label_remove_absent |
| S4.210 | **`delete` never writes `last-touched`; it only removes the file**, and only when the stored id is one of the ids just deleted. | `delete proj-5u2` on fixture `basic` (no `last-touched` present) → no last-touched dump at all | `src/cli/commands/delete.rs:329`–`336` (`clear_last_touched` when the stored id is in the deleted set), `src/util/mod.rs:145` | delete_basic, delete_json, delete_cascade, delete_with_dependents_force |
| S4.211 | **`label rename` does not write `last-touched`** although it mutates several issues. | `label rename backend server --json` → store dump, no last-touched dump | Golden: `goldens/label_rename.out:2` is the only dump marker (`--- .beads/issues.jsonl ---`); the ten-line file holds no `--- .beads/last-touched ---` marker. Source: `label_rename` spans `src/cli/commands/label.rs:597`–`src/cli/commands/label.rs:693` (mutation at `src/cli/commands/label.rs:639`, flush at `src/cli/commands/label.rs:667`) and holds no `set_last_touched_id` call; the file's only two calls are `src/cli/commands/label.rs:253` (`label add`) and `src/cli/commands/label.rs:334` (`label remove`) | label_rename, r15_label_rename_to_invalid, r15_label_rename_upper, r15_label_rename_quiet |
| S4.212 | **Id resolution is all-or-nothing and happens before any mutation.** When any positional id of a multi-id `update`, `close`, `delete` (or the single id of the other commands) cannot be resolved, the whole command fails with `Error: Issue not found: <input>` on stderr, hint `Hint: Run 'br list' to see available issues.`, exit **3**, and the store is not rewritten — even when the other ids were resolvable. An id that matches several issues fails the same way with `Error: Ambiguous ID '<input>': matches ["<id>", …]` and hint `Hint: Provide more characters of the ID`, exit **3**. | `update proj-mta proj-zzz -p 0` → nothing written, exit 3 · `update m -p 1` → ambiguous, exit 3 | `src/util/id.rs:1002`, `src/cli/commands/delete.rs:123`–`128`, `src/cli/commands/close.rs:874` | error_update_missing, error_close_missing, error_delete_missing, error_reopen_missing, error_comments_add_missing, error_dep_add_missing, error_update_ambiguous, (case to add: `error_update_two_ids_one_missing` — `["@fx=basic","update","proj-mta","proj-zzz","-p","0"]`) |
| S4.213 | **Precondition *skips* are different from id-resolution failures: they are per id and the batch continues.** A skipped id produces a warning and the remaining ids are still mutated and written back. This applies to `close`, `reopen`, `undefer` and `defer`. | `close proj-mkh proj-mta` → `proj-mta` closed and written, `proj-mkh` skipped, exit 3 | `src/cli/commands/close.rs:915`–`1031`, `src/cli/commands/reopen.rs:247`–`269` | close_already_closed, error_close_blocked, close_epic_with_open_child, (case to add: `close_partial_skip` — `["@fx=basic","close","proj-mkh","proj-mta"]`) |
| S4.214 | **Events are not observable in `--no-db` mode.** Every mutation records audit events (`Updated`, `StatusChanged`, `Closed`, `Reopened`, `Deleted`, `DependencyAdded`, `DependencyRemoved`, `Commented`) and, for `close` under a policy file, `close_metadata` rows. None of these are part of the JSONL record, so no golden shows them and the port is not required to produce them. The only mutation-recorded artifact that DOES reach the JSONL is a comment (S4.315, S4.380). Likewise `content_hash` is recomputed on every change but is never exported. | `update proj-mta --status in_progress` → the store line gains no event or hash field | `src/storage/sqlite.rs:1820` (`record_field_change`), `src/storage/sqlite.rs:7822` (`content_hash` recomputed), fixture lines carry no `content_hash` | update_status, close_basic |
| S4.215 | **Exit-code families used by the mutation surface.** `0` success (including a preview and a no-op); `2` argument-parser refusal; `3` issue/operational (`ISSUE_NOT_FOUND`, `AMBIGUOUS_ID`, `NOTHING_TO_DO`, `CLOSE_INCOMPLETE`); `4` validation (`VALIDATION_FAILED`, `INVALID_PRIORITY`, `INVALID_STATUS`, `INVALID_TYPE`); `5` dependency (`CYCLE_DETECTED`, `SELF_DEPENDENCY`, `HAS_DEPENDENTS`, `DUPLICATE_DEPENDENCY`, `DEPENDENCY_NOT_FOUND`); `6` sync/JSONL; `7` config; `8` I/O. | `create X -p 9` → 4 · `dep add proj-mta proj-170` → 5 · `close proj-170` → 3 | `src/error/structured.rs:233`–`276` | error_create_bad_priority, error_dep_add_cycle, error_close_blocked, usage_update_alone |
| S4.216 | **Plain-mode messages go to the streams the goldens show:** successes and previews on stdout (with the glyphs `✓` U+2713, `⊘` U+2298, `⏱` U+23F1 and a following space); `Warning: …` and `Error: …`/`Hint: …` on stderr. In JSON mode the machine payload is printed to stdout *before* an error is raised, so a partially-failing command prints both a payload line and the pretty `{"error":{…}}` block. | `close proj-170 --json` → `{"closed":[],"skipped":[…]}` then the pretty error block, exit 3 | `src/cli/commands/close.rs:750`–`798` | error_close_blocked_json, close_already_closed |
| S4.217 | **No mutating command creates `.beads/` or any file other than `issues.jsonl` and `last-touched` within this surface.** When `.beads/` does not exist, the command fails at workspace discovery before touching anything. | `@fx=none create X` → workspace error, nothing created | Golden: `goldens/error_no_workspace_create.err:1`–`goldens/error_no_workspace_create.err:2` (`Error: Beads not initialized: run 'br init' first` / `Hint: Run: br init`), `goldens/error_no_workspace_create.exit:1` is `2`, `goldens/error_no_workspace_create.out` is zero bytes — no dump marker, so neither `issues.jsonl` nor `last-touched` came into existence (`scripts/ws_inner.py:133`, `dump_changes`, reports a file that appears). Source: `src/config/mod.rs:502` (`discover_beads_dir_with_cli`), `src/config/mod.rs:365` (the walk ends in `NotInitialized`); its call sites on this path are `src/main.rs:1010` (startup) and `src/cli/commands/create.rs:128` (`create`, ahead of opening the store) | error_no_workspace_create, OQ-005, OQ-008 |
| S4.218 | **Tombstoned issues are immutable to `update`.** An `update` naming an issue whose status is `tombstone` fails with `Error: Validation failed: issue_id: cannot update tombstone issue: <id>`, exit **4**, store unchanged. | — Amended (2026-09-25, round 21): the refusal's field is `issue`: `Validation failed: issue: cannot update tombstone issue: <id>`. | `src/storage/sqlite.rs:7431`–`7436` | (case to add: `error_update_tombstone` — a two-step scenario: `delete` then `update` the same id), r21_up_tomb_multi, r21_up_tomb_status_open, error_update_tombstone_plain |
| S4.219 | **Only `blocks`, `conditional-blocks`, `waits-for` and parent→child propagation make an issue "blocked" for the purposes of the mutation guards** (`update --claim`, `close` without `--force`). `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes` and `caused-by` never block. An epic additionally rolls up its own still-open children, which blocks `close` but NOT `--claim` (the claim guard uses start-blockers, which omit the child rollup). | `update proj-170 --claim` → refused, blocker `proj-mta` (a `blocks` edge) · `update proj-uly --claim` after its child is open → permitted | `src/cli/commands/update.rs:1268`–`1275` (`get_start_blockers`), `src/storage/sqlite.rs:11336`–`11346` | error_update_blocked_claim, error_close_blocked, close_epic_with_open_child, scn_lifecycle |

### S4.220–S4.249 `create`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.220 | **Defaults of a bare `create <title>`.** The new record carries `status` `open`, `priority` `2`, `issue_type` `task`, `created_at` = `updated_at` = the invocation instant, `created_by` = the resolved actor (S4.200), `source_repo` = the workspace directory's basename, `source_repo_path` = the absolute workspace directory, `compaction_level` `0`, `original_size` `0`. No other field is set. | `create "First issue"` at `2026-01-02T03:04:05Z` in `/mnt/proj` → `{"id":"proj-fyw","title":"First issue","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-02T03:04:05Z","created_by":"tester","updated_at":"2026-01-02T03:04:05Z","source_repo":"proj","source_repo_path":"/mnt/proj","compaction_level":0,"original_size":0}` | `src/cli/commands/create.rs:484`–`535`, `src/cli/commands/create.rs:42` (`canonical_source_repo`), `src/cli/commands/create.rs:143`–`144` | create_min, create_min_json, r17_cfg_bd_parent_chain, r17_cfg_quoted_json, r17_cfg_bd_silent, r17_par_child_next, r17_par_child_plain, r17_par_grand_next, r17_par_child_of_loose, r17_par_child_short, r17_tree_child_12, r17_tree_grand_10, r17_create_labels_dup, r17_create_labels_empty_item, r17_create_labels_space, r17_create_labels_colon, r17_create_labels_unicode, r17_create_labels_only_comma, r17_create_estimate_zero, r17_create_extref_plain, r17_create_extref_dup, r17_create_all_silent, r17_create_design_ac, r17_create_type_word, r17_create_owner_json, r18_create_deps_typed_prio, r18_create_deps_badtype, r18_create_deps_parent_mix, r19_create_p_lower, r19_create_p_space, r19_create_p_empty, r19_create_p_P5, r19_create_p_neg, r19_create_type_upper, r19_create_type_empty, r19_create_type_spaces, r19_create_assignee_empty, r19_create_owner_empty, r19_create_est_min, r19_create_est_plus, r19_create_est_empty, r19_create_due_feb30, r19_create_due_tomorrow, r19_create_due_nextweek, r19_create_due_past, r19_create_defer_past_plain, r19_create_due_defer_both, r19_create_status_closed, r19_create_status_closed_plain, r19_create_status_deferred_json, r19_create_status_Open, r19_create_acceptance_json, r19_create_prereq_json, r19_create_design_only, r19_create_notes_only, r19_create_desc_crlf_tab, r19_create_desc_zwj, r19_create_desc_esc, r19_create_title_newline, r19_create_title_padded, r19_create_title_zwsp, r19_create_title_emoji_500, r19_create_title_emoji_501, r19_create_parent_tree_json, r19_create_parent_tree_leaf, r19_create_parent_closed_plain, r19_create_parent_deferred, r19_create_parent_dryrun, r19_create_deps_dryrun, r19_create_silent_json, r19_create_dryrun_full, r19_create_large_parent, r19_none_create_json, r19_malformed_create_json, r19_conflict_create, r19_usage_create_p_noval, r19_usage_create_type_noval_json, r21_ws_cwd_deep_create, r21_ws_nostore_create_json, r21_ws_redirect_parent_create, r21_ws_redirect_missing_create_json, r21_jr_create_title_long, r21_jr_create_prio9, r21_jr_create_label_bad, r21_jr_create_parent_missing, r21_jr_create_dep_missing, r21_u_create_tab_title, r21_u_create_newline_title, r21_u_create_cr_title, r21_u_create_emoji, r21_u_create_zwj_rtl, r21_u_create_500_multibyte, r21_u_create_501_multibyte, r21_u_create_del_char, r21_u_create_desc_ctrl_plain, r22_g_create_l_eq, r22_g_dashdash_create, r22_e_fmt_json_create, r22_e_fmt_json_validation, r22_e_beads_actor_create, r22_e_agent_env_create |
| S4.221 | `source_repo` is the last path component of the directory that holds `.beads/`; `source_repo_path` is that directory's absolute path. Both are computed once per invocation from the discovered beads directory, never from the record. | workspace `/mnt/proj` → `"source_repo":"proj"`, `"source_repo_path":"/mnt/proj"` | `src/cli/commands/create.rs:42`, `src/sync/mod.rs` (`canonical_source_repo_path`) | create_min, where_plain |
| S4.222 | **Title.** The positional `<title>` and `--title <t>` are mutually exclusive at the parser; the positional wins when both reach the logic. The title is stored verbatim (leading/trailing spaces are NOT trimmed off the stored value), but validation rejects a title whose trimmed form is empty and a title longer than 500 **characters** (Unicode scalar count, not bytes). | `create --title "Via flag"` → `"title":"Via flag"` · `create ""` and `create "   "` → refused · 500 `t` → accepted, 501 `t` → refused | `src/cli/commands/create.rs:406`–`414`, `src/validation/mod.rs:161`–`167` (`TITLE_MAX_CHARS` = 500) | create_title_flag, error_create_empty_title, error_create_blank_title, error_create_long_title, edge_create_max_title |
| S4.223 | **`-t/--type` accepts any string**, including types outside the built-in set; the value is stored verbatim in `issue_type`. There is no refusal for an unknown issue type at create time. | `create "Custom typed" -t spike` → `"issue_type":"spike"` | `src/cli/commands/create.rs:424`–`428`, `src/validation/mod.rs:272` (`validate_custom_issue_type`) | create_custom_type, create_full_json |
| S4.224 | **`-p/--priority` accepts the bare digit and the `P`-prefixed form**, case-insensitively, and stores the integer. Out-of-range values are refused (S4.239). | `-p 0` → `"priority":0` · `-p P3` → `"priority":3` | `src/cli/commands/create.rs:417`–`421` | create_full_json, create_priority_p_form |
| S4.225 | **`-d/--description` stores the value verbatim** including newlines, tabs and control characters; JSON encoding escapes them. `--description-file <PATH>` reads the file's entire content verbatim instead (`-` means stdin) and is mutually exclusive with `-d`; the guard message is `Validation failed: description_file: cannot be combined with --description`. The file is read exactly once, before any mutation. | `-d "quote \" slash \\ bell ␇ del ␡ nbsp  "` → `"description":"quote \" slash \\ bell \u0007 del \u007f nbsp  "` as the golden's bytes show | `src/cli/commands/create.rs:340` (`read_description_file`), `src/cli/commands/create.rs:366` (`resolve_create_description`) | create_unicode, (case to add: `create_description_file` — needs a fixture-side file) |
| S4.226 | `-a/--assignee` sets `assignee`; `--owner` sets `owner`. Both are stored verbatim. | `-a carol --owner dave` → `"assignee":"carol","owner":"dave"` | `src/cli/commands/create.rs:500`–`501` | create_full_json |
| S4.227 | **`-l/--labels` is comma-split and repeatable.** Each value is trimmed; empty pieces are dropped; duplicates are dropped keeping the first occurrence. Every surviving label is validated (S4.239) before anything is written. The stored array is then sorted and deduplicated by the export. | `-l one,two` → `"labels":["one","two"]` | `src/cli/commands/create.rs:689`–`696` (validation), `src/cli/commands/create.rs:773`–`779` (population), `src/cli/mod.rs:1089` (`value_delimiter = ','`) | create_full_json, error_create_bad_label |
| S4.228 | **`--parent <id>` does two things: it changes the id scheme, and it adds one `parent-child` edge on the CHILD.** The new id is `<resolved parent id>.<n>` where `n` is the next free child number (1 for the first child); nesting repeats the rule. The edge is written into the child's own `dependencies` array with `issue_id` = the child, `depends_on_id` = the parent, `type` `parent-child`. **The parent's record is not modified at all** — its `updated_at` is unchanged. | `create "Child task" --parent proj-uly` → id `proj-uly.1`, one edge `proj-uly.1 → proj-uly (parent-child)`, `proj-uly` byte-identical · second child → `proj-uly.2` · `--parent proj-uly.1` → `proj-uly.1.1` | `src/cli/commands/create.rs:600`–`625` (`generate_new_id`), `src/cli/commands/create.rs:781`–`791` (`populate_relations`) | create_with_parent, scn_child_ids |
| S4.229 | **`--deps` is comma-split and repeatable; each element is `<id>` or `<type>:<id>`.** A bare id means type `blocks`. `blocked-by:` is accepted and stored as `blocks`. The type must be one of the eleven standard names, otherwise the create is refused with `Validation failed: deps: Unknown dependency type: '<t>'. Allowed types: blocks, blocked-by, parent-child, conditional-blocks, waits-for, related, discovered-from, replies-to, relates-to, duplicates, supersedes, caused-by` (note this list includes `blocked-by`, unlike `dep add`'s list). Each target id is resolved; an unresolvable one fails the whole create. Several edges are written in argv order into the child's array, then sorted by the export. | `--deps proj-mta` → one edge `proj-tne → proj-mta (blocks)` · `--deps "parent-child:proj-uly,related:proj-mta"` → two edges, exported ordered `proj-mta (related)` then `proj-uly (parent-child)` Amended (2026-09-25, round 17): elements are trimmed and empty ones are kept: an empty element or an empty target (`--deps ""`, `proj-mta,`, `blocks:`) is `Invalid issue ID format: ` (INVALID_ID, exit 3). The part before the first `:` is a type only when it is a known type (any case, trimmed); otherwise the whole element is resolved as an id (`frob:proj-mta` is `Issue not found: frob:proj-mta`, exit 3). | `src/cli/commands/create.rs:718`–`755` (`parse_create_dependency`), `src/cli/commands/create.rs:793`–`806` | create_with_deps, (case to add: `create_deps_typed` — `["@fx=basic","create","Multi","--deps","parent-child:proj-uly,related:proj-mta","--json"]`), r17_cfg_bd_deps_missing, r17_par_child_deps, r17_create_deps_typed, r17_create_deps_pc, r17_create_deps_discovered, r17_create_deps_badtype, r17_create_deps_missing, r17_create_deps_dup, r17_create_deps_space, r17_create_deps_empty, r17_create_deps_short, r17_deps_blocks_empty_id, r17_deps_colon_first, r17_deps_upper_type, r17_deps_badtype_json, r17_deps_trailing_comma, r17_deps_waits_for, r17_q_deps_empty, create_deps_type_case, create_deps_type_spaced, create_deps_blocked_by, error_create_deps_type_missing_target, error_create_deps_unknown_prefix_empty, error_create_deps_type_empty_target, error_create_deps_leading_comma, error_create_deps_extra_colon |
| S4.230 | **`-e/--estimate` sets `estimated_minutes` (an integer)**, `--due` sets `due_at`, `--defer` sets `defer_until`, `--external-ref` sets `external_ref`. `--due`/`--defer` take the same date grammar as `defer --until` (S4.322): a bare `YYYY-MM-DD` resolves to **09:00:00** of that day in the process time zone. `--defer` does NOT change `status`; the created issue stays `open` with a `defer_until` set. | `-e 90 --due 2026-03-01 --external-ref gh-42` → `"estimated_minutes":90`, `"due_at":"2026-03-01T09:00:00Z"`, `"external_ref":"gh-42"` · `--defer 2027-01-01` → `"status":"open"`, `"defer_until":"2027-01-01T09:00:00Z"` Amended (2026-09-25, round 19): `--external-ref ""` is written as `"external_ref":""`. | `src/cli/commands/create.rs:429`–`430`, `src/cli/commands/create.rs:498`–`507` | create_full_json, (case to add: `create_defer` — `["create","Defer me","--defer","2027-01-01","--json"]`), r15_update_three_status_json, r15_update_valid_missing, r15_update_valid_missing_json, r15_update_two_close_json, r15_update_two_claim, r15_update_two_labels_json, r15_update_reopen_status, r15_update_type_prio_word, r15_update_due_rel, r15_update_defer_rel, r15_update_nothing, r15_update_nothing_json, r15_update_estimate, r15_create_prio_word, r15_create_prio_word_bad, r15_create_labels_repeat, r15_create_labels_bad, r15_create_due_rel, r15_create_defer_rel, r15_create_due_date, r15_create_due_bad, r15_create_parent_silent, r15_create_parent_quiet, r15_create_parent_missing, r15_create_parent_closed, r15_create_parent_grand, r15_create_type_epic_assignee, r15_create_type_bad, r15_create_estimate, r15_create_status_deferred, r19_create_extref_empty, error_update_extref_space, create_extref_empty_plain |
| S4.231 | **`-s/--status` sets the initial status.** `in_progress` is stored plainly. `closed` additionally sets `closed_at` to the create instant but leaves `close_reason` unset. `tombstone` additionally sets `deleted_at` to the create instant and `deleted_by` to the actor, but leaves `delete_reason` and `original_type` unset. All are written to the store (there is no `br update`-style terminal-state refusal on `create`). | `-s in_progress` → `"status":"in_progress"` · `-s closed` → `"status":"closed","closed_at":"2026-01-02T03:04:05Z"`, no `close_reason` · `-s tombstone` → `"status":"tombstone","deleted_at":"2026-01-02T03:04:05Z","deleted_by":"tester"`, no `delete_reason`/`original_type` Amended (2026-09-25, round 19): `--status tombstone` sets `deleted_at` and `deleted_by` and no `closed_at`. | `src/cli/commands/create.rs:446`–`486`, `src/cli/commands/create.rs:518`–`524` | create_status_in_progress, (case to add: `create_status_closed` — `["create","Closed start","-s","closed","--json"]`; `create_status_tombstone` — `["create","Tombstone start","-s","tombstone","--json"]`), r19_create_status_tombstone, create_status_closed_json |
| S4.232 | **`--acceptance-criteria` (alias `--acceptance`) and `--prerequisites` are stored verbatim** in `acceptance_criteria` / `prerequisites`. `--agent-context` takes inline JSON, `@path.json`, or `@path.yaml`/`@path.yml` (converted to JSON); an empty string leaves the field unset. It is parsed and normalized to canonical JSON **before** any record is created, so an invalid context leaves no record behind. Measured (2026-09-23, OQ-058): the inline value is stored as serde_json prints it back: compact, keys in INPUT order, `1.50` as `1.5`, `-0` as `-0.0`, `1e3` as `1000.0`, `2.0` kept, a u64 integer kept, `\u00e9` decoded to `é`; `42` is stored as `"42"`; `""` stores nothing. Invalid inline JSON is `CONFIG_ERROR`, exit 7: `Configuration error: agent-context: inline argument is not valid JSON: key must be a string at line 1 column 2 (hint: use `--agent-context @path/to/instructions.yaml` for a file, or pass an empty string to clear)`, JSON `hint` null, `retryable` false, `context` null. The port prints floats only up to 15 significant digits with the decimal point between -3 and 15 places, and answers its own failure beyond that and for `@path` (a file read). | `--acceptance-criteria "It works."` → `"acceptance_criteria":"It works."` | `src/cli/commands/create.rs:504`–`505`, `src/cli/commands/create.rs:434`–`437`, `src/cli/commands/update.rs:1866` (`agent_context_update_from_arg`) | create_full_json, (case to add: `create_prerequisites`, `create_agent_context_bad` — `["create","X","--agent-context","{not json"]`), create_agent_context, create_agent_context_scalar, create_agent_context_empty, error_create_agent_context, error_create_agent_context_json |
| S4.233 | **`--slug <text>` changes the id shape only:** the id becomes `<prefix>-<normalized slug>-<hash>`. Normalization lowercases to ASCII alphanumerics, collapses every run of other characters to a single hyphen, strips leading and trailing hyphens, and caps the result at 48 characters. A `--parent` id ignores the slug entirely. Measured (2026-09-23, OQ-031, OQ-059): the 48-character cap gives `proj-abcdefghij-abcdefghij-abcdefghij-abcdefghij-abcd-u5m`; a slug that normalizes to empty gives the plain id (`create_slug_long`, `create_slug_empty`). | `--slug "Survey My Thing!!"` → id `proj-survey-my-thing-p00` | `src/cli/mod.rs:1048`–`1056`, `src/cli/commands/create.rs:598` (`generate_new_id`) | (case to add: `create_slug` — `["create","Sluggy","--slug","Survey My Thing!!","--json"]`), create_slug_long |
| S4.234 | **`--ephemeral` marks the issue and keeps it OUT of the store.** The command reports the record on stdout with `"ephemeral":true` appended after `original_size`, exit 0 — but `.beads/issues.jsonl` is byte-unchanged. `.beads/last-touched` IS still written with the new id. | `create Scratch --ephemeral --json` → stdout record with `"ephemeral":true`, no `--- .beads/issues.jsonl ---` dump, `--- .beads/last-touched ---` `proj-kr0\n` | `src/cli/commands/create.rs:502` (`ephemeral: args.ephemeral`), export exclusion of ephemeral records (`src/sync/mod.rs`, handover to S2/S5) | create_ephemeral |
| S4.235 | **`--dry-run` writes nothing at all** — no store, no `last-touched` — and still allocates and prints the id the create would have used. Plain output is four to eight lines in a fixed order: `Dry run: would create issue <id>`, `Title: <title>`, `Type: <type>`, `Priority: P<n>`, then `Labels: <a, b>` when `-l` was given, `Parent: <id>` when `--parent` was given, `Dependencies: <raw --deps values>` when `--deps` was given. In JSON mode the in-memory record is printed instead, which **omits `original_size`** (it carries `compaction_level` but no `original_size`, unlike a real create, because the dry-run path serializes the model rather than the export view). | `create "Not written" --dry-run` → `Dry run: would create issue proj-8h9\nTitle: Not written\nType: task\nPriority: P2\n` · `--dry-run --json` → `{…,"compaction_level":0}` with no `original_size` Amended (2026-09-25, round 19): `--dry-run --json` prints the edges as built in memory, without `metadata` and `thread_id`; under `--silent` the dry run prints the bare id alone, over `--json` and `--quiet`. | `src/cli/commands/create.rs:161`–`208`, `src/cli/commands/create.rs:558`–`561` | create_dry_run, create_dry_run_json, (case to add: `create_dry_run_full` — `["@fx=basic","create","Dry labels","--dry-run","-l","a,b","--parent","proj-uly","--deps","proj-mta"]`), r19_create_parent_dryrun_json, r19_create_dryrun_silent, create_dry_run_deps_json, create_dry_run_silent_json, create_dry_run_silent_quiet |
| S4.236 | **`--silent` replaces all output with the bare id and a newline**, and still performs the write. It takes precedence over `--json`. | `create "Silent one" --silent` → stdout `proj-1yw\n`, store written | `src/cli/commands/create.rs:170`–`172` | create_silent |
| S4.237 | **`-f/--file <PATH>` switches `create` into a bulk markdown import** and is refused together with a title (`Validation failed: file: cannot be combined with title arguments`) or with `--external-ref` (`Validation failed: external_ref: --external-ref is not supported with --file`). The importer reads `## Title` headings and the first paragraph after each as the description, creates one issue per heading with the same defaults and the same `source_repo`/`source_repo_path`, resolves deferred parent and dependency references after all issues exist, and writes back once at the end; `last-touched` receives the LAST created id. An empty parse result prints an empty JSON array in JSON mode and nothing otherwise, and writes nothing. Measured (2026-09-23): the title conflict is judged first (`Validation failed: file: cannot be combined with title arguments`), then a name without the `.md` or `.markdown` extension (`Validation failed: file: must have .md or .markdown extension`, also for `-` and `/dev/null`), then a missing file (`Validation failed: file: file not found: nope.md`), all exit 4 with `context` `{"field","reason"}` in JSON. No case reaches the import itself: the sandbox stages no `.md` file (OQ-051). | — Corrected and measured (2026-09-24, OQ-051, harness mode `@file`): the import reads lines; `# ` lines and text before the first `## ` are ignored; each `## <title>` (trimmed) is one issue, in heading order; before any section, the FIRST NON-BLANK LINE after the heading is the description (not the paragraph); a `### <name>` section (name trimmed, case-insensitive) runs to the next heading and its value is its lines joined and trimmed as one text, empty meaning absent; `description` replaces the first line; `design` and `acceptance criteria` keep their lines; `assignee`; `type` lowercased, any word kept; `priority` trimmed, upper-cased, one leading `P` dropped, a digit 0–4 or the heading is refused with `✗ Failed to create <title>: Priority must be 0-4, got: <that text>` on stderr while the others are created; `labels` takes comma lists and `-`/`*` bullet lines; `notes`, `estimate`, `status`, `external ref` and unknown names are ignored; `### Parent` names a heading created EARLIER in the file (its exact title) or a stored or created id (full or short): the issue then gets the dotted child id and a `parent-child` edge; the title of a LATER heading creates the issue with a fresh id and adds the edge after every heading exists; anything else refuses that heading with `✗ Failed to resolve parent for <title>: Issue not found: <text>` (exit unchanged). `### Dependencies` and `### Deps` are split on commas and white space (a bullet dropped per line), each word `<type>:<ref>` when the type is known, else a `blocks` reference; a reference is a heading's exact title anywhere in the file, else an id; an unresolved one warns `warning: unresolved dependency '<word>' for issue <id>: Issue not found: <word>` and the run exits 5. The edges are added after all headings, in heading order, each checked for a cycle: the first refused one is announced as `warning: bulk dependency import failed (Cycle detected in dependencies: <path from its least id>); retrying dependencies one by one`, and each refused edge as `warning: failed to add dependency <from> → <to>: Cycle detected in dependencies: Adding dependency <from> -> <to> would create a cycle`; exit 5. Every edge carries the actor as `created_by`; the records carry none. The id seed is the title, the final description and an EMPTY creator, and the records carry no `created_by`. Plain output: `✓ Created <n> issues from <path>:` (`1 issues` too) and `  <id>: <title>` per issue; `--json`: one compact array of the created records. Exit 0. | `src/cli/commands/create.rs:110`–`122`, `src/cli/commands/create.rs:820`–`1430` | (case to add: `create_from_file` — needs a fixture-side markdown file; `error_create_file_with_title` — `["create","X","-f","plan.md"]`), stdin_create_file, stdin_create_file_json, error_create_file_with_title, error_create_file_missing, create_file_empty, create_file_md, create_file_md_json, create_file_md_edges, create_file_md_rules, create_file_md_one, create_file_md_parent, create_file_md_parent_json, create_file_md_parent_store, create_file_md_deps, create_file_md_deps_json, create_file_md_cycle, create_file_md_cycle3, create_file_md_mixed |
| S4.238 | **Validation order for `create`.** In order: `--file` conflicts; workspace discovery; strict-workflow status check; description resolution (`--description-file` read); priority parse; type parse; `--due`/`--defer` parse; `--agent-context` parse; `--parent` resolution (an unresolvable parent fails here); `--status` parse; id generation; issue-level validation (title empty, title length, priority range, `created_at ≤ updated_at`, estimate range, NUL bytes, `external_ref` ≤ 200 chars and no whitespace, label charset and count); relation validation (each `-l` label's charset, `--parent` ≠ self, each `--deps` type and self-reference); relation population; `--dry-run` early return; the store write; the write-back; `last-touched`. Nothing is written until every check above has passed. | `create Orphan --parent proj-zzz` → parent failure before any record exists | `src/cli/commands/create.rs:110`–`166`, `src/cli/commands/create.rs:405`–`560`, `src/validation/mod.rs:77`–`160` | error_create_missing_parent, error_create_bad_priority, error_create_bad_label |
| S4.239 | **`create` refusals, verbatim.** Priority out of range: stderr `Error: Priority must be 0-4, got: 9` + `Hint: Priority must be 0-4 or P0-P4 (0=critical, 4=backlog); list filters also accept ranges and comma lists such as 0-1 or 0,2.`, exit **4**; in JSON mode the pretty block with `"code": "INVALID_PRIORITY"`, the same message and hint, `"retryable": true`, `"context": {"priority": "9", "hint": …}`. Empty or all-blank title: stderr `Error: Validation failed: title: cannot be empty`, exit **4**, no hint line. Over-length title: `Error: Validation failed: title: exceeds 500 characters`, exit **4**. Bad label: `Error: Validation failed: label: invalid characters (only alphanumeric, hyphen, underscore, colon allowed)`, exit **4**. Unknown parent: `Error: Issue not found: proj-zzz` + `Hint: Run 'br list' to see available issues.`, exit **3**. In every case the store is untouched and `last-touched` is not written. | see the cases | `src/validation/mod.rs:164`, `:167`, `:441`; `src/error/structured.rs` | error_create_bad_priority, error_create_bad_priority_json, error_create_empty_title, error_create_blank_title, error_create_long_title, error_create_bad_label, error_create_missing_parent |
| S4.240 | **Plain success line of `create`:** `✓ Created <id>: <title>` and one `\n` (U+2713 then one space). JSON mode prints the exported record as one compact line. | `create "First issue"` → `✓ Created proj-fyw: First issue\n` | `src/cli/commands/create.rs:222`–`226` | create_min, create_title_flag, scn_dup_title |

### S4.250–S4.259 `q` (quick capture)

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.250 | **`q` takes its title from ALL positional words joined with a single space, then trimmed**, instead of one positional. An empty result is refused with `Error: Validation failed: title: cannot be empty`, exit **4**. | `q Quick one` → `"title":"Quick one"` | `src/cli/commands/q.rs:46`–`50` | q_basic, r15_q_type_labels, r15_q_prio_word, r15_q_prio_word_lower, r15_q_json, r15_q_parent, r15_q_multiword, r17_cfg_bd_q, r17_cfg_nested_q, r17_par_child_q, r17_q_silent, r17_q_json_labels, r17_q_estimate, r19_q_type_epic_json, r19_q_labels_comma, r19_q_est_neg, r19_q_p5, r19_q_parent_missing, r19_q_dashdash, r19_q_dryrun, r19_q_body_alias, r19_q_parent_json, r19_none_q, r19_usage_q_unknown_flag, r21_u_q_unicode, r22_g_dashdash_q |
| S4.251 | **`q` writes NEITHER `source_repo` NOR `source_repo_path` into the model.** The export then supplies the default `"source_repo":"."` (the same default every record without one receives, OQ-002) and leaves `source_repo_path` absent. This is the one field-level difference between `q` and `create` in the resulting store line. | `q "Quick one"` → `{"id":"proj-asw","title":"Quick one","status":"open","priority":2,"issue_type":"task","created_at":"2026-01-02T03:04:05Z","created_by":"tester","updated_at":"2026-01-02T03:04:05Z","source_repo":".","compaction_level":0,"original_size":0}` | `src/cli/commands/q.rs:161`–`162` (both fields set to `None`), OQ-002 | q_basic, q_priority |
| S4.252 | **`q` supports only `-p`, `-t`, `-l`, `-d`, `--parent`, `-e`** — no `--status`, `--due`, `--defer`, `--external-ref`, `--owner`, `-a`, `--dry-run`, `--silent`, `--slug`, `--ephemeral`, `--acceptance-criteria`, `--prerequisites`, `--agent-context`, `-f`. Defaults are identical to `create`'s (S4.220). `--parent` has the same child-id and parent-child-edge effect as S4.228, including the pre-check that refuses a parent-child cycle with `Cycle detected in dependencies: <child> -> <parent>`. | `q "Quick urgent" -p 0` → `"priority":0` | `src/cli/mod.rs:1174`–`1200`, `src/cli/commands/q.rs:171`–`205` | q_priority |
| S4.253 | **`q` handles an invalid label by WARNING and skipping it, not by refusing.** The warning is `Warning: invalid label '<label>': <reason>` on stderr with terminal control characters escaped; the issue is still created with the remaining labels. This is the opposite of `create`, which refuses (S4.239). | — | `src/cli/commands/q.rs:32`–`37`, `src/cli/commands/q.rs:122`–`129` | (case to add: `q_bad_label` — `["q","Quick","-l","bad label!"]`) |
| S4.254 | **`q`'s output is the bare id and a newline in plain mode**, and `{"id":…,"title":…}` — only those two fields — in JSON mode. Quiet mode prints nothing. The write-back and the `last-touched` write are identical to `create`'s. | `q "Quick one"` → stdout `proj-asw\n` | `src/cli/commands/q.rs:225`–`243` | q_basic, q_priority |

### S4.260–S4.289 `update`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.260 | **With no positional ids, `update` acts on the id in `.beads/last-touched`.** When that file is missing, unreadable, empty or larger than 4096 bytes, the command fails with `Error: Validation failed: ids: no issue IDs provided and no last-touched issue`, exit **4**. | fixture `basic_touched` (`last-touched` = `proj-mta\n`), `update --status in_progress` → `proj-mta` updated; `last-touched` is rewritten with the same bytes so the golden shows no last-touched dump | `src/cli/commands/update.rs:380`–`389`, `src/util/mod.rs:112` | update_last_touched, scn_last_touched, usage_update_alone, r17_upd_title_json, r17_upd_desc, r17_upd_desc_empty, r17_upd_design, r17_upd_design_empty, r17_upd_notes_plain, r17_upd_ac, r17_upd_ac_then_add, r17_upd_add_ac_check, r17_upd_check_out_of_range, r17_upd_add_existing, r17_upd_remove_missing, r17_upd_add_remove_same, r17_upd_remove_plain, r17_upd_reparent_child, r17_upd_clear_parent_dotted, r17_upd_parent_to_grandchild, r17_upd_parent_hier, r17_upd_due_abs, r17_upd_due_clear, r17_upd_defer_clear, r17_upd_defer_past, r17_upd_due_bad, r17_upd_defer_plain, r17_upd_status_deferred, r17_upd_status_blocked_plain, r17_upd_status_pinned, r17_upd_status_bogus, r17_upd_status_ip_closed, r17_upd_status_deferred_to_open, r17_upd_status_upper, r17_upd_ext_ref, r17_upd_ext_ref_clear, r17_upd_owner, r17_upd_assignee_clear, r17_upd_estimate_neg, r17_upd_type_epic, r17_upd_many_fields_plain, r18_update_due_rel, r18_update_due_tomorrow, r18_update_due_rfc, r18_update_due_date, r18_update_due_bad, r18_update_due_empty, r18_update_defer_rel, r18_update_defer_today, r18_update_defer_bad_unit, r18_update_defer_empty, r18_update_bad_prio_json, r18_update_parent_prio, r18_update_parent_status, r18_update_parent_clear_title, r19_update_title_same, r19_update_title_same_json, r19_update_type_upper, r19_update_type_mixed, r19_update_assignee_same, r19_update_owner_empty, r19_update_est_zero, r19_update_est_over, r19_update_extref_empty_plain, r19_update_status_same_ip, r19_update_status_deferred_same, r19_update_status_open_closed_plain, r19_update_status_closed_plain, r19_update_status_Closed, r19_update_status_blocked_json, r19_update_notes_empty, r19_update_notes_empty_force, r19_update_append_existing, r19_update_p_lower, r19_update_mixed_closed, r19_update_mixed_ambig, r19_update_mixed_status_plain, r19_update_due_empty_none, r19_update_defer_closed, r19_none_update, r19_malformed_update, r19_dup_update, r19_usage_update_est_bad_json, r19_usage_update_claim_twice, r20_mu_claim_quiet, r20_mu_update_multi_quiet, r20_mu_update_mixed_json, r21_jr_update_bad_status, r21_jr_update_prio_word, r21_jr_update_title_empty, r21_jr_update_est_neg, r21_jr_update_est_max, r21_jr_update_due_bad, r21_jr_update_defer_bad, r21_jr_update_parent_self, r21_jr_update_parent_missing, r21_jr_update_label_bad, r21_jr_ambiguous_update, r21_up_closed_multi, r21_up_closed_to_open_notes, r21_up_deferred_defer_clear, r21_up_deferred_due_defer, r21_up_open_status_deferred, r21_up_close_via_update_blocked, r21_up_close_via_update_plain, r21_up_closed_closed, r21_up_source_repo, r21_up_source_repo_path, r21_up_ext_owner_est, r21_up_add_rm_same_label, r21_up_title_padded, r21_up_deferred_multi_ids, r21_up_type_custom_closed, r21_up_desc_ctrl, r21_up_defer_leap, r21_u_update_title_combining, r21_u_assignee_unicode, r22_e_fmt_json_update, r22_m_custom_update_title, r22_m_template_update, r22_m_unicode_update_desc, r22_m_empty_opt_assignee, r22_j_invalid_id, r22_j_update_closed_status |
| S4.261 | **Several ids are processed in command-line order**, each producing its own output block, and all changes are written back in one export. `last-touched` becomes the last resolved id. | `update proj-mta proj-5u2 -p 4` → `Updated proj-mta: …\n  priority: P1 → P4\nUpdated proj-5u2: …\n  priority: P3 → P4\n`, both records changed, `last-touched` `proj-5u2\n` | `src/cli/commands/update.rs:520`–`526` | update_two_ids |
| S4.262 | **`--status <s>` is written unconditionally: naming the status an issue already has still bumps `updated_at` and rewrites the store.** Only `closed` and `tombstone` are refused (S4.269). | `update proj-mta --status open` on an open issue → `Updated proj-mta: Set up database schema\n` with no field line, `updated_at` bumped, store rewritten | `src/storage/sqlite.rs:7523`–`7526` (the `add_update("status", …)` is outside the change comparison) | update_status, (case to add: `update_status_same` — `["@fx=basic","update","proj-mta","--status","open"]`) |
| S4.263 | **Any non-terminal `--status` also CLEARS `closed_at` and `close_reason`.** This is how a closed issue is re-opened through `update`. `closed_by_session` is likewise cleared. | `update proj-mkh --status open --json` on the closed fixture issue → `"status":"open"`, `closed_at` and `close_reason` gone, `updated_at` bumped | `src/cli/commands/update.rs:1821`–`1826` (`Some(_) => (Some(None), Some(None), Some(None))`) | (case to add: `update_status_reopen` — `["@fx=basic","update","proj-mkh","--status","open","--json"]`) |
| S4.264 | **`-p/--priority` and `-t/--type` are compared before being written**: when the requested value equals the stored one, no column is touched, `updated_at` is NOT bumped and the store is NOT rewritten. This is the only source of the observable no-op. | `update proj-mta -p 1` on a P1 issue → `[{…,"priority":1,…,"updated_at":"2026-01-01T10:00:00Z"}]`, no store dump, `last-touched` written | `src/storage/sqlite.rs:7635`–`7652` (priority), `:7651`–`7663` (type) | update_noop_same_value |
| S4.265 | **`--title`, `-d/--description`, `--design`, `--acceptance-criteria`, `--prerequisites`, `--notes`, `--assignee`, `--owner`, `--estimate`, `--due`, `--defer`, `--external-ref`, `--source-repo`, `--source-repo-path`, `--agent-context` are written whenever the flag is present**, without comparing. For the `Option<String>` ones an empty string clears the field (the record's key disappears from the store). Measured (2026-09-23): `update --agent-context` stores the canonical text of S4.232 (`update_agent_context`). | `--notes "Some notes."` → `"notes":"Some notes."` added · `--assignee erin` → `"assignee":"erin"` added · `-d "" --force` → `description` key removed | `src/cli/commands/update.rs:1829`–`1846`, `src/cli/commands/update.rs:1867`–`1876` (`optional_string_field`) | update_title, update_assignee, update_notes, update_agent_context |
| S4.266 | **`--force` waives two guards:** the blocked-issue guard on `--claim`/`--status in_progress`, and the accumulated-context guard. The accumulated-context guard refuses to clear, or to shorten below half its current length, a non-empty `description`, `design`, `acceptance_criteria`, `prerequisites`, `notes` or `agent_context`: `Error: Validation failed: update: <id>: refusing to overwrite non-empty '<field>' (<n> chars) with an empty value without --force` + `Hint: These fields accumulate context: a write that clears the field or keeps less than half its length needs --force; run ` + "`br show <id>`" + ` first to read what is there, or use --append-notes / --add-acceptance to extend instead of replace.`, exit **4**, store unchanged. | `update proj-mta -d ""` → refused, exit 4 · `update proj-mta -d "" --force` → `description` removed, `updated_at` bumped Amended (2026-09-25, round 17): the guard's JSON context is `field` `update`, `reason` (the message after `Validation failed: update: `) and `hint`. | `src/cli/mod.rs:1303`–`1310`, `src/cli/commands/update.rs:1262`–`1264` | (case to add: `error_update_clear_description` — `["@fx=basic","update","proj-mta","-d",""]`; `update_clear_description_force` — `["@fx=basic","update","proj-mta","-d","","--force"]`), r17_upd_desc_shrink_plain, r17_upd_notes_shrink_json |
| S4.267 | **`--claim` is exactly "assignee = actor, status = in_progress", refused when the issue is blocked.** Both fields are written in one update. The pre-check refuses with `Error: Validation failed: claim: cannot claim blocked issue: <blocker id>[, <blocker id>…]`, exit **4**, store unchanged; blockers are the start-blockers of S4.219 (the epic child rollup does not count). `--claim` on a closed or tombstoned issue is refused separately (it is not a reopen). `--force` waives the blocked check. When the issue is already assigned to someone else the claim fails with `Validation failed: claim: issue <id> already assigned to <name>`; a re-claim by the same actor is idempotent. | `update proj-mta --claim --json` → `"status":"in_progress"`, `"assignee":"tester"`, plain form adds `  assignee: (none) → tester` · `update proj-170 --claim` → refused, blocker `proj-mta` Amended (2026-09-25, round 15): the order is the blocked check (which `--force` waives), then the closed check, `Validation failed: claim: cannot claim closed issue <id>: `--claim` starts work on an open issue and never reopens one, so its close_reason and closed_at are left intact. Reopen it first with `br reopen <id>`, then claim it`, then the holder check, which `--force` does NOT waive (`update proj-170 --claim --force` is `already assigned to alice`); a claim by the holder succeeds. Amended (2026-09-25, round 16): the issue's status is judged before the blocked check and whatever `--force` says: closed, then DEFERRED (`Validation failed: claim: cannot claim deferred issue <id>: `--claim` does not cancel deferral. Run `br undefer <id>` first, then claim it`, exit 4); then blocked (waived by `--force`), then the holder. Amended (2026-09-25, round 21): the closed and deferred refusals judge the status as loaded: a `--status` given beside `--claim` does not lift them. | `src/cli/commands/update.rs:1736`–`1741`, `src/cli/commands/update.rs:1268`–`1276`, `src/cli/commands/update.rs:1806`–`1810`, `src/storage/sqlite.rs:7444`–`7468` | update_claim, error_update_blocked_claim, scn_lifecycle, r15_update_claim_owned, error_update_claim_closed, error_update_claim_owned_force, update_claim_by_holder, error_update_claim_dup_closed_first, r16_update_twice_json, r16_update_twice_q, r16_claim_closed_json, r16_claim_held_json, r16_claim_held_same_actor, r16_claim_blocked_json, r16_claim_inprog_json, r16_claim_deferred, r16_claim_q, r16_claim_deferred_json, r16_claim_deferred_due, r16_update_deferred_inprog, error_update_claim_deferred_held, error_update_claim_deferred_blocked_force, error_update_claim_closed_blocked, r21_up_deferred_inprog_claim, error_update_claim_deferred_status_plain, error_update_claim_closed_status_open |
| S4.268 | **Label edits.** `--add-label` appends each named label that is not already present; `--remove-label` drops each named label that is present; `--set-labels` replaces the whole array (comma-split, repeatable). The stored array is sorted and deduplicated by the export. Every one of these is a no-op when it would not change the set: `updated_at` is not bumped and the store is not rewritten. | `--add-label urgent` → `["backend","db"]` → `["backend","db","urgent"]` · `--remove-label db` → `["backend"]` · `--set-labels x,y` → `["x","y"]` · `--add-label db` (already present) → no store dump Amended (2026-09-25, round 17): `--set-labels` wins: with it, `--add-label` and `--remove-label` are ignored. Without it, an added label that was absent, or a removed label present after the adds, is a change even when the two cancel (`--add-label z --remove-label z` stamps `updated_at` and rewrites the store). Amended (2026-09-25, round 21): with `--set-labels`, an added label absent before, or a removed label present, still counts as a change (`--set-labels "" --add-label x` on an issue without labels stamps `updated_at`, rewrites the store and stores no label). | `src/cli/mod.rs:1288`–`1298`, `src/cli/commands/update.rs:1041`–`1118` (label-only route) | update_add_label, update_remove_label, update_set_labels, (case to add: `update_add_label_existing` — `["@fx=basic","update","proj-mta","--add-label","db","--json"]`), r17_upd_set_labels_empty, r17_upd_set_add, r17_upd_set_remove, r17_upd_add_label_multi, r17_upd_add_label_comma, r17_set_add_plain, r17_set_add_remove, r17_add_remove_same_plain, r17_add_remove_new_same, r21_up_set_labels_add, update_set_labels_empty_add_plain, update_set_labels_remove_present, update_set_labels_add_present |
| S4.269 | **`update --status closed` and `--status tombstone` are refused before any I/O.** Closed: JSON code `VALIDATION_FAILED`, message `Validation failed: status: refusing to close via ` + "`br update --status closed`" + `: terminal-state transitions must go through ` + "`br close`" + ` so close-policy (close-reason / AC / attribution) is enforced. Use ` + "`br close <id> --reason \"...\"`" + ` instead, or ` + "`br close <id> --bypass-policy --bypass-reason \"...\"`" + ` to opt out explicitly. See https://github.com/Dicklesworthstone/beads_rust/issues/301.`, hint `null`, `"retryable": true`, `"context": {"field": "status", "reason": <the same sentence without the `Validation failed: status: ` prefix>}`, exit **4**. Tombstone: the analogous `refusing to tombstone via ` + "`br update --status tombstone`" + `: use ` + "`br delete <id>`" + ` instead so dependency rewiring and tombstone metadata are applied correctly.` | `update proj-5u2 --status closed --json` → the pretty error block, exit 4, store unchanged Amended (2026-09-25, round 17): `--status tombstone` (any case) has its own words: `Validation failed: status: refusing to tombstone via `br update --status tombstone`: use `br delete <id>` instead so dependency rewiring and tombstone metadata are applied correctly.` | `src/cli/commands/update.rs:1751`–`1769` | update_status_closed, r17_upd_status_tombstone, r17_upd_tombstone_json, r17_upd_tombstone_upper |
| S4.270 | **`--parent <id>` re-parents:** the issue's existing `parent-child` edge is removed and one new `parent-child` edge to the resolved id is added. `--parent ""` removes the parent edge and adds none. The issue's id does NOT change (unlike `create --parent`). A re-parent that would close a blocking cycle is refused with `Error: Cycle detected in dependencies: Setting parent of <child> to <parent> would create a cycle` and the cycle hint of S4.354, exit **5**, store unchanged. Measured (2026-09-22): the plain block has no change line for a re-parent (`update_parent`); the new edge is stamped now by the actor; a parent id resolves like any partial id (`update_parent_partial`) and one that resolves to nothing is `ISSUE_NOT_FOUND`, exit 3; the issue itself as parent is the `SELF_DEPENDENCY` refusal of S4.356, exit 5; the cycle refusal is `CYCLE_DETECTED` with `context.cycle_path` equal to the message tail (`error_update_parent_cycle_json`). Measured (2026-09-23, OQ-062): `--parent proj-b75` replaces the edge to `proj-uly` (`update_reparent`). | `update proj-170 --parent ""` → `proj-170`'s `parent-child` edge to `proj-uly` removed, `blocks` edge kept, `updated_at` bumped · `update proj-170 --parent proj-mta` → cycle refusal, exit 5 Amended (2026-09-25, round 17): a `--parent` equal to the current parent, or `--parent ""` on an issue with none, changes nothing: no store write, `updated_at` unchanged (`last-touched` is still written). | `src/cli/commands/update.rs:1977`–`1990` (`resolve_parent_update`), `src/cli/commands/update.rs:1991`+ (`apply_parent_update`) | update_parent, update_parent_json, update_parent_move, update_parent_clear, update_parent_partial, error_update_parent_cycle, error_update_parent_cycle_json, error_update_parent_self, error_update_parent_missing, update_reparent, r17_upd_clear_parent_none, r17_upd_parent_same, r17_late_parent_same, r17_late_clear_parent_none, r17_late_basic_parent_same, r17_late_clear_parent_none_basic, r17_late_close_parent_force, update_parent_same_plain |
| S4.271 | **`--session <s>` is only legal together with a close**, which `update` refuses, so it is effectively unusable on `update`: `Validation failed: session: --session can only be used when closing with --status closed`, exit **4**. | — Amended (2026-09-25, round 21): `--session` is judged after the ids resolve and before an `--agent-context` that is not JSON (`--agent-context ctx --session s1` names the session; with an id that does not resolve, `Issue not found` comes first). | `src/cli/commands/update.rs:1816`–`1820` | (case to add: `error_update_session` — `["@fx=basic","update","proj-mta","--session","s1"]`), r21_up_agent_ctx, error_update_session_before_context, error_update_context_after_ids, error_update_context_plain |
| S4.272 | **`update` with ids but no field flag is a success no-op.** Plain output is `No updates specified for <id>` per id, exit **0**, the store is not rewritten, and `last-touched` IS written. | `update proj-mta` → `No updates specified for proj-mta\n`, last-touched `proj-mta\n` | `src/cli/commands/update.rs:762`+ (`execute_prepared_route_with_resources`) | (case to add: `update_no_flags` — `["@fx=basic","update","proj-mta"]`) |
| S4.273 | **Plain output of a successful `update`** is `Updated <id>: <title>` followed by one indented line per *observed* change, in this fixed order: `  status: <old> → <new>`, `  priority: P<old> → P<new>`, `  type: <old> → <new>`, `  assignee: <old> → <new>`, `  owner: <old> → <new>` (`(none)` stands for an unset value; the arrow is U+2192 surrounded by single spaces). Fields outside that list change silently. JSON output is a one-line array of `{"id","title","status","priority","assignee","owner","updated_at"}`, with `null` for unset `assignee`/`owner`. | `update proj-mta --status in_progress` → `Updated proj-mta: Set up database schema\n  status: open → in_progress\n` · `--json` → `[{"id":"proj-mta","title":"Set up database schema","status":"in_progress","priority":1,"assignee":null,"owner":null,"updated_at":"2026-01-02T03:04:05Z"}]` | `src/cli/commands/update.rs:1281`–`1325` | update_status, update_status_json, update_priority, update_title, update_claim |
| S4.274 | **`--transition-comment <text>` adds a real comment to the issue, but only when the status value actually changes.** The text is trimmed and validated like any comment; the comment's numeric id follows S4.382. | — | `src/storage/sqlite.rs:7541`–`7546` | (case to add: `update_transition_comment` — `["@fx=basic","update","proj-mta","--status","in_progress","--transition-comment","Starting","--json"]`) |
| S4.275 | **`update` refusals, verbatim.** Priority out of range: `Error: Priority must be 0-4, got: 7` + the same hint as S4.239, exit **4**. Unknown id: S4.212. Ambiguous id: `Error: Ambiguous ID 'm': matches ["proj-7vm", "proj-mkh", "proj-mta"]` + `Hint: Provide more characters of the ID`, exit **3** (the candidate list is hash-ordered in the original and is canonicalized to byte order by the harness — DISC-005). In each case the store is untouched. | `update proj-mta -p 7` → exit 4 · `update m -p 1` → exit 3 | Goldens: `goldens/error_update_bad_priority.err:1`–`goldens/error_update_bad_priority.err:2` with `goldens/error_update_bad_priority.exit:1` = `4`; `goldens/error_update_ambiguous.err:1`–`goldens/error_update_ambiguous.err:2` with `goldens/error_update_ambiguous.exit:1` = `3`; both `.out` files are zero bytes (no store dump). Source: `src/error/mod.rs:106` (priority message), `src/error/mod.rs:468`–`src/error/mod.rs:470` (its hint), `src/error/mod.rs:69` (ambiguous message, `{matches:?}`), `src/error/mod.rs:456` (its hint), exit categories `src/error/structured.rs:244` (`AmbiguousId` → 3) and `src/error/structured.rs:254` (`InvalidPriority` → 4); canonicalizer `scripts/ws_inner.py:99` (`canon`, DISC-005) | error_update_bad_priority, error_update_missing, error_update_ambiguous, error_show_ambiguous_json |

### S4.290–S4.309 `close`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.290 | **Step order.** `--bypass-policy`/`--bypass-reason` arity check → workspace discovery → empty-ids fallback to `.beads/last-touched` (same message and exit as S4.260) → the `--suggest-next` single-id check → id resolution (S4.212) → epic child counts → the pre-close blocked snapshot when `--suggest-next` → per-id precondition pass (skips collected) → the policy-gate pass → one atomic update of every surviving id → one write-back → `last-touched` → output → exit status. | — | `src/cli/commands/close.rs:639`–`803`, `src/cli/commands/close.rs:846`–`1346` | close_basic, scn_last_touched, usage_close_alone, r17_close_parent_open_kids, r17_close_parent_open_kids_json, r17_close_parent_force, r17_close_blocked_child, r17_close_blocked_child_force, r17_close_blocker_suggest, r17_close_blocker_suggest_json, r17_close_grand_suggest, r17_close_graph_tip_suggest, r17_close_graph_ah0_kxh, r17_close_hier_ip, r17_close_hier_blocked, r17_close_session_tc_plain, r17_close_deferred_due, r17_close_then_ptp_force, r18_ambig_close, r18_close_reason_empty_many, r18_close_reason_empty_json, r20_mu_close_mixed_json, r20_mu_close_mixed_quiet, r21_jr_close_suggest_two, r21_cl_graph_tip_suggest_json, r21_cl_graph_a14_suggest, r21_cl_graph_kxh_suggest, r21_cl_graph_ah0_force_suggest, r21_cl_hier_ig8_suggest, r21_cl_hier_in9_force_suggest, r21_cl_basic_mta_suggest_q, r21_cl_basic_170_force_reason, r21_cl_tree_root_force, r21_cl_diamond_b1_force, r21_cl_cycle_three_force, r21_cl_suggest_closed, r21_cl_suggest_transition, r22_g_close_unknown_short, r22_e_fmt_json_close, r22_e_bd_actor_close, r22_m_custom_close, r22_m_spell_close_done, r22_m_template_close, r22_m_empty_opt_close, r22_m_precision_close, r22_m_longtexts_close, r22_l_close_one_json, r22_j_ambiguous_close |
| S4.291 | **Fields written on a successful close:** `status` = `closed`; `closed_at` = the invocation instant; `close_reason` = the `--reason` value or, when absent, the literal `done` — it is always written; `closed_by_session` = the `--session` value when given, otherwise untouched; `updated_at` = the invocation instant. Nothing else changes: dependencies, labels, comments and `defer_until` are all preserved. | `close proj-mta` → `"status":"closed","closed_at":"2026-01-02T03:04:05Z","close_reason":"done"` · `--reason Shipped` → `"close_reason":"Shipped"` · `--session sess-1` → additionally `"closed_by_session":"sess-1"` · closing a deferred issue keeps its `defer_until` Amended (2026-09-25, round 17): `--reason ""` is echoed as `"close_reason":""` in the JSON element but stores no `close_reason` member. | `src/cli/commands/close.rs:1135`–`1150`, `src/model/mod.rs:536` | close_basic, close_reason_json, (case to add: `close_session` — `["@fx=basic","close","proj-mta","--session","sess-1","--json"]`; `close_deferred` — `["@fx=basic","close","proj-7vm"]`), r17_close_reason_empty, r17_close_reason_empty_plain, r17_close_reason_blank |
| S4.292 | **Per-id skip reasons, verbatim.** `issue not found`; `already <status>` for any terminal status (`already closed`, `already tombstone`); `<label> has <open>/<total> open children (use --force to close anyway)` where `<label>` is `epic` when the issue's type is `epic` and `parent issue` otherwise, `<open>` is `total − closed`; `<label> has <n> open dot-notation child issue(s): <first five ids joined by ", ">[, +<n−5> more] (use --force to close anyway)`; `blocked by: <blocker ids joined by ", "> — close the open blocker(s) first, or use --force to close anyway` (and, when the blocker list is empty, `blocked by dependencies — close the open blocker(s) first, or use --force to close anyway`). Each becomes `Warning: Skipped <id>: <reason>` on stderr in plain mode and `{"id":…,"reason":…}` inside `"skipped"` in JSON mode. Measured (2026-09-22): the dot-notation reason is judged after the open-children one and before the blockers, and only when no `parent-child` edge names the children (an edge-linked child gives the `<open>/<total>` reason instead); a dot-notation child is a DIRECT one, `<id>.<part>` with no further `.` (the grandchild `proj-b75.2.1` neither counts nor holds the close back), whose status is not `closed` or `tombstone`; the ids are listed in byte order (`proj-b75.1, proj-b75.10, proj-b75.11, proj-b75.2, …`); `--force` waives it (`scn_close_dotted`, `scn_close_dotted_many`). | `close proj-170` → `Warning: Skipped proj-170: blocked by: proj-mta — close the open blocker(s) first, or use --force to close anyway` · `close proj-uly` → `Warning: Skipped proj-uly: epic has 1/1 open children (use --force to close anyway)` · `close proj-mkh` → `Warning: Skipped proj-mkh: already closed` | `src/cli/commands/close.rs:930`, `:940`, `:957`–`961`, `:997`–`1002`, `:1106`–`1111` | error_close_blocked, close_epic_with_open_child, close_already_closed, scn_close_dotted, scn_close_dotted_many, scn_r9_dotted_modes, r15_close_three_json, r15_close_valid_and_missing, r15_close_valid_and_missing_json, r15_close_missing_then_valid, r15_close_closed_and_open_json, r15_close_blocked_and_free, r15_close_blocked_and_free_json, r15_close_blocker_then_blocked, r15_close_blocked_then_blocker, r15_close_ambig_and_valid, r15_close_deferred_json, r15_close_two_reason_quiet, r15_close_suggest_next_two |
| S4.293 | **When EVERY id was skipped the command fails with `NOTHING_TO_DO`, exit 3, and writes nothing.** stderr carries the per-id warnings, then `Error: Nothing to do: all <n> issue(s) skipped — <summary>` and one hint. `<summary>` lists up to five `<id>: <reason>` pairs joined by `"; "`, with `+<n−5> more` appended beyond five. | `close proj-170` → `Error: Nothing to do: all 1 issue(s) skipped — proj-170: blocked by: proj-mta — close the open blocker(s) first, or use --force to close anyway` | `src/cli/commands/close.rs:789`–`792`, `:812`–`830`, `src/error/mod.rs:275` | error_close_blocked, error_close_blocked_json, close_already_closed, close_epic_with_open_child |
| S4.294 | **When some ids closed and some were skipped the command still WRITES the closures and then fails with `CLOSE_INCOMPLETE`, exit 3.** The message is `Error: Partially applied: <closed> closed, <skipped> skipped — <summary>`. This is the one place in the mutation surface where a non-zero exit accompanies a rewritten store. | `close proj-mkh proj-mta` → `✓ Closed proj-mta: Set up database schema (done)` on stdout, the store rewritten with `proj-mta` closed, stderr `Warning: Skipped proj-mkh: already closed` and `Error: Partially applied: 1 closed, 1 skipped — proj-mkh: already closed`, exit 3 | `src/cli/commands/close.rs:793`–`798`, `src/error/mod.rs:288` | (case to add: `close_partial_skip` — `["@fx=basic","close","proj-mkh","proj-mta"]`) |
| S4.295 | **The hint attached to a `NOTHING_TO_DO`/`CLOSE_INCOMPLETE` is chosen by substring search over the whole summary, in this order:** if it contains `blocked by` → `Hint: Skipped issue(s) have open blocking dependencies. Close the blockers first, or re-run with --force to close anyway.`; otherwise if it contains `open children` or `child issue` → `Hint: Skipped issue(s) have open children. Close the children first, or re-run with --force to close anyway.`; otherwise → `Hint: Skipped issue(s) were already closed or not found.`. A mixed batch therefore always shows the blocked hint. | `close proj-170` → the blocked hint · `close proj-uly` → the children hint · `close proj-mkh` → the already-closed hint | `src/error/structured.rs:1055`–`1066` | error_close_blocked, close_epic_with_open_child, close_already_closed |
| S4.296 | **`--force` waives the open-children checks and the blocker check, and nothing else.** It does not waive "issue not found" or "already closed". | `close proj-170 --force --json` → `[{"id":"proj-170","title":"Implement user auth","status":"closed","closed_at":"2026-01-02T03:04:05+00:00","close_reason":"done"}]`, store written, exit 0 | `src/cli/commands/close.rs:946`, `:1012`–`1017` | close_blocked_force |
| S4.297 | **`--suggest-next` is restricted to one target id** (`Validation failed: suggest-next: --suggest-next only works with a single issue ID`, exit 4) — the check runs after the last-touched fallback, so a bare `close --suggest-next` is legal. The computed set is: the blocked-id set taken **before** the write, minus the blocked-id set taken **after** the write, keeping only candidates whose status is then `open` or `in_progress`. Blockedness uses the edge types of S4.219. Each is emitted as `{"id","title","priority"}`. Plain mode prints `Unblocked <n> issue(s):` and then `  <id>: <title>` per row. | `close proj-mta --suggest-next --json` → `{"closed":[{"id":"proj-mta","title":"Set up database schema","status":"closed","closed_at":"2026-01-02T03:04:05+00:00","close_reason":"done"}],"unblocked":[{"id":"proj-170","title":"Implement user auth","priority":1}]}` Amended (2026-09-25, round 21): the unblocked issues are listed in `list`'s default order, priority ascending then newest first (`r12_diamond` `close proj-a1 --suggest-next` lists `proj-c1` before `proj-b1`). | `src/cli/commands/close.rs:654`–`658`, `:893`–`903`, `:1247`–`1313` | close_suggest_next, r21_cl_diamond_a1_suggest, close_suggest_diamond_plain, close_suggest_graph_json |
| S4.298 | **`close` output shapes.** Plain success per id: `✓ Closed <id>: <title> (<close_reason>)` — the reason is always shown, `done` included. JSON without `--suggest-next`: a one-line array of `{"id","title","status","closed_at","close_reason"}` where **`closed_at` is rendered with the `+00:00` offset, not `Z`** (the store uses `Z`). With `--suggest-next` the payload is the object of S4.297. With a skip present, JSON is `{"closed":[…],"skipped":[{"id","reason"},…]}`. When nothing at all was closable the plain line is `No issues to close.`. | `close proj-mta --reason Shipped --json` → `[{…,"closed_at":"2026-01-02T03:04:05+00:00","close_reason":"Shipped"}]` while the store holds `"closed_at":"2026-01-02T03:04:05Z"` | `src/cli/commands/close.rs:750`–`780`, `:1216` | close_basic, close_reason_json, close_two, close_suggest_next, error_close_blocked_json |
| S4.299 | **Several ids are closed in one atomic update and one write-back**; `last-touched` becomes the last id actually closed. Ordering of the success lines follows the command line. | `close proj-mta proj-5u2` → `✓ Closed proj-mta: …\n✓ Closed proj-5u2: …\n`, both records changed, `last-touched` `proj-5u2\n` | `src/cli/commands/close.rs:1170`–`1177`, `:746` | close_two |
| S4.300 | **`--transition-comment <text>` adds a comment to each id whose status genuinely changes**, with the actor as author; `--bypass-policy`/`--bypass-reason` are inert without `.beads/policy.yaml` and refuse each other's absence (`--bypass-policy requires --bypass-reason "<text>"`, `--bypass-reason was set without --bypass-policy`, exit 4). No `.beads/policy.yaml` exists in any fixture, so the policy-gate pass never fires in this corpus. | — | `src/cli/commands/close.rs:88`, `:98`, `:1142` | (case to add: `error_close_bypass_alone` — `["@fx=basic","close","proj-mta","--bypass-policy"]`) |

### S4.310–S4.319 `reopen`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.310 | **With no positional ids, `reopen` uses `.beads/last-touched`**, with the same missing-file message and exit **4** as S4.260. | scenario `last_touched`: `create` → `update --status in_progress` → `close --reason Finished` → `reopen` all act on `proj-8f2` | `src/cli/commands/reopen.rs:72`–`81` | scn_last_touched, r17_reopen_due_closed, r17_reopen_deferred, r19_reopen_batch_mixed, r19_reopen_due_batch, r20_mu_reopen_mixed_json, r20_mu_reopen_quiet_reason, r21_jr_reopen_open, r22_m_custom_reopen |
| S4.311 | **A successful reopen sets `status` to plain `open`** — never the pre-close status — and clears `closed_at`, `close_reason`, `closed_by_session`, `defer_until`, `deleted_at`, `deleted_by` and `delete_reason`. `updated_at` becomes the invocation instant. | `reopen proj-mkh` → `"status":"open"`, `closed_at` and `close_reason` removed, `"updated_at":"2026-01-02T03:04:05Z"` | `src/cli/commands/reopen.rs:278`–`292` | reopen_closed, reopen_closed_json, scn_last_touched |
| S4.312 | **`--reason <text>` is stored as a COMMENT, not a field.** The comment's text is exactly `Reopened: <reason>`; its author is the resolved actor and its `created_at` is the invocation instant. The reason is also echoed in the plain success line. Without `--reason` no comment is created. | `reopen proj-mkh --reason Regressed --json` → the record gains `"comments":[{"id":2,"issue_id":"proj-mkh","author":"tester","text":"Reopened: Regressed","created_at":"2026-01-02T03:04:05Z"}]` · `reopen proj-mkh` → no comment Amended (2026-09-25, round 17): an empty `--reason` stores the comment `Reopened:` with no trailing space. | `src/cli/commands/reopen.rs:288`–`290`, `src/storage/sqlite.rs:7541`–`7546` | reopen_closed_json, reopen_closed, r17_reopen_reason_plain, r17_reopen_reason_empty |
| S4.313 | **Reopening an issue that is not closed is a SKIP, not an error.** Skip reasons, verbatim: `already <status>` (`already open`, `already in_progress`, `already deferred`, …) and `cannot reopen tombstone issue`, plus `issue not found`. Plain output is `⊘ Skipped <id>: <reason>` (U+2298 then one space) on stdout; the store is not rewritten and `last-touched` is not written. **Unlike `close`, an all-skipped `reopen` still exits 0** — there is no `NOTHING_TO_DO` on this path. When nothing at all happened the plain line is `No issues to reopen.`. Resolved (2026-09-23, OQ-093): an id that resolves to nothing is the `Issue not found` error before the batch, exit 3 (`error_reopen_missing`), not an `issue not found` skip. | `reopen proj-mta` → `⊘ Skipped proj-mta: already open\n`, exit **0**, no dumps · `reopen proj-7vm` (deferred) → `⊘ Skipped proj-7vm: already deferred`, exit 0 | `src/cli/commands/reopen.rs:247`, `:255`–`258`, `:264`–`269`, `:190`–`194` | reopen_open, (case to add: `reopen_deferred` — `["@fx=basic","reopen","proj-7vm"]`) |
| S4.314 | **Output shapes.** Plain success: `✓ Reopened <id>: <title>` plus ` (<reason>)` when `--reason` was given. JSON: `{"reopened":[{"id","title","status"}]}` with `status` always `"open"`. Successes are printed before skips when both occur. | `reopen proj-mkh --reason Regressed --json` → `{"reopened":[{"id":"proj-mkh","title":"Already finished work","status":"open"}]}` · `reopen proj-mta proj-mkh --reason Redo` → `✓ Reopened proj-mkh: Already finished work (Redo)\n⊘ Skipped proj-mta: already open\n` | `src/cli/commands/reopen.rs:167`–`192`, `:322`–`327`, `:427`–`429` | reopen_closed, reopen_closed_json, (case to add: `reopen_mixed` — `["@fx=basic","reopen","proj-mta","proj-mkh","--reason","Redo"]`) |
| S4.315 | **Reopen is the only non-`comments` command in this surface that adds a JSONL-visible record other than a field change**, via S4.312. It writes back once for the whole batch. | — | `src/cli/commands/reopen.rs:309`–`349` | reopen_closed_json |

### S4.320–S4.329 `defer` / `undefer`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.320 | **`defer` and `undefer` require explicit ids — there is NO last-touched fallback.** With none: `Error: Validation failed: ids: at least one issue ID is required`, exit **4**, nothing written. (Contrast `update`/`close`/`reopen`, S4.260.) | `defer` alone → exit 4 · `undefer` alone → exit 4 | `src/cli/mod.rs:2624`–`2630`, `src/cli/commands/defer.rs` | (case to add: `error_defer_no_ids` — `["@fx=basic","defer"]`; `error_undefer_no_ids` — `["@fx=basic","undefer"]`) |
| S4.321 | **A successful `defer` sets `status` to `deferred`, sets `defer_until` when `--until` was given, and bumps `updated_at`.** Deferring an already-deferred issue with a new `--until` changes only `defer_until` and `updated_at` (`status` is already `deferred`); the JSON reports `"previous_status":"deferred"`. | `defer proj-mta --until 2027-06-01 --json` → `"status":"deferred"`, `"defer_until":"2027-06-01T09:00:00Z"` · `defer proj-7vm --until 2028-01-01` on the already-deferred fixture issue → only `defer_until` and `updated_at` change | `src/cli/commands/defer.rs` | defer_until_date, defer_relative, (case to add: `defer_already_deferred` — `["@fx=basic","defer","proj-7vm","--until","2028-01-01","--json"]`), r15_defer_two_json, r15_defer_closed, r15_defer_closed_json, r15_defer_already_deferred, r15_defer_missing_and_valid, r15_defer_until_past, r15_defer_relative_week, r15_defer_tomorrow, r18_defer_until_yesterday, r18_defer_until_nextweek, r18_defer_until_minutes, r18_defer_until_neg, r18_defer_until_rfc_frac, r18_defer_until_spaces, r18_defer_until_nounit, r18_defer_until_bigrel, r19_defer_batch_mixed, r19_defer_inprogress_json, r19_none_defer_json, r19_usage_defer_until_noval, r20_mu_defer_quiet, r20_mu_defer_mixed_json, r21_jr_defer_closed_until, r21_df_month_end_plus1d, r21_df_leap_tomorrow, r21_df_leap_day_date, r21_df_nonleap_feb29, r21_df_year_end_plus1h, r21_df_midnight_today, r21_df_late_yesterday, r21_df_nextweek_upper, r21_df_rfc_offset, r21_df_rfc_lower_tz, r21_df_plus0m, r21_df_date_space_time, r21_df_month_13, r21_df_same_until_skip, r22_e_fmt_json_defer, r22_m_custom_defer |
| S4.322 | **`--until` grammar and the 09:00 default.** Accepted forms, tried in this order: RFC 3339 (a `+00:00:00`-style zero offset is repaired); a bare date `YYYY-MM-DD`; a relative offset `+<n><unit>` or `-<n><unit>` with unit `m`, `h`, `d` or `w`; and the case-insensitive keywords `today`, `yesterday`, `tomorrow`, `next-week`/`nextweek`. **A bare date and the keywords `yesterday`/`tomorrow`/`next-week` resolve to 09:00:00 LOCAL time of that day; `today` resolves to 17:00:00 local time.** A relative offset is added to the invocation instant. The stored value is normalized to `Z`; the JSON output renders it with a `+00:00` offset. Under the pinned environment (`TZ=UTC`) local equals UTC. A bad unit gives `Validation failed: defer_until: invalid unit (use m, h, d, w)` and an out-of-range offset gives `Validation failed: defer_until: relative duration is out of supported range`. | `--until 2027-06-01` at `2026-01-02T03:04:05Z`, `TZ=UTC` → stored `"2027-06-01T09:00:00Z"`, printed `"2027-06-01T09:00:00+00:00"` · `--until +1h` → stored `"2026-01-02T04:04:05Z"`, printed `"2026-01-02T04:04:05+00:00"` | `src/util/time.rs:35`–`98` (`parse_flexible_timestamp`; the `9, 0, 0` literal is inlined at `:45`, `:77`, `:83`, `:89`, and `17, 0, 0` at `:71`), `src/cli/commands/defer.rs:251`–`256` | defer_until_date, defer_relative |
| S4.323 | **`defer` without `--until` sets `status` to `deferred` and CLEARS `defer_until`** — the update carries an explicit "set to nothing", so an existing `defer_until` is wiped and the key disappears from the record. The plain line names it: `⏱ Deferred <id>: <title> (indefinitely)` (U+23F1 then one space); with `--until` the suffix is ` (until <rfc3339>)`. In JSON, `defer_until` is omitted when absent. | `defer proj-mta` → `"status":"deferred"`, no `defer_until` key, `updated_at` bumped · `defer proj-7vm` (already carrying `defer_until`) → the key is removed | `src/cli/commands/defer.rs:310`–`316`, `:198`–`210` | defer_no_until, (case to add: `defer_clears_until` — `["@fx=basic","defer","proj-7vm","--json"]`) |
| S4.324 | **An invalid `--until` is refused once, before any issue is examined:** `Error: Validation failed: defer_until: invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)`, exit **4**, store unchanged, no id touched even when several were named. | `defer proj-mta --until not-a-date` → exit 4 | `src/util/time.rs:53`–`96`, `src/cli/commands/defer.rs:251`–`256` | error_defer_bad_until |
| S4.325 | **A successful `undefer` always REMOVES `defer_until`, and sets `status` to `open` only when the status was `deferred`.** An issue that merely carried a `defer_until` while `in_progress` keeps its status and just loses the field. `updated_at` is bumped either way. Plain line: `✓ Undeferred <id>: <title> (now open)`. JSON: `{"undeferred":[{"id","title","previous_status","status"}]}`. | `undefer proj-7vm --json` → `{"undeferred":[{"id":"proj-7vm","title":"Refactor config loader","previous_status":"deferred","status":"open"}]}`, record loses `defer_until`, `"status":"open"` | `src/cli/commands/defer.rs:607`–`622` | undefer_basic, r15_undefer_two, r15_undefer_two_json, r15_undefer_missing, r19_undefer_batch_json, r19_undefer_due_batch, r20_mu_undefer_quiet, r20_mu_undefer_mixed_json, r21_jr_undefer_open, r21_undf_after_gate, r22_m_spell_undefer |
| S4.326 | **Per-id skips of `defer`/`undefer`, verbatim.** `defer`: `already deferred` — and only when BOTH the status is already `deferred` AND the stored `defer_until` already equals the requested one, so re-deferring to a different instant is a normal update; `cannot defer <status> issue` for a `closed` or `tombstone` issue; `issue not found`. `undefer`: `not deferred (status: <status>)` when the status is not `deferred` and no `defer_until` is stored; `cannot undefer <status> issue` for a terminal issue that still carries a stale `defer_until`; `issue not found`. Each prints `⊘ Skipped <id>: <reason>` on **stdout**, exit **0**; an all-skipped run rewrites nothing and does not write `last-touched`. When literally nothing happened the line is `No issues to defer.` / `No issues to undefer.`. | `undefer proj-mta` → `⊘ Skipped proj-mta: not deferred (status: open)\n`, no dumps Amended (2026-09-25, round 15): the not-deferred skip is judged FIRST, so a closed issue without a gate is `not deferred (status: closed)`; `cannot undefer closed issue` needs a stale gate. | `src/cli/commands/defer.rs:279`–`308`, `:575`–`604`, `:211`–`218`, `:158`–`160`, `:464`–`466` | undefer_not_deferred, r15_undefer_closed |
| S4.327 | **Several ids: a skip never stops the rest, successes are printed before skips, one atomic batch and one write-back cover them all, and `last-touched` becomes the last SUCCESSFUL id.** A hard failure on any eligible id (a capacity or workflow refusal) rolls back the whole batch, so no sibling is deferred. | `defer proj-mta proj-5u2` → both deferred, `last-touched` `proj-5u2\n` · `undefer proj-7vm proj-mta` → `✓ Undeferred proj-7vm: …\n⊘ Skipped proj-mta: not deferred (status: open)\n`, `last-touched` `proj-7vm\n` Amended (2026-09-25, round 15): the ids the batch ACTS on must not repeat: `close`, `reopen`, `update`, `defer` and `undefer` given one issue twice (as typed, or once in full and once as a partial id) are refused with `Validation failed: issue_ids: duplicate issue ID in atomic update batch: <id>`, VALIDATION_FAILED, exit 4, `retryable` true, `context` `{"field": "issue_ids", "reason": …}`, and nothing else is printed or written; the id named is the first met twice scanning in order (`close proj-uly proj-mta proj-mta proj-uly` names `proj-mta`). Skipped ids do not count (`close proj-mkh proj-mkh` of a closed issue skips twice; `undefer` of an issue that is not deferred skips twice). Unresolvable ids, `update --session` and `update --parent`, and each id's own checks (a claim of a closed issue) are reported first; `update` without an update flag has no batch and answers `No updates specified for <id>` per id. | `src/cli/commands/defer.rs:332`–`339`, `:109`–`156` | (case to add: `defer_two` — `["@fx=basic","defer","proj-mta","proj-5u2"]`; `undefer_mixed` — `["@fx=basic","undefer","proj-7vm","proj-mta"]`), r15_close_dup_id, r15_close_dup_id_json, r15_reopen_two_json, r15_reopen_two_plain, r15_reopen_missing_and_closed, r15_reopen_deferred, error_close_dup_after_skip, error_close_dup_first_repeat, close_dup_both_skipped, error_reopen_dup_ids, error_update_dup_ids, update_dup_no_flags, error_update_dup_session_first, error_defer_dup_ids, error_undefer_dup_ids, undefer_dup_not_deferred, r16_close_twice_json, r16_close_twice_q, r16_reopen_twice_json, r16_reopen_twice_q, r16_defer_twice_json, r16_undefer_twice_json, r16_undefer_twice_q, r16_close_partial_dup |
| S4.328 | **`defer`/`undefer` accept `--transition-comment <text>`, which adds a comment to each id whose status actually changes**, with the same id rule as S4.382. On `undefer` it is dropped when the status does not change. | — | `src/cli/mod.rs:2637`–`2639`, `src/cli/commands/defer.rs:614`–`617` | (case to add: `defer_transition_comment`) |

### S4.330–S4.349 `delete`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.330 | **`delete` is a TOMBSTONE, not a removal: the record stays in the store.** A successful soft delete sets `status` to `tombstone`, `deleted_at` to the invocation instant, `deleted_by` to the resolved actor, `delete_reason` to the `--reason` value (default the literal `delete`), `original_type` to the issue's `issue_type` **as it was before**, and bumps `updated_at`. `issue_type` itself is left unchanged, as are `title`, `description`, `priority`, `assignee`, `created_at`, `labels` and `comments`. | `delete proj-5u2` → `{"id":"proj-5u2","title":"Write API docs","status":"tombstone","priority":3,"issue_type":"docs",…,"updated_at":"2026-01-02T03:04:05Z","source_repo":"proj","source_repo_path":"/mnt/proj","deleted_at":"2026-01-02T03:04:05Z","deleted_by":"tester","delete_reason":"delete","original_type":"docs","compaction_level":0,"original_size":0}` · `--reason Duplicate` → `"delete_reason":"Duplicate"` Amended (2026-09-25, round 19): `--reason ""` stores no `delete_reason` member. | `src/cli/mod.rs:1412`–`1414` (`default_value = "delete"`), `src/storage/sqlite.rs:7892`–`7983` | delete_basic, delete_json, delete_cascade, r15_delete_two, r15_delete_two_json, r15_delete_valid_missing, r15_delete_valid_missing_json, r15_delete_blocker_and_blocked, r15_delete_blocker_and_blocked_json, r15_delete_closed_dry, r15_delete_epic, r15_delete_epic_force_json, r17_del_parent_children, r17_del_parent_children_json, r17_del_parent_force, r17_del_parent_cascade, r17_del_parent_cascade_plain, r17_del_parent_cascade_dry, r17_del_blocker_force, r17_del_blocker, r17_del_leaf_grand, r17_del_mid_child, r17_del_hier_cascade, r17_del_hier_cascade_dry, r17_del_hier_force_dry, r17_del_graph_two, r17_del_graph_both_blockers, r17_del_short_ids, r17_del_from_file_md, r17_del_closed, r17_del_tree_cascade_hard, r17_del_hard_dry, r19_delete_graph_tip, r19_delete_graph_tip_force, r19_delete_graph_tip_cascade_json, r19_delete_graph_tip_cascade_dry, r19_delete_reason_empty_json, r19_delete_tombstone, r19_delete_closed_reason, r19_delete_dup_ids, r19_delete_ext_dep, r19_none_delete_json, r19_dup_delete_json, r19_usage_delete_reason_noval, delete_reason_empty_plain, r20_mu_delete_quiet, r21_jr_delete_missing, r22_e_fmt_json_delete |
| S4.331 | **Every dependency edge touching a deleted issue is removed, in BOTH directions and for ALL edge types**, before the tombstone is written. The deleted issue's own `dependencies` array disappears; every other issue that pointed at it loses that edge and has its `updated_at` bumped to the same instant, and is therefore rewritten. | `delete proj-5u2` → `proj-5u2`'s `related` edge to `proj-170` gone · `delete proj-mta --force` → `proj-170`'s `blocks` edge to `proj-mta` gone, `proj-170.updated_at` bumped, its `parent-child` edge kept · `delete proj-mta --cascade` → `proj-5u2` loses its `related` edge to the cascaded `proj-170` and is rewritten | `src/storage/sqlite.rs:12812`–`12880` (`remove_all_dependencies`), `src/cli/commands/delete.rs:276`–`290` | delete_basic, delete_with_dependents_force, delete_cascade |
| S4.332 | **"Dependents" for the refusal check are: every issue with a `blocks`/`conditional-blocks`/`waits-for` edge pointing AT a deleted id, plus the PARENT named by a `parent-child` edge whose child is a deleted id.** Ids already in the delete set are excluded; the list is sorted and deduplicated. `related` and the other non-blocking types never count. | `delete proj-mta` → dependents `[proj-170]` (a `blocks` edge) · `delete proj-5u2` → no dependents, so it deletes straight away | `src/storage/sqlite.rs:14326`–`14347` (`get_blocked_issue_ids`), `src/cli/commands/delete.rs:898`–`917` | delete_with_dependents, delete_basic |
| S4.333 | **With dependents present and neither `--force` nor `--cascade`, `delete` is a PREVIEW that writes nothing and exits 0.** Plain output, verbatim: `The following issues depend on issues being deleted:` then `  - <id>` per dependent; then, when the transitive closure is larger, a blank line, `<n> additional issue(s) would be transitively affected by --cascade:` and `  - <id>` per extra; then a blank line, `Use --force to orphan these dependents, or --cascade to delete them recursively.`, `--cascade would delete <n> total dependent(s).` and `No changes made (preview mode).`. Measured (2026-09-22): the preview wins over `--hard` and `--dry-run` alike (`delete_hard_preview`). Under `--json` the preview is one compact line, `{"preview":true,"would_delete":[<named ids, ascending>],"cascade_delete":[<the closure beyond the named ids>],"blocked_dependents":[<the direct dependents>],"orphaned_issues":[]}`, exit 0 (`delete_preview_json`, `delete_preview_json_many`). | `delete proj-mta` → the six-block preview naming `proj-170` as the dependent and `proj-uly` as transitively affected, `--cascade would delete 2 total dependent(s).`, exit **0**, no dumps | `src/cli/commands/delete.rs:141`–`194` | delete_with_dependents, delete_hard_preview, delete_preview_json, delete_preview_json_many |
| S4.334 | **`--force` deletes the named ids only and ORPHANS the dependents** (their edges to the deleted ids vanish, they themselves are untouched apart from the edge and `updated_at`). The orphan list appears in the plain output and in the JSON `orphaned_issues`. `--force` and `--cascade` are mutually exclusive at the parser. | `delete proj-mta --force` → `Deleted 1 issue(s):\n  - proj-mta\nRemoved 1 dependency link(s)\nOrphaned 1 issue(s):\n  - proj-170\n` | `src/cli/commands/delete.rs:293`–`295`, `src/cli/mod.rs:1425`–`1428` | delete_with_dependents_force |
| S4.335 | **`--cascade` deletes the transitive closure of dependents** (repeatedly applying S4.332's relation), excluding nothing, and reports `orphaned_issues` as empty. Edge removal is applied first over the whole set; the tombstones are then written in **ascending id order**, and the reported `deleted` array is in that same ascending id order — not the discovery order. | `delete proj-mta --cascade --json` → `{"deleted":["proj-170","proj-mta","proj-uly"],"deleted_count":3,"dependencies_removed":3,"labels_removed":0,"events_removed":0,"references_updated":0,"orphaned_issues":[]}`; `proj-uly` is reached because `proj-170` is its child via `parent-child` | `src/cli/commands/delete.rs:873`–`896`, `:263`–`267`, `:298`–`326` | delete_cascade |
| S4.336 | **JSON counters.** `deleted` = the ids tombstoned or purged, ascending. `deleted_count` = its length. `dependencies_removed` = the total number of dependency rows deleted, counting an edge once per endpoint pass (outgoing rows of each deleted id plus incoming rows pointing at each deleted id). `labels_removed` and `events_removed` are counted **only on the `--hard` path** and are `0` for every soft delete. `references_updated` is never incremented and is always `0`. `orphaned_issues` is non-empty only with `--force`. Measured (2026-09-22): on the `--hard` path `labels_removed` is the number of labels the removed records carry, and `events_removed` equals `dependencies_removed` in every captured case (comments do not count: `proj-ptp`, with one comment, purges with `events_removed` 0). Correction (2026-09-22, later the same day): that holds without `--cascade` only; under `--cascade --hard` the original's `events_removed` varies between runs (1, 2 or 3 of 3 for `delete proj-mta --cascade --hard --json` over twelve runs), and the port prints `dependencies_removed` (DISC-011). | `delete proj-5u2 --reason Duplicate --json` → `{"deleted":["proj-5u2"],"deleted_count":1,"dependencies_removed":1,"labels_removed":0,"events_removed":0,"references_updated":0,"orphaned_issues":[]}` although `proj-5u2` carries no labels and `proj-mta` (which has two) is untouched Amended (2026-09-25, round 19): measured on the `--hard` path, `events_removed` is the number of purged records that an edge still touches when each is purged (two edges of one record are one event; a record with labels or comments only is none; two purged records with their own edges are two); with an edge between two purged records, which of them counts it depends on the original's order (DISC-011). | `src/cli/commands/delete.rs:26`–`34`, `:277`–`301`, `src/storage/sqlite.rs:12832` | delete_json, delete_cascade, delete_hard_force_json, delete_hard_cascade_json, r19_delete_hier_root_hard_json, delete_hard_events_out_same, delete_hard_events_in_diff, delete_hard_events_two, delete_hard_labels_only |
| S4.337 | **Plain output of a soft delete**, verbatim: `Deleted <n> issue(s):`, then `  - <id>` per id, then `Removed <n> dependency link(s)` when any edge was removed, then `Orphaned <n> issue(s):` and `  - <id>` per orphan when `--force` produced any. | `delete proj-5u2` → `Deleted 1 issue(s):\n  - proj-5u2\nRemoved 1 dependency link(s)\n` | `src/cli/commands/delete.rs:339`–`380` | delete_basic, delete_with_dependents_force |
| S4.338 | **`--dry-run` writes nothing** (no store, no `last-touched` removal) and prints `Dry-run: Would delete <n> issue(s):` then `  - <id>: <title>` per id, plus `Would also cascade delete <n> dependent(s):` / `Would orphan <n> dependent(s):` blocks when the corresponding flag was given. It is evaluated AFTER the dependents preview, so `--dry-run` on an issue with dependents and no `--force`/`--cascade` prints the *preview* text of S4.333 instead. Measured (2026-09-22): the `Would orphan <n> dependent(s):` (under `--force`: the direct dependents) and `Would also cascade delete <n> dependent(s):` (under `--cascade`: the closure beyond the named ids) blocks list `  - <id>` lines without titles and are absent when they would name nothing (`delete_dry_run_cascade_none`). Under `--json` a dry run prints the preview shape of S4.333: `--force` puts the dependents in `orphaned_issues`, `--cascade` the closure in `cascade_delete`, and `blocked_dependents` is empty under either (`delete_dry_run_force_json`, `delete_dry_run_cascade_json`). Under `--quiet` it prints nothing, exit 0. | `delete proj-5u2 --dry-run` → `Dry-run: Would delete 1 issue(s):\n  - proj-5u2: Write API docs\n`, exit 0, no dumps | `src/cli/commands/delete.rs:196`–`261` | delete_dry_run, delete_dry_run_json, delete_dry_run_quiet, delete_dry_run_force, delete_dry_run_force_json, delete_dry_run_cascade, delete_dry_run_cascade_json, delete_dry_run_cascade_none |
| S4.339 | **`--hard` purges instead of tombstoning: the record leaves the store entirely**, along with its comments, labels, edges and events. The plain output is identical to a soft delete's. With **no ids at all**, `--hard` purges every existing tombstone (`No tombstones to purge.` or `Purged <n> tombstone(s)` then `  - <id>` lines, tombstone ids in ascending order). Measured (2026-09-22): the other end of every edge is cleaned and its `updated_at` bumped as in S4.331; `--hard` on an id that is ALREADY a tombstone purges it (unlike S4.341), so the store is always rewritten (`scn_delete_hard_tombstone`). The purge prints `✓ Purged <n> tombstone(s)` (no colon) and `  - <id>` lines, or `✓ No tombstones to purge.`; its dry run `Dry-run: Would purge <n> tombstone(s):` with the lines, or `Dry-run: no tombstones to purge.`; its JSON the counters of S4.336 over the tombstones (an empty set writes nothing), its dry-run JSON the preview shape with the tombstones as `would_delete` (`scn_delete_purge`). | `delete proj-5u2 --hard` → the store loses the `proj-5u2` line completely; the remaining seven records are unchanged | `src/cli/commands/delete.rs:299`–`312`, `src/storage/sqlite.rs:7998`–`8063`, `src/cli/commands/delete.rs:389`–`479` | delete_hard, delete_hard_quiet, delete_hard_touched, delete_hard_all, delete_hard_all_dry_run, scn_delete_hard_tombstone, scn_delete_purge, r8_delete_hard_from_file, scn_r9_purge_modes |
| S4.340 | **`--from-file <PATH>` appends ids read from a file**: one per line, each line trimmed of whitespace, lines that are empty or begin with `#` after trimming are skipped, and there is no inline-comment stripping (`proj-1 # note` is taken whole and then fails id resolution). The combined id list — positionals then file ids — is deduplicated and **sorted ascending** before resolution, so the command line order never reaches the output. With neither positionals nor a file and without `--hard`: `Error: Validation failed: ids: no issue IDs provided`, exit **4**. Measured (2026-09-22): the file ids join the positionals before the sort (`delete_from_file_json`: `proj-b75` and `proj-mta`); a file with no id left is the no-ids refusal (`error_delete_from_file_empty`, `/dev/null`); a line is resolved whole, so a JSON line fails as `Issue not found: <the line>` for the first line in ascending order (`error_delete_from_file_lines`). Reading the file: S4.397. | — | `src/cli/commands/delete.rs:850`–`871`, `:88`–`113` | usage_delete_alone, delete_from_file, delete_from_file_json, error_delete_from_file_empty, error_delete_from_file_lines |
| S4.341 | **Deleting an already-tombstoned issue is a silent no-op for that id** (the storage returns the record unchanged); no field is rewritten for it. | — | `src/storage/sqlite.rs:7903`–`7905` | scn_delete_twice (measured 2026-09-20: the second `delete proj-5u2` prints `Deleted 1 issue(s):` and `  - proj-5u2`, with no `Removed … dependency link(s)` line, and writes NOTHING — the record keeps the `updated_at`, `deleted_at`, `deleted_by` and `delete_reason` of the first delete) |

### S4.350–S4.369 `dep add` / `dep remove`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.350 | **A new edge is written into the DEPENDENT issue's `dependencies` array** with exactly six fields: `issue_id` (the resolved dependent), `depends_on_id` (the resolved target or the verbatim `external:…` string), `type` (the canonicalized type, default `blocks`), `created_at` (the invocation instant), `created_by` (the resolved actor), `metadata` (the `--metadata` value verbatim when it is non-blank valid JSON, otherwise the literal `"{}"`), `thread_id` (always the empty string `""`, since the insert never names the column and the schema default is `''`). The target issue's record is NOT modified. Measured (2026-09-23): valid `--metadata` is stored VERBATIM (`"{"b": 1, "a": 2.50}"`, spaces and `2.50` kept); blank is `"{}"`; invalid is exit 4, `Validation failed: metadata: dependency metadata must be valid JSON for proj-ptp -> proj-mta (type=blocks); found "{bad": key must be a string at line 1 column 2`. | `dep add proj-5u2 proj-mta` → `{"issue_id":"proj-5u2","depends_on_id":"proj-mta","type":"blocks","created_at":"2026-01-02T03:04:05Z","created_by":"tester","metadata":"{}","thread_id":""}` appended; `proj-mta` byte-identical · `--metadata '{"a":1}'` → `"metadata":"{\"a\":1}"` Amended (2026-09-24, OQ-136): the refused text is written as Rust's `{:?}` writes a string: `\0` `\t` `\n` `\r` `\"` `\\` by name, every other control character, DEL through U+00A0, U+00AD, the combining marks and every code point Rust's `is_printable` refuses (unassigned ones included) as `\u{<lower-case hex, no leading zeros>}`, all else raw (the table was measured over every scalar value, not typed from Unicode data); the position is `line <l> column <c>` where the line counts the line feeds before the error and the column counts bytes after the last of them (`{` LF ` x` → `key must be a string at line 2 column 2`; a text ending in a line feed → column 0; an error at a multi-byte character is at its first byte). | `src/storage/sqlite.rs:12505`–`12519`, `src/storage/schema.rs:358` | dep_add_blocks, dep_add_json, dep_add_related, (case to add: `dep_add_metadata` — `["@fx=basic","dep","add","proj-5u2","proj-mta","--metadata","{\"a\":1}","--json"]`), dep_add_metadata, error_dep_add_metadata_bad, dep_add_metadata_blank, error_dep_add_metadata_lines, error_dep_add_metadata_escape, error_dep_add_metadata_quoting, error_dep_add_metadata_prefix |
| S4.351 | **`dep add` bumps ONLY the dependent's `updated_at`**, and only when a row was actually inserted. `dep remove` likewise bumps only the dependent's `updated_at`, and only when a row was actually deleted. | `dep add proj-ptp proj-mta -t related` → `proj-ptp.updated_at` `2026-01-01T12:30:00Z` → `2026-01-02T03:04:05Z`, `proj-mta` unchanged | `src/storage/sqlite.rs:12525`–`12532`, `:12787`–`12795` | dep_add_related, dep_remove_basic |
| S4.352 | **The eleven accepted `--type` values for `dep add`, verbatim and in this order:** `blocks`, `parent-child`, `conditional-blocks`, `waits-for`, `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes`, `caused-by`. The default is `blocks`. Matching is case-insensitive and the stored value is the canonical spelling. **Custom types are REJECTED** with message `Validation failed: type: Unknown dependency type: '<t>'. Allowed types: blocks, parent-child, conditional-blocks, waits-for, related, discovered-from, replies-to, relates-to, duplicates, supersedes, caused-by` and hint `Use --type with one of: blocks, parent-child, conditional-blocks, waits-for, related, discovered-from, replies-to, relates-to, duplicates, supersedes, caused-by.` (or, when a near match exists by normalization, unique prefix, or edit distance ≤ 2, `Did you mean --type <nearest>?`), exit **4**, store unchanged. Measured (2026-09-23, OQ-126): `BLOCKS` is accepted; `Parent_Child`, `paren`, `dup`, `waits`, `relate` (→ `related`), `block`, `blcks` hint `Did you mean --type <type>?`; `xyz` hints `Use --type with one of: <the eleven>.` | `dep add proj-ptp proj-mta -t inspired-by --json` → the pretty `VALIDATION_FAILED` block with that message and hint, `"retryable": true`, `"context": {"field":"type","reason":…,"hint":…}`, exit 4 | `src/cli/commands/dep.rs:843`–`915`, `src/cli/mod.rs:2226` | dep_add_custom_type, dep_add_related, dep_add_type_parent_child, dep_add_type_paren, dep_add_type_relate, dep_add_type_dup, dep_add_type_waits, dep_add_type_blocks, dep_add_type_block, dep_add_type_blcks, dep_add_type_xyz |
| S4.353 | **Validation order for `dep add`**, each step refusing before the next: cross-project route check (skipped entirely for an `external:` target) → resolve the dependent id → resolve the target id (skipped for `external:`) → parse `--type` → self-dependency → (inside the write) `--metadata` must be valid JSON → `parent-child` endpoints must both be local → the dependent must exist and not be a tombstone → the target must exist and not be a tombstone (existence skipped for `external:`) → the single-parent rule for `parent-child` → **duplicate edge** → **cycle**. Because duplicate precedes cycle, re-adding an existing edge never reports a cycle. Measured (2026-09-22, OQ-124): a tombstoned target fails `Validation failed: depends_on_id: cannot depend on tombstone issue: <id>`, a tombstoned dependent `Validation failed: issue_id: cannot add dependency from tombstone issue: <id>`, both `VALIDATION_FAILED`, exit 4, nothing written (`scn_dep_tombstone`). | `dep add proj-mta proj-zzz` → target resolution fails first, exit 3 | `src/cli/commands/dep.rs:82`–`105`, `:545`–`655`, `src/storage/sqlite.rs:12412`–`12537` | error_dep_add_missing, error_dep_add_self, dep_add_duplicate, error_dep_add_cycle, usage_dep_add_one_arg, scn_dep_tombstone, scn_r9_dep_tombstone_quiet, r16_dep_add_hier_json, r16_dep_add_hier_cycle, r16_dep_add_hier_dup, r16_dep_add_hier_dup_json, r16_dep_add_hier_dup_related, r16_dep_add_par_pc_dup, r16_dep_add_par_pc_second, r16_dep_add_par_pc_second_json, r16_dep_add_par_blocks_parent, r16_dep_add_par_partial, r16_dep_add_par_partial_q, r16_dep_add_par_ambig, r16_dep_add_large_json, r16_dep_add_large_closed_target, r16_dep_add_n984, r16_dep_add_cycle3_close, r16_dep_add_cycle3_again, r16_dep_add_cycle3_related, r16_dep_add_tomb_from, r16_dep_add_ext_json, r16_dep_add_ext_q, r16_dep_add_ext_dup, r16_dep_add_waits, r16_dep_add_cond, r16_dep_add_disc, r16_dep_add_missing_from_json, r16_dep_add_missing_to_json, r16_dep_add_par_rev_blocks, r16_dep_add_par_rev_related, r16_dep_add_tomb_other, r16_dep_add_to_tomb, r20_d_add_dup_blocks, r20_d_add_related_existing_blocks, r20_d_add_discovered_json, r20_d_add_waits_plain, r20_d_add_condblocks_json, r20_d_add_custom_type_json, r20_d_add_self_pc, r20_d_add_self_related_json, r20_d_add_missing_src_json, r20_d_add_ext_json, r20_d_add_ext_related, r20_d_add_ext_empty, r20_d_add_to_closed, r20_d_add_from_closed, r20_d_add_partial_ids, r20_d_add_quiet_json, r21_jr_dep_cycle, r22_e_fmt_json_dep_add, r22_m_template_dep_add, r22_j_self_dep |
| S4.354 | **Cycle detection considers only the blocking edge types** `blocks`, `parent-child`, `conditional-blocks`, `waits-for`; a `related`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes` or `caused-by` edge is never cycle-checked. For `parent-child` the direction is INVERTED relative to the stored row: a stored row is child → parent, and the blocker edge is parent → child, so the check runs from the parent to the child. The traversal is a breadth-first search from the target back towards the dependent over `depends_on_id` of `blocks`/`conditional-blocks`/`waits-for` rows unioned with `issue_id` of `parent-child` rows, bounded by a maximum depth beyond which no cycle is reported. | `dep add proj-mta proj-170` on fixture `basic` (where `proj-170` already blocks on `proj-mta`) → refused Amended (2026-09-25, round 16): a new `parent-child` edge is checked too, on the graph where every `parent-child` edge runs parent → child: `dep add proj-9vo proj-9vo.1 -t parent-child` (the child already a child of the parent) is the cycle refusal of S4.355, exit 5; a new `blocks` edge that closes a loop through a `parent-child` edge is one as well; a reverse `blocks` edge alone does not make a `parent-child` edge a cycle. | `src/storage/sqlite.rs:6958`–`7039`, `:12488`–`12503` | error_dep_add_cycle, error_dep_add_cycle_json, r16_dep_add_par_pc_rev, r16_dep_add_basic_pc_rev, r16_dep_add_par_pc_grand_rev, error_dep_add_pc_loop, error_dep_add_blocks_through_pc, dep_add_pc_over_reverse_blocks |
| S4.355 | **The cycle refusal never prints a path.** stderr: `Error: Cycle detected in dependencies: Adding dependency <dependent> -> <target> would create a cycle` then `Hint: Remove one dependency to break the cycle. Note: epic containment participates in blocking cycles — depending on an epic implies depending on its entire subtree, so the cycle may traverse parent-child edges that the message does not list (see docs/CLI_REFERENCE.md, ` + "`dep add`" + `)`. Exit **5**, store unchanged. In JSON mode the pretty block carries `"code": "CYCLE_DETECTED"`, that message, that hint, `"retryable": false` and `"context": {"cycle_path": "Adding dependency <dependent> -> <target> would create a cycle"}` — the "path" is the same sentence, not a list of ids. | `dep add proj-mta proj-170` → `Error: Cycle detected in dependencies: Adding dependency proj-mta -> proj-170 would create a cycle` | `src/storage/sqlite.rs:12500`–`12503`, `src/error/mod.rs:205`, `:459`–`465` | error_dep_add_cycle, error_dep_add_cycle_json |
| S4.356 | **Self-dependency is refused by identity of the RESOLVED ids**: stderr `Error: Issue cannot depend on itself: <id>` then `Hint: An issue cannot depend on itself`, exit **5**, store unchanged. | `dep add proj-mta proj-mta` → exit 5 Amended (2026-09-25, round 16): the self-dependency is named before a tombstone refusal (`dep add proj-tmb proj-tmb` on a tombstoned issue is `Issue cannot depend on itself`, exit 5). | `src/cli/commands/dep.rs:565`+, `src/error/mod.rs:213`, `:466` | error_dep_add_self, r16_dep_add_tomb_to, r16_dep_add_tomb_self_json |
| S4.357 | **An `external:<anything>` target skips route validation, id resolution and the existence check**, and is stored verbatim as `depends_on_id`. It is rejected only for `parent-child` (`parent-child dependencies must link local issues: <endpoint>`, exit 4). Measured (2026-09-22, OQ-124): the external-parent refusal is `VALIDATION_FAILED` with field `depends_on_id`, hint `null`, exit 4 (`error_dep_add_external_parent_json`). | `dep add proj-mta external:other-1` → `✓ Added dependency: proj-mta -> external:other-1 (blocks)` and the edge `{"issue_id":"proj-mta","depends_on_id":"external:other-1","type":"blocks",…}` | `src/cli/commands/dep.rs:298`–`300`, `:557`–`561`, `src/storage/sqlite.rs:12130`–`12167` | dep_add_external, error_dep_add_external_parent, error_dep_add_external_parent_json |
| S4.358 | **Adding an edge that already exists is a success no-op.** stdout `Dependency already exists: <dependent> → <target>` (U+2192 arrow), exit **0**, the store is NOT rewritten, no `updated_at` bump — but `.beads/last-touched` IS written with the dependent's id. JSON: `{"status":"exists","issue_id":…,"depends_on_id":…,"type":…,"action":"already_exists"}`. | `dep add proj-170 proj-mta` → `Dependency already exists: proj-170 → proj-mta\n`, last-touched `proj-170\n`, no store dump | `src/storage/sqlite.rs:12474`–`12486`, `src/cli/commands/dep.rs:597`–`648` | dep_add_duplicate |
| S4.359 | **`dep remove <issue> <target>` removes the single edge between the pair.** When several types connect the pair, `--type` is required: `Validation failed: <…> multiple dependency types connect <issue> to <target>; specify --type to remove one relationship`, exit **4**. `--type` on removal also accepts a custom type verbatim when an edge with exactly that type exists (so JSONL-imported custom edges can be removed). | `dep remove proj-170 proj-mta --json` → `{"status":"ok","issue_id":"proj-170","depends_on_id":"proj-mta","type":"blocks","action":"removed"}`, that edge gone, `proj-170.updated_at` bumped, the `parent-child` edge kept · `dep remove proj-170 proj-uly -t parent-child` → the parent-child edge gone | `src/cli/commands/dep.rs:820`–`836`, `src/storage/sqlite.rs:12758`–`12764` | dep_remove_basic, dep_remove_typed |
| S4.360 | **Removing an edge that is not there is a success no-op with a WARNING.** stdout is empty, stderr `Warning: Dependency not found: <issue> → <target>` (U+2192), exit **0**, the store is NOT rewritten — but `.beads/last-touched` IS written with the dependent's id. JSON: `{"status":"not_found","issue_id":…,"depends_on_id":…,"type":"<type or the literal unknown>","action":"not_found"}`. | `dep remove proj-mta proj-ptp` → stderr warning only, last-touched `proj-mta\n`, no store dump | `src/cli/commands/dep.rs:711`–`806` | dep_remove_absent, dep_remove_absent_json (measured 2026-09-20: under `--json` the `not_found` payload goes to stdout and NO warning is written to stderr) |
| S4.361 | **Plain success lines.** Add: `✓ Added dependency: <issue> -> <target> (<type>)` (ASCII `->` in plain mode). Remove: `✓ Removed dependency: <issue> -> <target> (<type>)`. JSON add: `{"status":"ok","issue_id":…,"depends_on_id":…,"type":…,"action":"added"}`. | `dep add proj-5u2 proj-mta` → `✓ Added dependency: proj-5u2 -> proj-mta (blocks)\n` | `src/cli/commands/dep.rs:639`–`642`, `:786`–`797` | dep_add_blocks, dep_add_json, dep_add_related, dep_remove_basic |
| S4.362 | **Both `dep add` and `dep remove` write `.beads/last-touched` with the RESOLVED DEPENDENT id, unconditionally on any non-refusing path**, before the output is produced, and therefore also in `--json` and `--quiet` mode. The target id is never the last-touched id. | `dep add proj-mta external:other-1` → last-touched `proj-mta\n` | `src/cli/commands/dep.rs:594`, `:778` | dep_add_blocks, dep_add_external, dep_remove_basic, dep_remove_absent, dep_add_duplicate, r16_dep_rm_hier_both, r16_dep_rm_hier_q, r16_dep_rm_hier_absent, r16_dep_rm_hier_absent_json, r16_dep_rm_par_pc, r16_dep_rm_par_wrong_type, r16_dep_rm_ext, r16_dep_rm_missing, r16_dep_rm_self, r20_d_rm_missing_edge_json, r20_d_rm_type_mismatch, r20_d_rm_related, r20_d_rm_ext, r20_d_rm_missing_target, r22_j_dep_remove_missing |
| S4.363 | **A `parent-child` edge is subject to a single-parent rule.** Adding a second, different parent is refused: `issue <child> already has parent <existing>; clear or replace the existing parent before adding <new>`, exit **4**. Re-adding the same parent is the duplicate no-op of S4.358. | `dep add proj-170 proj-mta -t parent-child` on fixture `basic` (where `proj-170`'s parent is `proj-uly`) → stderr `Error: Validation failed: depends_on_id: issue proj-170 already has parent proj-uly; clear or replace the existing parent before adding proj-mta`, no `Hint:` line, exit 4, store unchanged: the field of the refusal is **`depends_on_id`**, not `parent` (measured 2026-09-20, `error_dep_add_second_parent`) | `src/storage/sqlite.rs:12186`–`12211` | error_dep_add_second_parent |

### S4.370–S4.379 `label add` / `label remove` / `label rename`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.370 | **Positional split for `label add`/`label remove`.** When `-l/--label` is given, EVERY positional is an issue id. When it is not, there must be at least two positionals: the LAST one is always a label; the split point then walks left while the preceding positional does not parse as an id-shaped string, and never walks past index 1, so at least one leading issue always remains. `-l` is repeatable and comma-split by the parser; positionals are never comma-split and are not trimmed, while `-l` values are trimmed and blanks dropped. With fewer than two positionals and no `-l`: `Validation failed: arguments: usage: label add <issue...> <label...> or label add <issue...> -l <label>[,<label>]`, exit **4**. A positional meant as an issue that resolves to nothing gives `'<input>' is not an issue ID and does not match any issue` with hint `Labels go after all issue IDs: br label <subcommand> <issue...> <input>; or pass -l <input> to name the label explicitly.` Measured (2026-09-22, OQ-122, OQ-123): without `-l`, the FIRST positional is always an issue and the LAST always a label (`proj-b75` included, `label_add_label_looks_like_id`); walking left from the one before the last, a word is a label until the first id-shaped one, which and everything left of it are issues (`proj-mta 5u2 x7 beta` adds three labels). A word is id-shaped when its LAST `-` has a non-empty part of `a`–`z`, `0`–`9`, `_`, `.`, `-` before it and a non-empty part of `a`–`z`, `0`–`9`, `.` after it (`a-b`, `1-2`, `a-b-c`, `my_proj-abc`, `proj-mta.1` are; `Proj-mta`, `proj-MTA`, `ab-c_d`, `proj-`, `5u2` are not; `scn_label_shapes`). The issue words resolve left to right, `-l` or not; the first that resolves to nothing refuses the command: an id-shaped one with the `ISSUE_NOT_FOUND` of S4.212 (exit 3, `error_label_add_missing`), any other with `VALIDATION_FAILED`, `Validation failed: issues: '<w>' is not an issue ID and does not match any issue`, hint `Labels go after all issue IDs: br label <add|remove> <issue...> <w>; or pass -l <w> to name the label explicitly.`, the hint repeated as a third context member after `field` and `reason`, exit 4 (`error_label_remove_word_json`). A non-id-shaped word that does resolve is an issue (`5u2`, `label_add_partial_first`). The adds print label by label, each over every issue. Measured (2026-09-23, OQ-054): `label add proj-mta needs-review` takes the last positional as the label (`label_add_id_shaped`). | `label add proj-mta urgent` → issues `[proj-mta]`, labels `[urgent]` · `label add proj-mta proj-5u2 alpha beta` → issues `[proj-mta, proj-5u2]`, labels `[alpha, beta]` | `src/cli/commands/label.rs:181`–`222` (`parse_issues_and_labels`), `:418`–`430`, `src/cli/mod.rs:2337`–`2345` | label_add, label_remove, label_add_flag_two, (case to add: `label_add_many` — `["@fx=basic","label","add","proj-mta","proj-5u2","alpha","beta"]`), label_add_many, label_add_label_looks_like_id, label_add_middle_words, label_add_partial_first, error_label_add_word_before_id, error_label_add_not_an_issue, error_label_add_flag_word, error_label_add_missing, error_label_add_missing_json, error_label_remove_word_json, scn_label_shapes, r8_label_remove_walk, r9_label_add_walk_quiet, r9_error_label_not_an_issue_quiet, label_add_id_shaped, r15_label_add_upper, r15_label_add_colon, r15_label_add_dup_args, r15_label_add_dup_args_plain, r15_label_add_two_issues_one_has, r15_label_add_two_issues_one_has_json, r15_label_add_dash, r15_label_add_unicode, r15_label_add_space_trim, r15_label_add_closed, r15_label_remove_two_issues, r15_label_remove_mixed_has, r15_label_remove_quiet, r15_label_remove_precision_dup |
| S4.371 | **The command iterates LABELS in the outer loop and ISSUES in the inner loop**, so the output and the per-pair JSON rows are label-major, not issue-major. | `label add proj-mta proj-5u2 alpha beta` → `✓ Added label alpha to proj-mta\n✓ Added label alpha to proj-5u2\n✓ Added label beta to proj-mta\n✓ Added label beta to proj-5u2\n`, `last-touched` `proj-5u2\n` | `src/cli/commands/label.rs:231`–`254` | (case to add: `label_add_many`) |
| S4.372 | **Each label must be 1–50 BYTES of ASCII alphanumerics, `-`, `_` or `:`.** Every named label is validated BEFORE any storage is opened, so an invalid label anywhere aborts the whole command with zero writes and zero output: `Error: Validation failed: label: invalid characters (only alphanumeric, hyphen, underscore, colon allowed)`, exit **4**. The empty and over-length messages are `Validation failed: label: cannot be empty` and `Validation failed: label: exceeds 50 characters`. An issue may carry at most **64** labels; exceeding it gives `Validation failed: labels: exceeds 64 labels`. Measured (2026-09-22, OQ-125): the ceiling is 64 DISTINCT labels per issue, `Validation failed: labels: exceeds 64 labels`, exit 4. `label add` runs label by label and, before each label goes onto any issue, checks every named issue: one that lacks the label and already holds 64 stops the command there, and the labels added before that point STAY WRITTEN (the store is rewritten, `.beads/last-touched` is not, no success line is printed): `label add proj-5u2 -l m01,…,m65` leaves `m01`–`m64` on `proj-5u2`; a label the issue already has passes even at 64. `update` (`--add-label` repeated, `--set-labels`) and `create --labels` refuse as a whole and write nothing; `create` with exactly 64 succeeds. `update --add-label` does not split commas (`a,b` is one label, refused for its characters) (`scn_label_ceiling`, `scn_label_ceiling_update`). | `label add proj-ptp "bad label!"` → exit 4, store unchanged Amended (2026-09-25, round 20): an empty label is refused as `label: cannot be empty` (positional, `update --add-label ""`, `--remove-label ""`); `-l` values that split to no label at all are refused first, before any issue id is resolved: `Validation failed: label: at least one label required`. | `src/validation/mod.rs:428`–`448`, `src/validation/mod.rs:24` (`ISSUE_LABEL_MAX_COUNT` = 64), `src/cli/commands/label.rs:231`–`233`, `:314`–`316` | error_label_add_invalid, error_create_bad_label, scn_label_ceiling, scn_label_ceiling_update, r8_update_bad_label_status, r8_create_labels_64, scn_r8_ceiling_json, r20_l_add_three_ids_two_labels, r20_l_add_mixed_existing, r20_l_add_uppercase_l, r20_l_add_empty_l, r20_l_add_comma_only, r20_l_add_colon_space, r20_l_add_missing_mid, r20_l_add_quiet_json, r20_l_rm_three_ids, r20_l_rm_all_labels, r20_l_rm_missing_json, error_update_add_label_empty, error_update_remove_label_empty, update_set_labels_empty_plain, error_label_add_positional_empty, error_label_remove_flag_empty, label_add_flag_trailing_comma, error_label_add_empty_before_ids, r21_jr_label_rm_missing, r21_u_label_unicode, r22_e_fmt_json_label_add, r22_m_unicode_label, r22_j_validation_label |
| S4.373 | **`label add` adds the label and bumps `updated_at`.** The stored array is always ascending by label text and never holds duplicates (the labels are read back ordered, so insertion order is not observable). Adding a label to a tombstoned issue is refused: `Validation failed: issue_id: cannot add label to tombstone issue: <id>`, exit **4**. | `label add proj-ptp urgent` → `proj-ptp` gains `"labels":["urgent"]`, `updated_at` `2026-01-01T12:30:00Z` → `2026-01-02T03:04:05Z` · `label add proj-mta proj-5u2 alpha beta` → `proj-mta` `["backend","db"]` → `["alpha","backend","beta","db"]` | `src/storage/sqlite.rs:13054`–`13118`, `:13663`–`13666`, `:13850`–`13858` | label_add, label_add_json |
| S4.374 | **Adding a label the issue already carries is a success no-op:** stdout `✓ Label <label> already exists on <id>`, exit **0**, the store is NOT rewritten, `updated_at` unchanged — and `.beads/last-touched` IS written with the last processed issue's id. | `label add proj-mta db` → `✓ Label db already exists on proj-mta\n`, last-touched `proj-mta\n`, no store dump | `src/storage/sqlite.rs:13079`–`13081`, `src/cli/commands/label.rs:136`–`138`, `:252`–`254` | label_add_existing |
| S4.375 | **`label remove` drops the label and bumps `updated_at`.** Removing a label the issue does not carry is a success no-op printed on **stdout** (not stderr): `✓ Label <label> not found on <id> (no-op)` — with the ✓ glyph — exit **0**, no store rewrite, and `last-touched` written. | `label remove proj-mta db --json` → `[{"status":"removed","issue_id":"proj-mta","label":"db"}]`, labels `["backend","db"]` → `["backend"]` · `label remove proj-mta nope` → `✓ Label nope not found on proj-mta (no-op)\n`, no store dump | `src/storage/sqlite.rs:13337`–`13359`, `src/cli/commands/label.rs:140`–`142`, `:494`–`497` | label_remove, label_remove_absent |
| S4.376 | **JSON output of `label add`/`label remove` is a one-line array with one object per (issue, label) pair**, in label-major order: `[{"status":<one of `added`, `exists`, `removed`, `not_found`>,"issue_id":…,"label":…}]`. | `label add proj-ptp urgent --json` → `[{"status":"added","issue_id":"proj-ptp","label":"urgent"}]` | `src/cli/commands/label.rs:84`–`89`, `:285`–`289`, `:362`–`366` | label_add_json, label_remove, label_add_flag_two, label_add_existing_json, label_remove_absent_json (the `exists` and `not_found` rows, measured 2026-09-20) |
| S4.377 | **`label rename <old> <new>` replaces the label on every non-tombstoned issue that carries `<old>` and bumps each such issue's `updated_at` with one shared instant.** Both names are validated first (S4.372), before anything is written. An issue that already carries `<new>` as well has its `<old>` entry DELETED rather than renamed, so no duplicate appears. `.beads/last-touched` is NOT written. Plain success line: `✓ Renamed label '<old>' to '<new>' on <n> issue(s)` with the trailing `s` suppressed when `n` is 1. JSON: `{"old_name":…,"new_name":…,"affected_issues":n}`. | `label rename backend server --json` → `{"old_name":"backend","new_name":"server","affected_issues":2}`; `proj-170` `["auth","backend"]` → `["auth","server"]`, `proj-mta` `["backend","db"]` → `["db","server"]`, both `updated_at` bumped, no last-touched dump · `label rename backend db --json` → `{"old_name":"backend","new_name":"db","affected_issues":2}` and `proj-mta` → `["db"]` Amended (2026-09-25, round 20): an empty old or new name is refused as `label: cannot be empty`, exit 4, and nothing is written. | `src/cli/commands/label.rs:597`–`693`, `src/storage/sqlite.rs:13921`–`13995` | label_rename, label_rename_merge, (case to add: `error_label_rename_invalid` — `["@fx=basic","label","rename","backend","bad name!"]`), r20_l_rename_quiet_json, r20_l_rename_bad_json, r20_l_rename_empty, r20_l_rename_precision, error_label_rename_to_empty_json, error_label_rename_from_empty |
| S4.378 | **Renaming a label no issue carries is a success no-op:** stdout `Label '<old>' not found on any issues.`, JSON `{"old_name":…,"new_name":…,"affected_issues":0}`, exit **0**, store unchanged (the write-back is not even attempted on this branch). Renaming a label to itself short-circuits earlier still: `Label '<old>' already has that name; no changes made.`, `affected_issues` 0, exit **0**. | `label rename nosuch other` → `Label 'nosuch' not found on any issues.\n`, no dumps | `src/cli/commands/label.rs:641`–`665`, `:607`–`631` | label_rename_absent, label_rename_same |
| S4.379 | **`affected_issues` is the number of label rows actually renamed PLUS the number of issues whose `<old>` row was dropped because they already carried `<new>`.** The two terms never double-count. | `label rename backend server` over `proj-170` and `proj-mta` → `2` · `label rename backend db` where `proj-mta` already carries `db` → still `2` (one rename, one drop) | `src/storage/sqlite.rs:13993` | label_rename |

### S4.380–S4.389 `comments add`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.380 | **A new comment is appended to the issue's `comments` array with exactly five fields:** `id` (S4.382), `issue_id` (the resolved issue), `author` (S4.201), `text`, `created_at` (the invocation instant). The issue's `updated_at` is bumped to the same instant. | `comments add proj-mta "Looks good."` → `"comments":[{"id":2,"issue_id":"proj-mta","author":"tester","text":"Looks good.","created_at":"2026-01-02T03:04:05Z"}]` and `updated_at` `2026-01-01T10:00:00Z` → `2026-01-02T03:04:05Z` | `src/storage/sqlite.rs:20975`–`20987` (`insert_comment_row`), `src/cli/commands/comments.rs:210`–`236` | comments_add, comments_add_json, comments_add_second, r15_comments_add_unicode, r15_comments_add_spaces, r15_comments_add_spaces_plain, r15_comments_add_whitespace_only, r15_comments_add_newline, r15_comments_add_tab, r15_comments_add_long, r15_comments_add_closed, r15_comments_add_tomb, r15_comments_add_author_unicode, r15_comments_add_partial_id, r15_comments_add_ambig, r15_comments_add_dash_msg |
| S4.381 | **Text precedence, first match wins: `-f/--file` (or `-f -` for stdin), then `-m/--message` (alias `--content`), then the positional words joined with a single ASCII space.** The file's content is used verbatim, so a trailing newline survives into the stored text; both stdin and a file are capped at 10485760 bytes (`<field> input exceeds maximum size of 10485760 bytes`) and must be valid UTF-8 (`<field> input must be valid UTF-8: <err>`). The chosen text is NEVER trimmed before storage — trimming is used only for the emptiness test, which refuses with `Error: Validation failed: text: comment text cannot be empty`, exit **4**, store unchanged. When no source at all is given the message is `Validation failed: text: comment text required`. Measured (2026-09-22): the file's text is taken untrimmed (`proj-mta\n`), `-f` wins over `-m` (`comments_add_file_and_message`), and an empty file is the empty-text refusal (`error_comments_add_file_empty`). Reading the file: S4.397. | `comments add proj-mta one two three` → `"text":"one two three"` · `comments add proj-mta positional -m flagform` → `"text":"flagform"` · `comments add proj-mta ""` → exit 4 | `src/cli/commands/comments.rs:431`–`447` (`read_comment_text`), `:411`–`429`, `:229`–`234` | comments_add, comments_add_author, error_comments_add_empty, comments_add_words, error_comments_add_no_text, comments_add_file, comments_add_file_json, comments_add_file_and_message, error_comments_add_file_empty, r8_comments_file_author, r9_comments_file_quiet, stdin_comments_add, stdin_comments_add_empty |
| S4.382 | **The comment's numeric `id` is one more than the LARGEST comment id anywhere in the store, across all issues** — not per issue. It comes from the auto-increment row id of the comments table into which the whole JSONL was imported with each comment's own id preserved. A store whose only comment has id `1` therefore produces `2` for a comment added to any issue, including a different one. An empty store produces `1`. Measured (2026-09-23, OQ-061): in a store with no comments the first id is 1 (`scn_comments_first`). | fixture `basic` (only comment is `proj-ptp`'s id `1`): `comments add proj-mta …` → `"id":2` · `comments add proj-ptp …` → `"id":2` · `reopen proj-mkh --reason Regressed` → its comment gets `"id":2` | `src/storage/sqlite.rs:20975`–`20987` (`INSERT … ; last_insert_rowid()`) | comments_add, comments_add_second, comments_add_author, actor_flag_comment, reopen_closed_json, scn_comments_first |
| S4.383 | **Output shapes.** Plain: `Comment added to <id>`. JSON: the comment object as one compact line, `{"id","issue_id","author","text","created_at"}`. | `comments add proj-mta "Looks good."` → `Comment added to proj-mta\n` · `--json` → `{"id":2,"issue_id":"proj-mta","author":"tester","text":"Looks good.","created_at":"2026-01-02T03:04:05Z"}` | `src/cli/commands/comments.rs:108`–`120` | comments_add, comments_add_json |
| S4.384 | **`comments add` writes `.beads/last-touched` with the issue's id**, and writes back once. An unknown issue fails per S4.212 before anything is written. | `comments add proj-mta "Looks good."` → last-touched `proj-mta\n` · `comments add proj-zzz hi` → exit 3, nothing written | `src/cli/commands/comments.rs:108`–`120` | comments_add, error_comments_add_missing |
| S4.385 | **`--author` overrides the author for this comment only;** it does not change `created_by`, the actor, or anything else in the record. | `comments add proj-mta --author zoe -m "Flag form" --json` → `"author":"zoe"` while the issue's `created_by` stays `tester` | `src/cli/commands/comments.rs:449`–`458` | comments_add_author, actor_flag_comment |

### S4.390–S4.399 `epic close-eligible`

| S4.n | clause | examples (input → output) | provenance | cases |
|---|---|---|---|---|
| S4.390 | **An epic is "eligible" when it is a non-closed issue of type `epic` that has at least one child and every child is closed.** Candidate epics are the non-closed issues of type `epic`; deferred epics are candidates, closed ones are not. Children are the issues holding a `parent-child` edge whose `depends_on_id` is the epic (the child depends on the epic); template issues are excluded, and a dot-notation child with no such edge does NOT count here (that separate rule belongs to `close`, S4.292). A child counts as closed when its status is `closed` **or** `tombstone`; every other status counts as open. **An epic with zero children is never eligible.** | fixture `basic`: `proj-uly` has one child (`proj-170`, open) → not eligible; after `close proj-170` → `epic status` reports `Progress: 1/1 children closed (100%)` and `   Eligible for closure` | `src/cli/commands/epic.rs:260`–`288`, `src/storage/sqlite.rs:12318`–`12346` | epic_status, epic_status_json, scn_lifecycle, scn_child_ids |
| S4.391 | **`epic close-eligible` closes every eligible epic in ONE atomic batch with a single shared instant**, setting `status` `closed`, `closed_at` = that instant, `updated_at` = that instant and **`close_reason` to the hard-coded literal `All children completed`** — NOT `done`, and `--reason` does not exist on this subcommand. `--transition-comment <text>` adds a comment to each closed epic. The batch is all-or-nothing: a refusal on one epic rolls back every one of them. Plain output: `✓ Closed <n> epic(s)` then `  - <id>` per epic. JSON: `{"closed":[<ids>],"count":<n>}` with a `warnings` array only when capacity warnings were produced. | after `close proj-mta` and `close proj-170` on fixture `basic`, `epic close-eligible --json` → `{"closed":["proj-uly"],"count":1}` and `proj-uly` comes back `"status":"closed","close_reason":"All children completed"` with `closed_at` = `updated_at` = the invocation instant; the plain form of the same run prints `✓ Closed 1 epic(s)\n  - proj-uly\n`; neither writes `.beads/last-touched` (measured 2026-09-20) | `src/cli/commands/epic.rs:121`–`226`, `:181`–`182`, `:217`–`220` | scn_epic_close_eligible, scn_epic_close_eligible_plain |
| S4.392 | **With nothing eligible it prints `No epics eligible for closure`, exits 0 and writes nothing** (the write-back is not reached on that branch); JSON is `{"closed":[],"count":0}`. There is no `NOTHING_TO_DO` on this path. | `@fx=basic epic close-eligible` → `No epics eligible for closure\n`, exit 0, no dumps · `--json` → `{"closed":[],"count":0}\n` (measured 2026-09-20) | `src/cli/commands/epic.rs:176`–`178` | epic_close_eligible_none, epic_close_eligible_none_json |
| S4.393 | **`--dry-run` returns before any mutation and writes nothing, including `.beads/last-touched`.** Plain output is `Would close <n> epic(s):` then `  - <id>: <title>` per epic — with nothing eligible it prints `Would close 0 epic(s):` and no lines, NOT the "no epics" message of S4.392. In JSON mode the dry run serializes the epic-status list itself, not the close result. | `@fx=basic epic close-eligible --dry-run` → `Would close 0 epic(s):\n`, exit 0, no dumps · with one eligible epic → `Would close 1 epic(s):\n  - proj-uly: Auth epic\n` · `--dry-run --json` with nothing eligible → `[]\n`, i.e. the epic-status list of the ELIGIBLE epics alone, not of every epic (measured 2026-09-20) | `src/cli/commands/epic.rs:136`–`154`, `src/cli/mod.rs:2020`–`2022` | epic_close_eligible_dry_run, epic_close_eligible_dry_run_json, scn_epic_close_eligible |
| S4.394 | **`dep tree --format`.** The value is free text, never refused: `mermaid` in any ASCII letter case (`MERMAID` included) selects the mermaid rendering of S5.253; every other value (`bogus`, `json`, `toon`, `text`) renders the text tree of S5.179 unchanged. `--json` wins over `--format mermaid` and prints the node array of S5.47 byte for byte as without it, and `--quiet` prints nothing. The walk, its order and its depth limit are those of S4.182–S4.185 whatever the format | — | `goldens/dep_tree_mermaid_upper.out:1`, `goldens/dep_tree_format_other.out:1`, `goldens/dep_tree_mermaid_json.out:1`, `goldens/dep_tree_mermaid_quiet.out` (empty) | dep_tree_mermaid, dep_tree_mermaid_upper, dep_tree_format_other, dep_tree_mermaid_json, dep_tree_mermaid_quiet |
| S4.395 | **`update --append-notes <text>`** extends the notes instead of replacing them: onto empty notes the text alone; onto notes that already end in a newline the text directly; otherwise one `\n` and the text. The edit always counts as a change (`updated_at` bumps, the store is rewritten), and it is applied after `--notes`, which the parser refuses together with it (`the argument '--notes <NOTES>' cannot be used with '--append-notes <TEXT>'`, exit 2). An empty text is refused: `Validation failed: append-notes: text to append must not be empty`, `VALIDATION_FAILED`, exit 4, retryable, with `field`/`reason` context. The plain block gains, after the owner line of S4.273, `  notes: appended <n> chars (now <l> line[s], <c> chars)` — `chars` counts code points and is never singular, `line` is singular for one, lines counted as `str::lines` counts them (a trailing newline does not open a line). The JSON element gains, after `updated_at`, `"notes":{"appended":<text>,"applied":true,"chars_before":<n>,"chars_after":<n>,"text":<new notes>}`. Several ids each get the text appended and their own block; `--quiet` prints nothing | — Amended (2026-09-25, round 19): `--append-notes` is repeatable: the values are joined by a line feed and appended as one text. | `goldens/update_append_notes.out:1-2`, `goldens/update_append_notes_json.out:1`, `goldens/update_append_notes_unicode.out:2`, `goldens/update_append_notes_combined.out:1-4`, `goldens/update_append_notes_two.out`, `goldens/error_update_append_empty_json.out`, `goldens/usage_update_notes_append.err:1`, `goldens/scn_append_notes.out` | update_append_notes, update_append_notes_json, update_append_notes_unicode, update_append_notes_combined, update_append_notes_two, error_update_append_empty, error_update_append_empty_json, usage_update_notes_append, scn_append_notes, r19_update_append_twice, update_append_three |
| S4.396 | **`--transition-comment <text>` adds one comment to every issue a transition acted on; `close --session <s>` sets `closed_by_session`.** `close`, `defer` and `undefer` append a comment by the actor (`--actor`, else `USER`) at the invocation instant to each id they close, defer or undefer, and none to a skipped id; the ids continue one past the store-wide greatest (S4.382), in command-line order. The plain and JSON outputs are unchanged. `update --transition-comment` requires `--status` to move the issue to a different status (any value, `bogus` included): without `--status`, `Validation failed: status: issue <id>: transition comments and workflow-policy bypasses require a real status transition`; to the status it already has, `… cannot be attached to a same-status update`; exit 4, nothing written. `update --session` is refused after id resolution and the priority check with `Validation failed: session: --session can only be used when closing with --status closed`, exit 4. `reopen` takes no `--transition-comment` (a usage error). Measured (2026-09-22): `epic close-eligible --transition-comment` gives every epic it closes the comment the same way (author the actor, ids continuing store-wide); a dry run and a run with nothing eligible print and write exactly as without it (`scn_epic_close_comment`). | `close proj-b75 proj-5u2 proj-mkh --transition-comment tc --session s7 --actor ann --json` → comments 2 and 3 by `ann` on `proj-b75` and `proj-5u2`, both `closed_by_session` `s7`, `proj-mkh` skipped, exit 3 | `goldens/close_transition_comment_batch.out`, `goldens/update_transition_comment.out`, `goldens/error_update_transition_no_status.err:1`, `goldens/error_update_transition_same_status.err:1`, `goldens/error_update_session.err:1` (measured 2026-09-22) | close_transition_comment, close_transition_comment_batch, close_session, defer_transition_comment, undefer_transition_comment, update_transition_comment, error_update_transition_no_status, error_update_transition_same_status, error_update_session, error_update_session_json, scn_epic_close_comment, r8_update_label_comment_json, r8_close_session_skip_json, r8_close_force_comment, r8_defer_until_comment_json, scn_r8_epic_json, r9_error_update_transition_same_json, r9_error_update_session_quiet |
| S4.397 | **Files the argv names are read whole, as UTF-8, relative to the working directory, and their text is used as it is (no trimming: a file `proj-mta\n` gives a comment or description `proj-mta\n`).** The four are `delete --from-file` (S4.340), `comments add -f/--file` (S4.381; `-` is standard input), `update --description-file` and `create --description-file`. A file that cannot be read fails with the words of Rust's `io::Error`, `<strerror> (os error <n>)` (`No such file or directory (os error 2)`; a directory opens and then fails to read, `Is a directory (os error 21)`): for `delete` and `comments add` as `IO_ERROR`, `I/O error: <words>`, hint and context `null`, exit **8**; for `--description-file` as a validation failure `description_file: failed to read description file '<path>': <words>`, exit 4. `update` reads its file BEFORE resolving any id (`error_update_description_file_missing_id` reports the file, not the unknown id); `comments add` resolves its issue first (`error_comments_add_file_missing_id` reports `Issue not found`). The text of `--description-file` meets the accumulated-context guard of S4.266 exactly as a `--description` value would (`error_update_description_file_guard`). Measured (2026-09-23): `-` names standard input for `comments add -f -` and `--description-file -` (read to its end, untrimmed; empty input is the empty-text refusal), but NOT for `delete --from-file -`, which opens a file named `-` (`stdin_delete_from_file_dash`: `IO_ERROR`, exit 8). | `comments add proj-b75 -f nope.txt` → `Error: I/O error: No such file or directory (os error 2)`, exit 8 | `goldens/error_delete_from_file_missing.err:1`, `goldens/error_delete_from_file_dir.err:1`, `goldens/error_update_description_file_missing_json.out`, `goldens/comments_add_file.out` (measured 2026-09-22) | error_delete_from_file_missing, error_delete_from_file_missing_json, error_delete_from_file_dir, error_comments_add_file_missing, error_comments_add_file_missing_id, update_description_file, error_update_description_file_guard, error_update_description_file_missing_json, error_update_description_file_missing_id, create_description_file, error_create_description_file_missing, r8_update_description_file_json, r9_error_description_file_quiet, r9_error_delete_from_file_quiet, stdin_update_description, stdin_create_description, stdin_delete_from_file_dash |
| S4.398 | **`update --add-acceptance <TEXT>` (repeatable), `--check-acceptance <ITEMS>` and `--uncheck-acceptance <ITEMS>` (both repeatable) edit the checklist inside `acceptance_criteria` without rewriting the rest.** An ITEM is a line that, after leading whitespace, is `- ` or `* `, then `[ ]`, `[x]` or `[X]`, then a space and the text (`- [] Delta` and `1. [ ] Epsilon` are not items); items are numbered from 1 in line order and every other line is kept byte for byte. A selector is a comma list of 1-based numbers when every piece is digits (a repeat counts once), otherwise a text: case-insensitive, an exact match wins, else it must be a unique substring (`2,x` is a text). Every selector is judged against the items as they were BEFORE this run's additions, and every one is judged before anything is written. Refusals, exit 4, nothing written, the field named after the flag (`check-acceptance` or `uncheck-acceptance`): `<id>: acceptance item <n> does not exist (<m> items present)`; `<id>: acceptance item numbers are 1-based; 0 is not an item`; `<id>: no acceptance item matches "<sel>"; items are:` then every item line; `<id>: "<sel>" matches <k> acceptance items; use the item number instead:` then the matching item lines; and, with no item at all, always under the field `check-acceptance`: `<id>: acceptance_criteria has no checklist items to tick; append items first (--add-acceptance / add_acceptance) or set the whole field (--acceptance-criteria)`. A check writes `[x]` over `[ ]` and leaves `[x]`/`[X]` as they are; an uncheck writes `[ ]`; an addition appends `- [ ] <TEXT>`: alone onto empty criteria, after a `\n` onto text that does not end in one, and followed by `\n` onto text that does (`ends\n` + `B` → `ends\n- [ ] B\n`). A run that changes no byte writes no store. Plain output, after the `Updated` block: `<id> acceptance criteria (<n> item[s])`, one line `  [x] <i>  <text>` or `  [ ] <i>  <text>` per item after the run, then one summary line joined by ` · `: `checked <k> (<i,j>)`, `unchecked <k> (…)`, `added <k> (…)` for the flags given, then `<c> of <n> complete` and `<r> remaining`. JSON: the element of S5.39 gains `"acceptance_criteria":{"items":[{"index","text","checked"}…],"checked_count","total","remaining","checked":[…],"unchecked":[…],"added":[…]}`. `show --json` gains `"acceptance_items":[{"index","text","checked"}…]` as its LAST member when the criteria hold at least one item; `list --json` never does. `--acceptance-criteria` together with any of the three flags is a usage error. | `update proj-b75 --add-acceptance First --add-acceptance Second` → `proj-b75 acceptance criteria (2 items)` / `  [ ] 1  First` / `  [ ] 2  Second` / `added 2 (1,2) · 0 of 2 complete · 2 remaining` Amended (2026-09-25, round 17): an item number 0 is refused before the no-items check, `--check-acceptance` first: `Validation failed: <flag>: <id>: acceptance item numbers are 1-based; 0 is not an item`. | `goldens/update_add_acceptance.out:2-4`, `goldens/scn_acceptance_list.out`, `goldens/scn_acceptance_text.out`, `goldens/scn_acceptance_prose.out`, `goldens/scn_acceptance_select.out` (measured 2026-09-22) | update_add_acceptance, update_add_acceptance_json, error_update_check_acceptance_none, error_update_check_acceptance_none_json, scn_acceptance_list, scn_acceptance_text, scn_acceptance_prose, scn_acceptance_select, r8_update_acceptance_comment, r9_acceptance_quiet, r17_upd_uncheck_0, r17_uncheck0_with_items, r17_check0 |
| S4.399 | **`close --bypass-policy` and `--bypass-reason <REASON>` come as a pair, judged before anything else** (before the ids resolve and before the last-touched fallback): `--bypass-policy` without a reason, or with an empty or all-whitespace one, fails `Validation failed: bypass-reason: --bypass-policy requires --bypass-reason "<text>"`; `--bypass-reason` without the flag fails `Validation failed: bypass-policy: --bypass-reason was set without --bypass-policy`; exit 4, nothing written. With no close policy configured, a valid pair closes exactly as a plain `close` does: no `bypassed_policy`, `bypass_reason` or `policy_gates_fired` member is written, and the output is the same (`close_bypass`); skips and their refusals are unchanged. | `close proj-zzz --bypass-policy` → the bypass-reason refusal, not `Issue not found` | `goldens/error_close_bypass_no_reason.err:1`, `goldens/error_close_bypass_reason_alone.err:1`, `goldens/close_bypass.out:1` (measured 2026-09-22) | close_bypass, error_close_bypass_no_reason, error_close_bypass_no_reason_json, error_close_bypass_blank, error_close_bypass_reason_alone, error_close_bypass_no_ids, r9_bypass_quiet, r9_error_bypass_reason_alone_json |

---


## S5. Outputs

<!-- from docs/spec-parts/S2_S5a.md -->

### S5.A `.beads/issues.jsonl` as it is written back

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.1 | A mutation rewrites the **whole** store, never part of it: every record that survives S5.16 is re-serialized from the loaded model, so records the command never named are normalized too. A command that changes nothing leaves the file byte-identical and the golden then carries no `--- .beads/issues.jsonl ---` dump | `src/config/mod.rs:4914`, `src/sync/mod.rs:11098` | edge_precision_rewrite, update_status_json, close_basic, update_noop_same_value |
| S5.2 | One record per line. Each line is the compact serialization of the record — no space after `:` or `,`, no indentation, no trailing space — followed by one `\n`. The last record also ends with `\n`, so a non-empty store ends with exactly one newline byte and never with a blank line. An export of zero records produces a zero-byte file | `src/sync/mod.rs:10795`, `:10868`, `:12963` | create_min, create_title_flag, create_unicode, edge_precision_rewrite, delete_cascade |
| S5.3 | Lines are ordered by `id` ascending in **byte** order — the query is `ORDER BY id ASC` over TEXT under SQLite's default binary collation, and the Rust fallback comparator used for large stores is `String::cmp`, which agrees. Digits therefore sort before lower-case letters: `proj-170`, `proj-5u2`, `proj-7vm`, `proj-b75`, `proj-bsx`, `proj-mkh`, `proj-mta`, `proj-ptp`, `proj-uly`. No tie-break exists or is needed, because a duplicate id was already refused by S2.40 | `src/storage/sqlite.rs:15006`, `src/sync/mod.rs:10613`, `:10636` | create_full_json, update_status_json, close_two, delete_cascade, edge_precision_rewrite |
| S5.4 | The members of an issue record appear in exactly this fixed order and no other, regardless of the order they had in the file that was read: `id`, `title`, `description`, `design`, `acceptance_criteria`, `prerequisites`, `notes`, `status`, `priority`, `issue_type`, `assignee`, `owner`, `estimated_minutes`, `created_at`, `created_by`, `updated_at`, `closed_at`, `close_reason`, `closed_by_session`, `bypassed_policy`, `bypass_reason`, `policy_gates_fired`, `due_at`, `defer_until`, `external_ref`, `source_system`, `source_repo`, `source_repo_path`, `agent_context`, `deleted_at`, `deleted_by`, `delete_reason`, `original_type`, `compaction_level`, `compacted_at`, `compacted_at_commit`, `original_size`, `sender`, `ephemeral`, `pinned`, `is_template`, `labels`, `dependencies`, `comments` | `src/model/mod.rs:462`–`:650` | create_full_json, delete_cascade, show_deferred_json, edge_precision_rewrite, create_ephemeral |
| S5.5 | Always present in every written record without exception: `id`, `title`, `status`, `priority`, `issue_type`, `created_at`, `updated_at`, `compaction_level`, `source_repo`, `original_size` | `src/model/mod.rs:462`, `:471`, `:495`, `:499`, `:503`, `:519`, `:526`, `:623`, `src/storage/sqlite.rs:19468`, `:19489` | create_min, edge_precision_rewrite, q_basic |
| S5.6 | `compaction_level` is written as a bare integer even when it was never set, because it is the one member with a hand-written serializer that emits `0` for "not set". A record that carried no `compaction_level` comes back with `"compaction_level":0` | `src/model/mod.rs:26`, `:623` | edge_precision_rewrite, create_min, create_dry_run_json |
| S5.7 | `source_repo` and `original_size` become non-empty by a different route: the record passes through the private in-memory database, whose insert binds `source_repo` as `"."` and `original_size` as `0` when the loaded record had neither. A record that carried neither therefore comes back with `"source_repo":"."` and `"original_size":0`, while a record minted by `br create` carries the workspace name, `"source_repo":"proj"`, and `"source_repo_path":"/mnt/proj"` | `src/storage/sqlite.rs:19468`, `:19489`, `src/storage/schema.rs:267` | edge_precision_rewrite, create_min, q_basic |
| S5.8 | Every other member is omitted when it is not set; `ephemeral`, `pinned` and `is_template` are omitted when false and written as `true` otherwise; `labels`, `dependencies` and `comments` are omitted when empty. `content_hash` is never written. `bypassed_policy`, `bypass_reason` and `policy_gates_fired` are sourced from a database-only table that the in-scope commands never populate, so they never appear either | `src/model/mod.rs:467`, `:545`, `:635`, `:645`, `src/storage/sqlite.rs:15960` | create_min, create_ephemeral, show_deferred_json, edge_create_max_title |
| S5.9 | An optional string member whose loaded value is `""` survives a write-back only for `compacted_at_commit`. Every other optional string is read back out of the private database through an accessor that maps `""` to "not set" (`description`, `design`, `acceptance_criteria`, `prerequisites`, `notes`, `assignee`, `owner`, `created_by`, `close_reason`, `closed_by_session`, `source_system`, `source_repo`, `source_repo_path`, `agent_context`, `deleted_by`, `delete_reason`, `original_type`, `sender`), and `external_ref` loses its `""` even earlier, to the trim of S2.36. This is why the `precision` fixture's `"description":""` and `"assignee":""` vanish from the rewritten record while `"compacted_at_commit":""` is kept. [inference] from the accessor table, confirmed by the golden; resolves OQ-007 Corrected (2026-09-23, OQ-018): `""` in `agent_context`, `bypass_reason`, `source_repo` or `source_repo_path` never reaches a write-back: the import refuses it (S2.57). The rule here holds for the other members. | `src/storage/sqlite.rs:15945`, `:15970`, `:15977`, `:19430`, `:19488` | edge_precision_rewrite, edge_precision_show_json, edge_empty_optionals_list |
| S5.10 | String values are escaped by the `serde_json` rules and no others: `"` becomes `\"`, `\` becomes `\\`, and the control characters below U+0020 become `\b`, `\t`, `\n`, `\f`, `\r` where those short forms exist and `\u00XX` with four lower-case hex digits otherwise. Nothing else is escaped: U+007F (DEL) is written as the raw byte `7F`, U+00A0 as the raw bytes `C2 A0`, every other non-ASCII scalar as raw UTF-8 — `日本語のタイトル 🚀` occupies its own 25 bytes — and `/`, `<`, `>` and `&` are never escaped. There is no post-processing of the serialized text at all | `src/sync/mod.rs:10781`, `:10862`; the byte dump of `goldens/create_unicode.out` | create_unicode, show_unicode_json, list_json, search_unicode, edge_create_max_title |
| S5.11 | Numbers are written as bare JSON integers with no sign, no leading zero and no exponent: `priority`, `estimated_minutes`, `compaction_level` and `original_size` are 32-bit signed, a comment's `id` is 64-bit signed. No floating-point value ever reaches the store | `src/model/mod.rs:152`, `:516`, `:938` | create_full_json, comments_add_json, show_comments_json |
| S5.12 | Every timestamp is written as RFC 3339 with the literal `Z` zone, never `+00:00`, and with 0, 3, 6 or 9 fraction digits — the fewest of those four that represent the instant exactly: `…:05Z` for a whole second, `…:05.100Z` for one hundred milliseconds, `…:05.000000001Z` for one nanosecond. A source spelling of `…05.1Z` is therefore rewritten `…05.100Z`, and `12:00:04+02:00` is rewritten `10:00:04Z` Measured (2026-09-23, OQ-020): 3, 6 or 9 fraction digits, the fewest that hold the value: `.000001Z`, `.123456Z`, `.000000001Z` kept; `.1Z` and `.100000000Z` written `.100Z`. | `src/model/mod.rs:519` (chrono `DateTime<Utc>` serde) | edge_precision_rewrite, create_min, show_deferred_json, scn_last_touched, edge_precision_micros, edge_precision_micros_rewrite |
| S5.13 | `labels` inside a record is written sorted ascending by byte order and de-duplicated: the `precision` fixture's `["zeta","alpha","alpha"]` is rewritten `["alpha","zeta"]` | `src/sync/mod.rs:12294`, `src/storage/sqlite.rs:13857` | edge_precision_rewrite, list_json, update_set_labels, update_add_label, update_remove_label |
| S5.14 | `dependencies` inside a record is written ordered by `issue_id`, then `depends_on_id`, then the dependency type's text, then `created_at`, then `created_by` ("not set" first), then `metadata`, then `thread_id`, all ascending; the sort is stable and nothing is de-duplicated at this point. Each dependency object's members appear in the fixed order `issue_id`, `depends_on_id`, `type`, `created_at`, `created_by`, `metadata`, `thread_id`, the first four always present and the last three omitted when not set. In practice the last three are always present after a write-back, because the insert binds a loaded dependency's missing `created_by` as `import`, its missing `metadata` as `{}` and its missing `thread_id` as the empty string, while a dependency minted by `dep add` or `create --deps` binds the acting actor, `{}` and the empty string Measured (2026-09-23, OQ-026): a dependency with four members comes back with `"created_by":"import"`, `"metadata":"{}"`, `"thread_id":""`. | `src/sync/mod.rs:12299`, `src/storage/sqlite.rs:19963`, `:6875`, `src/model/mod.rs:896`–`:928` | update_status_json, create_with_deps, create_with_parent, dep_add_json, dep_remove_basic, dep_add_related, dep_add_blocks, edge_sparse_dependency_rewrite |
| S5.15 | `comments` inside a record is written ordered by `issue_id`, then `created_at`, then `author`, then body text, then `id`, all ascending, with a stable sort. Each comment object's members appear in the fixed order `id`, `issue_id`, `author`, `text`, `created_at`, all five always present; note the member is spelled `text` while the model calls it a body | `src/sync/mod.rs:12312`, `src/storage/sqlite.rs:15200`, `src/model/mod.rs:933`–`:943` | comments_add_json, comments_add_second, show_comments_json, comments_add, comments_add_author |
| S5.16 | Three populations are excluded from the written file: a record with `ephemeral` true, a record whose `id` contains the substring `-wisp-`, and a tombstone past its retention window. The `--no-db` flush builds its export configuration with every default, and the default retention is "keep forever", so the third exclusion never fires on this path and tombstones are always written — a `delete` leaves `"status":"tombstone"` lines behind rather than removing lines. `br create --ephemeral` therefore leaves the store untouched, and with an empty workspace the file is not even created, while still writing the sidecar of S5.17 | `src/sync/mod.rs:12468`, `:10617`, `:10857`, `src/config/mod.rs:4928`, `src/model/mod.rs:848` | create_ephemeral, delete_cascade, delete_json |
| S5.17 | `.beads/last-touched` holds one issue id followed by one `\n` and nothing else. It is written with the file created if needed and truncated first, and on Unix with mode `0600`; every failure is swallowed. `proj-mta` is stored as the nine bytes `proj-mta\n` | `src/util/mod.rs:82`, `:94`, `:104` | create_min, update_status_json, scn_last_touched, update_last_touched |
| S5.18 | The sidecar is written by `create`, `q`, `update`, `close`, `reopen`, `defer`, `undefer`, `dep add`, `dep remove`, `label add`, `label remove` and `comments add`, naming the **last** issue the command acted on. It is written even when the command changed nothing: a duplicate `dep add`, a `label add` of an existing label, a `label remove` of an absent label, a `dep remove` of an absent edge and an `update` to the value already held all leave `issues.jsonl` untouched and still write it. It is *not* written by `delete`, which instead removes the file when it points at a deleted issue, nor by `label rename`. A command that refuses outright — `close` on an already-closed issue, `reopen` on an open issue, `undefer` on an undeferred issue — writes neither file. Under `--no-db` the sidecar write is deliberately ordered **after** the store flush succeeds, so a failed flush leaves no stale pointer | `src/cli/commands/create.rs:160`, `q.rs:216`, `update.rs:525`, `close.rs:747`, `reopen.rs:140`, `defer.rs:159`, `:465`, `dep.rs:595`, `:766`, `label.rs:253`, `:334`, `comments.rs:126`, `delete.rs:835` | update_noop_same_value, dep_add_duplicate, label_add_existing, label_remove_absent, dep_remove_absent, close_already_closed, reopen_open, undefer_not_deferred, label_rename, delete_json, defer_no_until |
| S5.19 | `br q` writes a record with `"source_repo":"."` and no `source_repo_path`, while `br create` writes `"source_repo":"proj"` and `"source_repo_path":"/mnt/proj"` in the same workspace: `q` never sets the two members, so the column defaults of S5.7 apply. Reproduce, do not repair (handover: S10) | `src/cli/commands/q.rs:216`, `src/storage/sqlite.rs:19468` | q_basic, q_priority, create_min |

### S5.B The `--json` envelope: rules common to every command

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.20 | A successful `--json` payload is written to **stdout** in compact form — no spaces, no indentation — followed by exactly one `\n`, and nothing else reaches stdout. Diagnostics stay on stderr, which every successful golden shows empty | `src/output/context.rs:1039`, `:220` | list_json, show_json, ready_json, count_json, where_json, stats_json, blocked_json, search_json |
| S5.21 | A failing `--json` command writes the error envelope to **stdout** instead, and it is the one payload that is **pretty-printed**: two-space indentation per level, `": "` after each key, and one `\n` at the end. Its shape is fixed at five members in this order — `code` (a string), `message` (a string), `hint` (a string or `null`), `retryable` (a boolean), `context` (an object or `null`) — all five always present, wrapped in a single `error` member. An array inside `context` is printed one element per line at the next indentation level | `src/main.rs:1875`, `:1885`, `src/error/structured.rs:593` | error_no_workspace_list_json, error_create_bad_priority_json, error_show_missing_json, error_show_ambiguous_json, error_conflict_markers_json, error_malformed_jsonl_json, error_dep_add_cycle_json, dep_add_custom_type, update_status_closed |
| S5.22 | A command that produced output and *then* failed writes both documents to stdout in that order: the compact success payload with its newline, then the pretty envelope. `close` on a blocked issue writes `{"closed":[],"skipped":[…]}` and then the `NOTHING_TO_DO` envelope, exit code 3 | `src/cli/commands/close.rs:437`, `src/main.rs:1875` | error_close_blocked_json |
| S5.23 | `--quiet` suppresses the payload entirely: stdout is empty and the exit code is unchanged | `src/output/context.rs:1039` | list_quiet |
| S5.24 | `--fields` has no effect on a `--json` payload; it selects columns for CSV output only, so `list --json --fields id,title,status,priority` is byte-identical to `list --json` | `src/cli/commands/list.rs:258`, `:364` | list_fields, list_json |
| S5.25 | Repeating `--json` is accepted and changes nothing Resolved (2026-09-23, OQ-098): a repeated `--json` is refused by the parser, exit 2 (`usage_json_twice`), as S1.12 says. | `src/cli/mod.rs:1` (a clap flag) | usage_json_twice |

### S5.C The `--json` shape of each in-scope command

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.30 | `show --json` is an **array**, one element per requested id, in the order the ids were given. Each element is the issue's own members in the order of S5.4, then, appended in this order and each omitted when empty: `labels` (a string array), `dependencies`, `dependents`, `comments`, `events`, `parent` (a string), `rollup`, `acceptance_items`, `inherited_context`. The issue's own `labels`, `dependencies` and `comments` are cleared before serialization, so each of those names appears at most once. Under `--no-db`, `show` reads `.beads/issues.jsonl` **directly** rather than the loaded model, so the element carries the record exactly as the file spells it — unsorted and duplicated labels, `""` optionals, and no `source_repo` or `original_size` when the file had none — which is why `show --json` and `list --json` disagree about the same issue | `src/format/output.rs:165`, `src/cli/commands/show.rs:506`, `:893` | show_json, show_comments_json, show_deferred_json, show_unicode_json, edge_precision_show_json, show_two, show_partial_hash, show_epic, show_closed, show_plain, show_comments, show_unicode |
| S5.31 | An element of `dependencies` or `dependents` is an object of exactly five members in this order: `id`, `title`, `status`, `priority`, `dependency_type`. Both lists are ordered by priority ascending, then `created_at` descending, then id ascending. `parent` is the `depends_on_id` of the **last** `parent-child` edge in the record's own dependency array | `src/format/output.rs:224`, `src/cli/commands/show.rs:876`, `:905` | show_json, show_epic, show_two |
| S5.32 | `list --json` is a five-member envelope around an array: `{"issues":[…],"total":<n>,"limit":<n>,"offset":<n>,"has_more":<bool>}`, all five always present, always in that order, emitted by a hand-rolled streaming writer rather than a struct. `total` is the full filtered count before paging; `limit` is `0` when `--limit` was not given, meaning unlimited; `has_more` is true only when a non-zero limit left rows behind | `src/output/context.rs:166`, `:1072`, `src/format/output.rs:136` | list_json, list_all_json, empty_list_json, list_limit_one, list_limit_offset, list_status_open, list_status_closed, list_type_bug, list_priority_one, list_priority_min_max, list_assignee, list_label, list_id_filter, list_json_again, edge_precision_list_json |
| S5.33 | An element of `list --json`'s `issues` array is the issue's own members in the order of S5.4 followed by `dependency_count` and `dependent_count`, both always present, both non-negative integers. Unlike `show`, these come from the loaded model, so labels are sorted and de-duplicated and the defaults of S5.7 are already applied | `src/format/output.rs:129` | list_json, edge_precision_list_json, list_label, search_json |
| S5.34 | `search --json` is a five-member envelope with a different second member: `{"issues":[…],"hidden_closed_count":<n>,"limit":<n>,"offset":<n>,"has_more":<bool>}` — there is **no** `total`. `limit` defaults to `50`. The elements have the shape of S5.33 | `src/cli/commands/search.rs:345`, `:232` | search_json, search_plain, search_none, search_status, search_description, search_case_insensitive |
| S5.35 | `blocked --json` uses the same five-key envelope as S5.32 with `limit` defaulting to `50`, but its elements are a different record: exactly `blocked_by` (a string array), `blocked_by_count`, `created_at`, `created_by`, `description`, `id`, `issue_type`, `priority`, `status`, `title`, `updated_at`, in that order, with `created_by` and `description` omitted when not set and the rest always present. There is no `source_repo`, no `compaction_level` and no `labels` | `src/format/output.rs:82`, `src/cli/commands/blocked.rs:278` | blocked_json, empty_blocked_json, blocked_plain |
| S5.36 | `ready --json` is a **bare array**, with no envelope and no paging members. Each element is exactly `acceptance_criteria`, `prerequisites`, `assignee`, `created_at`, `created_by`, `description`, `estimated_minutes`, `id`, `issue_type`, `labels`, `notes`, `owner`, `priority`, `status`, `title`, `updated_at`, in that order; `created_at`, `id`, `issue_type`, `priority`, `status`, `title`, `updated_at` and **`labels`** are always present, `labels` as `[]` when empty, and the rest are omitted when not set. An empty result is the two bytes `[]` and a newline | `src/format/output.rs:24`, `src/cli/commands/ready.rs:203` | ready_json, empty_ready_json, ready_include_deferred, ready_label, scn_lifecycle, ready_plain, ready_limit, ready_type, ready_unassigned, ready_sort_priority, ready_sort_oldest, edge_precision_ready |
| S5.37 | `create --json` prints the created issue as a single object in the shape of S5.4, re-read from the store after the write, so it carries `original_size` and any dependencies that `--parent` or `--deps` minted. `create --dry-run --json` prints the *unstored* object instead, which is why it ends `"compaction_level":0}` with **no** `original_size` | `src/cli/commands/create.rs:184`, `:294` | create_min_json, create_full_json, create_dry_run_json, create_with_parent, create_with_deps, create_status_in_progress, create_priority_p_form, create_custom_type, create_unicode, create_ephemeral, edge_create_max_title, actor_flag_create, scn_child_ids, scn_dup_title |
| S5.38 | `create --silent` prints the new id and a newline and nothing else, in every output mode, unaffected by `--json`. `br q` prints the new id the same way | `src/cli/commands/create.rs:170` | create_silent, q_basic, q_priority |
| S5.39 | `update --json` is an array, one element per updated id in the order given. Each element is exactly `id`, `title`, `status`, `priority`, `assignee`, `owner`, `updated_at`, then `acceptance_criteria` and `notes` when the call used the acceptance or append-notes flags. `assignee` and `owner` are echoed as JSON `null` when unset — the only members of any in-scope payload emitted as an explicit `null` rather than omitted. `priority` is a bare integer and `updated_at` uses the `Z` form of S5.12 | `src/cli/commands/update.rs:35` | update_status_json, update_title, update_assignee, update_claim, update_notes, update_add_label, update_remove_label, update_set_labels, update_noop_same_value, edge_precision_rewrite, update_status, update_priority, update_two_ids, update_last_touched |
| S5.40 | `close --json` has three shapes chosen by what happened. With no skips, no capacity warnings and no `--suggest-next` it is a **bare array** of closed issues. With `--suggest-next` it is `{"closed":[…],"skipped":[…],"unblocked":[…],"warnings":[…]}` with `skipped` and `warnings` omitted when empty. Otherwise it is `{"closed":[…],"skipped":[…],"warnings":[…]}` with the same omissions. A closed element is exactly `id`, `title`, `status`, `closed_at`, `close_reason`, the last omitted when absent; a skipped element is exactly `id`, `reason`; an unblocked element is exactly `id`, `title`, `priority` | `src/cli/commands/close.rs:284`, `:294`, `:305`, `:313`, `:323`, `:393` | close_reason_json, close_suggest_next, close_blocked_force, error_close_blocked_json, close_basic, close_two, error_close_blocked |
| S5.41 | `close`'s `closed_at`, and `defer`'s `defer_until`, are the timestamps written as `+00:00` rather than `Z`: `2026-01-02T03:04:05+00:00`. They are pre-formatted string members, not instants, so the fraction-digit rule of S5.12 does not apply to them | `src/cli/commands/close.rs:1217`, `defer.rs:352` | close_reason_json, close_suggest_next, close_blocked_force, defer_until_date, defer_relative |
| S5.42 | `reopen --json` is `{"reopened":[…],"skipped":[…],"warnings":[…]}` with the last two omitted when empty. A reopened element is exactly `id`, `title`, `status`, `closed_at`, the last omitted when absent | `src/cli/commands/reopen.rs:23`, `:40` | reopen_closed_json, reopen_closed, reopen_open |
| S5.43 | `defer --json` is `{"deferred":[…],"skipped":[…],"warnings":[…]}` and `undefer --json` is `{"undeferred":[…],"skipped":[…],"warnings":[…]}`, the trailing two omitted when empty. An element is exactly `id`, `title`, `previous_status`, `status`, `defer_until`, the last omitted when the deferral is indefinite | `src/cli/commands/defer.rs:24`, `:41`, `:52` | defer_until_date, defer_relative, undefer_basic, defer_no_until, undefer_not_deferred |
| S5.44 | `delete --json` is a seven-member object, every member always present, in this order: `deleted` (an array of ids), `deleted_count`, `dependencies_removed`, `labels_removed`, `events_removed`, `references_updated`, `orphaned_issues` (an array of ids). `--cascade` widens `deleted`; `--dry-run` has no JSON form and prints the Plain preview even under `--json` | `src/cli/commands/delete.rs:27` | delete_json, delete_cascade, delete_dry_run, delete_basic, delete_with_dependents, delete_with_dependents_force |
| S5.45 | `dep add --json` and `dep remove --json` are the same five-member object, always in this order: `status`, `issue_id`, `depends_on_id`, `type`, `action`. `status` is `ok` and `action` is `added` or `removed` | `src/cli/commands/dep.rs:323` | dep_add_json, dep_remove_basic |
| S5.46 | `dep list --json` is a bare array; each element is exactly `issue_id`, `depends_on_id`, `type`, `title`, `status`, `priority`, in that order, all six always present | `src/cli/commands/dep.rs:494` | dep_list_json, dep_list, dep_list_none |
| S5.47 | `dep tree --json` is a bare array of nodes in pre-order; each node is exactly `id`, `title`, `depth`, `parent_id`, `priority`, `status`, `truncated`, `repeat`, in that order, all eight always present, `parent_id` being `null` at the root. The node's two internal key members are not serialized | `src/cli/commands/dep.rs:506` | dep_tree_json, dep_tree, dep_tree_leaf, scn_child_ids, dep_tree_depth_json, dep_tree_deep_json, dep_tree_back_edge_json, dep_tree_repeat_json, dep_tree_max_depth_json, dep_tree_max_depth_0, dep_tree_up_json, dep_tree_both_json |
| S5.48 | `dep cycles --json` is an eight-member object, always in this order: `cycles` (an array of id arrays), `count`, `active_count`, `archived_closed_count`, `total_count`, `blocking_only`, `include_closed`, `scope`. Two further members, `active_cycles` and `archived_closed_cycles`, are appended when non-empty. `scope` is the string `active` by default Amended (2026-09-24): `scope` is `active_and_archived` under `--include-closed`, cycles or not; `--quiet --json` still prints the object. | `src/cli/commands/dep.rs:530` | dep_cycles_json, empty_dep_cycles, dep_cycles, dep_cycles_none_include_json, dep_cycles_real_json |
| S5.49 | `label add --json` and `label remove --json` are a bare **array** whose elements are exactly `status`, `issue_id`, `label`, with `status` being `added` or `removed` | `src/cli/commands/label.rs:85` | label_add_json, label_remove, label_add, label_add_existing, label_remove_absent |
| S5.50 | `label list --json` is a bare array of strings ascending by byte order. `label list-all --json` is a bare array of objects, each exactly `label` then `count`, ascending by label. `label rename --json` is a three-member object `old_name`, `new_name`, `affected_issues` | `src/cli/commands/label.rs:102`, `:109`, `src/storage/sqlite.rs:9546` | label_list_json, label_list_all_json, label_rename, label_list, label_list_all, r10_n984_label_list_all |
| S5.51 | `comments add --json` is a single comment object and `comments list --json` is a bare array of them; both use the five-member record of S5.15 with its `text` spelling and its `Z` timestamp. A newly added comment's `id` is the next value of the workspace's own counter, so the first comment on a store that already holds one is `2` | `src/cli/commands/comments.rs:133`, `:255`, `src/model/mod.rs:933` | comments_add_json, comments_add_author, comments_add_second, comments_list_json, actor_flag_comment, comments_list, comments_list_empty, comments_add, r18_comments_list_basic_json, r18_comments_shorthand_json, r20_m_list_json_trailing, r20_m_list_wrap, r20_m_shorthand_wrap, r20_m_list_empty_json, r22_g_comments_unknown_opt |
| S5.52 | `count --json` is `{"count":<n>}`. With a `--by-*` grouping it is `{"total":<n>,"groups":[{"group":"<key>","count":<n>},…]}` instead, the groups ordered ascending by key; the `--by-priority` keys carry the `P<n>` spelling | `src/cli/commands/count.rs:15`, `:20`, `:26` | count_json, count_by_priority_json, count_plain, count_by_status, count_by_type, count_by_assignee, count_by_label, count_include_closed |
| S5.53 | `stats --json` is `{"summary":{…}}` plus `breakdowns`, `recent_activity` and `capacity` when non-empty — none of the three appears in the in-scope corpus, `recent_activity` because it needs git. `summary` carries eleven always-present integer members in this order: `total_issues`, `open_issues`, `in_progress_issues`, `closed_issues`, `blocked_issues`, `deferred_issues`, `draft_issues`, `ready_issues`, `tombstone_issues`, `pinned_issues`, `epics_eligible_for_closure`, followed by `average_lead_time_hours` when at least one issue is closed Measured (2026-09-22): `breakdowns` follows `summary` as `[{"dimension":"type","counts":[{"key":"bug","count":1},…]},…]`, dimensions in the fixed order of S5.172, `counts` empty on an empty store (`empty_stats_by_type_json`). | `src/format/output.rs:245`, `:377` | stats_json, empty_stats_json, stats_plain, empty_stats, stats_by_type_json, stats_by_all_json, empty_stats_by_type_json |
| S5.54 | `average_lead_time_hours` is the only floating-point value any in-scope payload emits. It is written in the shortest round-tripping form, which always carries a decimal point: two hours is `2.0`, never `2`. Handover: S7 | `src/format/output.rs:265` | stats_json |
| S5.55 | `epic status --json` is a bare array; each element is exactly `epic` — a full issue object in the shape of S5.4 — then `total_children`, `closed_children`, `eligible_for_close`, all four always present | `src/model/mod.rs:886`, `src/cli/commands/epic.rs:85` | epic_status_json, epic_status, scn_child_ids |
| S5.56 | `where --json` is a five-member object of which the first, `path`, is always present and the other four — `redirected_from`, `prefix`, `database_path`, `jsonl_path` — are omitted when not resolved. `database_path` names `beads.db` even under `--no-db`, where no such file exists or is created Amended (2026-09-26, OQ-137): `database_path` and `jsonl_path` (and the plain `database:` and `jsonl:` lines) follow the overrides of S2.1 and S1.60. | `src/cli/commands/where.rs:18`, `:71` | where_json, where_plain, r18_where_json_quiet, r21_ws_none_cwd_where, r21_ws_none_where_json, r21_ws_nostore_where_json, r21_ws_beadsdb_env, r21_ws_redirect_rel_cwd, r21_wv_where_json_quiet, r21_wv_where_extra, r21_wv_where_robot, where_empty_store_no_prefix |
| S5.57 | `version --json` is a seven-member object, always in this order: `version`, `build`, `commit`, `branch`, `rust_version`, `target`, `features` (a string array). Its values describe the original's build, which the port cannot truthfully reproduce — OQ-003 | `goldens/version_json.out`; `src/cli/commands/version.rs:1` | version_json, version_plain |
| S5.58 | The read-only commands write no file: `show`, `list`, `search`, `ready`, `blocked`, `count`, `stats`, `dep list`, `dep tree`, `dep cycles`, `label list`, `label list-all`, `comments list`, `epic status`, `where`, `version` and `create --dry-run`. A golden whose stdout carries no `--- .beads/` dump is a run that left `.beads/` byte-identical, and a read-only command that rewrote the store would fail its golden | `scripts/ws_inner.py:133` | list_plain, list_all, list_sort_title, list_sort_created, list_sort_updated, list_sort_bad, list_reverse, list_deferred, list_long, list_unassigned, list_title_contains, list_label_two_and, list_label_any, list_status_in_progress, list_status_bad, create_dry_run, create_dry_run_json, edge_precision_sort_title, edge_precision_sort_created, error_show_missing, error_show_missing_json, error_update_missing, error_update_bad_priority, error_update_blocked_claim, error_update_ambiguous, error_reopen_missing, error_delete_missing, error_defer_bad_until, error_create_missing_parent, error_close_missing, close_epic_with_open_child, error_comments_add_missing, error_comments_add_empty, error_dep_add_self, error_dep_add_missing, error_create_bad_priority, error_create_empty_title, error_create_blank_title, error_create_long_title, error_create_bad_label, error_label_add_invalid, show_partial_ambiguous, error_dep_add_cycle |


<!-- from docs/spec-parts/S5b.md -->

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.100 | Five output modes exist: Rich, Plain, Json, Toon, Quiet. Under the pinned capture environment (`NO_COLOR=1` set, stdout a pipe) every run without `--json`/`--robot` and without `--quiet` is **Plain**; `--json` or `--robot` selects Json; `--quiet`/`-q` selects Quiet. Plain output carries no ANSI escape: every byte in the plain goldens is literal text | `src/output/mod.rs:1` (mode-detection doc), `src/output/context.rs:773` | `list_plain`, `list_quiet`, `list_json` |
| S5.101 | **Rich (TTY tables, panels, ANSI) and TOON (`--format toon`, `BR_OUTPUT_FORMAT=toon`) output are OUT OF SCOPE for the port** and are not specified here; every Rich branch named in the provenance below is unreachable while stdout is a pipe, and no captured case enters it | `docs/PLAN_TO_PORT_BEADS_RUST_TO_BEND2.md` §3 exclusions; `src/output/context.rs:895` | `list_plain`, `show_plain`, `stats_plain` (piped stdout takes the Plain branch; no golden `.out` or `.err` holds an escape byte); no case enters Rich or TOON: excluded surface |
| S5.102 | Every non-empty plain golden ends with exactly one `\n`; there is no case with a missing final newline and **no plain golden contains a trailing space on any line** (verified over all non-`*json*` `.out` and `.err` files). Padding is therefore always interior (`stats`), never at end of line | verified over `goldens/*.out`, `goldens/*.err`; `src/output/context.rs:947` uses `println!` throughout | `stats_plain`, `list_long`, `show_plain` |
| S5.103 | Stream split in Plain mode: ordinary results and the `✓` success lines go to **stdout**; `Warning: …` lines, `Error: …`/`Hint: …` lines and `[note] …` truncation notes go to **stderr**. A command may write both in one run (`dep_remove_absent` writes only stderr; `ready_limit` writes stdout results and a stderr note) | `src/output/context.rs:1236` (success→stdout), `:1252` (error→stderr), `:1266` (warning→stderr), `:1282` (info→stdout) | `dep_remove_absent`, `ready_limit`, `close_already_closed` |
| S5.104 | `ctx.success(m)` in Plain prints `✓ ` + `m` + `\n` to stdout (U+2713, one space). `ctx.warning(m)` prints `Warning: ` + `m` + `\n` to stderr. `ctx.error(m)` prints `Error: ` + `m` + `\n` to stderr. `ctx.info(m)` prints `m` + `\n` to stdout with **no** prefix or glyph. `ctx.print_line(m)` prints `m` + `\n` to stdout. `ctx.newline()` prints a bare `\n` to stdout | `src/output/context.rs:1236,1252,1266,1282,1309` | `create_min` (`✓ Created …`), `dep_list` (info header), `dep_remove_absent` (warning) |
| S5.105 | A terminating error is rendered by the top level as `Error: {message}` and, when a hint exists, a second line `Hint: {hint}`, both on **stderr**, followed by a single `\n`; stdout for that run holds only whatever was already written before the failure. In `close_already_closed`, `close_epic_with_open_child` and `error_close_blocked` stdout is **empty (0 bytes)** while stderr carries `Warning:` then `Error:` then `Hint:`, exit 3. (The error catalogue itself — messages, codes, exit codes — is S9's.) | `src/error/structured.rs:606`, `src/main.rs:1893` | `close_already_closed`, `error_close_blocked`, `error_show_missing` |
| S5.106 | In **Quiet** mode every mode-aware printer is a no-op: `print`, `print_line`, `print_styled_line`, `success`, `warning`, `info` and `newline` emit nothing; only `error` still writes (`Error: {message}` to stderr). A quiet read therefore produces a **0-byte stdout and a 0-byte stderr** with exit 0 | `src/output/context.rs:936,947,973,1236,1266,1282,1309` | `list_quiet` (both streams empty, exit 0) |
| S5.107 | Quiet is additionally short-circuited before rendering in `list`, `ready`, `blocked`, `search`, `show`, `count`, `stats`, `where`, `version`, `epic`, `comments`, `dep` (every subcommand), `label` (every subcommand), `update`, `defer`, `undefer` and `delete`: those commands return before building any text at all. A quiet **mutation** still performs its write and its `.beads/last-touched` update; only the report is suppressed | `list.rs:210`, `ready.rs:197`, `blocked.rs:235`, `search.rs:211`, `show.rs:314`, `count.rs:98,114`, `stats.rs:161`, `where.rs:41`, `version.rs:97`, `epic.rs:89`, `comments.rs:128,250`, `dep.rs:610,697,781,996,1736,2019`, `label.rs:489,517,545,576,622,656,683`, `update.rs:546`, `defer.rs:190,496`, `delete.rs:165,230,355,464,586,755` | `list_quiet`, create_quiet, dep_add_quiet, label_add_quiet, comments_add_quiet, delete_quiet, close_quiet, close_blocked_quiet, defer_quiet, undefer_quiet, reopen_quiet — measured 2026-09-20: each of those mutations writes its files and prints nothing, EXCEPT `reopen`, which prints its `✓ Reopened <id>: <title>` line under `--quiet` all the same (the list above omits `reopen` on purpose), and a `close` that skips every id, which drops the per-item `Warning:` lines (S9.26) but still writes the `Error:`/`Hint:` pair of S9.12 to stderr and exits 3; (case to add: `update_quiet` — `["@fx=basic","update","proj-mta","-p","0","--quiet"]`), r18_quiet_unknown_id, r18_quiet_json_unknown |
| S5.108 | `br reopen` is the one in-scope command with **no** Quiet guard: its plain branch writes with bare `print!`/`println!`, which the mode never sees, so `br reopen --quiet` still prints `✓ Reopened …` on stdout. `br create --silent` behaves the same way and for the same reason (S5.205) [inference: no captured case exercises either combination] | `reopen.rs:168-190` (no `is_quiet` branch), `create.rs:171` | (case to add: `reopen_quiet` — `["@fx=basic","reopen","proj-mkh","--quiet"]`; `create_silent_quiet` — `["create","X","--silent","--quiet"]`) |
| S5.109 | Golden-file convention: when a run changed the store, the harness appends `--- .beads/issues.jsonl ---` (and, when it changed, `--- .beads/last-touched ---`) plus the file bodies to the captured **stdout**, and a scenario golden adds `$ [argv]` headers and `[exit N]` lines. Those bytes are the harness's, not the command's: the command's own stdout ends at the first `--- .beads/` line | `scripts/ws_inner.py:212`, `scripts/make-fixture.sh:26` | `close_basic`, `scn_lifecycle` |

---

### S5.110–S5.119 — Shared plain-text primitives

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.110 | Status icons, one per status, used wherever a status glyph appears: `○` U+25CB open · `◐` U+25D0 in_progress · `●` U+25CF blocked · `❄` U+2744 deferred **and** draft · `✓` U+2713 closed · `✗` U+2717 tombstone · `📌` U+1F4CC pinned · `?` U+003F any custom status | `src/format/text.rs:15-32,105-116` | `list_all` (`◐ ○ ✓ ❄`), `search_status` (`✓`) |
| S5.111 | Priority renders as `P` followed by the decimal priority number (`P0`…`P4`); the priority **badge** is `[● ` + that label + `]`, always with `●` U+25CF regardless of the issue's status. The type **badge** is `[` + the type label + `]` (`[task]`, `[bug]`, `[feature]`, `[epic]`, `[docs]`, `[chore]`, `[question]`, or the custom type verbatim) | `src/format/text.rs:120,184,190,196` | `list_plain`, `ready_plain`, `blocked_plain` |
| S5.112 | Every untrusted single-line field (id, title, label, author, type/status label, path, query echo) passes through an inline sanitizer that leaves printable text untouched and replaces **every** control character — including `\n`, `\t`, `\r` — with its Rust `escape_default` form (`\u{1b}`, `\r`, `\n`, `\u{7}`, `\u{9b}`, …). Free-text bodies (description, design, prerequisites, acceptance criteria, notes, comment body) use a second sanitizer that **preserves `\n` and `\t`** and escapes everything else | `src/format/text.rs:40,49,53` | `show_unicode` (a real tab survives inside the description body: `Line two<TAB>tabbed`) |
| S5.113 | Date rendering in plain text: `Created`, `Updated`, `Closed`, `Due`, `Deferred Until`/`Deferred until` all print `%Y-%m-%d` (e.g. `2026-01-01`) — the time of day is never shown. Comment timestamps print `%Y-%m-%d %H:%M UTC` (e.g. `2026-01-01 12:30 UTC`): minute resolution, the literal suffix ` UTC`, no seconds and no offset | `show.rs:1320,1345,1353,1376,1479`, `src/format/text.rs:371,375,381` | `show_plain`, `show_closed`, `show_comments`, `comments_list`, `list_long` |
| S5.114 | Long titles are **not truncated and not wrapped** in the captured (piped) mode. `list`, `search` and `ready` compute a max width only when stdout is a terminal, and pass none otherwise; `blocked` passes width 0 unless `--wrap`. The ellipsis form (`…` as three ASCII dots appended after cutting to `max_len - 3` display columns, by `unicode-width`) therefore never appears in any plain golden Measured (2026-09-23, OQ-063): a 306-character title prints whole in plain `list` (`scn_long_texts`). | `list.rs:265`, `search.rs:267`, `ready.rs:212`, `blocked.rs:244`, `src/format/text.rs:250-290` | `list_plain` (54-column title printed whole); (case to add: `list_long_title` on a fixture whose title exceeds 200 columns, to pin that no truncation happens when piped), scn_long_texts |
| S5.115 | Free-text bodies in `br show` ARE soft-wrapped, at `COLUMNS` when that variable parses to ≥ 20 and at **100** columns otherwise (the sandbox clears the environment, so 100 applies). Wrapping breaks on spaces only, never splits a word, preserves existing line breaks, and carries a line's leading indentation onto its continuations. `--no-wrap` disables it entirely Measured (2026-09-23, OQ-064): at 100 columns `Word ` repeated wraps into lines of 20 words, 99 characters (`scn_long_texts`). Amended (2026-09-24, bb-rvt): a body of any length is wrapped the same way; `show_long_texts` holds an 80 KB description of seven lines of words. | `show.rs:1196,1208-1245,1189` | `show_unicode` (body shorter than 100 columns, unwrapped); (case to add: `show_wrap_100` on a fixture description longer than 100 columns), scn_long_texts, show_long_texts |
| S5.116 | The one-line issue summary used by `list` (default), `search` and `list --long`'s first line is exactly `{icon} {id} [● P{n}] [{type}] - {title}`: single spaces between the five parts and the separator is space-hyphen-space | `src/format/text.rs:298-334` | `list_plain`, `search_plain`, `edge_precision_sort_title`, r10_n984_search_limit |
| S5.117 | The per-issue detail block used by `list --long` lists, in this fixed order and only when the field is present and non-empty: `Status: {status}`, `Priority: P{n}`, `Type: {type}`, `Assignee: {a}`, `Owner: {o}`, `Labels: {l1, l2}` (comma+space joined, already sorted in the store; amended 2026-09-20: golden `list_long` prints NO `Labels:` line for `proj-170`, which carries `auth` and `backend`, so under `--no-db` the rows of the text listing carry no labels and the line never appears; the JSON listing does carry them, S5.33), `Due: {date}`, `Deferred Until: {date}`, then always `Created: {date}` and `Updated: {date}`. `Status`, `Priority` and `Type` are always present | `src/format/text.rs:345-386` | `list_long` (the deferred row shows `Deferred Until: 2027-06-01` between `Type:` and `Created:`) |
| S5.118 | Pluralization is per-message and inconsistent across commands; each message's rule is stated in its own clause. Three families exist: true pluralization (`1 issue` / `3 issues`), the parenthesized escape (`issue(s)`, `dependency link(s)`, `epic(s)`), and hard-coded plurals kept for bd conformance even at count 1 (`Blocked by 1 open dependencies`, S5.141) | `ready.rs:248`, `label.rs:585`, `blocked.rs:441`, `delete.rs:363` | `ready_limit` vs `ready_plain`, `label_list_all`, `blocked_plain` |
| S5.119 | Indentation vocabulary in plain text: two spaces for a sub-item under a header (`  - `, `  -> `, `  <- `, `  backend`, `  status: …`), three spaces for the `epic status` progress lines, four spaces for `blocked --detailed` blocker bullets (`    • `). No tabs are ever emitted | `delete.rs:365`, `show.rs:1454`, `label.rs:526`, `update.rs:1286`, `epic.rs:300`, `blocked.rs:422` | `delete_basic`, `show_plain`, `label_list`, `update_status`, `epic_status` |

---

### S5.120–S5.129 — `list`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.120 | Default `br list` prints one line per issue in result order, each `{icon} {id} [● P{n}] [{type}] - {title}` + `\n`, with **no header, no count, no footer and no separators** | `list.rs:315-319`, `src/format/text.rs:301` | `list_plain`, `list_all`, `list_reverse`, `list_status_in_progress`, `list_unassigned`, `list_label_two_and`, `list_label_any`, `list_title_contains`, `list_deferred`, `list_sort_title`, `list_sort_created`, `list_sort_updated`, `edge_precision_sort_created`, `edge_precision_sort_title`, r10_n984_list_default, r10_n984_list_sort_title_limit, r10_n984_list_sort_created_rev, r10_n984_list_title_contains, r10_due_list_all_sort_updated |
| S5.121 | An empty `br list` result prints **nothing at all** — 0 bytes on stdout, 0 on stderr, exit 0. There is no "no issues found" message (kept deliberately for bd conformance) | `list.rs:315` (comment: "bd outputs nothing when no issues found") | `empty_list` |
| S5.122 | `br list --long` prints, per issue, the S5.116 summary line followed by the S5.117 detail lines each prefixed with exactly two spaces, and inserts **one blank line between consecutive issues** — never before the first and never after the last. The output therefore ends `…  Updated: {date}\n` | `list.rs:313,713-725`, `src/format/text.rs:390-398` | `list_long`, r10_tree_list_long_sort_title |
| S5.123 | `br list --all` widens the selection to every status; the rendering is unchanged, so closed rows carry `✓` and deferred rows `❄` | `list.rs:315` | `list_all` |
| S5.124 | When a limit truncated the result set, `br list` writes a note to **stderr** before the listing: `[note] Showing {shown} of {total} issues. Use --limit 0 for all results.` when the total is known from client-side filtering, otherwise `[note] Output truncated to {shown} issues. Use --limit 0 for all results.`. Both are suppressed under `--quiet` and under JSON/TOON | `list.rs:194-208` | (case to add: `list_limit_plain` — `["@fx=basic","list","--limit","2"]`), r10_n984_list_limit_offset, r10_n984_list_json_limit |
| S5.125 | `br list --quiet` prints nothing on either stream, exit 0 | `list.rs:210` | `list_quiet` |
| S5.126 | An invalid `--sort` value is a validation error, not an empty list: stdout stays empty and stderr carries `Error: Validation failed: sort: invalid sort field 'nonsense'`, exit 4. An invalid `--status` likewise, with the long built-in-status message | `list.rs:855`, `src/error/structured.rs:606` | `list_sort_bad`, `list_status_bad` |
| S5.127 | `br list --tree` indents children under the nearest listed ancestor using `├── ` / `└── ` connectors with `│   ` / `    ` continuation prefixes, prepended to the S5.116 line Measured (2026-09-22, fixtures `parents` and `tree`): the ancestry is the dotted CHILD ID alone (`proj-abk.10.1` → `proj-abk.10` → `proj-abk`); a `parent-child` edge between undotted ids does not nest (`list_tree` prints `basic` flat). A child hangs under its nearest LISTED ancestor, and with none it is a root; roots keep the list order; a node's direct children follow in child-number order (`.9` before `.10`), whatever `--sort` says (`list_tree_sort_title`), then the descendants it adopted past an unlisted parent, in list order (`list_tree_adopted`: `.10.1` then `.3.1`). `--limit` cuts the listed rows first and the note follows (`list_tree_limit`); with `--long` or `--pretty` the tree prints summary lines only (`list_tree_long`); `--json` is unchanged (`list_tree_json`). | `list.rs:805-834` | list_tree, list_tree_parents, list_tree_json, list_tree_long, list_tree_wide, list_tree_adopted, list_tree_orphans, list_tree_limit, list_tree_reverse, list_tree_sort_title, r10_tree_list_tree_json_limit |
| S5.128 | `br list --pretty` replaces the two-space detail indent with tree connectors: every detail line but the last gets `├── `, the last gets `└── `, and the summary line is unchanged. `--long` additionally adds the `Created:`/`Updated:` pair to that block Measured (2026-09-22): without `--long` the block has no `Created`/`Updated` pair (`list_pretty`); blocks are separated by one blank line as in S5.122; `--json` is unchanged (`list_pretty_json`). | `src/format/text.rs:402-421`, `list.rs:279` | list_pretty, list_pretty_long, list_pretty_json, list_pretty_parents, list_pretty_empty, list_pretty_limit, r10_n984_list_pretty_limit |
| S5.129 | `br list --fields` and `--json` affect only structured output; the plain renderer ignores `--fields` | `list.rs:257,262` | `list_fields` (JSON; cited for the boundary) |

---

### S5.130–S5.136 — `ready`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.130 | `br ready` prints the header `📋 Ready work ({n} issue{s} with no blockers):` followed by **two** newlines (the format string ends in `\n` and `println!` adds another), producing one blank line, then the numbered list. `📋` is U+1F4CB; `{s}` is empty when `{n}` is 1 and `s` otherwise; `{n}` is the count **after** any `--limit` truncation | `ready.rs:245-251` | `ready_plain` (`3 issues`), `ready_limit` (`1 issue`), `edge_precision_ready` (`8 issues`) |
| S5.131 | Each ready row is `{index}. [● P{n}] [{type}] {id}: {title}` + `\n`, where `{index}` is the 1-based position rendered in decimal with no padding and followed by a period and one space. Note the ordering differs from `list`: badges come **before** the id, and the separator is `: ` not ` - ` | `ready.rs:340-372` | `ready_plain`, `ready_sort_priority`, `ready_sort_oldest`, `ready_type`, `ready_unassigned`, `edge_precision_ready`, `scn_lifecycle`, `scn_child_ids` |
| S5.132 | The ready list has **no trailing blank line**: output ends with the last row's `\n` | verified `od -c goldens/ready_plain.out`; `ready.rs:253` | `ready_plain` |
| S5.133 | An empty ready result prints exactly one of three lines, chosen in this order, on stdout: `✨ All work complete — no issues to work on` when the store holds no active issue at all; `✨ No ready issues match the requested filters or configured ready status group` when any filter (assignee, unassigned, label, type, priority, parent, or a non-default ready status group) was requested; `✨ No ready issues — remaining work is not currently actionable` otherwise. `✨` is U+2728 and the dash is `—` U+2014 | `ready.rs:221,292-302` | `empty_ready` (first form); `ready_parent_none` (second form); (cases to add: `ready_filtered_empty` — `["@fx=basic","ready","-l","nosuchlabel"]`; `ready_all_blocked_empty`) |
| S5.134 | When `--limit` truncated the ready set, a note goes to **stderr after** the listing: `[note] Showing {shown} of {total} ready issues. Use --limit 0 for all results.` (`{total}` is the pre-truncation count). Suppressed under `--quiet` | `ready.rs:259-265` | `ready_limit` |
| S5.135 | `--sort priority`, `--sort oldest` and the default change only the row order, never the line format; the header count and numbering are recomputed | `ready.rs:246` | `ready_sort_priority`, `ready_sort_oldest` |
| S5.136 | The ready rendering is state-dependent: after a `close`, the freshly unblocked issue appears in the list with its own type badge (`scn_lifecycle` step 4 shows `1. [● P1] [feature] proj-170: Implement user auth`, which was absent from step 1) | `goldens/scn_lifecycle.out:35` (step 4, `ready`: the quoted row), `goldens/scn_lifecycle.out:2` (step 1, `ready --json`: ids `proj-mta`, `proj-5u2`, `proj-b75` only), the unblocking `close proj-mta` at `goldens/scn_lifecycle.out:20`; steps as written in `goldens/scenarios/lifecycle.scn:4` and `goldens/scenarios/lifecycle.scn:8` | `scn_lifecycle`, `scn_child_ids` |

---

### S5.137–S5.142 — `blocked`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.137 | A non-empty `br blocked` output **begins with a blank line**: the header is printed as `\n🚫 Blocked issues ({total}):\n` plus `println!`'s newline, so the bytes are `\n`, the header line, then one blank line, then the rows. `🚫` is U+1F6AB | `blocked.rs:393` | `blocked_plain`, `scn_lifecycle` |
| S5.138 | `{total}` in the blocked header is the count **before** any `--limit` truncation, so a truncated run shows a header count larger than the number of rows | `blocked.rs:207-213,393` | `blocked_plain` (2 = full count); (case to add: `blocked_limit` — `["@fx=basic","blocked","--limit","1"]`) |
| S5.139 | Each blocked issue renders as two lines. The first is `[● P{n}] {id}: {title}` — note there is **no status icon and no type badge**, unlike `list` and `ready` | `blocked.rs:409` | `blocked_plain`, `scn_lifecycle` |
| S5.140 | The second line is `  Blocked by {count} open dependencies: [{ids}]`, two leading spaces, the blocker ids joined with `, ` inside square brackets, each id stripped of any `:status` suffix | `blocked.rs:441-445,458` | `blocked_plain` (`[proj-mta]`, `[proj-170]`) |
| S5.141 | The word `dependencies` in that line is **never singularized**: a single blocker still prints `Blocked by 1 open dependencies`. This is deliberate bd conformance and is reproduced, not fixed | `blocked.rs:437-439` (comment: "bd uses 'dependencies' even for count=1") | `blocked_plain`, `scn_lifecycle` |
| S5.142 | An empty blocked result prints the single stdout line `✨ No blocked issues` (U+2728) and nothing else, exit 0 | `blocked.rs:388` | `empty_blocked` |
| S5.254 | `br blocked --detailed` keeps the header, the blank lines, the order, the limit, the stderr note and the empty line of S5.137–S5.142, and replaces each issue's second line with `  Blocked by:` followed by one bullet per blocker in the order of its `blocked_by` list: `    • {id}: {title} [P{n}] [{status}]` (four spaces, U+2022, S5.119) for a stored blocker, and `    • {id}:blocked (not found)` for a blocker with no stored record, such as an `external:` target — which the summary form lists bare (`[external:other-1]`) and JSON lists as `"external:other-1"`. JSON output (`--detailed --json`) is byte-identical to `blocked --json` | `goldens/blocked_detailed.out:4-9`, `goldens/blocked_detailed_graph.out`, `goldens/blocked_detailed_limit.out`, `goldens/blocked_detailed_limit.err:1`, `goldens/blocked_detailed_json.out` (equal to `goldens/blocked_json.out`), `goldens/scn_blocked_detailed_external.out` | blocked_detailed, blocked_detailed_json, blocked_detailed_graph, blocked_detailed_limit, scn_blocked_detailed_external |

---

### S5.143–S5.155 — `show`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.143 | The `show` header line is `{icon} {id} · {title}` + **three spaces** + `[● {P{n}} · {STATUS}]`, where `·` is U+00B7 with one space on each side and `{STATUS}` is the status name upper-cased (`OPEN`, `IN_PROGRESS`, `CLOSED`, `DEFERRED`, …). The three spaces before `[` are literal and load-bearing | `show.rs:1296-1301` | `show_plain`, `show_comments` (`IN_PROGRESS`), `show_closed` (`CLOSED`), `show_epic`, `show_unicode`, `show_two`, `show_partial_hash` |
| S5.144 | Line 2 is always `Owner: {owner} · Type: {type}`. When the issue has no stored owner the value falls back to the `USER` environment variable, and to the literal `unknown` when that is unset — in the pinned sandbox `USER=tester`, so every golden shows `Owner: tester` even though the fixture records no owner | `show.rs:1304-1314` | `show_plain`, `show_closed`, `show_epic` |
| S5.145 | Line 3 is always `Created: {date} · Updated: {date}` with `%Y-%m-%d` dates and the same ` · ` separator | `show.rs:1317-1322` | `show_plain`, `show_two` |
| S5.146 | Optional metadata lines follow, each on its own line, only when present and in exactly this order: `Assignee: {a}` · `Labels: {l1, l2}` (comma+space) · `Ref: {external_ref}` (non-empty only) · `Due: {date}` · `Deferred until: {date}{countdown}` · `Estimate: {…}` · `Closed: {date} ({reason})` · `Rollup: {status} ({n status, …})`. Note `Deferred until:` here is lower-case `u`, unlike `list --long`'s `Deferred Until:` (S5.117) | `show.rs:1324-1392` | `show_plain` (Assignee+Labels), `show_comments` (Assignee), `show_partial_hash` (Labels), `show_closed` (Closed), r16_show_par_multi, r16_show_par_multi_json, r16_show_par_9vo1, r16_show_hier_6ja, r16_show_hier_in9_json, r16_show_hier_three, r16_show_hier_dup, r16_show_hier_dup_json, r16_show_tomb, r16_show_tomb_json, r16_show_cyc_tomb, r16_show_cyc_tomb_json, r16_show_cycle3_multi_json, r16_show_cycle2, r16_show_cycle_pc, r16_show_cycle_pc_json, r16_show_large_closed, r16_show_large_multi_json, r16_show_basic_ptp_mkh, r16_show_basic_multi_missing_json, r16_show_basic_q, r16_show_epic_e1, r16_show_epic_e4_json, r16_show_tree_abk_json, r16_show_due_all, r18_ambig_show_json, r19_show_long_texts_json, r19_show_long_texts_b1, r19_show_due_future, r19_show_status_done, r19_show_unicode_json, r19_show_graph_tip, r19_show_hier_in9, r19_show_parents_grand_json, r19_show_tree_10_json, r19_show_basic_three_json, r19_show_large_ambig, r19_show_large_ambig_json, r19_show_ext_ok, r19_none_show, r19_none_show_json, r19_malformed_show, r19_missing_title_show, r20_x_lead_subsecond_show, r20_x_micros_show, r20_x_timespell_show, r20_x_v_labels_many_show, r21_sh_nowrap_long, r21_sh_cols40, r21_sh_cols0, r21_sh_cols_abc, r21_sh_cols200_wrap, r21_sh_wrap_nowrap, r21_sh_unicode_desc_nowrap, r21_sh_two_ids_nowrap, r22_g_json_after_dashdash, r22_g_show_format_bad, r22_e_fmt_json_show, r22_m_template_show |
| S5.147 | `Closed: {date} ({reason})` uses the stored close reason, or the literal `closed` when the record has a closed timestamp but no reason | `show.rs:1371-1379` | `show_closed` (`Closed: 2026-01-01 (Done before the fixture)`) |
| S5.148 | The `Deferred until:` countdown suffix is computed against the current instant: empty once the gate has elapsed; ` (gate opens in under a day)` when under 24 hours remain; ` (gate opens in 1 day)` / ` (gate opens in {n} days)` on an exact multiple of a day; ` (gate opens in over 1 day)` / ` (gate opens in over {n} days)` when a partial day remains | `show.rs:1259-1277,1348-1355` | (case to add: `show_deferred_plain` — `["@fx=basic","show","proj-7vm"]`; the pinned clock makes the countdown deterministic) |
| S5.149 | `Estimate:` renders as `{h}h {m}m` when both are non-zero, `{h}h` when the remainder is zero, and `{m}m` when under an hour; it is omitted when the estimate is absent or zero | `show.rs:1357-1369` | (case to add: `show_estimate` — a fixture issue with `estimated_minutes` 90) |
| S5.150 | Every body section is preceded by **one blank line**. The description has no heading — a blank line then the (wrapped, S5.115) body. `Design:`, `Prerequisites:`, `Acceptance Criteria:` and `Notes:` each get a blank line, then their heading line, then the body Amended (2026-09-25, round 16): a description stored as `""` still prints its block (one blank line and an empty line); `design`, `acceptance_criteria` and `notes` stored as `""` print nothing. An assignee stored as `""` prints `Assignee: ` with an empty value, and an owner stored as `""` prints `Owner:  · Type:` with no `USER` fallback (S5.144, S5.146). | `show.rs:1394-1450` | `show_partial_hash`, `show_two`, `show_unicode`, r16_show_prec_aaf, r16_show_empty_optionals, r16_show_empty_optionals_two, r16_show_prec_aaf_aaa, show_empty_texts, show_empty_texts_json |
| S5.151 | `Dependencies:` is preceded by a blank line and each edge renders as `  -> {id} ({type}) - {title}`: two spaces, ASCII arrow, the dependency type in parentheses, then space-hyphen-space and the target title Measured (2026-09-23, OQ-045): an `external:` end renders with its reference as the id and `⏳ ` plus the reference as the title: `  -> external:other-1 (blocks) - ⏳ external:other-1` (`scn_external_blocker`). Measured (2026-09-24, OQ-132): an end that is neither stored nor `external:` (only `show` reads such a store; the import refuses it, S2.57) renders as `  -> proj-zz (blocks) - [missing issue: proj-zz]`, and in the JSON form as `{"id": <id>, "title": "[missing issue: <id>]", "status": "tombstone", "priority": 2, "dependency_type": <type>}`; it sorts (S5.31) with priority 2 and the edge's own `created_at` as its creation instant (`show_dep_missing`: after a stored P2 record created later than the edge, before a P4 one).  Measured (2026-09-24, round 12, OQ-134): an `external:` end sorts the same way (priority 2, the edge's own `created_at`, then id), and its title drops the `external:` prefix when the rest is `<project>:<id>` with both parts non-empty (`external:p:q:r` → `⏳ p:q:r`); `external:solo`, `external:` and `external:p:` keep the whole reference. | `show.rs:1450-1463`; `goldens/show_dep_missing.out`, `goldens/show_dep_missing_json.out`, `goldens/r12_ext_names_show.out`, `goldens/r12_show_mix.out` | `show_plain`, `show_two`, scn_external_blocker, show_dep_missing, show_dep_missing_json, edge_load_dep_dangling_show, r12_ext_names_show, r12_show_mix, r12_show_mix_json, r12_ext_pc_show_json, r12_dep_tomb_target_show_json, r12_dep_to_ephemeral_show |
| S5.152 | `Dependents:` mirrors it with the reversed arrow: a blank line, the heading, then `  <- {id} ({type}) - {title}` per edge | `show.rs:1465-1478` | `show_plain`, `show_epic`, `show_partial_hash`, `show_two` |
| S5.153 | `Comments:` is preceded by a blank line, then per comment one line `  [{%Y-%m-%d %H:%M UTC}] {author}: {body}` — two leading spaces, the timestamp in square brackets, the author, `: `, and the body. This is a different shape from `comments list` (S5.193) Measured (2026-09-23, S5.115): a comment body longer than the wrap width is wrapped ON ITS OWN at 100 columns and then prefixed, so its continuation lines start at column 0; `--no-wrap` prints it whole and `--wrap` is the default (`scn_long_comment`). | `show.rs:1477-1489` | `show_comments`, scn_long_comment |
| S5.154 | Several ids in one `br show` invocation are separated by exactly **one blank line**, emitted before each issue after the first; the ids render in the order given on the command line, and no separator precedes the first or follows the last | `show.rs:366`, `show.rs:221` | `show_two`, r10_n984_show_short, r10_tree_show_root, r10_due_show_json |
| S5.155 | A `show` that resolves no issue prints nothing on stdout; stderr carries `Error: Issue not found: {id}` + `Hint: Run 'br list' to see available issues.` (exit 3) or the ambiguity pair `Error: Ambiguous ID '{p}': matches [{"id", …}]` + `Hint: Provide more characters of the ID` (exit 3). A successfully resolved partial id renders identically to the full id | `show.rs:234-240`, `src/error/structured.rs:606` | `error_show_missing`, `show_partial_ambiguous`, `show_partial_hash` |

---

### S5.156–S5.160 — `search`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.156 | `br search {q}` prints a header line on stdout, then the matching issues in the S5.116 one-line format. The header is `Found {n} issue(s) matching '{q}'` — the count is always followed by the literal `issue(s)`, and the query is echoed between plain ASCII single quotes, sanitized but not otherwise escaped | `search.rs:321-327,330-333` | `search_plain`, `search_case_insensitive`, `search_description`, `search_unicode`, `search_status` |
| S5.157 | When the page omitted further matches the header verb changes to `Showing`: `Showing {n} issue(s) matching '{q}'` | `search.rs:315-320` | (case to add: `search_limit` — `["@fx=basic","search","a","--limit","1"]`) |
| S5.158 | A search with no matches prints only the header, `Found 0 issue(s) matching '{q}'`, exit 0 — on stdout, with no "not found" error | `search.rs:321` | `search_none`, `empty_search` |
| S5.159 | When the default closed-issue exclusion hid matches, a trailing **stdout** line follows the results: `note: {n} closed match(es) hidden; rerun with --all to include them` | `search.rs:374-380` | (case to add: `search_hidden_closed` — `["@fx=basic","search","work"]`, whose only match is the closed `proj-mkh`) |
| S5.160 | The search truncation note goes to **stdout**, not stderr (unlike `list` and `ready`): `note: showing {shown} search result(s) from offset {offset}; more matches exist. Use --limit 0 for all results (current limit: {limit})` | `search.rs:352-371` (the `stderr` argument is `false` at both text call sites) | (case to add: `search_limit`, as above) |

---

### S5.161–S5.166 — `count`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.161 | Ungrouped `br count` prints the decimal total and nothing else: one line, no label, no thousands separator | `count.rs:109` | `count_plain` (`6`), `count_include_closed` (`8`), `empty_count` (`0`) |
| S5.162 | Grouped `br count --by-*` prints `Total: {n}` first, then one line per group as `{group}: {count}` with **no indentation** | `count.rs:125-133` | `count_by_status`, `count_by_type`, `count_by_assignee`, `count_by_label` |
| S5.163 | The group key for issues with no assignee is the literal `(unassigned)`; for issues with no labels it is the literal `(no labels)`. Both sort into the group list wherever their text places them — in the goldens they lead, ahead of the real names | `count.rs:297,309,316`; `src/storage/sqlite.rs:9303` | `count_by_assignee` (`(unassigned): 4`), `count_by_label` (`(no labels): 4`) |
| S5.164 | An issue with several labels contributes once to **each** of its label groups, so the grouped counts can exceed `Total:` | `count.rs:309-318` | `count_by_label` (4+1+2+1 = 8 > `Total: 6`) |
| S5.165 | `--include-closed` widens the population; the output shape is unchanged | `count.rs:88` | `count_include_closed`, r10_n984_count_by_status, r10_n984_count_title_json, r10_tree_count_by_priority, r10_due_count_by_status_all |
| S5.166 | `br count --quiet` returns before printing, in both the grouped and ungrouped paths | `count.rs:98,114` | (case to add: `count_quiet` — `["@fx=basic","count","--quiet"]`) |

---

### S5.167–S5.172 — `stats`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.167 | `br stats` opens with `📊 Issue Database Status` (U+1F4CA) followed by a blank line, then the literal line `Summary:` | `stats.rs:1222-1226` | `stats_plain`, `empty_stats` |
| S5.168 | Every summary row is two leading spaces, the label with its colon, spaces padding the whole label field so the **value starts at column 27** (i.e. the two-space indent plus a 24-character label field), then the decimal value. The padding is written literally in the format strings, so the exact byte counts are: `  Total Issues:` + 11 spaces, `  Open:` + 19, `  In Progress:` + 12, `  Blocked:` + 16, `  Closed:` + 17, `  Ready to Work:` + 10, `  Deferred:` + 15, `  Tombstones:` + 13, `  Pinned:` + 17, `  Epics ready to close:` + 3, `  Avg Lead Time:` + 10, `  Deleted:` + 16 | `stats.rs:1228-1266` | `stats_plain` (every row is 27 bytes wide up to the value), `empty_stats` |
| S5.169 | The six rows `Total Issues`, `Open`, `In Progress`, `Blocked`, `Closed`, `Ready to Work` are always printed, even at 0. `Deferred`, `Tombstones`, `Pinned` and `Epics ready to close` are printed **only when greater than zero** | `stats.rs:1228-1246` | `empty_stats` (six rows, no `Deferred`), `stats_plain` (seven rows, `Deferred: 1`) |
| S5.170 | The `Extended:` section — a blank line, the literal `Extended:`, then its rows — appears only when an average lead time exists or tombstones exist. Its `Avg Lead Time` value renders as `{x:.1} hours` when the average is under 24 hours and as `{x/24:.1} days` at 24 hours or more (one fractional digit, half-away-from-zero at the decimal, then the literal unit) Measured (2026-09-22): the `Deleted:` row reads `  Deleted:                1 (tombstones)` — the count followed by ` (tombstones)` (`scn_stats_tombstone`). Measured (2026-09-23): `1.3 hours` for a mean of 4/3 hours: `{:.1}` of the double, rounded half to even on its exact value; the days figure is the double of the mean divided by 24.0, rounded the same way. | `stats.rs:1249-1267` | `stats_plain` (`2.0 hours`), `scn_lifecycle` final step (`23.2 days`), `empty_stats` (section absent), scn_stats_tombstone, r10_n984_stats, r10_parents_stats_json, r10_due_stats, scn_lead_time_third |
| S5.171 | `br stats` always closes with a blank line then `For more details, use 'br list' to see individual issues.` and the final newline | `stats.rs:1294` | `stats_plain`, `empty_stats` |
| S5.172 | Optional trailing blocks, absent from every captured case, keep the text above byte-stable: per-dimension breakdowns print a blank line, `By {dimension}:`, then `  {key}: {count}` rows; a recent-activity block prints `Recent Activity (last {h} hours):` with six padded rows in the same 27-column shape; a capacity block prints a blank line, `Capacity:`, a header row `  {CAPACITY:<24} {COUNTED:>7} {AGGREGATES:>10} {EXEMPT:>6} {SOFT:>5} {HARD:>5} {REMAINING:>9}  STATE` and one data row per capacity with `-` for absent numbers. Git-derived commit activity is out of scope for the port Measured (2026-09-22): the blocks come after the `Extended:` block and before the closing line, in the fixed order type, priority, assignee, label whatever the order of the flags (`stats_by_label_type`); the rows are the groups of `count --by` (S4.148, S4.149) over the records that are not tombstones (`scn_stats_tombstone`: after one delete, `task: 2`); an empty store keeps each heading with no rows (`empty_stats_by_type_label`). | `stats.rs:1269-1291,1309-1340` | `stats_plain`, `empty_stats` (none of the three optional blocks appears in either golden), ; capacity/activity require configuration the fixtures do not carry, stats_by_type, stats_by_priority, stats_by_assignee, stats_by_label, stats_by_label_type, empty_stats_by_type_label, stats_by_type_graph, scn_stats_tombstone |

---

### S5.173–S5.182 — `dep`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.173 | `br dep add` on success prints on stdout `✓ Added dependency: {issue} -> {depends_on} ({type})` — ASCII arrow, type in parentheses. The default type is `blocks`; `external:` targets render verbatim | `dep.rs:640-644` | `dep_add_blocks`, `dep_add_related`, `dep_add_external` |
| S5.174 | A duplicate edge is **not** an error: stdout gets `Dependency already exists: {issue} → {depends_on}` with the **Unicode** arrow `→` U+2192 and no `✓` prefix (it is an info line), exit 0 | `dep.rs:646-650` | `dep_add_duplicate` |
| S5.175 | `br dep remove` on success prints `✓ Removed dependency: {issue} -> {depends_on} ({type})` (ASCII arrow). When the edge does not exist the command still exits 0 but stdout stays **empty** and stderr carries `Warning: Dependency not found: {issue} → {depends_on}` with the Unicode arrow | `dep.rs:788-802` | `dep_remove_absent` (stderr only; the store is still rewritten) |
| S5.176 | `br dep list {id}` prints a header via the info channel (stdout, no glyph): `Dependencies of {id} ({n}):` for the default downward direction, `Dependents of {id} ({n}):` for `--direction up`, `Dependencies and dependents of {id} ({n}):` for `both` | `dep.rs:1032-1046` | `dep_list`, `dep_list_up`, `dep_list_both` |
| S5.177 | Each `dep list` row is `  -> {other_id} ({type}): {title} [P{n}] [{status}]` when the row's subject is the queried issue, and `  <- {other_id} ({type}): {title} [P{n}] [{status}]` when the queried issue is the target. Note the colon after the closing parenthesis — `show` uses ` - ` in the same position (S5.151) Measured (2026-09-23, OQ-045): an `external:` end shows `⏳ {ref}` as its title, `blocked` as its status and priority 2, in `dep list` text and JSON, in `dep tree`, and in `show --json` `dependencies` (`scn_external_json`). Amended (2026-09-25, round 20): the title of an `external:` end drops the `external:` prefix when the rest is `<project>:<id>` with both parts non-empty, as in `show` (S5.151); `dep tree` spells it the same. | `dep.rs:1048-1067` | `dep_list`, `dep_list_up`, `dep_list_both`, `dep_list_both_basic`, scn_external_json, r20_d_list_ext_types, r20_d_list_ext_types_plain, dep_list_ext_titles, dep_list_ext_titles_json, dep_tree_ext_titles, show_ext_titles, dep_tree_ext_titles_json |
| S5.178 | `br dep list` on an issue with no edges prints the single stdout line `No dependencies for {id}` (or `No dependents for {id}` / `No dependencies or dependents for {id}` per direction), exit 0, with **no trailing period** | `dep.rs:1014-1024` | `dep_list_none`, `dep_list_up_none`, `dep_list_both_none` |
| S5.179 | `br dep tree {id}` prints one line per node: `{indent}{prefix}{id}: {title} [P{n}] [{status}]`, where `{indent}` is two spaces repeated once per depth level and `{prefix}` is empty at depth 0, `├── ` at any deeper node, `├── (truncated) ` when the subtree was cut off, and `├── (shown above) ` when the node repeats an already-printed one. A node can carry BOTH flags at once — an already-expanded node with neighbours is `repeat` by S4.185 and, because it does not expand here either, `truncated` as well — and the `(truncated) ` prefix wins: `dep_tree_repeat` prints `├── (truncated) proj-in9` for the occurrence whose JSON (`dep_tree_repeat_json`) carries `"truncated":true,"repeat":true`. `└──` is never used by this renderer Resolved (2026-09-23, OQ-097): no header line; the tree starts with its root line (`dep_tree`). | `dep.rs:1756-1775` | `dep_tree`, `dep_tree_leaf`, `scn_child_ids` (root-only); `dep_tree_depth`, `dep_tree_deep`, `dep_tree_diamond`, `dep_tree_back_edge` (indent and glyphs at real depth); `dep_tree_repeat`, `dep_tree_max_depth`, `dep_tree_max_depth_1`, `dep_tree_max_depth_0` (both marker prefixes and their precedence); `dep_tree_up`, `dep_tree_both`, `dep_tree_up_basic` |
| S5.180 | An empty `dep tree` prints the stdout line `No dependency tree for {id}` | `dep.rs:1748` | (case to add: `dep_tree_empty`) |
| S5.253 | `br dep tree {id} --format mermaid` prints `graph TD`, then one line per node of the walk in its pre-order, `    n{k}["{id}: {title} [P{n}]"]` with `k` counting from 0 at the root, then one line per non-root node in the same order, `    n{p} --> n{k}`, where `p` is the place of that node's parent occurrence (the latest earlier node one level shallower), so an edge into a repeated id comes from the branch that reached it. Indent is four spaces; there is no status and no `truncated`/`repeat` mark, and a repeated id is a node of its own. The label is the inline-sanitized id and title of S5.112 (a tab prints as `\t`) with every `"` replaced by `'`; `<`, `>`, `&`, `\`, `|`, `[`, `]` and non-ASCII text pass unchanged. A root with no neighbour prints `graph TD` and the one node line | `goldens/dep_tree_mermaid.out:1-12`, `goldens/dep_tree_mermaid_up.out:8-12`, `goldens/dep_tree_mermaid_leaf.out:1-2`, `goldens/dep_tree_mermaid_unicode.out:2`, `goldens/scn_mermaid_titles.out` | dep_tree_mermaid, dep_tree_mermaid_up, dep_tree_mermaid_leaf, dep_tree_mermaid_unicode, scn_mermaid_titles |
| S5.181 | `br dep cycles` with no cycles prints `✓ No dependency cycles detected.` (with the trailing period) on stdout. With `--blocking-only` the noun phrase becomes `blocking dependency`: `✓ No blocking dependency cycles detected.`. When archived closed-only cycles were hidden the line instead reads `✓ No active {scope} cycles detected. {n} archived closed-only cycle(s) hidden; rerun with --include-closed to inspect them.` Amended (2026-09-24): `--include-closed` on a store without cycles prints the plain line; the hidden-cycles line becomes `✓ No active blocking dependency cycles detected. …` under `--blocking-only`. | `dep.rs:2023-2031,2067-2073` | `dep_cycles`, `empty_dep_cycles`, dep_cycles_closed_only_blocking |
| S5.182 | When cycles exist, stderr gets `Warning: Found {n} {scope} cycle(s):` and stdout gets one line per cycle, `  {i}. {id1 -> id2 -> …}` (two spaces, 1-based index, period, space, the ids joined by ` -> `) Amended (2026-09-24, OQ-074): measured; `{scope}` is `dependency`, or `blocking dependency` under `--blocking-only`; exit 5 when a shown cycle is active. | `dep.rs:2036-2040,2124-2130` | (case to add: `dep_cycles_found` on a fixture that contains a cycle), dep_cycles_real, edge_dep_cycles_two |

---

### S5.183–S5.190 — `label`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.183 | `br label add` prints one stdout line per (issue, label) pair: `✓ Added label {label} to {id}` when newly added, `✓ Label {label} already exists on {id}` when it was already present. Both carry the `✓` and both exit 0 | `label.rs:131-138,495` | `label_add`, `label_add_existing` |
| S5.184 | `br label remove` prints `✓ Removed label {label} from {id}`, or `✓ Label {label} not found on {id} (no-op)` when it was absent; both exit 0 | `label.rs:139-143` | `label_remove_absent` (the `label_remove` golden uses `--json`), r18_label_remove_multi, r18_label_remove_comma |
| S5.185 | `br label list {id}` prints `Labels for {id}:` then one line per label, two spaces then the label, in the record's (sorted) order Amended (2026-09-25, round 15): WITHOUT an issue, `label list` prints every label of the project by name: `Labels ({n} total):` then `  {label}` per label in ascending byte order (no counts, unlike `list-all`, S5.187); JSON is a plain array of names (`["auth","backend","db"]`); a project without labels prints `No labels in project.` (JSON `[]`). | `label.rs:524-527` | `label_list`, r15_label_list_dup_precision, r15_label_list_dup_precision_plain, r15_label_list_noarg, r15_label_list_noarg_json, r15_label_list_missing, label_list_all_names_empty, label_list_all_names_empty_json, r16_label_list_q, r16_label_list_basic, r16_label_list_prec_json, r18_label_list_issue_none, r18_label_list_unknown, r20_l_list_many_json, r20_l_list_partial, r22_l_label_list_one |
| S5.186 | `br label list {id}` on an issue with no labels prints `No labels for {id}.` — with a trailing period, unlike the `dep list` empty line (S5.178) | `label.rs:522` | (case to add: `label_list_empty` — `["@fx=basic","label","list","proj-5u2"]`) |
| S5.187 | `br label list-all` prints `Labels ({n} total):` then one line per label, `  {label} ({count} issue)` at count 1 and `  {label} ({count} issues)` otherwise — this message **does** pluralize properly | `label.rs:583-589` | `label_list_all` (`auth (1 issue)`, `backend (2 issues)`), r15_label_list_all_large, r15_label_list_all_large_json, r15_label_list_all_precision, r15_label_list_all_empty, r15_label_list_all_empty_json, r18_label_list_all_json, r20_l_listall_many, r22_e_fmt_json_label_listall |
| S5.188 | With no labels anywhere, `list-all` (and the unique-label form of `list`) prints `No labels in project.` | `label.rs:550,581` | (case to add: `label_list_all_empty` on the empty fixture) |
| S5.189 | `br label rename {old} {new}` prints `✓ Renamed label '{old}' to '{new}' on {n} issue` at count 1 and `… on {n} issues` otherwise, with the names in plain ASCII single quotes | `label.rs:161-169` | (case to add: `label_rename_plain` — `["@fx=basic","label","rename","backend","server"]`; the existing `label_rename` uses `--json`) |
| S5.190 | The two no-change rename paths print, without a `✓`: `Label '{old}' already has that name; no changes made.` when old and new are equal, and `Label '{old}' not found on any issues.` when nothing matched. Both exit 0 | `label.rs:147-159` | (case to add: `label_rename_noop` — `["@fx=basic","label","rename","backend","backend"]`), (case to add: `label_rename_missing` — `["@fx=basic","label","rename","nosuch","other"]`); both re-measured through the sandbox 2026-09-20: the quoted line on stdout, exit 0 |

---

### S5.191–S5.195 — `comments`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.191 | `br comments add` prints the single stdout line `Comment added to {id}` — no `✓`, no quoting of the comment text, no id of the new comment | `comments.rs:139,395-397` | `comments_add` |
| S5.192 | `br comments list {id}` prints the header `Comments for {id}:` | `comments.rs:274,399-401` | `comments_list`, r15_comments_list_missing, r15_comments_list_missing_json, r15_comments_list_partial, r15_comments_list_empty_json, r15_comments_bare_id, r15_comments_list_unicode |
| S5.193 | Each comment renders as **three** stdout lines: `[{author}] at {%Y-%m-%d %H:%M UTC}`, then the body with trailing newlines stripped (interior `\n`/`\t` preserved), then a **blank line**. The blank line follows every comment including the last, so the output ends with `\n\n` Amended (2026-09-24, bb-rvt): ONLY trailing line feeds are stripped: a trailing space or tab stays (`hello \n` prints `hello `, `hello\t` prints `hello` and a tab), and leading spaces stay; a body of any length renders the same way. | `comments.rs:275-287` | `comments_list` (bytes end `staging.\n\n`), comments_trailing_ws, show_comment_trailing_ws, comments_long_texts |
| S5.194 | An issue with no comments prints `No comments for {id}.` (trailing period), exit 0 Resolved (2026-09-23, OQ-097): no header line precedes the output (`comments_list_empty`). | `comments.rs:270,403-405` | `comments_list_empty` |
| S5.195 | The `comments list` timestamp shape (`[author] at {ts}` on its own line) differs from the `show` comment shape (`  [{ts}] {author}: {body}` on one line, S5.153); both use minute resolution and the ` UTC` suffix, and neither shows seconds | `comments.rs:276-281` vs `show.rs:1479-1484` | `comments_list`, `show_comments` |

---

### S5.196–S5.199 — `epic status`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.196 | `br epic status` prints, per epic, `{icon} {id} {title}` — the id and title separated by a single space, with **no** `·`, no priority badge and no type badge | `epic.rs:299` | `epic_status`, `scn_lifecycle` |
| S5.197 | The epic glyph is `✓` when the epic is eligible for closure (children exist and all are closed) and `○` otherwise — it does **not** follow the issue's own status icon | `epic.rs:311-326` | `epic_status` (`○`), `scn_lifecycle` step 7 (`✓`) |
| S5.198 | Line 2 is `   Progress: {closed}/{total} children closed ({pct}%)` with **three** leading spaces. The percentage is the integer truncation of `closed * 100 / total`, and 0 when there are no children | `epic.rs:300,435-443` | `epic_status` (`0/1 … (0%)`), `scn_lifecycle` (`1/1 … (100%)`) |
| S5.199 | When the epic is eligible, a third line `   Eligible for closure` (three leading spaces) follows. Every epic block then ends with a bare blank line, so a single-epic output ends `…\n\n`. With no open epics the command prints `No open epics found` | `epic.rs:302-309,97` | `epic_status`, `scn_lifecycle` |

---

### S5.200–S5.202 — `where`, `version`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.200 | `br where` prints the resolved `.beads` directory on line 1 with no indentation, then, each only when known and each with a two-space indent: `  (via redirect from {origin})`, `  prefix: {p}`, `  database: {db path}`, `  jsonl: {jsonl path}`. The `database:` line is printed even in `--no-db` mode, naming the path `beads.db` would occupy | `where.rs:234-248` | `where_plain` |
| S5.201 | `br version` prints one line: `br version {version} ({build})`, extended with ` ({branch}@{commit7})` when both are known, ` ({branch})` with branch only, ` ({commit7})` with commit only, where `{commit7}` is the first 7 characters of the commit hash. The pinned oracle prints `br version 0.6.0 (release) (v0.6.0@b1cfebe)` | `version.rs:117-133`; captured golden `goldens/version_plain.out:1` is exactly `br version 0.6.0 (release) (v0.6.0@b1cfebe)` plus one newline, `goldens/version_plain.exit:1` is `0`, stderr empty | `version_plain` |
| S5.202 | The port cannot truthfully reproduce the original's build metadata; that is OQ-003 and resolves to a DISC, not to a clause here. The **shape** above is what the port must keep | `docs/OPEN_QUESTIONS.md` OQ-003; the metadata is fixed at compile time of the original: `version.rs:33` (crate version), `version.rs:49-53` (build kind), `version.rs:55-58` (commit, branch, rustc, target from build-script variables); the captured bytes the shape is read from: `goldens/version_plain.out:1`, `goldens/version_json.out:1` | `version_plain`, `version_json` |

---

### S5.203–S5.212 — `create`, `q`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.203 | `br create` on success prints the single stdout line `✓ Created {id}: {title}` | `create.rs:224-229` | `create_min`, `create_title_flag`, `scn_dup_title`, `scn_last_touched`, `scn_child_ids` |
| S5.204 | `br q` prints only the new id and a newline — no glyph, no title | `q.rs:241` | `q_basic`, `q_priority` |
| S5.205 | `br create --silent` prints only the new id and a newline, and does so **before** any mode dispatch, so it wins over `--json`, `--quiet` and Rich alike | `create.rs:170-172` | `create_silent`; (case to add: `create_silent_json` — `["create","X","--silent","--json"]`) |
| S5.206 | `br create --dry-run` prints a block on stdout and writes nothing: `Dry run: would create issue {id}` (an info line, no glyph), then `Title: {title}`, `Type: {type}`, `Priority: P{n}`, then `Labels: {l1, l2}` only when `-l` was given, `Parent: {id}` only when `--parent` was given, and `Dependencies: {id, id}` only when `--deps` was given. The golden carries no `--- .beads/` dump, confirming nothing was written | `create.rs:196-220` | `create_dry_run` |
| S5.207 | The dry-run id is a real generated id (`proj-8h9` in the golden), not a placeholder | `create.rs:197` | `create_dry_run` |
| S5.208 | `Priority: P{n}` in the dry-run block comes from the priority's own display form, which is `P` + the number | `src/model/mod.rs:168-172` | `create_dry_run` (`Priority: P2`) |
| S5.209 | Capacity warnings, when any exist, follow the create/update report as `Warning: {text}` lines on stderr; no captured case configures capacity | `create.rs:231-235` | (case to add: `create_capacity_warning` — `["@fx=<fixture with a capacity policy>","create","X"]`; needs a fixture sidecar `.beads/policy.yaml`, which `scripts/ws_inner.py` does not copy today) |
| S5.210 | A create that fails validation prints nothing on stdout; stderr carries `Error: …` (+ `Hint:` where one exists) and the exit code is 3 or 4 per S9 | `src/error/structured.rs:606` | `error_create_empty_title`, `error_create_blank_title`, `error_create_long_title`, `error_create_bad_priority`, `error_create_bad_label`, `error_create_missing_parent` |
| S5.211 | `br q` reports an unusable label on **stderr** as `Warning: invalid label '{label}': {reason}` and continues creating the issue | `q.rs:33-39,129` | (case to add: `q_bad_label` — `["q","X","-l","bad label!"]`) |
| S5.212 | In Quiet mode `br create` (without `--silent`) prints nothing, because its report goes through the mode-aware success/info channels which no-op in Quiet; the issue is still created and `.beads/last-touched` still updated | `create.rs:225`, `src/output/context.rs:1236` | (case to add: `create_quiet` — `["create","X","--quiet"]`) |

---

### S5.213–S5.219 — `update`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.213 | `br update` prints, per updated id, a header line `Updated {id}: {title}` on stdout — no glyph — where `{title}` is the title **after** the update | `update.rs:1284,1327-1333` | `update_status`, `update_priority`, `update_two_ids`, `update_last_touched`, `scn_lifecycle`, `scn_last_touched` |
| S5.214 | Below the header, one indented line per changed field, two leading spaces, in this fixed order and only for fields that actually changed: `  status: {old} → {new}`, `  priority: P{old} → P{new}`, `  type: {old} → {new}`, `  assignee: {old} → {new}`, `  owner: {old} → {new}`. The arrow is `→` U+2192 with a space on each side | `update.rs:1283-1323` | `update_status`, `update_priority`, `update_two_ids`, `scn_lifecycle` |
| S5.215 | An absent old or new assignee/owner renders as the literal `(none)` | `update.rs:1302-1320` | `scn_lifecycle` (`  assignee: (none) → tester`) |
| S5.216 | Changes to title, description, labels, notes, due date, estimate and every other field produce **no** change line: only the five fields of S5.214 are reported, so a pure title change shows just the header carrying the new title | `update.rs:1283-1323` | (case to add: `update_title_plain` — `["@fx=basic","update","proj-mta","--title","Renamed"]`) |
| S5.217 | Several ids in one invocation print their blocks back to back with **no blank line between them**, in the order the ids were given | `update.rs:1417-1437` | `update_two_ids` |
| S5.218 | When a requested update changes nothing at all, the line is `No updates specified for {id}` | `update.rs:1335-1337,1435` | (case to add: `update_noop_plain` — `["@fx=basic","update","proj-mta","-p","1"]`; the existing `update_noop_same_value` uses `--json`) |
| S5.219 | `--claim` renders as the two change lines `  status: open → in_progress` and `  assignee: (none) → {actor}`; a blocked claim instead fails with `Error: Validation failed: claim: cannot claim blocked issue: {blocker ids}` on stderr, exit 4, stdout empty | `update.rs:1250-1277`, `scn_lifecycle` step 2 | `scn_lifecycle`, `error_update_blocked_claim` |

---

### S5.220–S5.228 — `close`, `reopen`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.220 | `br close` prints one stdout line per closed issue: `✓ Closed {id}: {title} ({reason})`. The reason is the stored close reason, which defaults to `done` when `--reason` was not given | `close.rs:453-464,763` | `close_basic` (`(done)`), `close_two`, `scn_lifecycle` (`(Schema done)`), `scn_last_touched` (`(Finished)`) |
| S5.221 | Several ids close in argv order, one line each, with no separator line | `close.rs:762-764` | `close_two` |
| S5.222 | A skipped issue prints `Warning: Skipped {id}: {reason}` on **stderr** (one line per skip), and the reason text is the same string that the terminal error then repeats | `close.rs:466-470,765-767` | `close_already_closed`, `close_epic_with_open_child`, `error_close_blocked` |
| S5.223 | When every requested id was skipped, the run fails: stdout is empty and stderr holds the `Warning:` line(s), then `Error: Nothing to do: all {n} issue(s) skipped — {id}: {reason}[; …][; +{k} more]` (the summary lists at most five id/reason pairs joined by `; `, with a `+{k} more` tail beyond that, and the dash is `—` U+2014), then the matching `Hint:` line. Exit 3 | `close.rs:790-812,812-840` | `close_already_closed`, `close_epic_with_open_child`, `error_close_blocked` |
| S5.224 | When a batch closed some ids and skipped others the run also fails (`CloseIncomplete`), so the `✓ Closed` lines are on stdout **and** a non-zero exit is returned | `close.rs:795-805` | (case to add: `close_mixed` — `["@fx=basic","close","proj-170","proj-mta"]`) |
| S5.225 | `br close --suggest-next` in plain mode appends, after the close lines, a blank line, then `Unblocked {n} issue(s):` on stdout, then one line per newly unblocked issue as `  {id}: {title}` | `close.rs:770-778,472-477` | (case to add: `close_suggest_next_plain` — `["@fx=basic","close","proj-mta","--suggest-next"]`; the existing `close_suggest_next` uses `--json`) |
| S5.226 | A close that matches nothing at all prints `No issues to close.` on stdout, exit 0 | `close.rs:759-761` | (case to add: `close_no_targets`) |
| S5.227 | `br reopen` prints `✓ Reopened {id}: {title}` per reopened issue, extended with ` ({reason})` when `--reason` was given. A skipped issue prints `⊘ Skipped {id}: {reason}` (U+2298) on **stdout** — not stderr — and the run still exits 0. With nothing to do it prints `No issues to reopen.` | `reopen.rs:168-192,427-429` | `reopen_closed`, `reopen_open` (`⊘ Skipped proj-mta: already open`), `scn_last_touched` |
| S5.228 | `close` and `reopen` differ in how they report a skip: `close` uses the warning channel (stderr, `Warning: Skipped …`) and turns it into a terminal error; `reopen` uses a raw stdout line with the `⊘` glyph and exit 0 | `close.rs:765` vs `reopen.rs:181-189` | `close_already_closed` vs `reopen_open` |

---

### S5.229–S5.235 — `defer`, `undefer`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.229 | `br defer` prints one stdout line per deferred issue: `⏱ Deferred {id}: {title} (until {defer_until})` when a target instant was resolved, and `⏱ Deferred {id}: {title} (indefinitely)` otherwise. `⏱` is U+23F1 with no variation selector | `defer.rs:196-210` | `defer_no_until` (`(indefinitely)`) |
| S5.230 | The `(until …)` value is the resolved instant in the same RFC 3339 form the JSON path emits, i.e. with an explicit `+00:00` offset rather than `Z` (`2027-06-01T09:00:00+00:00`, `2026-01-02T04:04:05+00:00` for `--until +1h`), not a `%Y-%m-%d` date | `defer.rs:205`, `defer_until_date.out` / `defer_relative.out` JSON bodies | (case to add: `defer_until_plain` — `["@fx=basic","defer","proj-mta","--until","2027-06-01"]`) |
| S5.231 | A skipped defer prints `⊘ Skipped {id}: {reason}` on stdout, exit 0; with nothing to do, `No issues to defer.` | `defer.rs:211-219` | (case to add: `defer_skipped`) |
| S5.232 | `br undefer` prints `✓ Undeferred {id}: {title} (now {status})` per issue | `defer.rs:504-512` | (case to add: `undefer_plain` — `["@fx=basic","undefer","proj-7vm"]`; the existing `undefer_basic` uses `--json`) |
| S5.233 | An undefer of an issue that is not deferred is a skip, not an error: stdout gets `⊘ Skipped {id}: not deferred (status: {status})`, exit 0 | `defer.rs:513-517` | `undefer_not_deferred` |
| S5.234 | With nothing to undefer, stdout gets `No issues to undefer.` | `defer.rs:518-520` | (case to add: `undefer_no_targets`) |
| S5.235 | An unparseable `--until` never reaches the renderer: stdout stays empty and stderr carries `Error: Validation failed: defer_until: invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)`, exit 4 | `src/error/structured.rs:606` | `error_defer_bad_until` |

---

### S5.236–S5.244 — `delete`

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.236 | A completed `br delete` prints on stdout `Deleted {n} issue(s):` — no glyph, with a colon — then one line per id as `  - {id}`, then, only when greater than zero, `Removed {n} dependency link(s)` | `delete.rs:363-370` | `delete_basic`, `delete_with_dependents_force` |
| S5.237 | With `--force`, any dependent left without its blocker is reported after the removal count as `Orphaned {n} issue(s):` followed by one `  - {id}` line each | `delete.rs:371-376` | `delete_with_dependents_force` |
| S5.238 | A second delete renderer exists for multi-workspace routing and differs in bytes (`✓ Deleted {n} issue(s)` via the success channel, without the colon, with `Removed …` as an info line and `Orphaned …` as a stderr warning). No `--no-db` single-workspace case reaches it; the S5.236/S5.237 form is what the goldens show and what the port reproduces | `delete.rs:591-610` (routed) vs `delete.rs:363` (local) | `delete_basic` (local form) |
| S5.239 | `br delete --dry-run` prints `Dry-run: Would delete {n} issue(s):` then one line per issue as `  - {id}: {title}` (id **and** title, unlike the applied form), and writes nothing | `delete.rs:240-250` | `delete_dry_run` |
| S5.240 | Deleting an issue that others depend on, without `--force` and without `--cascade`, is a **preview** that changes nothing and exits 0. Its stdout block is, in order: `The following issues depend on issues being deleted:`; one `  - {id}` line per direct dependent; then, only when the transitive closure is larger than that set, a blank line, `{k} additional issue(s) would be transitively affected by --cascade:` and one `  - {id}` line per additional id; then a blank line; then `Use --force to orphan these dependents, or --cascade to delete them recursively.`; then, when the closure is non-empty, `--cascade would delete {m} total dependent(s).`; then `No changes made (preview mode).` | `delete.rs:174-202` | `delete_with_dependents` |
| S5.241 | In that preview `{k}` is the closure size minus the direct-dependent count and `{m}` is the whole closure size — in the golden, 1 additional and 2 total | `delete.rs:178-181,195-199` | `delete_with_dependents` |
| S5.242 | `br delete --hard` with no ids purges tombstones and prints `Dry-run: Would purge {n} tombstone(s):` + `  - {id}` lines under `--dry-run`, `Dry-run: no tombstones to purge.` when there are none, and `✓ No tombstones to purge.` when a real purge finds none | `delete.rs:415-425,464-470` | (case to add: `delete_hard_purge_empty` — `["@fx=basic","delete","--hard"]`) |
| S5.243 | `br delete` with no ids at all is a validation failure, not an empty run: stderr `Error: Validation failed: ids: no issue IDs provided`, exit 4, stdout empty. (`show`, `update` and `close` instead fall back to `.beads/last-touched` and fail only when it is empty, with `… and no last-touched issue`.) | `delete.rs` id resolution; `src/error/structured.rs:606` | `usage_delete_alone`, `usage_show_alone`, `usage_update_alone`, `usage_close_alone` |
| S5.244 | A delete naming an unknown id prints nothing on stdout; stderr carries `Error: Issue not found: {id}` + `Hint: Run 'br list' to see available issues.`, exit 3 | `src/error/structured.rs:606` | `error_delete_missing` |

---

### S5.245–S5.250 — Cross-cutting recap and negative space

| S5.n | clause | provenance | cases |
|---|---|---|---|
| S5.245 | Complete inventory of glyphs that can appear in in-scope plain output, with meanings: `○` U+25CB open status / non-eligible epic · `◐` U+25D0 in progress · `●` U+25CF blocked status **and** the fixed bullet inside every priority badge · `❄` U+2744 deferred or draft · `✓` U+2713 closed status, eligible epic, and the success-line prefix · `✗` U+2717 tombstone (and the batch-create failure prefix on stderr) · `📌` U+1F4CC pinned · `?` unknown/custom status · `📋` U+1F4CB ready header · `🚫` U+1F6AB blocked header · `✨` U+2728 the "nothing to do" lines of `ready` and `blocked` · `📊` U+1F4CA stats header · `⏱` U+23F1 defer · `⊘` U+2298 reopen/defer/undefer skip · `·` U+00B7 `show` field separator · `→` U+2192 update field changes, duplicate/missing dependency messages · `—` U+2014 the `ready` empty line and the close "Nothing to do" summary | `src/format/text.rs:15-32`; `ready.rs:246,294`; `blocked.rs:388,393`; `stats.rs:1223`; `defer.rs:198`; `reopen.rs:183`; `update.rs:1286`; `dep.rs:649`; `show.rs:1299` | `list_all`, `ready_plain`, `blocked_plain`, `empty_ready`, `stats_plain`, `defer_no_until`, `reopen_open`, `update_status`, `dep_add_duplicate`, `show_plain` |
| S5.246 | Plain mode never emits an ANSI escape, a box-drawing table, a panel border or a progress spinner: those belong to Rich. The only box-drawing characters any in-scope plain path can emit are the `├──`/`└──`/`│` connectors of `list --tree`, `list --pretty` and `dep tree` | `src/output/context.rs:947` (Plain is a bare `println!`); `list.rs:826-829`; `dep.rs:1760` | `list_plain`, `dep_tree` |
| S5.247 | No in-scope plain command prints a column-aligned table; the only alignment in the entire plain surface is `stats`' fixed label padding (S5.168) and the capacity block's width specifiers (S5.172) | `stats.rs:1228,1315` | `stats_plain` |
| S5.248 | `[note] …` is the exact prefix of the stderr truncation notes of `list`, `ready` and `blocked` (square brackets, lower-case `note`, one space). `search`'s equivalent uses the different prefix `note: ` and goes to **stdout** (S5.160) | `list.rs:197`, `ready.rs:262`, `blocked.rs:217`, `search.rs:362` | `ready_limit` |
| S5.249 | Numbers in plain text are always plain decimal: no thousands separators, no padding, no sign for zero. The only fractional number in the plain surface is `stats`' average lead time, printed with exactly one fractional digit (S5.170) | `count.rs:109`, `stats.rs:1256-1260` | `count_plain`, `stats_plain` |
| S5.250 | Output is state-dependent, and the scenario goldens are the contract for that: the same argv produces different bytes after a mutation (`ready` gains and loses rows, `epic status` flips its glyph and gains the eligibility line, `blocked` shrinks its header count, `stats` moves from `2.0 hours` to `23.2 days`) | `goldens/scenarios/lifecycle.scn:4`–`goldens/scenarios/lifecycle.scn:16` (the steps). `ready`: `goldens/scn_lifecycle.out:2` → `goldens/scn_lifecycle.out:35` (gains `proj-170`, loses `proj-mta`) → `goldens/scn_lifecycle.out:67`. `epic status`: `goldens/epic_status.out:1` (`○`) → `goldens/scn_lifecycle.out:61` (`✓`) with `goldens/scn_lifecycle.out:63` (`Eligible for closure`). `blocked`: `goldens/blocked_plain.out:2` (`(2)`) → `goldens/scn_lifecycle.out:41` (`(1)`). `stats`: `goldens/stats_plain.out:13` (`2.0 hours`) → `goldens/scn_lifecycle.out:96` (`23.2 days`) | `scn_lifecycle`, `scn_child_ids`, `scn_last_touched`, `scn_dup_title` |
| S5.251 | Coverage clause for the plain failure surface: in every captured non-JSON failing run, **stdout is 0 bytes** and the whole report is on stderr in the two-line `Error:` / `Hint:` shape of S5.105 (a `Hint:` line is present only when the error carries one; `error_conflict_markers` instead carries a multi-line message whose continuation lines are part of the message text). No partial plain output precedes a failure in any of them | `src/error/structured.rs:606`, `src/main.rs:1893` | `error_close_missing`, `error_comments_add_empty`, `error_comments_add_missing`, `error_conflict_markers`, `error_dep_add_cycle`, `error_dep_add_missing`, `error_dep_add_self`, `error_label_add_invalid`, `error_no_workspace_create`, `error_no_workspace_list`, `error_reopen_missing`, `error_update_ambiguous`, `error_update_bad_priority`, `error_update_missing`, `error_create_bad_priority`, `error_defer_bad_until`, `error_update_blocked_claim`, `list_sort_bad`, `list_status_bad` |
| S5.252 | Argument-parser failures are a different surface: the message is lower-case `error: …`, it is followed by a blank line, a `Usage: …` line, another blank line and `For more information, try '--help'.`, all on **stderr** with stdout empty and exit 2. Those bytes are generated by the argument-parser library and belong to S1; this part records only the stream, the empty stdout and the fact that they never mix with the `Error:`/`Hint:` shape | `src/main.rs` clap dispatch; `src/error/structured.rs:606` (not used on this path) | `usage_none`, `usage_unknown_cmd`, `usage_unknown_flag`, `usage_empty_arg`, `usage_limit_noval`, `usage_limit_bad`, `usage_unexpected_flag`, `usage_dep_alone`, `usage_label_alone`, `usage_epic_alone`, `usage_search_alone`, `usage_dep_add_one_arg` |

---


## S6. Order-leak inventory

<!-- from docs/spec-parts/S6_S8_S10.md -->

Every place where an iteration or insertion order reaches an output on an
in-scope `--no-db` path.

**Determinism evidence.** `scripts/floor.sh --repeat 3` found exactly one
unstable case among the 231 captured cases (`show_partial_ambiguous`,
DISC-005). In addition, this extraction ran the oracle 5–8 times per probe
through `scripts/ws-run.sh --oracle` over outputs the corpus does not exercise
— `count --by-label/--by-assignee/--by-type/--by-status/--by-priority` (plain
and `--json`), `label list-all` (plain and `--json`), `stats` (plain and
`--json`), `blocked` (plain and `--json`), `dep cycles` (plain and `--json`),
`epic status --json`, `epic close-eligible`, `dep tree … --json`,
`dep list … --json`, `ready --json`, `list --json`, `search … --json`,
`close … --json`, `update … --claim`, `delete … --cascade --json`,
`close … --suggest-next --json`, `label rename … --json`,
`update … --set-labels z,a,m,b --json`, `create … -l zeta,alpha,mid,beta --json`,
`show <target with 18 tied dependent edges>` — plus the same probe set against a
24-issue store and a 20-issue synthetic store built inside the sandbox. Every
one of them was byte-identical across repeats. The single hash-random leak in
`--no-db` mode remains the ambiguous-partial-id candidate list (S6.31).

| S6.n | structure | order the original exhibits | reaches output via | provenance | cases |
|---|---|---|---|---|---|
| S6.1 | the `--no-db` backing store | `--no-db` is not an in-memory Rust collection: it opens a throwaway on-disk SQLite file and bulk-imports the whole JSONL into it on every invocation. Rows are inserted in JSONL line order, so SQLite rowid order equals file line order | every command; a `SELECT` without `ORDER BY` therefore yields JSONL line order | `src/config/mod.rs:5369`, `:5378` (`open_memory`), `:5415` (import); `src/storage/sqlite.rs:2694`–`:2714` | `list_plain`, `list_json` |
| S6.2 | JSONL write-back line order Measured (2026-09-23, OQ-079): `proj-uly.1`, `proj-uly.10`, `proj-uly.11`, `proj-uly.2` (`scn_child_export_order`). | `id` ascending in **byte** (BINARY) order; the `id` column is declared without a collation, so no case folding. Records whose `ephemeral` is true and ids containing `-wisp-` are excluded from the file entirely | `.beads/issues.jsonl` after every mutation | `src/sync/mod.rs:10613`–`10619`; `src/storage/sqlite.rs:14995`–`15006`; `src/storage/schema.rs:243` | `create_min` (new `proj-fyw` lands between `proj-b75` and `proj-mkh`), `create_ephemeral` (no `issues.jsonl` dump at all — only `last-touched` changed), `defer_until_date`, scn_child_export_order |
| S6.3 | `labels` array inside an exported record | byte-ascending and deduplicated, **case-sensitive** (`Mid` before `alpha` before `zeta`); the incoming array's order and duplicates are discarded | every rewritten record in `.beads/issues.jsonl` | `src/sync/mod.rs:12293`–12296 (sort then dedup) | `edge_precision_rewrite` (input `["zeta","alpha","alpha"]` comes back `["alpha","zeta"]`), `update_set_labels` (`z,a,m,b` stored ascending); (case to add: `edge_labels_mixed_case_rewrite` — `["Zulu","alpha","ALPHA","_under","9nine"]` in a fixture, then `update <id> -p 1`) |
| S6.4 | `dependencies` array inside an exported record | seven keys in order: `issue_id`, `depends_on_id`, `type`, `created_at`, `created_by`, `metadata`, `thread_id`, each ascending. The key is total, so no tie remains | every rewritten record | `src/sync/mod.rs:12300` | `create_with_parent`, `create_with_deps`, `dep_add_blocks`, `dep_add_related` |
| S6.5 | `comments` array inside an exported record | five keys in order: `issue_id`, `created_at`, `author`, `body`, `id`, each ascending. `created_at` before `id`, so an out-of-order comment id sorts by time first | every rewritten record | `src/sync/mod.rs:12311` | `comments_add`, `comments_add_second`, `comments_add_author` |
| S6.6 | `list` default order (no `--sort`) | `priority` ascending, then `created_at` **descending**, then `id` ascending. Stable in effect because the key is total (ids are unique). The Rust twin uses an unstable sort, which is safe for the same reason | stdout, plain and `--json` | `src/storage/sqlite.rs:18248` (`sort_list_default`), `:8475`, `:8481` (SQL twin) | `list_plain` (P1 group `proj-170` at 11:00 then `proj-mta` at 10:00), `list_all` (P2 group `proj-mkh` 13:00, `proj-b75` 10:00:06, `proj-uly` 10:00:04), `list_json`, `empty_list` |
| S6.7 | `list --sort priority` | `priority` ascending, then `created_at` descending, then `id` ascending — identical to the default | stdout | `src/storage/sqlite.rs:8458` | (case to add: `list_sort_priority` — `["@fx=basic","list","--sort","priority"]`) |
| S6.8 | `list --sort created_at` | `created_at` **descending** (newest first), then `id` ascending. The tie-break is id ascending even though the primary key is descending | stdout | `src/storage/sqlite.rs:8464` | `list_sort_created` (`b75` 10:00:06 first, `mta` 10:00:00 last), `edge_precision_sort_created` (`aab` `…05.100Z` before `aad` `…05.1Z` — equal instants, id ascending) |
| S6.9 | `list --sort updated_at` | `updated_at` descending, then `id` ascending | stdout | `src/storage/sqlite.rs:8467` | `list_sort_updated` (`proj-170` and `proj-5u2` both at 11:00:00, emitted id-ascending) |
| S6.10 | `list --sort title` Measured (2026-09-23, OQ-083): `apple`, `hotel`, `jackal`, `ZULU`, `İstanbul`, in `list` and in `search` with and without `--offset` (`scn_title_fold`). | title ascending under **ASCII-only** case folding (SQLite `COLLATE NOCASE` folds `A`–`Z` only), then byte order, then `id` ascending. Non-ASCII is compared by raw UTF-8 bytes and is never folded | stdout | `src/storage/sqlite.rs:8471` | `list_sort_title` (`Ünïcödé …` last), `edge_precision_sort_title` (`UPPER case title` between `one tenth short form` and `whole second`); probe: titles `apple match`, `hotel match`, `jackal match`, `ZULU match`, `İstanbul match` come out in exactly that order — full-Unicode folding would place `İstanbul` between `hotel` and `jackal`, so the fold is ASCII-only. (case to add: `edge_title_fold_ascii_only` on a fixture holding those five titles, `list --sort title`), scn_title_fold |
| S6.11 | `list --reverse` | reverses the primary and the secondary key and keeps `id` **ascending** as the tie-break: `priority` descending, `created_at` ascending, `id` ascending | stdout | `src/storage/sqlite.rs:8479` | `list_reverse` (P1 group `proj-mta` then `proj-170` — the reverse of the default P1 group; P2 group `proj-uly` then `proj-b75`) |
| S6.12 | `ready` default (`--sort hybrid`) | a two-value bucket first — `0` when `priority ≤ 1`, `1` otherwise — then `created_at` ascending, then `id` ascending. This is why a P3 can precede a P2 | stdout, plain and `--json` | `src/storage/sqlite.rs:18186` (`sort_ready_hybrid`), `:18257` (the bucket), `:10336` (SQL twin) | `ready_plain` (`mta` P1, then `5u2` P3 at 10:00:03, then `b75` P2 at 10:00:06), `ready_json`, `edge_precision_ready` (all P2, so pure `created_at` ascending with an id tie-break on the two equal instants), `empty_ready` |
| S6.13 | `ready --sort priority` | `priority` ascending, then `created_at` ascending, then `id` ascending. Note `created_at` **ascending** here, where `list --sort priority` uses descending | stdout | `src/storage/sqlite.rs:18312`, `:10341` | `ready_sort_priority` (P1, P2, P3) |
| S6.14 | `ready --sort oldest` | `created_at` ascending, then `id` ascending; no priority term | stdout | `src/storage/sqlite.rs:18318`, `:10345` | `ready_sort_oldest` (`mta`, `5u2`, `b75` — identical to the hybrid order on this fixture because the single P1 is also the oldest) |
| S6.15 | `blocked` row order Measured (2026-09-23): at equal priority the issue with two blockers (created later) lists before the one with one blocker; this case does not separate the blocker count from `created_at` (OQ-082 stays open) (`scn_blocked_tiebreak`). Measured (2026-09-23, OQ-082): the blocker count decides before `created_at` (`scn_blocked_count_first` with `scn_blocked_tiebreak`). | `priority` ascending, then the count of open blockers **descending**, then `created_at` descending, then `id` ascending. This is a *different* order from the storage query, so `blocked` never renders in storage order | stdout, plain and `--json` | `src/cli/commands/blocked.rs:318` (`sort_blocked_issues`), applied at `:207` for every format; storage order `src/storage/sqlite.rs:11693` | `blocked_plain`, `blocked_json`; probe on a synthetic store: `[P0 1 blocker]`, `[P2 3 blockers]`, `[P2 1 blocker]`, `[P4 2 blockers]`, `[P4 1 blocker]` — priority beats the count, the count beats the id. (case to add: `edge_blocked_count_tiebreak` on a fixture with two P4 issues blocked by 2 and 1 open issues), scn_blocked_tiebreak, scn_blocked_count_first |
| S6.16 | the blocked-by list of one row | ascending byte order of the blocker id, deduplicated. Internally the sort runs over the composite string `<id>:<status>` and the status suffix is stripped afterwards, so two blockers sharing an id prefix order by id then status | `Blocked by N open dependencies: [a, b, c]` and the `"blocked_by"` array | `src/storage/sqlite.rs:10996`–10998, `:11336`; strip at `:11130` | `blocked_plain`, `blocked_json`; probe: edges stored `b04, b01, b03, b02` render `[proj-b01, proj-b02, proj-b03, proj-b04]`; external ids render `[external:aa-1, external:mm-5, external:zz-9]`. (case to add: `edge_blocked_by_sorted` on a fixture whose dependency array is stored out of id order) |
| S6.17 | `search` row order | identical to `list`: default `priority` ascending, `created_at` descending, `id` ascending; the `--sort` ladder is the same, including ASCII-only title folding | stdout, plain and `--json` | `src/storage/sqlite.rs:9718`–9749 | `search_plain` (`proj-170` P1 then `proj-uly` P2), `search_json`, `search_case_insensitive`, `search_status`, `search_unicode`, `search_none`, `empty_search` |
| S6.18 | `count --by-<key>` group order | groups are accumulated into a byte-ordered map and emitted in ascending byte order of the group label. The synthetic buckets `(no labels)` and `(unassigned)` sort first because `(` is byte `0x28`, below every letter and digit — they are not special-cased to the front | stdout, plain and `--json` | `src/cli/commands/count.rs:271`, `:480`, `:309`–318 (synthetic bucket); `src/storage/sqlite.rs:9370`, `:9391` | `count_by_label` (`(no labels)`, `auth`, `backend`, `db`), `count_by_assignee` (`(unassigned)`, `alice`, `bob`), `count_by_type` (`bug`, `docs`, `epic`, `feature`, `task`), `count_by_status` (`in_progress`, `open`) |
| S6.19 | `count --by-priority` group labels | the group key is the rendered string `P0`…`P4`, so the byte order of the label coincides with numeric order over 0–4 | stdout and the `"groups"` array | `src/cli/commands/count.rs:282`; `src/model/mod.rs:168` (the `P{n}` rendering) | `count_by_priority_json` (`P0`, `P1`, `P2`, `P3`; `P4` absent because the only P4 is deferred and excluded by default) |
| S6.20 | `label list <id>` | byte ascending, case-sensitive, from the labels of that one issue | stdout and the `--json` array | `src/storage/sqlite.rs:13665` (`ORDER BY label`) | `label_list` (`backend`, `db`), `label_list_json`; probe on `["zeta","alpha","zeta","Mid"]` renders `Mid`, `alpha`, `zeta` — uppercase first, duplicate collapsed |
| S6.21 | `label list-all` | byte ascending over the distinct labels; the counting scan itself is unordered but drains into a byte-ordered map, so no hash order escapes | stdout and the `--json` array of `{label,count}` | `src/storage/sqlite.rs:13885`–13917 | `label_list_all`, `label_list_all_json`; 6 repeats on an 8-issue store and 5 repeats on a 24-issue store with 25 distinct labels were byte-identical |
| S6.22 | `comments list <id>` | `created_at` ascending, then `id` ascending | stdout and the `--json` array | `src/storage/sqlite.rs:14002`–14012 | `comments_list`, `comments_list_json`, `comments_list_empty` |
| S6.23 | `epic status` / `epic close-eligible` epic order | the `list` default order over the epic-typed issues: `priority` ascending, `created_at` descending, `id` ascending. Children are counted, never enumerated, so the child-count map's iteration order never reaches an output | stdout and the `--json` array | `src/cli/commands/epic.rs:260`–276 | `epic_status`, `epic_status_json`; probe with three epics `proj-e1`, `proj-e2`, `proj-e3` (equal priority and timestamps) renders them id-ascending over 8 repeats |
| S6.24 | `dep list <id>` **plain text** order | `priority` ascending, then `issue_id` ascending, then `depends_on_id` ascending, then dependency type ascending. Applied only to the human renderer, after the JSON branch has already returned | stdout in Plain mode | `src/cli/commands/dep.rs:1239` (`sort_dep_list_items_for_human`), branch at `:1001`–1012 | `dep_list`, `dep_list_none`; probe: three equal-priority dependencies render `proj-v1`, `proj-v2`, `proj-v3` (id order) |
| S6.25 | `dep list <id> --json` order Measured (2026-09-23, OQ-080): three P2 targets with equal `created_at`, added in the order `.3`, `.1`, `.2`, list as `.1`, `.2`, `.3` in both forms (`scn_dep_list_order`). | a **different** order from S6.24: `COALESCE(priority,2)` ascending, then the depended-on issue's `created_at` **descending**, then `depends_on_id` ascending | the `--json` array | `src/storage/sqlite.rs:14179` (dependencies), `:14202` (dependents) | `dep_list_json` (indistinguishable on `basic` because priority already separates the two rows); probe with three equal-priority dependencies created at `10:00:01`, `10:00:03`, `10:00:02` renders `proj-v2`, `proj-v3`, `proj-v1` in JSON but `proj-v1`, `proj-v2`, `proj-v3` in text. (case to add: `edge_dep_list_json_vs_text` — a fixture with three same-priority dependencies whose `created_at` order differs from their id order, then both `dep list <id>` and `dep list <id> --json`), scn_dep_list_order |
| S6.26 | `show <id>` dependency and dependent lists | `priority` ascending, then `created_at` descending, then the other issue's `id` ascending. Same key as S6.25 — so `show <id>` and `dep list <id>` (plain) disagree on the same data | the `Dependencies:` and `Dependents:` blocks and the `--json` `dependencies`/`dependents` arrays | `src/cli/commands/show.rs:881`, `:889`, `:964` | `show_plain`, `show_epic`, `show_two`, `show_partial_hash`; probe: `show proj-src` renders `proj-v2`, `proj-v3`, `proj-v1` while `dep list proj-src` renders `proj-v1`, `proj-v2`, `proj-v3` |
| S6.27 | ties between several edges of one pair in `show` | when one issue holds more than one edge to the same target, all four sort keys tie and the sort is stable, so the edges keep the order of the record's own `dependencies` array — file order, not a hash order | the `Dependents:` block | `src/cli/commands/show.rs:940`–968 | probe: a target with six dependents each holding `blocks`, `parent-child` and `related` edges renders eighteen lines grouped id-ascending and, inside each group, `blocks`, `parent-child`, `related` — byte-identical over 8 repeats for both `show proj-tgt` and the bare-hash `show tgt`. The candidate map is a hash map, but the stable sort by id makes its order unobservable here. (case to add: `edge_show_multi_edge_dependents` on such a fixture) |
| S6.28 | `dep tree <id>` sibling order | `priority` ascending (a missing neighbour sorts last), then a status rank `open` `0`, `in_progress` `1`, `blocked` `2`, `deferred` `3`, `closed` `4`, `deleted`/`tombstone` `5`, anything else `6` (missing last), then title ascending, then `id` ascending. Emission is depth-first, the first sorted child emitted first; node keys are numbered in emission order | stdout and the `--json` array | `src/cli/commands/dep.rs:1809`–1851; traversal `:1555`, `:1670` | `dep_tree`, `dep_tree_json`, `dep_tree_leaf`; note `dep tree` follows the `depends_on` direction, so an epic with children as *dependents* renders as a single root line (`dep_tree` on `proj-uly`) |
| S6.29 | `dep cycles` cycle order and the order inside a cycle | each cycle is emitted starting from its lowest member and closing back on it; the list of cycles is ascending by that path. Every adjacency list and component is sorted before use, so no hash order survives | `Warning: Found N dependency cycle(s):` lines on stderr, and the `"cycles"` array Amended (2026-09-24, OQ-081): measured as stated; the probe store is fixture `cycle_two`. | `src/storage/sqlite.rs:19180`–19307, `:19170`; `src/cli/commands/dep.rs:1989` | `dep_cycles`, `dep_cycles_json`, `empty_dep_cycles`; probe with a 3-cycle `c01→c02→c03→c01` and a 2-cycle `d01→d02→d01` renders the 3-cycle first, byte-identical over repeats. (case to add: `edge_dep_cycles_two` on a fixture holding both cycles), edge_dep_cycles_two_json |
| S6.30 | `delete --cascade` deleted-id list | ascending byte order of the id | the `"deleted"` array and the `Deleted N issue(s):` block | `src/cli/commands/delete.rs:926` (the cascade set is sorted), `:307` (the final delete set is sorted, and ids are pushed onto `deleted` in that order), `:362`–363 (the text block walks `deleted` in order); `goldens/delete_cascade.out:1` | `delete_cascade` (`["proj-170","proj-mta","proj-uly"]`), `delete_basic`, `delete_with_dependents_force` |
| S6.31 | candidates of an ambiguous partial id | **hash-random per process.** The candidate list is produced by iterating the keys of a standard hash map and is handed to the error unsorted. Eight runs of `show m` on `basic`, captured outside the canonicalizer, produced four distinct orders: `["proj-7vm", "proj-mta", "proj-mkh"]` (3), `["proj-mkh", "proj-7vm", "proj-mta"]` (2), `["proj-7vm", "proj-mkh", "proj-mta"]` (2), `["proj-mta", "proj-7vm", "proj-mkh"]` (1). The same list appears in the inline message, in `context.matches` and in the `match_count`. The exit code `3` and every other byte are stable | `Error: Ambiguous ID 'm': matches [...]` on stderr; the `message` and `context.matches` of the JSON envelope on stdout | `src/cli/commands/show.rs:1094`–1106 (the key iteration); `src/util/id.rs:910`, `:995` (no sort); `src/error/mod.rs:69`; `src/error/structured.rs:462`, `:671` | `show_partial_ambiguous`, `error_show_ambiguous_json`, `error_update_ambiguous` — all three captured through the canonicalizer of `scripts/ws_inner.py`, which sorts only this list. DISC-005; the port emits ascending byte order in all three places |
| S6.32 | JSON object key order | keys follow the Rust struct field declaration order for `list`, `search`, `show`, `create`, `update`, `close`, `defer`, `epic status`, `dep list`, `comments`, `where`, `version` and every exported record. The `ready` and `blocked` per-issue projections are the exception: their keys are **alphabetical** | every `--json` output and every JSONL line | `src/model/mod.rs` (field order); `src/output/context.rs:1039`, `:1054` | struct order: `list_json` starts `id`, `title`, `status`, `priority`, `issue_type`, `assignee`, `created_at`, …; alphabetical: `ready_json` starts `created_at`, `created_by`, `description`, `id`, `issue_type`, `labels`, `priority`, `status`, `title`, `updated_at`, and `blocked_json` starts `blocked_by`, `blocked_by_count`, `created_at`, … |
| S6.33 | comment id allocation | a comment's `id` is one past the largest comment id anywhere in the store, not per issue. Adding the first comment to an issue in `basic` yields `2` because `proj-ptp` already holds comment `1` | the `"id"` of the `comments add` result and of the stored comment | `src/storage/sqlite.rs:20978` (the insert) | `comments_add_json` (`{"id":2,…,"issue_id":"proj-mta"…}` on an issue with no comments), `comments_add_second` (`{"id":2,…,"issue_id":"proj-ptp"…}`), `comments_add_author`, `actor_flag_comment` |
| S6.34 | `stats` average lead time Measured (2026-09-23, OQ-090): each lead time is truncated to whole hours before the average: 1 s, 79 h 13 m 11 s and 35 h 22 m 33 s average to `38.0` (`scn_lead_times`). Measured (2026-09-23, OQ-078): `1.5` and `1.3333333333333333` in JSON; `1.5 hours`, `1.3 hours` in plain mode. | the per-issue lead times are summed in the order the unordered scan returns them, which in `--no-db` is JSONL line order. Floating-point addition is not associative, so the reduction order is part of the contract. Each per-issue lead time is first truncated to whole hours, which hides the effect on the corpus | `Avg Lead Time:` and `"average_lead_time_hours"` | `src/cli/commands/stats.rs:426`, `:477`; scan `src/storage/sqlite.rs:9002` | `stats_plain` (`2.0 hours` — one closed issue, 2h59m52s truncated to 2), `stats_json`; probe with three closed issues of 1s, 79h13m11s and 35h22m33s yields `38.0`, i.e. `(0 + 79 + 35) / 3` after truncation, not `38.2`, scn_lead_times, scn_lead_time_half, scn_lead_time_third |
| S6.35 | output order for a command given several ids | the argv order of the ids, restored explicitly after the storage layer returns them in an order that does not match the request | stdout, plain and `--json` | `src/cli/commands/show.rs:407`–453 and the same helper in `label.rs`, `update.rs`, `close.rs`, `defer.rs`, `reopen.rs`; unordered fetch at `src/storage/sqlite.rs:8194` | `show_two` (`proj-mta` then `proj-5u2`, the argv order, not the id order), `update_two_ids` (`proj-mta` then `proj-5u2`), `close_two` |
| S6.36 | scenario step order and the state dumps | each step's stdout is followed by a dump of `.beads/issues.jsonl` and then `.beads/last-touched`, in that fixed order, and only for the files the step changed; the harness writes the step header to both stdout and stderr | the captured scenario goldens | `scripts/ws_inner.py:133` (`dump_changes`), `:23` (`STATE_FILES`, which fixes the `issues.jsonl`-then-`last-touched` order), `:136` (a file is dumped only when its hash moved), `:208`–209 (the step header goes to both streams); `goldens/scn_lifecycle.out:8`, `:17` (the two dumps, in that order), `goldens/scn_lifecycle.err:2` | `scn_lifecycle`, `scn_last_touched`, `scn_dup_title`, `scn_child_ids` |
| S6.37 | `list --limit N` paging path | with a limit and no other filter the rows are fetched one priority bucket at a time, each bucket ordered `created_at` descending then `id` ascending, and the buckets concatenated `P0`…`P4`. The result equals the global default order because priority is the leading key | stdout | `src/storage/sqlite.rs:8512`–8557, `:8546` | `list_limit_one`, `list_limit_offset`, `ready_limit` |
| S6.38 | `show <id> --json` label array | **not normalized**: the labels come back exactly as the JSONL holds them, unsorted and with duplicates intact, unlike every other reader | the `"labels"` array of `show --json` | `src/cli/commands/show.rs:487`, `:506` (the JSONL-direct branch) | `edge_precision_show_json` renders `"labels":["zeta","alpha","alpha"]` while `edge_precision_rewrite` writes `["alpha","zeta"]` for the same record |
| S6.39 | every `list` filter keeps the S6.6 default order | a filter removes rows; it never reorders the survivors, and the same tie-break applies to the filtered set | stdout, plain and `--json` | `src/storage/sqlite.rs:8475` (the filter clauses precede the fixed `ORDER BY`) | `list_status_open`, `list_status_closed`, `list_status_in_progress`, `list_type_bug`, `list_priority_one`, `list_priority_min_max`, `list_assignee`, `list_unassigned`, `list_label`, `list_label_two_and`, `list_label_any`, `list_title_contains`, `list_id_filter`, `list_long`, `list_json_again` (byte-identical to `list_json`, which is the determinism floor for this order), `edge_precision_list_json` |
| S6.40 | every `ready` filter keeps the S6.12 hybrid order | same as S6.39 for the hybrid comparator; `--include-deferred` widens the set without reordering it | stdout, plain and `--json` | `src/cli/commands/ready.rs:169`–189 (order-preserving retain, then truncate) | `ready_include_deferred`, `ready_type`, `ready_unassigned`, `ready_label`, `ready_limit`, `empty_ready_json` |
| S6.41 | `show <id>` section order | the sections are emitted in a fixed order — the header line, `Owner:`/`Type:`, `Created:`/`Updated:`, `Closed:` when present, `Assignee:` when present, `Labels:` when present, the description, `Dependencies:`, `Dependents:`, then `Comments:` — and this order does not vary with the record's contents | stdout in Plain mode | `src/cli/commands/show.rs:1197`–1387 | `show_plain`, `show_closed`, `show_comments`, `show_comments_json`, `show_unicode`, `show_unicode_json`, `show_epic`, `show_two`, `show_json` |


## S7. Numeric inventory

<!-- from docs/spec-parts/S4a_S7.md -->

| S7.n | quantity | type in original | observed range / evidence | wrap or exact | provenance | cases |
|---|---|---|---|---|---|---|
| S7.1 | id seed field byte length | pointer-width unsigned (64-bit on the pinned target), printed as decimal | 0 (`0:` for an absent description) through 500-plus; corpus maximum 500 bytes for the 500-character ASCII title of `edge_create_max_title`, and 37 bytes for the 29-byte unicode title plus its prefix in `create_unicode` | exact; counts UTF-8 **bytes**, never characters (RS-09) | `src/util/id.rs:297` | create_unicode, edge_create_max_title |
| S7.2 | id seed timestamp | signed 64-bit nanoseconds since the Unix epoch, printed as a bare decimal with `-` only when negative | corpus values `1767323045000000000` (2026-01-02T03:04:05Z), `1770109200000000000` (2026-02-03T09:00:00Z), `1770195600000000000`–`1770195602000000000` (scn_child_ids) | exact within 1677-09-21T00:12:43.145224192Z … 2262-04-11T23:47:16.854775807Z; **outside that range the value becomes `0`** rather than wrapping or failing, which makes every out-of-range instant hash alike | `src/util/id.rs:283` | create_min, scn_dup_title, scn_child_ids |
| S7.3 | id nonce | unsigned 32-bit, printed as decimal | corpus reaches 0, 1 and 2 (`scn_dup_title`); the ladder can reach 9 at each length and 2000 on the fallback rung | exact; the fallback loop bound 2000 is a constant, and the ladder never exceeds it | `src/util/id.rs:281`, `src/util/id.rs:13` | scn_dup_title |
| S7.4 | id hash accumulator | unsigned 64-bit built from digest bytes 0..7, most-significant byte first | full 0 … 18446744073709551615; corpus values are opaque, e.g. the value behind `proj-fyw` | exact by construction: exactly eight 8-bit shifts, so no shift ever discards a bit and no wrap occurs | `src/util/id.rs:312` | create_min |
| S7.5 | base-36 digit extraction | unsigned 64-bit remainder and truncating division by 36 | remainder 0..35 | exact; both operands are non-negative so truncating and flooring division agree and the remainder is never negative (RS-03 does not bite) | `src/util/id.rs:337` | create_min |
| S7.6 | hash length | pointer-width unsigned | 3 in every captured case; the predicate's reachable values are 3, 4, 5, 6, 7, 8 and the fallback constant 12 | exact; the length never exceeds 8 on the adaptive path, and the fallback path is a constant 12 | `src/util/id.rs:103`, `src/util/id.rs:12` | create_min, create_full_json |
| S7.7 | issue count fed to the adaptive length | pointer-width unsigned, converted to a 64-bit binary float | 0 (empty fixture) and 8 (`basic`) and 11 (`basic` after three child creates in `scn_child_ids`) | the conversion is exact for every count below 2^53, far above any reachable store; the float comparison is the only float in id generation and its thresholds are the integers of S4.9 | `src/util/id.rs:104` | create_min, create_full_json |
| S7.8 | birthday-bound intermediates | 64-bit binary float: 36 raised to an integer power, a negated quotient, an exponential, one minus it, compared with 0.25 | 36^3 = 46656 through 36^8 = 2821109907456, each exact in a 64-bit float; the probability lies in 0.0 … 1.0 | the powers and the product 2 × space are exact; only the exponential is inexact, and the S4.9 band edges are far from any tie, so the table is stable | `src/util/id.rs:107` | create_min |
| S7.9 | child number | unsigned 32-bit, printed as decimal with no padding | corpus reaches 1 and 2 (`scn_child_ids`); the parser accepts 0 … 4294967295 | **saturating**: the next number is the maximum plus one with saturating addition, so 4294967295 stays 4294967295 instead of wrapping; the search for a free number stops 100 past the computed value | `src/util/id.rs:352`, `src/storage/sqlite.rs:14406` | scn_child_ids |
| S7.10 | priority | signed 32-bit, serialized as a bare JSON integer | corpus reaches 0 (`proj-ptp`), 1, 2, 3, 4 (`proj-7vm`); accepted band 0..=4 | exact; a value outside 0..=4 is rejected before storage, and the parse itself rejects anything not fitting a signed 32-bit integer | `src/model/mod.rs:152`, `src/model/mod.rs:181` | list_priority_one, count_by_priority_json, error_create_bad_priority |
| S7.11 | estimated minutes | signed 32-bit option, serialized as a bare JSON integer, omitted when absent | corpus reaches 90 (`create_full_json`); accepted band 0 … 525960 | exact; negative and above-cap values are rejected by validation, and a value outside the signed 32-bit range is rejected by the argument parser with exit 2 | `src/model/mod.rs:515`, `src/validation/mod.rs:112` | create_full_json |
| S7.12 | comment id | signed 64-bit, serialized as a bare JSON integer | corpus reaches 1 (the `basic` fixture's existing comment on `proj-ptp`) and 2 (every `comments add`) | exact. In `--no-db` the ids are minted by a fresh in-memory auto-increment sequence rebuilt from the file on every invocation: an imported comment keeps the id written in the file, and a new comment takes **one more than the largest comment id present anywhere in the store**, not per issue. A comment id of 0 or less fails validation with `id: must be positive` | `src/model/mod.rs:933`, `src/storage/sqlite.rs:20975` | comments_add (id 2 on `proj-mta`), comments_add_json, comments_add_author, comments_add_second (id 2 on `proj-ptp`), actor_flag_comment, comments_list_json |
| S7.13 | dependency and dependent counts | pointer-width unsigned, serialized as bare JSON integers | corpus reaches 0, 1 and 2 (`proj-170` has `"dependency_count":2,"dependent_count":1`) | exact | `src/format/output.rs:136` | list_json, list_priority_one |
| S7.14 | record timestamps | signed 64-bit seconds plus an unsigned 32-bit nanosecond field (a chrono UTC instant), rendered per S4.54 | corpus spans 2026-01-01T10:00:00Z through 2027-06-01T09:00:00Z, and `…05.000000001Z` exercises 1 nanosecond | exact. The 0/3/6/9 fraction rule is a rendering choice, not a loss: the stored nanosecond field is preserved. The **printable** instant range is wider than the nanosecond-count range of S7.2, which is why an id seed can silently see `0` | `src/model/mod.rs:519` | edge_precision_list_json, edge_precision_rewrite, defer_until_date |
| S7.15 | relative-duration amount | signed 64-bit | `+1h` reaches 1; the parser accepts −9223372036854775808 … 9223372036854775807 | **checked**, never wrapping: an amount that overflows the integer parse, a unit conversion that overflows, and an addition that leaves the instant range each produce `relative duration is out of supported range` | `src/util/time.rs:195`, `src/util/time.rs:208` | defer_relative |
| S7.16 | `--limit` / `--offset` | pointer-width unsigned (64-bit on the pinned target) | corpus reaches limit 0, 1, 2 and offset 0, 1 | exact; `has_more` uses a **saturating** addition of offset and limit, so the sum never wraps. `0` means unlimited and is what the envelope prints when the user passed no limit | `src/cli/mod.rs:1886`, `src/cli/commands/list.rs:224` | list_limit_one, list_limit_offset, list_json |
| S7.17 | envelope `total` and every `count` | pointer-width unsigned, serialized as bare JSON integers | corpus reaches 0 (`empty_list_json`), 3 (`scn_dup_title`), 6 (`count_json`), 7 (`list_json`), 8 (`stats_json`) | exact | `src/output/context.rs:161` | empty_list_json, count_json, list_json, stats_json |
| S7.18 | stats counters | pointer-width unsigned, serialized as bare JSON integers | corpus reaches 0 … 8 (`stats_json`: total 8, open 5, blocked 2, in-progress 1, closed 1, deferred 1, ready 3, the rest 0) | exact | `src/format/output.rs:254` | stats_json, empty_stats_json |
| S7.19 | epic children counts | pointer-width unsigned | corpus reaches 0 and 3 (`scn_child_ids`: `"total_children":3,"closed_children":0`) | exact | `src/model/mod.rs:889` | epic_status_json, scn_child_ids |
| S7.20 | **average lead time — the only float that reaches an output in scope** Measured (2026-09-23, OQ-078): the JSON number is the binary64 nearest `sum / count` in its shortest round-trip spelling (`1.5`, `1.3333333333333333`, `38.0`). The port computes the double exactly with limb arithmetic (core/float.bend) and prints it as ryu does. | 64-bit binary float option, omitted from JSON when absent | corpus reaches 2.0 (`stats_json`) and absence (`empty_stats_json`) | The per-issue lead time is `closed_at` minus `created_at` **truncated to whole hours toward zero** before it becomes a float, so `proj-mkh`'s 2 h 59 m 53 s contributes 2.0, not 2.998. The mean is the float sum divided by the float count. **JSON printing** is serde's shortest round-trip form, which renders a whole value with a `.0` suffix: `2.0`. **Plain printing** is one fraction digit: when the mean is 24.0 or more it is divided by 24 and printed as `<n.n> days`, otherwise `<n.n> hours`; the one-digit rounding is banker's rounding (RS-07/08, ties to even) | `src/cli/commands/stats.rs:428`, `src/cli/commands/stats.rs:479`, `src/cli/commands/stats.rs:1252` | stats_plain (`  Avg Lead Time:          2.0 hours`), stats_json (`"average_lead_time_hours":2.0`), empty_stats_json (key absent), scn_lead_time_half, scn_lead_time_third |
| S7.21 | percentage bars in `stats` | 64-bit binary float, printed with zero fraction digits and a `%` sign, plus a bar width rounded half-away-from-zero | never reached in scope | out of scope: the only float percentages live on the Rich rendering path, and every captured run is piped and therefore Plain (PLAN §3 excludes Rich). No captured byte depends on them | `src/cli/commands/stats.rs:1514`, `src/cli/commands/stats.rs:1545` | stats_plain (shows the Plain path, no bars) |
| S7.22 | content-hash field length prefix | pointer-width unsigned written as 8 bytes **little-endian** | not observable (S4.51) | exact; the writer copies the low 8 bytes of the length, so a pointer narrower than 64 bits is zero-extended | `src/util/hash.rs:198` | (case to add: none is possible — see S4.51) |
| S7.23 | compaction level and original size | signed 32-bit options; `compaction_level` is always serialized, as `0` when absent, and `original_size` is materialized to `0` on load | corpus shows `"compaction_level":0` and `"original_size":0` on every record | exact | `src/model/mod.rs:624`, `src/model/mod.rs:630` | create_min, edge_precision_rewrite |
| S7.24 | exit codes | signed 32-bit, the process status | corpus reaches 0, 2, 3, 4, 5 | exact; 2 is the argument parser and database-family failures, 3 is not-found / ambiguous / id-collision / invalid-id, 4 is validation, 5 is dependency errors | `src/error/structured.rs:233` | usage_limit_bad (2), error_show_missing (3), error_create_bad_priority (4), error_dep_add_cycle (5) |
| S7.25 | integer printing | every integer above is written by the JSON encoder or by Rust's default integer display | — | no thousands separator, no `+` sign, no padding, no exponent form; a negative value carries a single leading `-`. A JSON integer is never quoted and never gains a `.0` (contrast S7.20's float) | `src/output/context.rs:196` | list_json, count_json, comments_list_json |


## S8. Effect inventory

<!-- from docs/spec-parts/S6_S8_S10.md -->

Every effect on an in-scope `--no-db` path, when it happens, and its failure
behavior. "Read-only commands" means `where`, `version`, `list`, `ready`,
`blocked`, `show`, `search`, `count`, `stats`, `dep list`, `dep tree`,
`dep cycles`, `label list`, `label list-all`, `comments list`, `epic status`,
`epic close-eligible`.

| S8.n | effect | when | failure behavior | provenance | cases |
|---|---|---|---|---|---|
| S8.1 | argv | parsed once at startup, before any file is touched. An unknown subcommand, an unknown flag, a repeated single-use flag or a bad flag value is refused by the argument parser with its own text on **stderr** and exit `2`, before the workspace is discovered | the command never runs; nothing is read or written | `src/main.rs:35` | `usage_none`, `usage_unknown_cmd`, `usage_unknown_flag`, `usage_empty_arg`, `usage_json_twice`, `usage_limit_noval`, `usage_limit_bad`, `usage_unexpected_flag`, `usage_dep_alone`, `usage_label_alone`, `usage_epic_alone`, `usage_search_alone`, `usage_show_alone`, `usage_close_alone`, `usage_update_alone`, `usage_delete_alone`, `usage_dep_add_one_arg` |
| S8.2 | environment: `BEADS_DIR` | read before the upward walk. A non-empty value replaces the walk entirely; the directory is used as-is | not a directory ⇒ `Error: Configuration error: BEADS_DIR not found or not a .beads directory: <path>` on stderr; a whitespace-only value is ignored and the walk runs | `src/config/mod.rs:691`, `:284`, `:707` | probe: `BEADS_DIR=/mnt/other/.beads br --no-db where` prints the redirected path and **omits the `prefix:` line entirely**. (case to add: `env_beads_dir_where` — needs a ws-run mode that sets one extra variable) |
| S8.3 | environment: `USER` | read to resolve the creator of a new issue, the `actor` of every mutation event and the `creator` field of the id seed. The chain is: the `--actor` flag or a configured `actor` key (which `BD_ACTOR` also populates through the generic `BD_<KEY>` sweep), then `USER`, then the literal | unset and unconfigured ⇒ the literal `unknown` | `src/config/mod.rs:7063`–7073, `:6019` | `create_min` (`"created_by":"tester"`, the sandbox's `USER`), `actor_flag_create` (`--actor robot-7` yields `"created_by":"robot-7"`); probe: `USER=zed` yields `"created_by":"zed"`, `env -u USER` yields `"created_by":"unknown"`; `BEADS_ACTOR`, `BR_ACTOR`, `BEADS_USER` and `BEADS_AUTHOR` have **no** effect on `created_by` |
| S8.4 | environment: the comment author chain | `comments add` resolves its author from `--author`, then the configured actor, then the first non-empty of `BD_ACTOR`, `BEADS_ACTOR`, `USER`, `LOGNAME`, `USERNAME`, then `unknown`. This chain is **not** the one used for `created_by` on issues | none of these unset is an error | `src/cli/commands/comments.rs:449`–477 | `comments_add_json` (`"author":"tester"`), `comments_add_author` (`--author zoe`), `actor_flag_comment` (`--actor robot-7` reaches the comment author) |
| S8.5 | environment: `NO_COLOR`, `TERM` | read at startup. A non-empty `NO_COLOR`, `TERM=dumb`, or a non-terminal stdout forces Plain output; a captured run is piped, so Plain is always what the goldens hold | no failure path | `src/logging.rs:29`–30; `src/output/context.rs:763`–768 | every golden, e.g. `list_plain` and `create_min` (neither `.out` holds an escape byte; the sandbox sets `NO_COLOR=1` at `scripts/ws-run.sh:35`); probe: removing `NO_COLOR` from a piped run changes nothing |
| S8.6 | environment: `RUST_LOG` | read once to build the tracing filter; all tracing output goes to **stderr** | an unparsable value silently falls back to the verbosity-derived default | `src/logging.rs:64`, `:83`–107 | the sandbox pins `RUST_LOG=error` (`scripts/ws-run.sh:35`), which is why no golden carries tracing noise — `create_min` and `list_plain` both have a zero-byte `.err`. (case to add: `env_rust_log_debug` — needs a ws-run mode that sets one extra variable, then `["@fx=basic","list"]`) |
| S8.7 | environment: `BR_OUTPUT_FORMAT`, `TOON_DEFAULT_FORMAT` | read at startup to pick the default output format and to decide whether errors are rendered as JSON on stdout. Values are trimmed and lowercased; accepted: `text`, `plain`, `json`, `csv`, `toon` | an unrecognized value falls through to the next source | `src/cli/mod.rs:1651`–1672; `src/main.rs:36` | probe: `BR_OUTPUT_FORMAT=json br --no-db count` prints `{"count":10}` where the flagless run prints `10`. (case to add: `env_output_format_json`) |
| S8.8 | environment: `BEADS_JSONL` | highest-priority override of the JSONL path, above `metadata.json` | a whitespace-only value is ignored | `src/config/mod.rs:5899` | (case to add: `env_beads_jsonl_elsewhere`) |
| S8.9 | environment: `BEADS_CACHE_DIR` | relocates `last-touched` out of `.beads/` | an empty value is ignored | `src/util/mod.rs:38`, `:48` | (case to add: `env_beads_cache_dir`) |
| S8.10 | environment: `BR_DISABLE_PARALLEL_JSONL_EXPORT` | read at export time; any value other than `0` or `false` (case-insensitive) forces a single-threaded export Measured (2026-09-24, OQ-087): the variable changes no byte of the export (`env_serial_export`, the same sha256 as the same `update` without it). | no failure path; the bytes written are identical either way | `src/sync/mod.rs:10846` | every mutating case exercises the default; (case to add: `env_serial_export` to pin that the bytes do not change), env_serial_export |
| S8.11 | environment: the remaining reads on in-scope paths | `HOME` (user-level config layers), `BD_<KEY>` for any key (each becomes a configuration key, including `BD_ACTOR`), `BD_DB` / `BD_DATABASE` / `BEADS_DB`, `BEADS_FLUSH_DEBOUNCE`, `BEADS_IDENTITY`, `BEADS_REMOTE_SYNC_INTERVAL`, `BEADS_AUTO_START_DAEMON`, `BR_HISTORY_MIN_INTERVAL_SECS`, `BR_HISTORY_MAX_BYTES`, `BR_SESSION`, `BR_INHERITED_CONTEXT`, `BR_STARTUP_CACHE`, `BR_STARTUP_CACHE_DIR`, `XDG_CACHE_HOME`, `COLUMNS`, `TOON_STATS`, `TMPDIR`, `COMPLETE` (the completion hook, which exits before anything else), and `PATH` (only `stats` uses it, to find `git`). `TZ` is never read by name but is consulted implicitly when a date argument is a keyword such as `today` or `next-week` | `HOME` unset ⇒ the user-level config layers are skipped, not an error. An existing but unparsable `config.yaml` at any layer is a hard error | `src/config/mod.rs:216`, `:231`, `:677`, `:6019`–6037, `:5599`, `:6375`, `:6661`; `src/sync/history.rs:712`; `src/cli/commands/mod.rs:298`; `src/inheritance.rs:67`; `src/format/text.rs:229`; `src/util/time.rs:68` | the sandbox clears the environment and passes only `PATH`, `HOME`, `USER`, `TZ`, `NO_COLOR`, `RUST_LOG`, plus the harness's own variables, so every golden runs with all of the above unset (`scripts/ws-run.sh:34`–35), e.g. `list_plain` and `create_min`; the implicit `TZ` read is exercised by the bare-date cases `defer_until_date` and `create_full_json` (09:00 local converted under `TZ=UTC`, `src/util/time.rs:44`–47). No existing case passes a keyword date: (case to add: `defer_until_tomorrow` — `["@fx=basic","defer","proj-ptp","--until","tomorrow","--json"]`) |
| S8.12 | current directory and the upward walk | the process's current directory is read once; the walk tries `<dir>/.beads`, then `<dir>/_beads`, then moves to the parent, and stops only at the filesystem root. There is no `$HOME` ceiling, no `.git` ceiling and no depth limit. The walk is lexical on the already-resolved current directory | no workspace anywhere ⇒ `Error: Beads not initialized: run 'br init' first` with `Hint: Run: br init` on stderr, exit `2`; in JSON mode the envelope `{"error":{"code":"NOT_INITIALIZED",…,"context":null}}` pretty-printed on stdout | `src/config/mod.rs:299`–364 | `error_no_workspace_list`, `error_no_workspace_list_json`, `error_no_workspace_create`; probe: running from `/mnt/proj/sub/deep` finds `/mnt/proj/.beads` |
| S8.13 | file read: `.beads/issues.jsonl` | the whole file is read and hashed on **every** invocation, read-only ones included, then imported into the scratch database | absent ⇒ an empty store, not an error (`empty_list` prints nothing and exits `0`). A merge-conflict marker is refused before parsing. A malformed line aborts the whole command | `src/config/mod.rs:5395`, `:5415`; `src/sync/mod.rs:14631`, `:13501` | `empty_list`, `list_plain`, `error_conflict_markers` (exit `7`, the three marker lines named with their line numbers), `error_conflict_markers_json`, `error_malformed_jsonl` (`Error: Configuration error: Invalid JSON at line 2: EOF while parsing a string at line 1 column 40`, exit `7`), `error_malformed_jsonl_json`, edge_large_count, edge_large_list_limit, edge_large_create (fixture `large`: 512 records, 167,808 bytes — the store size at which a port that joins its read chunks with `List.concat` faults on the JS lane, DISC-008) |
| S8.14 | file read: `.beads/metadata.json` | read during discovery to learn the database and JSONL file names | absent ⇒ the defaults `beads.db` and `issues.jsonl`; present but unreadable or unparsable ⇒ a hard error | `src/config/mod.rs:121`–137 | every case runs without the file, so the defaults are what `where_plain` prints. (case to add: `edge_metadata_json_renames_jsonl`) |
| S8.15 | file read: `.beads/config.yaml` and the user-level layers | four layers merged lowest to highest: `$HOME/.beads/config.yaml`, then `$HOME/.config/beads/config.yaml` (or `$HOME/.config/bd/config.yaml` when the first is absent), then `<beads_dir>/config.yaml`, then the environment layer | a missing file is an empty layer; an existing but unreadable or unparsable file is a hard error | `src/config/mod.rs:6009`–6022, `:6197`–6227, `:6538` | the sandbox sets `HOME=/mnt/home`, which is empty, and the fixtures ship no `config.yaml`, so every golden runs on the built-in defaults; the harness copies `goldens/fixtures/<fx>.config.yaml` into place when one exists (`scripts/ws_inner.py` `setup`) — `scripts/ws_inner.py:62`; `create_min` and `list_plain` therefore run on the defaults. (case to add: `config_yaml_layer` — a fixture shipping `<fx>.config.yaml`, then `["create","X","--json"]`) |
| S8.16 | file read: `.beads/redirect` | read during discovery, after a `.beads` directory is found. The content is trimmed; a relative target resolves against the `.beads` directory itself; chains are followed to depth 10 | absent ⇒ no redirect. Empty ⇒ a warning and no redirect. Larger than 4096 bytes ⇒ `Redirect file exceeds maximum size of 4096 bytes`; non-UTF-8 ⇒ `Redirect file must be valid UTF-8`; a loop ⇒ `Redirect loop detected: X -> Y`; depth 10 ⇒ `Redirect chain exceeds max depth (10)`; a missing or wrongly named target ⇒ `Redirect target not found` / `Redirect target must be a .beads or _beads directory` | `src/config/routing.rs:180`–310 | no fixture ships one. (case to add: `edge_redirect_relative` and `edge_redirect_loop`) |
| S8.17 | file read: `.beads/last-touched` | read only by `show`, `update`, `close`, `reopen` and `delete`, and only when no id is given on the command line. The first line, trimmed, is the id | absent or empty ⇒ `Error: Validation failed: ids: no issue IDs provided and no last-touched issue`. Larger than 4096 bytes or non-UTF-8 ⇒ treated as empty, not an error | `src/util/mod.rs:112`–138; `src/cli/commands/update.rs:381`, `close.rs:644`, `reopen.rs:75`, `show.rs:239`, `delete.rs:338` | `update_last_touched` (fixture `basic_touched` ships `proj-mta` in the sidecar; `update --status in_progress` with no id updates that issue), `scn_last_touched` |
| S8.18 | file read: `.beads/routes.jsonl` and an ancestor `mayor/town.json` | probed by the routed commands (`show`, `update`, `close`, `dep`, `label`, `comments add`) | absent ⇒ an empty route set; a bad line ⇒ `Invalid route at <path>:<line>: <e>` | `src/config/routing.rs:100`–148, `:332` | no fixture ships either; every routed golden runs with the empty set, e.g. `show_plain`, `update_status`, `close_basic`, `dep_add_blocks`, `label_add`, `comments_add`. (case to add: `edge_routes_jsonl_bad_line` — needs a harness that copies a `<fx>.routes.jsonl` sidecar holding one malformed line, then `show <id>`) |
| S8.19 | file read: `--description-file <path>` (`create`, `update`) | read verbatim, with no trimming and no truncation. A literal `-` reads standard input once, before any retry | unreadable ⇒ `Error: Validation failed: description_file: failed to read description file '<p>': <e>` | `src/cli/commands/create.rs:340`–359; `src/cli/commands/update.rs:1790` | (case to add: `create_description_file` and `create_description_stdin`) |
| S8.20 | file read: `comments add --file <path>` | read and size-capped; a literal `-` reads standard input | over the cap ⇒ `… exceeds maximum size of N bytes`; non-UTF-8 ⇒ `… must be valid UTF-8: …` | `src/cli/commands/comments.rs:420`–438 | (case to add: `comments_add_file`) |
| S8.21 | standard input | never read implicitly. The only three reads are the explicit `-` forms of S8.19 and S8.20 | not applicable | `src/cli/commands/create.rs:341`; `src/cli/commands/update.rs:1790`; `src/cli/commands/comments.rs:433` | every golden runs with stdin closed and no case blocks, e.g. `create_min`, `update_status` and `comments_add` — the three commands that own a `-` form, none of which blocks when the form is absent. (case to add: `create_description_stdin` — see S8.19) |
| S8.22 | user-level files under `$HOME` | the only reads are the two config layers of S8.15 and, when the startup cache is switched on, one cache file. Nothing is ever **written** under `$HOME` on an in-scope path | absent ⇒ skipped | `src/config/mod.rs:6206`–6227, `:6602` | probe: after `create` in the sandbox, `ls -aR /mnt/home` shows an empty directory; the probe's argv is that of `create_min` (`HOME=/mnt/home` at `scripts/ws-run.sh:34`, created empty at `scripts/ws_inner.py:52`). (case to add: `edge_home_untouched` — needs a ws-run mode that lists `/mnt/home` after the step) |
| S8.23 | file written: the scratch database | **every** `--no-db` invocation, read-only ones included, creates `<temp dir>/beads_mem_<pid>_<counter>.db` together with its `-wal`, `-shm` and engine sidecars, imports the whole JSONL into it, and unlinks it when the storage object is dropped. The counter starts at `0` per process | creation failure is retried 8 times with a 2 ms→100 ms backoff, removing the partial files each time. A `SIGKILL`, a second `SIGINT` or a `SIGPIPE` kill leaks the files | `src/storage/sqlite.rs:2694`–2736 | probe: during `br --no-db list` the files `/tmp/beads_mem_<pid>_0.db`, `…-wal`, `…-shm`, `…-fsqlite-ns-gate`, `…-fsqlite-ns-use`, `….fsqlite-migration-state` exist and are gone afterwards. Out of the port's scope (PLAN §3 excludes the SQLite store) but it is the reason a "read-only" run is not read-only on the filesystem. The probe's argv is that of `list_plain`; no golden can show the files, which live outside the workspace |
| S8.24 | file written: `.beads/issues.jsonl` | only on a mutating command, and only when the store is actually dirty. The bytes are staged into `.beads/issues.jsonl.<pid>.tmp` created exclusively with mode `0600`, written record by record with a `\n` after each, flushed, `fsync`ed, re-hashed and compared, then published by a flag-carrying rename (no-replace when the destination was absent, exchange when it was present) followed by an `fsync` of the parent directory. After an exchange the old file sits at the temp name and is removed | a hash mismatch between the staged bytes and the streamed content ⇒ `Staged JSONL bytes do not match the export content hash`, the staged file retained. A filesystem without the rename flags falls back to a witness-checked plain rename with a warning. A pre-existing symlink at the temp path ⇒ `Temporary export file already exists: …` | `src/sync/mod.rs:2939`–2990, `:11412`–11428, `:2461`–2531, `:2717` | every mutating golden's `--- .beads/issues.jsonl ---` dump; `create_min`, `update_status`, `close_basic`, `delete_json`, `label_add`, `dep_add_blocks`, `comments_add`. DISC-003 records that the port writes the same final bytes in place |
| S8.25 | file written: `.beads/last-touched` | written by `create`, `q`, `update`, `close`, `reopen`, `defer`, `undefer`, `dep add`, `dep remove`, `label add`, `label remove`, `label rename` and `comments add`. The bytes are the id followed by exactly one `\n` and nothing else (`proj-fyw\n`, 9 bytes for a 8-character id). Mode `0600`, truncating, no `fsync`, no temp file. In `--no-db` mode it is written **after** the JSONL flush succeeds; in database mode before | **every error is discarded silently** — a failure to open or write leaves the sidecar stale and the command still succeeds | `src/util/mod.rs:86`–106; ordering `src/cli/commands/create.rs:159`–167 | `create_min`, `create_silent`, `update_status`, `close_basic`, `label_add`, `comments_add`, `dep_add_blocks`, `scn_last_touched`; probe: `xxd .beads/last-touched` shows `70 72 6f 6a 2d 75 35 6d 0a` |
| S8.26 | file removed: `.beads/last-touched` | `delete` removes the sidecar rather than rewriting it | removal errors are discarded | `src/util/mod.rs:145`–148; `src/cli/commands/delete.rs:340` | `delete_basic`, `delete_json` (the golden shows no `--- .beads/last-touched ---` section, because the harness only dumps a file whose hash changed and the file was absent before and after in a fixture that ships none) |
| S8.27 | files written: the lock sidecars | a mutating `--no-db` command creates `.beads/.br-jsonl-write-<24 hex>.lock` and holds an advisory lock on it for the whole command. A capture also shows `.beads/.br-db-write-<24 hex>.lock` and `.beads/.write.lock`. All three are zero bytes and are **never deleted**. The hex suffix is the first 24 characters of a SHA-256 over the artifact's path, so the same workspace always produces the same names | a contended lock queues through a `.write-waiters.lock/` directory holding one file per waiter | `src/sync/mod.rs:1374`, `:1389`, `:266`, `:1762`; acquisition `src/config/mod.rs:5386` | probe after `create` on `basic`: `.beads` holds `.br-db-write-ded993a0aa2bb60f132e9ae2.lock`, `.br-jsonl-write-c9f16ff8a0bce34cff66a1d1.lock`, `.write.lock`, all zero bytes. The harness dumps only `issues.jsonl` and `last-touched`, so no golden carries them. DISC-002, DISC-004. The probe's argv shape is that of `create_with_parent` (a `create` on `basic`). (case to add: `edge_beads_dir_listing_mutating` — needs a ws-run mode that lists `.beads/` recursively after the step) |
| S8.28 | files written: `.beads/.br_history/` backups | before every JSONL export. The backup is `issues.<YYYYMMDD>_<HHMMSS>_<9-digit nanoseconds>.jsonl` plus a sidecar `issues.<same stamp>.jsonl.meta.json`, both created exclusively, `fsync`ed, with the parent directory `fsync`ed. A name collision appends `.<1..1023>`. The snapshot is skipped when the newest backup's recorded hash equals the current source's, and when the newest backup is younger than 5 seconds (overridable). Retention: newest 100, maximum age 30 days, then a global 1 GiB byte budget pruned oldest-first, always keeping the globally newest pair | the directory being a symlink or a non-directory is refused; a partially written backup is removed | `src/sync/history.rs:209`–250, `:541`–614, `:676`–728 | probe after `create` at the pinned instant: `.beads/.br_history/issues.20260102_030405_000000000.jsonl` and `issues.20260102_030405_000000000.jsonl.meta.json`, the sidecar holding `{"target":{"kind":"relative","path":"issues.jsonl"},"protected":false,"raw_sha256":"03d6b9206c8032422636b97c676347f3b1ff3f5c9c4755b520b7a306e3616fd9"}`. DISC-004. The recorded hash is the SHA-256 of `goldens/fixtures/basic.jsonl`, so the probe's argv shape is that of `create_with_parent` (a `create` on `basic`). (case to add: `edge_beads_dir_listing_mutating` — the harness mode of S8.27) |
| S8.29 | read-only commands create no file under `.beads/` | `list`, `where` and `version` leave `.beads/` holding exactly `issues.jsonl` | not applicable | `src/main.rs:1180`–1181 (in `--no-db` mode the startup lock is taken only under JSONL write intent), `:1534`–1536 (write intent means a mutating command), `src/config/mod.rs:5381` (the JSONL-family lock file is created only under write intent), `:4921` (a clean store returns before any export or history backup) | `list_plain`, `where_plain`, `version_plain` (no state dump in any of the three); probe: three successive read-only runs on `basic` each leave `.beads` with `issues.jsonl` and nothing else. This settles the "do read-only commands ever create files" half of OQ-008 for `.beads/`; they do create the scratch database of S8.23 outside the workspace |
| S8.30 | the clock: one `create` | one reading of the wall clock supplies the id seed's nanosecond timestamp, `created_at`, `updated_at` and the `created_at` of every dependency edge the command generates. All of them are therefore equal | no failure path | `src/cli/commands/create.rs:456`; edges at `:553` | `create_min` (`created_at` and `updated_at` both `2026-01-02T03:04:05Z`), `create_with_parent` and `create_with_deps` (the edge's `created_at` equals the issue's); probe with the real clock: issue `created_at` `…29.124238920Z`, `updated_at` the same, both edges the same |
| S8.31 | the clock: one invocation observes several instants | a mutation takes a fresh reading for the record's `updated_at`, another for each event row and another for the dirty marker. `close` computes `closed_at` in the command layer and `updated_at` later in the storage layer, so `closed_at` is strictly earlier. `comments add` takes the comment's `created_at` from the database engine at second resolution and the issue's `updated_at` from the process clock at nanosecond resolution | no failure path | `src/cli/commands/update.rs:1823`; `src/storage/sqlite.rs:7813`, `:20978`, `:14153`, `:1815`, `:6562` | invisible under the pinned clock (`close_basic`, `comments_add_json` all show the one pinned instant); probe with the real clock: after `comments add`, the comment's `created_at` is `2026-09-20T06:12:29Z` while the issue's `updated_at` is `2026-09-20T06:12:29.705963839Z` — the same command, two different instants at two different precisions |
| S8.32 | the clock: monotonic readings | a monotonic clock is read only for lock budgets, import timing and poll deadlines; no monotonic value ever reaches an output | not applicable | `src/config/mod.rs:5427`; `src/sync/mod.rs:1641`, `:1661` | the sandbox sets `DONT_FAKE_MONOTONIC=1` precisely because a fully frozen monotonic clock hangs `br` (PLAN §2) — `scripts/ws_inner.py:79`; `create_min` and `list_json_again` run under it and carry no elapsed-time value |
| S8.33 | randomness | the id nonce is a deterministic counter, not a random value: nonces `0..9` are tried at each hash length, the length then grows, and a fallback ladder follows at length 12. The only entropy in a new id is the nanosecond timestamp and the actor string. No random number generator, no UUID and no entropy source is called on any in-scope path. The one real random value in the tree names a lock-waiter file and never reaches an output | exhausting the ladder ⇒ an id-collision error | `src/util/id.rs:213`–295 | `scn_dup_title` (three creates of `Same title` at one pinned instant yield `proj-86w`, `proj-e1f`, `proj-fa9`), `create_min` reproducing `proj-fyw` byte-for-byte across every capture |
| S8.34 | stdout and stderr: which stream | plain text and every successful `--json` document go to stdout. Warnings, notes and human-form errors go to stderr. In JSON mode the structured error envelope goes to **stdout**, deliberately, so that a machine reader sees one parseable stream. Tracing output always goes to stderr | not applicable | `src/output/context.rs:1261`, `:1277`; `src/main.rs:1875`–1896; `src/logging.rs:33` | `error_show_missing` (message and hint on stderr, empty stdout, exit `3`), `error_show_missing_json` (envelope on stdout, empty stderr, exit `3`), `ready_limit` (`[note] Showing 1 of 3 ready issues. Use --limit 0 for all results.` on stderr while the list is on stdout), `dep_remove_absent` (`Warning: Dependency not found: proj-mta → proj-ptp` on stderr, exit `0`) |
| S8.35 | stdout buffering and flush order | plain text is printed through the unlocked global handle with no explicit flush; JSON is streamed through a locked buffered writer that ends with a `\n` and an explicit flush. Every deliberate exit funnels through one routine that flushes stdout and then stderr before the process dies. On a human-mode error path the unbuffered stderr message therefore reaches a pipe **before** any buffered stdout content, so the relative interleaving of the two streams is not fixed when stdout is redirected | a serialization failure is recorded and reported on stderr and turns into a non-zero exit code | `src/output/context.rs:225`–228, `:1042`; `src/shutdown.rs:207`–233 | the harness captures the two streams separately, so no golden depends on the interleaving; `error_close_blocked_json` is the one case where stdout carries two JSON documents in order — the command result `{"closed":[],"skipped":[…]}` and then the pretty error envelope |
| S8.36 | process exit codes | the full ladder is `0` success · `1` internal error · `2` database and not-initialized errors, and every argument-parser refusal · `3` issue not found, ambiguous id, id collision, invalid id, nothing-to-do, close-incomplete · `4` validation failure, invalid status, invalid type, invalid priority, required field, policy violation, capacity exceeded · `5` cycle detected, dependency not found, has dependents, self-dependency, duplicate dependency · `6` JSONL parse error, prefix mismatch, import collision, sync conflict, conflict markers, path traversal · `7` configuration errors · `8` I/O, JSON and YAML errors · `130` cooperative shutdown after an interrupt. The corpus exercises `0`, `2`, `3`, `4`, `5` and `7`. Note that conflict markers and a malformed JSONL line are classified as **configuration** errors (`7`), not as the JSONL-parse errors of `6`. Exit is by an explicit call that skips destructors, which is why the storage handle is dropped before returning | not applicable | `src/error/structured.rs:220`–278; `src/main.rs:57`–60, `:1896`; `src/shutdown.rs:207` | `create_min` (`0`), `error_no_workspace_list` (`2`), `usage_unknown_cmd` (`2`), `error_show_missing` (`3`), `close_already_closed` (`3`), `error_create_bad_priority` (`4`), `list_sort_bad` (`4`), `error_comments_add_empty` (`4`), `update_status_closed` (`4`), `error_dep_add_cycle` (`5`), `error_dep_add_self` (`5`), `error_conflict_markers` (`7`), `error_malformed_jsonl` (`7`) |
| S8.37 | signals: `SIGPIPE` | the default disposition is restored at startup for every command **except** those producing structured output. In plain mode a closed downstream pipe therefore kills the process at the first write, with status `141` and no message, exactly as a stream filter behaves. In `--json` mode the signal stays ignored and the broken pipe is absorbed as a non-error with exit `0` | the kill skips destructors, so the scratch database of S8.23 is leaked | `src/shutdown.rs:78`–103; gate `src/main.rs:1844`–1857 | no golden exercises a closed pipe; the harness captures both streams fully. (case to add: `edge_sigpipe_plain` — needs a harness mode that closes stdout early) |
| S8.38 | signals: `SIGINT`, `SIGTERM`, `SIGHUP` | a handler thread is installed before argument parsing. The first signal records `128 + signal` and sets a flag the main thread checks once, after the command finishes and before any auto-flush. The second matching signal exits immediately without flushing or unlinking anything | if the handler cannot be installed the default action stands; that failure is only logged | `src/shutdown.rs:57`–176 | no golden exercises a signal. (case to add: `edge_sigint_during_mutation` — needs a harness mode that sends `SIGINT` to the running step, on `["@fx=basic","update","proj-mta","-p","0"]`) |
| S8.39 | git and the network | **none on the in-scope surface, with one qualification.** No in-scope command opens a socket; no HTTP client is compiled into the pinned binary except behind the self-update feature. Worktree resolution and repository detection parse `.git` files by hand rather than spawning git. The qualification: `stats` spawns `git log` and `git diff` for its activity section unless `--no-activity` or quiet mode is in force, and `version --check` spawns `curl`. PLAN §3 excludes commit activity in `stats`, and every failure is swallowed, so the activity section is simply absent | a missing `git` binary silently omits the activity section; a missing `curl` for `version --check` ⇒ an external-command error and exit `2` | `src/cli/commands/stats.rs:717`–731, `:1158`, `:280`; `src/cli/commands/version.rs:284`–291; `src/config/mod.rs:433` | `stats_plain` and `stats_json` (no activity section at all — the sandbox has no git repository), `empty_stats`, `version_plain`, `version_json` (plain `version` spawns nothing) |
| S8.40 | telemetry, log files and caches | no telemetry of any kind. No log file: the logging subsystem supports one but no command-line flag or environment variable supplies a path, so all logging is stderr-only and ephemeral. The startup cache is the only cache file and it is off unless `BR_STARTUP_CACHE` is truthy, so by default no cache file is written | not applicable | `src/main.rs:39`; `src/logging.rs:44`; `src/config/mod.rs:6647` | every golden, e.g. `create_min` and `list_plain` (zero-byte `.err`, no file dumped beyond the two state files); probe confirms nothing under `/mnt/home` |
| S8.41 | the completion hook | before any other work, the argument parser's dynamic-completion hook runs and, when its environment variable is set, exits before the workspace is discovered | not applicable | `src/main.rs:26` | no golden sets it; PLAN §3 excludes `completions`. (case to add: `env_complete_hook` — needs a ws-run mode that sets `COMPLETE=bash`, then `["list"]`) |
| S8.42 | which commands write the store, and which write only the sidecar | a command writes `.beads/issues.jsonl` exactly when it actually changed a record, and writes `.beads/last-touched` whenever it addressed one, whether or not anything changed. The harness dumps a file only when its hash moved, so a golden with an `issues.jsonl` section changed the store and one with only a `last-touched` section did not | see S8.24, S8.25; the sidecar write is unconditional at `src/cli/commands/label.rs:253`, `:334`, `src/cli/commands/dep.rs:595`, `:766`, `src/cli/commands/update.rs:525`, and skipped on a dry run at `src/cli/commands/create.rs:165`; the store is exported only when dirty, `src/config/mod.rs:4921`; the harness dumps a file only when its hash moved, `scripts/ws_inner.py:136`; `goldens/label_add_existing.out:2` (the only dump is `last-touched`) | store **and** sidecar: `create_title_flag`, `create_status_in_progress`, `create_unicode`, `update_status_json`, `update_priority`, `update_assignee`, `update_claim`, `close_suggest_next`, `close_blocked_force`, `reopen_closed`, `reopen_closed_json`, `undefer_basic`, `dep_add_json`, `dep_remove_basic`, `label_add_json`, `label_rename`. Sidecar only: `label_add_existing`, `label_remove_absent`, `dep_add_duplicate`, `dep_remove_absent`, `update_noop_same_value`, `create_ephemeral`. Neither: `create_dry_run`, `create_dry_run_json`, `delete_dry_run`, and every read-only case |
| S8.43 | a refused command touches nothing | when validation or resolution fails, no record is written, `last-touched` is not written, and the process exits before any export. The harness proves it by dumping no state section at all | not applicable | `src/config/mod.rs:4918` (a clean store skips the flush) | `error_create_empty_title`, `error_create_blank_title`, `error_create_long_title`, `error_create_bad_priority`, `error_create_bad_label`, `error_create_missing_parent`, `error_update_missing`, `error_update_bad_priority`, `error_update_blocked_claim`, `error_close_missing`, `error_reopen_missing`, `error_delete_missing`, `error_dep_add_missing`, `error_dep_add_self`, `error_dep_add_cycle`, `error_dep_add_cycle_json`, `error_comments_add_empty`, `error_comments_add_missing`, `error_label_add_invalid`, `error_defer_bad_until`, `update_status_closed`, `close_already_closed`, `error_close_blocked`, `error_close_blocked_json`, `close_epic_with_open_child` |
| S8.44 | read-only commands write nothing under the workspace | `list`, `ready`, `blocked`, `show`, `search`, `count`, `stats`, `dep list`, `dep tree`, `dep cycles`, `label list`, `label list-all`, `comments list`, `epic status`, `where` and `version` leave `.beads/` byte-identical, which is what makes a state dump in their goldens a failure | not applicable | see S8.29: `src/main.rs:1180`–1181, `src/config/mod.rs:5381`, `:4921`; no read-only command calls the sidecar writer `src/util/mod.rs:86`; `scripts/ws_inner.py:7`–8 (the harness rule) | `count_json`, `count_plain`, `count_include_closed`, `show_closed`, `show_comments`, `show_comments_json`, `show_unicode`, `show_unicode_json`, `empty_ready_json`, and every other read-only case; `scripts/ws_inner.py` states that a read-only command which rewrites the store fails its golden |


## S9. Error and edge behavior

<!-- from docs/spec-parts/S1_S9.md -->

### S9.1 The two error renderings

| S9.n | clause | provenance | cases |
|---|---|---|---|
| S9.1 | Every error that survives argument parsing is turned into one structured value with five fields — `code`, `message`, `hint`, `retryable`, `context` — and then rendered in exactly one of two ways, chosen by the same test that selects JSON output (S1.6) *plus* `--format json`/`--format toon` on the command: human, or JSON. There is never both. The process exits with the code the error's category gives (S9.20) and no further output follows. | `src/main.rs:1875`–`src/main.rs:1896`, `src/main.rs:1816`–`src/main.rs:1830` | error_show_missing, error_show_missing_json |
| S9.2 | **Human rendering** goes to **stderr** and is `Error: ` + message, and when a hint exists, LF + `Hint: ` + hint; one trailing LF closes the whole thing. Stdout receives nothing from the error itself. Control characters in the message and hint other than LF and TAB are replaced by their Rust escape spelling (`\u{7}` and the like) before printing; the JSON rendering does not do this. | `src/error/structured.rs:606`–`src/error/structured.rs:631`, `src/format/text.rs:49`–`src/format/text.rs:82` | error_no_workspace_list, error_close_missing |
| S9.3 | **JSON rendering** goes to **stdout** (not stderr), pretty-printed with two-space indentation and one trailing LF, as a single object whose only key is `error`, whose value has exactly these five keys in this fixed order: `code`, `message`, `hint`, `retryable`, `context`. An absent hint or context is the literal `null` — the keys are always present. Example, `error_no_workspace_list_json`: `{` LF `  "error": {` LF `    "code": "NOT_INITIALIZED",` LF `    "message": "Beads not initialized: run 'br init' first",` LF `    "hint": "Run: br init",` LF `    "retryable": false,` LF `    "context": null` LF `  }` LF `}` LF. | `src/error/structured.rs:593`–`src/error/structured.rs:603`, `src/main.rs:1884`–`src/main.rs:1891` | error_no_workspace_list_json, error_conflict_markers_json, error_malformed_jsonl_json |
| S9.4 | A clap parse failure never uses either rendering: it is plain text on stderr with exit `2` and empty stdout, whatever the output mode (S1.30). The port's error table therefore has two disjoint halves: exit `2` from parsing (S1.15–S1.24) and exit `1`–`8` from the structured errors below. | `src/main.rs:35` | usage_limit_bad, usage_json_twice |
| S9.5 | `retryable` is a function of the code alone, except for a write-lock timeout, which carries its own flag. The codes reachable in `--no-db` mode that set `retryable` to `true` are `VALIDATION_FAILED`, `INVALID_STATUS`, `INVALID_TYPE`, `INVALID_PRIORITY`, `REQUIRED_FIELD` and `AMBIGUOUS_ID`; every other reachable code sets it to `false`. | `src/error/structured.rs:190`–`src/error/structured.rs:206`, `src/error/structured.rs:317`–`src/error/structured.rs:322` | error_create_bad_priority_json, error_show_ambiguous_json, error_show_missing_json |

### S9.6 The exit-code map

| S9.n | clause | provenance | cases |
|---|---|---|---|
| S9.6 | Exit codes are assigned per error category and not per command: `0` success; `1` `INTERNAL_ERROR`; `2` the database family `DATABASE_NOT_FOUND`, `DATABASE_LOCKED`, `SCHEMA_MISMATCH`, `DATABASE_ERROR`, `NOT_INITIALIZED`, `ALREADY_INITIALIZED`; `3` the issue and batch family `ISSUE_NOT_FOUND`, `AMBIGUOUS_ID`, `ID_COLLISION`, `INVALID_ID`, `NOTHING_TO_DO`, `CLOSE_INCOMPLETE`; `4` the validation family `VALIDATION_FAILED`, `INVALID_STATUS`, `INVALID_TYPE`, `INVALID_PRIORITY`, `REQUIRED_FIELD`, `POLICY_VIOLATION`, `WORKFLOW_CAPACITY_EXCEEDED`; `5` the dependency family `CYCLE_DETECTED`, `DEPENDENCY_NOT_FOUND`, `HAS_DEPENDENTS`, `SELF_DEPENDENCY`, `DUPLICATE_DEPENDENCY`; `6` the sync family `JSONL_PARSE_ERROR`, `PREFIX_MISMATCH`, `IMPORT_COLLISION`, `SYNC_CONFLICT`, `CONFLICT_MARKERS`, `PATH_TRAVERSAL`; `7` the config family `CONFIG_ERROR`, `CONFIG_NOT_FOUND`, `CONFIG_PARSE_ERROR`; `8` the I/O family `IO_ERROR`, `JSON_ERROR`, `YAML_ERROR`; `130` `SHUTTING_DOWN`. Exit `2` is *also* what every argument-parse failure produces, from a different path. | `src/error/structured.rs:233`–`src/error/structured.rs:277` | error_no_workspace_list (2), error_show_missing (3), error_create_empty_title (4), error_dep_add_self (5), error_conflict_markers (7) |
| S9.7 | Which exit codes the in-scope surface actually reaches, measured over the corpus: `0` (success and the tolerant no-ops of S9.24), `2` (parse failures and `NOT_INITIALIZED`), `3` (`ISSUE_NOT_FOUND`, `AMBIGUOUS_ID`, `INVALID_ID`, `NOTHING_TO_DO`, `CLOSE_INCOMPLETE`), `4` (`VALIDATION_FAILED`, `INVALID_PRIORITY`), `5` (`CYCLE_DETECTED`, `SELF_DEPENDENCY`), `7` (`CONFIG_ERROR`). Codes `1`, `6`, `8` and `130` are not reached by any captured case: in particular the merge-conflict and malformed-JSONL refusals, which the code's `CONFLICT_MARKERS`/`JSONL_PARSE_ERROR` names suggest would be `6`, are raised as a plain configuration error and exit `7` (S9.17, S9.18). Where the source's category names and the goldens disagree, the GOLDEN wins. | goldens `*.exit`; `src/sync/mod.rs:10381`, `src/sync/mod.rs:5933` | error_conflict_markers, error_malformed_jsonl |

### S9.8 The error table

Each row gives the trigger, the verbatim human stderr, the JSON envelope's
`code`/`hint`/`retryable`/`context` keys, and the exit code. Stdout is empty
for every row unless the row says otherwise. The hint, where one exists, is
the second stderr line and the `hint` value; where the row says "none" the
`Hint:` line is absent and `hint` is `null`.

| S9.n | trigger | human stderr (verbatim, each line closed by LF) | JSON `error` | exit | provenance | cases |
|---|---|---|---|---|---|---|
| S9.8 | No `.beads` directory anywhere up the walk, on any command that needs the store | `Error: Beads not initialized: run 'br init' first` / `Hint: Run: br init` | code `NOT_INITIALIZED`, hint `Run: br init`, retryable `false`, context `null` | 2 | `src/error/mod.rs:242`, `src/error/mod.rs:444`, `src/main.rs:1111` | error_no_workspace_list, error_no_workspace_list_json, error_no_workspace_create |
| S9.9 | An id that resolves to no issue, from `show`, `update`, `close`, `reopen`, `delete`, `dep add` (either side), `comments add`, or `create --parent` | `Error: Issue not found: <id>` / `Hint: Run 'br list' to see available issues.` | code `ISSUE_NOT_FOUND`, hint `Run 'br list' to see available issues.`, retryable `false`, context `{"searched_id":"<id>"}` | 3 | `src/error/mod.rs:61`, `src/error/structured.rs:993`–`src/error/structured.rs:995`, `src/error/structured.rs:667` | error_show_missing, error_show_missing_json, error_update_missing, error_close_missing, error_reopen_missing, error_delete_missing, error_dep_add_missing, error_comments_add_missing, error_create_missing_parent |
| S9.10 | A partial id matching more than one issue | `Error: Ambiguous ID '<partial>': matches ["<id>", "<id>", …]` / `Hint: Provide more characters of the ID` | code `AMBIGUOUS_ID`, hint `Provide more characters of the ID`, retryable `true`, context `{"partial_id":"<partial>","matches":[…]}` | 3 | `src/error/mod.rs:69`, `src/error/mod.rs:456`, `src/error/structured.rs:669` | show_partial_ambiguous, error_show_ambiguous_json, error_update_ambiguous |
| S9.11 | An empty-string id positional | `Error: Invalid issue ID format: ` (the message ends with a space before the LF) | code `INVALID_ID`, hint `null`, retryable `false`, context `{"id":""}` | 3 | `src/error/mod.rs:76`, `src/error/structured.rs:673` | (case to add: `error_show_empty_id` — `["@fx=basic","show",""]`, and `error_show_empty_id_json`) |
| S9.12 | A batch in which **every** requested item was skipped (`close` on an already-closed issue, on a blocked issue without `--force`, or on an epic with open children) | `Error: Nothing to do: <reason>` / `Hint: ` + the skip hint of S9.13 | code `NOTHING_TO_DO`, hint as S9.13, retryable `false`, context `{"reason":"<reason>"}` | 3 | `src/error/mod.rs:277`, `src/error/structured.rs:1012` | close_already_closed, error_close_blocked, error_close_blocked_json, close_epic_with_open_child |
| S9.13 | The skip hint is chosen from the reason text, in this order: a reason containing `blocked by` gives `Skipped issue(s) have open blocking dependencies. Close the blockers first, or re-run with --force to close anyway.`; a reason containing `open children` or `child issue` gives `Skipped issue(s) have open children. Close the children first, or re-run with --force to close anyway.`; otherwise `Skipped issue(s) were already closed or not found.` | (hint text only) | same hint in `hint` | — | `src/error/structured.rs:1055`–`src/error/structured.rs:1065` | error_close_blocked, close_epic_with_open_child, close_already_closed |
| S9.14 | A batch in which some items landed and some were skipped | `Error: Partially applied: <closed> closed, <skipped> skipped — <summary>` / `Hint: ` + the skip hint of S9.13. Measured on `@fx=basic` with `close proj-mta proj-mkh`: stdout carries `✓ Closed proj-mta: Set up database schema (done)` LF and the store is rewritten with `proj-mta` closed, while stderr carries `Warning: Skipped proj-mkh: already closed` LF then the two error lines; the em dash is `—`. | code `CLOSE_INCOMPLETE`, hint as S9.13, retryable `false`, context `{"closed":…,"skipped":…,"summary":"…"}` Measured (2026-09-22, OQ-013): in JSON the error's context is `{"closed":<n>,"skipped":<n>,"reason":"<summary>"}`, in that order (`close_transition_comment_batch`). | 3 | `src/error/mod.rs:288`, `src/error/structured.rs:1013` | (case to add: `error_close_partial` — `["@fx=basic","close","proj-mta","proj-mkh"]`), close_transition_comment_batch |
| S9.15 | A priority value outside `0`–`4`/`P0`–`P4` on `create`, `update`, or a `list`-family priority filter | `Error: Priority must be 0-4, got: <value>` / `Hint: Priority must be 0-4 or P0-P4 (0=critical, 4=backlog); list filters also accept ranges and comma lists such as 0-1 or 0,2.` | code `INVALID_PRIORITY`, hint as shown, retryable `true`, context `{"priority":"<value>","hint":"<same hint>"}` — the hint is duplicated inside `context` | 4 | `src/error/mod.rs:106`, `src/error/mod.rs:468`, `src/error/structured.rs:1028`–`src/error/structured.rs:1039` | error_create_bad_priority, error_create_bad_priority_json, error_update_bad_priority, (case to add: `error_list_priority_min_bad`) |
| S9.16 | A reversed range such as `--priority 3-1` gives the hint `Ranges run low to high: use 1-3.` instead; a recognized synonym such as `high` gives `Did you mean --priority 1?`. Both replace only the hint; code, message shape and exit stay those of S9.15. | as S9.15 with the substituted hint | same | 4 | `src/error/structured.rs:1028`–`src/error/structured.rs:1039` | (case to add: `error_priority_reversed_range`, `error_priority_synonym`) |
| S9.17 | Any field-level validation failure | `Error: Validation failed: <field>: <reason>` and **no** `Hint:` line | code `VALIDATION_FAILED`, hint `null`, retryable `true`, context `{"field":"<field>","reason":"<reason>"}` | 4 | `src/error/mod.rs:81`, `src/error/structured.rs:677`–`src/error/structured.rs:680` | error_create_empty_title, list_status_bad, list_sort_bad, error_comments_add_empty, error_update_blocked_claim |
| S9.18 | The `<field>: <reason>` pairs reachable from the in-scope surface, verbatim: `title: cannot be empty` (an empty or all-whitespace title on `create`/`q`); `title: exceeds 500 characters` (a title over the 500-character limit); `ids: no issue IDs provided and no last-touched issue` (`show`/`update`/`close`/`reopen` with no id and no `.beads/last-touched`); `ids: no issue IDs provided` (`delete` with no id); `ids: at least one issue ID is required` (`defer`/`undefer` with no id); `id: missing issue id` (bare `br comments`); `text: comment text cannot be empty` (`comments add` with an empty text); `label: invalid characters (only alphanumeric, hyphen, underscore, colon allowed)` (`create -l`, `update --add-label`, `label add`); `claim: cannot claim blocked issue: <blocker-id>` (`update --claim` on a blocked issue — the id named is the **blocker**, not the target); `arguments: usage: label add <issue...> <label...> or label add <issue...> -l <label>[,<label>]` (`label add` with an issue but no label); `defer_until: invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)` (`defer --until` with an unparseable value); `sort: invalid sort field '<value>'` (`list --sort` with an unknown key); the long `status: unknown status …` of S9.19; the long `status: refusing to close via …` of S9.20. | as S9.17 | as S9.17 | 4 | `src/error/mod.rs:81`; measured through the sandbox | error_create_empty_title, error_create_blank_title, error_create_long_title, usage_show_alone, usage_close_alone, usage_update_alone, usage_delete_alone, error_comments_add_empty, error_create_bad_label, error_label_add_invalid, error_update_blocked_claim, error_defer_bad_until, list_sort_bad, list_status_bad |
| S9.19 | An unknown `--status` filter value on `list` (including the empty string) gives, verbatim on one line: `Error: Validation failed: status: unknown status '<value>'. Built-in statuses: open, in_progress, blocked, deferred, draft, closed, tombstone, pinned. Custom statuses must be declared in .beads/policy.yaml (workflow.statuses) or exist on at least one issue. Use --status all to include every status`. The same text is the `message`, and `context.reason` repeats everything after `status: `. | as S9.17 | as S9.17 | 4 | `src/cli/commands/list.rs:484`–`src/cli/commands/list.rs:487` (the reason text), inside `validate_status_filter` at `src/cli/commands/list.rs:448`; golden `goldens/list_status_bad.err:1` (the whole line, value `bogus`), `goldens/list_status_bad.exit:1` is `4`. The empty-string value was re-measured through the sandbox 2026-09-20 (`unknown status ''`, exit `4`); no golden holds it yet | list_status_bad, (case to add: `list_status_empty` — `["@fx=basic","list","--status",""]`) |
| S9.20 | `update --status closed` (or any other terminal status) gives, verbatim on one line: `Error: Validation failed: status: refusing to close via ` + "`br update --status closed`" + `: terminal-state transitions must go through ` + "`br close`" + ` so close-policy (close-reason / AC / attribution) is enforced. Use ` + "`br close <id> --reason \"...\"`" + ` instead, or ` + "`br close <id> --bypass-policy --bypass-reason \"...\"`" + ` to opt out explicitly. See https://github.com/Dicklesworthstone/beads_rust/issues/301.` The backticks are literal bytes of the message. | as S9.17 | as S9.17 | 4 | `src/cli/mod.rs:1302`–`src/cli/mod.rs:1306`; measured | (case to add: `error_update_status_closed`) |
| S9.21 | Adding a dependency that would close a cycle | `Error: Cycle detected in dependencies: Adding dependency <from> -> <to> would create a cycle` / `Hint: Remove one dependency to break the cycle. Note: epic containment participates in blocking cycles — depending on an epic implies depending on its entire subtree, so the cycle may traverse parent-child edges that the message does not list (see docs/CLI_REFERENCE.md, ` + "`dep add`" + `)`. The arrow in the message is the two-character ASCII `->`; the dash in the hint is `—`; the backticks around `dep add` are literal. | code `CYCLE_DETECTED`, hint as shown, retryable `false`, context `{"cycle_path":"Adding dependency <from> -> <to> would create a cycle"}` | 5 | `src/error/mod.rs:205`, `src/error/mod.rs:459`–`src/error/mod.rs:465` | error_dep_add_cycle, error_dep_add_cycle_json |
| S9.22 | `dep add <id> <id>` with the same issue on both sides | `Error: Issue cannot depend on itself: <id>` / `Hint: An issue cannot depend on itself` | code `SELF_DEPENDENCY`, hint `An issue cannot depend on itself`, retryable `false`, context `{"id":"<id>"}` | 5 | `src/error/mod.rs:213`, `src/error/mod.rs:466` | error_dep_add_self |
| S9.23 | Git merge-conflict markers anywhere in `issues.jsonl` Measured (2026-09-23, OQ-016): only the FIRST FIVE markers are reported, with no line about the rest (`error_conflict_markers_many`: six markers). | `Error: Configuration error: Merge conflict markers detected in <abs path>.` LF `<abs path>:<line> Start (HEAD)` LF `<abs path>:<line> Separator` LF `<abs path>:<line> End (branch)` LF `Resolve conflicts before importing.` — a multi-line message with no `Hint:` line | code `CONFIG_ERROR`, hint `null`, retryable `false`, context `null`; the whole multi-line text is one `message` string with embedded `\n` escapes | 7 | `src/sync/mod.rs:10357`–`src/sync/mod.rs:10384` | error_conflict_markers, error_conflict_markers_json, error_conflict_markers_many |
| S9.24 | Malformed JSON on any line of `issues.jsonl` | `Error: Configuration error: Invalid JSON at line <n>: <serde message>` and no `Hint:` line. `<n>` is the 1-based line number in the file; the `serde` message reports its own position **within that single line**, so its line is always `1` and its column is the byte offset where parsing stopped. Fixture `malformed` has its second line truncated at 40 bytes, giving `Invalid JSON at line 2: EOF while parsing a string at line 1 column 40`. | code `CONFIG_ERROR`, hint `null`, retryable `false`, context `null` | 7 | `src/sync/mod.rs:5933`, `src/sync/mod.rs:10435` | error_malformed_jsonl, error_malformed_jsonl_json |
| S9.25 | `BEADS_DIR` pointing at something that is not an existing `.beads` directory | `Error: Configuration error: BEADS_DIR not found or not a .beads directory: <value>` and no `Hint:` line | code `CONFIG_ERROR`, hint `null`, retryable `false`, context `null` | 7 | `src/config/mod.rs:285`, `src/config/mod.rs:306` | (case to add: `error_env_beads_dir_missing`) |

### S9.26 Warnings, tolerant no-ops and edge behavior

| S9.n | clause | provenance | cases |
|---|---|---|---|
| S9.26 | A per-item skip inside a batch is a **warning on stderr**, written before the summary error (when one follows) and **suppressed by `--quiet`** (amended 2026-09-20: the first text said "never suppressed"; running the original shows `--quiet` drops the `Warning:` lines, `src/output/context.rs:1278`; OQ-015): `Warning: Skipped <id>: <reason>` LF. The reasons observed are `already closed`, `blocked by: <id> — close the open blocker(s) first, or use --force to close anyway`, and `epic has <n>/<m> open children (use --force to close anyway)`. A missing dependency edge on `dep remove` warns as `Warning: Dependency not found: <from> → <to>` LF, with `→` (not `->`), and the run still exits `0`. | Goldens: `goldens/error_close_blocked.err:1` (`blocked by:` reason), `goldens/close_already_closed.err:1` (`already closed`), `goldens/close_epic_with_open_child.err:1` (`epic has 1/1 open children`), `goldens/dep_remove_absent.err:1` with `goldens/dep_remove_absent.exit:1` = `0`. Source: `src/output/context.rs:1277` (Plain mode writes `Warning: ` + message to stderr), `src/cli/commands/close.rs:469` (`Skipped <id>: <reason>`), `src/cli/commands/close.rs:766` (one warning per skipped item, before the summary error is returned), reasons at `src/cli/commands/close.rs:940` (`already <status>`), `src/cli/commands/close.rs:1109` (`blocked by: …`), `src/cli/commands/close.rs:959` (`… open children …`); `src/cli/commands/dep.rs:801` (`Dependency not found: … → …`). Conflict to settle (OQ-015): `src/output/context.rs:1278` makes the warning a no-op in Quiet, JSON and TOON modes, and the sandbox run of 2026-09-20 (`close proj-170 --quiet` on `@fx=basic`) wrote only the `Error:` and `Hint:` lines to stderr — the `Warning:` line WAS suppressed, contrary to this clause's "never suppressed by `--quiet`" | error_close_blocked, close_already_closed, close_epic_with_open_child, dep_remove_absent |
| S9.27 | In JSON mode a partially-successful batch prints **two** JSON documents to stdout: first the command's own compact result document with its trailing LF, then the pretty-printed error envelope. Golden `error_close_blocked_json` is `{"closed":[],"skipped":[{"id":"proj-170","reason":"blocked by: proj-mta — close the open blocker(s) first, or use --force to close anyway"}]}` LF followed by the envelope of S9.3 with code `NOTHING_TO_DO` — eleven lines here, because a non-null `context` adds a nested object (the whole `.out` file is twelve lines; the `NOT_INITIALIZED` envelope with `"context": null` is nine). Stderr is empty. A consumer that reads one document per stream is wrong about this case. | `src/main.rs:1884`; golden bytes | error_close_blocked_json |
| S9.28 | Several conditions that read like errors are tolerant no-ops with exit `0` and a message on **stdout**: `dep add` of an existing edge prints `Dependency already exists: <from> → <to>` LF; `label add` of a label already present prints `✓ Label <name> already exists on <id>` LF; `label remove` of an absent label prints `✓ Label <name> not found on <id> (no-op)` LF; `reopen` of an already-open issue prints `⊘ Skipped <id>: already open` LF; `undefer` of a non-deferred issue prints `⊘ Skipped <id>: not deferred (status: <status>)` LF. None of these produces `DUPLICATE_DEPENDENCY` or any other exit-5 code, although those codes exist in the source. The GOLDEN wins over the code's category names. | goldens' `.out`/`.exit` bytes; `src/error/structured.rs:161` (`DUPLICATE_DEPENDENCY` unreached) | dep_add_duplicate, label_add_existing, label_remove_absent, reopen_open, undefer_not_deferred |
| S9.29 | `delete` of an issue that has dependents is likewise not an error: without `--force`/`--cascade` it prints the dependent report to stdout and exits `0` (`The following issues depend on issues being deleted:` LF `  - <id>` LF …), and with `--force` it deletes and orphans, also exit `0`. `HAS_DEPENDENTS` (exit 5) is therefore unreached in scope. | goldens' `.out`/`.exit`; `src/error/mod.rs:209` | delete_with_dependents, delete_with_dependents_force, delete_cascade |
| S9.30 | **Empty store.** An existing `.beads/issues.jsonl` of zero bytes is a valid, empty store: every read command succeeds with exit `0`. Plain: `list` prints nothing at all; `count` prints `0` LF; `search <q>` prints `Found 0 issue(s) matching '<q>'` LF; `ready` prints `✨ All work complete — no issues to work on` LF; `blocked` prints `✨ No blocked issues` LF; `dep cycles` prints `✓ No dependency cycles detected.` LF; `stats` prints the ten-line summary block beginning `📊 Issue Database Status` LF LF `Summary:` LF. JSON: `list --json` prints `{"issues":[],"total":0,"limit":0,"offset":0,"has_more":false}` LF; `blocked --json` prints the same envelope with `"limit":50`; `ready --json` prints the bare array `[]` LF — not an envelope; `stats --json` prints a single `{"summary":{…}}` object with every counter `0`. | goldens' bytes; `src/cli/mod.rs:30`, `src/cli/mod.rs:2797` Measured (2026-09-24, OQ-011, OQ-103, fixture selector `@fx=nostore`): a `.beads/` directory with no `issues.jsonl` at all is an empty store too; `create` writes the first record. | empty_list, empty_list_json, empty_ready, empty_ready_json, empty_blocked, empty_blocked_json, empty_stats, empty_stats_json, empty_count, empty_search, empty_dep_cycles, edge_nostore_list, edge_nostore_count_json, edge_nostore_where, edge_nostore_create |
| S9.31 | **No workspace.** With no `.beads` directory at all (`@fx=none`), every command that touches the store fails with S9.8 before reading anything, in both renderings, and writes no file. `version` is the exception: it needs no workspace and exits `0` (measured on `@fx=none`: `br version 0.6.0 (release) (v0.6.0@b1cfebe)` LF). `where` is **not** an exception: it produces S9.8 with exit `2`. | `src/main.rs:1111`; measured | error_no_workspace_list, error_no_workspace_list_json, error_no_workspace_create, (case to add: `error_no_workspace_where`, `version_no_workspace`) |
| S9.32 | **Workspace present, store healthy.** `where` reports the resolved paths and exits `0`. Plain output is four lines: `<beads dir>` LF `  prefix: <prefix>` LF `  database: <db path>` LF `  jsonl: <jsonl path>` LF, each continuation line indented by two spaces. In the sandbox these are `/mnt/proj/.beads`, `proj`, `/mnt/proj/.beads/beads.db`, `/mnt/proj/.beads/issues.jsonl` — note that the database path is reported although `--no-db` never opens it and the file need not exist. JSON output is one compact line, `{"path":"/mnt/proj/.beads","prefix":"proj","database_path":"/mnt/proj/.beads/beads.db","jsonl_path":"/mnt/proj/.beads/issues.jsonl"}` LF, with the key order `path`, `redirected_from`, `prefix`, `database_path`, `jsonl_path` and absent optionals omitted entirely (unlike the error envelope, which keeps `null`s). | `src/cli/commands/where.rs:18`–`src/cli/commands/where.rs:29`, `src/cli/commands/where.rs:235`–`src/cli/commands/where.rs:249` | where_plain, where_json |
| S9.33 | **`version`.** `br version` prints `br version <ver> (<build>) (<branch>@<short commit>)` LF and exits `0` — golden `version_plain`: stdout is exactly `br version 0.6.0 (release) (v0.6.0@b1cfebe)` LF, stderr empty, exit `0`. `br version --json` prints one compact line with the key order `version`, `build`, `commit`, `branch`, `rust_version`, `target`, `features` — golden `version_json`: stdout is exactly `{"version":"0.6.0","build":"release","commit":"b1cfebe05437463e91a353cf2bedafac27266f5b","branch":"v0.6.0","rust_version":"1.100.0-nightly","target":"x86_64-unknown-linux-gnu","features":["self_update"]}` LF, stderr empty, exit `0`. `br version --short` prints `0.6.0` LF. `br version --check` reaches the network and, with no network, prints `Error checking for updates: External command failed: curl: curl failed with status exit status: 60` LF to stderr and exits `2`; that option is outside the port's surface (PLAN §3 excludes `upgrade`). The port cannot truthfully print the commit, rust version or target: OQ-003 owns the substitute contract. Amended (2026-09-25, round 18): `version --short` (`-s`) prints the bare version and LF, and wins over `--json` (`version -s --json` prints `0.6.0`); the port prints its own bare version (DISC-006). | Goldens: `goldens/version_plain.out:1`, `goldens/version_plain.exit:1`, `goldens/version_json.out:1`, `goldens/version_json.exit:1` (both `.err` files are zero bytes). Source: `src/cli/commands/version.rs:117`–`src/cli/commands/version.rs:133` (the plain line; the short commit is the first seven characters, `src/cli/commands/version.rs:120`), `src/cli/commands/version.rs:12`–`src/cli/commands/version.rs:25` (JSON key order; absent optionals and an empty `features` are omitted), `src/cli/commands/version.rs:93` (JSON emission), `src/cli/commands/version.rs:38` (`--short`), `src/cli/commands/version.rs:238`–`src/cli/commands/version.rs:240` (`--check` failure text and exit `2`). `--short` re-measured through the sandbox 2026-09-20 | version_plain, version_json (both captured; the port's substitute contract stays with OQ-003), (case to add: `version_short` — `["version", "--short"]`), r21_wv_version_json_quiet, r21_wv_version_none, r21_wv_version_extra, r21_wv_version_verbose, r22_e_fmt_json_version |
| S9.34 | **Ambiguous-id candidate order.** The list inside the `Ambiguous ID` message, and the identical list in `context.matches`, comes from a hash-ordered structure in the original and varies between runs of the same command; the exit code, the hint and every other byte are stable. DISC-005 canonicalizes the list to ascending byte order of the id in all three places, applied identically to the original and to the port, and the goldens are captured through that canonicalizer. The canonical order for `show m` on `@fx=basic` is `["proj-7vm", "proj-mkh", "proj-mta"]` — note the `, ` separator with a space, and the double quotes inside the human message. | DISC-005; `src/error/mod.rs:69` (`{matches:?}`), probe RS-01 | show_partial_ambiguous, error_show_ambiguous_json, error_update_ambiguous |
| S9.35 | **Partial output at the moment of failure.** A validation or resolution failure is raised before any store write, so the workspace is untouched and no golden for such a case carries a `--- .beads/issues.jsonl ---` dump. A batch failure of S9.14 is the opposite: the successful items are already written when the error is printed, and the golden's stdout continues with the rewritten store. `create --dry-run` and `delete --dry-run` print their preview and write nothing. | goldens: absence/presence of the dump; `src/main.rs:1875` (`handle_error` runs after destructors are in scope) | error_create_bad_priority, delete_dry_run, create_dry_run, (case to add: `error_close_partial`) |
| S9.36 | **Order of checks on startup.** Argument parsing (exit 2, S9.4) precedes workspace discovery (S9.8, exit 2), which precedes the conflict-marker scan (S9.23, exit 7), which precedes JSON parsing of the store (S9.24, exit 7), which precedes id resolution (S9.9–S9.11, exit 3), which precedes field validation (S9.17, exit 4), which precedes graph checks (S9.21–S9.22, exit 5). Evidence: fixture `conflict` holds four syntactically valid JSON lines and two marker lines, and reports the markers rather than a parse error; `usage_limit_bad` fails with exit 2 on `@fx=` default rather than reaching the store. | `src/main.rs:35`, `src/main.rs:70`, `src/sync/mod.rs:10352` | usage_limit_bad, error_conflict_markers, error_malformed_jsonl, error_show_missing |
| S9.37 | **Streams never interleave within one run.** Human errors, warnings and the `tracing` log all go to stderr; every command result and every JSON error envelope goes to stdout. A command therefore never writes an error to stdout in human mode, and never writes an error to stderr in JSON mode. The sandbox captures the two streams into separate files, so no golden pins their relative ordering. | `src/main.rs:1877`–`src/main.rs:1894`, `src/logging.rs` | error_close_blocked (stderr only), error_close_blocked_json (stdout only) |

### S9.38 Coverage note

| S9.n | clause | provenance | cases |
|---|---|---|---|
| S9.38 | Five cases of `cases.tsv` exercise no behavior of S1 or S9 and are cited by no clause here, deliberately: `edge_precision_list_json`, `edge_precision_sort_created`, `edge_precision_sort_title`, `edge_precision_show_json` and `edge_precision_rewrite` pin timestamp ordering and store rewriting (S4/S5/S6, OQ-001 and OQ-002), and the three scenarios `scn_lifecycle`, `scn_dup_title`, `scn_child_ids` pin multi-step state and id generation (S4/S8). Their argv is nonetheless covered by the option inventories of S1.31, S1.35, S1.39 and by the write-path edge rule of S9.35. | The argv of each case as listed: `goldens/cases.tsv:228` (edge_precision_list_json), `goldens/cases.tsv:230` (edge_precision_sort_created), `goldens/cases.tsv:231` (edge_precision_sort_title), `goldens/cases.tsv:232` (edge_precision_show_json), `goldens/cases.tsv:233` (edge_precision_rewrite), `goldens/cases.tsv:243` (scn_lifecycle), `goldens/cases.tsv:245` (scn_dup_title), `goldens/cases.tsv:246` (scn_child_ids); OQ-001, OQ-002 | edge_precision_list_json, edge_precision_sort_created, edge_precision_sort_title, edge_precision_show_json, edge_precision_rewrite, scn_lifecycle, scn_dup_title, scn_child_ids |


## S10. Known bugs and oddities (to be reproduced, not fixed)

<!-- from docs/spec-parts/S6_S8_S10.md -->

| S10.n | behavior | why it is a bug | reproduce with case | decision |
|---|---|---|---|---|
| S10.1 | `""` for `description` and `assignee` vanishes on a rewrite, while `""` for `compacted_at_commit` survives | three optional string fields of the same shape are treated differently: two empty strings are normalized away, the third is preserved verbatim. A reimplementer who applies one rule to all optional strings loses or gains bytes | `goldens/edge_precision_rewrite` — the input record for `proj-aaf` carries all three as `""`; the rewritten line keeps `compacted_at_commit` and drops the other two. `goldens/edge_precision_show_json` shows all three as `""` before the rewrite (`goldens/edge_precision_rewrite.out:8`, `goldens/edge_precision_show_json.out:1`; `src/storage/sqlite.rs:15946` and `:15954` read `description` and `assignee` back through the non-empty reader, `:15977` reads `compacted_at_commit` verbatim; the import list `src/sync/mod.rs:14403`–14420 names the first two and not the third) | bug-compatible (default) · OQ-002 resolved; the per-field table is OQ-007 |
| S10.2 | every mutation rewrites and normalizes **all** records, not only the one that changed | an unrelated record loses its unknown fields, gains defaults it never had, and has its timestamps and label arrays rewritten, purely because some other issue was touched | `goldens/edge_precision_rewrite` — `update proj-aaf -p 1` rewrites all eight records: `unknown_field` is dropped everywhere, `…05.1Z` becomes `…05.100Z`, `12:00:04+02:00` becomes `10:00:04Z`, and records gain `"source_repo":"."`, `"compaction_level":0`, `"original_size":0` | bug-compatible (default) · OQ-002 |
| S10.3 | a bare date for `--until` and `--due` lands at 09:00:00, not at midnight | `2027-06-01` becomes `2027-06-01T09:00:00Z`. Midnight is the obvious reading and is what a reimplementer writes | `goldens/defer_until_date` (`"defer_until":"2027-06-01T09:00:00+00:00"`), `goldens/create_full_json` (`--due 2026-03-01` becomes `"due_at":"2026-03-01T09:00:00Z"`) | bug-compatible (default) |
| S10.4 | the same instant is spelled two different ways in one command's output | the command's JSON result spells an instant with a numeric offset (`+00:00`) while the JSONL line spells the same instant with `Z` | `goldens/defer_until_date` — the result says `"defer_until":"2027-06-01T09:00:00+00:00"`, the rewritten record says `"defer_until":"2027-06-01T09:00:00Z"`. Same in `goldens/close_basic` and `goldens/close_reason_json` for `closed_at` | bug-compatible (default) |
| S10.5 | the JSON error envelope is pretty-printed with two-space indentation on **stdout**, while every successful JSON document is compact | two different serializers on the same stream. A reimplementer who uses one encoder produces the wrong bytes for one of the two | `goldens/error_create_bad_priority_json`, `goldens/error_show_missing_json`, `goldens/error_no_workspace_list_json`, `goldens/error_conflict_markers_json` versus `goldens/list_json`, `goldens/create_min_json` (`goldens/error_create_bad_priority_json.out:2` is an indented line, `goldens/list_json.out:1` is the whole document; pretty `src/main.rs:1888`, compact `src/output/context.rs:1044`) | bug-compatible (default) |
| S10.6 | a failing command can print **two** JSON documents on stdout | the command result is printed first, then the error envelope, so the stream is not a single JSON value | `goldens/error_close_blocked_json` — `{"closed":[],"skipped":[{"id":"proj-170","reason":"blocked by: proj-mta — close the open blocker(s) first, or use --force to close anyway"}]}` followed by the pretty `NOTHING_TO_DO` envelope, exit `3` (`goldens/error_close_blocked_json.out:1` and `:2`; the result is printed at `src/cli/commands/close.rs:445`, the error is returned afterwards at `:790` and rendered at `src/main.rs:1888`) | bug-compatible (default) |
| S10.7 | `ready --json` is a bare array while `list --json` is an envelope | two list-shaped commands disagree on their JSON shape | `goldens/ready_json` (`[ … ]`) versus `goldens/list_json` (`{"issues":[…],"total":7,"limit":0,"offset":0,"has_more":false}`) | bug-compatible (default) |
| S10.8 | the per-issue key order differs between the two shapes | `list`, `search` and `show` emit struct order; `ready` and `blocked` emit alphabetical order | `goldens/list_json` begins `"id","title","status","priority","issue_type",…`; `goldens/ready_json` begins `"created_at","created_by","description","id",…`; `goldens/blocked_json` begins `"blocked_by","blocked_by_count","created_at",…` (`goldens/list_json.out:1`, `goldens/ready_json.out:1`, `goldens/blocked_json.out:1`; field declaration order at `src/model/mod.rs:462`, `src/format/output.rs:26`, `:86`) | bug-compatible (default) · see S6.32 |
| S10.9 | the `limit` reported in an envelope differs by command for the same "no limit given" | `list --json` reports `"limit":0`, `search --json` and `blocked --json` report `"limit":50` | `goldens/list_json` (`"limit":0`), `goldens/search_json` (`"limit":50`), `goldens/blocked_json` (`"limit":50`), `goldens/empty_list_json`, `goldens/empty_blocked_json` | bug-compatible (default) |
| S10.10 | `ready`'s default sort puts a P3 before a P2 | the hybrid bucket collapses P2, P3 and P4 into one group ordered by age, so a P3 created earlier outranks a P2 created later | `goldens/ready_plain` — `1. [● P1] proj-mta`, `2. [● P3] proj-5u2`, `3. [● P2] proj-b75` (`goldens/ready_plain.out:4` before `:5`; `src/storage/sqlite.rs:18257`–18258, used at `:18186`) | bug-compatible (default) · see S6.12 |
| S10.11 | a duplicate title at one instant yields a different id each time | the nonce ladder walks `0..9` until the id is free, so nothing about the visible inputs explains the id | `goldens/scn_dup_title` — three creates of `Same title` at `2026-02-03 09:00:00` yield `proj-86w`, `proj-e1f`, `proj-fa9` | bug-compatible (default) |
| S10.12 | closing an already-closed issue is an **error**, exit `3`, with the warning repeated in the error message | the skip is reported on stderr twice, once as a warning and once inside a `Nothing to do` error, and a no-op is a failure | `goldens/close_already_closed` — stdout empty; stderr `Warning: Skipped proj-mkh: already closed` then `Error: Nothing to do: all 1 issue(s) skipped — proj-mkh: already closed` then `Hint: Skipped issue(s) were already closed or not found.`; exit `3`; no file changes (`goldens/close_already_closed.err:1`, `:2`, `:3`; reason `src/cli/commands/close.rs:940`, warning `:766`, error `:790` with `src/error/mod.rs:277`, hint `src/error/structured.rs:1063`) | bug-compatible (default) |
| S10.13 | reopening an already-open issue is a **success**, exit `0`, and writes nothing | the symmetric no-op of S10.12 takes the opposite decision | `goldens/reopen_open` — stdout `⊘ Skipped proj-mta: already open`, exit `0`, no `last-touched` and no `issues.jsonl` change (`goldens/reopen_open.out:1`; reason `src/cli/commands/reopen.rs:269`, line `:185`, sidecar written only for a reopened issue `:139`–140) | bug-compatible (default) |
| S10.14 | `undefer` on a non-deferred issue is a success and writes nothing | a third no-op with a third shape: a `⊘` glyph, exit `0`, and no sidecar write | `goldens/undefer_not_deferred` — stdout `⊘ Skipped proj-mta: not deferred (status: open)`, exit `0`, no dumps (`goldens/undefer_not_deferred.out:1`; reason `src/cli/commands/defer.rs:588`, line `:517`, sidecar written only for an undeferred issue `:464`–465) | bug-compatible (default) |
| S10.15 | adding a label that already exists is a success, prints a **check mark**, and still writes `last-touched` but not `issues.jsonl` | the check mark announces a change that did not happen, and the sidecar records a touch of an untouched issue | `goldens/label_add_existing` — stdout `✓ Label db already exists on proj-mta`, then a `--- .beads/last-touched ---` dump holding `proj-mta` and **no** `issues.jsonl` dump (`goldens/label_add_existing.out:1`, `:2`; message `src/cli/commands/label.rs:137`, unconditional sidecar write `:253`) | bug-compatible (default) |
| S10.16 | removing a label that is not there prints a check mark and the word `(no-op)` and still writes `last-touched` | same contradiction as S10.15, with the no-op spelled out in the message | `goldens/label_remove_absent` — stdout `✓ Label nope not found on proj-mta (no-op)`, `last-touched` dumped, no `issues.jsonl` dump (`goldens/label_remove_absent.out:1`, `:2`; message `src/cli/commands/label.rs:141`, unconditional sidecar write `:334`) | bug-compatible (default) |
| S10.17 | adding a dependency that already exists is a success with no glyph, and writes `last-touched` | a fourth no-op shape: plain text, an arrow glyph inside the message, exit `0`, sidecar written | `goldens/dep_add_duplicate` — stdout `Dependency already exists: proj-170 → proj-mta`, `last-touched` dumped as `proj-170`, no `issues.jsonl` dump (`goldens/dep_add_duplicate.out:1`, `:2`; message `src/cli/commands/dep.rs:649`, unconditional sidecar write `:595`) | bug-compatible (default) |
| S10.18 | removing a dependency that is not there prints a **warning on stderr**, exits `0`, prints nothing on stdout, and still writes `last-touched` | a fifth no-op shape, and the only one that puts its message on stderr | `goldens/dep_remove_absent` — stdout holds only the `--- .beads/last-touched ---` dump; stderr `Warning: Dependency not found: proj-mta → proj-ptp`; exit `0` (`goldens/dep_remove_absent.err:1`, `goldens/dep_remove_absent.out:1`; warning `src/cli/commands/dep.rs:801`, unconditional sidecar write `:766`) | bug-compatible (default) |
| S10.19 | `update` with the value the field already holds is a no-op **only for `--priority`**; every other field rewrites the record, bumps `updated_at` and rewrites the whole store | one field out of a dozen is guarded by an equality check, and a reimplementer who guards all of them or none of them diverges either way | `goldens/update_noop_same_value` — `update proj-mta -p 1` on an already-P1 issue prints `[{"id":"proj-mta",…,"updated_at":"2026-01-01T10:00:00Z"}]` (the *old* stamp) and dumps only `last-touched`, not `issues.jsonl`. Probe on the same fixture: `update proj-mta --status open` on an already-open issue changes the store hash from `03d6b9206c…` to `a54b294d43…` and reports `"updated_at":"2026-01-02T03:04:05Z"` — the pinned instant — while the `-p 1` that follows leaves the hash untouched. (case to add: `update_noop_same_status` — `["@fx=basic","update","proj-mta","--status","open","--json"]`) | bug-compatible (default) |
| S10.20 | `show` accepts a bare hash with no prefix | `show mta` resolves to `proj-mta`; a reimplementer who requires the full id breaks it, and one who matches too loosely turns a unique match into S10.21 | `goldens/show_partial_hash` — `show mta` renders `proj-mta` in full (`goldens/show_partial_hash.out:1`; the missing-prefix step `src/util/id.rs:872`–877, fallible twin `:962`–967) | bug-compatible (default) |
| S10.21 | an ambiguous partial id lists its candidates in hash-random order, and calls the error `retryable` | the order differs between two runs of the same command on the same bytes, and retrying an ambiguous id produces the same failure forever | `goldens/show_partial_ambiguous`, `goldens/error_show_ambiguous_json` (`"retryable": true`), `goldens/error_update_ambiguous` — all captured through the canonicalizer of `scripts/ws_inner.py` (`goldens/show_partial_ambiguous.err:1`, `goldens/error_show_ambiguous_json.out:4`, `:6`, `goldens/error_update_ambiguous.err:1`; unsorted key iteration `src/cli/commands/show.rs:1105`–1106, error raised at `src/util/id.rs:910`, `AmbiguousId` listed as retryable at `src/error/structured.rs:214`; canonicalizer `scripts/ws_inner.py:99`) | DISC-005 (OrderLeak): candidates in ascending byte order in all three places |
| S10.22 | an unknown **issue type** is accepted while an unknown **dependency type** is refused | two vocabularies of the same shape take opposite decisions, one silently open and one closed with a long allow-list | accepted: `goldens/create_custom_type` (`-t spike` stores `"issue_type":"spike"`, exit `0`). Refused: `goldens/dep_add_custom_type` (`-t inspired-by` gives `VALIDATION_FAILED`, `Unknown dependency type: 'inspired-by'. Allowed types: blocks, parent-child, conditional-blocks, waits-for, related, discovered-from, replies-to, relates-to, duplicates, supersedes, caused-by`, exit `4`) (`goldens/create_custom_type.out:1`, `goldens/dep_add_custom_type.out:4`; an unknown issue type becomes a custom one at `src/model/mod.rs:265`, an unknown dependency type is refused at `src/cli/commands/dep.rs:907`) | bug-compatible (default) |
| S10.23 | an unknown **status** is refused with a message that offers an escape hatch the filter does not otherwise advertise | the message names eight built-in statuses, a policy file and a `--status all` form | `goldens/list_status_bad` — `Error: Validation failed: status: unknown status 'bogus'. Built-in statuses: open, in_progress, blocked, deferred, draft, closed, tombstone, pinned. Custom statuses must be declared in .beads/policy.yaml (workflow.statuses) or exist on at least one issue. Use --status all to include every status`, exit `4` (`goldens/list_status_bad.err:1`; `src/cli/commands/list.rs:484`) | bug-compatible (default) |
| S10.24 | priority accepts both `3` and `P3` | two spellings of one value, and the out-of-range message names both forms | `goldens/create_priority_p_form` (`-p P3` stores `"priority":3`), `goldens/error_create_bad_priority` (`Error: Priority must be 0-4, got: 9` with `Hint: Priority must be 0-4 or P0-P4 (0=critical, 4=backlog); list filters also accept ranges and comma lists such as 0-1 or 0,2.`) | bug-compatible (default) |
| S10.25 | `update --status closed` is refused and redirects to `br close` | the obvious way to close an issue is the one that does not work; the message carries a URL and two alternative invocations | `goldens/update_status_closed` — `VALIDATION_FAILED`, `refusing to close via `br update --status closed`: terminal-state transitions must go through `br close` …`, exit `4` (`goldens/update_status_closed.out:4`; `src/cli/commands/update.rs:1754`) | bug-compatible (default) |
| S10.26 | `update --claim` on a blocked issue names the **blocker** in the error, not the issue that was claimed | the message reads as though the wrong issue was addressed | `goldens/error_update_blocked_claim` — the command is `update proj-170 --claim` and the message is `Error: Validation failed: claim: cannot claim blocked issue: proj-mta`, exit `4` (`goldens/error_update_blocked_claim.err:1`; `src/cli/commands/update.rs:1274` joins the blockers, not the claimed id) | bug-compatible (default) |
| S10.27 | the glyph vocabulary is inconsistent across commands | `✓` means "closed" in a list row, "success" in a create line and "no change" in a label no-op; `⊘` marks a skip in `reopen`/`undefer` while `close` puts its skip behind a `Warning:` on stderr; `✨` appears only in the two empty-result messages; `❄` marks deferred; `◐` in progress; `○` open; `●` precedes every priority; `🚫`, `📋`, `📊`, `⏱` head four different sections | `goldens/list_all` (`✓ proj-mkh`, `◐ proj-ptp`, `○ proj-170`, `❄ proj-7vm`), `goldens/create_min` (`✓ Created proj-fyw: First issue`), `goldens/label_remove_absent` (`✓ Label nope not found on proj-mta (no-op)`), `goldens/reopen_open` (`⊘ Skipped`), `goldens/empty_ready` (`✨ All work complete — no issues to work on`), `goldens/empty_blocked` (`✨ No blocked issues`), `goldens/blocked_plain` (`🚫`), `goldens/ready_plain` (`📋`), `goldens/stats_plain` (`📊`), `goldens/defer_no_until` (`⏱`) (`goldens/list_all.out:1`, `:2`, `:4`, `:8`; `goldens/create_min.out:1`; `goldens/label_remove_absent.out:1`; `goldens/reopen_open.out:1`; `goldens/empty_ready.out:1`; `goldens/empty_blocked.out:1`; `goldens/blocked_plain.out:2`; `goldens/ready_plain.out:1`; `goldens/stats_plain.out:1`; `goldens/defer_no_until.out:1`. Status icons `src/format/text.rs:17`–25, priority bullet `:185`, success mark `src/output/context.rs:1247`, `src/cli/commands/label.rs:141`, `src/cli/commands/reopen.rs:185`, `src/cli/commands/ready.rs:294`, `:247`, `src/cli/commands/blocked.rs:388`, `:393`, `src/cli/commands/stats.rs:1223`, `src/cli/commands/defer.rs:201`) | bug-compatible (default) |
| S10.28 | an empty result set produces four different shapes | `list` prints nothing at all, `ready` and `blocked` print a decorated sentence, `search` prints a count line, `count` prints a bare `0` | `goldens/empty_list` (zero bytes on stdout), `goldens/empty_ready` (`✨ All work complete — no issues to work on`), `goldens/empty_blocked` (`✨ No blocked issues`), `goldens/empty_search` (`Found 0 issue(s) matching 'anything'`), `goldens/empty_count` (`0`) (`goldens/empty_ready.out:1`, `goldens/empty_blocked.out:1`, `goldens/empty_search.out:1`, `goldens/empty_count.out:1`; `goldens/empty_list.out` is zero bytes. `src/cli/commands/list.rs:315`–316, `src/cli/commands/ready.rs:294`, `src/cli/commands/blocked.rs:388`, `src/cli/commands/search.rs:323`, `src/cli/commands/count.rs:109`) | bug-compatible (default) |
| S10.29 | `q` writes a different `source_repo` from `create` and omits `source_repo_path` | two creation commands in the same workspace produce records that differ in two fields | `goldens/q_basic` stores `"source_repo":"."` with no `source_repo_path`; `goldens/create_min`, on the same empty fixture at the same instant, stores `"source_repo":"proj","source_repo_path":"/mnt/proj"`. Also `goldens/q_priority` (`goldens/q_basic.out:3`, `goldens/q_priority.out:3`, `goldens/create_min.out:3`; `q` builds the record with neither field at `src/cli/commands/q.rs:163`–164 and the store falls back to `.` at `src/storage/sqlite.rs:6806`, while `create` fills both at `src/cli/commands/create.rs:143`–144) | bug-compatible (default) |
| S10.30 | `create --dry-run --json` omits `original_size`, which the real create emits | the dry run is not a faithful preview of the record | `goldens/create_dry_run_json` ends `…,"compaction_level":0}` while `goldens/create_min_json` ends `…,"compaction_level":0,"original_size":0}` | bug-compatible (default) |
| S10.31 | `create --ephemeral` reports a created issue and writes `last-touched`, but the store file never changes | the issue exists only in the answer; the next invocation cannot find it | `goldens/create_ephemeral` — stdout carries the full record with `"ephemeral":true`, the golden dumps `--- .beads/last-touched ---` with `proj-kr0` and **no** `issues.jsonl` section (`goldens/create_ephemeral.out:1`, `:2`, `:3`; flag stored at `src/cli/commands/create.rs:502`, export filter `src/storage/sqlite.rs:15004`, sidecar write `src/cli/commands/create.rs:166`) | bug-compatible (default) · see S6.2 |
| S10.32 | `list --fields id,title,status,priority` is ignored | the flag is accepted and the full object is emitted anyway | `goldens/list_fields` is byte-identical to `goldens/list_json` apart from being the same document; every field including `source_repo_path`, `compaction_level`, `dependency_count` and `dependent_count` is present (`goldens/list_fields.out:1` equals `goldens/list_json.out:1`; the only reader of the flag is the CSV branch, `src/cli/commands/list.rs:258`, and the JSON branch `:248` never consults it) | bug-compatible (default) |
| S10.33 | `list --deferred` changes nothing | the flag is accepted and the output equals the default listing, which already includes deferred issues | `goldens/list_deferred` is byte-identical to `goldens/list_plain` (`goldens/list_deferred.out:7` and `goldens/list_plain.out:7` both hold the deferred `proj-7vm`; `src/cli/commands/list.rs:539`–541 — deferred issues are already included when no status filter is given) | bug-compatible (default) |
| S10.34 | `list --quiet` produces **zero bytes** | a listing command with a quiet flag prints nothing at all, not even ids | `goldens/list_quiet` — stdout and stderr both empty, exit `0` (`goldens/list_quiet.out` and `goldens/list_quiet.err` are zero bytes; the early return for quiet mode is `src/cli/commands/list.rs:210`–211) | bug-compatible (default) |
| S10.35 | `dep list <id>` and `show <id>` order the same dependencies differently, and `dep list --json` differs from `dep list` text | three renderings of one relation, three orders | `goldens/dep_list` and `goldens/dep_list_json` agree on `basic` only because priority separates the rows; the probe of S6.25 separates them. (case to add: `edge_dep_list_json_vs_text`) (`goldens/dep_list.out:2`, `:3`, `goldens/dep_list_json.out:1`; text order `src/cli/commands/dep.rs:1239`, JSON order `src/storage/sqlite.rs:14179`, `show` order `src/cli/commands/show.rs:881`) | bug-compatible (default) · see S6.24, S6.25, S6.26 |
| S10.36 | `show --json` returns a bare array even for a single issue, and its projection is not the `list` projection | a single-issue lookup returns a one-element array, and the fields differ from every other issue rendering | `goldens/show_json`, `goldens/show_deferred_json`, `goldens/edge_precision_show_json` (a one-element array carrying `description`, `assignee`, `compacted_at_commit` and an unnormalized `labels` array, and omitting `dependency_count`/`dependent_count`) (`goldens/show_json.out:1`, `goldens/show_deferred_json.out:1`, `goldens/edge_precision_show_json.out:1`; always an array at `src/cli/commands/show.rs:160`, `:333`; the JSONL-direct projection `:506`–508) | bug-compatible (default) |
| S10.37 | `update --json` returns a fixed seven-field projection with explicit `null`s and no trace of what changed | `--add-label`, `--remove-label`, `--set-labels` and `--notes` all produce the same document, which mentions neither labels nor notes, and spells absent values as `null` where every other rendering omits them | `goldens/update_add_label`, `goldens/update_remove_label`, `goldens/update_set_labels`, `goldens/update_notes` all emit `[{"id":"proj-mta","title":"Set up database schema","status":"open","priority":1,"assignee":null,"owner":null,"updated_at":"2026-01-02T03:04:05Z"}]`; `goldens/update_title` differs only in `title` | bug-compatible (default) |
| S10.38 | the comment id counter is global, so the first comment on an issue is not comment `1` | a per-issue reading of the identity gives `1` and is wrong whenever any other issue already has comments | `goldens/comments_add_json` — the first comment on `proj-mta` gets `"id":2` because `proj-ptp` holds comment `1`. Also `goldens/comments_add_author`, `goldens/actor_flag_comment` | bug-compatible (default) · see S6.33 |
| S10.39 | `close` on an epic with open children is refused with a count that reads `1/1` | the fraction reports open over total, not closed over total, which is the opposite of the `epic status` progress line | `goldens/close_epic_with_open_child` — `Warning: Skipped proj-uly: epic has 1/1 open children (use --force to close anyway)`, exit `3`; `goldens/epic_status` reports the same epic as `Progress: 0/1 children closed (0%)` (`goldens/close_epic_with_open_child.err:1`, `goldens/epic_status.out:2`; `src/cli/commands/close.rs:959`–961 prints open over total, `src/cli/commands/epic.rs:300` prints closed over total) | bug-compatible (default) |
| S10.40 | `stats` omits the `Deferred:` summary line when the count is zero but always prints the other six | a conditional row inside an otherwise fixed block | `goldens/stats_plain` on `basic` prints `Deferred: 1`; `goldens/empty_stats` omits the line entirely, and `empty_stats_json` also omits `average_lead_time_hours` while keeping `"deferred_issues":0` | bug-compatible (default) |
| S10.41 | the per-issue lead time is truncated to whole hours before averaging | an average that looks like a real number is computed from truncated integers, so a 2 h 59 m 52 s lead time counts as 2 hours | `goldens/stats_plain` (`Avg Lead Time: 2.0 hours` for a single 2 h 59 m 52 s issue); probe with lead times of 1 s, 79 h 13 m 11 s and 35 h 22 m 33 s yields `"average_lead_time_hours":38.0`, not `38.2`. (case to add: `edge_stats_lead_time_truncation` on a fixture with three closed issues of those lead times) | bug-compatible (default) · see S6.34 |
| S10.42 | `error_create_bad_priority_json` repeats the hint inside `context` | the same string appears twice in one envelope, once as `hint` and once as `context.hint` | `goldens/error_create_bad_priority_json` (`goldens/error_create_bad_priority_json.out:5`, `:9`; the context copy `src/error/structured.rs:729`–731, the top-level hint `:982`) | bug-compatible (default) |
| S10.43 | `retryable` is `true` for errors that cannot succeed on a retry | an ambiguous id, an out-of-range priority and an unknown dependency type are all marked retryable | `goldens/error_show_ambiguous_json`, `goldens/error_create_bad_priority_json`, `goldens/dep_add_custom_type` (all `"retryable": true`), against `goldens/error_show_missing_json` and `goldens/error_close_blocked_json` (`"retryable": false`) (`goldens/error_show_ambiguous_json.out:6`, `goldens/error_create_bad_priority_json.out:6`, `goldens/dep_add_custom_type.out:6`, `goldens/error_show_missing_json.out:6`, `goldens/error_close_blocked_json.out:7`; the retryable set `src/error/structured.rs:205`–216 names `ValidationFailed` `:209`, `InvalidPriority` `:212` and `AmbiguousId` `:214`) | bug-compatible (default) |
| S10.44 | `blocked` prints `Blocked by 1 open dependencies` | the plural is unconditional, while `ready` and `search` do inflect | `goldens/blocked_plain` (`Blocked by 1 open dependencies: [proj-mta]`) against `goldens/ready_limit` (`📋 Ready work (1 issue with no blockers):`) and `goldens/ready_plain` (`(3 issues with no blockers)`) (`goldens/blocked_plain.out:5`, `goldens/ready_limit.out:1`, `goldens/ready_plain.out:1`; the fixed plural `src/cli/commands/blocked.rs:442`, the inflected header `src/cli/commands/ready.rs:247`) | bug-compatible (default) |
| S10.45 | `close` writes `closed_at` from a different clock reading than `updated_at`, so the record's `closed_at` is strictly earlier than its `updated_at` | the two stamps of one atomic transition are not equal, which a reimplementer taking one reading cannot reproduce off the pinned clock | invisible under the pinned clock: `goldens/close_basic` shows both at `2026-01-02T03:04:05`. Probe with the real clock, and S8.31 for the same effect in `comments add`. (case to add: none is capturable through the pinned harness — this is a clock-seam note) | bug-compatible (default) · see S8.31 |
| S10.46 | a comment's `created_at` is taken at **second** resolution while every other stamp carries nanoseconds | the record mixes two precisions written by the same command | invisible under the pinned clock (`goldens/comments_add_json` shows `2026-01-02T03:04:05Z`); probe with the real clock gives a comment at `2026-09-20T06:12:29Z` inside a record whose `updated_at` is `2026-09-20T06:12:29.705963839Z` | bug-compatible (default) · see S8.31 |
| S10.47 | a lock sidecar and a history backup are left behind by every mutation and never cleaned up | three zero-byte lock files and a growing `.br_history/` directory accumulate in a directory that is committed to git | probe of S8.27 and S8.28 after `goldens/create_min`'s argv; no golden dumps them (lock names `src/sync/mod.rs:1374`, `:1389`, `:266`; backup directory `src/sync/history.rs:550`; the harness dumps only the two files of `scripts/ws_inner.py:23`) | DISC-002, DISC-003, DISC-004 |
| S10.48 | `where` prints a `database:` line naming `beads.db` even in `--no-db` mode, where no such file exists or is ever created | the reported path is a fiction in the mode the port implements | `goldens/where_plain` (`database: /mnt/proj/.beads/beads.db`), `goldens/where_json` (`"database_path":"/mnt/proj/.beads/beads.db"`) (`goldens/where_plain.out:3`, `goldens/where_json.out:1`; the path is resolved and always reported at `src/cli/commands/where.rs:71`, `:79`, printed at `:244`) | bug-compatible (default) |
| S10.49 | `where` omits the `prefix:` line entirely when the workspace came from `BEADS_DIR` | one of four lines disappears depending on how the workspace was found | probe: `BEADS_DIR=/mnt/other/.beads br --no-db where` prints the path, `database:` and `jsonl:` and no `prefix:`. (case to add: `env_beads_dir_where`) (the line is printed only when a prefix was detected, `src/cli/commands/where.rs:240`–241; detection yields none when the JSONL holds no id and no database file exists, `:97`–105. Oracle run made for this provenance pass: `where` on the `empty` fixture, with no `BEADS_DIR`, prints the same three lines and no `prefix:`, so the trigger is an undetectable prefix; (case to add: `where_empty` — `["where"]`)) | bug-compatible (default) · see S8.2 |
| S10.50 | a read-only command is not read-only on the filesystem | `list`, `ready`, `show` and the rest each create, populate and unlink an on-disk database in the system temporary directory, and a `SIGPIPE` or `SIGKILL` leaks it | probe of S8.23 during `goldens/list_plain`'s argv; `goldens/list_plain` itself shows no change under `.beads/` (`goldens/list_plain.out:7` is the last line, no state dump follows; scratch file named at `src/storage/sqlite.rs:2714` inside `open_memory` `:2705`, called for every `--no-db` run at `src/config/mod.rs:5379`) | out of scope (PLAN §3 excludes the SQLite store); recorded so the port's "no writes on a read path" is understood as a divergence from the original's filesystem behavior, not from its output |
| S10.51 | statuses, issue types and priorities are accepted in any casing and stored folded to lower case | `--status OPEN`, `-t Bug` and `-p P1` are all accepted and silently rewritten, so the stored bytes differ from the argv the user typed | probe on `basic`: `create CaseTest -t Bug -s OPEN -p P1 --json` stores `"status":"open","priority":1,"issue_type":"bug"`. `in_progress` and `inprogress` are both accepted; `in-progress` is not and becomes a custom status. Priority strips exactly one leading `P` after upper-casing and trims surrounding spaces. (case to add: `create_odd_casing` — `["create","CaseTest","-t","Bug","-s","OPEN","-p","P1","--json"]`) | bug-compatible (default) |
| S10.52 | an unknown status is refused by `list` but accepted by `create` and `update` on a default workspace | the same vocabulary is closed on the read side and open on the write side, so a status the writer stores is a status the reader rejects | refused: `goldens/list_status_bad` (exit `4`). Accepted: the write-side gate only fires when `.beads/policy.yaml` declares a strict workflow, and no fixture ships one. (case to add: `create_custom_status` — `["create","X","-s","frobnicate","--json"]`, then `list --status frobnicate`) (`goldens/list_status_bad.err:1`, read-side refusal `src/cli/commands/list.rs:484`; the write-side gate returns early without a strict workflow, `src/cli/commands/create.rs:259`–260; oracle run made for this provenance pass: `create X -s frobnicate --json` stores `"status":"frobnicate"`, exit `0`) | bug-compatible (default) |
| S10.53 | `--until today` lands at **17:00** while every other bare-date and keyword form lands at 09:00 | one keyword out of five uses a different hour | probe on `basic`: `defer proj-5u2 --until today` at the pinned instant gives `"defer_until":"2026-01-02T17:00:00+00:00"`, while `defer proj-ptp --until tomorrow` gives `"2026-01-03T09:00:00+00:00"`. (case to add: `defer_until_today` and `defer_until_tomorrow`) | bug-compatible (default) · see S10.3 |
| S10.54 | the relative-time grammar advertises a form the argument parser refuses | the error message for a bad `--until` names `-7d` as a valid form, but a leading `-` is consumed by the argument parser before the time parser ever sees it | `goldens/error_defer_bad_until` — `Error: Validation failed: defer_until: invalid time format (try: +1h, -7d, tomorrow, next-week, or 2025-01-15)`. Probe: `defer proj-b75 --until -7d` fails earlier, with `error: unexpected argument '-7' found` and `tip: to pass '-7' as a value, use '-- -7'`. (case to add: `error_defer_negative_relative`) (`goldens/error_defer_bad_until.err:1`; `src/util/time.rs:95`) | bug-compatible (default) |
| S10.55 | the relative unit is the last character only and is case-sensitive | `+1H` is refused while `+1h` is accepted, and the message names a different failure from the one in S10.54 | `goldens/defer_relative` (`+1h` at `03:04:05` gives `04:04:05`). Probe: `defer proj-b75 --until +1H` gives `Error: Validation failed: defer_until: invalid unit (use m, h, d, w)`. (case to add: `error_defer_bad_unit`) | bug-compatible (default) |
| S10.56 | a rewrite **adds** three keys a record never had | `compaction_level` is always emitted as an integer, `original_size` comes back as `0` where the input had no key, and a record with no `source_repo` gains `"source_repo":"."` | `goldens/edge_precision_rewrite` — records that carried none of the three come back with `"source_repo":".","compaction_level":0,"original_size":0` | bug-compatible (default) · OQ-002 · see S10.2 |
| S10.57 | `close` reports an epic's children as open-over-total while `epic status` reports closed-over-total | two commands print the same fraction shape with opposite numerators | `goldens/close_epic_with_open_child` (`1/1 open children`) against `goldens/epic_status` (`0/1 children closed`) (`goldens/close_epic_with_open_child.err:1`, `goldens/epic_status.out:2`; `src/cli/commands/close.rs:959`, `src/cli/commands/epic.rs:300`) | bug-compatible (default) · see S10.39 |
| S10.58 | `ready` omits the status glyph that every other list-shaped command leads with, and uses a different separator | a `ready` line and a `list` line for the same issue differ in three ways: the glyph, the separator and a leading index | `goldens/ready_plain` (`1. [● P1] [task] proj-mta: Set up database schema`) against `goldens/list_plain` (`○ proj-mta [● P1] [task] - Set up database schema`) (`goldens/ready_plain.out:3`, `goldens/list_plain.out:3`; `src/cli/commands/ready.rs:369`, `src/format/text.rs:332`) | bug-compatible (default) · see S10.27 |
| S10.59 | `●` carries two meanings and `❄` carries two statuses | `●` is the `blocked` status glyph **and** the bullet inside every priority badge, so a blocked issue's line begins `● … [● P2]`; `❄` marks both `deferred` and `draft`, which are also the same colour | `goldens/list_plain` and `goldens/list_all` show `[● P2]` on every row regardless of status; `goldens/blocked_plain` shows `[● P1]` on a blocked issue. (case to add: `edge_glyph_blocked_row` — a fixture with an issue whose stored `status` is `blocked`, listed) (`goldens/list_plain.out:1`, `goldens/list_all.out:8`, `goldens/blocked_plain.out:4`; blocked icon `src/format/text.rs:21`, priority bullet `:185`, one icon for `deferred` and `draft` `:110`, one colour `:156`) | bug-compatible (default) · see S10.27 |
| S10.60 | a dangling dependency renders as a fabricated tombstoned P2 issue | an edge pointing at an id the store does not hold is rendered as a real issue titled `[missing issue: <id>]` with status `tombstone` and priority `2`, rather than being reported as missing | `goldens/dep_add_external` adds `proj-mta -> external:other-1` and the store keeps the edge; a later `show proj-mta` renders the external target. (case to add: `edge_dangling_dependency_show` — a fixture with an edge to an absent id, then `show <id>` and `show <id> --json`) (`goldens/dep_add_external.out:8` holds the stored edge; the fabricated entry — title, `tombstone`, medium priority — is built at `src/cli/commands/show.rs:1084`–1086) | bug-compatible (default) |


## S11. Performance characteristics of the original

<!-- from docs/spec-parts/ARCHITECT_S3_S11_S12.md -->

| S11.n | clause | provenance | cases |
|---|---|---|---|
| S11.1 | In `--no-db` mode every invocation opens a private in-memory SQLite database and imports the whole of `issues.jsonl` into it before answering, so the cost of every command, reads included, grows with the size of the store | `src/config/mod.rs:4458` (the `--no-db` open path; S2.A) | `list_json`, `ready_json` |
| S11.2 | Every mutating invocation rewrites the whole store, normalizing every record, whatever the number of records it changed (OQ-002) | `src/sync/mod.rs:10613`, `src/sync/mod.rs:12293` (export and normalization; S5.A) | `edge_precision_rewrite`, `update_priority` |
| S11.3 | The hot computations of the in-scope surface are: JSON decoding and encoding of every record; SHA-256 for each id candidate; the sort of `list`, `ready` and `blocked`; the BLOCKED relation's propagation over parent-child edges. These are the Phase 5 hotspot candidates; none is measured in Phase 1 | `src/util/id.rs:306`, `src/storage/sqlite.rs:10977`, `src/storage/sqlite.rs:18186`, `src/sync/mod.rs:10779` | `list_json`, `blocked_json`, `create_min` |
| S11.4 | No timing of the original is recorded in Phase 1. The measured baseline is produced in Phase 5 by `scripts/incumbent-bench.sh --pin`, against the pinned binary inside the same sandbox, at thread parity (PLAN §5) | `docs/PLAN_TO_PORT_BEADS_RUST_TO_BEND2.md:103` (PLAN §5) | `list_json` |


## S12. Provenance and coverage

<!-- from docs/spec-parts/ARCHITECT_S3_S11_S12.md -->

| S12.n | clause | provenance | cases |
|---|---|---|---|
| S12.1 | The source read is the pinned tag `v0.6.0` (commit `b1cfebe05437463e91a353cf2bedafac27266f5b`, tree `45cc06f56529d614d2602a327f469a8478fea17e`), a `git archive` under `legacy/BEADS_RUST_v0.6.0/`: 113 Rust files, 257,744 lines. The live tree `/dp/beads_rust` (271 commits past the tag on 2026-09-20) was not read for any clause | `legacy/BEADS_RUST_v0.6.0/src/lib.rs:1` | `create_min` |
| S12.2 | Behavior evidence is the 235 captured cases of `goldens/cases.tsv` (MANIFEST sha256 `ae5b18770a62af73…`, floor STABLE 235/235) plus the extractors' own runs of the pinned binary through `scripts/ws-run.sh --oracle br --no-db ::`. Where source and golden disagreed the golden won, and the clause says so | `goldens/MANIFEST.txt:1` | `list_json`, `error_conflict_markers` |
| S12.3 | Extraction was one pass by seven section-owning extractors on 2026-09-20: S1+S9 105 clauses, S2+S5.1–99 110, S4.1–99+S7 104, S4.100–199 99, S4.200–399 134, S5.100+ 153, S6+S8+S10 139; the architect's S3, S11, S12 are 23. Every one of the 235 cases is cited by at least one clause; 46 of the 113 source files are cited | `docs/spec-parts/EXTRACTOR_BRIEF.md:1` | `usage_none`, `scn_lifecycle` |
| S12.4 | **Not yet done, and therefore not claimed:** the second extraction pass over the same files and the third, case-keyed expansion pass (SPEC-EXTRACTION "Three passes"); the self-containment review; the capture of the 215 `(case to add: …)` proposals and the resolution of the open rows of `docs/OPEN_QUESTIONS.md`. Until those run, a clause marked `[inference]` or carrying `(case to add` is a hypothesis | `docs/OPEN_QUESTIONS.md:1` | `create_min` |
| S12.5 | Source files of the snapshot that no clause cites, by area: `cli/` 33 (the excluded commands: doctor, upgrade, agents, audit, history, query, schema, vcs and the like), `output/` 7 and `format/` 6 (Rich and TOON rendering), `mcp/` 4, `sync/` 3 and `storage/` 2 (database reconciliation), and 14 single files (`close_policy.rs`, `policy.rs`, `coordination.rs`, `write_combining.rs`, `franken_sync.rs`, `health.rs`, `cache.rs`, `lib.rs` and others). They correspond to PLAN §3 exclusion rows; a behavior of an in-scope command that lives in one of them is a finding for pass two | `legacy/BEADS_RUST_v0.6.0/src/lib.rs:1` | `usage_unknown_cmd` |

## Appendix A. Handover notes, as the extractors wrote them

### from docs/spec-parts/S1_S9.md


- **S3 (data model).** The `error` envelope is a five-field record with a fixed
  key order and `null`-preserving optionals; `context` is an untyped JSON value
  whose shape varies per code (the key sets are listed per row in S9.8). The
  `where` output is a five-field record with *omitted* optionals — the opposite
  optional policy from the error envelope. `--limit`/`--offset` are
  pointer-width unsigned; `--priority-min`/`--priority-max` are 8-bit unsigned;
  `--estimate` is signed 32-bit; `--lock-timeout` and `--activity-hours` are
  unsigned 64- and 32-bit.
- **S5 (outputs).** S9.30 quotes the empty-store bytes only as edge evidence;
  the general Plain and JSON layouts of `list`, `ready`, `blocked`, `stats`,
  `count`, `search`, `show`, `where` — including the glyph vocabulary
  (`✓ ○ ● ⊘ ✨ 📊`), the pagination envelope
  `{issues,total,limit,offset,has_more}`, the bare-array shape of `ready --json`
  and the two-space-indented pretty-printing used *only* by the error envelope —
  belong to S5. S5 also owns the observation that command results are compact
  JSON with one trailing LF while the error envelope is pretty-printed.
- **S6 (order).** Two order leaks surface through my sections: the
  ambiguous-id candidate list (hash-ordered in the original, canonicalized to
  byte order by DISC-005, S9.34) and the fixed declaration order of the
  subcommand list in `usage_none` and of the option lines in the `Options:`
  block (S1.16, S1.28). The `context` object's key order follows the literal
  construction order, not any sort.
- **S7 (numerics).** The argument-parse error texts expose the parsers:
  `invalid digit found in string` for the integer types and
  `<n> is not in 0..=255` for the 8-bit range check (S1.21, S1.22). `--limit +1`
  parses; `--limit -1` does not reach the parser at all. `0` is a sentinel
  meaning "unlimited" for `list --limit`, `ready --limit`, `blocked --limit` and
  `search --limit`, which S7 needs as a range note.
- **S8 (effects).** The error path performs exactly two effects: one write to
  stderr or stdout and one process exit with the code of S9.6. Environment
  reads are enumerated in S1.57–S1.67. `.beads/last-touched` is read by
  `show`/`update`/`close`/`reopen` when no id is given (S1.55). A run that
  reaches S9.14 has already written `issues.jsonl` and `last-touched` before it
  exits non-zero.
- **S10 (oddities).** Four candidates found here: (1) the usage line of
  `usage_unknown_flag` names the *suggested* option next to the already-consumed
  one (`Usage: br --no-db --no-auto-flush <COMMAND>`), and `--li` likewise
  produces `Usage: br list --limit <LIMIT>` (S1.15, S1.11); (2) the literal
  backticks around `SQLite`, `br close`, `dep add` and the like inside help and
  error text, which come from doc comments written for a lint (S1.28, S9.20,
  S9.21); (3) `INVALID_PRIORITY` duplicates its hint inside `context.hint`
  (S9.15); (4) the comma-splitting of label options is inconsistent across
  `create -l` (splits), `label add -l` (splits), `update --set-labels` (splits,
  by hand), `update --add-label` (does not), `list -l` (does not) — S1.33,
  S1.40, S1.52. Also: several codes that exist with an exit-5 or exit-6
  category are never reached, because the corresponding situation is a
  stdout no-op or is reported as a configuration error (S9.7, S9.28, S9.29).
- **S4 (algorithms).** The partial-id resolution rule that decides between
  `ISSUE_NOT_FOUND`, `AMBIGUOUS_ID` and a hit (S1.56) is an S4 algorithm; S9
  only records its three outcomes.


### from docs/spec-parts/S2_S5a.md


| fact | owner |
|---|---|
| The `ORDER BY id ASC` of S5.3, the intra-record orderings of S5.13–S5.15, and the `show` dependency and dependent sort of S5.31 are order leaks | S6 |
| The comparators behind `list`, `ready`, `blocked` and `search`, and the chronological-versus-textual timestamp question settled by OQ-001 | S6 |
| The widths behind S5.11 — 32-bit signed priority, estimate, compaction level and original size; 64-bit signed comment id; unsigned counts — and the single 64-bit float of S5.54 | S7 |
| The birthday-bound id-length table, the nonce ladder that gives `scn_dup_title` three distinct ids, and the `<parent>.<n>` child-id rule that `scn_child_ids` exercises | S4, S7 |
| Effects: reading the store, writing the store, writing and removing `last-touched`, creating `.beads/` — with the `--no-db` ordering "flush first, sidecar second" of S5.18 | S8 |
| Every verbatim error message with its stream and exit code, including the validation texts of S2.43–S2.56 and the per-error contents of the S5.21 envelope | S9 |
| `br q` writing `"source_repo":"."` while `br create` writes the workspace name (S5.19); `--fields` silently ignored under `--json` (S5.24); `show` reading the file while every other read command reads the loaded model, so the two disagree about the same issue (S5.30, S2.39); a stored custom dependency type loading while `dep add -t` refuses one (S2.33) | S10 |
| The 44-member issue record, the 7-member dependency record and the 5-member comment record, with which members are optional and which are forced non-empty by the round trip (S5.5, S5.7, S5.9) | S3 |
| The Plain-text rendering of every command | S5.100+ |


### from docs/spec-parts/S4a_S7.md


- **S6 (order):** the substring-match candidate list is produced in ascending id order by the store's own `ORDER BY id` over a TEXT column (byte order), and the harness sorts it again — the two agree, but the underlying order is the store's. See S4.44 and DISC-005.
- **S6 (order):** `parse_priority_filter` returns its selection through an ordered set, so filter values are always ascending and de-duplicated regardless of the order typed (S4.67).
- **S6 (order):** labels collected at `create` keep first-occurrence order in memory, while the JSONL write-back sorts and de-duplicates them (`edge_precision_rewrite` shows `["alpha","zeta"]`). The write-back rule belongs to S5.
- **S5 (outputs):** the exact plain-text shapes quoted here (`✓ Created <id>: <title>`, `Dry run: would create issue <id>`, `  Avg Lead Time:          2.0 hours`, the `Error:`/`Hint:` pair) are cited only as evidence for the algorithm; their column widths, glyphs and trailing newlines are S5's to pin.
- **S5 (outputs):** the `defer` JSON payload's `+00:00` offset spelling versus the store's `Z` (S4.64) is a rendering split worth an S5 clause of its own.
- **S3 (data model):** `content_hash` is present on the record type but skipped by its JSON representation (S4.51); the model section owns that field's existence, this section owns its arithmetic.
- **S3 (data model):** `original_size` is declared as an optional signed 32-bit field yet appears as `0` on every exported record, so the loader materializes it — that materialization is S2/S5's.
- **S8 (effects):** the clock is read once per create attempt (S4.52) and again on each of the up-to-ten insert retries (S4.14); the retry path also sleeps 10, 20, … milliseconds, which is unreachable in `--no-db`.
- **S8 (effects):** `--no-db` rebuilds the child-counter table from the file on every invocation (S4.25); that rebuild is an effect of the load, not of `create`.
- **S9 (errors):** every error string quoted here carries its stream and exit code inline; S9 owns the full table.
- **S10 (oddities):** a non-numeric priority is reported **uppercased** (`-p x` → `got: X`) because the uppercase precedes the parse (S4.66). Reproduce, do not fix.
- **S10 (oddities):** a filter token such as `-1` splits at its first `-` into an empty left part and therefore falls through the range branch to the single-value branch, where it is reported as `-1` (S4.67).
- **S10 (oddities):** id lookup lowercases with full Unicode rules, not ASCII rules (S4.36), so a non-ASCII uppercase input can change byte length before the lookup.
- **S10 (oddities):** the id seed measures fields in bytes while the title limit measures in characters (S7.1 versus S4.75) — a 500-character CJK title is 1500 seed bytes and still valid.
- **S10 / DISC-005:** the ambiguous-candidate order is canonicalized by the harness (S4.44).


### from docs/spec-parts/S4b.md


**Order → S6.** Every ordering below is a sort or an iteration order that reaches stdout.

| structure / sort | order exhibited | reaches output via | clause |
|---|---|---|---|
| `list` default | priority ASC, `created_at` DESC (chronological), id ASC (bytes); total, no observable stability | `list`, `search`, `epic status`, the JSON page | S4.115, S4.110, S4.111 |
| `list --sort <key>` | see the four key orders; `created_at`/`updated_at` are DESC un-reversed; id ASC always last | `list`, `search` | S4.116, S4.117 |
| title collation | ASCII-only case-insensitive; any ≥ 0xC2 byte after all ASCII | `--sort title` | S4.112 |
| `ready` hybrid | bucket(priority ≤ 1) ASC, `created_at` ASC, id ASC — priority is *not* a key inside a bucket | `ready` | S4.174 |
| `ready` priority / oldest | priority ASC then `created_at` ASC then id ASC / `created_at` ASC then id ASC | `ready --sort` | S4.175, S4.176 |
| `blocked` | priority ASC, **blocker count DESC**, `created_at` DESC, id ASC | `blocked` | S4.169 |
| blocker ref list | ascending byte order of the annotated ref, then adjacent dedup | `blocked_by`, the text `[…]` list | S4.162 |
| `count` groups | ascending byte order of the group key string (`(` before letters) | `count --by-*` | S4.149 |
| `dep list` | JSON keeps the query order (endpoint priority ASC with missing = 2, `created_at` DESC, id ASC); text re-sorts by priority, `issue_id`, `depends_on_id`, type | `dep list` | S4.180 |
| `dep tree` | depth-first pre-order; siblings by priority, status rank, title, id | `dep tree` | S4.183, S4.184 |
| `dep cycles` | components sorted; witnesses sorted as id vectors; nodes inside a witness follow the walk from the lowest id | `dep cycles` | S4.187 |
| `show` | requested input order across targets; labels ASC; deps/dependents by S4.180; comments `created_at` ASC then id ASC | `show` | S4.194, S4.195 |
| BLOCKED build | three ordered steps — direct, propagate, epic rollup — and the order is observable | `ready`, `blocked`, `stats` | S4.161 |

**Numerics → S7.** `priority` is a small signed integer confined to `0..=4` by every parser (S4.107, S4.108). Counts (`total`, `count`, `blocked_by_count`, group counts, `total_children`) are unsigned machine words; the corpus reaches 8. The epic percentage is an integer truncating division computed in 128-bit width (S4.191). The average lead time is the only floating-point figure: whole-hour integers (a truncating conversion from a duration, S4.154) summed and divided by the count; the corpus reaches 2.0 and 558.0 hours, and the 558.0/24 = 23.25 case lands exactly on a ties-to-even boundary when printed to one decimal (`23.2`, not `23.3`) — S5 owns the printing, S7 owns the fact that the value is exact in binary.

**Effects → S8.** Every command in this section is read-only: it reads `.beads/issues.jsonl` (and `.beads/policy.yaml` for the ready status group and the custom-status vocabulary, and `.beads/last-touched` when `show` has no positional id), reads the wall clock once (S4.113), and writes only stdout/stderr. The stale blocked-cache projection is recomputed in memory and never persisted on a read path. Whether a read-only command ever creates a file under `.beads/` is OQ-008.

**Oddities → S10.** (1) `list --fields` is silently ignored outside CSV (S4.134). (2) `list --deferred` reads as "only deferred" but is a no-op on the default corpus (S4.131). (3) `count`'s default corpus hides deferred issues while `list`'s shows them (S4.144). (4) `--title-contains` and `search` fold ASCII case only, while `--desc-contains`/`--notes-contains` fold Unicode case (S4.109, S4.129, S4.137). (5) `search -p 0-1` is an error while `list -p 0-1` is accepted — `search` parses each `--priority` value as a single priority with no ranges or comma lists (S4.140). (6) The `blocked` text line says `open dependencies` even for one blocker and even when the blocker ref is a `:parent-blocked` or `:child-open` rollup rather than a dependency. (7) `stats` counts templates and every custom-status issue in `total_issues` but in none of the per-status counters (S4.151). (8) `ready`'s hybrid default can list a P3 above a P2 (S4.174). (9) `--blocking-only` on `dep cycles` changes nothing (S4.186).

**Data model → S3.** Fields this section reads: `id`, `title`, `description`, `notes`, `status`, `priority`, `issue_type`, `assignee`, `created_at`, `updated_at`, `closed_at`, `defer_until`, `due_at`, `ephemeral`, `pinned`, `is_template`, `labels`, `dependencies` (`issue_id`, `depends_on_id`, `type`), `comments` (`id`, `author`, `text`, `created_at`). Absent booleans (`ephemeral`, `pinned`, `is_template`) behave as false everywhere. Absent optional strings never match a containment filter. A dependency target that is not a stored issue is legal and carries the synthetic status `unknown` in a blocker ref (S4.157). `search`'s corpus includes comment text, so comments are part of the queryable surface.


### from docs/spec-parts/S4c.md


**To S6 (order).**
- Store line order after every mutation is ascending byte order of `id` (S4.206); hierarchical ids sort as plain strings, so `proj-uly.1.1` precedes `proj-uly.2`.
- Multi-id `update`/`close` emit their per-id output blocks in command-line order; `reopen`/`undefer`/`defer` emit all successes before all skips (S4.314, S4.327).
- `label add`/`label remove` iterate labels in the outer loop and issues in the inner loop (S4.370).
- `delete` sorts its id set ascending BEFORE resolution and again before tombstoning, so the command-line order never reaches the output; the `--cascade` edge-removal pass iterates a hash set in unspecified order, but that pass produces no ordered output (S4.335, S4.340).
- The `dependencies` array of a record created with several `--deps` is re-ordered by the export, not by the command (S4.229).
- `dep cycles` is the only surface that prints an actual cycle witness; `dep add` never does (S4.355).
- The ambiguous-id candidate list is hash-random in the original and is canonicalized to byte order by `scripts/ws_inner.py` (DISC-005) — S4.275.

**To S7 (numerics).**
- `priority` is an integer 0–4 inclusive; out-of-range is refused (S4.239). Default `2`.
- `estimated_minutes` is a signed 32-bit integer; negatives are refused (`cannot be negative`) and values above `525960` are refused (`exceeds maximum (525960 minutes / ~1 year)`).
- `TITLE_MAX_CHARS` = **500**, counted in Unicode scalars, not bytes (S4.222).
- Label length limit = **50** bytes; `external_ref` limit = **200** bytes with no whitespace allowed.
- `ISSUE_LABEL_MAX_COUNT` = **64** (`src/validation/mod.rs:24`); exceeding it gives `exceeds 64 labels`. The corpus never reaches it.
- `comments add -f` / `-f -` caps its input at **10485760** bytes (10 MiB) and requires valid UTF-8 (S4.381).
- A bare date given to `--until`, `--due` or `--defer` resolves to **09:00:00 local time** of that day, not midnight; the keyword `today` resolves to **17:00:00 local time** (S4.322). Under `TZ=UTC` local equals UTC.
- The cap on `create -f` bulk import and on `delete --from-file` input sizes is unmeasured.
- `compaction_level` and `original_size` default to `0` on every created record; `--dry-run --json` omits `original_size` (S4.235).
- Comment ids are a monotone integer sequence over the whole store, `max + 1` (S4.382).
- `close` reports `closed_at` with a `+00:00` offset in JSON while the store writes `Z` (S4.298); `defer` does the same for `defer_until` (S4.322).
- The cycle search is depth-bounded (`DEPENDENCY_TRAVERSAL_MAX_DEPTH`, `src/storage/sqlite.rs:6976`); beyond that depth a cycle is not reported.

**To S8 (effects).**
- Files written by this surface: `.beads/issues.jsonl` (one full rewrite per invocation, only when something is dirty — S4.202, S4.203) and `.beads/last-touched` (S4.207–S4.211).
- `.beads/last-touched` is created with mode `0600`, truncated, written best-effort (a failure never changes the exit code), and holds `<id>\n`.
- `delete` never creates `last-touched`; it removes the file when the stored id was deleted (S4.210).
- `label rename` writes the store but not `last-touched` (S4.211).
- `create --ephemeral` writes `last-touched` but not the store (S4.234).
- `create --dry-run`, `delete --dry-run` and `epic close-eligible --dry-run` write nothing at all (S4.235, S4.338, S4.393).
- Reads: `--description-file <path>` and `comments add -f <path>` read a file (or stdin for `-`) exactly once, before any mutation; `create -f`, `delete --from-file`, `--agent-context @path` likewise (S4.225, S4.237, S4.340).
- Clock: every mutating command reads the wall clock once per issue update for `updated_at`/`closed_at`/`deleted_at`/`created_at`; in a pinned run all of them equal the pinned instant.
- Environment read on this surface: `USER`, plus `BD_ACTOR`, `BEADS_ACTOR`, `LOGNAME`, `USERNAME` for the comment-author ladder, plus `BR_AGENT_NAME`, `BR_HARNESS`, `BR_MODEL` (recorded on events only, never in the JSONL — S4.214).
- Excluded effects observed in the source but not on this surface: `.br_history/` backups, the lock files, `close_metadata`, `policy.yaml` gates.

**To S10 (oddities, reproduce, do not fix).**
- `update --status <same value>` bumps `updated_at` and rewrites the store, while `-p <same value>` does not (S4.262 vs S4.264).
- `close` exits **3** on a partially applied batch although it did write the closures (S4.294) — the only non-zero exit with a rewritten store.
- `reopen` has no `NOTHING_TO_DO`: an all-skipped reopen exits **0** while an all-skipped close exits **3** (S4.313 vs S4.293).
- `delete` with dependents and no flag is a silent preview that exits **0** (S4.333).
- `references_updated` is always `0`; `labels_removed`/`events_removed` are always `0` for a soft delete even when the issue has labels (S4.336).
- `create --dry-run --json` omits `original_size` although a real create emits it (S4.235).
- `create --deps` accepts `blocked-by` and lists it in its error text; `dep add --type` does not (S4.229 vs S4.352).
- `q` writes no `source_repo`/`source_repo_path`, so its records end up with `"source_repo":"."` while `create`'s carry the directory basename (S4.251).
- `dep add`/`dep remove` write `last-touched` even when they changed nothing (S4.209, S4.358, S4.360).
- `close` prints `closed_at` with `+00:00` in JSON but writes `Z` to the store (S4.298).
- `create --status tombstone` produces a tombstone without `delete_reason` or `original_type`, unlike `delete` (S4.231 vs S4.330).
- `epic close-eligible` writes `close_reason` `All children completed` while `close` without `--reason` writes `done` — two different defaults for the same field (S4.391 vs S4.291).
- `defer` WITHOUT `--until` clears an existing `defer_until` rather than leaving it alone (S4.323).
- `label remove` of an absent label prints the SUCCESS glyph `✓` on stdout, while `dep remove` of an absent edge prints `Warning:` on stderr (S4.375 vs S4.360).
- `epic close-eligible --dry-run` prints `Would close 0 epic(s):` where the non-dry run prints `No epics eligible for closure` (S4.392 vs S4.393).
- `label add`'s positional split makes the LAST positional a label even when it is id-shaped (S4.370).

**To S3 (data model).**
- Fields this surface writes and their exact JSON key order as the store shows them: `id`, `title`, `description`, `acceptance_criteria`, `status`, `priority`, `issue_type`, `assignee`, `owner`, `estimated_minutes`, `created_at`, `created_by`, `updated_at`, `closed_at`, `close_reason`, `closed_by_session`, `due_at`, `defer_until`, `external_ref`, `source_repo`, `source_repo_path`, `deleted_at`, `deleted_by`, `delete_reason`, `original_type`, `compaction_level`, `original_size`, `notes`, `labels`, `dependencies`, `comments`, `ephemeral`.
- Dependency edge record: `issue_id`, `depends_on_id`, `type`, `created_at`, `created_by`, `metadata` (default the two-character string `"{}"`), `thread_id` (default the empty string `""`) — S4.350.
- Comment record: `id`, `issue_id`, `author`, `text`, `created_at` — S4.380.
- `content_hash` is recomputed on every mutation but never exported (S4.214).
- `status` reaches the values `open`, `in_progress`, `closed`, `deferred`, `tombstone` on this surface; `blocked`, `draft` and `pinned` appear only in skip messages.

---


### from docs/spec-parts/S5b.md


Facts discovered here that belong to another section's owner:

- **S5a (JSON / the JSONL store).** Every `*_json` case, the `{"error":{…}}` pretty-printed envelope that `dep_add_custom_type` and `update_status_closed` put on **stdout** with exit 4 (a plain run would have put `Error:` on stderr instead), and the `--- .beads/issues.jsonl ---` bodies. `close --suggest-next`, `close --force`, `reopen`, `defer`, `undefer` and `delete` emit a JSON document even without `--json` when `use_json` is set internally (`close.rs:635`, `reopen.rs:143`, `defer.rs:181`); `close_blocked_force` proves it. Also: `dep add`/`dep remove`/`label`/`comments`/`count` use the *pretty* JSON writer while `create`/`q`/`ready`/`list` use the compact one.
- **S6 (order).** Row order in `list`, `ready`, `blocked`, `search`, `dep list`, `count --by-*` group order, `label list-all` order, `show`'s dependency/dependent/comment order, `epic status` order, and the `close`/`update` per-id report order (argv order, `update.rs:1417`; `delete`'s ids are sorted, `delete.rs:636`). `blocked` sorts by priority ascending then blocker count descending (`blocked.rs:208`). `list --tree` sorts sibling groups by the numeric trailing id segment (`list.rs:775-788`).
- **S7 (numerics).** The epic percentage is integer truncation of `closed * 100 / total` computed in 128-bit (`epic.rs:435-443`). The average lead time is an `f64` printed with `{:.1}`, switching unit at exactly 24.0 hours (`stats.rs:1252-1260`) — the rounding mode at a `.x5` boundary is a NUMERIC_PLAN row. Display widths for truncation use `unicode-width`, not byte or char counts (`src/format/text.rs:258`), which matters only on the excluded TTY path.
- **S8 (effects).** `create --dry-run` and `delete --dry-run` and the `delete` dependents preview write no file. `dep remove` on a missing edge still rewrites the store (`dep_remove_absent`). `show`'s deferred countdown and `list --overdue` read the wall clock at render time. `br show` may open a second transient storage handle for inherited context (`show.rs:340-352`).
- **S9 (errors).** The full `Error:`/`Hint:` catalogue, exit codes, and the clap usage texts of `usage_*`. This part fixes only the stream (stderr), the two-line shape, and the fact that stdout is left empty or partial.
- **S10 (oddities to reproduce, not fix).** (a) `Blocked by 1 open dependencies` is never singularized. (b) `br reopen` ignores `--quiet` and prints anyway; `br create --silent` overrides every mode including `--json`. (c) `show` says `Deferred until:` while `list --long` says `Deferred Until:`. (d) `dep add` uses an ASCII `->` for success and a Unicode `→` for "already exists"; `dep remove` does the same split. (e) `search`'s truncation note goes to stdout while every other truncation note goes to stderr. (f) `blocked`'s header count is the pre-truncation total while `ready`'s is the post-truncation count. (g) `delete` has two renderers whose bytes differ (S5.238). (h) `show`'s `Owner:` line invents a value from `$USER` when the record has none.
- **S3 (data model).** The plain surface reveals: status is an open set (a custom status renders with `?` and its own lower-case name), issue type is an open set (`create_custom_type` stores `spike`), priority is a small integer rendered `P{n}`, and comment bodies may carry interior newlines and tabs.


### from docs/spec-parts/S6_S8_S10.md


Facts discovered here that belong to another section's table.

| fact | owner |
|---|---|
| `list` default filtering excludes `closed` and `tombstone` but includes `deferred`; `--all` adds closed (`list_plain` 7 rows, `list_all` 8 rows, `count_plain` `6` versus `count_include_closed` `8` — `count` excludes deferred as well as closed) | S4 |
| the `list` envelope is `{"issues":[…],"total":N,"limit":L,"offset":O,"has_more":B}`; `search`'s is `{"issues":[…],"hidden_closed_count":N,"limit":50,"offset":0,"has_more":B}`; `blocked`'s is the `list` envelope with `limit` `50` | S5 |
| the exit-code table `0`/`2`/`3`/`4`/`5`/`7` with the error codes `NOT_INITIALIZED`, `ISSUE_NOT_FOUND`, `AMBIGUOUS_ID`, `NOTHING_TO_DO`, `VALIDATION_FAILED`, `INVALID_PRIORITY`, `CYCLE_DETECTED`, `CONFIG_ERROR` and their verbatim messages and hints | S9 |
| blocking dependency types are `blocks`, `conditional-blocks` and `waits-for`; `related`, `parent-child`, `discovered-from`, `replies-to`, `relates-to`, `duplicates`, `supersedes` and `caused-by` do not block. Probe: an issue with four dependencies of which one is `related` is reported as `Blocked by 3 open dependencies` | S4 |
| a child id is `<parent>.<n>` with `n` starting at `1` (`create_with_parent` yields `proj-uly.1`; a grandchild of `proj-uly.1` is `proj-uly.1.1` in `scn_child_ids`) | S4 |
| the id seed is length-prefixed `title`, `description`, `creator`, the `created_at` nanosecond count and the nonce; the only entropy is the clock and the actor | S4 |
| the average lead time is an `f64` computed from per-issue integer hours; `average_lead_time_hours` is absent from the JSON when no issue is closed | S7 |
| `estimated_minutes` is an integer (`create_full_json` stores `90`); `compaction_level` and `original_size` are integers always emitted on an exported record | S7 |
| timestamps are rendered with `Z` in exported records and with `+00:00` in some command results; fractional digits follow a 0/3/6/9 rule (`…05Z`, `…05.100Z`, `…05.000000001Z`) | S5, S7 |
| `--sort title` folds only ASCII `A`–`Z`; non-ASCII is compared by raw UTF-8 bytes | S4 |
| `search` matches title and description, case-insensitively (`search_description`, `search_case_insensitive`, `search_unicode`) | S4 |
| labels are validated as alphanumeric, hyphen, underscore and colon (`error_create_bad_label`, `error_label_add_invalid`) | S2 |
| titles are limited to 500 characters (`error_create_long_title` at 501, `edge_create_max_title` at 500) | S2 |
| the `empty` fixture is a zero-byte `issues.jsonl`; `@fx=none` means no `.beads` directory at all | S2 |


## Appendix B. OQ proposals, as the extractors wrote them (registered in docs/OPEN_QUESTIONS.md)

### from docs/spec-parts/S1_S9.md


| question | clause | case to add |
|---|---|---|
| What are the exact bytes and exit code of `br version` and `br version --json` for the port, given that the commit, rust version and target cannot be truthfully reproduced? (OQ-003, still open) | S9.33 | `version_plain`, `version_json` (captured since: `goldens/version_plain.out:1`, `goldens/version_json.out:1`, quoted in S9.33; what remains is a DISC of class Platform stating the port's fields) |
| What happens when `.beads/` exists but `issues.jsonl` does not? Is it an empty store (exit 0) or `NOT_INITIALIZED`/an I/O error? | S9.30, S9.31 | `edge_no_jsonl_list` (a fixture selector that creates `.beads/` with no `issues.jsonl`) |
| Does an argument that is not valid UTF-8 abort inside the argument reader, and with what bytes and exit code (probe RS-20)? | S1.2 | `usage_non_utf8_arg` — argv `["\xff"]` |
| Is the `CLOSE_INCOMPLETE` context really `{"closed","skipped","summary"}`, and in that key order? Only the human rendering was measured. | S9.14 | `error_close_partial_json` — `["@fx=basic","close","proj-mta","proj-mkh","--json"]` |
| Does a clap parse failure with `BR_OUTPUT_FORMAT=json` set still print plain text on stderr, as it does with `--json`? | S1.30, S9.4 | `usage_limit_bad_env_json` (needs a ws-run mode that sets one environment variable) |
| Does `--quiet` suppress the per-item `Warning: Skipped …` lines of S9.26, or only the command's stdout? | S1.8, S9.26 | `close_blocked_quiet` — `["@fx=basic","close","proj-170","--quiet"]` |
| What are the exact bytes when more than five conflict markers are present (the preview is capped at five)? | S9.23 | `error_conflict_markers_many` (a fixture with six or more markers) |
| Does `--actor ""` fall through to the configuration and `USER`, or is the empty string taken as the actor? | S1.64 | `actor_flag_empty` — `["create","X","--actor","","--json"]` |

### from docs/spec-parts/S2_S5a.md


| question | clause | case to add |
|---|---|---|
| OQ-007 is answered by S5.9: `compacted_at_commit` is the only optional string that survives a rewrite as `""`. Confirm against the original rather than against the accessor table | S5.9, S2.38 | `edge_empty_optionals_rewrite` (a fixture with every optional string set to `""`) |
| Does a UTF-8 BOM refuse the load, and with which line number and column? | S2.25 | `error_jsonl_bom` (fixture `bom`) |
| Does a 6-fraction-digit instant exist in the timestamp ladder, i.e. is a microsecond written with exactly six digits? | S5.12 | `edge_precision_micros` (a fixture line with `…05.000001Z`) |
| Does a second line with the same `id` raise the duplicate message on the import path as well as on the `show` fast path, and with which line number? | S2.40 | `error_jsonl_duplicate_id` |
| Is a custom `status` really written back lower-cased, does `inprogress` normalize to `in_progress`, and does `done` really fold to `closed` and gain a `closed_at`? | S2.31, S2.36 | `edge_status_spellings` (a fixture with `InProgress`, `inprogress`, `QaReview`, `done`) |
| Does `BEADS_DIR` bypass the walk, and does a `redirect` file relocate the workspace? Both need a ws-run mode that plants a sidecar or adds one environment variable | S2.4, S2.5 | `env_beads_dir`, `redirect_basic` |
| Does a configured `issue_prefix` that sanitizes to empty abort rather than fall back to `br`? | S2.14 | `error_bad_issue_prefix` (a `config.yaml` fixture sidecar) |
| Does a record missing `title` produce serde's `missing field` wording inside the `Invalid JSON at line <n>` frame, and does a record that parses but fails validation produce the `Validation failed for issue … at line …` frame? | S2.28, S2.30 | `error_jsonl_missing_title`, `error_jsonl_bad_id_format` |
| Does an imported dependency with no `created_by` really come back as `"created_by":"import"`? | S5.14 | `edge_sparse_dependency_rewrite` (a fixture whose dependency carries only the first four members) |

### from docs/spec-parts/S4a_S7.md


| question | clause | case to add |
|---|---|---|
| At which exact issue counts does the id hash length step from 3 to 4 to 5? (OQ-006, still open; S4.9 states the computed table) | S4.9 | `edge_idlen_163` — `["@fx=n163","create","Boundary","--json"]`; `edge_idlen_164` — `["@fx=n164","create","Boundary","--json"]` (generated fixtures `n163`, `n164`) |
| Does the count that chooses the hash length include tombstones in `--no-db`? | S4.10 | `edge_idlen_tombstones` — a fixture of 163 live records plus one tombstone, then `create` |
| Does a tombstoned id block a colliding id candidate? | S4.13 | `edge_id_collides_with_tombstone` — a fixture whose tombstone id equals the nonce-0 candidate for a known title/instant |
| After deleting a child, is its number reused by the next child? | S4.25 | `edge_child_after_delete` — `@scn` deleting `proj-uly.1` then creating another child of `proj-uly` |
| What exactly does `--slug` produce, and what does it do when the slug normalizes to empty or exceeds the prefix budget? | S4.19–S4.23 | `create_slug` — `["create","Slugged","--slug","My Thing!","--json"]`; `create_slug_unicode` — `["create","S","--slug","  Survey — my  thing  ","--json"]`; `create_slug_empty` — `["create","S","--slug","!!!","--json"]` |
| What are the bytes for a wrong-prefix partial id and for an uppercase full id? | S4.36, S4.43 | `show_uppercase_id` — `["@fx=basic","show","PROJ-MTA"]`; `error_show_wrong_prefix` — `["@fx=basic","show","other-mta"]` |
| What is the message for a blank id argument? | S4.40 | `error_show_blank_id` — `["@fx=basic","show","  "]` |
| Which RFC 3339 spellings does `--until` accept, and what does `tomorrow` produce under the pinned clock? | S4.57, S4.60 | `defer_until_rfc3339` — `["@fx=basic","defer","proj-mta","--until","2027-06-01T12:00:00+02:00","--json"]`; `defer_until_tomorrow` — `["@fx=basic","defer","proj-mta","--until","tomorrow","--json"]` |
| What is the field name and message for a bad `create --due`? | S4.62 | `error_create_bad_due` — `["create","X","--due","not-a-date"]` |
| What are the messages for out-of-band estimates? | S4.69 | `error_create_negative_estimate` — `["create","X","-e","-1"]`; `error_create_huge_estimate` — `["create","X","-e","525961"]` |
| What do priority ranges, comma lists and a backwards range print? | S4.67 | `list_priority_range` — `["@fx=basic","list","-p","0-1","--json"]`; `list_priority_list` — `["@fx=basic","list","-p","0,3","--json"]`; `error_list_priority_backwards` — `["@fx=basic","list","-p","3-1"]` |
| What does a non-numeric priority report? (the uppercasing oddity of S4.66) | S4.66, S10 | `error_create_priority_word` — `["create","X","-p","high"]` |

### from docs/spec-parts/S4b.md


| question | clause | case to add |
|---|---|---|
| Does `--status all` include tombstones in `list`, and in what position do they sort? | S4.104 | `list_status_all` — `["@fx=basic","list","--status","all","--json"]` (needs a fixture with a tombstone: `basic_tomb`) |
| Is a custom status carried by a stored issue accepted by `--status`, and is it matched lowercased? | S4.106 | `list_status_custom` — `["@fx=custom_status","list","--status","Triage","--json"]` |
| Does `:parent-blocked` really reach every descendant, and does a grandchild of a blocked ancestor drop out of `ready`? | S4.159 | `blocked_parent_propagation` — a `hierarchy` fixture with `epic → child → grandchild` plus a `blocks` edge on the epic; run `blocked --json` and `ready` |
| What does a real cycle look like — the witness node order, the numbering, and the exit code? | S4.187, S4.188 | `dep_cycles_found` / `dep_cycles_found_json` on a `cycles` fixture with `a —blocks→ b —blocks→ c —blocks→ a` |
| How does `dep tree` render a diamond (shared node) and a cycle: which occurrence carries `repeat`, which carries `truncated`? | S4.185 | `dep_tree_shared` and `dep_tree_cycle` on a `diamond` / `cycles` fixture |
| Does `--max-depth` cut where S4.185 says, and does the cut node carry `truncated` true? | S4.185 | `dep_tree_max_depth` — `["@fx=hierarchy","dep","tree","<root>","--max-depth","1","--json"]` |
| Does an `external:` blocking edge really block its source with no external project configured, and what does `blocked` print for it? | S4.166 | `blocked_external` on a fixture carrying `proj-mta —blocks→ external:other-1` |
| What is the third empty-`ready` message, verbatim, on a store where every open issue is blocked? | S4.178 | `ready_none_actionable` on a fixture where the only open issue has an open `blocks` prerequisite |
| Are templates really invisible to `list`/`ready`/`count`, and does `count --include-templates` bring them back? | S4.103 | `list_template_hidden` and `count_include_templates` on a fixture with `"is_template":true` |
| Do `--desc-contains`/`--notes-contains` fold non-ASCII case (Unicode lowercasing) while `--title-contains` does not? | S4.129, S4.109 | `list_desc_contains_unicode` — `["@fx=basic","list","--desc-contains","LINE ONE"]` and `["@fx=basic","list","--title-contains","ünï"]` |
| Which sibling key decides first in `dep tree` — priority or status rank? | S4.184 | `dep_tree_siblings` on a fixture where one node has two dependencies differing in priority and in status |
| Does `epic status` list a *deferred* epic (include_deferred true) and hide a closed one? | S4.189 | `epic_status_deferred` on a fixture with a deferred epic and a closed epic |

### from docs/spec-parts/S4c.md


| question | clause | case to add |
|---|---|---|
| Does `create -f/--file` bulk import write `last-touched` with the last created id, and what are the exact plain and JSON output shapes and the per-issue defaults? | S4.237 | `create_from_file` (a markdown file staged next to the fixture; needs a ws-run mode that can place extra input files) |
| What exactly does `delete --from-file` accept, and does the sorted-id rule change which id is reported first? | S4.340 | `delete_from_file` (same staging need) |
| Does `epic close-eligible` write `last-touched`, and does an eligible epic really receive `close_reason` `All children completed` rather than `done`? | S4.391 | `scn_epic_close_eligible` — a scenario on `@fx=basic`: `["close","proj-170"]`, `["epic","close-eligible","--json"]`, `["epic","status"]` |
| Does `label add <issue> <id-shaped-label>` really treat the last positional as a label, and what happens when a label is id-shaped and `-l` is absent? | S4.370 | `label_add_id_shaped` — `["@fx=basic","label","add","proj-mta","needs-review"]` |
| Does `comments add -f <file>` keep a trailing newline in the stored `text`? | S4.381 | `comments_add_file` (needs a fixture-side file) |
| Is `close`'s `Partially applied` exit code 3 stable for every mix of skip kinds, and does the store always hold the successful closures? | S4.294 | `close_partial_skip` — `["@fx=basic","close","proj-mkh","proj-mta"]` |
| Does `update --status <same non-terminal value>` really rewrite the store on every status, or only on some? | S4.262 | `update_status_same` — `["@fx=basic","update","proj-mta","--status","open"]` |
| What does `--agent-context` store byte-for-byte after JSON re-serialization (key order, number formatting), and what is the verbatim refusal for invalid inline JSON? | S4.232 | `create_agent_context`, `error_create_agent_context` |
| What is the exact normalization of `--slug` at the 48-character cap and for a slug that normalizes to the empty string? | S4.233 | `create_slug`, `create_slug_empty` — `["create","X","--slug","!!!","--json"]` |
| Does `delete --hard` with no ids purge tombstones in a store that has some, and what is the resulting byte content? | S4.339 | `delete_hard_all` on a fixture containing a tombstone |
| Which comment id does a store with NO comments assign, and does the sequence survive a rewrite that renumbers nothing? | S4.382 | `comments_add_first` — `["@fx=empty"…]` is not possible (no issue); a scenario `create` then `comments add` |
| Does `update --parent <id>` replace an existing parent edge or add a second one when the single-parent rule is bypassed by the update path? | S4.270 | `update_reparent` — `["@fx=basic","update","proj-170","--parent","proj-b75","--json"]` |

### from docs/spec-parts/S5b.md


| question | clause | case to add |
|---|---|---|
| Is a long title truncated in plain mode when stdout is a pipe? (source says no; no case proves it) | S5.114 | `list_long_title` — `["@fx=longtitle","list"]` on a fixture with a 300-column title |
| At what width does `br show` wrap a free-text body when `COLUMNS` is unset? (source says 100) | S5.115 | `show_wrap_100` — `["@fx=longdesc","show","proj-aaa"]` |
| What are the exact bytes of `dep tree` at depth ≥ 1, and of the `(truncated)` / `(shown above)` prefixes? | S5.179 | `dep_tree_children` — `["@scn=child_ids"]` extended with `["dep","tree","proj-uly","--direction","up"]` |
| What does a plain `close --suggest-next` print, and is the `Unblocked` block preceded by exactly one blank line? | S5.225 | `close_suggest_next_plain` — `["@fx=basic","close","proj-mta","--suggest-next"]` |
| Does a mixed batch (one closed, one skipped) print the `✓` line on stdout and still exit non-zero? | S5.224 | `close_mixed` — `["@fx=basic","close","proj-170","proj-mta"]` |
| Does `br reopen --quiet` really print, and does `br create --silent --quiet` print the id? | S5.108 | `reopen_quiet`, `create_silent_quiet` |
| What are the bytes of a quiet mutation (stdout, stderr, and the store dump)? | S5.107 | `update_quiet` — `["@fx=basic","update","proj-mta","-p","0","--quiet"]` |
| Which stream carries the `list` truncation note and which of the two wordings applies to a plain `--limit`? | S5.124 | `list_limit_plain` — `["@fx=basic","list","--limit","2"]` |
| Does the `search` truncation note really land on stdout, and what is the hidden-closed note's exact text? | S5.159, S5.160 | `search_limit`, `search_hidden_closed` |
| What is the plain form of `defer --until` — RFC 3339 with `+00:00`, or a date? | S5.230 | `defer_until_plain` — `["@fx=basic","defer","proj-mta","--until","2027-06-01"]` |
| What are the plain forms of `undefer`, `label rename`, `update --title`, `update` no-op and `label list` on an unlabeled issue? | S5.232, S5.189, S5.216, S5.218, S5.186 | `undefer_plain`, `label_rename_plain`, `update_title_plain`, `update_noop_plain`, `label_list_empty` |
| What does `dep cycles` print when a cycle exists (stdout rows vs the stderr warning)? | S5.182 | `dep_cycles_found` — `["@fx=cycle","dep","cycles"]` on a fixture containing a cycle |
| What does `ready` print when filters exclude everything, versus when everything is blocked (three different `✨` lines)? | S5.133 | `ready_filtered_empty`, `ready_all_blocked_empty` |
| What is the countdown suffix on a plain `show` of a deferred issue under the pinned clock? | S5.148 | `show_deferred_plain` — `["@fx=basic","show","proj-7vm"]` |
| Does `blocked --limit` really print a header count larger than the row count? | S5.138 | `blocked_limit` — `["@fx=basic","blocked","--limit","1"]` |

### from docs/spec-parts/S6_S8_S10.md


| question | clause | case to add |
|---|---|---|
| Does the JSONL export order use raw byte order for ids containing `.` (child ids sort `proj-uly` before `proj-uly.1` before `proj-ulz`)? | S6.2 | `edge_child_id_export_order` — a fixture holding `proj-uly`, `proj-uly.1`, `proj-uly.10`, `proj-uly.2`, `proj-ulz`, then any mutation |
| Do `dep list --json` and `dep list` really disagree, and by which key? | S6.25, S10.35 | `edge_dep_list_json_vs_text` — a fixture with three same-priority dependencies whose `created_at` order differs from their id order, run both ways |
| What order does `dep cycles` give when two cycles share a member, and what does `--include-closed` change? | S6.29 | `edge_dep_cycles_two` and `edge_dep_cycles_include_closed` |
| Does `blocked` order by blocker count before or after `created_at` when priorities are equal? | S6.15 | `edge_blocked_count_tiebreak` — two P4 issues blocked by two and one open issues |
| Is the `--sort title` fold ASCII-only for every command, including `search` with an offset (which takes a different code path)? | S6.10, S6.17 | `edge_title_fold_ascii_only` — a fixture with `İstanbul match`, `jackal match`, `hotel match`, `ZULU match`, `apple match`, run as `list --sort title`, `search match --sort title` and `list --sort title --offset 1` |
| Exactly which files does a mutating run leave in `.beads/`, and does a read-only run ever create one? | S8.27, S8.28, S8.29 | a ws-run mode that lists `.beads/` recursively after the step, for both a read-only and a mutating argv (extends OQ-008) |
| What does `BEADS_DIR` do to `where`'s output, and why does the `prefix:` line disappear? | S8.2, S10.49 | `env_beads_dir_where` — needs a ws-run mode that sets one extra environment variable |
| What does `BR_OUTPUT_FORMAT=json` change for a command that already has a `--json` flag, and for one that does not? | S8.7 | `env_output_format_json` on `count` and on `where` |
| Does `BR_DISABLE_PARALLEL_JSONL_EXPORT` change the exported bytes on a large store? | S8.10 | `env_serial_export` on a generated 1000-issue fixture |
| Which per-field rule decides whether an empty optional survives a rewrite? | S10.1 | `edge_empty_optionals_rewrite` (already OQ-007) |
| Does `close`'s `closed_at` differ from `updated_at` under a clock the harness can move between the two readings? | S10.45, S8.31 | none is capturable through the pinned harness; recorded as a clock-seam note for DISC-001 |
| Is the average lead time truncated per issue before averaging, and in which direction? | S6.34, S10.41 | `edge_stats_lead_time_truncation` — three closed issues with lead times of 1 s, 79 h 13 m 11 s and 35 h 22 m 33 s |
| Does a plain-mode closed pipe kill the process with status `141` while a `--json` run exits `0`? | S8.37 | `edge_sigpipe_plain` — needs a harness mode that closes stdout after the first line |
